# Phase 6.0 Critic Pre-Flight — iter-v1/084 (CRVUSDT SPECIALIST)

OVERALL: PASS

Reformed negative-baseline selector is methodologically sound; `oi_price_divergence_30` is look-ahead-free and correctly LOCAL (global `V1_FEATURE_COLUMNS_PRUNED` untouched at 48); R-FADE is stateless, CRV-scoped, default-OFF, `fade_z=2.0` frozen with anti-tuning assertion; foundation embargo fix intact; EDA IS-only. Two non-blocking forward-requirements documented for QE (R-FADE-OFF control run + stale prose).

## Iteration Type (from Brief Section 0.5)
TYPE: SPECIALIST — single-coin cohort `("CRVUSDT",)`, NEW SYMBOL universe-extension; 5th BUNDLE-003 candidate; first under the REFORMED negative-baseline selector after /083 falsified the narrative selector.

## Pre-Flight Checks

### Check 1 (mini) — Brief Look-Ahead Audit: PASS
Traced `add_oi_price_divergence_30_feature` (`src/crypto_trade/features_v1/open_interest_v1.py:419-553`) line by line. All components are past-only: `oi_delta_30 = pct_change(30)` (bar t vs t-30), `ret_30 = log(close).diff(30)` (past-only), `div_raw[t] = sign(oi_delta_30[t]) - sign(ret_30[t])` (both legs past-derived). The z-score uses `s_shifted = div_raw.shift(1)` for the rolling mean/std (window sees t-90..t-1, NOT t), and the final output `zscore_final = zscore_raw.shift(1)` shifts the published value by one more bar so bar t carries `zscore_raw[t-1]` — built from `div_raw[t-1]` and stats over t-91..t-2, strictly ≤ t-1. Double-shift is conservative but correct; no contemporaneous-or-future leakage. OI merge is exact-int left-join on `open_time` (cannot pull future). The reformed-selector EDA (`eda.py`, `eda_crv_deep.py`, `eda_oi_fade_calib.py`) all enforce `open_time < OOS_CUTOFF_MS` with runtime leak assertions (`eda.py:47 assert df["open_time"].max() < OOS_CUTOFF_MS`; `eda_oi_fade_calib.py:80 assert ... == 0 "OOS leak!"`). The `.std()` calls in the EDA (eda.py:88, eda_crv_deep.py:147) are past-only rolling-vol profiles on already-IS-filtered frames — NOT forward-window label std (A2 clean). The R-FADE `fade_z=2.0` calibration is IS-only (Section 2.5-evidence). NOTE: the brief is commendably honest that the IS calibration is non-monotone and thin (only the 2.0-2.5 bucket shows DISAGREE underperformance at -35.2 bps on 40 bars) — this is correctly pre-registered as the dominant INERT/over-veto risk (F3), not buried.

### Check 13 (mini) — Anti-Pattern Static Scan on QE's src/ diff: PASS
Scanned the /084 src/ surface (`open_interest_v1.py` feature fn, `lgbm.py` R-FADE gate, `features_v1/__init__.py` constants, `run_iteration_084.py`, `run_baseline_v1.py:7637-7814` dispatch):
- A1 (train_end_ms = test_start_ms without subtraction): N/A to diff; foundation re-verified separately (PASS below).
- A2 (forward-window σ_t): no `returns[t:t+timeout].std()` pattern; feature z-score is past-only-shifted; EDA std is rolling past-only. CLEAN.
- A3 (fit_transform on combined train+test): no `fit_transform`/`StandardScaler`/`MinMaxScaler` in the diff — LightGBM is scale-invariant, no preprocessing introduced. CLEAN.
- A4 (universe survivorship): `V1_ITER084_UNIVERSE = ("CRVUSDT",)` is a static literal; selection is via committed IS-only trivial-momentum sweep (Section 2.1), not post-hoc volume filter. CLEAN.
- A12 (DSR/PSR wrong-granularity): N/A — DSR/PBO/PSR not computed at SPECIALIST EXPLORATION layer (Section 9), deferred to CONFIRMATION.
- A13 (report-file read-before-write): the comparison.csv append (run_baseline_v1.py:9102-9122) writes specialist_dispersion_mean + oi_divergence_fade_gate_count AFTER the backtest produces them; no read-before-write. CLEAN.
- Track isolation (Check 10): `grep "from crypto_trade.features_v2/v3" src/crypto_trade/features_v1/` returns only docstring grep-instructions, ZERO actual imports. CLEAN.
- R-FADE default OFF: `enable_oi_divergence_fade_gate: bool = False` (lgbm.py:255) → bit-identical to all prior cohorts when not explicitly enabled; True ONLY in the CRV cell (run_baseline_v1.py:7759). The gate is stateless (reads `feat_row[col_idx]`, no persistent state — no A8 deadlock risk), post-aggregator (lgbm.py:2157, fires AFTER mid-bull veto at 2135), NaN/inf → pass-through (conservative). Parity-clean: reads only the past-only feature value available in both backtest and live.
No unexplained anti-pattern matches.

### Foundation Regression: PASS
`src/crypto_trade/strategies/ml/walk_forward.py:113` carries `train_end_ms = test_start_ms - embargo_ms  # purge labels that would peek into test` — the iter-v3/057 fix (commit `5566a69`) is INTACT, not regressed by QE's commits. A1 grep on walk_forward.py returns only the math-expression form with the subtraction present. Regression-test coverage confirmed: `tests/test_lookahead_embargo.py` contains all 4 mandated tests — `test_labels_are_invariant_to_master_data_extent` (L120), `test_demonstrates_bug_without_embargo` (L171), `test_walk_forward_embargo_matches_cv_gap_formula` (L277), `test_time_series_split_with_gap_excludes_correct_rows` (L248). QE's commits touch only the /084-local feature/gate/runner surface; foundation untouched.

### Architecture Isolation (LOCAL-vs-GLOBAL diagnosis — the user's flagged concern): PASS
CONFIRMED the feature is LOCAL, not global. Diagnosis: `V1_FEATURE_COLUMNS_PRUNED` (features_v1/__init__.py:89-147) is 48 columns with a hard `assert len == 48` at line 213. The /084 feature lives ONLY in `V1_ITER084_FEATURE_COLUMNS = V1_FEATURE_COLUMNS_PRUNED + ("oi_price_divergence_30",)` = 49, asserted `== 49` at line 225. The dispatch branch (run_baseline_v1.py:7637) explicitly OVERRIDES `active_feature_columns = list(V1_ITER084_FEATURE_COLUMNS)` (line 7684) AFTER `--pruned-features` would have set the global 48-col, with guards `len == 49` (7685) and `"oi_price_divergence_30" in active_feature_columns` (7691). The runner (run_iteration_084.py:236-249) double-asserts global=48 AND local=49 at pre-flight. The DOT/063, ETH/064, BTC/065, AAVE/078 specialists are therefore UNAFFECTED — they never see column 49. The one-variable-at-a-time discipline holds. This matches the resolved test-fix: count-guards correctly stay at 48 (PASS as-is) because the feature is LOCAL. The prior BLOCK ("global modified to 49") was the REAL bug and was correctly reverted.

### Methodology Constants: PASS
All constants HELD at the dispatch (run_baseline_v1.py:7711-7762): `training_months=24`, `bounds_profile="v1_specialist"` (max_depth=5 / num_leaves=31 FIXED), `specialist_mode=True`, V1_SPECIALIST_SEED_COUNT=50 (seeds 42..91, asserted 42 and 91 at runner L254/258), V1_SPECIALIST_OPTUNA_TRIALS=30, n_estimators_max=500, n_startup_trials=10, R1=OFF, R2=OFF, R3=ON cutoff=0.70 SHARED, R5=ON vt_target_vol=0.3 lookback=45 min_scale=0.33, atr_tp=2.9/atr_sl=1.45 (Model A ETH cell), mean-of-signed-weights aggregator. `OI_DIVERGENCE_FADE_Z == 2.0` anti-tuning assertion present at runner L263 and dispatch L7760. OOS_CUTOFF_MS=1742774400000 sacred. Pairwise-disjoint: CRVUSDT ∉ {BTC,ETH,DOT,AAVE} ∪ V1_EXCLUDED_SYMBOLS ∪ {LINK,LTC,ATOM,ICP,FIL} — PASS by construction; per-cohort isolation guard `_e084_symbols.issubset({"CRVUSDT"})` at L7777.

### Cadence + Axis Sanity: PASS
Phase 5.5 gate OVERALL=PASS confirmed. Section 0.6 declares axis family `per-cohort-specialization-CRV` under the cycle-6/7 per-symbol regime-specialist mandate (5-family rotation SUSPENDED per `feedback_v1_cycle6_per_symbol_regime_specialist_mandate.md`); rotation status VALID (cohort-orthogonality, symbol-disjoint). Wall-clock: 2h EXPLORATION default with honest ~5.5-8h methodology-lock note matching /078, /083 SPECIALIST precedent (no mid-run kill-switch by design). Reformed-selector soundness: the QR is transparently HONEST that CRV's +0.069 min-horizon trivial Sharpe is BARELY-positive (near-zero), NOT genuinely negative, and headroom is regime-localized (bear -0.924, chop -0.055, bull +2.570; 84% of IS bars in bear+chop). This is the empirically-validated inverse of the FIL/083 positive-baseline trap (+1.45 → no ML headroom). LM Master MEDIUM confidence (modal IS +0.55) is correctly downgraded from the directive's "most-negative" framing — no ≥4y candidate had a genuinely negative min-horizon baseline; CRV is the best available. The reform is honestly characterized as MEDIUM not HIGH headroom.

### Falsifier Presence: PASS
Section 4 has explicit, frozen, pre-registered OOS-and-IS falsifiers. The hypothesis-rejection threshold is HARD (Section 4.1): "the PRIMARY hypothesis is REJECTED if the OOS Sharpe falls below +0.0 with adequate trades (F1-pass), OR if ANY of F1-F4 is breached." Four HARD falsifiers: F1 (≥50 IS AND ≥50 OOS trades), F2 (feature importance ≥30 / rank <40 in >50% months), F3 (R-FADE mechanically inert OR destroys trade count below the OOS floor), F4 (ML IS Sharpe must exceed BOTH trivial +0.069 min-horizon AND +0.293 21d — the reformed momentum-dominated bar). Plus FB1 (BUNDLE-layer pred-corr <0.50, inherited at assembly). Verdict matrix (Section 4.3) and absolute hard-gate table (Section 8.2) are mechanical and pre-registered. F4 correctly notes a fail at a reformed-selected symbol would be reform-falsifying (a critical finding), not merely a symbol rejection.

## Non-Blocking Forward-Requirements for QE (Phase 6) — NOT pre-flight BLOCKs

1. **R-FADE-OFF control run is a pre-registered Phase 6 deliverable the current runner cannot yet produce.** Brief Section 4 F3 (L264) and the Kill-Switch Criteria (L406) pre-register TWO CRV runs (R-FADE-ON and R-FADE-OFF) as a HARD requirement so F3's marginal R-FADE effect is isolable; brief L406 makes a missing control run a BLOCK-PENDING-FIX condition at Phase 7.5. However `run_iteration_084.py` launches only the single R-FADE-ON cell (`enable_oi_divergence_fade_gate=True`), with no `--rfade-off` flag or second dispatch, and `run_baseline_v1.py:7759` hardcodes the gate ON. This is correctly NOT a Phase 6.0 BLOCK (the control run is a Phase 6 backtest artifact, not a pre-flight artifact, and the brief has correctly pre-registered the obligation). But QE MUST add the R-FADE-OFF control cell at Phase 6 — otherwise F3 is unverifiable and Phase 7.5 fires BLOCK-PENDING-FIX per the brief's own kill-switch. Flagging now so it is not discovered at Phase 7.5 after compute is spent. The `oi_divergence_fade_gate_count` written to comparison.csv (run_baseline_v1.py:9122) is necessary but NOT sufficient — a zero count alone cannot distinguish "INERT" (branch a) from "the ON-run roster already cleared 50 without the gate ever firing" without the OFF-run roster to diff against.

2. **Stale / self-contradictory prose: brief Section 3.2 + runner docstring + dispatch comment all say "add to V1_FEATURE_COLUMNS_PRUNED (48→49)" — the CODE is correct, the PROSE is the laggard.** Brief Section 3.2 ("ADD exactly one column to `V1_FEATURE_COLUMNS_PRUNED` (48 → 49); verified `len == 49`"), run_iteration_084.py:302-303 ("The `--pruned-features` flag selects V1_FEATURE_COLUMNS_PRUNED (49 cols)"), and run_baseline_v1.py:7644/7671/7704 ("added to V1_FEATURE_COLUMNS_PRUNED (48 → 49)" / "features=V1_FEATURE_COLUMNS_PRUNED (49 cols)") all contradict the actual implementation, where the GLOBAL PRUNED stays at 48 and only the LOCAL `V1_ITER084_FEATURE_COLUMNS` is 49. The runtime behavior is CORRECT (the dispatch override at L7684 + the global-48 assertion at L213/236 guarantee it), so this is a documentation-integrity defect, not a code bug — hence not a BLOCK. But it is precisely the kind of prose that caused the prior Phase 5.5 BLOCK (someone read "add to PRUNED" and modified the global). QR/QE should correct the prose to read "V1_ITER084_FEATURE_COLUMNS (LOCAL, 49)" everywhere so the next reader is not misled into re-introducing the global-modification bug. The `--pruned-features` flag in the injected argv (runner L316) is technically a no-op for /084 since the dispatch immediately overrides `active_feature_columns` — harmless but worth noting it does NOT mean "use the 49-col set" as the comment claims.

## Notes (no action required)
- R-FADE veto logic (lgbm.py:1967-1969) matches brief Section 3.3 exactly: veto LONG when `oi_div < -fade_z`, veto SHORT when `oi_div > +fade_z`. The sign-convention prose ("bearish OI tilt") is loose but the directional logic is internally consistent and frozen against the IS-calibrated DISAGREE bucket.
- Attribution entanglement (NEW feature + NEW risk on a NEW symbol) is acknowledged in Section 2.5 with a hardwired per-axis attribution plan (R-FADE-OFF control isolates the gate; importance rank isolates the feature; F4 TS-mom-beat isolates the symbol). This is the correct discipline given the stacked axes; the control run (forward-requirement 1) is the load-bearing diagnostic.
