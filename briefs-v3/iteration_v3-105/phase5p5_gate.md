# Phase 5.5 Gate — iter-v3/105

OVERALL: PASS

## Per-Section Status

- Section 0 (Data Split): PASS — `OOS_CUTOFF_DATE = 2025-03-24`, `training_months = 24` explicitly declared IMMUTABLE. IS window (24 months prior to cutoff) and OOS window (post-cutoff) named. Walk-forward embargo fix `e149e9d` inherited unchanged. All EDA strictly IS-only (`open_time < OOS_CUTOFF_MS`) confirmed.
- Section 1 (Hypothesis): PASS — One specific, testable sentence: "Replacing the fixed 21-candle triple-barrier training label with a trend-scanning label produces a label the existing 14-feature stack predicts materially better (+52% aggregate feature-label IC, capped grid +31%), and that better-predicted estimand lifts the OOS monthly Sharpe vs the /060 anchor without collapsing the IS fit." References the specific structural lever (label geometry / estimand), the specific metric to move (feature-label IC ratio), and a specific mechanism. Not vague.
- Section 2 (IS-Only Evidence): PASS — Three committed EDA scripts at `analysis/iteration_v3-105/` (EDA commit `857e176`): `trend_scanning_gating_eda.py`, `trend_scanning_robustness.py`, `trend_scanning_grid_sensitivity.py`. Ten output CSVs (T1–T5, R1–R3, G1–G2). Numerical tables present for: selected-horizon distribution (T1, entropy 0.860–0.881); feature-label IC comparison triple-barrier vs trend-scanning on same shared-support IS rows (T2, aggregate ratio 1.52 gating / 1.31 capped grid); quartile sign-stability (T3); label balance (T4, 46–60% long, covering full IS panel with no abstention); screen verdict (T5). Adversarial probe (longest-wins degeneracy, R1) executed and the F-IC lift checked under capped-grid removing the confound (G1/G2, Wilcoxon p=0.013, 25/42 cells improve). The incumbent label is reconstructed with the exact production `label_trades("triple_barrier", use_atr=True, atr_tp=2.0, atr_sl=1.0, timeout=21 candles, fee_pct=0.1)` rule — apples-to-apples on shared-support IS rows. Category-matching absent; all evidence is numerical and committed.
- Section 3 (Proposed Changes): PASS — Three enumerated changes: (1) new `label_mode="trend_scanning"` branch in `label_trades` with closed-form OLS slope + t-stat, max-|t| horizon selection from capped grid {5,8,13,21}, realized-return PnL convention; (2) `trend_scan_grid` parameter plumbed through `LightGbmStrategy`; (3) runner changes (`label_mode="trend_scanning"`, `trend_scan_grid=(5,8,13,21)`, `ITERATION_LABEL="v3-105"`). What is NOT changed is explicitly enumerated: 14 features, BCH/LDO/TRX universe, triple-barrier TP/SL EXITS, 7-gate RiskV2, REQUIRED_GAP=66, embargo=22.
- Section 4 (Expected OOS Impact): PASS — Predicted OOS monthly Sharpe ≈ +0.45, band [+0.10, +0.95] vs anchor /060 +0.1403. Predicted IS monthly Sharpe ≈ +0.85, band [+0.55, +1.15]. Six pre-registered numerical falsifiers (F1–F6) with explicit thresholds: F1 OOS < 0.00, F2 IS < +0.60, F3 BCH-only with both LDO+TRX OOS regression, F4 OOS trade count < 60, F5 mean-duration gap > +1.0 candles OR IS/OOS daily-Sharpe ratio outside [0.2, 5], F6 OOS roster change < 10%. Behavioral-effect predictor present: 25–60% OOS roster change predicted; < 10% change triggers F6. Anchor correctly identified as /060 3-seed EXPLORATION-mode, NOT /059 CONFIRMATION.
- Section 5 (Risk Mitigation): PASS — The single-variable label-geometry change is classified as introducing NO new live risk gate. The /059 R1-R5 stack is inherited unchanged with explicit reasoning for each: R1 cooldown keys off realized SL in the EXECUTION layer (unchanged); R2 7-gate RiskV2 operates on emitted signal + market features (neither touched by the label at inference time); R3 OOD z-score operates on its own feature subset (no feature added); R4 BTC-trend kill unchanged; R5 per-symbol drawdown brake CLOSED per /020/054. Simulated historical effect stated as the EDA itself (T2/T3/T4 are the IS simulation). No IS-calibrated threshold change needed because no gate is changed.
- Section 6 (Risk Management Design): PASS — Two-layer structural defense. Layer 1: minimal blast radius (one new branch in `label_trades`, one plumbed parameter, two runner lines; one-line revert; the `trend_scanning` branch is retained as dead code if reverted, per `fixed_horizon`/`formulaic_v3.py` precedent). Layer 2: the GO is not a single number — it survived the longest-wins adversarial probe. Fail-fast EDA philosophy described. The structural argument (every input-side lever and estimator-side lever has failed; /105 is the first estimand-side attack) is articulated. The meta-prediction if the backtest fails is also stated (exit layer is the binding constraint, not the label).
- Section 7 (Failure-Mode Prediction): PASS — Four probability-ordered failure modes: (1) EXPLORATION-PROMISING (modal prediction, EDA positive and adversarially robust); (2) EXPLORATION-NULL-RESULT or SUSPICIOUS (unchanged exit layer caps realized benefit); (3) EXPLORATION-NEGATIVE via F1/F2/F3 (lower-probability, IS-collapse specifically predicted unlikely because no feature added, Optuna search space unchanged); (4) EXPLORATION-INERT via F6 (least likely — training target differs on substantial fraction of bars). The "IS-collapse is specifically absent" reasoning is tight: /102 collapsed IS by widening Optuna search space with a 15th feature; /105 adds NO feature and does NOT widen the search space. That is a valid mechanistic distinction.
- Section 8 (MERGE/NO-MERGE Criteria): PASS — Eight disjunctive precedence-ordered classifications with locked numerical thresholds: BLOCKED (Critic OVERALL=BLOCK), NEGATIVE (F1/F2/F3), SUSPICIOUS (F5), INERT (F6), NULL-RESULT (OOS improvement < +0.20 vs /060 anchor), PROMISING (all falsifiers clear AND OOS Δ ≥ +0.20 AND IS ≥ +0.60 AND sign-consistent per symbol). BASELINE_V3.md untouched by any EXPLORATION outcome. Section 8 is pre-registered and disjunctive (first-match-wins prevents post-hoc reclassification). Anchor is /060 throughout.
- Section 9 (Library Stack): PASS — No new library introduced. Trend-scanning uses only `numpy` closed-form OLS (faster than statsmodels, no external API surface). Pinned versions inherited from /059: Python 3.13, lightgbm 4.6.0, optuna 4.8.0, numpy 2.2.6, pandas 3.0.0, scikit-learn 1.8.0, scipy 1.17.0, statsmodels 0.14.6, pyarrow 23.0.1. Integration-test mandate spelled out: (1) unit tests in `tests/strategies/ml/test_trend_scanning_label_mode.py`; (2) integration smoke test one-cell LightGbmStrategy train; (3) Phase-6 pre-flight checks (ITERATION_LABEL, len(V3_FEATURE_COLUMNS), label_mode reaches label_trades, REQUIRED_GAP=66, embargo=22). End-to-end smoke test before full backtest specified.
- Section 10 (QR Audit Trail): PASS — Axis origin is iter-v3/104 closeout Recommendation #1 (TOP), not an orchestrator pick. EDA commit SHA `857e176` recorded (3 scripts + 10 result CSVs). The apples-to-apples discipline for the IC comparison is explicit. The GO vs /103/104 distinction is documented (positive and adversarially-robust IS-predictive evidence vs conclusive negative verdicts). Setup commit SHA is flagged as "backfilled by orchestrator at phase 5.5 gate" — acceptable per the established brief pattern (cf. iter-v3/047 commit `2b996fd`). NO CHEATING statement present.

## Gate Notes

One item flagged for Phase-6 verification (not a BLOCK — the brief is explicit on the mechanism):

- The trend-scanning label's `max(grid) = 21` candles is identical to the incumbent triple-barrier timeout, so REQUIRED_GAP=66 and embargo=22 are unchanged by design. The Phase-6 test suite MUST include a hard label-leakage assertion confirming `max(grid)` never exceeds 21 at runtime (brief Section 9 integration-test mandate item 3 + Section 3 "hard causality" test). The Engineer implements this as a named hard test (required by the Section 9 mandate and the `feedback_v3_methodology_axis_integration_test.md` rule).

## Reasons (if BLOCK)

None — OVERALL=PASS.
