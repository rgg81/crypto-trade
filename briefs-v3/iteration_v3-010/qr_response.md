# QR Response to Critic — iter-v3/010

## Clarifications

### Clarification 1 — Mechanism attribution for the IS Sharpe overshoot

QR endorses **mechanism (c) — tighter labels reduce label noise so features find real momentum more cleanly** as the dominant explanation for the +0.488 absolute IS Sharpe lift. Reasoning: (a) the lift is broad-based at the per-symbol IS level — 3 of 4 symbols turned positive (BCH +16.02, LDO +56.69, TRX +19.49) rather than being driven by a single-symbol feature exploit, ruling out (b) regime-specific tuning; (b) the methodology axis improved (PBO 0.1145 → 0.1077) on a strictly tighter label distribution with zero new feature/model/risk-gate code, which is structurally inconsistent with (a) — if the (2.9, 1.45) calibration had merely "hidden" pre-existing edge, we would expect PBO to be neutral or slightly worse, not better; (c) the same 2:1 TP:SL ratio was preserved, so the only mechanical change is the absolute barrier tightness, which compresses the noise bandwidth around each label without rotating directional structure. The catalog row will cite mechanism (c); QR explicitly defers the per-exit-type breakdown analysis Critic suggested — running it inside iter-v3/010 would expand scope beyond the labeling axis, and a future axis-diagnostic EXPLORATION (e.g., scanning (1.5, 0.75) or (2.5, 1.25)) is the right vehicle if CONFIRMATION-bundle planning needs sharper attribution.

### Clarification 2 — MKR structural failure across three iterations

REGISTER AND CONTINUE — three iterations is enough to flag a pattern in the catalog but not yet a per-symbol-exclusion trigger. Per the cadence rule (single-axis variation per EXPLORATION), per-symbol exclusion is itself a NEW axis (universe change) that would consume a future EXPLORATION slot, and the project memory `feedback_insist_on_symbols` rule explicitly says "don't reject candidates after one Gate 3 fail; run feature importance, labeling, and lookback analysis first" — feature engineering is the primary tool, not symbol-swapping. QR pre-commits the threshold: **if MKR remains net-negative across the next 3-4 EXPLORATIONs (i.e., 6-7 consecutive negatives total), THEN a per-symbol diagnostic EXPLORATION (drop MKR as a single-axis universe variation) becomes warranted**; until then, the catalog row records `MKR_OOS=-10.6%` with note "3rd consecutive negative; revisit at 6-7 if pattern persists" so future QRs inherit the audit trail without re-deriving the rule.

### Clarification 3 — OOS trade-rate floor implications for downstream CONFIRMATION

QR pre-commits to **option (c) — the floor is satisfied at the BUNDLE level, not per EXPLORATION row**. Reasoning: the 130-OOS-trade / 10-trade-per-month threshold (`feedback_trade_rate_floor`) is a MERGE-grade confidence requirement; CONFIRMATION's job is to test the bundled evidence base, and at `--seeds 2` (or 5) with full `n_trials=50` and `ensemble_size=5`, trade volume should multiply roughly 3-4× per EXPLORATION row via the inner ensemble averaging effect (each seed's ensemble produces independent trade decisions on the same candle), so a 109-trade EXPLORATION row plausibly contributes ~350-450 trades at CONFIRMATION density. Pre-commit (NOW, in iter-v3/010 catalog row): floor applies at the BUNDLE level — if CONFIRMATION-time bundled trades < 130, the CONFIRMATION's MERGE evaluation legitimately fails the edge axis (`OOS_trades_total < 130`); each EXPLORATION row need only be counted, not floor-cleared, for inclusion. QR will add this as a process feedback rule to memory (`feedback_trade_rate_floor_bundle_level`) so the next CONFIRMATION QR doesn't reinvent the rule under post-hoc pressure.

### Clarification 4 — Per-cell PBO outliers above 0.9

YES, record `n_high_pbo_cells = 5/175` (TRX/2025-10, TRX/2025-11, MKR/2024-10 or BCH/2024-10, MKR/2025-04, MKR/2025-07) in the iter-v3/010 catalog row alongside the headline `PBO_mean = 0.1077`. These cells are real evidence of structural instability in those (symbol, train_month) combinations that the cross-cell mean dilutes; future CONFIRMATION QRs can use `(1 - max_per_cell_pbo)` as a more conservative weighting alongside or instead of `(1 - mean_pbo)` when scoring bundle evidence — the catalog row enables that without forcing a methodology change at iter-v3/010.

## Position

**STAND BY VERDICT** with request that Round 2 final verdict resolve to `EXPLORATION-PROMISING with caveats`.

QR accepts all of Critic's preliminary findings (Checks 1-12 PASS / WARN-carry-forward / WAIVED-single-seed) and asks Round 2 to honor the favorable-direction calibration miss as PROMISING — Falsifier 1 was NOT triggered (IS=+0.5683 >> +0.10), all Section 7 falsifiers NOT triggered, methodology axes ALL PASS, the IS overshoot is in the favorable direction, OOS Sharpe +1.8122 is the highest in the v3 track, and 3 of 4 symbols are OOS-positive. The catalog row must record the four caveats — mechanism (c), MKR pattern flag (3rd consecutive negative), `n_high_pbo_cells=5/175`, and `OOS_trade_rate=8.07/month < 10/month floor (informational at EXPLORATION; floor applies at bundle level per Clarification 3)` — to preserve audit-trail discipline for the upcoming CONFIRMATION; this is the FIRST EXPLORATION-PROMISING since iter-v3/007 and a real candidate for next CONFIRMATION bundling.
