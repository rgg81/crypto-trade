# Phase 7.5 Critic Review — iter-v3/074

OVERALL: MERGE

INERT-AT-EXPLORATION classification certified clean. The regime-conditional kill switch (primitive 9, TRX-scoped) is a genuine holding-time-orthogonal axis, the BTC-regime classification is double-layered past-only with no look-ahead, and the LOCKED Section 8 taxonomy was applied faithfully. /074 does NOT advance to the cycle-2 CONFIRMATION bundle. One non-blocking forensic error in the engineering report's prior-iteration context table is flagged for the diary.

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (cycle 2 #4 of 10)

## Foundation Audit (Boot Steps 9-11)

- **Boot Step 9 — walk-forward lookahead fix:** PASS. `walk_forward.py:113` reads `train_end_ms = test_start_ms - embargo_ms` (the post-fix form, not the buggy `train_end_ms = test_start_ms`). `compute_embargo_candles(10080, 480) = 10080 // 480 + 1 = 22` (helper at `walk_forward.py:10-38`). `validation_v3.py:60` has `REQUIRED_GAP = (21 + 1) * 3 = 66`. Single source of truth intact.
- **Boot Step 10 — regime gate implementation:** PASS. `risk_v3.py:42-103` `_build_btc_regime_lookup` and `risk_v3.py:207-243` `_regime_gate_fires` both audited line-by-line — see Check 1. The /074 re-test is the regime gate's first post-walk-forward-fix data point; the /022 verdict was at the biased pre-fix baseline and is re-evaluation-eligible per `feedback_v3_walkforward_lookahead_bug.md`.
- **Boot Step 11 — §11 anti-pattern static scan:** PASS. `risk_v3.py` scanned for `.iloc[-1]`, negative `.shift()`, `.bfill`, `tail(1)`, future/lookahead patterns — zero matches (sole hit was `from __future__ import annotations`, a false positive). `run_baseline_v3.py:189` runs the `V3_EXCLUDED_SYMBOLS` overlap audit with a raise. `features_v3/` cross-track import grep is empty (sole hit is the docstring containing the grep command itself).

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS
This is the load-bearing check for this axis and it is clean on two independent layers. **Layer 1 (regime computation):** `_build_btc_regime_lookup` (`risk_v3.py:77-97`) computes drawdown from `close_shifted = df["close"].shift(1)`, with `rolling_max` over the shifted series and `drawdown_pct = (close_shifted - rolling_max) / rolling_max`; the vol z-score uses `logret_shifted = df["log_ret"].shift(1)` then `rolling_std` over the shifted series. Both metrics at BTC bar `t` therefore see only `close[t-1 .. t-lookback]` — bar `t`'s own close is never an input. **Layer 2 (trade-time selection):** `_regime_gate_fires` (`risk_v3.py:229`) does `idx = int(np.searchsorted(btc_times, open_time_ms, side="left")) - 1`, which selects the last BTC bar with `open_time STRICTLY LESS THAN` the symbol's bar time. So a TRX candidate at time `t` is gated on a BTC bar strictly older than `t`, whose DD/vol-z is itself computed from BTC data strictly older than that bar. The `expanding_mean`/`expanding_std` for the vol z-score are streaming-causal (the expanding window at index `idx` covers only `[0..idx]` of the already-shifted `rolling_std` series) — no future leak despite the lookup being built once over the full BTC CSV including OOS bars. Brief Section 5's disclosure that the expanding stats are full-series rather than IS-only is a deliberate conservative-OOS-estimate choice, honestly documented, not a leak. Pre-existing 14 features unchanged from the /059 baseline; no new feature to audit.

### Check 2 — Embargo Width: PASS
Label horizon = triple-barrier 21-candle (10080-minute) timeout, uniform across all symbols (`V3_ATR_MULTIPLIERS_PER_SYMBOL = {}` revert confirmed, all symbols on `DEFAULT_ATR_MULTIPLIERS = (2.0, 1.0)`). Required gap = `(timeout_candles + 1) × n_symbols = (21 + 1) × 3 = 66`. Actual `REQUIRED_GAP = 66` (`validation_v3.py:60`). Per-symbol walk-forward embargo = `compute_embargo_candles(10080, 480) = 22` candles. Exact match on both. The regime gate touches neither the label horizon nor the symbol count, so the embargo is identical to the /060 anchor.

### Check 3 — Multiple-Testing Correction: PASS (EXPLORATION-mode; DSR/PSR informational)
PBO = 0.1278 < 0.40 — PASS. `frac_positive_paths = 0.6444 ≥ 0.55` — PASS (independently verified: 29 of 45 CPCV paths positive in `cpcv_paths.csv` = 0.6444, matching `dsr.json`). DSR (legacy) = 0.0 and DSR_relative_B4 = 0.0715 FAIL their 0.95 thresholds but are **informational at EXPLORATION** per `feedback_v3_dsr_mode_artifact.md` — the EXPLORATION-mode `n_trials=315` regime is not comparable to CONFIRMATION-mode `n_trials=1050`, and these gates do not trigger BLOCK for an EXPLORATION iteration. PSR = 0.9985 (informational). `n_trials = 315` = 3 seeds × 3 symbols × 35 trials — matches the EXPLORATION budget. `n_eff = 19` — sensible (architecture-independent per-cell PCA-95 median). The one Check-3 axis that binds at EXPLORATION (PBO) clears comfortably.

### Check 4 — IC Correlation: PASS
No new feature family — brief Section 3 confirms `V3_FEATURE_COLUMNS = 14`, unchanged from the /059 baseline; the regime gate adds zero features. Check 4's "new vs existing" scope therefore has nothing to evaluate. For completeness: `ic_matrix.csv` max off-diagonal pair is `vwap_dev_20` / `regime_momentum_signed_5d` = 0.7642, slightly above 0.70 — but `regime_momentum_signed_5d` is a Category-2 composed feature (`ret_5d × sign(hurst_100 − 0.5)`) that mechanically correlates with its `vwap_dev_20`-adjacent primitive; per `feedback_v3_engineered_feature_pivot.md` the strict |IC| gate is carved out for composed features in favor of an importance ≥ 30 threshold, and the feature ranks 14/14 at importance 506.7 — far above 30. Pre-existing baseline feature, not a /074 introduction.

### Check 5 — ADF Stationarity: PASS
At 2025-03 — the final IS month (OOS cutoff 2025-03-24) — all 14 features pass the ADF test (p < 0.05) for all 3 symbols (`adf_test.csv` lines 870-883 BCH, 1304-1317 LDO, 2186-2199 TRX). Adversarial note: at 2025-02 three feature-cells fail (BCH `ret_kurt_200` p=0.051, TRX `ret_skew_200` p=0.374, TRX `ret_kurt_200` p=0.488), but Check 5 evaluates the end of the training window, and at 2025-03 those same cells recover (BCH `ret_kurt_200` p=0.045, TRX `ret_skew_200` p=0.032, TRX `ret_kurt_200` p=0.022 — all below 0.05). These are higher-moment statistics (kurtosis/skew at 200-bar windows) with known month-to-month instability; the pre-existing /059 baseline carries them and the regime gate adds no price-derived feature. No FAIL.

### Check 6 — Pareto Dominance: PASS (N/A — single-roster EXPLORATION)
Under the unified-architecture EXPLORATION mode, `pareto_front.csv` is not produced (per BASELINE_V3.md: Gate 10-Pareto retired, replaced by Gate 10-CPCV `frac_positive_paths ≥ 0.55`). `ensemble_summary.json` confirms the 3-seed EXPLORATION ensemble (`191664963, 1662057957, 1405681631`, all `outer=42` lineage) producing one deterministic trade roster. Gate 10-CPCV: `frac_positive_paths = 0.6444 ≥ 0.55` — PASS. No seed-selection question arises because the EXPLORATION mode averages a fixed 3-seed ensemble rather than selecting a winner.

### Check 7 — Reproducibility: PASS
Code-level: `ITERATION_LABEL = "v3-074"` (`run_baseline_v3.py:128`), `OOS_CUTOFF_DATE = "2025-03-24"` and `TRAINING_MONTHS = 24` IMMUTABLE (lines 81-82), `feature_columns=list(features_for_symbol(symbol))` explicit (line 1547 — NOT `None`), `ensemble_seeds` passed literal (line 1545), `label_mode="triple_barrier"` (line 1555). Setup commit SHA `a5d5cdd`, Phase 5.5 gate SHA `0c01d8f`. **PnL spot-check — 3 random OOS trades reconciled:** Row 2 (BCH SHORT, entry 303.870000 / exit 282.100779 / weight 0.3300): pnl_pct (303.870000−282.100779)/303.870000×100 = 7.1640 ✓, net 7.0640 ✓, weighted 7.0640×0.3300 = 2.3311 ✓. Row 6 (BCH SHORT, 419.340000/389.786128/0.8200): pnl_pct = 7.0477 ✓, net 6.9477 ✓, weighted 5.6971 ✓. Row 10 (TRX SHORT stop_loss, 0.278060/0.282650/0.5200): pnl_pct = −1.6507 ✓, net −1.7507 ✓, weighted −0.9104 ✓. All to rounding tolerance. **BCH bit-identity:** `out_of_sample/per_symbol.csv` reports BCH 37 OOS trades; `comparison.csv` line 18 reports BCH weighted_pnl +1.9078 / 37 trades — both cross-check the QE's "37/37 field-by-field identical to /060" claim at the aggregate level (count + weighted-PnL). The gate is scoped to TRX (`regime_gate_symbols=("TRXUSDT",)`); BCH cannot be touched. The QE's BIT-IDENTICAL claim is corroborated by every available cross-check.

### Check 8 — Hypothesis-Implementation Alignment: PASS
Brief Section 3 declares exactly two changes: (1) the axis — `enable_regime_gate=False → True`, and (2) the mandatory revert — `V3_ATR_MULTIPLIERS_PER_SYMBOL` back to `{}`. Both verified in source: `run_baseline_v3.py:1592-1595` (`enable_regime_gate=True`, `regime_gate_symbols=("TRXUSDT",)`, `regime_dd_threshold_pct=20.0`, `regime_vol_zscore_threshold=1.5`) and `features_v3/__init__.py:251-264` (`V3_ATR_MULTIPLIERS_PER_SYMBOL = {}` with the documented /073-revert history comment). `block_long_for=()` / `block_short_for=()` confirmed (primitive 10 stays reverted from /051) — no scope creep into other primitives. The QR predicted INERT at 35% probability (brief Section 7); observed INERT. The holding-time-orthogonal hypothesis is confirmed: the TRX kept-roster duration delta is −0.389 candles IS / −0.240 OOS — near-zero and slightly *negative* (opposite direction from the /065/071/073 holding-time-extension family), well inside the +1.5-candle falsifier. OOS/IS monthly Sharpe ratio = 0.247 (`comparison.csv` line 2 ratio column, byte-confirmed) vs the /065/071/073 SUSPICIOUS family — see the forensic note in Recommendations. **On adversarial question 4:** the 0.247 ratio is genuine evidence of the holding-time-orthogonal mechanism, not merely a small-effect artifact. The discriminating fact is the *sign and direction* of the duration delta (slightly negative), not just its magnitude — a holding-time-extension axis loads the regime factor by lengthening the kept roster regardless of how few trades it touches; /074's kept roster is marginally *shorter*-held, which is mechanistically incompatible with regime-factor loading. The small headline shift and the orthogonality are two independent observations that happen to agree, not one observation double-counted.

## Adversarial Questions — Disposition

1. **Is the regime gate's BTC-regime classification past-only?** YES — confirmed on two independent layers (Check 1). `.shift(1)` on both `close` and `log_ret` before any rolling computation; `np.searchsorted(..., side="left") - 1` for strict `< t` trade-time selection. No forward leak.

2. **Engineering report cites prior SUSPICIOUS ratios as "5.04/4.51/12.55" — verify.** The engineering report's prior-iteration ratio column is **factually wrong** and is flagged (non-blocking — see Recommendation 1). The byte-confirmed within-iteration `monthly_sharpe` ratios from each iteration's own `comparison.csv` are: /065 = 1.5608, /071 = 0.6774, /073 = 6.8468. The /073 diary's table records "2.87 / 4.51 / 6.85" — only the /073 entry (6.85) matches its comparison.csv; the 2.87 and 4.51 are a different (unreconciled) construction, and the /071 diary itself states 0.6774 — so the source artifacts already disagreed. The engineering report introduces a *third* incorrect set. Critically, the engineering report's "/073 = 12.55" contradicts /073's own byte-confirmed comparison.csv value 6.8468. This does NOT affect /074's INERT verdict — /074's own ratio 0.247 is byte-confirmed against `/074 comparison.csv` line 2 and is the only ratio the Section 8 classifier consumes.

3. **Should a near-zero-effect axis be NULL-RESULT rather than INERT?** No — INERT is the LOCKED-criteria-correct classification. Brief Section 8.5 defines NULL-RESULT strictly as `regime_gate_fires total = 0` (the gate never fired). The Gate Efficacy table confirms 3 IS + 5 OOS TRX suppressions — the gate *did* fire, so NULL-RESULT is explicitly excluded by its own definition. INERT (8.3) requires shifts within the noise bands and not-SUSPICIOUS; /074 satisfies both (IS Δ +0.013 ∈ ±0.10; OOS Δ +0.069 ∈ ±0.20; SUSPICIOUS does not fire). The brief did not pre-register a separate "small-but-nonzero" class; the QE applied the LOCKED disjunctive evaluation order faithfully.

4. **Is OOS/IS 0.247 genuine orthogonality evidence?** YES — see Check 8 disposition above.

## Recommendations to QR

(Process-level items for the cycle-2 #5 iteration and the eventual CONFIRMATION QR — /074 is INERT and does NOT carry forward as a bundle ingredient.)

1. **Fix the engineering-report prior-iteration ratio forensics, and standardize one OOS/IS ratio definition.** The /074 engineering report's "5.04/4.51/12.55" for /065/071/073 reconciles to no source artifact; the "/073 = 12.55" entry directly contradicts /073's byte-confirmed comparison.csv ratio of 6.8468. Separately, the /073 diary ("2.87/4.51/6.85") and the /071 diary ("0.6774") already disagreed on /071's ratio. The Phase 8 diary for /074 should record the corrected within-iteration ratios (/065 1.56, /071 0.68, /073 6.85) and the cycle-2 documentation should fix ONE definition of "OOS/IS ratio" (the within-iteration `comparison.csv` ratio column is the natural choice, since that is what the Section 8.4 SUSPICIOUS gate actually consumes) so future briefs and reports stop drifting between constructions.

2. **The regime-gate axis is now closed across two data points — record it.** Primitive 9 produced NEGATIVE-pre-fix at /022 and INERT-post-fix at /074. The post-fix re-evaluation discharged the `feedback_v3_walkforward_lookahead_bug.md` re-eval eligibility cleanly; the axis should be added to BASELINE_V3.md "Dead Ideas" as tested-twice-no-signal so it is not re-proposed in cycle 2 or cycle 3.

3. **Cycle-2 #5 should target the IS bear/chop drag directly with a holding-time-orthogonal mechanism.** The brief's EDA PART 1 (a genuine deliverable — the IS/OOS regime-stratified diagnostic limb) localized the structural drag to the IS bear/chop sub-period (monthly Sharpe −0.0242, 27.8% positive months). The regime gate confirmed that holding-time-orthogonal axes do not load the regime factor, but the kill-switch mechanism touched too few trades to lift that drag. The next EXPLORATION should pre-register a behavioral-effect predictor estimating a materially larger trade-population effect (the /074 3-IS/5-OOS suppression was too small to move the headline by construction) — e.g. a new feature family that discriminates bear vs bull regimes within the IS window, which is holding-time-orthogonal by mechanism and acts on the full roster rather than a stress-bar subset.
