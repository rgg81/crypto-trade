# Phase 7.5 Critic Review — iter-v3/118

OVERALL: EXPLORATION-NEGATIVE catastrophic — Section 8 Criterion 1a fires decisively (IS Sharpe Δ −0.4543 vs /060 anchor, 2.27× the −0.20 threshold)

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (cycle-6 slot #9 of 10; single structural axis: NEW Category-2 composed feature `ema_signed_volregime` = `ema_spread_atr_20 × sign(range_realized_vol_50 − rolling_median_200)` added to `V3_FEATURE_COLUMNS_TOP_N`).

## QR Response Considered (Round 2)

QR's Round-2 position is **STAND BY VERDICT** — a clean concession on the EXPLORATION-NEGATIVE catastrophic classification with no contestation of any of the 8 PRELIMINARY check statuses. The 4 clarifications received substantive, evidence-anchored answers:

1. **Axis closure scope** → QR adopts the Critic's broader-closure lean: the entire `value × sign(vol-regime-classifier)` Category-2 lineage is CLOSED on the BCH/LDO/TRX universe at single-seed n_trials=35, not just the specific median-200 + ema_spread + rv_50 composite. Catalog wording committed to broader closure. Critic CONCURS — this is the methodologically defensible scope.

2. **Mode 3 inversion mechanism** → QR identifies it as PRIMARILY single-seed-lottery + multivariate-interaction-cancellation per `feedback_v3_inert_features_at_higher_budget.md` (observed in reverse direction: WEAK-multivariate-lift feature REDIRECTS attention to a different per-symbol carrier at 15-dim Optuna scale), with a secondary intrinsic-property channel from C3's slow ~67-day regime classifier × fast ~6-day value primitive product geometry. QR explicitly does NOT recommend a separate multi-seed C3 experiment (expectation: would ATTENUATE inversion but stay NEGATIVE; ~5h CONFIRMATION cost to confirm what mechanistic reasoning already establishes). Critic CONCURS — the NO-RECOMMENDATION on a separate multi-seed C3 experiment is methodologically tight; running it would burn CONFIRMATION budget on a closure already supported by the EXPLORATION evidence.

3. **/119 axis recommendation** → QR commits to option (a) ANOTHER engineered-feature lineage on STRUCTURALLY DIFFERENT primitives — explicit reasoning given for rejecting (b) per-symbol XGBoost-with-categorical (the high-imbalance claim ground was removed when /117 closed candle-frequency at 8h) and (c) out-of-the-box (too vague to commit at /118 closeout). QR proposes ≥4 candidates from 2+ orthogonal categories: (i) volume-based primitives (OBV/MFI/CMF-derived), (ii) cross-asset BTC primitives, (iii) tail/higher-moment regime classifiers, (iv) microstructure-derived. New EDA gate proposed: Single-Symbol-Carrier RISK flag if T9 per-symbol asymmetry > 2× POOLED magnitude. Critic CONCURS — this is the right axis pivot, and the new SSC-RISK gate is a direct learning from /118's failure mode.

4. **/120 CONFIRMATION composition** → QR confirms /120 is VALID as a single-component CONFIRMATION of /116 no_confirm vs /059 canonical, citing iter-v3/018 precedent (single-component validation of /013 drop-MKR; FAILED at multi-seed — a methodologically clean closeout regardless). /119 NEGATIVE outcome does NOT block /120 from running with /116 alone; /119 PROMISING would simply add a second component. Critic CONCURS — the iter-v3/018 single-component CONFIRMATION precedent is valid and the four-cell decision tree QR articulated produces a meaningful cycle-6 closeout in all branches.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

`compute_ema_signed_volregime` at `src/crypto_trade/features_v3/engineered_v3.py:778-831` is past-only by construction. The function uses `rv.rolling(window=200, min_periods=200).median()` on `range_realized_vol_50`, a trailing window that at bar `t` consumes only bars `t-199..t` — strictly past at inference. The two upstream primitives are audited at source: (a) `range_realized_vol_50` at `tail_risk_v3.py:42` via `_range_realized_vol(high, low, 50)` is a trailing rolling window on `np.log(high/low)`, past-only by construction; (b) `ema_spread_atr_20` at `momentum_accel_v3.py:57` via `(ema10 - ema50) / atr20`, all three components past-only. GROUP_REGISTRY ordering in `features_v3/__init__.py:71-119` places `momentum_accel` and `tail_risk` BEFORE `engineered_v3`, so dependencies are satisfied without dispatch-order leakage. Phase 5.5 gate confirmed 0 OOS-leaked rows across BCH (5483/0), LDO (2497/0), TRX (5425/0).

### Check 2 — Embargo Width: PASS

`REQUIRED_GAP = 66 = (21+1) × 3` for 8h single-frequency with 21-bar triple-barrier timeout and 3-symbol universe (BCH/LDO/TRX). Verified at the runner accretion guard at `run_baseline_v3.py:1095` (entry `("REQUIRED_GAP", REQUIRED_GAP, 66)`). The /117 panel-aware CV-gap fix at `lgbm.py:498` was filed CONDITIONAL on the multi-offset architecture; /118 uses single-frequency 8h (default `--bar-interval 8h`), so the conditional path is not exercised. The /058 walk-forward lookahead bias FIX (commit `e149e9d`) carrying `train_end_ms = test_start_ms - embargo_ms` is verified present in `walk_forward.py:113`.

### Check 3 — Multiple-Testing Correction: FAIL (informational for EXPLORATION)

`dsr.json` reports DSR=0.0 (threshold > 0.95 — FAIL); PSR=0.0482 (threshold > 0.95 — FAIL); PBO=0.0885 (threshold < 0.40 — PASS); frac_positive_paths=0.6444 (threshold ≥ 0.55 — PASS). n_trials=315 = 3 seeds × 35 trials × 3 symbols; n_eff=19. Per `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode DSR/PSR are regime-specific artifacts (n_trials=315 with E[max_SR]≈2.6) NOT comparable to CONFIRMATION-mode (n_trials=1500+, E[max_SR]≈3.4). Per TYPE=EXPLORATION, Check 3 DSR/PSR axis FAILs are informational only and do NOT trigger BLOCK; only the PBO axis is BLOCK-triggering, and PBO PASSES decisively. No Check-3-axis BLOCK condition fires (the verdict-determinative BLOCK condition is Section 8 Criterion 1a IS Δ catastrophic).

### Check 4 — IC Correlation: PASS

`ic_matrix.csv` is 15×15. C3's `ema_signed_volregime` column shows max |IC| = **0.2119** with `sym_vs_btc_ret_7d` — well below the 0.70 threshold. Other C3 pairs: 0.180 with `regime_momentum_signed_5d` (the /025 baseline composite — orthogonal as designed); 0.179 with `ret_skew_200`; 0.152 with `btc_ret_14d`; 0.145 with `ret_skew_50`; 0.068 with `ema_spread_atr_20` (the value primitive C3 is composed from — the low |IC| despite shared primitive confirms the `sign(.)` regime classifier carries the bulk of the variance). EDA T2 R²=0.196 max|corr|=0.21 reproduces in production at max|IC|=0.21. The one pre-existing high-IC pair (`vwap_dev_20 × regime_momentum_signed_5d` at 0.7642) is unchanged from /059/060 baseline; C3 introduces no new collinearity risk.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` has 2355 rows across 3 symbols × ~80 months × 15 features. C3 (`ema_signed_volregime`) has 157 rows, 138 (87.9%) stationary at p < 0.05 — above the portfolio mean of 82.4%. Early-window NaN ADF rows (BCH 2020-01 through 2020-04; LDO 2022-09 through 2022-12; TRX 2020-01 through 2020-09) are during the C3 warm-up period (200-bar rolling median + 50-bar realized-vol warm-up) and are NaN-by-design. At the IS-end month 2025-03, C3 is decisively stationary across all 3 symbols (BCH ADF=−7.22 p=0.0; LDO ADF=−7.76 p=0.0; TRX ADF=−6.32 p=0.0). The `sign(.)` regime composition produces a bounded ±1 multiplier applied to bounded `ema_spread_atr_20`, structurally guaranteeing stationarity once warmed up.

### Check 6 — Pareto Dominance: N/A

Single-seed EXPLORATION mode (3 outer seeds via EXPLORATION_ENSEMBLE_SIZE=3 = ENSEMBLE_SEEDS[0:3], reported as the unified-seed-42-lineage; not 10-seed Pareto evaluation). Pareto-dominance check applies only to CONFIRMATION-mode 10-seed validation. The /118 brief Section 0.5 declares EXPLORATION; Pareto evaluation deferred to /120 CONFIRMATION.

### Check 7 — Reproducibility: PASS

(a) Commit SHA `0ac25c8` is stamped in the engineering report Section 1 and matches `git rev-parse HEAD` for `iteration-v3/118`; (b) `feature_columns` is explicitly the 15-element `V3_FEATURE_COLUMNS_TOP_N` enforced by the `n != 15` guard at runner line 432-440; (c) `ENSEMBLE_SEEDS` literal tuple at runner line 103 (`ENSEMBLE_SEEDS[0:3]` slice for EXPLORATION mode is deterministic); (d) trade arithmetic spot-check on `reports-v3/iteration_v3-118/out_of_sample/trades.csv` rows 2-4: short BCH entry 303.87 exit 282.10 → gross +7.164%, net +7.064% (after 0.1% fee), weighted +2.33% via 0.33 weight — math verified consistent. The integration test (Section 1 row "Integration test (Change 6) | PASS") confirms `V3_FEATURE_COLUMNS` includes `ema_signed_volregime` at runtime, parquet column non-NaN on last 100 IS rows, `n_features == 15` guard fires, past-only unit test passes.

### Check 8 — Hypothesis-Implementation Alignment: PASS (minor doc-string lag noted)

Brief Section 3.5 specifies 6 changes (plus Phase 5.5 gate advisory Change 0 for ITERATION_LABEL/MODEL_SPECS). All 6 + Change 0 delivered per the engineering report Section 2: (1) `compute_ema_signed_volregime` at `engineered_v3.py:778` matches the brief docstring exactly; (2) `"ema_signed_volregime"` is the 15th element of `V3_FEATURE_COLUMNS_TOP_N` at `features_v3/__init__.py:213` placed AFTER `regime_momentum_signed_5d` per spec; (3) runner `n != 15` guard at line 432-440; (4) parquet regeneration confirmed; (5) unit test landed; (6) integration test landed; (Change 0) ITERATION_LABEL="v3-118" at line 131, MODEL_SPECS prefixed "v3-118-" at lines 198-200. The runner pre-flight assertion at `run_baseline_v3.py:2929-2947` still contains the cosmetic string "iter-v3/117" in its error messages (advisory note #2 from Phase 5.5 gate) — documentation lag NOT a logic defect; the assertion still validates `enable_no_confirm_exit is False`, `no_confirm_trigger_atr == 0.50`, and `no_confirm_k_candles == 4`, all PASSING. No scope creep, no hypothesis-faking.

## Recommendations to QR for /119 + /120

1. **/119 axis = engineered-feature on STRUCTURALLY DIFFERENT primitives.** Per QR Round-2 Clarification 3, /119 should run another engineered-feature EXPLORATION drawn from the orthogonal-primitive list QR enumerated: (i) volume-based primitives (OBV/MFI/CMF-derived), (ii) cross-asset BTC primitives (BTC-correlation regime, BTC-funding alignment), (iii) tail/higher-moment regime classifiers (skew-sign, kurtosis-percentile-band), (iv) microstructure-derived primitives. The /119 QR makes the specific candidate selection under `feedback_v3_axis_selection_quant_discipline.md` (EDA-driven discipline, committed `analysis/iteration_v3-119/*.py` before brief). ADD the Single-Symbol-Carrier RISK pre-Falsifier QR proposed in Clarification 3: if T9 per-symbol asymmetry > 2× POOLED magnitude (a single-symbol carrier dominates the POOLED signal), brief Section 2 flags the candidate as Single-Symbol-Carrier-RISK and the Falsifier band tightens accordingly. The /119 EDA must REJECT any candidate using `sign(realized_vol − vol_threshold)` as the regime sign factor (per /118 broader-closure scope). The /025 hurst-regime branch is occupied (cannot be re-tested as a NEW axis without a different primitive on hurst).

2. **/120 CONFIRMATION = SINGLE-COMPONENT validation of /116 no_confirm vs /059 canonical at full 10-seed ensemble.** Per `feedback_v3_iter017_metalabeling_mandate.md` and QR Clarification 4 (iter-v3/018 single-component CONFIRMATION precedent). The /120 CONFIRMATION brief MUST pre-register: (i) IS Sharpe band accommodating the regime-cost per /116 Critic Rec 2 (e.g. IS NEGATIVE floor at /059 IS − 0.50 = +0.59 rather than the standard /059 IS − 0.10 = +0.99, since /116's mechanical cascade is expected to bear an IS cost that may not be a CONFIRMATION-FAIL signal); (ii) multi-seed cascade-attribution falsifier per /116 Critic Rec 3 ("if at 10-seed the per-symbol OOS net_pnl_pct Δ vs /059 is not broad-based positive across ≥ 2 of 3 symbols, RECLASSIFIED PROMISING-FALSIFIED-AT-CONFIRMATION"). If /119 is PROMISING, /120 bundles BOTH /116 and the /119 result with the caveat that mechanical and signal-discovery ingredients have different multi-seed-validation criteria per `feedback_promising_mechanical_subtype.md`. If /119 is NEGATIVE, /120 bundles ONLY /116 — single-component CONFIRMATION is canonical (iter-v3/018 precedent) and produces a clean cycle-6 closeout regardless of MERGE-vs-NO-MERGE outcome.

3. **C3 code defect retention: KEEP `compute_ema_signed_volregime` in `engineered_v3.py` AS-IS — DO NOT remove.** The `feedback_v3_inert_features_at_higher_budget.md` rule says drop INERT features but C3 is NOT INERT (rank 6/15 portfolio; rank 1/15 on TRX; rank 11/15 BCH; rank 13/15 LDO). However C3 MUST NOT be in `V3_FEATURE_COLUMNS_TOP_N` for /119 onward — the /119 setup-commit MUST REVERT `V3_FEATURE_COLUMNS_TOP_N` from 15 elements back to 14 (drop `ema_signed_volregime`). The function stays in `engineered_v3.py` for code-museum-value; if the broader `value × sign(vol-regime-classifier)` axis is ever re-opened under a different protocol (multi-seed CONFIRMATION-budget validation OR a different regime-classifier family), the implementation is preserved. The /119 runner `n != 15` guard reverts to `n != 14`.

4. **Catalog entry for /118.** EXPLORATION-NEGATIVE catastrophic. IS Δ vs /060 anchor = −0.4543 (2.27× the −0.20 threshold). OOS Δ vs /060 anchor = −0.2904. Confirmation candidate: NO. Closure scope: broader `value × sign(vol-regime-classifier)` Category-2 lineage CLOSED on the BCH/LDO/TRX universe at single-seed n_trials=35 EXPLORATION budget. Narrower vol-classifier specifics (median window, vol estimator) subsumed by the broader closure. Reopening requires either (a) multi-seed CONFIRMATION-budget validation OR (b) a different regime-classifier family.

## Clarifications Requested from QR — NONE
