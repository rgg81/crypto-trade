# Phase 7.5 Critic Review — iter-v3/118 — PRELIMINARY

(NO OVERALL line in Round 1.)

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (single structural axis: NEW Category-2 composed feature `ema_signed_volregime` added to `V3_FEATURE_COLUMNS_TOP_N`; cycle-6 slot #9 of 10).

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

`compute_ema_signed_volregime` at `src/crypto_trade/features_v3/engineered_v3.py:778-831` is past-only by construction. The function uses `rv.rolling(window=200, min_periods=200).median()` on `range_realized_vol_50`, a trailing window that at bar `t` consumes only bars `t-199..t` — strictly past at inference. The two upstream primitives are audited at source: (a) `range_realized_vol_50` at `tail_risk_v3.py:42` via `_range_realized_vol(high, low, 50)` is a trailing rolling window on `np.log(high/low)`, past-only by construction; (b) `ema_spread_atr_20` at `momentum_accel_v3.py:57` via `(ema10 - ema50) / atr20`, all three components past-only (EWM is incremental from index 0; ATR rolls on `tr.rolling(period).mean()`). The `sign(.).replace(0.0, NaN)` step is purely pointwise. GROUP_REGISTRY ordering in `features_v3/__init__.py:71-119` places `momentum_accel` and `tail_risk` BEFORE `engineered_v3`, so dependencies are satisfied without dispatch-order leakage. The runner setup commit `0ac25c8` introduces no new data-path with future leak: the only data-flow changes are the new function, the runner `n != 15` guard at line 432-440, and the parquet regeneration. The EDA `_shared.py` enforces `OOS_CUTOFF_MS = 1742774400000` with `out["close_time"] >= OOS_CUTOFF_MS` assertion (line 121); the Phase 5.5 gate confirmed 0 OOS-leaked rows across BCH (5483/0), LDO (2497/0), TRX (5425/0). No look-ahead in any audited component.

### Check 2 — Embargo Width: PASS

`REQUIRED_GAP = 66 = (21+1) × 3` for 8h single-frequency with 21-bar triple-barrier timeout and 3-symbol universe (BCH/LDO/TRX). Verified at the runner accretion guard at `run_baseline_v3.py:1095` (entry `("REQUIRED_GAP", REQUIRED_GAP, 66)` — 8h base constant from `validation_v3.py`). The /117 panel-aware CV-gap fix at `lgbm.py:498` was filed CONDITIONAL on the multi-offset architecture; /118 uses single-frequency 8h (default `--bar-interval 8h` per brief Section 0.5), so the conditional path is not exercised and the existing 8h-base implementation is correct. The /058 walk-forward lookahead bias FIX (commit `e149e9d`) carrying `train_end_ms = test_start_ms - embargo_ms` is verified present in `walk_forward.py:113` per the engineering report Section 13.

### Check 3 — Multiple-Testing Correction: FAIL (informational for EXPLORATION)

`dsr.json` reports DSR=0.0 (threshold > 0.95 — FAIL); PSR=0.0482 (threshold > 0.95 — FAIL); PBO=0.0885 (threshold < 0.40 — PASS); frac_positive_paths=0.6444 (threshold ≥ 0.55 — PASS). n_trials=315 = 3 seeds × 35 trials × 3 symbols (matches EXPLORATION-mode budget); n_eff=19 (sensible non-degenerate effective sample). Per `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode DSR/PSR are regime-specific artifacts (n_trials=315 with E[max_SR]≈2.6) that are NOT comparable to CONFIRMATION-mode (n_trials=1500+, E[max_SR]≈3.4). Per TYPE=EXPLORATION classification, Check 3 DSR/PSR axis FAILs are informational only and do NOT trigger BLOCK; only the PBO axis is BLOCK-triggering, and PBO PASSES decisively (0.0885 << 0.40). frac_positive_paths PASSES (0.644 ≥ 0.55). No Check-3-axis BLOCK condition fires.

### Check 4 — IC Correlation: PASS

`ic_matrix.csv` is 15×15. C3's `ema_signed_volregime` column shows max |IC| = **0.2119** with `sym_vs_btc_ret_7d` — well below the 0.70 threshold. Other C3 pairs: 0.180 with `regime_momentum_signed_5d` (the /025 baseline composite — orthogonal as designed); 0.179 with `ret_skew_200`; 0.152 with `btc_ret_14d`; 0.145 with `ret_skew_50`; 0.078 with `max_dd_window_50`; 0.068 with `ema_spread_atr_20` (the value primitive C3 is composed from — the low |IC| despite shared primitive confirms the `sign(.)` regime classifier carries the bulk of the variance). EDA T2 R²=0.196 max|corr|=0.21 reproduces in production at max|IC|=0.21 (Section 7 of engineering report). The one pre-existing high-IC pair (`vwap_dev_20 × regime_momentum_signed_5d` at 0.7642) is unchanged from /059/060 baseline; C3 introduces no new collinearity risk.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` has 2355 rows across 3 symbols × ~80 months × 15 features. C3 (`ema_signed_volregime`) has 157 rows, 138 (87.9%) stationary at p < 0.05 — above the portfolio mean of 82.4%. Early-window NaN ADF rows (BCH 2020-01 through 2020-04; LDO 2022-09 through 2022-12; TRX 2020-01 through 2020-09) are during the C3 warm-up period (200-bar rolling median + 50-bar realized-vol warm-up) and are NaN-by-design, not non-stationary. At the IS-end month 2025-03, C3 is decisively stationary across all 3 symbols (BCH ADF=−7.22 p=0.0; LDO ADF=−7.76 p=0.0; TRX ADF=−6.32 p=0.0). The `sign(.)` regime composition produces a bounded ±1 multiplier applied to bounded `ema_spread_atr_20`, structurally guaranteeing stationarity once warmed up.

### Check 6 — Pareto Dominance: N/A

Single-seed EXPLORATION mode (3 outer seeds via EXPLORATION_ENSEMBLE_SIZE=3 = ENSEMBLE_SEEDS[0:3], reported as the unified-seed-42-lineage; not 10-seed Pareto evaluation). Pareto-dominance check applies only to CONFIRMATION-mode 10-seed validation. The /118 brief Section 0.5 declares EXPLORATION; Pareto evaluation deferred to /120 CONFIRMATION.

### Check 7 — Reproducibility: PASS

(a) Commit SHA `0ac25c8` is stamped in the engineering report Section 1 and matches `git rev-parse HEAD` for `iteration-v3/118`; (b) `feature_columns` is explicitly the 15-element `V3_FEATURE_COLUMNS_TOP_N` enforced by the `n != 15` guard at runner line 432-440 (PASSES with `n=15`); (c) `ENSEMBLE_SEEDS` literal tuple at runner line 103 (`ENSEMBLE_SEEDS[0:3]` slice for EXPLORATION mode is deterministic); (d) trade arithmetic spot-check on `reports-v3/iteration_v3-118/out_of_sample/trades.csv` rows 2-4: short BCH entry 303.87 exit 282.10 → gross +7.164%, net +7.064% (after 0.1% fee), weighted +2.33% via 0.33 weight — math verified consistent. The integration test (Section 1 row "Integration test (Change 6) | PASS") confirms `V3_FEATURE_COLUMNS` includes `ema_signed_volregime` at runtime, parquet column non-NaN on last 100 IS rows, `n_features == 15` guard fires, past-only unit test passes.

### Check 8 — Hypothesis-Implementation Alignment: PASS (minor doc-string lag noted)

Brief Section 3.5 specifies 6 changes (plus Phase 5.5 gate advisory Change 0 for ITERATION_LABEL/MODEL_SPECS). All 6 + Change 0 delivered per the engineering report Section 2 table: (1) `compute_ema_signed_volregime` at `engineered_v3.py:778` matches the brief docstring exactly; (2) `"ema_signed_volregime"` is the 15th element of `V3_FEATURE_COLUMNS_TOP_N` at `features_v3/__init__.py:213` placed AFTER `regime_momentum_signed_5d` per spec; (3) runner `n != 15` guard at line 432-440 with /118 documentation; (4) parquet regeneration confirmed; (5) unit test landed; (6) integration test landed; (Change 0) ITERATION_LABEL="v3-118" at line 131, MODEL_SPECS prefixed "v3-118-" at lines 198-200. The runner pre-flight assertion at `run_baseline_v3.py:2929-2947` still contains the cosmetic string "iter-v3/117" in its error messages (advisory note #2 from Phase 5.5 gate) — this is a documentation lag NOT a logic defect; the assertion still validates `enable_no_confirm_exit is False`, `no_confirm_trigger_atr == 0.50`, and `no_confirm_k_candles == 4`, all PASSING. The reports directory `reports-v3/iteration_v3-118/` was correctly populated. The hypothesis (Section 1) — adding `ema_signed_volregime` to V3_FEATURE_COLUMNS_TOP_N — maps cleanly to the diff. No scope creep, no hypothesis-faking.

## Substantive Section 8 Classification Read

The engineering report's mechanical reading is correct: Section 8 Criterion 1a fires (IS Sharpe Δ = +0.3782 − +0.8325 = −0.4543, decisively below the −0.20 NEGATIVE-catastrophic threshold). First-match-wins binds the verdict to **EXPLORATION-NEGATIVE catastrophic**. Three corroborating diagnostics support this is not a borderline NEGATIVE but a structural one:

1. **Per-symbol role-reversal vs EDA T9 prediction.** EDA predicted Mode 3 PROMISING-PARTIAL with TRX as the sole positive carrier (T9 lift TRX +0.0082, BCH −0.0039, LDO −0.0106). Production INVERTED this: TRX collapsed to −5.49% net IS PnL (32.9% WR), BCH emerged as the sole IS positive carrier at +52.61% (42.7% WR), LDO catastrophic at −13.80% (26.7% WR). Mode 3's exact condition (TRX IS PnL Δ ≥ +2.0%) does not fire because TRX is NEGATIVE in IS. Mode 4's full condition (IS Δ < −0.20 AND OOS Δ < −0.50) does not fire because OOS Δ = −0.2904 misses the −0.50 OOS leg. Section 8's first-match-wins binds to Criterion 1a (IS Δ < −0.20 standalone) which has no OOS conjunction requirement.

2. **C3 importance fails the /025 PROMISING benchmark.** Portfolio rank 6/15 + 7.1% top-share fails BOTH bars (rank ≤ 5 AND ≥ 30% top-share). Per-symbol: TRX rank 1/15 ✓ but BCH rank 11/15, LDO rank 13/15 — pulled down by 2 of 3 symbols. The TRX-rank-1 result is consistent with EDA T5 predictions but BCH/LDO are well below the EDA-predicted rank 8-10. The portfolio aggregate is buoyed by TRX rank 1 obscuring 2 of 3 symbols being mid-to-low rank. This is a known "INERT-by-importance" lookalike pattern in inverse — the importance was allocated to TRX which then became the symbol the model OVERFIT.

3. **Single-seed lottery + multivariate-interaction-cancellation diagnostic.** With C3 added to all 3 symbols simultaneously at n_trials=35 single-seed, Optuna found a TRX-favorable hyperparameter config that maximized C3's TRX importance allocation (rank 1) at the cost of TRX's IS edge in the existing 14-feature stack. This is the `feedback_v3_inert_features_at_higher_budget.md` mechanism in a different guise: at n_trials=35 the larger 15-dim search space lets Optuna over-fit to a feature whose multivariate IS lift was thin (+0.0081 POOLED, just above the +0.005 gate) and whose per-symbol lift was negative on 2 of 3 symbols. The OOS partial recovery of TRX (+22.00% net OOS) coexists with LDO catastrophic OOS collapse (−33.20%, 20% WR, 368% concentration) and BCH OOS near-zero — the IS overfit transferred to a different OOS failure mode, not a recovery.

## Axis-Closure Recommendation Pre-Reasoning

The engineer's brief reads "vol-regime composite axis CLOSED at /118 (orthogonal classifier didn't lift)." A narrow read would CLOSE only the specific C3 = ema_spread_atr_20 × sign(rv_50 − rolling_median_200); a broader read would CLOSE the entire value × vol-regime-sign category (any value primitive × any vol-classifier sign). I lean toward the BROADER closure but want QR's judgment because: (a) the EDA's 6-candidate screen tested only one vol-regime classifier (the 200-bar rolling median of realized vol); the same lineage could be parametrized differently (e.g., rolling 100-bar, or expanding-median, or different vol estimator); (b) the failure mode is NOT specific to vol-regime classifiers — the cross-symbol role-reversal pattern would likely repeat with hurst-regime classifiers too IF the same single-seed n_trials=35 budget is used. The deeper finding may be "single-seed Category-2 composed features added to a 15-dim search space at n_trials=35 cause Optuna to over-fit to whichever symbol the new feature happens to favor at the single seed" — which would have implications BEYOND the vol-regime axis.

## Clarifications Requested from QR

1. **Section 8 axis closure scope.** Should iter-v3/118 CLOSE only the specific C3 composite (ema × vol-regime-sign at rolling-median-200), or the broader "value × vol-regime-sign" Category-2 lineage (any vol estimator × any window)? The EDA's Section 0 hand-chosen rolling-median window=200 is a single parameter point, not a family screen — does the QR view the family as exhausted or only this point?

2. **Mode 3 inversion mechanism — single-seed-lottery vs intrinsic feature properties.** The EDA T9 prediction was TRX-positive/BCH-negative/LDO-negative; production observed TRX-negative/BCH-positive/LDO-catastrophic-negative. Was this (a) a single-seed lottery artifact (n_trials=35 × seed=42 lineage found a different attractor than the EDA's depth-4 univariate-comparison LightGBM), in which case multi-seed CONFIRMATION might reveal a more symmetric per-symbol distribution, or (b) an intrinsic feature property where C3 disrupts the gradient-boosting loss surface across all seeds equally? The Engineering Report Section 11d argues against the frozen-baseline pattern (all 3 symbols received C3 simultaneously, so non-target-symbol freezing does not apply) — but does the QR have an explicit single-seed-vs-multi-seed read on whether this inversion would dissolve at 10-seed?

3. **/119 axis recommendation — cycle-6 menu items still LIVE.** The /117 review.md Recommendation 2 listed: (a) NEW engineered feature family on a DIFFERENT lineage (different value primitive AND different regime classifier — orthogonal to both /025 hurst and /118 vol-regime), (b) per-symbol XGBoost-with-categorical-handling, (c) out-of-the-box thinking. Given that /118 was the engineered-feature axis from the cycle-6 menu and it just failed catastrophically, does the QR prefer to (i) test ANOTHER engineered feature on a DIFFERENT lineage (e.g., volume × volume-regime-sign, or microstructure × microstructure-regime-sign — strictly orthogonal to both /025 hurst AND /118 vol-regime), or (ii) pivot to per-symbol XGBoost-with-categorical-handling (model architecture axis), or (iii) some other still-LIVE cycle-6 menu item I haven't identified from the diary?

4. **Trade-rate floor for the cycle-6 CONFIRMATION at /120.** /118 IS produced 182 trades, OOS 102 trades — well above the ≥130 OOS floor per `feedback_v3_trade_rate_floor_bundle_level.md`. But the CONFIRMATION at /120 will bundle /116 no_confirm as a strictly-accretive component decision (per `feedback_promising_mechanical_subtype.md`); /118 NEGATIVE means the C3 feature is NOT bundled. With only /116 no_confirm as the cycle-6 advanced component, does the /120 CONFIRMATION still have a non-trivial bundle to test (i.e., /116 no_confirm vs /059 canonical), or is /120 effectively a 1-knob CONFIRMATION? If the latter, this affects whether /119 has any room to add another LIVE candidate to the bundle.
