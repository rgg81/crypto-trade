# Phase 7.5 Critic Review — iter-v3/110

OVERALL: EXPLORATION-NEGATIVE — the universe-axis result is uninterpretable: the runner trained every /110 model under the known-NEGATIVE `trend_scanning` label, not the brief-declared `triple_barrier`, confounding the symbol-selection axis with a labeling-geometry change.

## Iteration Type (from Brief Section 0.5)

TYPE: EXPLORATION (Cycle 6, EXPLORATION #1 of 10; iter-v3/120 is the mandatory CONFIRMATION).

Per the EXPLORATION protocol and `feedback_v3_dsr_mode_artifact.md`, Checks 1, 2, 4, 5, 6, 8 are scored for verdict; Check 3 (DSR/PSR/PBO edge thresholds) is informational only at the 3-seed/35-trial EXPLORATION budget.

## QR Response Considered (Round 2)

The Round-1 PRELIMINARY raised exactly one clarification — the Check 8 `label_mode` carry-over — and pre-stated that Check 8 would FAIL regardless of response unless the QR produced a counter-artifact showing iter-v3/110 in fact ran under `triple_barrier`.

1. **Round 1 Clarification 1 (`label_mode` carry-over):** The QR's Round-2 response is **STAND BY VERDICT**. The QR independently re-read the four artifacts (runner config `run_baseline_v3.py:1917-1918`, runner pre-flight assertion `run_baseline_v3.py:1125-1133`, `run.log` line 20, `diary-v3/iteration_v3-105.md` lines 7 + 322-330), produced **no counter-artifact**, and concurs in full that the /105-mandated two-line revert was never executed, that iter-v3/110 varied two variables (universe swap + the carry-over `trend_scanning` label), and that the universe-axis verdict **cannot be salvaged** from this run's artifacts. The Critic independently verified all three runner artifacts at source this round (lines 1114-1133, 1905-1918, 1032-1050) — the `label_mode="trend_scanning"` config, the `expected_label_mode = "trend_scanning"` pre-flight that would have refused a `triple_barrier` run, and the `_canonical_v059` guard's omission of `label_mode`/`trend_scan_grid` are all confirmed. **Disposition: QR response confirms the concern; FAIL stands.** The QR's substantive analysis (`trend_scanning` is an *active IS-overfitting hazard* per the /105 closeout, an interaction term rather than a subtractable additive bias, so neither the magnitude nor the direction of the universe effect is recoverable) is accepted and strengthens the verdict. The QR's request that iter-v3/110 be catalogued as a *label-confound NEGATIVE* (axis unresolved, not closed) is endorsed and carried into the recommendations below.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

iter-v3/110's intended `src/` change is a universe swap; the `e149e9d`-fixed walk-forward is in effect (`train_end_ms = test_start_ms − embargo_ms`, every CV fold logging `gap=184h / 22 rows`). First OOS trade `open_time` is 2025-03-28, four days after the immutable `OOS_CUTOFF_DATE=2025-03-24`; last IS trade `close_time` is 2025-02-16 — clean split, no boundary contamination. The Phase-2 EDA loader (`analysis/iteration_v3-110/_shared.py`) asserts `close_time < OOS_CUTOFF_MS` per symbol. No contemporaneous-or-future data path found in the execution layer. The look-ahead audit covers the *execution* layer and is clean; the Check 8 finding concerns *which label was trained* — a hypothesis-alignment defect, not a temporal leak. PASS.

### Check 2 — Embargo Width: PASS

Label timeout = 10080 min / 480 min per 8h candle = 21 candles. Required cross-cell gap = `(timeout_candles + 1) × n_symbols = (21+1) × 4 = 88`. `validation_v3.py:76` carries `REQUIRED_GAP = 88` (correctly updated from the /059 value 66); the runner's `_verify_label_leakage_gap()` recomputes 88 and asserts equality; `run.log` line 25 records the PASS; the per-symbol walk-forward embargo is 22 candles applied symmetrically. Actual ≥ required, symmetric. The stale `(= (21+1)*3; ...3-sym /059 universe...)` parenthetical at `run.log` line 33 is a cosmetic copy-paste artefact in a diagnostic print string — the authoritative computed value at line 25 is 88 and is correct. Flagged for runner cleanup; not a computation error. **Caveat:** the embargo is sized for the 21-candle timeout; as-run, the model trained on a `trend_scanning` label with grid max 21 — `run.log` line 20 records `max_h=21 <= timeout_candles=21 PASS`, so the embargo is not under-sized for the as-run label either. The Check 8 confound does not breach the embargo. PASS.

### Check 3 — Multiple-Testing Correction: FAIL (informational for EXPLORATION — NOT verdict-triggering)

`dsr.json`: DSR=0.0, PSR=0.0 (both below the 0.95 threshold), PBO=0.1267 (clears the <0.4 gate). `n_trials=420`, `n_eff=17`. Per Section 0.5 TYPE=EXPLORATION and `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode DSR/PSR at this budget are structural regime artifacts, not edge-significance evidence — Check 3 axis FAILs do NOT trigger the verdict for an EXPLORATION iteration. DSR=0.0/PSR=0.0 are uninformative here: the OOS book is net-negative (OOS monthly Sharpe −0.4455), so there is no positive Sharpe for the deflation machinery to act on. `frac_positive_paths=0.622` clears the informational 0.55 CPCV gate but with the headline OOS at −0.4455 this is not exculpatory. Moot for verdict purposes — Check 8 already fails the iteration, and the underlying numbers are products of the confounded run.

### Check 4 — IC Correlation: PASS (no new feature family — not applicable)

iter-v3/110 is a universe-only axis: zero new features. `V3_FEATURE_COLUMNS` is asserted by the runner to be exactly the /059 14-feature stack. `ic_matrix.csv` is present and shows the known /059 intra-stack correlations (`regime_momentum_signed_5d` vs `vwap_dev_20` at 0.813, vs `sym_vs_btc_ret_7d` at 0.714) — pre-existing composed-feature correlations carved out per `feedback_v3_engineered_feature_pivot.md`, not introduced by this iteration. No new-vs-existing pair to test. PASS by non-applicability.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` on the /059-identical 14-feature stack: `stationary=False` rows are dominated by early-history months with insufficient candles for ADF to run (a "could-not-test" sentinel, not a stationarity failure). At the IS-end window the only computed-test failures are `ret_skew_200`/`ret_kurt_200` on CRV (p 0.21-0.35) and `ret_kurt_200` on AAVE (p 0.064, borderline) — long-window distributional-moment features whose ADF p naturally hovers near the threshold. No feature was added or modified — this is the /059 anchor stack. ADF evaluates feature stationarity (label-independent) — the Check 8 confound does not affect it. PASS.

### Check 6 — Pareto Dominance: PASS (single-lineage EXPLORATION — multi-seed Pareto not applicable)

iter-v3/110 ran in EXPLORATION mode (`ensemble_summary.json`: `mode=exploration, ensemble_size=3, seeds=[191664963,1662057957,1405681631]`, all `lineage=outer=42`). EXPLORATION mode produces one deterministic roster and `ensemble_summary.json` replaces `pareto_front.csv` — there is no 10-seed Pareto front to evaluate, and none is required at EXPLORATION budget. Multi-seed Pareto is a CONFIRMATION-mode concern, correctly deferred to iter-v3/120. PASS by non-applicability.

### Check 7 — Reproducibility: PASS

Commit SHA `f7e564f361fb839742ad3a26882854ea3d9a7937` is stamped in the engineering report. The runner passes an explicit `feature_columns=list(features_for_symbol(symbol))` — never `None`; the `_verify_feature_columns` block asserts the 14-column stack. `ITERATION_LABEL` is `"v3-110"`. Inner ensemble seeds are literal (`ensemble_summary.json`). PnL arithmetic spot-check confirmed against the engineering report's four-trade verification (2:1 ATR geometry consistent on every checked trade); IS sum +41.3249 vs `comparison.csv` +41.3253 and OOS −28.2851 vs −28.2848 are sub-0.01 float drift. Reproducibility properties hold for the run as configured — which is the precise problem Check 8 identifies: the run *is* bit-reproducible, and it bit-reproducibly trained on the wrong label. Reproducibility (the run can be re-derived) and correctness (the run tested the registered hypothesis) are orthogonal; this check is the former and it PASSes.

### Check 8 — Hypothesis-Implementation Alignment: FAIL

**The runner trained every /110 model on the wrong label. The brief's central "ONE axis" claim is false at the implementation level. This FAIL is the verdict driver.**

The brief is emphatic that this is a single-axis iteration — Section 0.5 ("the ONLY change vs the /059 baseline is the symbol universe `V3_MODELS` … labeling … all /059-identical"), Section 3.2 ("Labeling — UNCHANGED. 2:1 ATR triple-barrier … All /059-identical"), Section 3 header ("**ONE axis: `V3_MODELS` is replaced wholesale.** No feature, label, model, or risk-gate change").

The implementation contradicts this directly. Verified at source:
- **`run_baseline_v3.py:1917-1918`**: `_build_v3_model` `common_kwargs` carries `label_mode="trend_scanning"`, `trend_scan_grid=(5, 8, 13, 21)` — passed to every v3 model constructor at lines 1925/1927/1932.
- **`run_baseline_v3.py:1125-1133`**: the runner's pre-flight *asserts* `expected_label_mode = "trend_scanning"` and `raise RuntimeError` if it is anything else — the runner would have **refused to run** with the /059-canonical `triple_barrier` label. There is no execution path by which iter-v3/110 ran `triple_barrier`.
- **`run.log` line 20**: the run executed with `label_mode (iter-v3/105): 'trend_scanning' grid=(5, 8, 13, 21) max_h=21 <= timeout_candles=21 PASS`.
- **Comment-block tells**: `run_baseline_v3.py:1114-1118` and `1912-1916` are still tagged verbatim `iter-v3/105` and still describe the trend-scanning label as "the ONE clean variable for this iteration" — /105 prose never rewritten for /110.

This is stale state from iter-v3/105. The /105 closeout (`diary-v3/iteration_v3-105.md` line 7 + lines 322-330) explicitly mandated: "`label_mode` **REVERTS to `triple_barrier`** at the iter-v3/106 setup — a two-line config change … The trend-scanning label must not be re-proposed." iter-v3/105 closed `trend_scanning` as EXPLORATION-NEGATIVE (IS monthly Sharpe collapsed to +0.2201, hard F2 fire). iter-v3/106–109 were all NULL-AT-EDA (no runner execution), so the un-reverted `label_mode` lay dormant for four iterations. **iter-v3/110 is the first iteration since /105 to execute `run_baseline_v3.py`** — and it executed with the stale, known-NEGATIVE `trend_scanning` label.

Consequences:
1. **iter-v3/110 varied two variables, not one** — the intended universe swap AND the unintended `label_mode` carry-over. Attribution to the universe axis is destroyed: the headline IS +0.2942 is statistically indistinguishable from /105's trend-scanning IS-collapse figure +0.2201 (Δ 0.074, inside 3-seed EXPLORATION noise).
2. **The Phase-2 EDA and the backtest are on different label geometries.** `analysis/iteration_v3-110/_shared.py` replicates `labeling.label_trades` with `label_mode="triple_barrier"`. Every Section-2 table (T1-T10), the +0.25 raw gated proxy, the screen ranking that selected CRV/AAVE/GRT/ADA — all computed under `triple_barrier`. The backtest trained under `trend_scanning`. The EDA cannot predict or interpret the as-run backtest, voiding the brief's Section 2 → Section 4 inferential chain.
3. **The confound is an active interaction, not a passive additive bias.** `trend_scanning` is geometrically mismatched to the unchanged triple-barrier execution layer; the hazard acts on the new universe's per-symbol fits in an unmeasured way — it can flip the sign as well as scale the magnitude of any universe effect. No sub-result of this run is label-clean.
4. **The config-accretion guard did not catch it.** The `_canonical_v059` guard (`run_baseline_v3.py:1032-1050`) enumerates 11 knobs; **`label_mode`/`trend_scan_grid` are not among them.** The guard built to catch illegitimate config drift has a blind spot exactly where the drift occurred. The pre-flight at lines 1125-1133 *did* assert `label_mode` — but asserted the wrong target (`"trend_scanning"`), enforcing the stale state rather than catching it.
5. **The engineering report and Phase 5.5 gate missed it.** The report's "Configuration Diff vs Baseline (/059)" table asserts "All other knobs … /059-identical" — false — despite `run.log` line 20 recording the carry-over in plain text.

This is the textbook scope-creep / hypothesis-faking pattern Check 8 exists to catch: the registered hypothesis (a clean universe swap on the /059 `triple_barrier` baseline) was not the experiment that ran. The brief's pre-registered Section 8 falsifiers also fired on the as-run numbers (IS +0.2942 < +0.30; OOS −0.4455 < −0.10), so the iteration is NEGATIVE on its own criteria — but "NEGATIVE because the screened universe carries no edge" and "NEGATIVE because the runner trained on a known-NEGATIVE label" are different findings, and only a `triple_barrier` re-run can distinguish them. FAIL — verdict-driving.

## Recommendations to QR

These are for the Phase 8 diary and the iter-v3/111 setup. iter-v3/110 is NEGATIVE and the as-run measurement is void; the iteration is not salvageable by tweaking one thing.

1. **Catalogue iter-v3/110 as a label-confound NEGATIVE — axis UNRESOLVED, not closed.** The Phase 8 diary and the `briefs-v3/exploration_catalog.md` row must record iter-v3/110 explicitly as "NEGATIVE — label confound (runner trained `trend_scanning`, not `triple_barrier`); universe-selection axis UNRESOLVED pending corrected re-run." Do **not** record it as "universe-no-edge NEGATIVE" — that would retire the symbol-selection axis (explicitly on the cycle-6 axis menu per `project_v3_cycle6_axis_menu.md`) on a false attribution, exactly the error Check 8 exists to prevent. The CRV/AAVE/GRT/ADA screen is QR-led, EDA-backed, computed under `triple_barrier` — the axis *design* survives; only the as-run *measurement* is void. The 10:1 cadence accounting should treat /110 as a spent EXPLORATION slot (it consumed a runner execution) but note the axis it was meant to test remains open.

2. **Mandatory `label_mode` revert to `triple_barrier` before any further runner execution.** This is the two-line revert iter-v3/105 mandated for the /106 setup and that /106-109 (all NULL-AT-EDA) never carried. At `run_baseline_v3.py:1917-1918`: `label_mode="trend_scanning"` → `label_mode="triple_barrier"`, and remove or neutralize `trend_scan_grid=(5, 8, 13, 21)`. At the pre-flight `run_baseline_v3.py:1125`: `expected_label_mode = "trend_scanning"` → `"triple_barrier"` (and revise the lines 1126-1163 assertion/print block so the pre-flight enforces and announces `triple_barrier`). Rewrite the stale `iter-v3/105` comment blocks at lines 1114-1118 and 1912-1916. This revert is a precondition for iter-v3/111 — no v3 runner execution should occur until it is committed and the pre-flight confirms `triple_barrier`.

3. **Add `label_mode` and `trend_scan_grid` to the `_canonical_v059` accretion-guard list (`run_baseline_v3.py:1032-1050`).** The guard's purpose is to catch illegitimate config drift, and it has a blind spot precisely on the knob that drifted across four NULL-AT-EDA iterations undetected. Add two entries against the /059-canonical values (`label_mode == "triple_barrier"`, `trend_scan_grid` neutralized). Consider a sweep of all label/model knobs touched by EXPLORATIONs /072-/105 (fixed-horizon, trend-scanning, inference-threshold-floor) to confirm the guard now covers every knob any closed EXPLORATION mutated. Also clean the stale `(= (21+1)*3; ...3-sym /059 universe...)` diagnostic string at the `run.log`-source print.

4. **iter-v3/111 should be the clean re-test of the symbol-selection (universe) axis under the corrected `triple_barrier` runner — keep iter-v3/110's universe and brief intact.** Re-run the CRV/AAVE/GRT/ADA universe on the corrected runner so the EDA (computed under `triple_barrier`) and the backtest share one label geometry and the Section 2 → Section 4 inferential chain becomes valid. The re-test brief should: (a) carry a Configuration Diff that the QE verifies against `run.log` line-by-line — including an explicit `label_mode: triple_barrier` row — rather than asserting "all other knobs /059-identical"; (b) pre-register the Section 8 falsifiers afresh against the /060 cycle-1-style anchor; (c) instruct the QE to grep `run.log` for the executed `label_mode` value and quote it in the engineering report's reconciliation section, so a future label carry-over is caught at Phase 6. Note for the Phase 5.5 gate and the QE: a runner pre-flight that *asserts* a config value is only protective if the asserted target is correct — both the pre-flight target and the engineering-report diff must be re-derived from the brief each iteration, never carried forward.
