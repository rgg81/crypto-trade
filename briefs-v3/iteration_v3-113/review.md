# Phase 7.5 Critic Review — iter-v3/113 — FINAL (single round — Round 2 skipped, no clarification can change OVERALL)

OVERALL: EXPLORATION-NEGATIVE — IS monthly Sharpe collapsed Δ −0.6704 vs the /060 anchor (pre-registered C1 NEGATIVE falsifier fires); the multi-frequency daily-feature axis is implemented clean and look-ahead-free but does not convert to IS or OOS edge.

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (cycle-6 slot #4 of 10; iter-v3/120 is the mandatory cycle-6 CONFIRMATION). Verdict-driving checks: 1, 2, 4, 5, 6, 8. Check 3 informational at EXPLORATION budget.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS
The multi-frequency daily feature — the new surface — is causally clean, verified at source in `src/crypto_trade/features_v3/multifreq_v3.py`. Daily-bar `day_close` is the last 8h candle's `close_time` (= `23:59:59.999` of the UTC day). The `merge_asof(direction="backward", left_on="open_time", right_on="day_close")` matches every 8h decision candle in day D — opening at 00/08/16 — to daily bar D−1, because day D's own `day_close` (`23:59:59.999 of D`) exceeds all three intra-day `open_time`s and is not selected. Every 8h candle sees only strictly-past daily data. `allow_exact_matches=True` is moot — a `close_time` and a subsequent `open_time` differ by 1ms on the Binance grid, so the exact-match branch never fires. All 8 daily features are past-only on the daily grid (`_lag_ret`, trailing `rolling(min_periods=w)`, the `[t−9:t+1]` Kaufman loop, the `[t−win+1:t+1]` slope loop — no centered windows, no future shift). The production `_aggregate_to_daily` / `_add_daily_features` are bit-identical to the EDA's `analysis/iteration_v3-113/_shared.py` (EDA SHA `ebd84e2`). The adversarial `test_past_only_property` genuinely tests the property (appends 90 future rows from the same data, asserts row-60 daily features bit-identical to `<1e-10`); `test_day_close_alignment` independently asserts `day_close ≤ open_time` on every matched row. No look-ahead found.

### Check 2 — Embargo Width: PASS
Required cross-cell purge gap = `(timeout_candles + 1) × n_symbols = (21 + 1) × 3 = 66` (timeout 10080 min / 480 min-per-candle = 21 candles; 3-symbol BCH/LDO/TRX universe). `REQUIRED_GAP = 66`; `run.log` line 26 confirms the runner independently recomputed 66 and matched. The 22-candle walk-forward per-cell embargo (`train_end_ms = test_start_ms − embargo_ms`, the `e149e9d` fix) is inherited unchanged — this iteration touched only the feature set. Required 66, actual 66 — equal. PASS.

### Check 3 — Multiple-Testing Correction: FAIL (informational — NOT verdict-triggering for EXPLORATION)
`dsr.json`: DSR=0.0, PBO=0.1258, PSR=0.5062, n_trials=315, n_eff=19. PBO=0.1258 clears its <0.4 threshold (frac_positive_paths=0.644). DSR=0.0 and PSR=0.5062 fail their >0.95 thresholds — the mechanical consequence of a near-floor IS monthly Sharpe (+0.1621): the deflation machinery has almost no positive in-sample Sharpe to act on. Per `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode DSR/PSR at the 3-seed/35-trial budget are structural regime artifacts, not edge-significance evidence — only the PBO axis is meaningful at EXPLORATION budget, and PBO passes. Moot for the verdict. For the iter-v3/120 CONFIRMATION all three axes become hard BLOCK-triggering.

### Check 4 — IC Correlation: FAIL (strict reading) — does NOT change the OVERALL verdict
`ic_matrix.csv` (the required 22×22 matrix) shows 4 of the 8 new daily features exceed the |IC| < 0.70 family gate against the 14 existing features: d_ret_5d 0.849 (regime_momentum_signed_5d), d_ret_10d 0.778 (ema_spread_atr_20), d_realvol_10 0.804 (range_realized_vol_50), d_close_pos_20 0.852 (ema_spread_atr_20). The other 4 (d_trend_slope_10, d_realvol_ratio, d_atr_pctrank_60, d_efficiency_10) are clean (max |IC| < 0.30). The runner's matrix reproduces the brief's T7 table exactly. The brief Section 4.4 invokes the `feedback_v3_engineered_feature_pivot.md` carve-out — but that carve-out is scoped explicitly to **Category 2 algebraically-composed features** (`feature_A × sign(feature_B − threshold)`, sharing variance with primitives BY CONSTRUCTION). A coarser-frequency aggregation of the same OHLCV is not algebraically composed from an 8h feature — there is no formula `d_ret_5d = f(8h features)`; the 0.849 correlation is empirical, not constructive. By the carve-out's own text, a non-composed feature is Category 1 and "the IC hard gate < 0.70 applies in full." Check 4 is therefore FAIL on the strict reading. It does not change the OVERALL verdict — the iteration is EXPLORATION-NEGATIVE on the IS-Sharpe axis (C1, Δ −0.6704) regardless of how Check 4 resolves, and the carve-out's replacement gate (importance ≥ 30 for ≥ 1 symbol) is independently met (the 8 daily features carry importance 24-306 across symbols; the model consulted them). The carve-out-scope question is forwarded as a process recommendation.

### Check 5 — ADF Stationarity: PASS
`adf_test.csv` present with the required schema. At training-window end (month 2025-03) all 8 new daily features are stationary at p ≈ 0 on all three symbols (BCH 0.0–1e-06, LDO ≤2e-06, TRX 0.0). Blank `adf_statistic`/`p_value` cells are confined to first-history months — the documented insufficient-history could-not-test sentinel; `run.log` line 49 reports 84.9% of cells stationary, in the expected range. The 8 daily features are scale-invariant log-return / ratio / percentile-rank / channel-position constructs and pass ADF cleanly. PASS.

### Check 6 — Pareto Dominance: PASS (non-applicable)
Not applicable to an EXPLORATION under the unified-ensemble architecture — `pareto_front.csv` is no longer produced (confirmed absent from the report directory); the unified architecture yields one deterministic trade roster. iter-v3/113 ran `--exploration` (ENSEMBLE_SIZE=3, the outer=42 seed lineage, per `ensemble_summary.json`). The 10-seed Pareto validation is a CONFIRMATION concern (iter-v3/120).

### Check 7 — Reproducibility: PASS
Four properties verified: (a) code-pre-run SHA `d4dcce6` and report HEAD SHA `0bd50fd` stamped; (b) explicit 22-element `feature_columns` list passed to every model (`run_baseline_v3.py:1925`), never `None`, hard-asserted `n == 22` at `:432`, confirmed `feature-cols=22 PASS` in `run.log`; (c) ensemble seeds literal in `ensemble_summary.json`; (d) PnL spot-check — Trade 1 (BCH SHORT) net_pnl_pct 7.0640 / weighted_pnl 2.3311 / 2:1 ATR ratio 2.0000; Trade 8 (TRX LONG) net_pnl_pct 4.2418 / weighted_pnl 3.9449 / 2:1 ATR ratio 2.0002. No sign or off-by-one errors. PASS.

### Check 8 — Hypothesis-Implementation Alignment: PASS
The 22-feature multi-frequency stack genuinely ran with no scope creep, verified at source. The `multifreq_v3` module added all 8 daily features — `run.log` line 3 enumerates them by name, line 64 confirms `22 feature columns`, the module is registered in `GROUP_REGISTRY` (`features_v3/__init__.py:118`) and appended to `V3_FEATURE_COLUMNS_TOP_N` in brief-mandated order. The per-symbol architecture was restored: `V3_MODELS` has three distinct labels so `_is_pooled` (`run_baseline_v3.py:2550`) evaluates `False` and the per-symbol loop runs three separate models — `run.log` shows three `MODEL v3-113-{BCH,LDO,TRX}` blocks and no `[POOLED]` string, confirming the /112 pooled build was reverted. `label_mode=triple_barrier` preserved (`run.log` line 21). Exactly one substantive knob changed (`V3_FEATURE_COLUMNS` 14 → 22); track isolation holds (`multifreq_v3.py` imports only numpy/pandas; `run.log` line 27 `Track isolation: PASS`). The NEGATIVE is a genuine, attributable property of the axis. PASS.

## SUSPICIOUS Flag — Critic Adjudication
The engineering report's SUSPICIOUS flag (IS/OOS daily Sharpe ratio 0.0093, outside [0.5, 2.0]) is adjudicated NOT SUSPICIOUS-as-overfitting. The SUSPICIOUS taxonomy catches OOS-dominant overfitting (OOS Sharpe materially exceeds IS); here the inverse holds — IS Sharpe (+0.1621 monthly) is itself near-floor and OOS Sharpe (+0.0015 monthly) is near zero. The ratio is small because the OOS numerator is ~0, not because OOS is anomalously strong. This is a flat-everywhere result correctly subsumed by the NEGATIVE-AT-EXPLORATION classification (IS Δ −0.6704); the flag adds no independent signal.

## Clarifications Requested from QR — NONE
Every verdict-driving check is unambiguous. Check 4 is recorded FAIL on the strict reading, but no QR response can change the OVERALL verdict: Section 8 criterion C1 fails at Δ = −0.6704, firing the pre-registered NEGATIVE falsifier. Round 2 skipped.

## Recommendations to QR

(Process-level fixes for FUTURE iterations — this iteration's EXPLORATION-NEGATIVE verdict is final.)

1. **The `feedback_v3_engineered_feature_pivot.md` IC carve-out should not be stretched to cover coarser-frequency aggregations.** The carve-out is scoped to algebraically-composed Category 2 features; a daily aggregation correlating empirically with an 8h feature is a Category 1 feature for which the |IC| < 0.70 gate applies in full. For iter-v3/120 or any future multi-frequency axis, either (a) ship only the orthogonal daily features (the brief's T9/T11 annex already tested the orthogonal-4 subset — though it inverted the per-symbol gated-tail lift, itself evidence the redundant features add no independent signal), or (b) request a formal skill amendment extending the carve-out to "different-frequency transforms of the same primitive" with explicit reviewer sign-off. Do not carry the 4 high-IC daily features (d_ret_5d, d_ret_10d, d_realvol_10, d_close_pos_20) into the iter-v3/120 CONFIRMATION bundle on the strength of a contested carve-out.

2. **The behavioral-inertia falsifier fired (IS roster Δ 1.89% < 8%) — treat the axis as behaviorally inert and do not retest at higher Optuna budget.** The IS trade roster changed only 1.89% vs the /060 roster (156 vs 159), below the brief's pre-registered 8% threshold (Section 7 Mode 3). The 8 daily features carried non-trivial importance but barely shifted the confidence ranking near the gate threshold — the gated roster is materially the 14-feature roster. Per `feedback_v3_inert_features_at_higher_budget.md`, a behaviorally-inert axis at n_trials=35 must not be carried forward or retested at a higher budget. The multi-frequency-daily axis should be recorded CLOSED in the catalog and the 8 daily features dropped — same footing as /085's funding family and /086's basis family.

3. **For cycle-6 EXPLORATION slots 5-10, prefer an axis whose pre-registered behavioral-effect predictor is robustly above the inertia floor.** /113 is the modal "real-but-small standalone signal washes in the combined stack" outcome the brief Section 7 pre-registered — and the standalone-vs-combined gap (EDA T5 daily-only AUC 0.5275 clears its permutation null; T4 combined 8h+daily AUC 0.5015 does NOT) was visible in the EDA before the backtest. When the EDA's combined-stack horse race fails its own permutation null, the brief's predicted-effect interval should be centered at or below zero and the axis treated as low-probability. Cycle-6 slots 5-10 should pick axes where the EDA's combined-stack test (not just a standalone-feature test) clears its permutation null, so the EXPLORATION slot is not spent confirming a pre-visible wash.
