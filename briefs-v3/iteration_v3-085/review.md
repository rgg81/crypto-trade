# Phase 7.5 Critic Review — iter-v3/085

OVERALL: EXPLORATION-MERGE — methodology sound; iteration is a clean closeout-integrity certification of a non-advancing INERT result.

## Iteration Type (from Brief Section 0.5)

TYPE: EXPLORATION (cycle-3 EXPLORATION #4 of 10). Single-axis variation: ONE Category-2 composed feature `funding_regime_momentum_5d = regime_momentum_signed_5d × sign(funding_z_30)` appended to `V3_FEATURE_COLUMNS_TOP_N` (14→15). EXPLORATION-mode 3-seed, n_trials 35. Per `feedback_v3_dsr_mode_artifact.md`, Check 3 edge axes (DSR/PSR) are informational for EXPLORATION; only the Check 3 PBO axis and the CPCV `frac_positive_paths` gate are evaluated against thresholds. OVERALL reviews METHODOLOGY soundness — an INERT result on sound methodology is MERGE.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

The sole new feature `funding_regime_momentum_5d` was traced through `compute_funding_regime_momentum_5d` (`engineered_v3.py:946-1030`). Three legs audited:

1. **`regime_momentum_signed_5d`** (the momentum leg) — pre-existing verified-past-only column: `ret_5d = log_close − log_close.shift(15)`; `hurst_100` is a 100-bar trailing R/S window. No look-ahead.
2. **`funding_z_30`** (the regime leg) — `engineered_v3.py:1016-1022`. The denominator and rolling-mean window use `fr_lag = r.shift(1)` — bar t's own funding settlement never enters its own rolling window. The numerator uses `r` (raw `funding_rate[t]`). This `rate[t]`-in-numerator construction is the **identical convention** used by the production `compute_funding_rate_zscore` (`funding_v3.py:131-139`) and `compute_funding_family` (`funding_v3.py:444-459`), enforced by `tests/features_v3/test_funding_v3.py`. The justification — `funding_rate[t]` settled at candle `open_time` and is broadcast ~5 min before the 8h boundary, hence knowable at bar t open — is the documented v3 funding-feature convention (`funding_v3.py:17-29` module docstring), not a new claim. Consistent.
3. **The composed product** (`:1027-1029`) — element-wise product of two past-only series.

The mandated spike-perturbation test (`test_funding_regime_momentum_v3.py::test_past_only_no_lookahead`, lines 136-172) was verified independently: it perturbs `funding_rate[250]` by +5.0 and asserts every bar `< 250` is bit-identical (`pd.testing.assert_series_equal`), and confirms the perturbation propagates at/after bar 250. A second past-only test (`test_funding_z_window_shift_is_past_only`, bar 150) corroborates. Both tests match the actual implementation's leakage geometry — a spike at bar k can affect bar k (k is in its own numerator) and bars > k (the `.shift(1)`-lagged window sees k from k+1 onward), never bars < k. No look-ahead.

Minor note (non-defect): `compute_funding_regime_momentum_5d` does NOT clip the z-score to [-10, 10], unlike `compute_funding_rate_zscore`. This is harmless — /085 consumes only `sign(funding_z_30)`, and `sign()` of a clipped value equals `sign()` of the unclipped value. The funding-floor outlier concern that motivates clipping in the standalone z-score feature does not apply to a sign extraction.

### Check 2 — Embargo Width: PASS

Labeling: triple-barrier, `label_timeout_minutes = 10080`, 8h candles → `timeout_candles = 10080/480 = 21`. Required cross-cell gap = `(timeout_candles + 1) × n_symbols = (21+1) × 3 = 66`. Actual: `validation_v3.py:65` sets `REQUIRED_GAP = (21+1)*3 = 66`; the runner asserts it via `_verify_label_leakage_gap` (`run_baseline_v3.py:2590`) and the `_canonical_v059` 11-knob check (`:944`). The per-cell single-symbol CSCV purge gap is `PER_CELL_GAP = 22 = (timeout_candles+1)` (`run_baseline_v3.py:1542`), correctly the single-symbol value, and the per-cell `combinatorial_purged_cv` call passes `expected_gap=PER_CELL_GAP` (`:1638-1640`) so the constant cannot silently drift. Both the cross-cell and per-cell calls carry self-asserting `expected_gap` guards (`validation_v3.py:119-123`).

The mandatory-focus point #4 is confirmed: PER_CELL_GAP=22 is in effect (this is the first non-/084 iteration on the corrected value; the /083→/084 fix of the stale 43 landed permanently). The per-cell PBO is therefore computed with the correct single-symbol purge.

### Check 3 — Multiple-Testing Correction: PASS (informational for EXPLORATION; per-cell PBO clears threshold)

`dsr.json`: DSR=0.0, PSR=1.0, per-cell-mean PBO=0.0921, `frac_positive_paths`=0.6444, n_trials=315, n_eff=19. DSR=0.0 and PSR=1.0 are explicit EXPLORATION-mode structural artifacts (n_trials=315 → E[max_SR] greatly exceeds the observed annualized SR) and are informational only per `feedback_v3_dsr_mode_artifact.md` — they do NOT trigger BLOCK for an EXPLORATION iteration. The CONFIRMATION-mode DSR gate is reserved for iter-v3/092.

The two Check-3 quantities that ARE meaningful at any mode: per-cell PBO and CPCV `frac_positive_paths`. Per-cell PBO 0.0921 is far below the 0.40 threshold. CPCV `frac_positive_paths` 0.6444 clears the v3 0.55 gate (`cpcv_frac_positive_paths_gate_pass: true`).

One discrepancy I flag for the record (not BLOCK-relevant): I independently summed the 127 per-cell PBO values in `per_cell_pbo.csv` and obtained a mean of ≈0.102, against the reported 0.0921. The `dsr.json` `pbo_note` attributes the figure to an "iter-v3/004 cross-cell mean" aggregation convention, which may weight or subset differently than a flat 127-row mean — and hand-arithmetic over 127 floats carries its own error. The methodological point survives the discrepancy regardless: whether 0.092 or 0.102, both clear 0.40 with a wide margin, the per-cell gap is the corrected 22, and the binding v3 CPCV gate (`frac_positive_paths`) passes. Recommendation #2 below asks the QR to confirm the aggregation. n_trials=315 matches the EXPLORATION budget (35 × 3 seeds × 3 symbols), confirmed in `ensemble_summary.json` and the run.log.

### Check 4 — IC Correlation: PASS

`ic_matrix.csv` is present (15×15, pooled-Pearson). The sole new feature `funding_regime_momentum_5d`: max |IC| against the 14 anchors = **0.0840** (vs `range_realized_vol_50`); |IC| against its own primitive `regime_momentum_signed_5d` = **0.0065**. Both far below the 0.50 strict target and the 0.70 hard gate — the strict gate passes outright, so the `feedback_v3_engineered_feature_pivot.md` Category-2 IC carve-out is not even needed. The near-zero correlation with the primitive confirms the funding sign-switch genuinely restructures the feature rather than rescaling it (a sign-multiply on a roughly symmetric two-sided funding-z — 35-58% each side per EDA T4 — decorrelates the product from its momentum input by construction). The iter-v3/070 colsample-theft trap does not apply. Collinearity is not the issue with this feature; importance rank is (see Check 8 and the central adjudication).

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` checked at the **last IS retraining month (2025-03)** for all 15 features × 3 symbols. Every cell returns `stationary=True` with p < 0.05. `funding_regime_momentum_5d` specifically: BCH ADF −8.47 (p=0.0), LDO −12.43 (p=0.0), TRX −8.12 (p=0.0) — strongly I(0), as expected for a bounded sign times a stationary momentum series. The 402 non-stationary rows the run.log reports (1953/2355 stationary) are early-month warm-up cells with empty `adf_statistic`/`p_value` (insufficient history to run the test) — not stationarity failures of mature features. No feature is non-stationary at the end of the training window.

### Check 6 — Pareto Dominance / CPCV: PASS

This is a 3-seed EXPLORATION-mode run; the 10-seed `pareto_front.csv` does not exist and is not expected (the 10-seed Pareto front is a CONFIRMATION artifact — `feedback_v3_outer_seed_cap_2_v3.md` and the EXPLORATION/CONFIRMATION split). The EXPLORATION-mode robustness check is the CPCV path distribution: `cpcv_paths.csv` shows 45 return-proxy paths with `path_sharpe` q25/q50/q75 = −0.243 / +0.335 / +0.838 and `frac_positive_paths` = 0.644, clearing the 0.55 gate. The path distribution is plausible for a marginal-edge 3-symbol portfolio — a real spread of positive and negative paths, not a degenerate all-positive or all-negative set. `ensemble_summary.json` confirms the 3 seeds (191664963, 1662057957, 1405681631), all `lineage: outer=42`. No seed-selection pathology.

### Check 7 — Reproducibility: PASS

(1) Report commit SHA `d1f8b36` stamped in the engineering report; setup `ae38f1e`, gate `0a67b71`. (2) The runner passes `feature_columns=list(features_for_symbol(symbol))` (`run_baseline_v3.py:1785`) — explicit, never `None`; `V3_FEATURES_PER_SYMBOL` is empty so all 3 symbols resolve to the 15-feature `V3_FEATURE_COLUMNS_TOP_N`, and `_verify_feature_columns` asserts `len==15` + the new column present by literal. (3) The 3 EXPLORATION ensemble seeds are recorded literally in `ensemble_summary.json` and echoed in the run.log banner. (4) Trade spot-check: I verified `out_of_sample/trades.csv` row 2 — BCH short, entry 303.870000, exit 282.100779, `(303.87−282.10)/303.87 = 7.164%` gross, `pnl_pct=7.1640`, `net_pnl_pct=7.0640` after the 0.1% fee, `weighted_pnl = 7.0640 × 0.3300 = 2.3311` — matches to displayed precision. Row 3 (BCH short, stop_loss, −4.6402 net × 0.55 = −2.5521) also matches. Sign/weight/fee arithmetic is correct.

One stale-comment note (non-defect, recommendation only): `validation_v3.py:594` — the docstring of the CPCV iterator still reads "Default gap = REQUIRED_GAP = ... = 88 (4-symbol BCH+LDO+TRX+ADA universe)". The **constant** `REQUIRED_GAP` is correctly 66 (`:65`), the `_canonical_v059` check asserts 66, and the `expected_gap` self-assertion would catch a real mismatch — so the run is not affected. But the docstring is a leftover from the reverted /069 4-symbol era and contradicts the live constant. Cosmetic; flagged for cleanup.

### Check 8 — Hypothesis-Implementation Alignment: PASS

Brief Section 1/3.1 declares exactly one axis: append `funding_regime_momentum_5d = regime_momentum_signed_5d × sign(funding_z_30)` to `V3_FEATURE_COLUMNS_TOP_N`, count 14→15, nothing else. The implementation matches precisely:

- `engineered_v3.py` adds `compute_funding_regime_momentum_5d` + `add_funding_regime_momentum_v3_features` — the formula in code (`:1027-1029`) is the brief's formula exactly.
- `features_v3/__init__.py` — `funding_regime_momentum_5d` is the 15th entry of `V3_FEATURE_COLUMNS_TOP_N` (`:208`), inserted directly after its primitive `regime_momentum_signed_5d`; `GROUP_REGISTRY` registers `funding_regime_momentum_v3` after `engineered_v3` (`:85`), satisfying the dependency order.
- `run_baseline_v3.py` — `ITERATION_LABEL = "v3-085"` (`:131`); the `_canonical_v059` 11-knob guard (`:937-968`) asserts all RiskV3 config knobs (ATR multipliers, z-score threshold, ADX threshold, per-symbol overrides, block lists, drawdown brake) equal /059-canonical. **No scope creep** — no risk-gate, labeling, symbol, model-architecture or seed change. The 4 closed `funding_family` columns (`funding_sign_persist_9`, etc.) are explicitly asserted ABSENT per-symbol (`:602-613`); the closed Category-1 names `funding_rate_zscore_30` / `btc_funding_rate_zscore_30` appear only in comments, never as feature-tuple entries.

The iteration tested exactly the registered hypothesis. No hypothesis-faking.

## Optional Checks 9-12

- **Check 9 — Symbol Exclusion Enforcement: PASS.** `V3_EXCLUDED_SYMBOLS` (`features_v3/__init__.py:441-457`) is enforced; the universe is the 3-symbol BCH/LDO/TRX set, and the `_canonical_v059` check asserts `V3_MODELS symbols == ("BCHUSDT","LDOUSDT","TRXUSDT")`.
- **Check 10 — Feature Isolation: PASS.** `engineered_v3.py` and `features_v3/__init__.py` contain no imports from `crypto_trade.features` (v1) or `crypto_trade.features_v2` (v2); the new funding-regime code reads only `data/funding_rates/<SYMBOL>.csv`. The engineering report records the Phase-6 isolation grep as clean.
- **Check 11 — Forming-Candle Audit: PASS (by report).** The run.log records the data-freshness guard ("data fresh (<16h)") passing pre-flight. IS row counts (BCH 6983 / LDO 3997 / TRX 6925) are consistent with the declared windows. No forming-candle anomaly surfaced; the trade spot-check showed no degenerate tail rows.
- **Check 12 — Library Version Pinning: PASS.** Brief Section 9 declares no new libraries; the feature is pure numpy/pandas arithmetic on the /059-canonical pinned stack (lightgbm 4.6.0, optuna 4.8.0, numpy 2.2.6, pandas 3.0.0, scikit-learn 1.8.0, scipy 1.17.0, statsmodels 0.14.6, pyarrow 23.0.1).

## The Central Adjudication — INERT-by-importance vs the +0.142 IS lift

This is the mandatory-focus point #1 and the question the QR's Section 8 must resolve. My read, as forensic input:

**The funding-regime axis is INERT-by-importance.** `funding_regime_momentum_5d` ranks **13/15 (BCH), 14/15 (LDO), 15/15 (TRX)** by last-IS-month 3-seed-mean importance (portfolio rank 15/15), share ~2.9-4.1%. The brief's PROMISING binding gate (Section 8.1: rank ≤10 AND absolute importance ≥30 on ≥1 symbol) **FAILS on the rank leg on all three symbols** — no symbol clears rank ≤10. The brief's INERT clause (Section 8.4: rank ≥14 on ≥2/3 symbols) **FIRES** — LDO 14 and TRX 15 both satisfy rank ≥14. The absolute-importance leg passes everywhere (59/78/55, all >30), but absolute importance >30 only confirms the trees allocated a non-trivial number of *splits* to the column for partition efficiency — it is not evidence of NEW signal when the column still places dead-last in the ranked distribution. This is the exact pattern the QE's own report concedes (engineering report, "THE CORE SECTION" and Anomaly Notes).

**The +0.142 IS lift is not attributable to the funding-regime feature.** The brief pre-registered (Section 7) PROMISING-INERT at ~45% as the single most-likely outcome, precisely because the T3 quick-probe ranked the feature 14.0/15 mean with −0.0016 mean accuracy lift. The result landed exactly there. The mechanism is the documented `feedback_v3_inert_features_at_higher_budget.md` pattern: adding a 15th (effectively unused) column expands the Optuna hyperparameter search space, and at 3-seed single-lineage EXPLORATION resolution the perturbed search happens to land a favorable IS hyperparameter draw. This is the same mechanism /082 exhibited — there the 4-channel funding family ranked bottom-4/18 and IS/OOS moved without the family being used. The /085 IS lift (+0.142) is larger than /082's IS move (−0.012), but a feature ranked 13/14/15-of-15 cannot have *produced* +0.142 of IS Sharpe; an unused column has no path to the objective except via the search-space-perturbation side channel. The IS lift is an artifact, not signal.

**Does the INERT outcome close the funding axis in all constructions?** The Phase 5.5 gate's funding-differentiation adjudication was *legitimate* — `funding_regime_momentum_5d` is structurally distinct from the closed Category-1 funding-as-direct-feature axis: funding enters only as a `sign()` switch inside a composed feature, never as a splittable column, so the closed-axis failure mode ("the tree never splits on raw funding") genuinely cannot fire here. The differentiation was real, and running the EXPLORATION to test it was correct. But the *result* is now the fifth INERT funding data point — /019, /023, /024 (Category-1 direct z-scores), /082 (the 4-channel direct family), and now /085 (the Category-2 sign-switch construction). Funding-as-direct-feature was already closed; /085 now closes funding-as-regime-conditioner-inside-a-composed-feature as well. Across two structurally orthogonal constructions the model declines to allocate ranked importance to funding-derived information on the BCH/LDO/TRX universe. I would treat the v3 funding axis as **closed in all feature constructions** absent a fundamentally different vehicle (a different data feed entirely — open interest, basis, liquidations — or a non-tree model that can compose the interaction differently). That is a recommendation for the QR's catalog bookkeeping, not a Critic verdict.

**Classification call (the final Section 8 decision is the QR's):** /085 should be classified **INERT** (the brief's Category-2 PATH B / PROMISING-INERT). The disjunctive precedence in Section 8 runs SUSPICIOUS → NEGATIVE → PROMISING → INERT → NULL-RESULT. SUSPICIOUS does not fire (OOS/IS ratio 0.293 < 3.0; IS Δ +0.142 > 0 so the OOS-DOMINANT sub-mode is foreclosed; the /076 trade-selection sub-channel is a QR Phase-7/8 roster-diff check, not yet adjudicated here — flagged for the QR). NEGATIVE does not fire (IS Δ +0.142 ≥ −0.10; OOS Δ −0.047 ≥ −0.20). PROMISING is mechanically foreclosed — its binding importance gate fails on the rank leg on all three symbols. INERT fires unambiguously on the explicit Section 8.4 rank clause (rank ≥14 on LDO+TRX). It is a non-advancing result: it does not enter the iter-v3/092 CONFIRMATION bundle, and the feature must be dropped at the /086 setup per `feedback_v3_inert_features_at_higher_budget.md` (an INERT feature is not carried forward and not retested at higher budget).

**Why OVERALL=MERGE despite an INERT, non-advancing result.** The Critic's OVERALL verdict certifies METHODOLOGY soundness, not edge discovery. All 8 mandatory checks and all 4 optional checks PASS. The look-ahead discipline is correct and tested; the embargo arithmetic is correct with self-asserting guards; the IC orthogonality is genuine; the implementation matches the brief with zero scope creep; the result is exactly what the brief pre-registered as the most-likely failure mode. An INERT result honestly measured and honestly reported is a clean closeout — the merge here is a closeout-integrity certification, the same disposition the Critic issued for /082 (EXPLORATION-MERGE on a SUSPICIOUS, non-advancing axis). There is no methodology defect to BLOCK on.

## Recommendations to QR

For the cycle-3 sequence (these are process-level items for /086 and the /092 CONFIRMATION — /085 itself is final):

1. **Drop `funding_regime_momentum_5d` at the /086 setup and add the runner `ABSENT` ban.** Per `feedback_v3_inert_features_at_higher_budget.md`, an INERT feature is not carried forward and not retested at higher budget. The /086 setup should revert `V3_FEATURE_COLUMNS_TOP_N` to the 14-feature /059 anchor and add `funding_regime_momentum_5d` to the runner's pre-flight ABSENT-assertion list (the established `vol_normalized_ret_5d` / `range_efficiency_50` pattern), so a future iteration cannot silently re-inherit it. Record the v3 funding axis as a **5-data-point closed verdict** (/019/023/024/082/085) in BASELINE_V3.md Dead Ideas — now closed across both the direct-feature and the composed-sign-switch construction; future "funding" axes require a different data feed (OI/basis/liquidations) or a non-tree model, not another feature construction on the same funding rate.

2. **Confirm the per-cell-PBO aggregation in the /085 diary.** My independent 127-row flat mean of `per_cell_pbo.csv` came to ≈0.102 against the reported 0.0921; the `pbo_note` cites an "iter-v3/004 cross-cell mean" convention. The diary should state the exact aggregation (flat mean vs n_candles-weighted vs subsetted) so the figure is reproducible. The verdict is unaffected — both values clear 0.40 — but the number should be traceable.

3. **Clean up the stale `validation_v3.py:594` docstring** at the next runner-touching setup. The CPCV-iterator docstring still describes the reverted /069 4-symbol gap=88 universe while the live `REQUIRED_GAP` constant is correctly 66. The constant and the `_canonical_v059`/`expected_gap` guards are right, so the run is sound — but the contradiction is exactly the kind of stale-comment drift the /083 Critic flagged for the PER_CELL_GAP literals; fix it before it misleads.

4. **For the /086 EXPLORATION axis, pivot away from feature constructions on existing OHLCV/funding data.** Cycle 3 is 4/10 with 0 clean PROMISING (/082 SUSPICIOUS, /083 NEGATIVE, /084 REFERENCE, /085 INERT). The cycle-3 plan's Direction 2 (universe expansion — denominator expansion, distinct from the closed swap-by-replacement family) and Direction 3 (multi-symbol-pooled model) remain the structurally highest-value untried axes; /085's own EDA falsified the *naive* pooled model but the plan's Direction 2 is independent of that. A fifth consecutive feature-construction EXPLORATION on the same 3-symbol parquets risks the cycle-1/cycle-2 null-result pattern the cycle-3 mandate exists to break.
