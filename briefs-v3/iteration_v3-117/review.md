# Phase 7.5 Critic Review — iter-v3/117

OVERALL: EXPLORATION-NEGATIVE — Section 8 Criterion 1 (NEGATIVE) fires decisively on BOTH arms (IS −2.17 << +0.7325 floor; OOS −3.26 << −0.0597 floor); the pre-registered explicit falsifier ("OOS < −0.20 AND IS < +0.30") fires on both axes; the 24h-multi-offset hypothesis is FALSIFIED at production scale; the worst /117 result in cycle-6 history.

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (single structural axis: candle frequency 8h → 24h-multi-offset)

## QR Response Considered (Round 2)

1. **Clarification 1 (CV-gap under-sizing)** → QR CONCEDES the code defect. The brief Section 5 Risk 2 mitigation prose ("LightGBM walk-forward cv computes a fold-boundary gap of `8 daily bars × 3 = 24 daily bars per offset = 72 panel rows`") described INTENDED behavior, not IMPLEMENTED behavior. `lgbm.py:498` computes `cv_gap = embargo_candles × n_symbols = 8 × 1 = 8 rows` because `n_symbols=1` at per-symbol training and the `n_offset_series` multiplier is invisible to `LightGbmStrategy` (offset interleaving happens at the data-load layer). QR accepts Check 2 FAIL in full; pre-registers the /118 setup-commit fix ONLY-IF /118 uses multi-offset architecture. Check 2 FAIL stands; impact characterization (IS-direction biased; OOS collapse driven by Mechanisms 1-3 + refined Mechanism 4) is accepted as accurate.

2. **Clarification 2 (offset_id IC matrix omission)** → QR concedes oversight (not intentional). The reporting path iterates `V3_FEATURE_COLUMNS` (14-list) directly, missing the runner-extended `_feature_columns = V3_FEATURE_COLUMNS + ["offset_id"]`. Ruled DOCUMENTATION DEFECT (not methodology defect) because offset_id has bounded variance ({0,8,16}) producing bounded continuous-IC (≈ ±0.2 by variance-partition argument) AND LightGBM importance rank 15/15 at ~1% weight independently establishes non-load-bearing status. Check 4 WARN stands; one-line fix flagged for /118 setup if /118 also runs multi-feature-column architecture.

3. **Clarification 3 (Mechanism 4 refinement)** → QR concurs in full. Engineering report prose ("z-score gate calibrated on 8h IS statistics") is mechanically incorrect; per-symbol mean/std is rebuilt at every training-month boundary from the 24h-multi-offset IS slice. Refined mechanism (pooled-across-3-offsets std inflation + LDO small-sample noise + z=2.0 inappropriate for pooled tail) accepted; the over-fire is structurally induced by the 3-offset pool, NOT a separable threshold knob. This strengthens — not weakens — the FALSIFIED verdict because the over-fire is intrinsic to the multi-offset architecture.

4. **Clarification 4 (Frequency-axis closure scope)** → QR adopts BROADLY-CLOSED-FOR-CYCLE-6 stance with explicit reasoning. The /116 diary Section 9 Mode 4 RESIDUAL fallbacks (12h-2-offset, weekly-8-offset) are de-prioritized because the three confounders ((a) BCH 99.12% imbalance on alt-cohort 24h+ volatility profiles, (b) pooled-multi-offset z-score over-fire structurally present at ANY multi-offset cadence, (c) offset_id rank 15/15 is the falsifying observation for daily-or-longer aggregation on the cohort) all recur at 12h and weekly architectures. Critic ACCEPTS this closure scope: the 3-offset design was the BEST CASE for the candle-frequency hypothesis (maximum information + 3× training density), and offset_id-non-learning at ~1% importance is dispositive evidence that daily-frequency aggregation provides no separable signal lift on the BCH/LDO/TRX cohort. The Mode 4 RESIDUAL fallback paths were predicated on /117 filing as BEHAVIORAL-INERTIA (≤30 IS trades); /117 filed Mode 1 (170 IS trades, decisive Criterion 1 fire), so the fallback path activation condition did NOT fire. Frequency axis CLOSED for cycle 6 in full.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

`src/crypto_trade/features_v3/multioffset_24h.py` is causally clean. The QR-ported aggregation (`aggregate_to_24h()` using `groupby("bar_open_time")`), per-offset feature isolation (`compute_features_24h()` operating one-offset-at-a-time), BTC cross-asset joins on matching-offset panels (`bar_close_time` exact-equality), and past-only ATR labeling (`pd.Series(tr).rolling(21, min_periods=21).mean()`) all verified. The EDA's T4 audit (900/900 PASS) validates the aggregation primitive; the QE's Change 8 adversarial integration test (3 sub-tests including per-offset feature isolation) validates the production code path. No look-ahead leak in the aggregation, feature, or labeling pipeline. PASS.

### Check 2 — Embargo Width: FAIL

The brief's stated `REQUIRED_GAP = 72 = (timeout_candles+1) × n_symbols × n_offset_series` correctly sizes the panel-level cross-cell CPCV embargo at the global CPCV path. The walk-forward train-end-time embargo at `walk_forward.py:91-113` (8 daily bars = 8 calendar days = 691_200_000 ms) is sufficient at the WITHIN-OFFSET level. **However, the per-trial inner Optuna CV gap at `lgbm.py:494-498` is NOT panel-aware and is silently undersized**: `cv_gap = embargo_candles × n_symbols = 8 × 1 = 8 rows = 2.67 daily bars`, but the 24h-multi-offset panel has 3 rows per calendar day and the forward-label horizon is 7 daily bars = 21 panel rows. Within each per-symbol Optuna CV fold, the last ~13 rows of the training fold have forward-label deadlines reaching ~4.33 daily bars into the validation fold (the 7-daily-bar forward-scan window minus the 2.67-daily-bar gap). The brief's Risk 2 mitigation prose ("LightGBM walk-forward cv computes a fold-boundary gap of 8 daily bars × 3 = 24 daily bars per offset = 72 panel rows") described intended behavior, not implemented behavior — the `n_offset_series` multiplier is invisible to `LightGbmStrategy` because offset interleaving happens at the data-load layer. Log evidence confirms: `CV gap: 8 rows` followed by `gap=72h (8 rows)` across all 5 CV folds. QR conceded in Round 2 that this is a code defect requiring `n_offset_series` parameter threading. Expected impact: IS metrics biased upward by within-fold cross-offset label leakage; does not by itself explain OOS collapse (which is dominated by Mechanisms 1-3 + refined Mechanism 4). FAIL.

### Check 3 — Multiple-Testing Correction: FAIL (informational for EXPLORATION)

DSR = 0.0, PBO = 0.0, PSR = 0.0. PBO=0 is a degenerate result (100% of 128 cells at 0). frac_positive_paths = 0.60 (above 0.55 gate). The DSR=0.0/PSR=0.0 are EXPLORATION-mode structural artifacts per `feedback_v3_dsr_mode_artifact.md`; informational only, NOT BLOCK-triggering for TYPE=EXPLORATION. Per the v3 EXPLORATION protocol, only Check 3 PBO axis matters at BLOCK level for EXPLORATION iterations — PBO=0 at 100% of cells with the catastrophic Sharpe collapse is structurally consistent with an unviable hypothesis (no cells produce sufficient signal to differentiate over PBO's CSCV permutation). Informational FAIL.

### Check 4 — IC Correlation: WARN

`ic_matrix.csv` is 14×14 — the runner-appended `offset_id` feature is NOT in the matrix. QR conceded in Round 2 that this is a one-line documentation defect at the IC-matrix writer (iterates `V3_FEATURE_COLUMNS` instead of runtime `_feature_columns`). Methodology-level inert because: (i) offset_id has only 3 discrete values {0, 8, 16} → continuous-IC bounded by ≈ ±0.2 by variance-partition argument, (ii) LightGBM importance rank 15/15 at ~1% of leading-feature weight independently establishes non-load-bearing status, (iii) the 14-feature matrix correctly surfaces the pre-existing /059 `regime_momentum_signed_5d × vwap_dev_20 = 0.7642` composed-feature carve-out per `feedback_v3_engineered_feature_pivot.md`. Documentation defect, not methodology blocker. WARN.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` shows 1803/2198 stationary (82.0%) at the runner level. EDA-level T7 shows 17/42 (40.5%) at the IS-end snapshot — significantly lower than 8h baseline's typical ~100% — but the runner ADF runs at per-(symbol, feature, retraining_month) granularity where each retraining-month observes the most-recent feature window (closer to IS-end where 200-bar lookbacks have stabilized). Non-stationary cells concentrate in early IS months (warm-up artifact). Brief Section 5 Risk 3 correctly documents the warm-up artifact pattern; the runner's `min_periods` enforcement excludes warm-up rows from training. The 40.5% EDA / 82.0% runner gap is the warm-up-attenuation expected at 24h timescale. PASS.

### Check 6 — Pareto Dominance: N/A (single-seed EXPLORATION)

No `pareto_front.csv`. EXPLORATION mode uses `ENSEMBLE_SIZE=3` inner seeds with NO outer-seed loop per `feedback_v3_outer_seed_cap_2_v3.md`. Architecturally correct for EXPLORATION. N/A.

### Check 7 — Reproducibility: PASS

Commit SHA stamped (`ac2c9f6...`). `feature_columns` is explicit: 24h-runner constructs `_feature_columns = V3_FEATURE_COLUMNS + ["offset_id"]` and passes via `common_kwargs["feature_columns"]`. No auto-discovery. Ensemble seeds literal in `ENSEMBLE_SEEDS` constant (191664963, 1662057957, 1405681631). Trade-arithmetic spot-checks verified (Trades 1, 2, 7 in OOS trades.csv; SL/TP geometry matches 2:1 ATR barrier). All 16 accretion-guard knobs PASS. PASS.

### Check 8 — Hypothesis-Implementation Alignment: PASS

Brief Section 3.5's 8 changes all implemented and verified at source: 24h aggregation pipeline (570 lines in `multioffset_24h.py`), 24h feature parquets at startup, `--bar-interval` CLI flag, `REQUIRED_GAP=72` recomputation (at the global CPCV path level — see Check 2 caveat that the inner Optuna CV path was NOT panel-aware), /116 no_confirm REVERT at all 3 surfaces (line 1941-1943 argument, line 2837-2851 pre-flight, line 1108-1112 accretion guard), `ITERATION_LABEL/MODEL_SPECS/banner` v3-117 updates, accretion-guard extension, adversarial integration test (3 sub-tests). The brief's central hypothesis (24h-multi-offset representation lifts directional signal via daily-frequency aggregation + 3-offset training-data multiplication) is FALSIFIED at production scale per Section 4 explicit falsifier ("OOS < −0.20 AND IS < +0.30"), with both arms firing decisively (IS −2.17 << +0.30; OOS −3.26 << −0.20). The implementation matched the hypothesis; the hypothesis failed empirically. PASS.

## Diagnostic Refinement (Round 2)

The 4-mechanism trace from QE's engineering report stands with Mechanism 4 refined by QR concurrence:

- **Mechanism 1 (AUC vs gated tail)**: Universe-pooled AUC 0.5823's signal comes primarily from BCH's 99.12% positive labels being trivially rank-ordered above the 1% negative labels — an imbalanced-data AUC artifact, not a model-quality signal. The gated tail (post-confidence-threshold) selects from a population where TRX's high-confidence predictions are systematically wrong (24.1% WR << 33.3% breakeven).

- **Mechanism 2 (BCH 99.12% imbalance)**: LightGBM trained on a 99% positive panel produces near-degenerate `predict_proba` calibration; the rare-class minority (0.88% SL-hitting rows) provides insufficient signal for the model to learn directional discrimination.

- **Mechanism 3 (TRX 79 IS → 0 OOS evaporation)**: TRX produced 79 IS trades at WR=24.1% (the largest IS loss driver) then ZERO OOS trades. Classic IS-overfit-to-noise pattern; the model found false IS regularities that don't transfer OOS.

- **Mechanism 4 (gate over-fire — REFINED)**: Per QR Round-2 concurrence, the LDO 69.5% z-score kill rate is structurally induced by (a) per-symbol mean/std computed across 3-offset pooled IS distribution producing inflated total std (between-offset variance + within-offset variance compounded), (b) LDO's small per-offset sample size (~707 rows per offset vs 8h's ~2120 rows) producing noisier std estimation, (c) z=2.0 fixed threshold inappropriate for the heavier-tail-mass pooled-multi-offset distribution. NOT a separable threshold-recalibration knob — the over-fire is intrinsic to the multi-offset architecture.

- **Offset_id ignored (rank 15/15 at 0.3-2.2% per symbol, 1.1% portfolio)**: The pooled-with-offset_id design DEGENERATES to a 3×-oversampled single-offset training. The multi-offset hypothesis as conceived is FALSIFIED — LightGBM did NOT learn offset-conditional structure.

## Recommendations to QR

1. **NO bundling /117 into iter-v3/120 CONFIRMATION.** /117 is a catastrophic NEGATIVE (IS −2.17 / OOS −3.26 — worst /117-style result in cycle-6 history, with the prior worst /112 pooled architecture at IS −1.12 / OOS −0.72). The 24h-multi-offset architecture is NOT a bundle candidate. /120 CONFIRMATION carries forward only the iter-v3/116 no_confirm primitive (PROMISING-MECHANICAL — strictly accretive component per `feedback_promising_mechanical_subtype.md`) as the cycle-6 edge ingredient candidate.

2. **/118 axis pivot to a DIFFERENT cycle-6 menu axis.** Per QR's Round-2 closure-scope answer, the candle-frequency axis (24h, 12h-2-offset, weekly-8-offset variants) is broadly CLOSED for cycle 6. Cycle 6 has 2 EXPLORATION slots remaining (/118, /119) before /120 CONFIRMATION; cycle-6 axis menu state: universe (closed at /110-111 NEGATIVEs), model architecture (closed at /112 NEGATIVE; XGBoost not closed for per-symbol+imbalance-aware configurations per QR), multi-frequency features on 8h (closed at /113 NEGATIVE), risk management (closed at /114 NEGATIVE), labeling architecture (closed at /115 NEGATIVE), exit-layer primitive (PROMISING at /116), candle frequency (CLOSED at /117). The LIVE structural axes per QR's Round-2 suggestions are: (a) NEW engineered feature family at 8h (composed features per `feedback_v3_engineered_features_proven.md` — regime_momentum lineage), or (b) per-symbol XGBoost-with-categorical-handling for BCH's imbalanced cell (iter-v3/016 closure was at universal-cohort + cross-entropy + depth-wise defaults; per-symbol+imbalance-aware configurations remain LIVE). The /118 QR makes the axis selection with EDA-driven quantitative discipline per `feedback_v3_axis_selection_quant_discipline.md`.

3. **/118 code-defect fix is CONDITIONAL on architecture.** Per QR Round-2 Clarification 1, the `lgbm.py:498` panel-aware CV-gap fix (multiply by `n_offset_series`) is REQUIRED if /118 adopts a multi-offset architecture, but NOT REQUIRED if /118 stays at single-frequency 8h. Given QR's closure-scope answer on the frequency axis, /118 is expected to be single-frequency — the code defect is filed as a precondition for any FUTURE multi-offset axis (not /118 itself). Add `n_offset_series` parameter to `LightGbmStrategy.__init__` (default 1) AND the IC-matrix writer one-line fix (iterate `_feature_columns` not `V3_FEATURE_COLUMNS`) to the /118 setup-commit ONLY if /118 architecture warrants them.

4. **Catalog entry for `briefs-v3/exploration_catalog.md`**: /117 EXPLORATION-NEGATIVE — IS Δ vs /060 anchor = **−2.97** (catastrophic; floor at −0.10 with 30× margin of failure), OOS Δ = **−3.40** (catastrophic; floor at −0.20 with 17× margin of failure). Confirmation candidate? **NO**. Closure scope: candle-frequency axis (24h, 12h, weekly) broadly CLOSED for cycle 6. Failure mechanisms: BCH 99.12% label imbalance + pooled-multi-offset z-score over-fire + offset_id ignored (rank 15/15). The 3 confounders are not specific to the 3-offset design; they recur at any multi-offset cadence on the BCH/LDO/TRX cohort. Future re-opening requires either a different cohort with favorable 24h volatility profiles, or a structurally different multi-offset architecture (per-offset separate models + late-fusion, not pooled-with-offset_id).

## Clarifications Requested from QR — NONE
