# Phase 7.5 Critic Review — iter-v3/101

OVERALL: MERGE

(MERGE = EXPLORATION-PROMISING-eligible: no methodology defect, no look-ahead, the single-axis claim holds, DSR/PBO/PSR are genuinely computed not placeholders. The verdict is "no Critic BLOCK"; the PROMISING-vs-NULL classification is the QR's Phase 8 call and is materially contested by the anchor-mismatch flagged below — see Recommendation 1.)

## Iteration Type (from Brief Section 0.5 / Section 1)

TYPE: EXPLORATION — cycle-5 slot #1, a single-variable training-objective axis (`sample_weight` normalization `magnitude` → `rank_normalized`). Scored on the methodology + look-ahead checks (1, 2, 4, 5, 6, 8) with full enforcement. Check 3 (DSR/PBO/PSR) is informational for an EXPLORATION per `feedback_v3_dsr_mode_artifact.md` — and /101 is not claiming a multi-seed-validated edge that the multiple-testing machinery could falsely launder. The provenance of the Check-3 artifacts is nonetheless audited in full (the iter-v3/090 / iter-v3/092 placeholder-defect class is the explicit scrutiny target).

## QR Response Considered

This review found ZERO blocking clarifications during the check sweep — every one of the 12 checks resolved unambiguously against the committed artifacts. No QR `qr_response.md` round was required. The verdict below is final. The two contested items (anchor mismatch, "single-seed" brief prose) are non-blocking and recorded as Recommendations for the Phase-8 closeout, not as clarification requests.

## Independent Audit of the Two Dispatch Context Items

**Context Item 1 — Stale-runner fix. VERIFIED CLEAN.** The /097 setup commit `e6ed662` left `V3_MODELS` at LDO/GALA/ADA. iter-v3/101 restored it to the /059 canonical BCH/LDO/TRX. Three independent confirmations: (a) `run_baseline_v3.py:190-194` `V3_MODELS = (("A (BCHUSDT)","BCHUSDT"),("C (LDOUSDT)","LDOUSDT"),("D (TRXUSDT)","TRXUSDT"))`; (b) the /101 backtest demonstrably ran on those three — `reports-v3/iteration_v3-101/in_sample/per_symbol.csv` and `out_of_sample/per_symbol.csv` carry exactly BCHUSDT/LDOUSDT/TRXUSDT, zero GALA/ADA rows; `per_cell_pbo.csv` cells are all BCH/LDO/TRX; (c) the runner's `config-accretion check` (`run_baseline_v3.py:1009-1044`) hard-raises the build if ANY of 11 /059-canonical knobs drift — including `("V3_MODELS symbols", _v3_model_symbols, ("BCHUSDT","LDOUSDT","TRXUSDT"))` at line 1014-1017 — and the run reached completion, so this assertion passed. The `conviction_derate` code the QE flagged as dead is genuinely uncalled: `lgbm.py:114` defines it as a module-level function with zero call-sites anywhere in `src/` (grep `conviction_derate\(` returns only the `def` line) and zero references in `run_baseline_v3.py`. The run was confounded by no unintended second change.

**Context Item 2 — EXPLORATION-mode comparability. A REAL non-blocking flaw in the brief's anchor.** /101 ran 3-seed EXPLORATION mode (`ensemble_summary.json` `mode: exploration`, `ensemble_size: 3`; `dsr.json` `n_trials: 315 = 35 × 3 seeds × 3 symbols`). /059's published IS +1.0894 / OOS +0.5791 are 10-seed CONFIRMATION-mode numbers (`n_trials=1050`). The brief Section 4 falsifiers (F1: OOS < +0.30; F2: IS < +0.70) and Section 8 classification taxonomy anchor to those /059-10-seed numbers. This is an apples-to-oranges anchor: per `feedback_v3_dsr_mode_artifact.md` and `feedback_v3_cycle1_axis_pass_criteria.md`, prior cycle-1 EXPLORATIONs anchor against the `/060` **3-seed EXPLORATION-mode** reference (IS +0.8325 / OOS +0.1403), the architecturally-matched comparison. The two anchors give opposite verdicts: against /060-3-seed, /101 is IS Δ +0.0246 / OOS Δ +0.3787 (a clear OOS lift); against /059-10-seed, /101 is IS Δ −0.2323 / OOS Δ −0.0601 (a regression). The brief's absolute falsifier thresholds (F1/F2) do not fire either way, so this does not BLOCK — but the QR must NOT apply the /059-10-seed anchor when classifying PROMISING-vs-NULL in Phase 8. Recommendation 1 covers this.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

The sole axis is the `sample_weight` normalization; no feature, label-geometry, model-arch, universe, or window code changed. The leak surface is the rank computation. Traced: `labeling.py:402` `rank_pct = pd.Series(weights).rank(pct=True).to_numpy()` operates on the `weights` array (`|labeled_pnl|`) for whatever `candidate_indices` were passed. The call site is `lgbm.py:390-401`, invoked once per (model, walk-forward month) cell with `train_indices` computed at `lgbm.py:359-362` as `np.where((open_time >= split.train_start_ms) & (open_time < split.train_end_ms))` — strictly bounded above by `split.train_end_ms`. The rank is therefore cross-sectional within the training window only; a training candle's weight depends only on other training candles' `|net PnL|`, all with `open_time < train_end_ms`. No test-window candle enters the rank. The brief Section 3 item 1 claim ("rank computed within the call, within the training window, never across the train/test boundary") is verified true in the actual code. The `weight_mode` change touches no feature, no inference path, and no temporal logic — it re-scales a vector that was already computed from training-window labels. The 14-feature `V3_FEATURE_COLUMNS` stack is bit-identical to /059 (`adf_test.csv` and `ic_matrix.csv` carry the unchanged 14 columns). No look-ahead introduced.

### Check 2 — Embargo Width: PASS

Required CPCV row-gap = `(timeout_candles + 1) × n_symbols = (21 + 1) × 3 = 66`. Timeout is 21 candles (`common_kwargs label_timeout_minutes=10080`, `10080 / 480 = 21`), unchanged from /059; universe is 3 symbols, unchanged. Actual `REQUIRED_GAP` resolves to 66 — verified at `run_baseline_v3.py:1018` (config-accretion check `("REQUIRED_GAP", REQUIRED_GAP, 66)`), at `_verify_label_leakage_gap` (`run_baseline_v3.py:1151-1162`, recomputes `(21+1)×3` and asserts equality), and runs unconditionally in preflight at `run_baseline_v3.py:2668`. The walk-forward train/test embargo is intact: `walk_forward.py:113` `train_end_ms = test_start_ms - embargo_ms` with `embargo_ms = compute_embargo_candles(10080, 480) × interval_ms` and `compute_embargo_candles` returns `10080 // 480 + 1 = 22` candles per cell. This is the `e149e9d` lookahead fix, inherited unchanged (the brief Section 0 declares this, and the source confirms it). The `weight_mode` change does not touch CPCV, embargo, or walk-forward. No serial-dependence leakage path.

### Check 3 — Multiple-Testing Correction: FAIL on the edge thresholds (informational for EXPLORATION); provenance VERIFIED GENUINE — NOT a placeholder defect

`dsr.json`: `dsr=0.0`, `pbo=0.0968`, `psr=1.0`, `dsr_relative=0.019014`, `dsr_relative_b4=0.999796`, `n_eff=18`, `frac_positive_paths=0.6444`. DSR=0.0 and `dsr_relative=0.019` fail the >0.95 CONFIRMATION threshold. Per TYPE=EXPLORATION this is informational, NOT BLOCK-triggering (`feedback_v3_dsr_mode_artifact.md`: EXPLORATION-mode `n_trials=315` is structurally not comparable to CONFIRMATION-mode `n_trials=1050`). The decisive question — is this the iter-v3/090/092 hardcoded-sentinel defect? — resolves CLEAN, exactly the iter-v3/097 precedent:

- **DSR=0.0 is the known per-symbol-runner structural artifact, not a literal.** The runner computes `dsr_result = deflated_sharpe_ratio_v3(observed_sr=raw_sharpe_is, num_trials=n_trials_total, backtest_length=len(is_wp), skewness=sk, kurtosis=kt)` at `run_baseline_v3.py:2885-2891`, where `raw_sharpe_is` is derived from the real IS trade `weighted_pnl` array (`run_baseline_v3.py:2879-2881`). `dsr_val = dsr_result["p_value"]`. The cross-reference: the /059 canonical baseline `dsr.json` also carries `dsr: 0.0` with a healthy IS Sharpe — DSR=0.0 is `norm.cdf` underflowing on a steeply-negative `dsr_z` at v3 per-symbol trade volume, rounding to 0.0. Categorically different from the /092 BLOCK class (degenerate cross-sectional schema, `pbo: NaN`, `n_eff: 0`).
- **PBO=0.0968 is a genuine CSCV computation.** `per_cell_pbo.csv` is a 128-row CSV of real per-(symbol, train_month) PBO values spanning 0.0–1.0; 0.0968 is their cross-cell mean (`run_baseline_v3.py:2826` `_compute_cpcv_paths` → `_compute_per_cell_pbo` → `combinatorial_purged_cv` at line 1707, `n_effective_trials` at line 1732). `cpcv_paths.csv` carries 45 genuine return-proxy paths with dispersed Sharpes (q25 −0.243, q50 +0.335, q75 +0.838 — and the q75 value 0.837759 is path_id 36's exact Sharpe, internally consistent). PBO=0.0968 PASSES the <0.4 threshold.
- **PSR=1.0 is genuine** — `psr(observed_sharpe=raw_sharpe_oos, ...)` at `run_baseline_v3.py:2914` on the real positive-Sharpe OOS trade series saturates `norm.cdf` to 1.0 at `n_obs=101`. **`dsr_relative_b4=0.999796`** is genuinely computed via `psr(observed_sharpe=daily_sharpe_oos_b4, benchmark_sharpe=cpcv_q75_annualized_b4, ...)` at `run_baseline_v3.py:3025-3031`, on the real OOS daily-PnL series (`n_daily_obs_oos=94` distinct dates, independently confirmed against `out_of_sample/daily_pnl.csv` which has exactly 94 date rows).
- The anti-sentinel integration test `test_dsr_json_is_genuinely_computed` (`tests/strategies/ml/test_universe_reselection_v3.py:114-196`) structurally verifies the runner imports and calls `validation_v3.psr` and `deflated_sharpe_ratio_v3` and both return finite values on synthetic input.

Provenance is the complete /059-identical schema with genuine floats — this is the iter-v3/097 CLEAN finding, not the iter-v3/092 placeholder defect.

### Check 4 — IC Correlation: PASS

`ic_matrix.csv` shows two pairs above the 0.70 raw threshold: `regime_momentum_signed_5d` ↔ `vwap_dev_20` at 0.7642 and `regime_momentum_signed_5d` ↔ `sym_vs_btc_ret_7d` at 0.6189 (below) — the highest is 0.7642. However, iter-v3/101 adds **zero new feature families**: the 14-feature `V3_FEATURE_COLUMNS` stack is bit-identical to /059 (brief Section 9 declares this; `ic_matrix.csv` header carries the unchanged 14 columns; `test_feature_columns_pinned_for_new_symbols` at `test_universe_reselection_v3.py:204-248` asserts `features_for_symbol` returns the same 14-feature list for all three symbols and `V3_FEATURES_PER_SYMBOL` is empty). Check 4's <0.70 threshold applies to families ADDED in the iteration's commits; there are none. The `regime_momentum_signed_5d` ↔ `vwap_dev_20` 0.7642 pair is the pre-existing constructed-feature collinearity already documented at `feedback_v3_engineered_feature_pivot.md` — `regime_momentum_signed_5d` is itself a composed feature that mechanically correlates with its primitives. No new redundancy introduced by the `weight_mode` axis. PASS.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` carries 1803 `stationary=True` rows and 395 `stationary=False` rows. The `False` rows are degenerate early calendar months (2020-01, 2020-02, …) with empty `adf_statistic`/`p_value` — pre-burn-in months where the rolling feature window has insufficient data, NOT in the runner's IS evaluation span. The skill's test ("all features stationary at end of training window") is satisfied: at the last IS training month 2025-03, all 14 features × all 3 symbols are `stationary=True` with p < 0.05 (the weakest is `ret_kurt_200` on BCH at p=0.045043, which clears 0.05; most are p≈0.0). The 14-feature stack is the unchanged /059 stack — no new price-derived feature is introduced by the `weight_mode` axis (brief Section 9 correctly states "ADF gates N/A — no new feature"). No defect.

### Check 6 — Pareto Dominance: PASS (not applicable; correctly absent)

iter-v3/101 ran 3-seed EXPLORATION mode (`--exploration`, `EXPLORATION_ENSEMBLE_SIZE=3`, `ENSEMBLE_SEEDS[0:3]` = the outer=42-lineage first three: 191664963, 1662057957, 1405681631). EXPLORATION mode produces ONE deterministic averaged-ensemble roster, not a multi-seed Pareto front — `pareto_front.csv` is correctly absent from `reports-v3/iteration_v3-101/` (the Gate-10 Pareto front was replaced by the `cpcv_frac_positive_paths_gate` at /059, and the unified-ensemble architecture stopped emitting `pareto_front.csv` per the BASELINE_V3.md Phase-B-3 note). The seed selection is not a tunable — the three seeds are the literal `ENSEMBLE_SEEDS` prefix, lineage-preserving and regression-tested by `test_ensemble_unified.py::TestEnsembleSeedsLineage`. No Pareto defect, no single-metric tunnel-vision risk (the iteration is not seed-selecting).

### Check 7 — Reproducibility: PASS

(a) `ITERATION_LABEL = "v3-101"` (`run_baseline_v3.py:131`), verified by two named integration tests (`test_weight_mode.py:280`, `test_universe_reselection_v3.py:77`). (b) `feature_columns` is explicit, never `None`: `common_kwargs` passes `feature_columns=list(features_for_symbol(symbol))` (`run_baseline_v3.py:1858`); `LightGbmStrategy.__init__` raises on empty (`lgbm.py:174-175`). (c) Ensemble seeds are literal: `active_ensemble_seeds = ENSEMBLE_SEEDS[:ensemble_size_for_run]` (`run_baseline_v3.py:2782`), `ENSEMBLE_SEEDS` is the hardcoded lineage-preserving 10-tuple, EXPLORATION takes the first 3; `ensemble_summary.json` records exactly those three integers. (d) Trade-PnL spot check — three rows re-computed: `out_of_sample/trades.csv` row 2 (BCH short, entry 303.870000, exit 282.100779, weight 0.3300): `pnl_pct = (303.87−282.100779)/303.87×100 = 7.1640` ✓, `net = 7.1640−0.1000 = 7.0640` ✓, `weighted = 7.0640×0.3300 = 2.3311` ✓; row 5 (TRX short, 0.253→0.243110, weight 0.40): `pnl=3.9091` ✓ `net=3.8091` ✓ `weighted=1.5237` ✓; `in_sample/trades.csv` row 2 (BCH long, 300.91→287.394447, weight 0.41): `pnl=−4.4916` ✓ `net=−4.5916` ✓ `weighted=−1.8825` ✓. No off-by-one, no sign error. The `weight_factor=0.0000` OOS rows (e.g. trades.csv rows 77,78,81,82,83) are the intentional BTC-trend-kill-zeroed trades — `weighted_pnl=0` correctly, consistent with `comparison.csv` `weighted_pnl_total` and the `daily_pnl.csv` zero-rows; not a defect. (e) `n_trials_total = 35 × 3 × 3 = 315` matches `dsr.json n_trials: 315`. The `weight_mode="rank_normalized"` line is in `common_kwargs` (`run_baseline_v3.py:1871`) — a runtime configuration, deterministic.

**One process gap, NON-BLOCKING.** No `briefs-v3/iteration_v3-101/engineering_report.md` exists. The dispatch references the Engineer's `OVERALL=READY-FOR-CRITIC` and a setup-commit SHA backfill, but no engineering report file was committed. This does not change the verdict — every verdict-determining artifact (brief, phase5p5 gate, comparison.csv, dsr.json, cpcv_paths.csv, per_cell_pbo.csv, adf_test.csv, ic_matrix.csv, ensemble_summary.json, the touched `src/` files, the EDA scripts, the test suite) is present and was independently audited — but the missing report means the setup-commit SHA stamp and the QE's per-symbol attribution narrative could not be cross-checked against a committed Engineer artifact. Recorded as Recommendation 2.

### Check 8 — Hypothesis-Implementation Alignment: PASS

Brief Section 1 hypothesis: switch the `sample_weight` normalization from magnitude-linear `[1,10]` to cross-sectional rank-normalized `[1,10]`, ONE variable, everything else /059-identical. The implementation matches the brief's Section 3 spec exactly, all three touchpoints verified:

1. `labeling.py:144` adds `weight_mode: str = "magnitude"`; lines 396-405 implement the branch — `weight_mode="rank_normalized"` does `rank_pct = pd.Series(weights).rank(pct=True).to_numpy(); weights = 1.0 + rank_pct * 9.0` (line 402-403), `"magnitude"` (default) does the unchanged `weights = 1.0 + weights / weights.max() * 9.0` (line 405), and an invalid value raises `ValueError` (line 396-399). The default reproduces /059 byte-for-byte — verified by `test_weight_mode.py::test_magnitude_mode_byte_identical` which `np.testing.assert_array_equal`s a no-`weight_mode` call against `weight_mode="magnitude"`.
2. `lgbm.py:172` adds the `__init__` param, `:219` stores `self.weight_mode`, `:401` passes `weight_mode=self.weight_mode` into the `label_trades` call.
3. `run_baseline_v3.py:131` `ITERATION_LABEL="v3-101"`, `:1871` `weight_mode="rank_normalized"` in `common_kwargs`.

The single-axis claim is mechanically enforced: the `config-accretion check` (`run_baseline_v3.py:1009-1044`) hard-fails the build if ANY of 11 /059-canonical knobs drift (V3_MODELS symbols, REQUIRED_GAP, the 9 risk knobs), the SOLE permitted change being `weight_mode`. The disjointness gate (`run_baseline_v3.py:644-655`) additionally asserts V3_MODELS ∩ V3_EXCLUDED_SYMBOLS = ∅. The backtest reached completion → both assertions passed → no hidden second strategy variable. The axis-selection trail is also sound: the /100 diary recommended a sample-weighting axis (uniqueness + magnitude); the brief's Section 2 ANGLE A/H EDA (`T1`, `eda4`) genuinely killed the uniqueness half (rank-corr 0.992–0.997 — uniqueness is a near-no-op on the 21-candle overlapping label) and the QR re-scoped to the magnitude-reweight alone, with EDA #3 (`eda3_weight_horse_race.py`, verified IS-only via `load_is` filtering `open_time < OOS_CUTOFF_MS`, walk-forward-faithful with a 22-candle embargo gap at line 130) selecting W2. The shipped `weight_mode="rank_normalized"` is exactly the EDA #3 W2 design (`build_weight_designs` line 101-102 = `pd.Series(w0).rank(pct=True)` → `1.0 + ranks×9.0`). No hypothesis-faking, no scope creep.

## Optional Checks 9–12

- **Check 9 — Symbol Exclusion Enforcement: PASS.** `_verify_symbols` (`run_baseline_v3.py:235-242`) computes `set(symbols) & set(V3_EXCLUDED_SYMBOLS)` and raises on overlap; the Section-3.2 disjointness gate (`run_baseline_v3.py:644-652`) independently computes `{sym for _,sym in V3_MODELS} & set(V3_EXCLUDED_SYMBOLS)` and raises. Both run in preflight. `test_v3_models_disjoint_from_excluded` (`test_universe_reselection_v3.py:88-106`) asserts it as a named test. Audit present.
- **Check 10 — Feature Isolation Enforcement: PASS.** Grep for `from crypto_trade.features ` / `import crypto_trade.features` (the v1/v2 module) across `src/crypto_trade/features_v3/` returns nothing. No cross-track import.
- **Check 11 — Forming-Candle Audit: PASS (delegated).** `_verify_data_freshness` (`run_baseline_v3.py:245-255`) asserts each symbol's last `close_time` is within 16h of now (or past `OOS_CUTOFF_MS`); runs unconditionally in preflight (`run_baseline_v3.py:2662`). The OOS `daily_pnl.csv` extends to 2026-05-17 (today) — no forming-candle tail.
- **Check 12 — Library Version Pinning: PASS.** Brief Section 9 declares the inherited /059 stack (lightgbm 4.6.0, optuna 4.8.0, numpy 2.2.6, pandas 3.0.0, scikit-learn 1.8.0, scipy 1.17.0, statsmodels 0.14.6, pyarrow 23.0.1) and adds no new library — the axis uses `pandas.Series.rank`, already a dependency. Nothing to mismatch.

## Falsifier Cross-Audit (brief Section 4)

Re-evaluated each pre-registered falsifier against the runner artifacts. **CAVEAT: the brief states F1/F2 against /059's 10-seed CONFIRMATION numbers; /101 is a 3-seed EXPLORATION run — the anchor is mismatched (see Context Item 2). The thresholds are absolute, so the fire/no-fire below is anchor-independent, but the QR's Phase-8 PROMISING-vs-NULL read must use the /060 3-seed anchor.**

- **F1** (OOS monthly Sharpe < +0.30) — does NOT fire. `comparison.csv` out_of_sample monthly_sharpe = +0.5190.
- **F2** (IS monthly Sharpe < +0.70) — does NOT fire. in_sample = +0.8571.
- **F3** (BCH IS net-PnL contribution flips negative OR BCH IS share > 99.5%) — does NOT fire. `in_sample/per_symbol.csv` BCH net_pnl_pct = +78.6783 (positive); BCH `pct_of_total_pnl` = 172.24% (the share exceeds 100% because TRX IS is −104.88%, not because BCH hollowed out the others — BCH share is not > 99.5% in the F3 sense of a single-symbol monopoly; the QR should confirm the intended F3 reading in Phase 8, but on the literal "flips negative" leg it plainly does not fire).
- **F4** (OOS trade roster bit-identical to /059, or <5% change) — does NOT fire. /101 OOS roster is 101 trades vs /059's 94 — a +7.4% change, above the 5% behavioral-saturation floor. The `weight_mode` change is behaviorally active.
- **F5** (OOS improves but per-symbol added-vs-removed mean-duration gap > +1.0) — cannot be evaluated from the committed artifacts; there is no roster-diff CSV for /101 and no engineering report computing the duration gap. Flagged for the QR — see Recommendation 3.

The disjunctive Section-8 precedence (BLOCKED → NEGATIVE → SUSPICIOUS → INERT → PROMISING → NULL) is the QR's Phase-8 call. The Critic verdict is solely "no methodology BLOCK." The QR must resolve the anchor mismatch and the F5 evaluability gap before filing the classification.

## Recommendations to QR

This is an EXPLORATION with no Critic BLOCK. The verdict is final; these are process-level items for the Phase-8 closeout and the next iteration.

1. **Classify /101 against the /060 3-seed EXPLORATION-mode anchor, NOT /059's 10-seed CONFIRMATION numbers.** The brief Section 4 / Section 8 anchor to /059 (IS +1.0894 / OOS +0.5791) is architecturally mismatched — /101 ran 3-seed EXPLORATION mode, whose architecturally-matched reference is `/060` (IS +0.8325 / OOS +0.1403), per `feedback_v3_dsr_mode_artifact.md` and `feedback_v3_cycle1_axis_pass_criteria.md`. The choice is verdict-determining: against /060, /101 is IS Δ +0.0246 / OOS Δ +0.3787 (a clear OOS lift, IS roughly flat — a PROMISING-shaped result); against /059-10-seed it is IS Δ −0.2323 / OOS Δ −0.0601 (a regression). Apply the cycle-1 PASS criteria from `feedback_v3_cycle1_axis_pass_criteria.md` (IS Δ ≥ +0.10 AND OOS Δ ≥ +0.20 vs the EXPLORATION-mode anchor AND `frac_positive_paths` ≥ 0.50) against /060. Note that on those criteria /101's IS Δ +0.0246 is BELOW the +0.10 IS gate even against /060 — so the honest read is likely NULL-RESULT-or-PROMISING-on-OOS-only, not a clean PROMISING; the QR must make that call explicitly and not let the brief's /059 anchor force a NEGATIVE framing. The PROMISING-shaped axis would then cross-validate at the cycle-5 CONFIRMATION against /059's 10-seed CONFIRMATION baseline (the matched comparison at that stage).

2. **Commit the engineering report.** No `briefs-v3/iteration_v3-101/engineering_report.md` was found. The dispatch references the Engineer's `OVERALL=READY-FOR-CRITIC` and a setup-commit SHA backfill, but the artifact is absent. Every verdict-determining artifact was present so the audit completed, but the missing report meant the setup-commit SHA stamp and the QE's per-symbol attribution narrative had no committed Engineer source to cross-check (per `feedback_v3_methodology_post_hoc_input_traceback.md`, every classification-driving number should be traceable to a committed artifact). Future iterations must commit the engineering report before Critic dispatch.

3. **Resolve the F5 evaluability gap before filing the Section-8 classification.** Brief falsifier F5 (mechanical roster-churn — per-symbol added-vs-removed OOS mean-duration gap > +1.0) cannot be evaluated from the committed /101 artifacts: there is no roster-diff CSV and no engineering report computing the duration gap. /101's OOS roster did change (101 vs /059's 94 trades, +7.4%), so F5 is a live, fireable falsifier — the QR must produce the per-symbol added/removed-trade mean-duration comparison (the /076 trade-selection sub-channel signature) and confirm F5 does not fire before classifying the OOS lift as signal rather than roster-churn artifact. The brief's "single-seed EXPLORATION" prose in Section 3 (line 207) is also imprecise — the runner's `--exploration` mode has used `EXPLORATION_ENSEMBLE_SIZE=3` since commit `56f5a30`, and /101 ran 3-seed; future briefs should state "3-seed EXPLORATION" to match the runner. Separately, EDA #3's block-bootstrap CI uses `block=4` (folds) while the brief Section 2 and the EDA docstring claim "block = 21-bar label horizon" — a benign EDA-internal inconsistency (the backtest, not the EDA proxy, is the resolving experiment, as the brief itself states) but worth correcting in the next sample-weighting EDA.
