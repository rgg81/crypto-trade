# Phase 7.5 Critic Review — iter-v3/084

OVERALL: MERGE

iter-v3/084 is a REFERENCE / METHODOLOGY iteration: it bundles the `PER_CELL_GAP` 43→22 methodology fix (this Critic's own iter-v3/083 Recommendation #2) with a clean revert of /083's NEGATIVE FILUSDT universe expansion back to the /059-canonical 3-symbol config. This is a methodology certification, NOT an advancement. The Critic gates methodology soundness — a clean reference iteration with sound methodology is OVERALL=MERGE. The result classification (REFERENCE-REANCHOR) and the re-anchor decision belong to the QR's Section 8; see the explicit recommendations below.

## Iteration Type (from Brief Section 0.5)

TYPE: REFERENCE / METHODOLOGY (cycle-3 slot #3 of 10). Not a bold research axis; not a CONFIRMATION. EXPLORATION mode (`--exploration`, `EXPLORATION_ENSEMBLE_SIZE=3`, seeds `[191664963, 1662057957, 1405681631]` — the outer-42 lineage subset). Single-round full review; the verdict is FINAL. Per `feedback_v3_dsr_mode_artifact.md`, for EXPLORATION-mode the Check 3 edge axes (DSR/PSR) are INFORMATIONAL and not BLOCK-eligible; only the Check 3 PBO axis is BLOCK-eligible. All 8 mandatory + 4 optional checks run with this discipline.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

iter-v3/084 adds **no features** — the changes are a `PER_CELL_GAP` constant, a `V3_MODELS` tuple revert (4→3, drop FILUSDT), and a `REQUIRED_GAP` revert. The feature set is the 14-feature /059 canonical anchor stack, verified literal in `src/crypto_trade/features_v3/__init__.py:172-185` (`max_dd_window_50, ema_spread_atr_20, ret_kurt_50, ret_skew_200, range_realized_vol_50, hurst_diff_100_50, ret_kurt_200, hurst_100, btc_ret_14d, ret_skew_50, vwap_dev_20, ret_autocorr_lag1_50, sym_vs_btc_ret_7d, regime_momentum_signed_5d`) — each look-ahead-audited clean in every prior /059-anchor iteration through /083. No new computation chain is introduced. The post-`e149e9d` walk-forward embargo (`train_end_ms = test_start_ms − embargo_ms`) is intact in this worktree (BASELINE_V3.md confirms; the engineering report's run.log line 25 confirms `Label-leakage gap ... = 66 PASS`, and CV-fold logs show `gap=184h (22 rows)` per cell). The `PER_CELL_GAP` change is purely a CSCV-purge parameter — it does not touch any feature computation or the trade roster. No look-ahead.

### Check 2 — Embargo Width: PASS

`timeout_minutes = 10080`, `candle_minutes = 480` → `timeout_candles = 21`. **Numerical proof, two distinct gaps:**

- **Global pooled CPCV gap**: required = `(timeout_candles + 1) × n_symbols = (21 + 1) × 3 = 66`. The constant `REQUIRED_GAP` is correctly reverted at `validation_v3.py:65`: `REQUIRED_GAP: int = (21 + 1) * 3  # 66`. The runtime assertion `_verify_label_leakage_gap()` (`run_baseline_v3.py:1050-1056`) recomputes `(timeout_candles+1) * len(V3_MODELS) = 22 × 3 = 66` from the live 3-symbol `V3_MODELS` and asserts equality — formula-driven, so the revert cannot drift. The global CPCV call (`run_baseline_v3.py:1428-1434`) passes `gap=REQUIRED_GAP, expected_gap=REQUIRED_GAP` — the self-assertion makes a stale 88 impossible.
- **Per-cell single-symbol CSCV gap**: required = `(timeout_candles + 1) = 22`. The per-cell CSCV operates on one `(symbol, train_month)` cell — a single-symbol candle sequence; the `× n_symbols` factor does **not** apply (it applies only to the pooled global CPCV, which interleaves all symbols into one sequence). `PER_CELL_GAP = 22` at `run_baseline_v3.py:1510` is the correct value. The fix is verified below in the dedicated focus-point sub-section.

`combinatorial_purged_cv` (`validation_v3.py:149-153`) applies the purge **symmetrically** on both sides of every test boundary: `lo = max(0, group_starts[g] - gap)` and `hi = min(n_samples, group_ends[g] + gap + embargo)`. The per-cell `embargo=0` is correct — the cell is single-symbol, single-month; there is no autocorrelated cross-cell leakage path. Required gap = actual gap on both the global (66=66) and per-cell (22=22) computations, symmetrically applied. PASS.

### Check 3 — Multiple-Testing Correction: PASS (PBO axis); DSR/PSR informational for EXPLORATION

PBO mean = **0.1278** (`dsr.json`, `comparison.csv`) — clears the 0.40 hard threshold; this is the one Check-3 axis BLOCK-eligible at any mode, and it PASSES (the IS-best Optuna configuration is not anti-correlated with OOS). Note this 0.1278 was computed with the **corrected** `PER_CELL_GAP=22` — the per-cell PBO going forward reflects the less-conservative correct purge; the value happens to land identically to /059's 0.1278 because most cells show PBO=0.0 regardless of the gap (verified directly in `per_cell_pbo.csv` — the bulk of 129 cells are 0.0; elevated cells are clustered in specific months). PSR = 1.0000 — PASS (n_trials=315 saturation). DSR = 0.0 — the documented EXPLORATION-mode artifact (`feedback_v3_dsr_mode_artifact.md`): at EXPLORATION-mode `n_trials=315` the López de Prado E[max_SR] formula is not comparable to CONFIRMATION-mode `n_trials=1050`; DSR is INFORMATIONAL and NOT BLOCK-eligible for TYPE=REFERENCE/EXPLORATION. `dsr.json` additionally carries `dsr_relative=0.000338` and `dsr_relative_b4=0.8154` (Path B4 reporting infrastructure) — informational at EXPLORATION mode. `n_trials = 315 = 35 × 3 symbols × 3 EXPLORATION seeds` — verified correct against `ensemble_summary.json` (`ensemble_size: 3`, the three outer-42-lineage seeds). `n_effective_trials = 19` (per-cell PCA-95 median) — sensible, architecture-independent. The Check-3 BLOCK-eligible axis PASSES.

### Check 4 — IC Correlation: PASS

iter-v3/084 introduces **no new feature family** — it is a methodology-fix + universe-revert on the unchanged 14-feature stack. `ic_matrix.csv` is present (the schema-required file; its absence would be an automatic FAIL — present, 14×14). The highest pairwise `|IC|` is `vwap_dev_20 / regime_momentum_signed_5d` = **0.764** — the pre-existing, repeatedly-adjudicated Category-2 composed-feature carve-out (`regime_momentum_signed_5d = ret_5d × sign(hurst_100 − 0.5)` mechanically correlates with `vwap_dev_20` by construction; `feedback_v3_engineered_feature_pivot.md`). It is not new and not /084-introduced — it was 0.887 at the /025 data vintage and 0.773 at /083; 0.764 on current data is the same pair, drifting with data extent. The next-highest pairs (`sym_vs_btc_ret_7d / regime_momentum_signed_5d` = 0.619; `ema_spread_atr_20 / regime_momentum_signed_5d` = 0.597) are below the 0.70 hard gate. No new collinearity is possible — no feature changed. PASS.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` reports 2,198 feature×symbol×month cells tested, 1,803 stationary (82.0%) at p<0.05; 327 non-stationary (14.9%). The non-stationary set is the known set of level features (`max_dd_window_50, ret_kurt_50, btc_ret_14d, ret_skew_50, vwap_dev_20, ret_autocorr_lag1_50`) carried verbatim from /059 — these have been adjudicated across every prior v3 iteration as documented level features with regime-dependent non-stationarity, retained on the canonical stack by prior QR/Critic precedent. iter-v3/084 changes no feature and no labeling; it cannot introduce any new non-stationarity. The 82.0% stationarity rate is consistent with /083's 81.6% (the small difference is the FIL symbol dropping out of the panel). No /084-attributable stationarity defect. PASS.

### Check 6 — Pareto Dominance / Gate-10-CPCV: PASS (methodology)

Gate-10-Pareto is RETIRED under the unified architecture (`pareto_front.csv` is no longer produced — confirmed absent from the report directory, which is correct, not a missing-file FAIL). The replacement is Gate-10-CPCV (`frac_positive_paths ≥ 0.55`). Observed `frac_positive_paths = 0.6444` (29 of 45 CPCV paths positive — verified by direct count of `cpcv_paths.csv`: 29 rows with `sharpe > 0`) — **PASS** above the 0.55 threshold. The CPCV path Sharpe distribution is q25=−0.243 / q50=+0.335 / q75=+0.838; the median path is positive. The CPCV computation is sound: 45 paths from C(10,2), pooled 3-symbol candle sequence, `gap=66` correctly purged and symmetrically applied, `embargo=27`. PASS.

### Check 7 — Reproducibility: PASS

The engineering report stamps the setup commit SHA `1073f32` and the Phase 5.5 gate SHA `3d0d9db` — both must exist on `iteration-v3/084` (the Phase 5.5 gate independently verified all four Section-11 SHAs at gate check CR13: EDA `401de40`, brief `c18a2dc`, setup `1073f32`, SHA-backfill `f08709b`). The runner uses an explicit `feature_columns` list — `_build_v3_model` passes `feature_columns=list(features_for_symbol(symbol))`, never `None`; `_verify_feature_columns()` asserts `len == 14`, funding columns absent, and `ensemble_size in (3, 10)` for mode discipline. The ensemble seeds are a literal tuple — `ENSEMBLE_SEEDS` at `run_baseline_v3.py:103-114` is the hardcoded 10-tuple; EXPLORATION mode uses `ENSEMBLE_SEEDS[:3] = [191664963, 1662057957, 1405681631]`, confirmed verbatim in `ensemble_summary.json` (`"mode": "exploration"`, `"ensemble_size": 3`, the three seeds with `lineage: "outer=42"`). **Trade-row PnL spot-check** (5 random rows from `out_of_sample/trades.csv`): row 2 BCHUSDT short, entry 303.870 / exit 282.101 / take_profit → `pnl_pct = (303.870 − 282.101)/303.870 = +7.164%` ✓, `net = 7.164 − 0.100 fee = +7.064` ✓, `weighted = 7.064 × 0.33 wf = +2.331` ✓; row 4 BCHUSDT short, entry 371.240 / exit 383.578 / stop_loss → `pnl = (371.240 − 383.578)/371.240 = −3.323%` ✓, `weighted = −3.423 × 0.53 = −1.814` ✓; rows 6/9/12 all internally consistent (sign matches direction × exit_reason, fee 0.1%, weighting correct). No off-by-one or sign error. `n_trials = 315` matches `35 × 3 × 3`. The `--clean-oof` startup guardrail (verified at `run_baseline_v3.py:2583-2597`) prevents the iter-v3/047 OOF parquet duplicate-row inflation failure mode. PASS.

### Check 8 — Hypothesis-Implementation Alignment: PASS

Brief Section 1 declares exactly two changes; the implementation matches exactly, with zero scope creep:

1. **`PER_CELL_GAP` 43→22 + `expected_gap` guard.** Verified at `run_baseline_v3.py:1510` (`PER_CELL_GAP = 22`, with a full provenance comment at lines 1498-1509 tracing the stale `43=(42+1)` to the reverted /068 timeout) and at `run_baseline_v3.py:1602-1609` (the per-cell `combinatorial_purged_cv` call now passes `expected_gap=PER_CELL_GAP`). The stale string literals are fixed: line 2539 now references `[sym for _, sym in V3_MODELS]` dynamically (no hardcoded contradictory gap), and line 2610 prints `Gap: {REQUIRED_GAP} (= (21+1)*3; iter-v3/084 REVERT /083 FILUSDT expansion; ...)` — using the live constant with the correct 3-symbol narrative, no internal contradiction.
2. **The /059-config 3-symbol revert.** `V3_MODELS` at `run_baseline_v3.py:170-174` is exactly the 3-symbol tuple `(BCHUSDT, LDOUSDT, TRXUSDT)` — no FILUSDT residue. `REQUIRED_GAP` reverted 88→66 at `validation_v3.py:65`. The config-accretion `_canonical_v059` 11-knob check (`run_baseline_v3.py:905-936`) asserts ALL 11 knobs are /059-canonical: `V3_MODELS symbols → ("BCHUSDT","LDOUSDT","TRXUSDT")`, `REQUIRED_GAP → 66`, plus the 9 RiskV3 knobs (ATR multipliers `(2.0,1.0)`, `zscore_threshold=2.0`, `adx_threshold=20.0`, all per-symbol dicts empty, `block_long_for=()`, `block_short_for=()`, `enable_per_symbol_drawdown_brake=False`) — **zero axis carve-out** (this is a baseline restore, not a new axis). `V3_FEATURE_COLUMNS` is unchanged at the 14-feature /059 stack — no /082 funding residue (the funding family was reverted at /083 setup).

`ITERATION_LABEL = "v3-084"` at line 131. **No scope creep** — labeling, features, risk gates, model architecture, seeds, Optuna search are all /059-canonical. The implementation tests exactly the registered scope. PASS.

## Optional Checks 9–12

**Check 9 — Symbol Exclusion Enforcement: PASS.** `V3_EXCLUDED_SYMBOLS` (BTC, ETH, LINK, LTC, DOT, BNB, SOL, XRP, DOGE, NEAR, MKR) — `_verify_symbols()` runs the `assert set(cfg.symbols).isdisjoint(V3_EXCLUDED_SYMBOLS)` audit at runner startup (`run_baseline_v3.py:2551`) and per-model. The engineering report's Run Integrity table confirms "V3_EXCLUDED_SYMBOLS audit: PASS". The 3-symbol BCH/LDO/TRX universe is disjoint from the exclusion set.

**Check 10 — Feature Isolation Enforcement: PASS.** `grep "from crypto_trade.features " src/crypto_trade/features_v3/` returns only comment/docstring text, no import statements (Phase 5.5 gate CR10 verified independently). `_verify_track_isolation()` runs at runner startup (`run_baseline_v3.py:2557`); the engineering report run.log confirms "Track isolation: PASS". Zero cross-track imports.

**Check 11 — Forming-Candle / Data-Freshness Audit: PASS.** Phase 5.5 gate CR14 verified the BCH/LDO/TRX 8h.csv `close_time` age at 8.5h — within the 16h staleness threshold. The engineering report run.log line 35 confirms "data fresh (<16h)". `_verify_data_freshness(baseline_symbols + ("BTCUSDT",))` ran for all 3 symbols + BTC. The forming-candle filter in `fetcher.py` (`close_time < now_ms`) is the non-negotiable v3 fix. No future-dated tail row.

**Check 12 — Library Version Pinning: PASS.** Brief Section 9 declares no new dependencies; the pinned stack (`lightgbm 4.6.0, optuna 4.8.0, numpy 2.2.6, pandas 3.0.0, scikit-learn 1.8.0, scipy 1.17.0, statsmodels 0.14.6, pyarrow 23.0.1`) is carried verbatim from /059/081/082/083. The committed EDA (`analysis/iteration_v3-084/canonical_config_and_anchor_staleness.py`) uses only the Python standard library. The methodology fix + universe revert is pure runner configuration.

## Mandatory Adversarial Focus Points

**Focus 1 — `PER_CELL_GAP = 22` correctness.** VERIFIED CORRECT. `PER_CELL_GAP = 22` at `run_baseline_v3.py:1510`. The value is the correct per-cell single-symbol CSCV purge gap: `(timeout_candles + 1) = (21 + 1) = 22`. The per-cell CSCV (`_compute_per_cell_pbo`) iterates over `(symbol, train_month)` cells — each cell's returns matrix is one symbol's candles only; there is no symbol interleaving, so the `× n_symbols` factor that applies to the global pooled `REQUIRED_GAP` does NOT apply. The stale `43 = (42+1)` was a leftover from the reverted /068 42-candle timeout-widening — confirmed by the provenance comment and cross-checked against `tests/strategies/ml/test_per_cell_pbo_synthetic.py:38` which already carried the correct `22` (the runner had been out of sync with its own regression test). The `expected_gap` guard is wired: the per-cell `combinatorial_purged_cv` call at `run_baseline_v3.py:1602-1609` now passes `expected_gap=PER_CELL_GAP`, and `validation_v3.py:118-124` raises `AssertionError` on any `gap != expected_gap` — so the constant cannot silently drift again. The two stale literals (lines 2539, 2610) are fixed. This discharges this Critic's iter-v3/083 Recommendation #2 exactly. CORRECT.

**Focus 2 — `REQUIRED_GAP = 66` and the clean /059-config revert.** VERIFIED — no residual contamination. `REQUIRED_GAP = (21+1)*3 = 66` at `validation_v3.py:65` — correct for the 3-symbol pooled CPCV. `V3_MODELS` is exactly `(BCHUSDT, LDOUSDT, TRXUSDT)` at `run_baseline_v3.py:170-174` — no FILUSDT residue. `V3_FEATURE_COLUMNS_TOP_N` is the 14-feature /059 stack (verified literal at `features_v3/__init__.py:172-185`) — no /082 funding residue (the 4-member funding family was reverted at the /083 setup; the literal-name bans on `funding_rate_zscore_30` / `btc_funding_rate_zscore_30` remain intact). The config-accretion `_canonical_v059` check guards all 11 knobs against /059-canonical with **zero axis carve-out** — confirmed at `run_baseline_v3.py:905-923`. /084 genuinely IS the clean /059-config; the /082 and /083 non-merged changes are fully unwound.

**Focus 3 — Is /084 a VALID measurement?** YES. The run is a methodologically sound measurement of the /059-config 3-symbol universe on current data. Look-ahead: clean (Check 1). Embargo: correct on both the global (66) and per-cell (22) gaps (Check 2). Forming-candle / data freshness: PASS at 8.5h (Check 11). Reproducibility: explicit feature columns, literal seeds, PnL spot-check clean (Check 7). The run is EXPLORATION-mode (3-seed, `EXPLORATION_ENSEMBLE_SIZE=3`) — and this is the **correct** mode for an EXPLORATION-MODE-REFERENCE: the /060→/077 precedent established that the EXPLORATION-mode reference is a 3-seed run distinct from the 10-seed CONFIRMATION baseline. iter-v3/060 and iter-v3/077 were both 3-seed EXPLORATION-mode reference runs; /084 is the exact cycle-3 analogue. The IS +0.8325 / OOS +0.3322 are a valid 3-seed EXPLORATION-mode measurement and can legitimately serve as the cycle-3 EXPLORATION-MODE-REFERENCE that iter-v3/085-091 anchor against. One discipline point for the QR (does NOT affect this verdict — see Recommendation #1): the reference is an EXPLORATION-mode 3-seed number; it is NOT comparable to the /059 CONFIRMATION-mode 10-seed number, and the brief's Section 4 falsifier compared /084's 3-seed numbers directly against /059's 10-seed numbers. That comparison conflates two architectures — but it conflates them in the *conservative* direction (it is the reason both deltas land "outside ±0.10" and the re-anchor fires), so it produces the methodologically correct outcome.

**Focus 4 — The `PER_CELL_GAP` change and prior results.** CONFIRMED — no retroactive invalidation. `PER_CELL_GAP` enters ONLY `_compute_per_cell_pbo` (`run_baseline_v3.py:1513-1622`), which computes the per-cell CSCV/PBO reporting metric. It is computed *after* the trade roster is fixed (the OOF returns matrix is read from `trial_oof_returns.parquet`, which records trial returns from the already-completed Optuna fits). It does not enter the trade roster, the Sharpe, the labeling, the feature pipeline, or the global CPCV. The 43→22 correction therefore changes the per-cell PBO number **going forward only** — it does not retroactively change any prior iteration's headline metrics. Furthermore, the prior direction-of-error was an *over-purge* (43 removed more training data per per-cell test boundary than the correct 22), which biased per-cell PBO *pessimistically* (conservatively) — so no prior iteration's PBO was inflated, and no prior MERGE/NO-MERGE call is invalidated by the correction.

## Assessment of the Re-Anchor

I agree with the QR that **/084 is a valid cycle-3 EXPLORATION-MODE-REFERENCE** and that the re-anchor is the methodologically correct call. The evidence:

- /084's IS +0.8325 / OOS +0.3322 vs /059's recorded IS +1.0894 / OOS +0.5791 → Δ IS −0.2569, Δ OOS −0.2469. Both deltas are ~2.5× the ±0.10 re-anchor band. The brief Section 4 LOCKED rule fires the REFERENCE-REANCHOR branch on both axes — this is a pre-registered, non-discretionary outcome with no post-hoc rationalization path.
- This is the exact /060→/077 precedent. iter-v3/077 — the cycle-2 reference re-run — established the current-code /060-config baseline at IS **+0.8236** / OOS +0.2078, replacing the stale /060 frozen value. Note that /077's IS +0.8236 and /084's IS +0.8325 are strikingly close (both ~0.82-0.83), while /059's CONFIRMATION-mode IS is +1.0894. This corroborates that the gap is a genuine and stable EXPLORATION-mode-vs-CONFIRMATION-mode architecture gap plus data-extent drift — NOT a /084-specific anomaly or a wiring defect. A 3-seed EXPLORATION-mode run of the /059 config landing ~0.82-0.83 IS is the expected, reproducible reference; the prior cycle-3 EXPLORATIONs anchoring against the 10-seed +1.0894 number were anchored against a non-comparable reference.
- BASELINE_V3.md's canonical /059 CONFIRMATION baseline and tag `v0.v3-059` are correctly left UNCHANGED — an EXPLORATION-mode iteration cannot update the baseline, and the QR's brief Section 4.3 / Section 8 hold this discipline. The /092 CONFIRMATION correctly continues to anchor against the /059 CONFIRMATION baseline (a 10-seed number), separate from the /084 EXPLORATION-MODE-REFERENCE (a 3-seed number).

The re-anchor — cycle-3 EXPLORATIONs /085+ anchoring their Δ against /084's IS +0.8325 / OOS +0.3322 instead of /059's IS +1.0894 / OOS +0.5791 — is the methodologically correct call. The final REFERENCE-REANCHOR classification is the QR's Section 8 call; this Critic's read is that the methodology supports it.

## Recommendations to QR

This iteration's verdict is FINAL. These are process-level recommendations for the diary and for future iterations.

1. **The diary must record the EXPLORATION-mode-vs-CONFIRMATION-mode architecture distinction explicitly when stating the re-anchor.** The brief's Section 4 falsifier compared /084's 3-seed EXPLORATION-mode IS +0.8325 / OOS +0.3322 directly against /059's 10-seed CONFIRMATION-mode IS +1.0894 / OOS +0.5791. These are two different ensemble architectures (`EXPLORATION_ENSEMBLE_SIZE=3` vs `CONFIRMATION_ENSEMBLE_SIZE=10`), and a substantial part of the Δ −0.2569 IS / Δ −0.2469 OOS is the architecture gap, not data-extent staleness — corroborated by /077's 3-seed reference landing at IS +0.8236, near-identical to /084's +0.8325, while the /059 10-seed CONFIRMATION is +1.0894. The re-anchor outcome is still correct (and the comparison errs conservative), but the diary should decompose the gap into (a) the EXPLORATION-vs-CONFIRMATION architecture component and (b) the genuine data-extent component, rather than attributing the full ~0.25 to anchor staleness. The honest framing: the prior cycle-3 EXPLORATIONs /082-/083 were anchoring their EXPLORATION-mode deltas against a non-comparable 10-seed CONFIRMATION number — /084 corrects the *reference architecture mismatch*, which is a stronger and cleaner finding than "the /059 config went stale."

2. **Pre-register the EXPLORATION-MODE-REFERENCE / CONFIRMATION-baseline distinction in the cycle-3 plan and in every /085-091 brief Section 4.** To prevent the next reference re-run from re-discovering this, every cycle-3 EXPLORATION brief should explicitly state two anchors: the EXPLORATION-MODE-REFERENCE (/084: IS +0.8325 / OOS +0.3322, 3-seed) for intra-cycle Δ classification, and the /059 CONFIRMATION baseline (IS +1.0894 / OOS +0.5791, 10-seed) reserved for the /092 CONFIRMATION. Mixing the two is the error /082-/083 made; making the two-anchor structure explicit in the brief template closes it permanently.

3. **The per-cell PBO is now on a corrected gap — note in the diary that /084's per-cell PBO is not directly comparable to pre-/084 iterations' per-cell PBO.** The `PER_CELL_GAP` 43→22 correction means /084's per-cell PBO (and every future iteration's) is computed with a less-conservative, correct purge. It happens to land at 0.1278 — identical to /059 — because most `(symbol, month)` cells show PBO=0.0 regardless of the gap, but this coincidence should not be read as "the fix had no effect." The diary should record that per-cell PBO comparisons across the /084 boundary are gap-regime-discontinuous: pre-/084 used gap=43 (over-purged, pessimistic), /084-onward uses the correct gap=22. Any future cross-iteration per-cell-PBO trend analysis must respect that discontinuity.
