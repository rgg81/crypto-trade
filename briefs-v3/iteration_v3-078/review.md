# Phase 7.5 Critic Review — iter-v3/078

OVERALL: MERGE

The SUSPICIOUS-OOS-DOMINANT classification is certified clean. iter-v3/078 does NOT advance; LDOUSDT is retained; the universe-revision axis is closed for cycle 2. The diagnostic lessons (T6 escapability premise falsified for the *added* symbol; ADA loads the IS/OOS regime factor via systematically longer-held trades) are recorded. No methodology violation found: the T7 IS-edge screen is provably IS-only, the BCH/TRX bit-stability is independently verified, and no OOS-tuning violation exists.

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION — cycle 2 #8 of 10. Single-axis (universe revision, LDOUSDT→ADAUSDT). Per the brief Section 8.5 LOCKED taxonomy this is a NO-MERGE-advancing classification regardless of outcome; "MERGE" here means the Critic certifies the data point lands cleanly on the branch — it does not advance the swap to the cycle-2 CONFIRMATION.

## Foundation Audit (Boot Steps 9-11)

PASS. `run_baseline_v3.py` has `ITERATION_LABEL = "v3-078"` (line 128), `V3_MODELS = (("A (BCHUSDT)","BCHUSDT"), ("C (ADAUSDT)","ADAUSDT"), ("D (TRXUSDT)","TRXUSDT"))` (lines 154-158) — LDOUSDT removed, ADAUSDT added, 3-symbol universe preserved. `walk_forward.py:113` carries the embargo fix — `train_end_ms = test_start_ms - embargo_ms` — with `compute_embargo_candles(10080, 480) = 10080//480 + 1 = 22` candles purged per (symbol, month); this is the post-`e149e9d`-fix state inherited from /058, NOT the buggy `train_end_ms = test_start_ms` form. The `efficiency_ratio_50` / `range_efficiency_50` ban is intact at six runner sites (lines 361-368, 527-533, 540-546, 721-728) plus the pre-flight smoke loop. `DEFAULT_ATR_MULTIPLIERS = (2.0, 1.0)` asserted (line 459); `V3_FEATURES_PER_SYMBOL` and `V3_ATR_MULTIPLIERS_PER_SYMBOL` asserted empty. `REQUIRED_GAP = 66 = (21+1)×3` unchanged (universe count stays 3). Sacred constants `OOS_CUTOFF_DATE = "2025-03-24"`, `TRAINING_MONTHS = 24` confirmed.

## §11 Anti-Pattern Static Scan

CLEAN. The six `LDOUSDT` references remaining in `run_baseline_v3.py` (lines 141, 296, 303, 506, 559, 1582) are all in comments. Line 1582 (`# LDOUSDT returns (1.5, 0.75)`) is a stale boilerplate comment with zero behavioral effect — `V3_ATR_MULTIPLIERS_PER_SYMBOL` is empty so `atr_multipliers_for_symbol` returns `(2.0, 1.0)` for every symbol; recorded as a documentation hygiene item, not a defect. The pre-flight smoke loops (lines 507, 560-563) iterate `("BCHUSDT", "ADAUSDT", "TRXUSDT")` — no stale LDO hardcode in any executable assertion path. `feature_columns=list(features_for_symbol(symbol))` (line 1600) — explicit list, never None. `ensemble_seeds=list(ensemble_seeds)` (line 1598) explicit. `active_ensemble_seeds = ENSEMBLE_SEEDS[:3]` = `(191664963, 1662057957, 1405681631)` — matches the engineering report seed list verbatim.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS
No look-ahead in any audited path. The load-bearing concern — the T7 IS-edge screen that selected ADAUSDT — was traced end-to-end in `analysis/iteration_v3-078/axis_selection_eda.py:911-1058`. Line 962 builds `is_months` strictly as months with `start_time.value // 1e6 < OOS_CUTOFF_MS`; lines 968-973 iterate `test_month` ONLY over `is_months` with the training window terminating at `te = ts - embargo×_8H_MS` (embargo-purged, 22 candles); the monthly-PnL Sharpe at line 1051 is computed on that IS-only series. The screen never reads a bar at or past `OOS_CUTOFF_MS` for any candidate, ADAUSDT included. The BTC monthly regime label (`build_btc_monthly_regime`, lines 316-335) applies `.shift(1)` BEFORE the rolling SMA_270 (lines 328-329) — past-only. T1 and T6 (lines 451-489, 855-905) DO read /060 OOS roster CSVs, but every OOS quantity is emitted as a display-only table row tagged INFORMATIONAL and feeds no `sort`/`argmax`/`filter`/threshold; verified by reading both function bodies. The labeling σ_t path is unchanged from /059 (no axis touched it). Universe selection is not survivor-screened — ADAUSDT's parquet runs from 2020-01-31, the screen evaluates 59 IS months, the swap target is the T7 IS-Sharpe argmax.

### Check 2 — Embargo Width: PASS
Required gap = `(timeout_candles + 1) × n_symbols`. With `timeout_minutes = 10080`, `candle_minutes = 480` → `timeout_candles = 21`; `n_symbols = 3` → required gap `= 22 × 3 = 66`. The runner's `_verify_label_leakage_gap()` (lines 902-920) asserts the formula equals `REQUIRED_GAP = 66` and the engineering report Label Leakage Audit confirms `REQUIRED_GAP = 66`, walk-forward embargo `= 22` candles. Actual = required, symmetric. Note for the record: per `feedback_v3_walkforward_lookahead_bug.md`, all v3 absolute Sharpe values carry a uniform upward bias from a residual issue — but this worktree's `walk_forward.py:113` carries the `e149e9d` fix (verified above), and the brief / engineering report correctly flag that the /078-vs-/077 deltas are valid (same bias) while absolute magnitudes are uniformly inflated. The classification depends only on deltas; the bias does not move the verdict.

### Check 3 — Multiple-Testing Correction: INFORMATIONAL (EXPLORATION)
Per `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode DSR/PSR are structural regime artifacts at `n_trials = 315` (not the CONFIRMATION `n_trials = 1050+` regime) and do not trigger MERGE-gate evaluation. For the record: `dsr.json` reports `dsr = 0.0`, `psr = 1.0`, `dsr_relative_b4 = 0.999912`, `pbo = 0.12767`, `n_trials = 315`, `n_eff = 19`. PBO 0.1277 is the one threshold-comparable quantity here and clears the 0.40 ceiling comfortably; it is near-bit-identical to the /077 anchor PBO (0.1278) — expected, since PBO is cell-level near-invariant and only the ADA cells (vs LDO cells) changed. `frac_positive_paths = 0.733` (gate threshold 0.55, PASS). No Check-3 axis fails even on the informational reading. n_eff = 19 is sensible.

### Check 4 — IC Correlation: PASS
`ic_matrix.csv` present (14×14). No feature family was added or removed in this iteration — the 14-feature anchor stack is unchanged. The relevant pairwise maximum off-diagonal is `regime_momentum_signed_5d ↔ vwap_dev_20` at 0.7688. This pair is a pre-existing baseline characteristic (carried unchanged from /059), and `regime_momentum_signed_5d` is a Category-2 composed feature for which the strict |IC|<0.70 gate does not apply per `feedback_v3_engineered_feature_pivot.md` (mechanical correlation with primitives by construction; importance≥30 is the relevant gate). Since iter-v3/078 introduces no new feature family, Check 4 has nothing to flag — the iteration cannot have created a redundancy.

### Check 5 — ADF Stationarity: PASS
`adf_test.csv` present, 2646 rows (3 symbols × 14 features × 63 months — a per-month panel, not the single end-of-window check). 2187/2646 (82.7%) cells stationary at p<0.05. The `False` rows concentrate in thin early-history months (2020-2021); the 2020-01 rows have empty `adf_statistic` (insufficient sample). At the end-of-training-window month relevant to the merge gate (2025-02/2025-03, the last IS months before the 2025-03-24 cutoff), the 14 features are predominantly stationary — 2025-03 shows all 14 stationary for TRX. The two transient non-stationary features in 2025-02 (`ret_skew_200`, `ret_kurt_200`) are stationary in 2025-03. `run.log` reports zero ADF WARNING lines and zero `cusum_reset_count` lines; the stale ADF secondary-falsifier was removed at /077 (`20c65cd`). No iteration-introduced non-stationarity — the feature pipeline is unchanged from /059.

### Check 6 — Pareto Dominance: PASS (not applicable)
Under the unified-architecture (and EXPLORATION 3-seed) regime, Gate 10-Pareto is RETIRED and replaced by Gate 10-CPCV (`frac_positive_paths ≥ 0.55`). `pareto_front.csv` is no longer produced (BASELINE_V3.md Phase B-3 architecture). `dsr.json` reports `frac_positive_paths = 0.733` — clears 0.55. The CPCV 45-path matrix (`cpcv_paths.csv`) shows path-Sharpe Q25/Q50/Q75 = -0.0268/0.7666/1.4011, a healthy spread. No seed-selection dominance question arises — the EXPLORATION run is a single deterministic roster from the 3-seed `ENSEMBLE_SEEDS[0:3]` averaged inference path; `ensemble_summary.json` confirms the three outer=42-lineage seeds.

### Check 7 — Reproducibility: PASS
The setup commit `0648504` and gate `9b475b4` exist in the commit chain. The runner uses an explicit `feature_columns` list (line 1600) — not None, not auto-discovered. Ensemble seeds are the literal `ENSEMBLE_SEEDS` tuple sliced to `[0:3]`. PnL spot-check on `out_of_sample/trades.csv`: line 2 (BCH SHORT, take_profit) — `pnl_pct = (303.87 − 282.100779)/303.87 × 100 = 7.1640` ✓, `net_pnl_pct = 7.1640 − 0.10 = 7.0640` ✓, `weighted_pnl = 7.0640 × 0.33 = 2.3311` ✓; line 5 (ADA SHORT, stop_loss) — `pnl_pct = (0.7815 − 0.808994)/0.7815 × 100 = −3.5181` ✓, `net = −3.6181` ✓, `weighted = −3.6181 × 0.61 = −2.2070` ✓. Sign and arithmetic correct on both directions. **BCH/TRX bit-stability independently verified**: the /077 anchor `comparison.csv` reports OOS per-symbol BCH +1.9078/37, TRX +24.7184/54; the /078 `comparison.csv` reports BCH +1.9078/37, TRX +24.7184/54 — bit-identical to 4 decimals. LDO −18.4079/12 was replaced by ADA +0.4230/18. The universe swap of the third symbol did not leak into the independent BCH/TRX per-symbol models — no wiring defect. The Section 4.3 bit-stability falsifier does NOT fire.

### Check 8 — Hypothesis-Implementation Alignment: PASS
Brief Section 1 hypothesis: replace LDOUSDT with ADAUSDT to lift the IS aggregate without re-triggering the /075 regime tension. Brief Section 3.1 authorizes exactly one code change — `V3_MODELS` LDOUSDT→ADAUSDT — plus the `ITERATION_LABEL` bump and pre-flight assertion-site updates. The runner shows precisely that: `V3_MODELS` is BCH/ADA/TRX, `ITERATION_LABEL = "v3-078"`, the smoke loops iterate the new universe tuple, and every other parameter (14-feature stack, ATR labeling, 7-primitive risk gates, `ENSEMBLE_SEEDS`, Optuna search) is unchanged. The /077 conditional-orthogonality instrumentation is carried forward unchanged (accretive tooling, not a varied axis — `conditional_orthogonality.csv` is emitted, 14 rows, HYBRID PART A+B). No scope creep, no hypothesis-faking — the implementation tests exactly the registered single axis. The hypothesis was FALSIFIED by the result (IS Δ −0.0035 < the +0.10 Section 4.2 falsifier), but a falsified hypothesis with a faithful implementation is a clean negative data point, not a Check-8 failure.

## Optional Checks 9-12

- **Check 9 — Symbol Exclusion Enforcement: PASS.** `V3_EXCLUDED_SYMBOLS` (`src/crypto_trade/features_v3/__init__.py:387-403`) contains 11 symbols (BTC, ETH, LINK, LTC, DOT, BNB, SOL, XRP, DOGE, NEAR, MKR). ADAUSDT is NOT in the set — the swap-in is legal. LDOUSDT is NOT in the set either — LDO is removed from `V3_MODELS` only (still an allowed symbol). The runner's `_verify_symbols` asserts `set(cfg.symbols) & V3_EXCLUDED_SYMBOLS` is empty (lines 200-207); the EDA `main()` independently asserts `SWAP_IN not in V3_EXCLUDED` (lines 1168-1170). Both audits pass for BCH/ADA/TRX.
- **Check 10 — Feature Isolation: PASS (not applicable).** No cross-track feature import introduced; no feature module touched this iteration.
- **Check 11 — Forming-Candle Audit: PASS.** Phase 5.5 gate confirms `data/ADAUSDT/8h.csv` latest close 2026-05-14 16:00 UTC; the engineering report confirms a Phase-6 pre-flight fetch/regen (`--skip-features` used because ADA parquet was pre-regenerated at setup). `run.log` shows the data-extent assertion `[ADF] 2646 rows; expected ∈ [2646, 2646]` passed for all three symbols.
- **Check 12 — Library Version Pinning: PASS.** Brief Section 9 pins lightgbm 4.6.0 / optuna 4.8.0 / numpy 2.2.6 / pandas 3.0.0 / scikit-learn 1.8.0 / scipy 1.17.0 / statsmodels 0.14.6 / pyarrow 23.0.1 — unchanged from /077; no new library.

## Special Adjudications

### Adjudication #1 — SUSPICIOUS-OOS-DOMINANT classification: CERTIFIED

Independently applying the brief Section 8 LOCKED disjunctive taxonomy (evaluation order SUSPICIOUS → NULL-RESULT → NEGATIVE → PROMISING → INERT) against the re-anchored anchor IS +0.8236 / OOS +0.2078:

- IS shift = 0.8201 − 0.8236 = **−0.0035**. OOS shift = 0.6892 − 0.2078 = **+0.4814**. OOS/IS ratio = 0.8404.
- **Section 8.4 ratio gate**: 0.8404 < 3.0 — does NOT fire.
- **Section 8.4 SUSPICIOUS-OOS-DOMINANT sub-mode** (IS shift < 0 AND OOS shift ≥ +0.20): IS shift −0.0035 < 0 is TRUE; OOS shift +0.4814 ≥ +0.20 is TRUE — **FIRES**.
- **Section 8.5 NULL-RESULT**: a universe swap fully replaces the LDO sub-roster — mechanically impossible; the /078 IS/OOS rosters contain 74/18 ADA trades absent from /060. Does not apply.
- **Section 8.2 NEGATIVE**: IS −0.0035 > −0.10 and OOS +0.4814 > −0.20 — does not fire.
- **Section 8.1 PROMISING**: requires IS shift ≥ +0.10 independently. IS shift −0.0035 < +0.10 — **fails its own IS gate** irrespective of SUSPICIOUS precedence.

SUSPICIOUS-OOS-DOMINANT is the first canonical match in disjunctive order. The verdict is certified. Two points held the QE to discipline and both are confirmed: (1) the QE did NOT reclassify to PROMISING despite the eye-catching OOS +0.48 — and could not have, since PROMISING fails the +0.10 IS gate on its own terms; (2) the sub-mode is an independent SUSPICIOUS ground **with no magnitude qualifier** as the QR LOCKED it in Section 8.4 and pre-registered at ≈43% in Section 7. For the record, as the mandate notes: /078's IS shift is mild-negative (−0.0035, essentially flat) rather than the large IS collapses of /071/073/076 (−0.55 to −0.79), and the ratio (0.84) is healthy — but the classification is SUSPICIOUS-OOS-DOMINANT regardless because the sub-mode fires on its independent ground. The verdict stands and is not softened.

### Adjudication #2 — the +2.23-candle holding-time falsifier and the regime-divergence mechanism: VERIFIED — SUSPICIOUS DOUBLY CORROBORATED

The QE's added-vs-removed production duration gap was independently recomputed from the trade CSVs (duration = `(close_time − open_time) / 28,800,000` ms per 8h candle).

- **ADA IS timeout trades**: from `reports-v3/iteration_v3-078/in_sample/trades.csv` the 5 ADAUSDT rows with `exit_reason = timeout` (CSV lines 4, 148, 151, 186, 213) each show `close − open = 604,800,001 ms = 21.0 candles`. Exactly 5 timeout trades at 21.0 candles — QE claim confirmed.
- **LDO IS profile**: `reports-v3/iteration_v3-077/in_sample/trades.csv` has 11 LDOUSDT rows, **zero** with `exit_reason = timeout` (grep confirmed). LDO IS durations computed: {20, 4, 2, 1, 1, 2, 7, 3, 2, 4, 8} → mean = 54/11 = **4.909 candles** (QE reports 4.91 ✓).
- **ADA IS mean duration**: with 5 timeouts at 21.0 (= 105 candle-units) and 69 non-timeout trades, the QE's reported ADA mean 7.14 implies non-timeout mean (527.97 − 105)/69 ≈ 6.13 — internally consistent, and spot-checked non-timeout ADA durations confirm a ~6-candle non-timeout mean.
- **Production added-vs-removed gap = 7.135 − 4.909 = +2.226 ≈ +2.23 candles**, against the T8 label-implied prediction of +0.378 — the Section 4.4 added-vs-removed sub-channel falsifier (threshold +1.0 candle) **FIRES**. Even excluding the 5 timeout trades the non-timeout gap (6.13 − 4.91 = +1.22) clears +1.0.

Assessment of the mechanism: yes — the +2.23-candle gap means ADA's roster is systematically longer-held than LDO's was, and per `feedback_v3_is_oos_regime_divergence.md` longer-held trades win in the OOS uptrend and bleed in IS bear/chop. /078's OOS +0.48 lift is therefore the holding-time-extension regime-divergence mechanism (sub-channel c, the /076 trade-selection channel) arriving via the universe swap rather than via a barrier axis — the swap replaced an entire symbol's model with another symbol's model whose label-implied roster is duration-loaded. /078's SUSPICIOUS verdict is **doubly corroborated**: the OOS-DOMINANT sub-mode fires AND the holding-time channel falsifier fires. The OOS lift is confirmed uncorroborated regime-luck, not robust edge. This is reinforced by the per-symbol OOS attribution — ADA's own OOS is weak (18 trades, 27.8% WR, `net_pnl_pct` −14.56%, `weighted_pnl` +0.42), so the aggregate OOS lift to +0.6892 is dominated by the elimination of LDO's −18.41 OOS `weighted_pnl` drag (BCH +1.9078 and TRX +24.7184 are bit-frozen), not by ADA producing edge.

### Adjudication #3 — the EDA T6 "escapability" premise was falsified: RECORDED — premise was INCOMPLETE, not wrong

The QR's T6 was the load-bearing axis justification: it argued LDOUSDT is the one in-universe symbol whose drag is NOT regime-split-correlated (LDO bull-SHORT weak in BOTH windows: IS −1.16 AND OOS −12.58, `T6_escapability_bound.csv`), so removing LDO lifts IS without the IS-up/OOS-down tension. /078 falsified the *conclusion* — the OOS lifted, the IS did not, the holding-time falsifier fired.

The T6 reasoning about LDO itself was not wrong: T6's claim concerned the *removed* symbol's regime-cleanliness, and removing LDO did indeed not re-trigger the tension *through LDO*. The defect is that T6 reasoned exclusively about the **removed** symbol while the regime-loading came from the **added** symbol — ADA's longer-held roster (the +2.23-candle channel) loaded the regime factor. The /076 Critic already flagged exactly this gap ("evidence about the removed symbol is not a proof about the added symbol"), and the QR's own brief Section 7 explicitly acknowledged it: *"T6 is evidence that the removal is regime-clean; it is not a proof about the added symbol."* T8 was the QR's attempt to bound the added-symbol channel — but T8's label-implied first-touch simulation (fixed-parameter) predicted +0.38 candles and missed the production +2.23 by 6×, because T8 used screen-grade default barrier parameters whereas the production walk-forward uses Optuna-optimized timeout parameters that produced 5 ADA timeout trades LDO never incurred. The lesson for future universe-revision axes: an added-symbol holding-time falsifier must be computed under the production Optuna-tuned parameterization (or with an explicit duration-distribution band), not a fixed-parameter label-implied proxy — a screen-grade simulation systematically under-predicts the timeout-trade tail. T6's escapability bound was INCOMPLETE (correct about LDO, silent about the production behavior of the replacement), and the QR's honest Section 7 caveat means the verdict cannot be charged as a methodology failure — it is a recorded diagnostic lesson.

## Recommendations to QR

These are process-level inputs for the cycle-2 CONFIRMATION brief (iter-v3/081) and future universe-revision axes — not a fix list for this non-advancing iteration.

1. **Universe-revision axes need a production-parameterized added-symbol holding-time band.** The T8 label-implied +0.38 prediction missed the production +2.23 by 6× because it used fixed default barrier/Optuna parameters. Any future swap-target screen must either run the duration estimate under the production Optuna-tuned timeout regime or pre-register a duration-distribution band wide enough to admit the timeout-trade tail; a screen-grade fixed-parameter proxy is structurally biased low and gives false holding-time-orthogonality reassurance.
2. **An IS-edge screen measures per-symbol Sharpe; it does not predict portfolio-aggregate Sharpe lift under BCH dominance.** T7 correctly screened ADA at +0.617 per-symbol, but the production 3-seed walk-forward aggregate is dominated by BCH (76.9% of IS `weighted_pnl`); ADA's added IS variance (+34% monthly std) matched its added mean (+33%), pinning aggregate IS Sharpe flat. Future swap evidence should include a portfolio-aggregate IS-Sharpe sensitivity projection conditioned on the incumbent symbol's IS share, not only the candidate's standalone IS edge — this is the BCH-IS-concentration fragility flag from the /059 baseline, now empirically confirmed for a universe swap.
3. **The cycle-2 CONFIRMATION (iter-v3/081) cannot bundle the LDO→ADA swap as an edge ingredient.** /078 is the fourth cycle-2 SUSPICIOUS-OOS-DOMINANT data point (after /071, /073, /076); the universe-revision axis is now closed for the cycle. Per `feedback_v3_strict_both_is_oos_baseline.md` the CONFIRMATION's BOTH-must-improve gate would reject it on the flat IS regardless, and the doubly-corroborated regime-luck OOS lift is non-compoundable. Cycle 2 has now exhausted feature (T3), meta-labeling M2 (T5), and universe-revision axes against the LDO structural-weakness problem — the CONFIRMATION brief should treat LDO drag as a standing unresolved constraint and the cycle-3 axis priorities should pivot away from all three exhausted categories.
