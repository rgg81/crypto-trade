# Phase 7.5 Critic Review — iter-v3/082

OVERALL: MERGE

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (cycle-3 EXPLORATION #1 of 10; Direction 1 — NEW crypto-native funding-rate feature family). Per the two-round protocol, this iteration's Section 0.5 declares TYPE unambiguously; Check 3 (DSR/PBO/PSR) is scored informationally and only Check 3's PBO axis is BLOCK-eligible for EXPLORATION. All other checks are scored with full methodological rigor.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

Audited all four funding features in `src/crypto_trade/features_v3/funding_v3.py:395-492` (`compute_funding_family`), the runner's only iteration-touched feature path. The mandatory adversarial focus point #2 (funding settles every 8h = one 8h candle) is the central concern; I traced each construction independently rather than trusting the QE's spike-perturbation claim.

- `funding_sign_persist_9` (line 449-455): `np.sign(r).shift(1).rolling(9, min_periods=9).mean()`. The `.shift(1)` precedes the rolling window, so bar `t`'s window is `[t-9, t-1]` — bar `t`'s own settlement is excluded. Past-only confirmed.
- `funding_momentum_3` (line 459-460): `r - r.shift(3)`. `r[t]` is the rate that settled at candle `open_time` T, broadcast ~5 min before the 8h boundary — knowable at bar `t` open, which is when the bar-`t+1` decision is made. `r[t-3]` trivially past. This is the inherently-bar-close case from the Critic false-positive list, not a leak. Past-only confirmed.
- `funding_accel_3` (line 464): `mom - mom.shift(3)` where `mom` is the past-only momentum series. Past-only by construction.
- `funding_price_divergence_6` (line 469-490): the cumulative-funding leg uses `r.shift(1).rolling(6).sum()` then a 60-bar z-norm on the already-shifted series; the price leg uses `(logc - logc.shift(6)).shift(1)` then the same 60-bar z-norm. Both legs carry an explicit `.shift(1)` before any windowing. Window `[t-6, t-1]`, norm history `[t-66, t-1]`. Past-only confirmed.

The merge logic (lines 435-443) is a left-merge of the kline frame onto funding records on a minute-rounded `open_time` — it joins funding observation at `t` to kline bar `t`, then every downstream rolling stat shifts. No future funding record reaches an earlier bar. The QE's `tests/features_v3/test_funding_family_v3.py::test_past_only_no_lookahead` (perturbing `funding_rate[250]` by +5.0, asserting all four columns unchanged at bars `< 245`) is a genuine adversarial spike-perturbation test, and it is consistent with my hand-trace. No look-ahead found.

### Check 2 — Embargo Width: PASS

Labeling config: triple-barrier 21-candle timeout (`timeout_candles = 21`), `n_symbols = 3` (BCH/LDO/TRX, unchanged). Required gap per López de Prado purge = `(21+1) × 3 = 66`. The runner pins `REQUIRED_GAP = 66` and the config-accretion pre-flight (`run_baseline_v3.py:882`) asserts `REQUIRED_GAP == 66` — verified as a hard runtime check. The engineering report's "Pre-flight: label-leakage gap = 66 = (21+1) × 3 PASS" line confirms the runner-side assertion fired. Walk-forward embargo is 22 candles per BASELINE_V3.md (`compute_embargo_candles(10080, 480) = 22`) carried unchanged from /059 via the post-`e149e9d`-fix walk-forward. The iteration touches no barrier and no symbol-count knob, so the /059 embargo is the correct and unchanged requirement. Actual gap (66) equals required gap (66). Symmetric application verified via the single-source-of-truth helper. PASS.

### Check 3 — Multiple-Testing Correction: FAIL (informational for EXPLORATION — does NOT trigger BLOCK)

`dsr.json`: DSR = 0.0000, PBO = 0.1284, PSR = 1.0000, `frac_positive_paths` = 0.6444, `n_trials` = 315, `n_eff` = 19.

- **DSR = 0.0000** misses the 0.95 threshold. This is the documented EXPLORATION-mode structural artifact (`feedback_v3_dsr_mode_artifact.md`): at `n_trials = 315` the López de Prado `E[max_SR]` formula returns a required Sharpe exceeding the observed annualized Sharpe, collapsing DSR to zero. CONFIRMATION-mode DSR (`n_trials ≈ 3150`) is the gate-relevant metric, not this. DSR-axis FAIL is **informational only** for TYPE=EXPLORATION and does NOT trigger BLOCK.
- **PSR = 1.0000** clears 0.95. (Note `dsr_relative` = 1.0 and `dsr_relative_b4` = 1.0 in `dsr.json` — the Path-B4 reformulation; informational.)
- **PBO = 0.1284** clears the 0.4 threshold comfortably — this is the BLOCK-eligible Check 3 axis for EXPLORATION, and it PASSES. The atomic-write fix scrutiny (focus point #1) bears directly on this number's integrity; see Check 7 — the `combined` DataFrame feeding per-cell PBO is computed identically pre/post-fix.

`n_trials = 315` matches the EXPLORATION budget exactly (35 trials × 3 symbols × 3 seeds = 315; engineering report Run Integrity line confirms). `n_eff = 19` is sensible (per-cell PCA-95 median, architecture-independent, identical to /059). The DSR-axis FAIL is a known regime artifact, not a methodology defect; the BLOCK-eligible PBO axis passes. Net: Check 3 does not block this EXPLORATION.

### Check 4 — IC Correlation: PASS

`ic_matrix.csv` is present (the schema-required Engineer output — no auto-FAIL). The four new funding features are the "new" family vs the 14 anchor features.

Cross-family (new-vs-anchor) maximum |IC|, read directly from `ic_matrix.csv` rows 16-19:
- `funding_sign_persist_9`: max |IC| = 0.218 (vs `btc_ret_14d`)
- `funding_momentum_3`: max |IC| = 0.080 (vs `vwap_dev_20`)
- `funding_accel_3`: max |IC| = 0.012 (vs `ema_spread_atr_20`)
- `funding_price_divergence_6`: max |IC| = 0.220 (vs `vwap_dev_20`)

Maximum across all four = 0.220 — far below the 0.70 hard gate and below the 0.50 strict target. The family is genuinely orthogonal to the price/return/vol/regime anchor stack.

**Intra-family IC (focus point #6):** `funding_momentum_3` vs `funding_accel_3` |IC| = **0.816** (`ic_matrix.csv` row 17, col `funding_accel_3`). This is the highest intra-family pair and exceeds 0.70. The IC discipline carve-out applies and I have adjudicated it substantively, not waved it through: `funding_accel_3` is the exact algebraic second difference of `funding_momentum_3` (`accel[t] = mom[t] - mom[t-3]`, verified at `funding_v3.py:464` and unit-tested at `test_funding_family_v3.py::test_accel_is_second_difference_of_momentum`). A momentum / second-difference pair MECHANICALLY correlates by construction — this is the established v3 Category-2 carve-out (`feedback_v3_engineered_feature_pivot.md`), the identical pattern to the in-baseline `hurst_100` ~ `hurst_diff_100_50` pair. The brief pre-registered this exact pair at Section 2 T2 with the carve-out citation, and the EDA T3 shows the two carry *different* conditional signal (on LDO, accel separates -0.0145 while momentum separates -0.0041). The strict |IC|<0.50 gate is inapplicable to a derived second-difference feature; the relevant bar is importance ≥ 30, which the brief's Section 8.4 INERT taxonomy correctly applies. The 0.816 intra-pair is a known, pre-registered, mechanically-forced correlation — not an undisclosed redundancy. PASS.

One process note for the QR (not a check failure): the brief Section 2 T2 quoted intra-family max |IC| as "0.73–0.77" from the IS-only EDA, but the runner-computed pooled `ic_matrix.csv` reports 0.816. The engineering report (line 209) discloses this discrepancy honestly. The gap is the pooled-vs-per-symbol and full-date-vs-IS-only computation difference; it does not change the carve-out adjudication (both numbers are above 0.50, the pair is a mechanical momentum/2nd-difference pair either way), but the brief's pre-registered number under-stated the realized correlation.

### Check 5 — ADF Stationarity: PASS

The engineering report's ADF section reports per-feature stationarity pass rates pooled across IS months: `funding_accel_3` 96.8%, `funding_momentum_3` 96.8%, `funding_price_divergence_6` 93.0%, `funding_sign_persist_9` 89.8%; combined 94.1% (591/628 month-cells). The EDA T5 independently reports all 12 cells (4 features × 3 symbols) stationary at p < 0.05, most p < 1e-15. Every funding feature is price/funding-derived, not a raw-level regime indicator, so the regime-indicator exemption is not invoked and not needed. `funding_sign_persist_9` has the lowest rate (89.8%) — above the standard 80% v3 threshold — and is economically expected to be the least stationary (sign-persistence is the most regime-coupled member, consistent with its T6 marginal regime correlation of 0.10–0.21). All four features are stationary by ADF at the end of the training window. PASS.

### Check 6 — Pareto Dominance: PASS

Under the unified-architecture v3 baseline, Gate 10-Pareto was RETIRED and replaced by Gate 10-CPCV (`cpcv_frac_positive_paths ≥ 0.55`) per BASELINE_V3.md. `pareto_front.csv` here is a 45-row CPCV-path table (`path_id, sharpe, max_dd, n_candles`), not a 10-seed metric-vector front — there is no chosen-seed-vs-other-seed dominance comparison to run for a 3-seed EXPLORATION. The applicable replacement gate: `dsr.json` `pbo_frac_positive_paths` = 0.6444, `cpcv_frac_positive_paths_gate_pass` = true, threshold 0.55 — PASS at 0.6444 (28/44 of `cpcv_paths.csv` rows have positive Sharpe; consistent with the 0.644 figure). CPCV path-Sharpe distribution q25/q50/q75 = -0.243 / +0.335 / +0.838 is a healthy spread with a positive median. The Gate-10 successor passes. PASS.

### Check 7 — Reproducibility: PASS

Four reproducibility properties plus the focus-point-#1 atomic-write scrutiny:

1. **Commit SHA stamped and exists.** Engineering report stamps the commit chain (`37d4da8` EDA, `275d24a` brief, `87195d1` setup, `034d7e6` re-gate, `a7abba4` OOF fix, HEAD `a7abba4`). The repository's recent-commit log confirms the iteration-v3/082 branch carries the funding-family setup.
2. **Explicit `feature_columns`.** `run_baseline_v3.py:1696` passes `feature_columns=list(features_for_symbol(symbol))` — never `None`, never auto-discovered. `features_for_symbol` returns `V3_FEATURE_COLUMNS_TOP_N` (the 18-feature tuple). The `_verify_feature_columns` pre-flight (`run_baseline_v3.py:347`) hard-asserts `len == 18` and asserts each of the 4 funding columns is present by literal name. The `feedback_explicit_feature_columns.md` invariant holds.
3. **Ensemble seeds literal.** `ENSEMBLE_SEEDS` is a hardcoded 10-tuple at `run_baseline_v3.py:103-114`; EXPLORATION mode uses the literal `[0:3]` slice `(191664963, 1662057957, 1405681631)` — the outer-42-lineage subset, matching the engineering report's "3 seeds, all outer-42 lineage" line. No runtime seed derivation in the active path.
4. **Trade-row PnL spot-check.** I recomputed three random OOS trades from `reports-v3/iteration_v3-082/out_of_sample/trades.csv`. Row 8 (BCH SHORT, SL): `(392.92 − 407.255596)/392.92 = −3.6485%`; `net = −3.7485`; `weighted = −3.7485 × 0.71 = −2.6614` — exact. Row 21 (BCH LONG, TP): `(522.63894 − 488.65)/488.65 = +6.9557%`; `net = +6.8557`; `weighted = +6.8557 × 0.97 = +6.6500` — exact. Row 11 (TRX SHORT, SL): `(0.27806 − 0.28265)/0.27806 = −1.6507%`; `net = −1.7507`; `weighted = −0.9104` — exact. The PnL/sign/weight arithmetic reproduces with no off-by-one.

**Focus point #1 — the OOF atomic-write fix `a7abba4`.** I read the modified path at `src/crypto_trade/strategies/ml/optimization.py:409-454`. The fix is genuinely metric-neutral. The `combined` DataFrame — the object that feeds the OOF parquet consumed by per-cell PBO/DSR/PSR — is built at lines 424-439 identically to the pre-fix code: `new_df` from the same `oof_buffer` with the same 6-column schema; `combined = pd.concat([existing, new_df])` if the file exists, else `combined = new_df`. The ONLY change is the write mechanism: lines 441-447 write `combined` to a sibling temp file via `tempfile.mkstemp(...suffix=".parquet.tmp")` then `os.replace(tmp_name, oof_persist_path)` (atomic POSIX rename), replacing a prior in-place `to_parquet`. `os.replace` does not touch DataFrame contents — readers see either the complete old file or the complete new file. The failure-cleanup branch (`os.unlink` on exception then `raise`) only removes a partial temp file and never silently swallows. No OOF row reaching the PBO/DSR/PSR computation is altered by this fix; it is a pure file-integrity safety change. The "two failed launches before clean launch #3" detour is documented honestly in the engineering report and the run-integrity table confirms `grep -c "magic bytes|end of stream|Traceback" run.log = 0` on the clean run. Reproducibility-relevant: PASS — the fix could NOT alter any metric.

One non-blocking discrepancy noted: the engineering report's per-symbol OOS table reports BCH "% of Total OOS wpnl" = 110.2%, while `comparison.csv`'s per_symbol block reports BCH `concentration_pct` = 103.69. Both are computed against different denominators (the report uses total OOS weighted_pnl 53.62 → 55.60/53.62 = 103.7%; the 110.2% appears to use a different base, possibly net of the LDO detractor). This is a reporting-presentation inconsistency in a non-load-bearing summary table, not a code defect — the underlying per-symbol weighted_pnl values (BCH +55.60, LDO −7.00, TRX +5.02) are internally consistent and sum to 53.62. I flag it for the QR's awareness; it does not affect any check verdict because both numbers say the same thing qualitatively (BCH carries the entire OOS book, LDO is negative). It is recorded in Recommendations.

### Check 8 — Hypothesis-Implementation Alignment: PASS

Brief Section 1 hypothesis: add a 4-member funding-rate family — `funding_sign_persist_9`, `funding_momentum_3`, `funding_accel_3`, `funding_price_divergence_6` — to `V3_FEATURE_COLUMNS` (14→18). I traced the implementation against this claim:

- **Code change maps to brief.** `src/crypto_trade/features_v3/funding_v3.py:387-550` adds exactly the 4 named columns via `compute_funding_family` / `add_funding_family_v3_features`; `features_v3/__init__.py:87` registers `funding_family_v3` in `GROUP_REGISTRY`; `features_v3/__init__.py:196-199` appends exactly those 4 names to `V3_FEATURE_COLUMNS_TOP_N` after the 14 anchors, making 18. The runner pre-flight (`run_baseline_v3.py:347-371`) asserts count == 18 and all 4 names present.
- **Single-axis discipline verified.** The config-accretion pre-flight (`run_baseline_v3.py:853-900`, the Critic-/081-Rec-#3-mandated `_canonical_v059` check) asserts 11 behavior-affecting knobs (V3_MODELS, DEFAULT_ATR_MULTIPLIERS, V3_ATR_MULTIPLIERS_PER_SYMBOL, zscore_threshold, adx_threshold, adx_threshold_per_symbol, vol_scale_floor_per_symbol, block_long_for, block_short_for, enable_per_symbol_drawdown_brake, REQUIRED_GAP) all equal /059-canonical values, raising `ValueError` on any drift. The engineering report's "config-accretion (11 knobs == /059) PASS" confirms it fired clean. No labeling, symbol, risk-gate, or model-architecture change rode along — no scope creep.
- **No hypothesis-faking.** The funding family modules genuinely exist and are genuinely wired into the runner's `process_symbol_v3` path. This is the opposite of the "brief claims funding, diff shows no funding modules" failure mode.
- **The closed single-z-score axis stays closed.** The runner retains the literal bans on `funding_rate_zscore_30` and `btc_funding_rate_zscore_30` (`run_baseline_v3.py:289-290` docstring + assertion). The 4 family members use entirely distinct names. The banned columns are not re-introduced.

**Focus point #3 — the /019 differentiation.** The cycle-3 plan §3 mandated the brief argue why /082's funding FAMILY is a genuinely distinct axis from the closed /019/023/024 `funding_rate_zscore_30`. I adjudicate the brief Section 3.4 differentiation as **substantive, not a re-tread**: (1) the structural argument is real — a mean-zero rolling z-score algebraically discards sign and level; `funding_sign_persist_9` and `funding_price_divergence_6` carry signed crowding-direction and a price leg that the z-score cannot represent (this is not cosmetic — the EDA T4 winner/loser separation is *on* the sign-persistence and divergence features, which a z-score could not produce); (2) the axis is a 4-member family vs a single 14th-column feature, giving the model multiple split entry points; (3) the family is literature-channel-selected (Section 10 cites BIS WP 1087, MDPI 14(2):346, CFB/Phemex crowding-reversal) not univariate-rank-selected. The BASELINE_V3.md Dead Ideas entry explicitly marks the /019/023/024 funding axis "Eligible for re-evaluation" post-walk-forward-fix. The differentiation clears the cycle-3-plan bar. PASS.

## Optional Checks 9–12

**Check 9 — Symbol Exclusion Enforcement: PASS.** `run_baseline_v3.py:202-206` computes `overlap = set(symbols) & set(V3_EXCLUDED_SYMBOLS)` and raises on non-empty overlap; `V3_EXCLUDED_SYMBOLS` is imported (line 47) and carries the 11-symbol v1/v2/MKR exclusion list. The engineering report's "Pre-flight: track isolation PASS" and the config-accretion `V3_MODELS symbols == (BCHUSDT, LDOUSDT, TRXUSDT)` assertion together confirm the universe audit ran. PASS.

**Check 10 — Feature Isolation Enforcement: PASS.** `grep` for `from crypto_trade.features ` and `from crypto_trade.features_v2` across `src/crypto_trade/features_v3/` returns only docstring/comment mentions (the isolation-rule documentation itself in `__init__.py:7-8`, `funding_v3.py:3`, `fracdiff_v3.py:23`) — zero actual cross-track import statements. The new `funding_v3.py` imports only `numpy`, `pandas`, `pathlib` (lines 49-54). Track isolation holds. PASS.

**Check 11 — Forming-Candle Audit: PASS (within Critic read-only scope).** The engineering report Run Integrity table reports "Zero-trade months IS 0 / OOS 0" and "NaN Sharpe none", and the data-freshness pre-flight ("Pre-flight: branch + data freshness PASS"). The Phase 5.5 gate notes the QE was directed to re-run `fetch-funding` before parquet regeneration. The `fetcher.py` `close_time < now_ms` forming-candle filter is the standing v3 invariant (BASELINE_V3.md Measurement Discipline). I cannot tail the raw kline CSVs to a closed-candle guarantee without exceeding read-only scope on transient data, but the run-integrity evidence (no NaN Sharpe, no zero-trade months, 14 OOS months populated) is consistent with non-forming data. No contrary evidence. PASS.

**Check 12 — Library Version Pinning: PASS.** Brief Section 9 declares no new dependencies; the funding family is built with `numpy` + `pandas` only (confirmed at `funding_v3.py:53-54` imports). The pinned stack (lightgbm 4.6.0, optuna 4.8.0, numpy 2.2.6, pandas 3.0.0, scikit-learn 1.8.0, scipy 1.17.0, statsmodels 0.14.6, pyarrow 23.0.1) matches the /059/081 carry-forward in BASELINE_V3.md verbatim. `adfuller` is from the already-pinned `statsmodels`. No version drift. PASS.

## On the Result Classification (focus points #4 and #5 — input to QR's Section 8 call)

The methodology is sound and OVERALL is MERGE. However, the Critic is asked to state explicitly whether the *result* reads SUSPICIOUS / INERT / NEGATIVE / PROMISING — with the final Section 8 call belonging to the QR.

The result has the **INERT / SUSPICIOUS-OOS-DOMINANT signature simultaneously**, and the brief's own Section 8 disjunctive precedence (SUSPICIOUS → NEGATIVE → PROMISING → INERT) is what resolves it:

- **Feature-level INERT criterion is met (focus point #4).** The 4 funding features rank 15/16/17/18 of 18 by `last_month_importance_share_portfolio` (`conditional_orthogonality.csv` / engineering report), combined share 9.90%, each averaging 2.475% vs the 5.56% uniform-parity baseline — below-parity. The brief Section 4.2 feature-level falsifier ("all 4 family members rank ≥ 14/18 across ≥ 2 of 3 symbols → INERT") and Section 8.4 are triggered on the portfolio-pooled rank. The model barely uses the family. This is the /019/023/024 outcome reproduced under a family construction — and per `feedback_v3_inert_features_at_higher_budget.md`, INERT features at n_trials=35 actively perturb the Optuna search.
- **The OOS-DOMINANT criterion is also met (focus point #4 tension).** IS Δ = −0.0118 (< 0) and OOS Δ = +1.2081 (≥ +0.20) vs the /059 anchor — this is exactly the brief Section 8.3 SUSPICIOUS-OOS-DOMINANT sub-mode trigger (IS Δ < 0 AND OOS Δ ≥ +0.20), the /078 signature. The OOS monthly Sharpe jumped to +1.7872 while IS went nowhere. The brief itself pre-registered (Section 7) this as a ≈25% failure path.
- **The OOS jump is NOT attributable to the funding axis.** The features are bottom-4 by importance — the model is not splitting on them in any load-bearing way. A +1.21 OOS Sharpe move from a 9.90%-combined-importance feature family that left IS flat is not a funding-signal discovery; it is a 3-seed-lottery / Optuna-search-perturbation artifact, precisely the mechanism `feedback_v3_inert_features_at_higher_budget.md` documents (the larger 18-column search space lets Optuna land on a different IS-overfit hyperparameter region whose OOS realization happens to be favorable). The OOS/IS monthly Sharpe ratio is 1.6585 — below the brief's 3.0 ratio gate — so the ratio gate alone does not fire, but the IS-Δ<0 / OOS-Δ≥+0.20 sub-mode fires independently and is sufficient.
- **Concentration corroborates the fragility (focus point #5).** BCH carries 103.7%–110.2% of OOS weighted PnL (BCH +55.60, TRX +5.02, LDO −7.00 on a 53.62 total). The aspirational ≤30% top-symbol gate is missed by ~74 percentage points. The entire OOS "lift" is a single-symbol bet — the OOS Sharpe's robustness is therefore weak; one symbol's OOS-uptrend realization is doing all the work, which is the textbook regime-luck-via-concentration pattern, not a portfolio edge.

**My read for the QR: this iteration should be classified SUSPICIOUS-OOS-DOMINANT.** Section 8's disjunctive precedence places SUSPICIOUS before INERT, and both the OOS-DOMINANT sub-mode (IS Δ < 0, OOS Δ ≥ +0.20) and the feature-level INERT signature are present — under the brief's own LOCKED taxonomy, SUSPICIOUS wins precedence. Either way the axis does NOT advance to the iter-v3/092 CONFIRMATION bundle: SUSPICIOUS produces no edge ingredient, and INERT does not advance. If the QR instead reads the IS-Δ as inside the ±0.10 noise band (it is: −0.0118) and declines the OOS-DOMINANT sub-mode on a technicality, the fallback is INERT — also non-advancing. There is no reading of these artifacts under which /082 is PROMISING: the brief's PROMISING criterion (Section 8.1) requires at least one family member rank ≤ 9/18 with absolute importance ≥ 30, and the best funding feature ranks 15/18 — PROMISING is mechanically foreclosed.

To be unambiguous about the OVERALL verdict: a SUSPICIOUS-OOS-DOMINANT or INERT *result* with *sound methodology* is OVERALL=MERGE — the iteration is validly recorded as that classification, the Critic gates methodology not outcome. All 8 mandatory checks pass on methodology (Check 3's lone FAIL is the known EXPLORATION-mode DSR artifact, informational and non-BLOCK-eligible; the BLOCK-eligible PBO axis passes). No methodology DEFECT exists. OVERALL = MERGE.

## Recommendations to QR

For a PROMISING iteration this block lists items the CONFIRMATION should pre-register; /082 is not PROMISING, so these are process-level items for the QR's Section 8 write-up and subsequent cycle-3 EXPLORATIONs:

1. **Classify /082 SUSPICIOUS-OOS-DOMINANT (or INERT) per the brief's own LOCKED Section 8 disjunctive precedence — do not let the +1.21 OOS Sharpe headline rationalize an advance.** The funding family ranks bottom-4/18 at 9.90% combined importance; the +1.21 OOS move with IS flat is the documented INERT-feature Optuna-perturbation artifact (`feedback_v3_inert_features_at_higher_budget.md`), not funding signal. Combined with /019/023/024 this is now a 4-data-point structural verdict on the v3 funding axis at EXPLORATION budget — the brief Section 8.4 explicitly acknowledged this stake. The diary should record the funding *family* construction as a genuine, well-motivated, but unsuccessful attempt to break the /019 INERT pattern, and the funding axis as closed for the remainder of cycle 3 absent a fundamentally different construction (e.g. a multi-symbol-pooled model that could give the funding family more training data per fit, or open-interest data which has no current feed).

2. **Reconcile the per-symbol concentration-percentage reporting before the diary.** The engineering report's per-symbol OOS table (BCH 110.2%) and `comparison.csv` per_symbol block (BCH `concentration_pct` 103.69) disagree on the BCH OOS-PnL share figure because they use different denominators. Pick one definition (the `comparison.csv` total-OOS-weighted-PnL base is the cleaner one), state it in the diary, and have the Engineer align the report table in future iterations — divergent summary numbers in the artifact pair invite post-hoc cherry-picking of whichever number reads better.

3. **For the remaining cycle-3 EXPLORATIONs (iter-v3/083–091), weight Direction 2 (universe expansion) up the priority list.** /082's OOS result is 103.7%-concentrated in BCH on a 3-symbol universe — the OOS Sharpe is structurally a single-symbol bet and no feature-family axis can fix that denominator problem. The cycle-3 plan's own Direction 2 (universe expansion = denominator expansion, distinct from the closed swap-by-replacement family) and Direction 3 (multi-symbol-pooled model) directly attack the BCH-concentration fragility that makes every v3 OOS number fragile. A funding feature added to a still-3-symbol universe was always going to produce a concentration-dominated result regardless of whether the feature carried signal.
