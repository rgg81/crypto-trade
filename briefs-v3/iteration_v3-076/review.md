# Phase 7.5 Critic Review — iter-v3/076

OVERALL: MERGE

## Verdict Summary

iter-v3/076 ran correctly. The SUSPICIOUS-OOS-DOMINANT classification is certified clean: it does NOT advance to the cycle-2 CONFIRMATION bundle, and it does NOT update BASELINE_V3.md (an EXPLORATION cannot, and SUSPICIOUS never advances). The two critical adjudications resolve as follows:

- **Adjudication #1 (banned-feature re-evaluation): LEGITIMATE rule-sanctioned re-eval, NOT a process violation.** Reasoning below.
- **Adjudication #2 (falsified hypothesis + holding-time forensic): the falsification is genuine — no wiring bug; the QE's roster-composition forensic is independently verified; the QR's 3%-SUSPICIOUS calibration and the Section 4.3 holding-time predictor were both brief defects, documented as process-level recommendations.**

`OVERALL: MERGE` here means the iteration is certified, the verdict is recorded, and /076 closes as a non-advancing EXPLORATION data point. It does not mean a baseline change.

## Iteration Type (from Brief Section 0.5)

TYPE: EXPLORATION (cycle 2 #6 of 10). Check 3 (DSR/PSR/PBO) edge-significance axes are informational for EXPLORATION; only methodology integrity (look-ahead, embargo, IC, ADF, hypothesis-implementation alignment, anti-pattern scan) is BLOCK-triggering. No MERGE-gate evaluation applies.

## Adjudication #1 — The /043 Banned-Feature Re-Evaluation

`range_efficiency_50` is **bit-identical math** to `efficiency_ratio_50`. I verified this directly, not via the QE's assertion: `compute_range_efficiency_50` (engineered_v3.py:758-769) computes `path = (close - close.shift(50)).abs()`, `noise = close.diff().abs().rolling(50, min_periods=50).sum()`, `er = (path/(noise + 1e-9)).clip(0,1)`, `er.shift(1).fillna(0.0)`. `compute_efficiency_ratio_50` (engineered_v3.py:428-433) does the same with the same literal `1e-9` epsilon (`_RANGE_EFFICIENCY_50_EPS = 1e-9` at line 681). Same formula, same window, same epsilon, same clip, same shift, same fillna — these are the same feature.

The re-introduction is a **legitimate, rule-sanctioned re-evaluation**, not a process violation, on four independently-confirmed grounds:

1. **Rule eligibility is genuine.** `feedback_v3_walkforward_lookahead_bug.md` explicitly lists iter-v3/029–/057 (which includes /043) as "eligible for re-evaluation as part of the post-fix axis-rethink rule." /043's verdict was produced under the buggy walk-forward (`train_end_ms = test_start_ms`, no embargo). The /076 worktree carries the fixed walk-forward — I verified `walk_forward.py:113` is `train_end_ms = test_start_ms - embargo_ms`. /043's verdict legitimately does not transfer. The Critic adds the QR's own observation against itself: the lookahead bug biased Sharpe UPWARD, so /043's true result was *worse* than the recorded −0.8445/−0.8990 — which makes re-evaluation *more* warranted, not less, since the pre-fix verdict was an optimistic measurement.

2. **The literal-name ban is intact and meaningful.** I verified `run_baseline_v3.py:344-353` still raises `RuntimeError` on `"efficiency_ratio_50" in V3_FEATURE_COLUMNS`; `test_v3_feature_count.py` keeps `efficiency_ratio_50` in `_PROHIBITED_FEATURES`; and `compute_efficiency_ratio_50` remains REMOVED from the `add_engineered_v3_features` dispatch (engineered_v3.py:927) so the `efficiency_ratio_50` *column* is never generated. The new feature carries a distinct name, so the ban is neither satisfied nor circumvented — it is orthogonal. This is honest naming, not name-laundering: the function docstring (engineered_v3.py:719-734), the `V3_FEATURE_COLUMNS_TOP_N` comment (`__init__.py:177-189`), brief Section 10.2, the EDA module docstring, and the runner ban-comment all disclose the math identity explicitly.

3. **The "different role" argument is substantive, not a fig leaf — but it is qualified.** /043 added the unsigned ER to a 4-symbol universe that included ALGO (the direction-asymmetric bottleneck symbol); /076 runs the 3-symbol BCH/LDO/TRX universe with ALGO excluded. That is a real universe difference. The role difference (15th conditioning feature alongside 14 directional features vs /043's standalone feature) is *partially* substantive: the brief is correct that /063's `trend_efficiency_signed` is the precedent for a same-family-different-name feature. The honest qualifier — which the Critic states for the record — is that /043 *also* added the feature to the LightGBM feature set (it was never a literal standalone strategy). The "role" distinction is therefore the universe + the 14-vs-1-of-15 feature-stack composition, not a categorical signal/feature dichotomy. This weakens the *strength* of the re-evaluation thesis but does not make the re-evaluation itself improper — the rule grants eligibility on the walk-forward-fix grounds alone, and points (2) and (4) keep the process honest.

4. **The result re-confirmed the ban.** The re-evaluation did NOT vindicate the feature. IS Sharpe collapsed −0.7894 to +0.0431; IS MaxDD blew out to 61.69%; BCH IS edge destroyed (+79.45% → −8.27%). The feature produced the SUSPICIOUS-OOS-DOMINANT pattern — a *different* failure mode from /043's bilateral −0.84/−0.90 damage, but a failure. The closing position is correct: the Kaufman path-efficiency axis is definitively closed, second data point, in a more favorable role, IS still broken. The re-evaluation discharged the `feedback_v3_walkforward_lookahead_bug.md` eligibility for this axis cleanly.

A process violation would be: (a) silently re-adding the literal `efficiency_ratio_50` name; (b) re-adding the math with no disclosure; (c) re-adding it without rule eligibility; (d) re-adding it and post-hoc rationalizing a bad result as PROMISING. None occurred. The QR disclosed the math identity in five places, the rule grants eligibility, the ban stayed intact, and the bad result was honestly classified SUSPICIOUS. This is the textbook shape of a legitimate (if unproductive) re-evaluation. NOT a BLOCK.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

This is the load-bearing check for a new feature; I traced the full computation chain in `compute_range_efficiency_50` (engineered_v3.py:758-769). `path = (close - close.shift(50)).abs()` — bar t uses close[t−50], past-only. `noise = close.diff().abs().rolling(50, min_periods=50).sum()` — `close.diff()` uses close[t]−close[t−1], `.rolling(50).sum()` sums bars [t−49..t], all past-or-current. `er = (path/(noise+1e-9)).clip(0,1)` is the raw ER at bar t. Then `er.shift(1)` — the decisive operation — moves the value so bar t's feature reads the ER computed *at bar t−1*, i.e. using close[t−51..t−1]. The signal bar's own close[t] is never observed. `.fillna(0.0)` neutralizes the 51-bar warm-up. The docstring's past-only claim ("bar t sees only close[t−51..t−1]") is accurate. The new test file `tests/features_v3/test_range_efficiency_50.py` re-asserts the property adversarially (appending future bars does not change the value at t). The feature does not change the label horizon, the symbol count, or the embargo. No look-ahead.

### Check 2 — Embargo Width: PASS

Label horizon is the 21-candle (10080-min / 8h, `candle_minutes=480`) triple-barrier timeout, unchanged. Required gap = `(timeout_candles + 1) × n_symbols = (21+1) × 3 = 66`. Engineering report Label Leakage Audit confirms `REQUIRED_GAP=66`, embargo=22 candles, both unchanged from the /059/060 baseline. Walk-forward fix verified intact at `walk_forward.py:113` (`train_end_ms = test_start_ms - embargo_ms`). The feature axis touches neither the label horizon nor the symbol count. Actual gap = required gap. PASS.

### Check 3 — Multiple-Testing Correction: PASS-equivalent (informational for EXPLORATION)

`dsr.json`: DSR=0.0, PSR=1.0, PBO=0.0974, `dsr_relative_b4`=0.999995, n_trials=315, n_eff=18, `min_trl_months`=18.78. PBO=0.0974 is well under the 0.40 threshold (and improved from /060's 0.1278) — the only Check-3 axis that is BLOCK-triggering even for EXPLORATION, and it PASSES. DSR=0.0 is the structural-at-v3-trade-volume artifact (same root cause as every prior v3 run); `dsr_relative_b4`≈1.0 is the Path-B4 reformulation. Per `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode (n_trials=315) DSR/PSR are regime-specific artifacts, not comparable to CONFIRMATION-mode and not edge-significance evidence — informational only. `frac_positive_paths`=0.6444 (CPCV invariant, identical to /060). No Check-3 failure.

### Check 4 — IC Correlation: PASS

`ic_matrix.csv` is a 15×15 square matrix; `range_efficiency_50` is present in both header and row index. I read the `range_efficiency_50` row directly: its maximum |IC| against the 14 baseline features is **0.2157** (with `sym_vs_btc_ret_7d`), with `vwap_dev_20` second at 0.1524 and `ema_spread_atr_20` third at 0.1761. This is a Category 1 off-the-shelf indicator (external Kaufman formula, not a Category 2 composed feature built from v3 primitives) — the strict |IC|<0.70 gate applies, NOT the engineered-feature carve-out. 0.2157 ≪ 0.70: comfortable PASS. The feature is genuinely orthogonal to the existing 14-feature stack — it does not steal `colsample_bytree` picks from a near-duplicate. The engineering report's stated 0.216 matches my read; the brief's EDA T7 estimate of 0.206 was slightly low but in the right neighborhood and well below the gate either way.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` is present (2355 rows). I verified `range_efficiency_50` directly across all 3 symbols. For every training-window month from the post-warm-up period through 2025-03 (the last training month), `stationary=True` with p≈0.0: BCHUSDT 2020-04→2025-03 all True, LDOUSDT 2022-12→2025-03 all True, TRXUSDT 2024-01→2025-03 all True (and earlier). The only non-stationary rows are the 51-bar warm-up months (BCH 2020-01/02/03 with blank/p=0.335/p=0.222; LDO 2022-09/10/11 with blank/blank/p=0.325) — those are the `.fillna(0.0)` warm-up period, which is a constant or near-constant series and is untestable, not a stationarity failure. ADF tests stationarity at the end of the training window — that criterion is met for all 3 symbols. The `TRXUSDT/range_efficiency_50/2020-02 "x is constant"` error and the `LDOUSDT/cusum_reset_count_200 not found` warning are both benign: the first is the warm-up constant-fill (training splits skip the warm-up), the second is the persistent LDO-short-history artifact verified identical at /074 and /075. PASS.

### Check 6 — Pareto Dominance: PASS-by-non-applicability

EXPLORATION mode runs 3 seeds (outer=42 lineage subset `[191664963, 1662057957, 1405681631]`, verified literal in `ensemble_summary.json`). The 10-seed multi-seed Pareto pre-MERGE validation is a CONFIRMATION artifact; `pareto_front.csv` is not produced under the unified architecture (BASELINE_V3.md). No seed-selection decision is being certified for production here — SUSPICIOUS axes do not advance. No Pareto dominance violation possible at EXPLORATION scope.

### Check 7 — Reproducibility: PASS

Commit chain stamped and consistent: EDA `40b6e66`, brief `ed7b27b`, backfill `45f4832`, setup `79a62b0`, test-fix `9285505`, Phase 5.5 gate `2ef683f`. Runner uses the explicit `feature_columns` list — `V3_FEATURE_COLUMNS_TOP_N` is a 15-element literal in `features_v3/__init__.py` (14 BASELINE_V3 anchor features in /059-spec order + `range_efficiency_50` at index 14), not `None` and not auto-discovered. Ensemble seeds are literal and lineage-annotated. The `_verify_feature_columns` pre-flight asserts count==15, `range_efficiency_50` present, `efficiency_ratio_50` absent, `enable_regime_size_scalar is False`, `regime_size_scalar_symbols == ()`. PnL spot-check on OOS trades passes: row 2 `BCHUSDT,-1,303.87→282.100779,w=0.33` recomputes to pnl 7.1640% / net 7.0640 / weighted 2.3311 ✓; row 23 `BCHUSDT,1,482.58→516.658069,w=0.98` recomputes to 7.0616 / 6.9616 / 6.8224 ✓; row 10 `TRXUSDT,-1,w=1.0` recomputes to 4.1288 ✓; zero-weight rows (e.g. OOS row 6) correctly carry `weighted_pnl=0.0000`. Direction signs, fee deduction, and weight multiplication are all internally consistent. PASS.

### Check 8 — Hypothesis-Implementation Alignment: PASS (hypothesis FALSIFIED, but cleanly)

The implementation matches the brief exactly: brief Section 3 declares exactly TWO changes — (1) the NEW feature `range_efficiency_50` as the 15th `V3_FEATURE_COLUMNS` entry, (2) the mandatory Primitive-12 revert. The code shows precisely those: `compute_range_efficiency_50` added + dispatched + in `__all__`; `range_efficiency_50` is the 15th `V3_FEATURE_COLUMNS_TOP_N` entry; `enable_regime_size_scalar=False`, `regime_size_scalar_symbols=()` (run_baseline_v3.py:1655-1656, the /075 revert); `ITERATION_LABEL="v3-076"`. No scope creep — no labeling change, no model change, no risk-cascade change, no walk-forward change. No hypothesis-faking — the registered axis (a sign-invariant feature-internal IS-regime discriminator) is exactly what was tested.

The **hypothesis is falsified**, and I confirm the falsification is genuine, not a wiring artifact. The brief's Section 1 thesis was that `range_efficiency_50`, being regime-orthogonal (EDA T3 |regime-sign corr| 0.010), is "structurally unable to produce the /071/073 SUSPICIOUS signature," and Section 7 weighted SUSPICIOUS at ≈3%. The observed result is SUSPICIOUS-OOS-DOMINANT with OOS/IS ratio 15.04 — the clearest instance of the pattern in cycle 2. This is a real model-behavior outcome: the feature was learned (importance ranks 15/12/10 for BCH/LDO/TRX — not rank-15/15-for-all, so INERT does not fire; 100 IS adds / 90 IS removes — not bit-identical, so NULL-RESULT does not fire), it was past-only and stationary, and the 315 trials ran cleanly. The IS collapse is a genuine consequence of adding a 15th feature that disrupts the BCH bull-period split structure carrying the IS edge — not a bug.

The QR's 3%-SUSPICIOUS calibration was a real miss, and the brief's Section 4.3 holding-time predictor reasoning was a real defect. I independently verified the QE's roster-composition forensic and concur:
- The OOS roster is 102 trades in both /060 and /076; the trade-identity diff on `(symbol, open_time)` shows substantial churn (the QE's 40 added / 40 removed / 62 common). Sampled common trades — e.g. `BCHUSDT 1743667199999→1743983999999`, `TRXUSDT 1747238399999→1747612799999`, `BCHUSDT 1746719999999→1747295999999` — have **identical close_times** in both rosters, hence identical durations. This is mechanically forced: a common `(symbol, open_time)` trade enters on the same signal bar, and with the ATR multipliers `(2.0,1.0)` and 21-candle timeout unchanged, the same entry produces the same exit. The common-subset 0.000 duration delta is sound by construction.
- Sampled added /076 OOS trades skew toward longer holds, including 21-candle timeout-length trades (e.g. `BCHUSDT 1744646399999→1745251199999` = 21.0 candles). The +0.480-candle full-roster OOS duration shift is therefore roster-composition-driven (the model selected longer-duration OOS trades and dropped shorter ones), NOT per-trade barrier extension. The holding-time falsifier (>+1.0 candle full-roster shift) correctly did NOT fire (IS Δ +0.005, OOS Δ +0.480) — but the QE is right that the +0.480 is the forensic fingerprint of regime-loading via trade selection.

The QR's Section 4.3 reasoning ("a feature has no duration-extension mechanism, so ~0 duration change") was directionally wrong: it considered only per-trade barrier mechanics and ignored that a feature *can* shift WHICH trades the model selects, including a preference for longer-duration trades. And T3's |regime-sign corr| measured only the *marginal* feature distribution's correlation with the calendar label — it does not bound the *conditional* model use (how LightGBM integrates `range_efficiency_50` in interaction with the 14 directional features). The "structurally unable" claim conflated marginal feature orthogonality with conditional model orthogonality. These are brief defects in *prediction* — but predictions are estimates, the *gates* (the disjunctive SUSPICIOUS classifier with the pre-registered OOS/IS>3.0 ratio gate) fired correctly and caught it, and the QE documented the mechanism. Check 8 tests whether the code matched the brief and whether the result is genuine — both hold. PASS.

## §11 Anti-Pattern Static Scan

Clean. Spot-checked the catalog entries relevant to a feature axis: no `feature_columns=None` / auto-discovery (explicit 15-element literal); no cross-track import (`compute_range_efficiency_50` lives in `features_v3`, uses only numpy/pandas, no import from `crypto_trade.features` or `features_v2`); no silent label-horizon change; no `start_time` trimming (brief Section 0 confirms unchanged); ensemble seeds literal; the `efficiency_ratio_50` literal-name ban and the dispatch-removal both intact (the banned column is never generated). No same-family stacking — exactly ONE new feature, tested alone (`feedback_v3_engineered_features_dont_stack.md` satisfied). The adjacent `test_pbo_in_range` NamedTuple fix (commit `9285505`) is an unrelated pre-existing test-drift repair, correctly committed separately and excluded from the /076 verdict. No unexplained matches.

## Foundation Audit (Boot Steps 9-11)

- **Walk-forward lookahead fix:** intact — `walk_forward.py:113` is `train_end_ms = test_start_ms - embargo_ms`. The `feedback_v3_walkforward_lookahead_bug.md` upward bias applies uniformly to /076 and /060; cross-iteration deltas remain valid, absolute magnitudes biased — which the QR/QE both acknowledge and which does not change the SUSPICIOUS classification (a ratio-based gate).
- **Runner config:** `ITERATION_LABEL="v3-076"`; `enable_regime_size_scalar=False` + `regime_size_scalar_symbols=()` (the /075 Primitive-12 revert, run_baseline_v3.py:1655-1656); `V3_FEATURE_COLUMNS_TOP_N` is 15 features including `range_efficiency_50` as the 15th; `OOS_CUTOFF_DATE` and `TRAINING_MONTHS` untouched (sacred constants).
- **efficiency_ratio_50 ban:** intact as a `raise RuntimeError` pre-flight (run_baseline_v3.py:344-353) and as a prohibited test entry; the `range_efficiency_50` re-evaluation is separately named and does not satisfy or violate the ban.
- **Symbol exclusion:** BCH/LDO/TRX, none in `V3_EXCLUDED_SYMBOLS`; the disjointness assertion is present in the runner.

All Foundation Audit items PASS.

## QR Response Considered

This review was dispatched single-round with the orchestrator's two critical adjudications pre-specified; no separate `qr_response.md` round was required because both adjudications were resolvable directly from the artifacts (the math-identity verification, the EDA OOS-tuning scan, the ADF table, the trades.csv roster diff, and the runner config). The adjudications are resolved in the dedicated section above.

## Recommendations to QR

These are process-level recommendations for FUTURE cycle-2 EXPLORATIONs (/077+). This iteration's verdict is final; SUSPICIOUS does not advance.

1. **Replace marginal-orthogonality tests with a conditional-orthogonality proxy.** The /076 T3 |regime-sign corr|=0.010 measured only whether the feature's *marginal* value distribution correlates with the IS/OOS calendar label. It does NOT bound how the model *conditionally* uses the feature in interaction with the other 14 features — and the conditional use produced regime-correlated trade selection (OOS/IS 15.04). Future "regime-orthogonal feature" briefs must address conditional orthogonality. A concrete proxy: on the /060 roster, measure the correlation of the candidate feature's *model-split allocation or SHAP attribution* (not the raw feature value) with the regime label. A near-zero marginal corr with a non-zero conditional corr is the /076 failure signature; only the conditional measure is the binding test.

2. **The holding-time predictor must include the roster-composition channel.** Brief Section 4.3 predicted "~0 duration change" on the grounds that a feature has no barrier-extension mechanism — and was directionally wrong by +0.480 candles OOS because it ignored that a feature changes WHICH trades are selected, and the model can prefer longer-duration trades. Future feature-axis briefs should pre-register, in addition to the >+1.0-candle full-roster falsifier, an explicit roster-composition sub-channel: if the added-vs-removed trade-set mean-duration difference exceeds a stated bound, the feature is loading the regime factor via selection. /076's added OOS trades (mean 7.25) vs removed (mean 6.03) would have fired such a sub-channel.

3. **Calibrate SUSPICIOUS probability against the cycle-2 base rate, not the mechanism story.** Cycle 2 is now /071 SUSPICIOUS, /072 NEGATIVE, /073 SUSPICIOUS, /074 INERT, /075 INERT, /076 SUSPICIOUS — three SUSPICIOUS-OOS-DOMINANT in six EXPLORATIONs against a sharply IS-dominant /059 baseline (IS +1.09 / OOS +0.58, BCH IS concentration 95.76%). A 3% SUSPICIOUS weight while the empirical cycle-2 base rate is 50% is a mechanism-story override of the data. Future briefs' Section 7 SUSPICIOUS probability should be floored near the running cycle base rate unless the brief presents a *conditional*-orthogonality proof (Rec #1) strong enough to justify deviating below it. The Kaufman path-efficiency axis is now closed across two data points (/043 + /076) — do not re-propose it.
