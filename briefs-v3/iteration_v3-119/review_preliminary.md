# Phase 7.5 Critic Review — iter-v3/119 — PRELIMINARY

(NO OVERALL line in Round 1.)

**Preliminary direction**: leaning **EXPLORATION-PROMISING but with substantive concerns** — the headline numbers clear PROMISING gates and broad-based per-symbol attribution clears the /118 SSC-RISK failure mode, but the importance-rank-15/15 + OOS-Δ-2.8×-modal-upper-bound + TRX-OOS-concentration-80% triad bears uncomfortable resemblance to the /116 PROMISING-MECHANICAL signature. The Engineer's reading (Category-2 carve-out: Sharpe-Δ over importance) is defensible but does not address whether the lift is FEATURE-SIGNAL or FEATURE-MECHANICAL (loss-surface reorganization enabling other features to fit). The verdict hinges on QR's response to the clarifications below.

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION (cycle-6 slot #10 of 10, FINAL EXPLORATION)

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS

`compute_ret5d_signed_tbi` at `src/crypto_trade/features_v3/engineered_v3.py:834-888` constructs `ret_5d = log_close - log_close.shift(15)` (past-only by `.shift(15)`) and uses `taker_buy_imbalance_20` which itself is past-only by `add_taker_buy_imbalance_20` at `microstructure_v3.py:28-72` (`tbr.shift(1).rolling(20).mean() - 0.5` — both lag and rolling are trailing). The GROUP_REGISTRY ordering in `features_v3/__init__.py:71-127` places `microstructure_v3` BEFORE `engineered_v3` (lines 78-89), so `taker_buy_imbalance_20` exists when `compute_ret5d_signed_tbi` runs. Zero-imbalance edge case is correctly NaN'd (line 886). Unit test in `test_engineered_v3.py` adversarially appends future bars and confirms past-only invariant; engineering report Section 1 reports 252 passed, 3 skipped including C6-specific past-only + sign-convention 2/2 PASS. The /015 `tbr_zscore_30` dead-path used the SAME `tbr` primitive family but as a standalone z-score MAGNITUDE feature; C6 uses only `sign(taker_buy_imbalance_20)` (the discretized regime label), structurally different from the magnitude-input failure mode.

### Check 2 — Embargo Width: PASS

`REQUIRED_GAP = 66 = (timeout_candles + 1) × n_symbols = (21+1) × 3` per `validation_v3.py`, with `timeout_minutes=10080` and 8h candles (`candle_minutes=480`) giving `timeout_candles=21`. The runner enforces this at `run_baseline_v3.py:1304-1313` and the engineering report confirms `gap = 66` matches. Unchanged from /118. The walk-forward lookahead bug is FIXED post-/058 (`walk_forward.py:113` applies `train_end_ms = test_start_ms - embargo_ms` — `feedback_v3_walkforward_lookahead_bug.md`).

### Check 3 — Multiple-Testing Correction: INFORMATIONAL (EXPLORATION mode)

`dsr.json`: DSR=0.0 (EXPLORATION-budget structural artifact per `feedback_v3_dsr_mode_artifact.md`); PBO=0.0957 PASS (< 0.40); PSR=1.0000 PASS (> 0.95); frac_positive_paths=0.6444 PASS (> 0.55); DSR_relative=0.9997; n_trials=315 (3 seeds × 35 trials × 3 syms — matches EXPLORATION budget); n_eff=19. Per `feedback_v3_dsr_mode_artifact.md` + the cycle-1 axis-PASS criteria, only PBO is BLOCK-triggering at EXPLORATION; DSR/PSR at single-seed EXPLORATION budget are not comparable to CONFIRMATION-mode (n_trials=1500). PBO at 0.0957 is identical to /060/059 — a CPCV invariant of the universe + label, not a per-iteration discriminator. **All three EXPLORATION-relevant axes PASS.** No Check 3 BLOCK.

### Check 4 — IC Correlation: PASS-CARVEOUT (per Category-2 composed-feature pivot)

`ic_matrix.csv` shows C6 (`ret5d_signed_tbi`) max |IC| = 0.7229 with `regime_momentum_signed_5d` — exceeds the strict 0.70 threshold by 0.02. Per `feedback_v3_engineered_feature_pivot.md`, Category-2 composed features that share a value primitive (`ret_5d` in this case) MECHANICALLY correlate with their sibling; the carve-out replaces the IC gate with importance ≥30% threshold. Engineering report Section 7 documents the carve-out application and the lineage discipline correctly. Secondary high-|IC| pairs: vwap_dev_20 (-0.567) and ema_spread_atr_20 (-0.431) — both expected by transitivity through regime_momentum (which has IC 0.764 with vwap_dev_20 — a pre-existing high pair documented at /058 and unchanged). C6 introduces NO new high-IC pair beyond what is algebraically inherited from sharing `ret_5d`. The brief's R²=0.51 pre-disclosure at EDA T2 (Section 2.2) is consistent with production IC measurement.

### Check 5 — ADF Stationarity: PASS

`adf_test.csv` confirms C6 stationary at IS-end month 2025-03 across all three symbols: BCH ADF=-7.753 p=0.0; LDO ADF=-8.128 p=0.0; TRX ADF=-10.295 p=0.0. Engineering report Section 8 traces full stationarity timeline (BCH from 2020-04, LDO from 2022-11 due to shorter history, TRX from 2020-07). Overall stationarity rate 85.4% (1949/2283 rows) is consistent with cycle-6 cohort; early-window NaN-ADF artifacts are clustered before training-window adequacy. Structurally guaranteed by the sign-bounded multiplication: `sign(tbi) ∈ {-1, +1}` × bounded log-return = stationary product.

### Check 6 — Pareto Dominance: N/A (single-seed EXPLORATION)

EXPLORATION ENSEMBLE_SIZE=3 (`EXPLORATION_ENSEMBLE_SIZE` at `run_baseline_v3.py:95`); no 10-seed pareto front file exists at `reports-v3/iteration_v3-119/`. Pareto evaluation is deferred to /120 CONFIRMATION (10-seed unified ensemble). This is per the established cycle-6 EXPLORATION protocol — not a Critic-actionable gap. The 3-seed lottery risk remains live and is encoded in clarification questions below.

### Check 7 — Reproducibility: PASS

Commit SHA `82baf43` exists (`git log iteration-v3/119 --oneline | head` confirms — head at d761580 includes engineering report on top of 82baf43). Runner enforces explicit `n != 15` guard at `run_baseline_v3.py:432-442` with the iter-v3/119 error message naming `ret5d_signed_tbi` directly. `feature_columns` is explicitly enumerated in `V3_FEATURE_COLUMNS_TOP_N` (`features_v3/__init__.py:207-221`, 15 named columns); the lgbm strategy raises if columns are None/auto-discovered per `feedback_explicit_feature_columns.md`. Spot-check on row 2 of `in_sample/trades.csv` (BCH long, entry 292.99, exit 279.019378, weight 0.46): pnl_pct = (279.019378-292.99)/292.99×100 = -4.768% matches reported -4.7683%; net after 0.1% fee = -4.868% matches reported -4.8683%; weighted_pnl = -4.8683 × 0.46 = -2.2394 ✓. OOS row 1 (BCH short, entry 303.87, exit 282.10): (303.87-282.10)/303.87 = 7.16% matches reported 7.1640% ✓. Reproducibility intact.

### Check 8 — Hypothesis-Implementation Alignment: PASS

Brief Section 3.5 specifies 6 substantive changes + 1 mandatory housekeeping revert. Engineering report Section 2 enumerates all 6 as DELIVERED with line/file references. Verified by direct read: (a) `compute_ret5d_signed_tbi` at `engineered_v3.py:834-888` matches brief spec docstring + math byte-equivalent (`ret_5d * np.sign(tbi).replace(0.0, np.nan)`); (b) wired in `add_engineered_v3_features` at line 1055 AFTER `compute_ema_signed_volregime` at line 1054 (correct dispatch order); (c) GROUP_REGISTRY reorder at `features_v3/__init__.py:78-89` places microstructure_v3 BEFORE engineered_v3 (mandatory dependency satisfaction); (d) `V3_FEATURE_COLUMNS_TOP_N` 15th element is `"ret5d_signed_tbi"` (line 221); (e) `ema_signed_volregime` ABSENT-ban added to runner at line 593-602 fires correctly per engineering report Section 1 row 3; (f) `n != 15` guard at line 432-442 updated with /119-specific error message. The hypothesis is "add ret5d_signed_tbi as 15th to V3_FEATURE_COLUMNS_TOP_N with /118 housekeeping revert" — implementation matches verbatim. No scope creep into RiskV2 or labeling.

## Clarifications Requested from QR

The following 5 clarifications would meaningfully change the verdict between PROMISING and concerning sub-classifications. The Critic recognizes that headline gates PASS, but the importance-vs-Sharpe-lift coexistence is structurally novel in the v3 catalog and merits adjudication BEFORE the verdict is rendered.

1. **Importance-15/15 vs Sharpe-+0.70 paradox — feature-SIGNAL or feature-MECHANICAL?** The /118 C3 (`ema_signed_volregime`) had portfolio rank **6/15 at 7.1% of top importance** with real model allocation → IS Sharpe Δ = **-0.4543** catastrophic. The /119 C6 (`ret5d_signed_tbi`) has portfolio rank **15/15 (LAST) at 27.8% of top importance** (153/551) with substantially LESS model allocation → IS Δ = +0.1167 and OOS Δ = +0.7017 PROMISING. The Sharpe-Δ outcomes inverted as feature-allocation decreased. This contradicts the standard "model uses feature → result" causality and resembles the /116 PROMISING-MECHANICAL signature where the mechanism was book-composition (slot-freeing) rather than direct signal. **Question for QR**: Has the QR specifically tested whether C6 is contributing direct signal or whether C6's presence is reorganizing LightGBM's loss surface such that other features (e.g., the 13.7% lift in `vwap_dev_20`'s portfolio importance from 487.67 to a presumed re-ordering) capture lift that ex-C6 they did not? Concrete diagnostic the QR could provide: compare the 14-feature anchor's model_importance_last_month_portfolio.csv (the /060 baseline) row-by-row against /119's importance ranks for the 14 anchor features — if anchor-feature importance ranks materially SHUFFLE (more than typical multi-seed variance), C6 is acting as a loss-surface reorganizer (feature-MECHANICAL). If anchor importances are nearly unchanged and only C6's 153 is "new", C6 is feature-SIGNAL at low-but-nonzero allocation.

2. **OOS Δ +0.7017 = 2.8× modal upper bound (+0.25); how is this NOT Mode 6 SUSPICIOUS-OOS-DOMINANT?** Section 7 Mode 6 fires when OOS Δ > +0.30 AND IS Δ < +0.05. Observed: OOS Δ = +0.7017 (5.6× over +0.30 threshold), IS Δ = +0.1167 (above the +0.05 cutoff by 0.0667). The Mode 6 falsifier MISSES the IS Δ leg by **0.067 Sharpe units** — a knife-edge margin given 3-seed variance. The OOS/IS daily-Sharpe ratio of 1.34 is HEALTHY; the OOS/IS monthly-Sharpe ratio is 0.99 (the engineering report's Section 3 metric) — these contradict each other for the same dataset. **Question for QR**: which Sharpe scale is canonical for SUSPICIOUS-OOS-DOMINANT? At the daily-Sharpe scale 1.74→2.32 = 1.34× (within healthy band); at the monthly-Sharpe scale 0.85→0.84 = 0.99×. The brief's Mode 6 threshold (+0.30) is in monthly units — but if OOS daily Sharpe 2.32 vs IS 1.74 represents a 33% lift (not 4× lift), Mode 6's "SUSPICIOUS-OOS-DOMINANT" framing may be artificial. Please reconcile.

3. **/116 PROMISING-MECHANICAL precedent — is /119 a feature-form analog?** /116 cleared all PROMISING criteria on Sharpe-Δ axes (broad-based per-symbol positive Δ, healthy OOS/IS ratio) but was classified PROMISING-MECHANICAL because the mechanism was book-composition (slot-freeing cascade) rather than new edge ingredient. Per `feedback_promising_mechanical_subtype.md`, PROMISING-MECHANICAL is **non-compoundable as a signal source** in CONFIRMATION bundles. /119 has the same broad-based-OOS-positive signature, similar magnitude (+0.70 OOS Δ for /119 vs +0.97 for /116), AND the additional importance-15/15 indicator that the model is barely using C6. **Question for QR**: should /119 be classified PROMISING-FEATURE-MECHANICAL (a NEW sister-subtype to /116's PROMISING-MECHANICAL, also non-compoundable)? Or is the broad-based per-symbol IS positive Δ (BCH +4.57 / LDO +18.75 / TRX +37.63 — the inverse of /118's single-carrier failure) sufficient evidence that C6 IS contributing direct edge despite low importance allocation? The /118 vs /119 inversion (higher feature usage → worse result, lower feature usage → better result) is the load-bearing diagnostic.

4. **TRX OOS concentration_pct = 80.14% — sign-divergence inflation or genuine concentration risk?** `comparison.csv` per-symbol section shows TRX concentration_pct=80.14% (well above the 30% per-symbol merge gate). Engineering report Section 5 + Anomaly Note 5 attribute this to sign-divergence (LDO at -24.03% deflates the positive denominator). However: the absolute TRX OOS PnL +33.89% / total positive PnL = +33.89+31.22 = 65.11% → TRX share of positive PnL = 33.89/65.11 = **52%**. Even after correcting for LDO sign-divergence, TRX dominates the positive PnL by a wide margin. **Question for QR**: at /120 CONFIRMATION 10-seed, if TRX shows multi-seed mean OOS PnL share above 50% of positive total (a /118-style single-symbol carrier signature), does the QR commit a falsifier to BLOCK the bundle?

5. **/120 bundling adjudication — does /119 advance as a NEW edge ingredient, or as PROMISING-FEATURE-MECHANICAL strictly-accretive?** Per the user's prompt, /120 was provisionally planned as SINGLE-COMPONENT of /116 no_confirm. The /119 PROMISING raises three options: (a) TWO-COMPONENT bundle (/116 + C6 both); (b) SINGLE-COMPONENT (just /116 OR just C6, not both); (c) deferred /119 to a separate post-/120 multi-seed validation if importance-15/15 cannot be reconciled with direct signal at single-seed. **Question for QR**: which option does the QR endorse? Specifically: if Clarification 1 reveals C6 is feature-MECHANICAL, does that change the answer (e.g., feature-MECHANICAL + rule-MECHANICAL cannot stack per `feedback_promising_mechanical_subtype.md`)? Or are the two mechanism-types orthogonal enough to bundle? The user's prompt specifically asks the Critic to rule on this; the Critic defers to QR's domain analysis for option (a) vs (b) vs (c).

## Clarifications Requested from QR — END
