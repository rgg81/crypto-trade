# Phase 7.5 Critic Review — iter-v3/092

OVERALL: BLOCK — Gates G4/G5/G6 (DSR/PBO/PSR) are hardcoded EXPLORATION-era sentinels in the runner, NOT computed; a CONFIRMATION's multiple-testing gates were never built into the cross-sectional runner despite brief Section 4.3 mandating real computation.

## Iteration Type (from Brief Section 0.5)
TYPE: CONFIRMATION — the cycle-3 CONFIRMATION, a multi-seed CONFIRMATION-grade closing verdict on the cross-sectional `LGBMRanker` line. Per the dispatch and `feedback_v3_iter018_confirmation_baseline_validation.md`, all 8 standard checks + optionals are scored with full threshold enforcement. DSR/PBO/PSR (G4/G5/G6) are hard gates at a CONFIRMATION — the brief itself states this at Section 4.3: *"/092 is a CONFIRMATION, not an EXPLORATION — so DSR/PBO/PSR are computed for real and are MERGE gates (G4/G5/G6)."*

## Resolution of the Four Scrutiny Items

### Item 1 — The NEW multi-seed code. RESOLVED — the multi-seed machinery is correct; the headline mean is the genuine mean of the two per-seed numbers. PASS.

I traced the entire multi-seed path and it is sound.

- **Outer-seed loop** (`run_cross_sectional_v3.py:1132-1146`): iterates `outer_seeds = CONFIRMATION_OUTER_SEEDS[:args.seeds] = [42, 123]`, runs `_run_one_book` once per outer seed into `seed_<s>/`. Correct.
- **`_derive_ensemble_seeds` copy** (`run_cross_sectional_v3.py:132-134`): character-for-character identical to `run_baseline_v3.py:119-128` — `rng = np.random.default_rng(outer_seed); return [int(s) for s in rng.integers(low=0, high=2**31-1, size=size)]`. The integration test `test_derive_ensemble_seeds_reproduces_baseline_lineage` (test_cross_sectional.py:530) independently re-derives and asserts equality. Faithful copy verified.
- **5-model inner ensemble** (`cross_sectional.py:580-617`): `_train_for_month` loops `for inner_seed in self.ensemble_seeds`, trains one `LGBMRanker` per inner seed with `TPESampler(seed=inner_seed)` and `random_state=inner_seed`, appends to `self._models`. Five distinct objects — `test_inner_ensemble_trains_distinct_models` asserts `len(set(id(m) for m in _models)) == 5`.
- **Score averaging** (`cross_sectional.py:626-631`): `_predict_ensemble` does `np.stack([m.predict(x) for m in self._models], axis=0).mean(axis=0)` — arithmetic mean of the 5 raw ranker scores, applied at the backtest scoring loop (`cross_sectional.py:1171`) before `build_positions`. Score-averaging, not position-averaging — correct per brief Section 3.3 item 2.
- **Aggregate arithmetic** (`_write_aggregate_reports`, lines 811-822): `mean_is = np.mean(is_sharpes)` etc. Verified against the artifacts: aggregate `comparison.csv` `monthly_sharpe` IS = (0.016889620540854328 + 0.1601684410336085)/2 = **0.08852903078** — matches the artifact exactly. OOS = (0.4328433881583013 + 0.20603061801945707)/2 = **0.3194370030** — matches. ratio 0.0885.../0.3194... = 3.6082... — matches. The per-seed `seed_42/comparison.csv` and `seed_123/comparison.csv` are not cross-contaminated (distinct IS/OOS values, distinct per_symbol rosters: seed 42 OOS max AAVE 16.24%, seed 123 OOS max CRV 11.93%).

The multi-seed headline (IS +0.0885 / OOS +0.3194) is the genuine mean of two correctly-computed per-seed books. **No bug in the multi-seed code.** This item PASSES.

### Item 2 — OOS/IS ratio 3.61 and the seed-42 divergence. RESOLVED — a real SUSPICIOUS-class finding, correctly diagnosed by the QE; it does NOT itself BLOCK because the result is NO-MERGE on the floors regardless.

The aggregate OOS/IS ratio 3.61 exceeds the v3 SUSPICIOUS heuristic of 3.0. It is driven entirely by seed 42 (IS +0.0169, OOS +0.4328, ratio 25.63) against a healthy seed 123 (IS +0.1602, OOS +0.2060, ratio 1.29). This is the textbook single-seed-lottery signature — a near-zero IS book makes any modest OOS positive produce an extreme ratio; `frac_positive_paths` for seed 42 is 0.489 (sub-0.5, IS-weak), exactly the /091-reference-book pattern (`feedback_v3_engineered_features_dont_stack.md`). The engineering report (FINDING 2, Anomaly Note 1, Seed Concentration Audit) diagnoses this correctly and explicitly, and does NOT present OOS +0.32 as a durable edge. This is the honest read.

For the Section 8 classification: a 3.61 ratio on a near-flat IS book is a SUSPICIOUS finding, but it does not on its own invalidate the iteration — the result is NO-MERGE by an order of magnitude on G1/G2 (the +1.0 floors), and the OOS-positivity is a lottery artifact the QE has flagged for the record, not bundled as edge. **If the only issue with /092 were the 3.61 ratio, the iteration would be clean enough to FILE as CONFIRMATION-NO-MERGE.** It is not the only issue — see Item 4.

### Item 3 — The inherited /091 fixes at H=21. PARTIALLY RESOLVED — embargo and CPCV row-gap PASS; the gross-Sharpe artifact PASSES; but the DSR/PSR machinery the gross-Sharpe fix's sibling discipline demanded was NOT built (see Item 4).

The embargo fix is correct at H=21. `cross_sectional.py:1027`: `embargo_ms = (XS_HORIZON + 1) * interval_ms` — the corrected wall-clock form, N-independent; arithmetic = 22 × 28,800,000 = 633,600,000 ms ≈ 7.33 days. `_generate_xs_monthly_splits` (`cross_sectional.py:1345`) carries `train_end_ms = test_start_ms - embargo_ms` — the look-ahead fix is present and load-bearing for the trained book's training window. The bugged `XS_REQUIRED_GAP * interval_ms` form is absent (grep confirms). The CPCV row-gap: `XS_REQUIRED_GAP = (21+1)×22 = 484`, asserted at `_verify_xs_gap_assertion` (line 237-241), at `_verify_xs_universe` (line 164-169), and at the CPCV call-site via `expected_gap=XS_REQUIRED_GAP` (`cross_sectional.py` `_compute_xs_cpcv` line 347). Three independent assertions. `cpcv_paths.csv` shows 45 paths with `n_trades=672` per path — consistent. Embargo and purge gap: PASS.

`gross_monthly_sharpe` is a genuine reproducible runner artifact: the shared `_monthly_sharpe(sub, pnl_col)` helper (`_write_xs_reports` lines 456-461) computes both net (`pnl_col="net_pnl"`) and gross (`pnl_col="gross_pnl"`), emitted to per-seed `comparison.csv` and `dsr.json`. The aggregate `_write_aggregate_reports` re-means these (lines 813-814). Aggregate `gross_monthly_sharpe` OOS +0.34732626756 = (0.4586650358275182 + 0.2359874993005352)/2 — matches. Gross-Sharpe artifact: PASS.

### Item 4 — DSR/PBO/PSR at a CONFIRMATION. **NOT RESOLVED — the BLOCKING defect.** DSR and PSR are hardcoded `0.0` literals in the runner. They were NEVER computed. The cross-sectional runner has no CONFIRMATION-grade multiple-testing machinery, and the /092 setup commit — whose entire job was to upgrade the EXPLORATION-era runner to CONFIRMATION spec — did not build it.

This is the defect that BLOCKs the iteration. The evidence is unambiguous and it is in the runner source.

**Per-seed `dsr.json` (`_write_xs_reports`, `run_cross_sectional_v3.py:601-607`):**
```python
dsr_data = {
    "dsr": 0.0,  # not applicable for cross-sectional path at EXPLORATION
    "pbo": pbo_result.pbo if pbo_result.pbo is not None else float("nan"),
    "psr": 0.0,  # not applicable
    ...
    "n_eff": 0,  # not computed for cross-sectional path
```

**Aggregate `dsr.json` (`_write_aggregate_reports`, `run_cross_sectional_v3.py:945-952`):**
```python
dsr_data = {
    "dsr": 0.0,
    "pbo": float("nan"),
    "psr": 0.0,
    ...
    "n_eff": 0,
```

`dsr`, `psr`, `n_eff` are **literal constants**, not computed values. `pbo` is `float("nan")` at the aggregate (hardcoded) and `None`→`NaN` per-seed because `_compute_xs_cpcv` hardcodes `pbo=None` (`cross_sectional.py:397`, comment: `# PBO requires a 2D matrix (multiple strategies)`). The per-seed comment `# not applicable for cross-sectional path at EXPLORATION` is the smoking gun — this is verbatim EXPLORATION-era code. The `0.0` is not a sentinel emitted after an attempted computation; it is a constant the runner has carried since the cross-sectional path was an EXPLORATION-only line.

**Why this is a BLOCK and not a "noted FAIL":**

1. **The brief mandated real computation with a precise escape clause, and the runner satisfies neither branch.** Brief Section 4.3: *"/092 is a CONFIRMATION, not an EXPLORATION — so DSR/PBO/PSR are computed for real and are MERGE gates (G4/G5/G6). The QE computes them on the multi-seed-aggregate book... PSR — Probabilistic Sharpe Ratio of the multi-seed-aggregate OOS monthly-Sharpe series against a benchmark Sharpe of 0, using the OOS monthly return series (15 OOS months)... DSR — the Deflated Sharpe Ratio: PSR deflated for the trial count [350]... the QE locates the canonical v3 DSR helper and applies it."* The escape clause: *"If DSR/PBO/PSR are structurally not computable on the multi-seed-aggregate cross-sectional book (e.g. too few OOS months for a stable PSR), the QE emits an honest sentinel with an explicit note (NOT a fabricated value) and the gate is recorded as a FAIL."* The runner does neither: it did not compute (Branch A), and it did not emit a post-attempt honest sentinel with a structural-impossibility note (Branch B) — it emitted a pre-existing EXPLORATION constant. The Section 4.3 build item was specified and was not implemented.

2. **The gate IS computable — the escape clause does not even apply.** The escape clause is conditioned on *"structurally not computable... (e.g. too few OOS months)."* `seed_42/out_of_sample/monthly_pnl.csv` carries **15 monthly observations** — exactly what brief Section 4.3 names as the PSR input. `psr()` and `deflated_sharpe_ratio_v3()` **exist** in `src/crypto_trade/strategies/ml/validation_v3.py` (lines 508 and 428) — the canonical v3 helpers the brief explicitly directs the QE to "locate and apply." The cross-sectional runner imports `combinatorial_purged_cv` from `validation_v3` (line 82-85) but does NOT import `psr` or `deflated_sharpe_ratio_v3`. A 15-month OOS series with a 350-trial count is a textbook DSR input; `deflated_sharpe_ratio_v3` is explicitly documented (lines 463-468) to handle exactly this case without clamping — *"the runner must not override its return value with 0.0."* The runner overrides it with 0.0. The brief's own anticipated escape hatch ("too few OOS months") does not obtain — the gate could have been computed and was not.

3. **The engineering report narrates the hardcoded zeros as if they were computed — a false provenance.** Engineering report "Note on DSR/PBO/PSR": *"DSR=0.0 and PSR=0.0 reflect that the observed Sharpe does not clear the trials-deflated threshold — the cross-sectional book at 350 trials/cell produces n_eff=0 effective trials, meaning the signal is too faint... PBO=NaN means the CPCV path structure did not produce a valid PBO estimate (the path-level Sharpe distribution has zero discriminative power at this signal level). These are honest sentinel values per Section 4.3."* Every clause of this is a post-hoc rationalization of a literal constant. `n_eff=0` is not a computed effective-trial count from a PCA on a trial-return matrix — it is `"n_eff": 0` hardcoded on line 951. DSR=0.0 is not a deflation result — it is `"dsr": 0.0` on line 946. The "350 trials/cell produces n_eff=0" sentence describes a computation that does not exist anywhere in the runner. This is precisely the /090 BLOCK failure mode the /091 Critic codified: a CONFIRMATION-gate-determining artifact presented as a genuine runner output when it is not. The /091 Critic's Recommendation 1 — *"falsifier-driving numbers must be reproducible runner artifacts"* — and the gross-Sharpe SETUP-2 discipline both exist to forbid exactly this. The gross-Sharpe fix was applied; its DSR/PSR sibling was not.

4. **The Section 8 classification is not honestly FILE-able with three of ten gates fabricated.** Section 8.2 CONFIRMATION-NO-MERGE fires on "any CONFIRMATION-MERGE conjunct fails" — and the conjunction G1∧G2∧G4∧G5∧G6 does fail. But the iteration's job (Section 1.3, the dispatch) was to deliver a *rigorous* multi-seed close — *"convert 'four single-seed iterations suggest the line does not work' into 'a multi-seed CONFIRMATION-grade verdict.'"* A CONFIRMATION-grade verdict whose multiple-testing gates (the gates that exist specifically to certify a result is not overfit) are EXPLORATION-era placeholders is not CONFIRMATION-grade. The verdict G1/G2 deliver (IS +0.09 / OOS +0.32 vs +1.0 floors) is real and reproducible and would, on its own, cleanly close the line. But the brief sells /092 as the *definitive* close, the CONFIRMATION the cross-sectional line "owes" before cycle 4 — and a definitive close cannot be filed with G4/G5/G6 uncomputed. The honest disposition: this is INCONCLUSIVE-adjacent — the CONFIRMATION did not produce its full CONFIRMATION-spec artifact set (Section 4.3's mandated DSR/PBO/PSR), even though it produced a runnable two-seed book. The defect is in the verdict-determining artifact, so it BLOCKs.

The contrast with the /091 PASS is exact and inverts in /092's favor on Items 1-3 but fails on Item 4. At /091 the Critic PASSED because *"the Section 8 classification rests entirely on the runner's own reproducible artifacts"* and the inflated number "appears nowhere in the falsifier evaluation." At /092 three of the ten pre-registered CONFIRMATION gates are hardcoded constants the engineering report's Gate Evaluation table reads as `FAIL` — G4/G5/G6 rows cite `dsr.json` as the source for values that were never computed. The classification rests on fabricated gate inputs. That is the /090 root cause, and the /091 Critic Recommendation 1 was written to prevent its recurrence.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS
The trained book's features are the pre-existing /088 13-feature stack, cross-sectionally rank-normalized per timestamp (within-`open_time` `.rank()` — no temporal leak). The `LGBMRanker` label `label_cross_sectional_rank` at H=21 is a forward-return tercile grade used only as the training target, never as a feature; `cross_sectional.py:247` documents *"r_fwd is the LABEL (the prediction target), not a [feature]."* The 1-bar PnL return is taken strictly after `ts`. Walk-forward `train_end_ms = test_start_ms - embargo_ms` (`cross_sectional.py:1345`). The 60-day (180-bar) listing burn-in is applied per symbol. The multi-seed addition (5 inner models, score averaging) touches no temporal logic — it averages `predict()` vectors on the same `x_t` the single-seed path used. No look-ahead.

### Check 2 — Embargo Width: PASS
Required wall-clock embargo = (H+1)×interval_ms = (21+1)×28,800,000 ms = 633,600,000 ms ≈ 7.33 days; actual `embargo_ms = (XS_HORIZON+1)*interval_ms` at `cross_sectional.py:1027`. Required CPCV row-gap = (H+1)×N = (21+1)×22 = 484; actual `XS_REQUIRED_GAP = 484`, triple-asserted (`_verify_xs_gap_assertion:237`, `_verify_xs_universe:164`, CPCV `expected_gap` parameter at `_compute_xs_cpcv:347`). The /091 Phase-5.5-BLOCK fix is present and unchanged at H=21. The two quantities reconcile: `gap_ts // N = 484 // 22 = 22` timestamp-steps = (H+1) = the wall-clock embargo in timestamp units. Correct.

### Check 3 — Multiple-Testing Correction: FAIL — BLOCKING.
`dsr.json` (aggregate and both per-seed): `dsr=0.0`, `pbo=NaN`, `psr=0.0`, `n_eff=0`. These are **hardcoded literals**, not computed values — `run_cross_sectional_v3.py:601-607` (per-seed) and `:945-952` (aggregate). The per-seed code carries the comment `# not applicable for cross-sectional path at EXPLORATION` — verbatim EXPLORATION-era code. For a CONFIRMATION, G4/G5/G6 are hard gates (brief Section 4.3). The brief mandated real computation via the canonical v3 helpers (`validation_v3.deflated_sharpe_ratio_v3` line 428, `validation_v3.psr` line 508 — both exist; neither is imported by the cross-sectional runner). The brief's escape clause ("structurally not computable, e.g. too few OOS months → honest sentinel with explicit note + FAIL") does not apply: the OOS book has 15 monthly observations (`seed_42/out_of_sample/monthly_pnl.csv`), which is exactly the PSR input brief Section 4.3 specifies, and `n_trials=350` is a valid DSR trial count. The gate was computable and was not computed. The engineering report's "Note on DSR/PBO/PSR" narrates `n_eff=0`, the "trials-deflated threshold," and "350 trials/cell produces n_eff=0" — describing a computation that does not exist in the runner. This is a fabricated-provenance gate artifact: the /090 BLOCK root cause. Per the rubric, a missing/uncomputed required input on a gate that determines the verdict is an automatic FAIL on the affected check, and at a CONFIRMATION it is BLOCKing.

### Check 4 — IC Correlation: PASS (no FAIL; not applicable in the standard sense)
iter-v3/092 adds no new feature family — it is a multi-seed validation run of the RETAINED /088 13-feature stack (`_verify_feature_columns:204-232` asserts exactly 13, /090 downside features ABSENT, `btc_ret_14d` dropped). No new-vs-existing feature pair to test; the |IC|<0.7 gate has no subject. The cross-sectional path emits no `ic_matrix.csv` and the architecture's OOS rank-IC (+0.0358 mean, both seeds +0.033–0.038) is the relevant diagnostic and is reported. PASS by inapplicability — there is no redundancy to find and nothing the iteration introduced to audit.

### Check 5 — ADF Stationarity: PASS (no FAIL; not applicable)
No new price-derived feature is introduced — /092 is a seed-dimension build only. The 13-feature stack is the pre-existing /088 stack, cross-sectionally rank-normalized to bounded [0,1] per timestamp (rank-normalization is stationary by construction). The cross-sectional path emits no `adf_test.csv` and never has; /092 changes nothing here. PASS by inapplicability.

### Check 6 — Pareto Dominance: WARN
The /092 "Pareto" gate (G10) is the v3 2-outer-seed CONFIRMATION form: both outer seeds individually OOS-net-positive. `ensemble_summary.json` `pareto_both_oos_positive: true` — seed 42 OOS +0.4328, seed 123 OOS +0.2060, both positive. G10 PASS. But this is WARN, not clean PASS, because both-seeds-positive on a near-flat IS book is a thin reed: seed 42's IS is +0.0169 (effectively zero) and its `frac_positive_paths` is 0.489 (sub-0.5). Per brief Section 7's own ≈10% failure mode, "one or both outer seeds draw a lucky OOS window" is a named, non-rationalized risk, and seed 42 plainly exhibits it (OOS/IS 25.63). G10 passes mechanically; the underlying robustness it is meant to certify is weak. This does not BLOCK — the result is NO-MERGE on the floors regardless — but the QR closeout must not present G10 PASS as evidence of seed-robustness; it is a 2-seed both-positive coincidence on a faint-IC architecture.

### Check 7 — Reproducibility: PASS
Setup commit SHA `a6217d3ff5fe5545de3807cb60153901cfdd6670` stamped in the engineering report. The runner passes `feature_columns=XS_FEATURE_COLUMNS` explicitly (`run_cross_sectional_v3.py:713`); `CrossSectionalRankStrategy.__init__` raises on `None`/empty (the pre-existing guard). Each outer seed pins `TPESampler(seed=inner_seed)` and `LGBMRanker(random_state=inner_seed)` per inner model (`cross_sectional.py:602, 612`). The `_derive_ensemble_seeds` copy is bit-faithful (Item 1). Multi-seed integration tests present (test_cross_sectional.py:530-696, four tests covering the four brief Section 3.3 item 7 assertions). `ITERATION_LABEL = "v3-092"` (`run_cross_sectional_v3.py:92`). Trade-row spot check (engineering report Anomaly Note 4) on `seed_42/out_of_sample/trades.csv`: weights, timestamps, no-NaN — clean. Note: the multi-seed integration test `test_aggregate_comparison_csv_mean_equals_seed_mean` verifies the aggregate `comparison.csv` mean arithmetic but asserts **nothing** about `dsr.json` — there is no test covering the `dsr.json` emission at all, which is part of why the Check 3 defect shipped (see Recommendation 2). Reproducibility of what the runner *does* compute: PASS. The defect is what it does not compute, scored under Check 3.

### Check 8 — Hypothesis-Implementation Alignment: PARTIAL — counts as FAIL.
The configuration alignment is correct: `score_mode="trained"`, `XS_HORIZON=21`, `XS_HOLD_BARS=21`, the 13-feature stack, the /089 construction constants, the corrected embargo — all match brief Section 3.1, and the runner docstring and inline comments are consistent. The outer-seed loop, 5-model inner ensemble, score-averaging, per-seed + aggregate reports, `ensemble_summary.json`, the 2-seed Pareto boolean — all match brief Section 3.3 items 1-6. **But brief Section 3.3 is a seven-item spec and Section 4.3 is a load-bearing build item, and the DSR/PBO/PSR-for-real requirement (Section 4.3, explicitly *"computed for real... MERGE gates"*) was not implemented** — the runner emits EXPLORATION-era hardcoded sentinels. The brief at Section 1.3 / Section 10.4 sells /092 as a *CONFIRMATION-grade* close; the implementation delivers an EXPLORATION-grade multiple-testing surface. The code implements most of the registered build but omits the CONFIRMATION-specific gate machinery the brief mandated — a build-completeness gap on a verdict-determining component. FAIL.

## Optional Checks 9–12

- **Check 9 — Symbol Exclusion Enforcement: PASS.** `_verify_xs_universe` (`run_cross_sectional_v3.py:155-169`) computes `set(XS_UNIVERSE) & set(V3_EXCLUDED_SYMBOLS)` and raises on overlap; runs unconditionally in `main()` preflight (line 1067). Audit present.
- **Check 10 — Feature Isolation Enforcement: PASS.** `cross_sectional.py` imports only from `crypto_trade.config` and stdlib/lgb/optuna; the runner imports `crypto_trade.features_v3` (the v3 track) and `crypto_trade.strategies.ml.validation_v3`. Grep for `from crypto_trade.features ` (the v1/v2 module) returns nothing. No cross-track import.
- **Check 11 — Forming-Candle Audit: PASS (delegated).** `_verify_data_freshness` (lines 172-201) asserts each symbol's last `close_time` is within 16h of now OR past `OOS_CUTOFF_MS`; runs unconditionally in preflight. The OOS book extends to 2026-05 (`seed_42/out_of_sample/monthly_pnl.csv` last row 2026-05) consistent with today. No forming-candle tail evident.
- **Check 12 — Library Version Pinning: PASS.** Brief Section 9 declares LightGBM/Optuna/pandas/numpy + the v3 DSR/PBO/PSR helpers, no new third-party dependency. The multi-seed build adds none — it is a count change (10 models/cell vs 1) plus pure pandas/numpy aggregation. Nothing to mismatch. (Note: the DSR/PSR helpers Section 9 declares as a dependency are precisely the helpers the runner failed to call — a Section-9-vs-implementation gap, but scored under Check 3 as the substantive defect, not double-counted here.)

## Recommendations to QR

The /092 result is NO-MERGE — that is correct and pre-registered, and not in dispute. But /092 cannot be FILED as a clean CONFIRMATION-NO-MERGE: three of its ten CONFIRMATION gates (G4/G5/G6) are EXPLORATION-era hardcoded sentinels, not computations, and the engineering report narrates them as if computed. This is the /090 BLOCK root cause recurring. These recommendations are process-level for the corrected re-run / next iteration — the verdict on the current artifact set is BLOCK.

1. **Build the CONFIRMATION-grade DSR/PBO/PSR machinery into the cross-sectional runner before re-filing /092.** The brief (Section 4.3) mandated it; the setup commit `a6217d3` did not deliver it. `validation_v3.psr` (line 508) and `validation_v3.deflated_sharpe_ratio_v3` (line 428) already exist and are documented to handle the /092 inputs (15-month OOS series, 350 trials, negative-or-faint SR — `deflated_sharpe_ratio_v3` explicitly does not clamp to 0.0 and its docstring warns *"the runner must not override its return value with 0.0"*; the runner does exactly that override). The cross-sectional runner imports only `combinatorial_purged_cv` from `validation_v3` — it must additionally import and call `psr` and `deflated_sharpe_ratio_v3` on the multi-seed-aggregate OOS monthly-return series, and the QE must document the exact SR granularity fed to `psr()` per `feedback_v3_methodology_post_hoc_input_traceback.md`. PBO requires the CPCV 2D path matrix; if a genuine PBO is structurally unavailable (the `_compute_xs_cpcv` comment claims *"PBO requires a 2D matrix (multiple strategies)"*), that is a real structural-impossibility case — but then the brief's escape clause must be followed literally: an honest sentinel emitted *after* the attempt, with an explicit note stating the structural reason, recorded as FAIL — NOT a pre-existing `float("nan")` constant inherited from EXPLORATION code.

2. **Add a `dsr.json` integration test to the cross-sectional multi-seed test suite.** The four /092 integration tests (test_cross_sectional.py:530-696) cover seed derivation, inner-ensemble distinctness, score-averaging, and aggregate-mean arithmetic — but none asserts anything about `dsr.json`. A test asserting the aggregate `dsr.json` `dsr`/`psr` are finite computed values (not `0.0` literals) and that PBO is either a genuine fraction or an explicitly-noted structural sentinel would have caught this defect at Phase 6. This is the `feedback_v3_methodology_axis_integration_test.md` discipline applied to the gate that was actually un-tested.

3. **The engineering report must never narrate a hardcoded constant as a computed result.** The /092 report's "Note on DSR/PBO/PSR" describes `n_eff=0`, a "trials-deflated threshold," and "350 trials/cell produces n_eff=0" — a causal story for values that are literal constants on lines 946-951 of the runner. Per the /090 closeout and `feedback_v3_methodology_post_hoc_input_traceback.md`, every classification-driving number in an engineering report must be a reproducible runner artifact with a stated code path. A future report stating any DSR/PBO/PSR value must cite the `validation_v3` call-site that produced it, or — for a genuine sentinel — state plainly "hardcoded sentinel, not computed, because [structural reason]; gate recorded FAIL." The corrected /092 re-run must produce real G4/G5/G6 numbers (which, on this faint book, will still FAIL the >0.95 / <0.4 thresholds — but as honest computations, making the CONFIRMATION-NO-MERGE close genuinely rigorous and FILE-able).
