# Phase 7.5 Critic Review — iter-v3/091

OVERALL: MERGE

(EXPLORATION FILE verdict — the iteration is methodologically clean enough to record as NEGATIVE. "MERGE" here is the orchestrator-routing token meaning "file the closeout"; iter-v3/091 is an EXPLORATION and does NOT update BASELINE_V3.md — it stays `v0.v3-059`. The result is plainly NEGATIVE: F1 and F2 both fire.)

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION — cycle-3 slot #10 of 10, scoring-function-axis EXPLORATION on the RETAINED /088 cross-sectional architecture. DSR/PSR/PBO edge FAILs are informational and non-blocking per `feedback_v3_dsr_mode_artifact.md`. The model-free book additionally has no overfitting surface at all (no Optuna, no seed) — Check 3 is doubly vacuous for the primary book.

## Resolution of the Four Scrutiny Items

### Item 1 — The EDA-vs-runner fidelity gap: the load-bearing concern. RESOLVED — real Phase-1-5 EDA-fidelity flaw, but NOT a Section-8-invalidating defect; does NOT BLOCK.

I traced this to root cause. The QE's engineering report (Anomaly Note 3) hypothesizes the gap is "a different construction (no overlapping-hold tranche, no no-trade-band)" or "the 484-row CPCV gap eliminates more data." **Both QE hypotheses are wrong.** The actual cause is an **IS-window mismatch**, and the EDA's own committed CSVs prove it:

- The EDA's headline +0.2964 is produced by `holding_horizon_eda.py::model_free_horizon_scan` (and re-confirmed by `model_free_vs_ranker_eda.py::_run_cost_aware_book` via `_model_free_bars`). Both functions iterate **every** timestamp in `mom_wide.index` with **zero walk-forward segmentation** — `holding_horizon_eda.py:429` `for ts in timestamps` and `model_free_vs_ranker_eda.py:355` `for ts in mom_wide.index.to_numpy()`. The EDA console (`model_free_vs_ranker_eda_output.txt:13440`) confirms: `[panel] IS panel 103492 rows, 5547 timestamps` — the EDA scores the **full IS panel, 2020-04 → 2025-03**.
- The runner's model_free book is run inside `run_cross_sectional_backtest`, whose outer loop is `for split in splits` over `_generate_xs_monthly_splits(training_months=24)` (`cross_sectional.py:1011, 1048`). Even in `score_mode="model_free"` — which skips training — the runner STILL iterates only the walk-forward **test windows**, which begin after the first 24 calendar months. The runner's IS book is **2022-03 → 2025-03 (37 months)**: `reports-v3/iteration_v3-091/in_sample/monthly_pnl.csv` first row is `2022-03`; `in_sample/trades.csv` first `open_time=1646121600000` = 2022-03-01.
- The EDA's own M1b sub-period table (`M1b_model_free_subperiod_stability.csv`) shows the 5-year window the EDA scored is dominated by one regime: `third_1` (2020-04..2021-12) net **+0.5564**; `third_2` (2021-12..2023-08) net **−0.1039**; `third_3` (2023-08..2025-03) net **+0.1552**. The runner's IS window (2022-03→2025-03) **excludes the entire +0.5564 third_1 bull regime** and consists of ~75% of the −0.1039 third_2 plus all of the +0.1552 third_3. A model-free book restricted to that span lands near breakeven — the runner's IS net **+0.017** is fully consistent with the EDA's own sub-period decomposition. There is no "simpler construction" mystery; the constructions are structurally faithful (I diffed `_run_cost_aware_book` against `build_positions` + the `run_cross_sectional_backtest` tranche/no-trade-band loop — corrected-sign quintile, inverse-vol, `_scale_to_vol_target`, overlapping `H`-tranches, `_apply_no_trade_band`, `searchsorted(side="right")` PnL, turnover fee — all match). The EDA simply measured a different and far more favorable IS window than the runner structurally can.

**This is a genuine Phase-1-5 EDA-fidelity flaw.** The brief's Section 2 evidence — +0.2964, the M1a [17,25] plateau, the M2 "+0.2020 gap" — is computed over a 2020-04→2025-03 window that the production walk-forward cannot reproduce. Brief Section 1's predicted "+0.2020 IS net spread improvement" and Section 4.2's "modal OOS net [+0.00, +0.20]" therefore rested on an inflated, non-runner-faithful base. The brief, the Phase 5.5 gate, and the QE engineering report all missed it — the gate's "Additional Notes" even cross-checked +0.2964 against the committed CSV and called it verified, without noticing the CSV itself is a non-walk-forward measurement. This is a methodology weakness in the iteration's design phase and the QR's closeout must own it explicitly (Recommendation 1).

**But it does NOT meet the BLOCK bar, and the contrast with /090 is precise.** The /090 BLOCK was a defect in the **engineering report's classification-driving number** — `gross_monthly_sharpe` was hand-computed on a non-per-month basis with a mathematically-impossible /089 anchor (+0.5947), and that corrupt number fed falsifier F3, which feeds Section 8. The defect was *inside the artifact that determines the verdict*. Here, the opposite holds: the /091 **run is clean**, and the Section 8 classification rests **entirely on the runner's own reproducible artifacts** — F1 reads `comparison.csv` `monthly_sharpe` OOS = −0.0995, F2 reads `gross_monthly_sharpe` OOS = −0.0178, both produced by the shared `_monthly_sharpe` helper on the genuine walk-forward book. The EDA's inflated +0.2964 appears nowhere in the falsifier evaluation. The flaw corrupted the brief's *predictions* (which are estimates, not gates) — and a NEGATIVE result delivered against over-optimistic predictions is exactly the outcome the pre-registered falsifier mechanism exists to catch and FILE. The classification (8.2 CONSTRUCTION-FALSIFIED) is filed on sound, reproducible evidence. The flaw is a recorded process lesson, not a verdict-invalidating defect.

### Item 2 — The embargo fix. PASS — verified correct at both sites; CPCV row-gap correctly unchanged.

`cross_sectional.py:998` reads `embargo_ms = (XS_HORIZON + 1) * interval_ms` — the corrected wall-clock form, N-independent. `run_cross_sectional_v3.py:915` (the `_run_smoke_test` path) reads `embargo_ms = (XS_HORIZON + 1) * interval_ms` — same fix. The bugged `XS_REQUIRED_GAP * interval_ms` form is absent from both `src` and the runner (grep confirms no live occurrence). The arithmetic: corrected = 22 × 28,800,000 ms = 633.6M ms ≈ 7.33 days; the prior bug at H=21 would have been 484 × 28.8M ≈ 160.9 days — the engineering report's "Embargo Fix Audit" figures are correct. The CPCV flattened-row gap is correctly left at `XS_REQUIRED_GAP = (H+1)×N = 484` (`cross_sectional.py:107`, asserted at the CPCV call-site `run_cross_sectional_v3.py:327` via `expected_gap`, and `_cv_rank_ic:648` converts `gap_ts // len(symbols) = 484 // 22 = 22` timestamp-steps = (H+1) — identical to the wall-clock embargo in timestamp units, as it must be). The IT-3 embargo-distinction test (`test_iter091_embargo_distinction`) codifies both quantities and asserts the bugged form is N× larger. The Phase-5.5 BLOCK that caught the original bug is fully resolved. Note: the primary model_free book trains nothing and is embargo-independent regardless — the fix matters only for the reference LGBMRanker comparator's training window, which is correct.

### Item 3 — The gross-Sharpe runner artifact (Critic /090 Rec #1). PASS — genuine reproducible runner output via the shared helper.

`run_cross_sectional_v3.py:432-445`: a single `_monthly_sharpe(sub, pnl_col)` helper computes both net and gross — `is_ms`/`oos_ms` pass `pnl_col="net_pnl"`, `is_gross_ms`/`oos_gross_ms` pass `pnl_col="gross_pnl"`. Both emit to `comparison.csv` (the `gross_monthly_sharpe` row, `run_cross_sectional_v3.py:516-520`) and `dsr.json` (`gross_monthly_sharpe_is/_oos`, lines 586-587). The model_free book's `dsr.json` carries `gross_monthly_sharpe_is=+0.11697749978527425`, `gross_monthly_sharpe_oos=−0.017813224162000176` — full float precision, identical to `comparison.csv`. The engineering report's gross figures (+0.117 / −0.018) are the rounded artifact values, NOT hand-computations — this is the structural opposite of the /090 BLOCK root cause. IT-2 (`test_iter091_gross_sharpe_shared_helper`) monkeypatch-asserts the shared code path. Critic /090 Rec #1 is closed.

### Item 4 — The reference LGBMRanker IS −0.111 / OOS +0.461 inversion. NOTED — single-seed artifact on a non-/091 reference book; no construction issue; QR closeout should note it but it does not affect the verdict.

The reference book is `score_mode="trained"` — explicitly the comparator, NOT the /091 strategy. The IS/OOS ratio −4.17 is structurally suspicious in isolation, but: (a) it runs at the EXPLORATION single-seed (`seed=42`, `ensemble_size=1`, `n_trials=35`) and v3 has a documented single-seed-inversion precedent (`feedback_v3_engineered_features_dont_stack.md`, iter-v3/026/027); (b) it shares the identical /089 cost-aware construction with the model_free book, which does NOT invert (model_free IS +0.017 / OOS −0.100, same-sign-ish) — so the construction is not the cause; the trained model's Optuna single-seed lottery is. (c) `frac_positive_paths` for the reference book is 0.356 — sub-0.5, IS-weak, consistent with a noisy single-seed fit that got a lucky OOS draw. This is not a SUSPICIOUS-class finding **for the /091 iteration** because the /091 strategy is the model_free book, and the model_free book's own SUSPICIOUS check (brief 8.1) does not fire — model_free OOS net/IS net = −0.0995/+0.0170 = −5.85 (negative ratio, the 8.1(a) N/A case); model_free OOS rank-IC +0.0142 is not ≥ 2× its own IS rank-IC; model_free OOS gross −0.018 is not ≥ 3× +0.1717. The QR's closeout should record the reference-book inversion as a single-seed comparator artifact (not escalate it), and note it weakens the brief's Section 4.2 prediction that "the model-free book beats the LGBMRanker OOS" — in fact the reference LGBMRanker OOS net +0.461 *beat* the model_free OOS net −0.100. That prediction was not a falsifier (brief 4.2 explicitly flags it "a prediction, not a falsifier"), so it does not gate — but it is a second over-optimistic prediction that the EDA-window-inflation (Item 1) contaminated, and the closeout should say so.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS
The model_free score (`cross_sectional.py:1035`) is `mom_wide = close_wide / close_wide.shift(XS_HORIZON) - 1.0` — `close(ts)` and `close(ts − H·interval)` are both ≤ `ts`. The 1-bar PnL return is taken via `ret_wide.index.searchsorted(ts, side="right")` (`cross_sectional.py:1199`) — strictly after `ts`. The reference book's features are cross-sectionally rank-normalized per timestamp (`build_cross_sectional_panel:475-486`, within-timestamp `.rank()` — no temporal leak) and the LGBMRanker label `label_cross_sectional_rank` is a forward-return rank used only as the training target, never as a feature. The 60-day (180-bar) listing burn-in is applied per symbol. Walk-forward `train_end_ms = test_start_ms − embargo_ms` (`cross_sectional.py:1314`). No look-ahead in either book.

### Check 2 — Embargo Width: PASS
Two distinct quantities, both correct (full detail in Item 2). Required CPCV row-gap = (H+1)×N = (21+1)×22 = 484; actual `XS_REQUIRED_GAP = 484`. Required wall-clock embargo = (H+1)×interval_ms = 22×8h ≈ 7.33 days; actual `embargo_ms = (XS_HORIZON+1)*interval_ms` at both `cross_sectional.py:998` and `run_cross_sectional_v3.py:915`. The CPCV's `gap_ts // len(symbols) = 22` timestamp-steps reconciles the row-count gap to the wall-clock embargo exactly. The model_free primary book is embargo-independent (trains nothing); the fix is load-bearing only for the reference comparator and is correct.

### Check 3 — Multiple-Testing Correction: FAIL (informational — non-blocking for EXPLORATION)
`dsr.json`: `dsr=0.0`, `pbo=NaN`, `psr=0.0`, `n_trials=0` for the model_free book. These are sentinels, correctly so — a parameter-free trailing-return book has zero fitted parameters, zero Optuna trials, and no overfitting surface; DSR/PBO/PSR are not merely EXPLORATION-mode artifacts here, they are structurally undefined for the primary book (brief Section 6.1 states this honestly and the runner emits sentinels rather than fabricating values). The reference book's `n_trials=35`, `frac_positive_paths=0.356`. Per `feedback_v3_dsr_mode_artifact.md` and the EXPLORATION dispatch, Check 3-edge axis FAILs do NOT trigger BLOCK for an EXPLORATION; flagged for the record. `frac_positive_paths` for the model_free book is **0.489** (44.9% of 45 CPCV paths net-positive) — sub-0.5, an honest IS-weak reading consistent with the breakeven +0.017 IS net; the CPCV is the informative diagnostic here and it agrees the book is weak.

### Check 4 — IC Correlation: PASS (not applicable in the standard sense; no FAIL)
iter-v3/091 adds **no new feature family** — the /091 axis is a scoring-function change (LGBMRanker prediction → parameter-free trailing-return), and the /090 downside features are **reverted** (`expand_downside=False`, `XS_FEATURE_COLUMNS` back to the 13-feature base, verified `run_cross_sectional_v3.py:107` and `_verify_feature_columns:184-212`). The model_free book uses no feature stack at all (it scores by raw trailing return). There is no new-vs-existing feature pair to test, so the |IC|<0.7 gate has no subject. No `ic_matrix.csv` is expected for this axis type (the cross-sectional path has never emitted one; the architecture's IS rank-IC is the relevant diagnostic and is reported). PASS by inapplicability — there is no redundancy to find.

### Check 5 — ADF Stationarity: PASS (not applicable; no FAIL)
No new price-derived feature is introduced. The model_free score is a trailing-21-bar return — a return ratio, inherently a differenced/stationary transform — and it is the *score*, not a feature fed to a model. The 13-feature reference stack is the pre-existing /088/089 stack, cross-sectionally rank-normalized to [0,1] per timestamp (rank-normalization is bounded and stationary by construction). The cross-sectional path emits no `adf_test.csv` and never has; nothing in /091 changes that. PASS by inapplicability.

### Check 6 — Pareto Dominance: PASS (not applicable; no FAIL)
The model_free primary book is deterministic — no seed, no Optuna, no ensemble. There is no seed sweep and no `pareto_front.csv` (the cross-sectional EXPLORATION path does not produce one; the unified-architecture baseline retired it). The chosen "configuration" is the single pre-registered H=21, defended in brief Section 2.7 as the literature-canonical interior point of the smooth [17,25] M1a plateau rather than the IS arithmetic-max H=22/23 — disciplined, not tunnel-vision. No dominated-seed risk exists. PASS by inapplicability; the /092 CONFIRMATION would validate the book by IS sub-period / OOS-window stability rather than a seed sweep, as the brief states.

### Check 7 — Reproducibility: PASS
Setup commit SHA `b94b90f5e0a6a1d8ac4fc656c90592d45c9cc2d4` is stamped in the engineering report. The runner passes `feature_columns=XS_FEATURE_COLUMNS` explicitly (`run_cross_sectional_v3.py:690`) — `CrossSectionalRankStrategy.__init__:523-527` raises `ValueError` on `None`/empty; no auto-discovery. The model_free book is deterministic (no seed dependence); the reference book pins `seed=42` and `TPESampler(seed=self.seed)`. Trade spot-check on `in_sample/trades.csv` row 1 (`TRXUSDT`): `fee = |pos − prev_pos|·0.001 = |−0.038369660198546346|·0.001 = 3.836966e-05` — matches reported `3.836966019854635e-05` exactly; `net_pnl = gross − fee = −0.00029232842102961315 − 3.836966e-05 = −0.0003306980812281595` — matches exactly. OOS `trades.csv` row 1 `open_time=1742774400000` = 2025-03-24 = `OOS_CUTOFF_MS` exactly. The `_verify_xs_universe`, `_verify_feature_columns`, `_verify_xs_gap_assertion` preflight assertions all run. The full cross-sectional test suite (`tests/strategies/ml/test_cross_sectional.py`) carries the three /091 integration tests (IT-1 model_free scoring, IT-2 gross-Sharpe shared-helper, IT-3 embargo distinction) plus the 16 pre-existing groups.

### Check 8 — Hypothesis-Implementation Alignment: PASS
Brief Section 1/3.1 hypothesis: replace the LGBMRanker prediction with a parameter-free trailing-21-bar-return cross-sectional score, retaining the /089 cost-aware construction. The implementation matches: `score_mode` parameter added to `run_cross_sectional_backtest` (default `"trained"`, preserving /088/089/090); `score_mode=="model_free"` branch at `cross_sectional.py:1112-1135` computes `mom_wide.loc[ts]` as the score and skips `_train_for_month`; `run_cross_sectional_v3.py` runs the dual-book (`_run_one_book` ×2 — model_free primary to `reports-v3/iteration_v3-091/`, trained reference to `reference_lgbmranker/`). The /089 construction constants (`XS_QUANTILE_FRAC=0.20`, `XS_NO_TRADE_BAND=0.020`, `XS_TURNOVER_CEILING=0.138`, overlapping holds) are retained verbatim. The two SETUP items (embargo fix, gross-Sharpe artifact) are correctly scoped as correctness/instrumentation, not edge axes — both verified above. The /090 feature expansion is reverted, not re-axised. `ITERATION_LABEL="v3-091"`. No scope creep, no hypothesis-faking — the code tests exactly the registered scoring-function axis. (The one caveat: the brief's *quantitative predictions* derived from a non-runner-faithful EDA window — Item 1 — but that is an evidence-base flaw, not a hypothesis-implementation misalignment; the code implements the stated hypothesis faithfully.)

## Optional Checks 9–12

- **Check 9 — Symbol Exclusion Enforcement: PASS.** `run_cross_sectional_v3.py::_verify_xs_universe` (lines 135-149) computes `set(XS_UNIVERSE) & set(V3_EXCLUDED_SYMBOLS)` and raises on any overlap; `test_xs_universe_not_in_v1v2` regression-covers it. The 22-symbol `XS_UNIVERSE` is disjoint from v1/v2 symbols.
- **Check 10 — Feature Isolation Enforcement: PASS.** Grep for `from crypto_trade.features ` (the v1/v2 feature module) in `cross_sectional.py` returns nothing — the module imports only from `crypto_trade.config` and stdlib/lgb/optuna; the runner imports `crypto_trade.features_v3` (the v3 track). No cross-track import.
- **Check 11 — Forming-Candle Audit: PASS (delegated).** `_verify_data_freshness` (lines 152-181) asserts each symbol's last `close_time` is within 16h of now OR past `OOS_CUTOFF_MS`. The OOS `trades.csv` extends to 2026-05 (`out_of_sample/monthly_pnl.csv` last row 2026-05) consistent with today 2026-05-17. No evidence of a forming-candle tail; the runner's freshness guard ran (preflight is unconditional).
- **Check 12 — Library Version Pinning: PASS.** Brief Section 9 declares LightGBM/Optuna/pandas/numpy with **no new third-party dependency** — and the implementation adds none (the model_free score is pure pandas `close_wide.shift`; the embargo fix is a one-line constant; the gross-Sharpe artifact is a pandas refactor). No mlfinlab/mlfinpy/pypbo/fracdiff change. Nothing to mismatch.

## Recommendations to QR

Three items the /092 CONFIRMATION brief (and the /091 closeout diary) must carry. The /091 result is a clean NEGATIVE — CONSTRUCTION-FALSIFIED (brief 8.2: F1 and F2 both fire) — and the diary should file it as such; these recommendations are process-level for the next iteration, not a "fix /091" list.

1. **The EDA-runner IS-window mismatch must be documented as a process lesson in the /091 diary, and the cross-sectional EDA harness must be made walk-forward-faithful.** The brief's entire Section 2 evidence base (+0.2964 IS net, the M1a [17,25] plateau, the M2 "+0.2020 gap") was computed by `model_free_horizon_scan` / `_run_cost_aware_book` over the **full 2020-04→2025-03 IS panel with no walk-forward segmentation**, while the production runner's model_free book is structurally restricted to the **2022-03→2025-03 post-training-window span** (first 24 months are training-only). The EDA's own M1b shows the excluded 2020-21 third carries net +0.5564 — the EDA measured a window dominated by the strongest regime the runner cannot trade. The runner's +0.017 IS net is not a mystery; it is the EDA's own sub-period evidence. **Fix:** any future cross-sectional EDA whose number is cited as a runner-fidelity target MUST run the model_free book through `_generate_xs_monthly_splits(training_months=24)` — i.e., score only the post-burn-in walk-forward test windows — so the EDA's IS span is bit-identical to the runner's. The /090 closeout established that falsifier-driving numbers must be reproducible runner artifacts; the /091 lesson is the dual — EDA *prediction-driving* numbers must be measured on the runner's actual evaluation window. The Phase 5.5 gate's "Additional Notes" cross-check (which verified +0.2964 against the committed CSV) must, in future, also verify the CSV was produced on the walk-forward span — verifying a number against a non-walk-forward CSV verifies the wrong thing.

2. **The /092 CONFIRMATION must not bundle a model-free cross-sectional book as a validated edge ingredient — the honest /091 finding is that the cross-sectional line has produced no net-positive OOS book in four iterations.** /088 (−0.5418) → /089 (−0.0985, mechanical) → /090 (−0.0770, FALSIFIED) → /091 model_free (−0.100, FALSIFIED). The /091 axis was justified on an inflated EDA window; on the runner's real window the model_free book scores IS +0.017 / OOS −0.100 — at or below the /089 anchor it was meant to beat. The reference LGBMRanker book's OOS +0.461 is a single-seed lottery draw (IS −0.111; `frac_positive_paths` 0.356) and must NOT be cited as evidence the trained model "works" — a single-seed inversion is the documented `feedback_v3_engineered_features_dont_stack.md` artifact. The cross-sectional line remains a research direction with a genuine but faint OOS rank-IC (+0.014) and **zero net-positive OOS books**; the /092 CONFIRMATION should treat it accordingly and the diary should state the four-iteration ledger plainly.

3. **Future cross-sectional EXPLORATION briefs must pre-register a falsifier on the EDA-vs-runner IS-window equivalence, not only on the OOS outcome.** /091's F1–F5 gate the OOS book against /089's anchors — correctly — but there was no gate catching that the IS *prediction base* (+0.2964) was unreproducible. A brief that pre-registers "the EDA model_free book and the runner model_free book must agree on IS net monthly Sharpe to within ±0.05 on the shared 2022-03→2025-03 walk-forward span" would have caught the +0.28 gap before Phase 6, at the Phase 5.5 gate — turning a wasted EXPLORATION slot into a corrected brief. This is the EDA-fidelity analogue of `feedback_v3_methodology_axis_integration_test.md` and should become standard for any cross-sectional axis where the brief's predictions are derived from a separate EDA harness rather than the runner itself.
