# Phase 7.5 Critic Review — iter-v3/080

OVERALL: MERGE

## Iteration Type (from Brief Section 0.5)
TYPE: EXPLORATION — cycle 2 EXPLORATION #10 of 10 (the FINAL cycle-2 EXPLORATION). Axis category: PASSIVE-DIAGNOSTIC (persist per-trade M1 `confidence` to `trades.csv` + emit `confidence_distribution.csv`; mandatory secondary baseline-restore reverts /079's conviction-derate to flat `weight = 100`).

## Verdict Basis

A single-round full review. Every check resolved unambiguously; zero clarifications were required from the QR. The verdict is FINAL. `OVERALL=MERGE` certifies the NULL-RESULT classification clean, the fix-and-rerun methodologically clean, the persisted-`confidence` deliverable sound and the /079 Critic Rec #1 debt discharged. iter-v3/080 does NOT advance — a PASSIVE-DIAGNOSTIC produces no edge ingredient; it does not bundle into the /081 CONFIRMATION and does not update `BASELINE_V3.md` (/059 stays canonical, tag `v0.v3-059`).

## Three Special Adjudications

### Adjudication #1 — NULL-RESULT classification: CERTIFIED CLEAN

Independently re-applied the brief Section 8 LOCKED disjunctive taxonomy (SUSPICIOUS → NULL-RESULT → NEGATIVE → PROMISING → INERT). Anchor IS +0.8236 / OOS +0.2078; observed IS +0.8236 (Δ 0.0000), OOS +0.2908 (Δ +0.0830), OOS/IS ratio 0.3531.

- **SUSPICIOUS 8.4 ratio gate**: 0.3531 > 3.0 → NO.
- **SUSPICIOUS 8.4 OOS-DOMINANT sub-mode**: requires IS shift < 0 AND OOS shift ≥ +0.20. IS shift is exactly **0.0000** — the `IS shift < 0` clause is FALSE (0.0000 is not strictly negative). Sub-mode does not fire. The `OOS shift ≥ +0.20` clause also fails independently (+0.0830 < +0.20). SUSPICIOUS cannot fire on either ground.
- **NULL-RESULT 8.5**: FIRES. Independently verified the bit-identical roster by diffing `reports-v3/iteration_v3-080/{in_sample,out_of_sample}/trades.csv` against `reports-v3/iteration_v3-077/`: IS 159 trades (0 keys added, 0 removed), OOS 103 trades (0 keys added, 0 removed), every IS+OOS `(symbol, open_time)` key and `weight_factor` matches line-for-line. The single OOS divergence is `(LDOUSDT, 1778716799999)`: bit-identical key, bit-identical `weight_factor=0.7700`, `close_time` extended +16h, `pnl_pct` 1.8051 → 6.3435 — exactly one `end_of_data` trade resolving with 2 more candles. IS shift = 0.0000 exactly; OOS shift +0.0830 falls within the brief's pre-registered "± a small data-extent drift" — the identical LDO `end_of_data` mechanism the /077 (+0.0675) and /079 (+0.0713) Critics certified benign, and the OOS key roster is bit-identical (0/0), so the +0.0830 cannot trip any other gate. NULL-RESULT is the first canonical match in disjunctive order.

### Adjudication #2 — the fix-and-rerun: CLEAN AND LEGITIMATE

(a) **The fix `d71adf6` is pure passive metadata.** Audited both wrapper `Signal(...)` reconstruction sites. `RiskV2Wrapper.get_signal` (`risk_v2.py:367-373`) has exactly ONE reconstruction site; the fix added `confidence=sig.confidence` at line 372. The reconstructed `Signal`'s `weight` is `sig.weight × scale × cap_scale` (line 366) — `confidence` is NOT in that arithmetic, NOT in any gate (`_zscore_ood`, `_hurst_ood`, `_adx_gate_fails`, `_low_vol_filter_fails`, `_per_symbol_cap_scale`, `_vol_scale`, drawdown brake), NOT in any conditional. `RiskV3Wrapper.get_signal` (`risk_v3.py:405-411`) has exactly ONE reconstruction site — the primitive-12 regime-size-scalar branch; the fix added `confidence=sig.confidence` at line 410; that branch's `weight = sig.weight × s` does not consult `confidence`. The primitive-10 direction-block path returns the module-level `NO_SIGNAL` constant (no reconstruction); the `direction == 0` and other early returns pass `sig` through unmodified. The fix covers the exactly-two drop points the QE identified, and the bit-identical re-run roster corroborates that the fix touched no decision path. (b) **The regression test genuinely covers the wrapper chain.** `tests/strategies/ml/test_confidence_passthrough.py::TestWrapperChainConfidencePassthrough` routes a `Signal` with a known `confidence` through both wrappers with gates disabled and asserts the field survives — including `test_risk_v3_wrapper_passes_confidence_through_regime_scalar_fires`, which injects a `_btc_trend_lookup` to FORCE the primitive-12 reconstruction branch (the second drop point) and asserts both `weight==50` (scalar applied) AND `confidence` preserved. `TestNoDecisionPathReadsConfidence` adds a static scan over `src/crypto_trade/*.py` outside an allow-list, asserting no `if/while .confidence` decision branch — the defect cannot recur. (c) The pre-Critic Phase 6 correction did not and could not change the classification — the re-run `comparison.csv` is bit-identical to the pre-fix run (IS +0.8236, OOS +0.2908, 159/103); the fix is pure metadata. The fix-and-rerun is methodologically clean.

### Adjudication #3 — Check 1 look-ahead on the confidence threading + deliverable verification: CLEAN

`confidence` is sourced at `lgbm.py:672-673` — `directional_conf = max(float(proba[0]), float(proba[2]))`, where `proba` (line 665) is the inner-ensemble mean of `predict_proba`. This is the SAME `proba` vector that already drives `direction` (line 723) and the existing `_confidence_threshold` gate (line 678) — threading it into the `Signal` introduces ZERO new data dependency. The /079 Critic already verified `proba` is past-only (embargoed walk-forward, models trained strictly on the past window). Threading an already-look-ahead-clean value `Signal → Order → TradeResult → trades.csv` cannot create look-ahead. The `confidence_distribution.csv` writer (`iteration_report.py:144-285`) is pure post-backtest report emission — no model retraining, no OOS quantity, no decision path; it only WRITES. **Deliverable verified sound:** the `confidence` column in `reports-v3/iteration_v3-080/in_sample/trades.csv` and `out_of_sample/trades.csv` is populated on all 262 IS+OOS trades with real varied floats (IS [0.5429, 0.9943]; OOS [0.6257, 0.9981]; correctly populated even on `weight_factor=0.0000` BTC-killed rows); `confidence_distribution.csv` is non-degenerate (2016 data rows, genuine per-symbol/per-IS-month histograms over the a-priori `conf_bin_edges` grid, per-symbol last-month Optuna threshold overlaid). The distribution carries usable signal for a cycle-3 conviction-derate re-attempt — it empirically shows realized Optuna thresholds at 0.5859/0.6507/0.6893 with surviving trades clustered at/above, confirming the /079 root cause. /080 discharges the /079 Critic Rec #1 methodology debt.

## Per-Check Status

### Check 1 — Look-Ahead Audit: PASS
Traced the `confidence` threading end-to-end (Adjudication #3). `confidence` is `max(proba[0], proba[2])` from the past-only inner-ensemble mean `predict_proba` (`lgbm.py:672`) — the identical `proba` vector already driving `direction` and the `_confidence_threshold` gate; zero new data dependency. The fix `d71adf6` only forwards this value through the wrappers. `_write_confidence_distribution` is post-backtest report emission and cannot introduce look-ahead. Foundation Audit confirms `walk_forward.py:113` carries the embargo fix `train_end_ms = test_start_ms - embargo_ms` — the lookahead bug is FIXED in this worktree. No look-ahead in any audited path.

### Check 2 — Embargo Width: PASS
`timeout_minutes = 10080`, `candle_minutes = 480` → `timeout_candles = 21`. Required gap = `(21 + 1) × 3 symbols = 66`. `run_baseline_v3.py:914-917` asserts `(timeout_candles + 1) × len(V3_MODELS) == REQUIRED_GAP`; `_verify_label_leakage_gap()` is called at line 2391; `compute_embargo_candles(10080, 480) = 22` candles applied symmetrically at the train/test boundary. Actual gap = required gap = 66. A PASSIVE-DIAGNOSTIC adds/removes no trades and changes no label horizon — the leakage surface is identical to /060.

### Check 3 — Multiple-Testing Correction: PASS-equivalent (informational for EXPLORATION)
`dsr.json`: PBO = 0.1278 (< 0.40 threshold) PASS — bit-identical to the /060/077/079 anchor, the clean null signature of a bit-identical roster (the CPCV path-return proxy is weight-factor-independent). `frac_positive_paths = 0.6444` clears the 0.55 Gate 10-CPCV threshold. DSR = 0.0 and `dsr_relative_b4 = 0.4963` are INFORMATIONAL at EXPLORATION mode per `feedback_v3_dsr_mode_artifact.md` (n_trials=315 is not comparable to CONFIRMATION-mode n_trials=1050) and do NOT trigger a BLOCK for an EXPLORATION iteration. PSR = 1.0. PBO — the one Check-3 axis meaningful at any mode — passes.

### Check 4 — IC Correlation: PASS
No NEW feature family was added in /080 — the 14-feature stack is byte-identical to /060 (`confidence` is a post-model passive metadata scalar, not a feature; it never enters the feature matrix). The new-vs-existing IC gate is therefore not triggered. The 14×14 `ic_matrix.csv` is present; its highest off-diagonal (`regime_momentum_signed_5d` × `vwap_dev_20` = 0.7642) is a pre-existing carry-over of the /028-validated composed feature — a Category-2 composed-feature IC mechanically tied to its primitive per `feedback_v3_engineered_feature_pivot.md`, not a /080 introduction.

### Check 5 — ADF Stationarity: PASS
No NEW price-derived feature was added — the new-feature ADF gate is not triggered. `adf_test.csv` is present (2198 rows, within the expected [1302, 2646] band); 82.0% of per-month cells stationary. The non-stationary cells concentrate in thin early-history warm-up months (e.g. 2020-01 BCH rows with empty `adf_statistic` — the documented insufficient-data artifact).

### Check 6 — Pareto Dominance: PASS-equivalent
`pareto_front.csv` is correctly ABSENT — Gate 10-Pareto is RETIRED under the unified 10-seed architecture and replaced by Gate 10-CPCV (`frac_positive_paths ≥ 0.55`), which passes at 0.6444. The engineering report's prose reference to `pareto_front.csv` is a stale template artifact; the file's absence is the correct architectural state, not a missing output.

### Check 7 — Reproducibility: PASS
Full commit chain stamped and consistent (EDA `0029155` → brief `6df3d04` → backfill `ea0339e` → setup `dec440e` → gate `3d8afd7` → implementation `8e7e62b` → confidence-wiring fix `d71adf6`). `ensemble_summary.json` carries the literal seeds `[191664963, 1662057957, 1405681631]` (outer=42 lineage, EXPLORATION ensemble_size=3). `feature_columns` is an explicit 14-element list — `LightGbmStrategy.__init__` raises on empty; no auto-discovery. Spot-checked OOS trade PnL: row 2 `BCHUSDT direction=-1`, `(303.870000 - 282.100779) / 303.870000 × 100 = 7.164%` matches `pnl_pct`; `net_pnl_pct = 7.064` after 0.1% fee; `weighted_pnl = 7.064 × 0.33 = 2.331` matches. `run.log` has zero WARNING/ERROR/Traceback lines.

### Check 8 — Hypothesis-Implementation Alignment: PASS
The brief Section 3 declares exactly TWO changes; `git`-touched `src/` files audited match exactly. (1) PRIMARY AXIS — `confidence: float | None = None` trailing optional field on `Signal`/`Order`/`TradeResult` (`backtest_models.py`, no positional shift), forwarded by `make_order`/`make_result` (`backtest.py`), written by `_write_trades_csv` (`iteration_report.py`), plus the new `_write_confidence_distribution` function — and the two wrapper forwards (`risk_v2.py:372`, `risk_v3.py:410`) added by the fix `d71adf6`. (2) BASELINE-RESTORE — `lgbm.py:754` is the literal `weight = 100`; the `conviction_derate` helper remains as dead code (unreferenced, harmless); the runner no longer imports it. NO feature, labeling, risk-gate, model-architecture, universe, seed, or Optuna change. `ITERATION_LABEL = "v3-080"`, `V3_MODELS` = BCH/LDO/TRX, `OOS_CUTOFF_DATE`/`TRAINING_MONTHS` immutable, `efficiency_ratio_50`/`range_efficiency_50` ban intact, no cross-track import, no `start_time` manipulation. No scope creep. The bit-identical roster is the implementation's own proof that the passive field altered no decision path.

## Recommendations to QR

For the cycle-2 CONFIRMATION (iter-v3/081) and cycle 3. /080's verdict is final; a PASSIVE-DIAGNOSTIC does not advance.

1. **The /081 CONFIRMATION is, on current evidence, a /059-baseline multi-seed re-validation.** Cycle 2 produced ZERO clean PROMISING across 10 EXPLORATIONs (4 SUSPICIOUS-OOS-DOMINANT, 1 NEGATIVE, 3 INERT, 2 PASSIVE-DIAGNOSTIC NULL/INERT, 1 conviction-derate NULL). There is no edge ingredient to bundle. The /081 brief should pre-register the re-validation branch explicitly and carry the standing unresolved constraints (LDO directional weakness; BCH ~77% IS-PnL concentration fragility; the bull-month entry-discrimination drag) into cycle 3.

2. **A cycle-3 conviction-derate re-attempt now has its instrument — but place `C_REF` against the per-trade `confidence` column, not the `confidence_distribution.csv` last-month overlay.** The `realized_optuna_conf_threshold` column is a single-point last-month map (the lazy-monthly-training degeneracy the brief Section 7 anticipated). The load-bearing deliverable is the per-trade `confidence` column on `trades.csv`; a cycle-3 EDA should build its own per-symbol/per-month histogram directly from that column. The empirical finding (realized thresholds 0.5859/0.6507/0.6893; the de-rate window must sit BELOW the per-symbol pile-up to engage ≥15% of trades) is the placement constraint.

3. **For any cycle-3 conviction-derate re-attempt, also confirm a populated de-rate band actually exists before committing the axis.** The /079 structural caveat stands: if Optuna's IS-optimized thresholds intrinsically sit high for the 8h directional-classification problem, the surviving signals are nearly all high-conviction by construction and a conviction-derate has structurally little to act on regardless of constant choice. The cycle-3 brief Section 2 must demonstrate, from /080's persisted column, that a materially-populated below-threshold band exists — otherwise the axis re-saturates.
