# Team 08 exact center sign inversion

This falsification control runs the complete frozen 42-event/80% regime-side center internally,
then negates every submitted target weight. A skipped rebalance stays skipped and an explicit flat
target stays flat.

Signal formation, state classification, selection, cohorts, exposure limits, and the symmetric
20% volatility overlay are identical to the nominee. Only the final target sign changes.
