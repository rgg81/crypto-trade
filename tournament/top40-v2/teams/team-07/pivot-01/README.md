# Team 07 pivot 01 — Market-State and Relative-Opportunity Ensemble

This directory records the prospective mechanism-pivot decision. The executable pivot is promoted
at canonical Team 07 root paths because tournament execution imports only root
`strategy.py:build_strategy` and root `risk_policy.json`.

The immutable parent no-control result consumed material trial 1 and is terminal. This pivot would
use material trial 2 only after serialized validation, independent semantic review, `pivot-team`
family registration, canonical A5 control-file first-adds, exact source hashing, and A7 package
rebind.

These are drafts, not journal entries, registrations, reviews, score evidence, or performance
evidence:

- `pivot-registration.template.json` records parent evidence and the authorization boundary;
- `family-registration.template.json` is the prospective new-family input;
- `trial-registration.template.json` is the prospective no-control material-trial input; and
- `control-activation.template.json` keeps every risk overlay dormant pending a broad core pass.

The root no-control risk policy remains unchanged. Neither the positive parent stress slice nor a
future risk overlay can rescue the failed shock-diffusion mechanism.
