# Team 07 second-pivot registration drafts

The root family/trial JSON files and their copies under `pivot-02/` are drafts, not registrations.
Their epoch timestamps and all-zero hashes are sentinels. Materialize them only from final reviewed
bytes, after canonical A5 controls exist and before using the active Amendment 0007 entrypoint.
`pivot-01/` remains historical first-pivot authoring material and must not be reused.

The organizer must register `t07-two-tape-rank-durability-v1` with the A7 `pivot-team` command,
parent `t07-market-state-relative-ensemble-v1`, and prospective material trial number 3. Both parent
results remain immutable; these drafts do not edit or reinterpret them.

Before trial registration:

- require root `risk_policy.json` to be byte-identical to `risk_policies/no-control.json`;
- materialize and freeze the new executable-source manifest;
- obtain an independent semantic-coupling approval;
- add and hash the canonical 168-hour score manifest;
- fill the score adapter hash in the trial parameters;
- fill exact strategy, post-family source-bundle, risk, config, and timestamp values;
- keep the materialized report input in the bare 12-field A5-compatible form, without top-level
  `schema_version` or `event_type`; and
- confirm the completed Team 07 A7 identity rebind names the reviewed family/candidate.

`pivot-02/control-activation.template.json` deliberately contains no authorization. It may become
evidence only after the completed no-control second-pivot result makes every listed gate true.
Controls cannot rescue either terminal parent or a failed second-pivot core.
