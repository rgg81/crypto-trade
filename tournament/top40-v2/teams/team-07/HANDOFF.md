# Team 07 pivot handoff

The shock-diffusion family is terminal after its completed no-control failure. Do not register a
control or neighbor under that family. The canonical root package now declares
`t07-market-state-relative-ensemble-v1-base`, a genuine mechanism pivot and prospective material
trial 2. Organizer formatting, lint, JSON validation, 15 strategy tests, 6 no-control risk tests,
independent A5 semantic review, canonical fold correction, and the A7 identity rebind passed. The
family and trial remain unregistered and unevaluated.

Organizer sequence:

1. inspect the static source and confirm only Team 07 prospective files changed;
2. run formatting, lint, focused strategy tests, risk-policy tests, JSON validation, source
   fingerprinting, and clean-sandbox checks serially;
3. create the exact executable-source manifest and obtain an independent A5 semantic-coupling
   review before adding the canonical score manifest;
4. register the new family through the A7 `pivot-team` path with parent `shock-diffusion-v1`;
5. materialize candidate/source/config/risk/score hashes and confirm the completed Team 07 A7
   authority rebind still names the new identities;
6. register material trial 2 before running the visible-development window; and
7. read the no-control result against every noncompensatory gate before considering anything else.

The lifecycle imports only root `strategy.py` and root `risk_policy.json`. Root risk bytes must
remain identical to `risk_policies/no-control.json` for this reference. No risk-control template
may be copied to root unless the exact pivot reference passes every broad activation minimum.
