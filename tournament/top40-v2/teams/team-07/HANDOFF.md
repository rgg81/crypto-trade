# Team 07 second-pivot handoff

The `t07-market-state-relative-ensemble-v1` no-control reference is terminal: net Sharpe +0.0153,
annualized return -0.97%, doubled-cost Sharpe -0.4928, bear Sharpe -0.7394, chop Sharpe -0.6013,
50% positive quarters, and 15,763 trades. Do not register a control, neighbor, or ablation under it.

The canonical root package now declares `t07-two-tape-rank-durability-v1-base`, a genuine second
mechanism pivot and prospective material trial 3. This authoring pass did not validate, register,
bind, or evaluate it.

After author handoff, organizer formatting, lint, JSON validation, 15 strategy tests, 6 no-control
risk tests, risk/source preflight, fail-closed duplicate hardening, independent A5 review,
governance-gate correction, and the A7 identity rebind passed. The family and trial remain
unregistered and unevaluated.

Organizer sequence:

1. inspect the static source and confirm only Team 07 prospective files changed;
2. run formatting, lint, focused strategy/risk tests, JSON validation, source fingerprinting, and
   clean-sandbox checks serially;
3. materialize the new executable-source manifest and obtain an independent A5 semantic-coupling
   review before adding the canonical score manifest;
4. confirm the completed Team 07 A7 authority rebind still names the new family/candidate;
5. register the family through the active A7 `pivot-team` path with parent
   `t07-market-state-relative-ensemble-v1`, then recompute the post-family source identity;
6. replace every timestamp/hash sentinel and register material trial 3 before visible-development
   execution; and
7. read the exact no-control result against every noncompensatory gate before considering anything
   else.

The lifecycle imports only root `strategy.py` and root `risk_policy.json`. Root risk bytes must
remain identical to `risk_policies/no-control.json` for this reference. No risk-control template may
be copied to root unless the exact second-pivot reference passes every broad activation minimum.
