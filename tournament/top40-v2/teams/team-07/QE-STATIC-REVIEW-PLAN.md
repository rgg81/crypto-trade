# Team 07 pivot 02 QE and static review plan

No validation or review is claimed by this static authoring pass. Before second-family or trial
registration, the organizer should complete this freeze sequence serially:

1. verify every changed byte is under Team 07 and confirm families/experiments journals, reports,
   historical results, risk-policy bytes, and Amendment 0007 files were not edited;
2. compare every executable constant with `frozen_config.json` and the active trial draft;
3. prove the exact 127-close history, point-in-time membership, per-bar rank, and up/down tape rules
   are causal and fail closed;
4. confirm funding, future bars, current executable open, high/low, volume, auxiliary fields,
   files, network, subprocess, positions, PnL, evaluator state, private data, and OOS data cannot
   affect targets;
5. verify cross-tape smaller-magnitude scoring, disagreement shrinkage, three-block median,
   agreement/dispersion shrinkage, final rank, A5 call, selection, weekly schedule, gross/net/name
   caps, and active sleeves;
6. prove the public score boundary is called exactly once on scheduled construction, after the
   final durability transform and before selection, sizing, caps, or organizer risk;
7. run formatting/lint, focused strategy/risk tests, JSON parsing, and source fingerprinting
   serially;
8. run the official clean sandbox twice and require identical target and artifact hashes;
9. materialize and validate family, executable-source, semantic-review, score, and trial controls;
10. rebind the A7 identity to the reviewed second pivot, validate it, then use A7 `pivot-team` with
    parent `t07-market-state-relative-ensemble-v1` before preregistering material trial 3; and
11. require positive ordinary/doubled-cost, bull/bear/chop, role, fold, quarter, sleeve,
    concentration, A5 coverage, and IC evidence before any risk overlay or qualification step.

The top-level `risk_policy.json` must remain byte-identical to `risk_policies/no-control.json` for
the second-pivot reference. A failed core cannot be rescued by volatility targeting, stops, brakes,
or turnover controls.
