# Team 04 BER pivot provenance

BER is Team 04's first documented mechanism pivot after the exact UTC reference was stopped. The
only outcome knowledge used to motivate the pivot is Team 04's own visible-development review:
UTC had two flat early quarters, only seven positive quarters, concentrated fold performance, a
negative whole-window short sleeve before costs, and no persisted score suitable for its required
IC. No private/final observation, other V2 team, V1 result, ballot, or leaderboard was inspected.

The pivot changes the causal economic mechanism from multi-week continuation plus funding carry to
short-lived inventory-exhaustion reversal. The 21-day baseline, three-day shock, fixed coherence
multiplier, two-day schedule, 24-symbol minimum, broad 30-percent sleeves, 0.48 gross, and 0.03 cap
were specified before observing any BER target, trade, return, score IC, or symbol outcome.

The initial package included a team-owned score serializer designed with the strategy. After the
organizer selected the A5 identity-hook architecture—but before registration, testing, or any BER
evaluation—that serializer was explicitly demoted to a noncanonical audit-only artifact. It is not
promoted and is never an official score source.

The canonical design imports organizer-owned
`crypto_trade.tournament.score_adapter_protocol_v5.score_boundary` and calls it on the exact ranked
dictionary after transformation and before selection. Amendment 0005 is now active through
`scripts/top40_v2_tournament_score_diagnostics_v5.py` (SHA-256
`0dc9228f3b9c6fe41b2655055f766fc92f323a289a050e6bdf4e48a30b0105f4`) and integration-freeze
commit/SHA-256 `d2b95f610722aab65b4e67466b34efeaa3554101` /
`b3b2b96245a479ccfff95b5cd5b5cd0aef3b2d0aac0fc9faf5367d3e6772958c`. This rebind changes no
BER mechanism or parameter.

The active dispatcher retains Amendment 0006 as the universe authority. The A6 integration-freeze
commit/SHA-256 is `ed3af1398543dfee4a50150915dc2e37b3631fc9` /
`3e93bdfe031e2589888c3bbcaae583437bbd074fa9d86c6dc0a54187bc0f1e34`, and the exact passing
pure-crypto report SHA-256 is
`b9c55b40fef331861af068272159f45860870182a58c93652eff2a819b3d5d1b`. BER may therefore see only
the certified native-crypto candidate set, excluding stablecoins and direct TradFi, index, equity,
metal, and commodity exposures.

Canonical promotion and serialized synthetic/risk validation completed on 2026-07-16 without
market-data access. Registration remains blocked on complete executable-source hashes, a later
independent semantic-coupling review, and a hash-bound score manifest and opt-in. The schema-shaped files in this directory contain zero digests and event-time epoch
placeholders and are neither authority nor evidence. The one deliberate exception is the score
manifest's `1970-01-01T00:00:00Z` 48-hour schedule anchor: it is the actual immutable authority and
must never be replaced. Only materialized `registered_at_utc`, trial `timestamp_utc`, and review
`reviewed_at_utc` event fields receive current organizer times.

Ordinary lifecycle execution hardcodes Team 04's top-level `strategy.py` and `risk_policy.json`.
The pivot sources were promoted byte-for-byte before review and registration; the nested pivot is
not an alternate lifecycle entrypoint. The promoted test is layout-aware and passed from its
canonical top-level path while using pivot-only templates as support.

BER family registration is a distinct mandatory `pivot-team team-04` event. It must occur after
the canonical A5 controls are prepared but before the final source-tree fingerprint and trial
registration, because the appended `families.jsonl` row is part of the complete candidate tree.
Family and trial inputs are materialized outside that tree so replacing their event/hash sentinels
does not create a self-changing source fingerprint.

No tournament script, test, evaluator, lifecycle command, or network request was run while creating
the pivot or performing the authority rebind. Later organizer validation ran only lint/format,
synthetic tests, source preflight, and risk-policy validation; it accessed no market data and
consumed no material trial. `validation_commands.json` and `test_evidence.json` record that pass.

The stopped UTC reference already consumed one material configuration. BER remains unregistered;
its future registration would consume Team 04 material trial 2. Organizer-owned family and
experiment ledgers were not edited by this rebind.
