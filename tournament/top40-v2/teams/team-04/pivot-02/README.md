# Team 04 pivot 02: PARD organizer handoff

This directory is an uncommitted, statically promoted package for the new family
`team-04-path-adaptive-relative-dynamics-v1` and exact no-control candidate
`team-04-pard-reference-001`.

The stopped UTC and BER families are historical evidence. Do not rewrite their source, family
records, trial records, results, score artifacts, or organizer ledgers. BER's completed visible-IS
run had 3,732 decisions but zero trades and exactly zero return statistics. Its family falsifier
therefore terminated BER before controls or neighbors.

PARD's complete source package remains isolated here and is copied byte-for-byte to Team 04's
canonical mutable root files. It is not validated, registered, or executable tournament authority
yet. The organizer must perform all of these steps serially:

1. statically review `failure-diagnosis.md`, the mechanism, and every synthetic test;
2. lint and run the pivot-local tests without changing historical artifacts;
3. validate the materialized disabled `risk_policy.json` in this directory and its byte-identical
   canonical root copy;
4. verify `strategy.py`, `frozen_config.json`, `risk_policy.json`, and `test_strategy.py` are
   byte-identical to Team 04's canonical root, then run the pivot-local and root suites;
5. update organizer-owned active-pivot promotion state and replace Team 04's candidate-specific
   `a7_execution_authority.json` through the governed A7 rebind process for the new family and
   candidate; the BER state and A7 binding must not be reused as PARD authority;
6. materialize the executable-source manifest, obtain an independent semantic-coupling review,
   and materialize the score manifest and exact trial opt-in in A5's required first-add order;
7. materialize the event timestamp in `family-registration-input.json` and register the new family
   as the second and final pivot from the stopped BER family before computing the final complete
   source-tree hash;
8. materialize and validate the trial registration with post-pivot hashes; and
9. use only `scripts/top40_v2_tournament_runtime_preload_v7.py` for registration and execution.

The exact no-control center must clear every numeric core and A5 gate before any neighbor,
ablation, or risk arm is observed.

Registration or execution before the governed A7 rebind is forbidden. All epoch event timestamps
and zero hashes in templates are explicit placeholders. The score schedule's epoch anchor is the
sole exception: it is the actual immutable 48-hour schedule anchor.
