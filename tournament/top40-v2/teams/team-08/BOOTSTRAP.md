# team-08 · Top-40 V2

Status: `ACTIVE_A7_A6_A5_READY_PROSPECTIVE_UNREGISTERED`

Amendment 0006 remains the frozen pure-crypto universe authority. Every future lifecycle command
must use the active Amendment 0007 runtime-preload entrypoint, which preserves A5 diagnostics and
delegates through that exact A6 preflight. This family package is prospective only: no family or
trial is registered, no evaluation has run, and no performance is claimed.

The first material candidate must use the no-control bytes at root `risk_policy.json`. Controls are
dormant until that core passes the complete official development gate and every stricter declared
gate; they cannot rescue a failed or subthreshold core.

Negative research is evidence, not a submission. Work only inside this V2 namespace and the
visible-development data boundary.
