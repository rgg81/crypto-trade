# Team 05 final LDM pivot-02 bootstrap

The active candidate is `team05-ldm-pivot02-core-v1` in parent-linked family
`team05-liquidity-depth-migration-v1`. It is prospective and must not run until the strict A5/A7
registration sequence in `trial_plan.json` completes.

Read `pivot-02/PIVOT.md`, `research_brief.md`, `frozen_config.json`,
`qualification_thresholds.json`, and `SCORE-ADAPTER-CONTRACT.md`. Validate the six operative files,
the byte-identical no-control policy, invalid-observation handling, A6 membership, and source-bundle
sandbox serially.

Do not modify `families.jsonl` or `experiments.jsonl` by hand. Do not reuse CRTR or UDCC score
manifests, controls, neighbors, or Amendment 0008. Use only the pinned Amendment 0007 runtime for
lifecycle commands and the eventual official development run. Failure of this final core makes Team
05 DNF.
