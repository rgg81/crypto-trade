# Team 05 UDCC pivot-01 bootstrap

The active candidate is `team05-udcc-pivot01-core-v1` in parent-linked family
`team05-up-down-capture-convexity-v1`. It is prospective and must not run until the strict A5/A7
registration sequence in `trial_plan.json` completes.

Read `pivot-01/PIVOT.md`, `research_brief.md`, `frozen_config.json`,
`qualification_thresholds.json`, and `SCORE-ADAPTER-CONTRACT.md`. Validate the six operative files,
the byte-identical no-control policy, A6 membership handling, and source-bundle sandbox serially.

Do not modify `families.jsonl` or `experiments.jsonl` by hand. Do not reuse CRTR score manifests,
controls, neighbors, or Amendment 0008. Use only the pinned Amendment 0007 runtime for lifecycle
commands and the eventual official development run.
