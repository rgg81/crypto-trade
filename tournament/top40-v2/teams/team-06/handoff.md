# Team 06 second-pivot handoff

Status: source and prospective registration drafts implemented; no test, Python, validator,
registration, diagnostic, evaluator, lifecycle, or Git command was run by the pivot author.

Organizer progress after author handoff: formatting, lint, JSON validation, 14 focused tests,
risk/source preflight, independent A5 review, governance-gate correction, and the A7 identity
rebind passed. Family registration, canonical A5 first-adds, final hashes, trial registration, and
development evaluation remain pending.

## Outcome

The direct parent `t06-relative-rank-acceleration-v1-base` is preserved as immutable completed
negative evidence and consumed Team 06 material trial 2. Its net Sharpe was
`-1.587344094268312`, annualized return `-0.1808286621124474`, doubled-cost Sharpe
`-2.533098193879541`, maximum drawdown `0.4948179135020112`, positive-quarter fraction
`0.21428571428571427`, and bear/bull/chop/stress Sharpe respectively
`-2.7698181411008513`, `-2.3350736294964585`, `-0.013447000471358682`, and
`-0.45042125791257903`. The run made 16,761 trades. No parent control, neighbor, diagnostic, or
private ticket activates.

The canonical root executable now drafts new family `t06-relative-rank-persistence-v1`, candidate
`t06-relative-rank-persistence-v1-base`. It is a mechanism pivot, not a risk variation:

- positive relative-rank displacement replaces the falsified reversal direction;
- current persistent relative leadership contributes independently, so this is not a sign flip;
- 12-bar prior/recent windows and a 72-bar volatility baseline replace 6/6/63;
- the fixed schedule changes from 48 to 96 hours to address the parent's cost sensitivity;
- one-fifth broad tails, exact zero net, 0.40 gross, and 0.025 caps replace the parent construction;
- every evaluator control remains disabled in root `risk_policy.json`.

Detailed causal equations, universe contract, intended regime roles, and the noncompensatory
falsifier are in `research_brief.md`. `pivot-02/` contains drafts only. Historical ledgers,
results, pivot-01 files, canonical A5 controls, root risk policy, and A7 binding were not touched.

## Required serialized organizer sequence

1. Inspect the exact Team 06 diff and verify that no journal, report, result, run state, historical
   A5 control, root risk policy, or A7 authority file changed.
2. Run formatting/lint, canonical JSON validation, the Team 06 synthetic suite, organizer risk
   validation, source preflight, and clean import/target smoke checks serially. Fix source only if
   evidence requires it; do not register a failing package.
3. Confirm the change is a new mechanism family rather than a neighbor: direction, level feature,
   windows, horizon, and construction all differ prospectively from the terminal parent.
4. Materialize the new family input outside the Team 06 source tree from
   `pivot-02/family-registration.template.json`, replacing only its event timestamp, and run
   `pivot-team team-06` through the active A7 dispatcher. The family append must precede final
   source fingerprinting.
5. Materialize a new executable-source manifest from the persistence template at its exact new
   canonical path, obtain and first-add an independent semantic review, then hash that review and
   first-add the 96-hour score manifest. Never alter a historical parent A5 file.
6. Confirm the completed `a7_execution_authority.json` rebind still names the exact persistence
   family/candidate. After the family and three A5 controls are immutable, recompute source bundle,
   strategy, config, and no-control risk hashes; no candidate-tree byte may change afterward.
7. Materialize the trial input outside the source tree from
   `pivot-02/trial-registration.template.json`, include the exact score-manifest opt-in SHA, and
   register it as material trial 3. Templates and placeholders are never executable inputs.
8. Run the visible-development no-control reference once; the same run emits 1x and 2x cost views.
   If any PnL, regime, role, fold, quarter, sleeve, or concentration gate fails, terminate the
   family without running a non-decision-relevant diagnostic.
9. Only after a positive material core, reserve and run the non-material A5 score diagnostic and
   apply its coverage and IC gates. No stop, volatility target, drawdown brake, turnover cap,
   neighbor, or private ticket can compensate for a failure.

The active authority is A7 entrypoint SHA-256
`8a4ada10176d2606df3f358fc188e21b45153ad9a7270f36908736f03322a3a9`, integration-freeze
commit `c8b917ca49306d5200a4e08848e73ff6f5a18bf3`, freeze SHA-256
`6f77a146e7b414eabd20c5cc9321a95493bb98116dde07ef79d9eb009b6a6f51`. Its delegated A6
pure-crypto report SHA-256 is
`b9c55b40fef331861af068272159f45860870182a58c93652eff2a819b3d5d1b` with zero violations.
