# Team 06 second-pivot provenance

## Evidence boundary

This pivot was designed after the direct parent's completed visible-development no-control result
was public to Team 06. Only the recorded aggregate, doubled-cost, drawdown, quarter, trade-count,
and regime statistics were used to falsify `t06-relative-rank-acceleration-v1` and define the new
mechanism's objectives. No private qualifier data, final OOS data, later diagnostic, or other
team's performance evidence was inspected.

No Python, test, validator, evaluator, tournament lifecycle, or Git command was run by the pivot
author. No journal, report, result, run state, risk policy, historical A5 file, or A7 authority file
was edited. New registration, review, and hash-shaped files are explicit templates and cannot
serve as evidence.

## Mechanism boundary

- Runtime dependencies: Python standard library and pandas already present in the neutral worker.
- Canonical entrypoint: root `strategy.py:build_strategy`, seed `20260801`.
- Candidate identity: exact root `candidate_variant.py`; unknown identities and all overrides fail.
- Inputs: decision time, organizer-certified pure-crypto `eligible_symbols`, and canonical
  RangeIndex `open_time` / `close` bars only.
- History: one finite positive completed close at all 97 required opens; missing or duplicate rows
  fail that coin closed.
- Transform: per-bar cross-sectional centered ranks, fixed past-only rank-shift and recent-rank
  level persistence, baseline rank volatility, fixed displacement coherence, and a final centered
  cross-sectional rank.
- State and randomness: none. No filesystem, network, subprocess, environment, report, result,
  snapshot, label, position, equity, drawdown, fill, cost, funding, or auxiliary read occurs.
- Score capture: one direct organizer-owned identity call after the final score transform and
  before selection, sizing, caps, or risk at each scheduled decision; scheduled failure captures
  one empty built-in dictionary.
- Portfolio: broad equal-dollar long/short sleeves, exact zero net, at most 0.40 gross and 0.025 per
  coin, every 96 hours with holds between.
- Risk: root `risk_policy.json` is unchanged and completely disabled. The pivot is not a control.

Amendment 0006 remains the universe authority. Only native crypto coins/tokens may reach the
strategy; stablecoins, TradFi/equities, commodities/metals, and indexes are excluded by the
organizer even when listed as Binance perpetuals.

## Evidence still required

After author handoff, organizer formatting/lint, JSON validation, 14 focused tests, risk/source
preflight, independent semantic review, and the A7 identity rebind passed. The organizer must still
register the new family, materialize three new candidate-specific A5 controls, compute exact
post-family source hashes, register material trial 3, and run the visible-development no-control
reference. Only a core that passes the complete official development gate warrants the
non-material A5 diagnostic. Two clean official workers must ultimately reproduce exact targets and
artifact hashes before any private ticket can be considered.
