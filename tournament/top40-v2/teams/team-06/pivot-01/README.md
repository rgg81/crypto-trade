# Team 06 pivot 01 — Relative-Rank Acceleration Reversal

This directory records the mechanism-pivot decision and prospective registration material. The
executable pivot is promoted at the canonical Team 06 root paths because ordinary tournament
execution imports only root `strategy.py:build_strategy` and root `risk_policy.json`.

The parent no-control result is immutable negative evidence. It consumed material trial 1 and is
terminal. This pivot will use material trial 2 only after family registration, A5 control-file
first-adds, exact source hashing, and trial registration. The organizer completed serialized
validation, independent semantic review, and the A7 package rebind after this draft was authored.

Files in this directory are drafts, not journal records, registrations, reviews, score evidence,
or performance evidence:

- `pivot-registration.template.json` documents the exact parent evidence and authorization
  boundary for the new family.
- `family-registration.template.json` is the prospective `pivot-team` input; its event timestamp
  is deliberately unmaterialized.
- `trial-registration.template.json` is the prospective no-control trial input; every hash and
  timestamp sentinel must be filled only after the new family and canonical A5 controls exist.

The mechanism specification and falsifier are in root `research_brief.md`; executable constants
are in root `frozen_config.json`. The retained root risk policy has every control disabled. No
controlled result may repair a failed pivot reference.
