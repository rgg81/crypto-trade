# Team 06 pivot 02 — Relative-Rank Persistence

This directory records the second mechanism-pivot decision and prospective registration material.
The executable draft is promoted at the canonical Team 06 root paths because ordinary tournament
execution imports root `strategy.py:build_strategy` and root `risk_policy.json`.

The direct parent `t06-relative-rank-acceleration-v1-base` is immutable negative evidence. It
consumed material trial 2 and is terminal. This pivot would use material trial 3 only after
family registration, new A5 control-file first-adds, exact source hashing, and trial registration.
Organizer serial validation, independent review, and A7 package rebinding are complete.

Files here are drafts, not journal records, registrations, reviews, score evidence, or performance
evidence:

- `pivot-registration.template.json` documents the exact direct-parent evidence and authorization
  boundary.
- `family-registration.template.json` is the prospective `pivot-team` input; its timestamp is
  deliberately unmaterialized.
- `trial-registration.template.json` is the prospective no-control trial input; hash and timestamp
  sentinels must be filled only after the family and exact A5 controls exist.

Historical pivot-01 and canonical score-control files are untouched. The root risk policy still
has every control disabled. No controlled, neighboring, diagnostic, or private result may repair a
failed no-control reference.
