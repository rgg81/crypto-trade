# Team 03 pivot provenance

The confirmed residual-shock-absorption family was developed inside Team 03's Top-40 V2
visible-development boundary. No other V2 team, V1 team/result, private qualifier, final OOS,
ballot, leaderboard, or external performance data was inspected or used.

## Parent evidence and pivot origin

The only result-bearing input was Team 03's own terminal journal record for
`t03-rlsa-base-h3-b30-v21-f25`: portfolio insolvency at `2022-05-13 00:00:00+00:00`, with no
promoted metric or artifact payload. The old preregistration had already named falling-knife and
squeeze continuation as a stress risk. The pivot therefore asks a new causal question—whether a
closed residual sign reversal must evidence absorption before entry—without attributing the old
failure to a hidden sleeve, instrument, event, or metric.

The old family is stopped. Its registered hashes and terminal result remain in the organizer-owned
journal projection and are not reused as performance evidence.

## Authorized inputs and implementation

The child strategy uses only protocol-supplied fully closed 8-hour `open_time`, `close`, and
`quote_volume` fields; eligible-symbol membership; and funding rows whose `funding_time` is
strictly before the decision. BTCUSDT is an untraded factor anchor. The executable implementation
is `../strategy.py`, its deterministic synthetic specifications are in `../test_strategy.py`, and
the unchanged no-control policy is `../risk_policy.json`.

Runtime numerical dependencies are NumPy, pandas, and the local tournament `DecisionContext`
protocol. The strategy has no network access, learned external artifact, random selection,
timestamp-to-target table, current fill input, future row, private data, or P&L-dependent logic.

## Evidence state

No test, lint, policy validation, family registration, trial registration, development evaluation,
neighbor, or risk-control run has been performed for the child revision while preparing these
files. `test_evidence.json` is therefore a placeholder record, not pass evidence.

After the declared validation commands pass, the organizer must register the child family with its
parent ID. The family journal change participates in the source bundle, so trial hashes must be
serialized only afterward and only when no further source edit is planned.
