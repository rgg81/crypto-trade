# Team 03 pivot 01

Status: implemented; validation and registration pending.

This namespace contains the preregistration package for child family
`t03-confirmed-residual-shock-absorption-v1`. Its parent
`t03-residual-liquidity-shock-absorption-v1` is stopped and falsified by insolvency.

The tournament executable remains the required Team 03 entrypoint at `../strategy.py`, with
synthetic specifications at `../test_strategy.py`, active candidate configuration at
`../frozen_config.json`, and the unchanged no-control policy at `../risk_policy.json`. Nothing in
this namespace is result evidence until the serialized checks in `RUNBOOK.md` are run and recorded.
