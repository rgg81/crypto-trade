# Team09 pivot-02 independent static-review plan

This is a prospective checklist, not a completed review or run authorization.

1. Confirm changes are confined to `tournament/top40-v2/teams/team-09`; four prior experiment
   rows, two family rows, both pivot reservations/results, and every prior score artifact are
   unchanged. Confirm no pivot-02 ledger row exists and bootstrap status is
   `PIVOT_02_A7_A6_PROSPECTIVE_UNREGISTERED`.
2. Pin every future command to the A7 entrypoint and exact delegated A5/A6 identities in
   `a7_execution_authority.json` and `a5_score_lineage.json`. Require Amendment 0006's canonical
   report to have zero violations. Its point-in-time eligible set is the sole universe authority;
   no ticker heuristic or perpetual-listing shortcut is allowed.
3. Match every `StrategyConfig` field/value to `frozen_config.json` and the trial template. Verify
   all family ranges and ten one-axis neighbors are feasible. Confirm family/candidate identity is
   `team09-defensive-residual-persistence-v1` / `team09-drp-pivot02-v1`.
4. Trace price inputs to contiguous completed eight-hour closes. Check exact epoch-grid and
   72-hour rebalance arithmetic, latest-close freshness, duplicate/future rejection, 270-return
   persistence history, 90-return fragility window, under-history exclusion, and no imputation.
5. Recompute per-bar median residuals, three nonoverlapping block ranks, agreement factor,
   downside semideviation, maximum absolute return, drawdown, `0.40/0.35/0.25` fragility
   composite, percentile exclusion, and deterministic tie behavior.
6. Prove the highest fragility quintile is encoded as zero before A5 for both potential sleeves.
   Prove remaining score magnitude incorporates fragility shrink and no later code reads raw
   fragility. Confirm one invariant rule is used in all regimes and no funding, route, sign flip,
   portfolio equity, drawdown, positions, fills, or risk state enters the strategy.
7. Verify the exact one-positional `strategy.score_boundary` call occurs after final score
   transformation and before selection, weight mapping, cap, sleeve pairing, or risk. Confirm the
   same returned dictionary supplies exclusion, sign, ordering, and direct weight shrinkage.
8. Verify at least six names per side, no more than eight, center base weight `0.0125`, hard symbol
   cap `0.015`, side gross at most `0.10`, total requested gross at most `0.20`, and larger-sleeve
   pairing only scales downward. Confirm exact flat on insufficient breadth.
9. Confirm deterministic input-order invariance, exact seed, no I/O or hidden state, and that all
   returned symbols are a subset of the A6-certified context.
10. Enumerate the complete executable Team09 tree, hash every executable file, and reject unlisted
    helpers. Complete the new executable-source and semantic-review templates only from final
    bytes. Verify the manifest schedule is epoch-anchored 72 hours and its executable-open label
    horizon is exactly 72 hours with cross-fold endpoints purged.
11. Validate all six risk JSON files with the organizer parser. Root `risk_policy.json` must remain
    byte-identical to `risk_policies/00-none.json`. Both prior families are terminal for controls.
    Pivot-02 controls remain dormant until center plus neighborhood pass every core, pivot-02,
    full-development, and A5 gate.
12. Confirm the no-control center must complete without insolvency and pass positive ordinary and
    doubled-cost return/Sharpe, four positive folds, positive bull/bear/chop return and Sharpe,
    positive long-bull/short-bear/combined-chop attribution, active sleeves, maximum drawdown
    `<= 0.25`, and all other frozen gates. No aggregate metric compensates for a failed gate.
13. Enforce `neighbor_staging_plan.json`: center first, all ten neighbor identities and matching A5
    manifests frozen before any result read, serial execution, then post-barrier stability evidence.
    A failed center cannot activate neighbors; a failed neighborhood cannot activate controls.
14. Replace timestamps, zero hashes, and reviewer placeholders only through the actual first-added
    lifecycle. Any material correction after review requires a new exact-byte review. Failure of
    final pivot-02 is DNF, not another pivot or submission.
