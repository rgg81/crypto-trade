# team-09 · Top-40 V2

Status: `PIVOT_02_A7_A6_PROSPECTIVE_UNREGISTERED`

Two prior no-control families are immutable terminal history. `team09-fcpc-center-v1` completed
with `0.5332863362` maximum drawdown and `-2.1891077697` bear Sharpe. The distinct pivot
`team09-ccf-pivot01-v1` then became insolvent at `2022-05-13T00:00:00Z`. Neither family may
activate controls, and all ledger rows, registrations, results, reservations, and reviewed score
artifacts must remain unchanged.

Root files now propose the final mechanism pivot: family
`team09-defensive-residual-persistence-v1`, candidate `team09-drp-pivot02-v1`. It uses a uniform
72-hour residual-persistence rule, crash-fragility exclusion from both sleeves, captured-score
weight shrinkage, up to eight names per side, `0.10` center gross per side, and a `0.015` symbol
cap. It is neither a regime router nor an optional risk overlay. Root `risk_policy.json` remains
byte-exact no-control.

The pivot is unregistered and unevaluated. Every future command must use the exact A7 authority
and delegated A5/A6 lineage in the root identity drafts. A6 alone supplies the pure-crypto
eligible set; perpetual listing text is insufficient, and stablecoins plus direct TradFi, metal,
commodity, equity, ETF, index, FX, premarket, and leveraged-token exposures remain excluded.

Failure of any center gate is terminal DNF. Neighbors may begin only after all center core,
pivot-02, A5, and non-neighborhood full-development gates pass. Controls require the complete
neighborhood stability pass as well and cannot rescue the final pivot.
