# Team09 pivot-01 handoff

Preserve the two existing parent ledger rows and every `team09-fcpc-center-v1` score artifact.
Do not register a parent-family control: the core activation rule made that family terminal.

The next material configuration is the new no-control candidate `team09-ccf-pivot01-v1` under
family `team09-conditional-carry-fragility-v1`, canonical `strategy.py:build_strategy`, seed
`20260801`. Amendment 0006 remains the sole universe authority: only certified native-crypto
assets are eligible, while stablecoins and direct TradFi, metals, commodities, equities, ETFs,
indexes, FX, premarket instruments, and leveraged tokens are excluded regardless of perpetual
listing. Before registration:

1. require root `risk_policy.json` to be byte-identical to `risk_policies/00-none.json`;
2. run focused tests and independent static/A5 semantic review on the final bytes;
3. materialize the candidate score manifest and exact executable-source manifest from the new
   templates, then replace all family/trial hashes and timestamps;
4. first-add pivot/family/trial lifecycle records using only the active A7 entrypoint; and
5. require exact result identity equality through delegated A5 and A6.

The pivot core must pass `core_alpha_activation`, `pivot_01_mechanism_gate`, all full development
gates, and A5 score diagnostics. Bear return and Sharpe must be positive, drawdown at most 0.30,
and bull/chop returns remain positive. Controls cannot rescue failure.

Neighbors remain dormant until every non-neighborhood gate passes. Then follow
`neighbor_staging_plan.json`: freeze and register all ten identities before reading any result,
run strictly serially, and construct the result-bearing manifest only after the read barrier.
