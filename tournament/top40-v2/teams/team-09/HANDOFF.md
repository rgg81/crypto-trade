# Team09 first-trial handoff

Team09 remains prospective, unregistered, and unevaluated. The first material configuration is
`team09-fcpc-center-v1` with canonical `strategy.py:build_strategy`, seed `20260801`, and the
no-control bytes at root `risk_policy.json`. Before computing the registration risk hash, require
root bytes to equal `risk_policies/00-none.json` exactly.

Run and read that core before registering any control. It must have positive ordinary- and
doubled-cost return/Sharpe, at least four positive folds, positive bull/bear/chop returns, positive
long-bull/short-bear/combined-chop attribution, and active long and short sleeves. Controls cannot
rescue failure; use a separately preregistered mechanism revision/pivot or DNF instead.

The exact numeric core-activation and full qualification comparisons are frozen locally in
`qualification_thresholds.json`. Broad activation is not qualification: every full development
threshold still applies to a candidate before neighborhood evidence can complete qualification.

The lifecycle imports only root `strategy.py` and root `risk_policy.json`. For a later policy, copy
the selected immutable `risk_policies/` template byte-for-byte to root before commit, registration,
and run, and recompute all affected hashes. Replace family/trial timestamp and hash placeholders only
at the actual lifecycle events. Use only the active A7 entrypoint pinned in
`a7_execution_authority.json`; A5 is its exact delegated score protocol, not a direct command.
Require every registration, staged result, and A5 score outcome to match the identity tuple and
hash fields in `a5_score_lineage.json` exactly.

Non-core mechanism and neighbor JSON files are declarations only. Before any such registration,
materialize the declared override into separately hash-bound executable strategy/config bytes and
prove the canonical zero-argument builder exposes it. Follow `neighbor_staging_plan.json`: freeze
the complete neighbor set before reading any neighbor result, register and run neighbors serially,
and construct the result-bearing neighborhood manifest only after every preregistered return
artifact exists. A result hash may never be an input to its own source/config/registration hash.
