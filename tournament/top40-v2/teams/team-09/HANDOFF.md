# Team09 pivot-02 handoff

Preserve all four existing experiment rows, both family rows, both prior registration/result
identities, and every `team09-fcpc-center-v1` and `team09-ccf-pivot01-v1` score artifact. Do not
register a control for either terminal prior family.

The next and final mechanism configuration is no-control candidate `team09-drp-pivot02-v1` under
family `team09-defensive-residual-persistence-v1`, canonical `strategy.py:build_strategy`, seed
`20260801`. Amendment 0006 remains the sole universe authority: only certified native crypto is
eligible, irrespective of which other perpetuals Binance happens to list.

Before registration:

1. require root `risk_policy.json` to be byte-identical to `risk_policies/00-none.json`;
2. run focused tests and independent static/A5 semantic review on the final bytes;
3. confirm the one operative score call captures the final crash-screened and safety-shrunk score
   dictionary before selection, weights, caps, pairing, or risk;
4. materialize the pivot-02 72-hour score manifest and exact executable-source manifest from the
   new templates, then replace every family/trial hash and timestamp placeholder;
5. first-add pivot/family/trial lifecycle records only through the active A7 entrypoint; and
6. require exact result identity equality through delegated A5 and A6.

The center must complete without insolvency and pass `core_alpha_activation`,
`pivot_02_mechanism_gate`, every non-neighborhood full-development gate, and every A5 diagnostic.
Bull, bear, and chop returns and Sharpes must all be positive, drawdown at most `0.25`, requested
gross at most `0.20`, and both roles active. Controls and neighbors cannot rescue center failure.

Only after the center passes may the ten frozen neighbors proceed under
`neighbor_staging_plan.json`: materialize matching A5 manifests, preregister every identity before
reading a result, run serially, then apply stability gates. Optional controls remain dormant until
that complete neighborhood also passes. Any final-pivot failure is recorded as DNF.
