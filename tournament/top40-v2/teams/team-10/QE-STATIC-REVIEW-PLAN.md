# Team 10 final pivot-02 QE static review plan

This checklist is prospective, not evidence.

1. Bind every executable root byte and verify both family ledger records, all four experiment
   records, and completed score artifacts remain unchanged.
2. Verify every `StrategyConfig` field matches frozen config, singleton family ranges, trial
   parameters, and the center-only no-neighbor declaration.
3. Prove exact past-closed 8h price adjacency, strict-past funding timestamps, future/corrupt append
   invariance, and one 168h Unix-epoch-anchored schedule. Off-schedule calls return hold without A5
   capture.
4. Confirm price admissibility enforces one-bar, tail, drawdown, range, volatility, volume, breadth,
   and exact-history thresholds. Confirm funding admissibility enforces 14-day lookback, 12 unique
   events, 24h staleness, finite values, and `0.01` absolute-event cap.
5. Confirm funding pressure equals cumulative transfer plus `0.30` times the frozen full/recent sign
   persistence term, and that the operative score is its negative robust cross-sectional z-score.
6. Confirm the exact finite built-in dict calls imported `strategy.score_boundary` at one literal
   call site after all score transforms and before spread filtering, selection, retention, sizing,
   caps, or central risk. All downstream ranking uses the identity return.
7. Verify `0.12` gross, zero net, four names per side at `0.015`, two-rank retention, and hold on
   unchanged sleeves. Results, equity, positions, fills, future data, and ticker heuristics are
   forbidden inputs.
8. Independently attest that A6 membership contains native crypto base assets only and excludes
   stablecoin bases plus tokenized/synthetic TradFi securities, commodities, metals, and indexes.
   Stable quote/collateral is not base-asset classification; noneligible product bars/funding cannot
   enter.
9. Require root no-control bytes exactly. Confirm every retained control policy file is historical,
   unreferenced by the active policy list, and forbidden before and after final core failure.
10. Validate the 168h A5 schedule/label, minimum 240 pooled pairs, six folds, positive pooled
    Pearson, at least four positive fold correlations, complete coverage, and independent semantic
    approval.
11. Confirm ablation and neighbor lists are empty, their execution is forbidden, stability gates are
    not waived or fabricated, and any final core failure is DNF.
