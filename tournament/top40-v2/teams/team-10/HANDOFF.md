# Team 10 pivot-01 handoff

The terminal initial family is historical evidence, not a candidate. Do not run a control against it.

The next material candidate is `t10-dac-core-v1` in new parent-linked family
`t10-defensive-anchor-convergence-v1`, using root `strategy.py`, seed `20260801`, and no-control root
`risk_policy.json`. Before hashing, require exact equality with `risk_policies/no-control.json`.

Materialize and independently review the new A5 score/source/semantic templates. The operative score
schedule is 24h anchored at Unix epoch, with a justified 24h executable-open-to-open label. Replace
all sentinels only at actual family/pivot/trial lifecycle events through A7.

Run pivot-01 core once. Insolvency, missing metrics, or any numeric/A5/regime/role/sleeve gate failure
terminates it; controls cannot rescue it. Only after a complete non-neighbor pass may the already
fixed neighbors be materialized. Register all 12 before reading any neighbor result and withhold
qualification until all stability gates pass.
