# Team 10 final pivot-02 handoff

The two prior families are terminal evidence and their canonical artifacts must not be changed.

The sole next material candidate is `t10-fpu-core-v1` in parent-linked family
`t10-funding-pressure-unwind-v1`, using root `strategy.py`, seed `20260801`, and root no-control
`risk_policy.json`. Require byte equality with `risk_policies/no-control.json` before hashing.

Materialize and independently review the three `t10-fpu-core-v1` A5 templates. The operative score
schedule is 168h from `1970-01-01T00:00:00Z`; its label is the matching 168h
executable-open-to-open simple return. Replace sentinels only at actual family/pivot/trial lifecycle
events through the pinned A7 entrypoint.

Before registration, independently confirm that the A6 membership artifact includes only native
crypto base assets and excludes stablecoin bases plus tokenized/synthetic TradFi securities,
commodities, metals, and indexes. Do not classify by ticker text; stable quote/collateral is not a
stablecoin base.

Run the final no-control core once. Insolvency, missing metrics, or any numeric, cost, A5, fold,
quarter, regime, role, or sleeve gate failure is DNF. Controls, ablations, and neighbors are
forbidden before and after the result. Official stability gates remain binding and cannot be claimed
or fabricated without organizer-authorized evidence.
