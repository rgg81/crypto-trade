# Team 10 pivot-01 provenance

Status: **prospective, unregistered, no pivot result**

Pivot-01 was designed only from Team 10's terminal result/source and public tournament authorities.
The only observed performance fact is the exact terminal insolvency reason in `experiments.jsonl`;
there are no metrics. No other team, private qualifier, OOS data, report, leaderboard, ballot, or
snapshot row was inspected.

The registered initial family and reservation/result records in `families.jsonl` and
`experiments.jsonl` remain organizer-owned and unchanged. Its completed score manifest, executable
source manifest, and semantic review remain under `score-adapters/t10-rtre-core-v1*`. Pivot-01 uses
new family/candidate IDs and separate prospective A5 templates.

`strategy.py` consumes only decision time, A6-authorized eligible symbols, and causal `open_time`,
`close`, and `quote_volume`. Funding and auxiliary data are excluded. Amendment 0006 is the sole
pure-crypto classifier and its locally pinned report has zero violations. A7 preloads the frozen A5
protocol. The pivot calls its imported identity hook once on the exact final score dict at a 24h UTC
schedule; the matching label is 24h executable-open to executable-open simple return.

Before registration, the organizer must independently review exact source bytes, materialize the
source/score/semantic templates, replace all sentinels, verify root no-control policy equality, and
register the parent-linked family then trial through A7. No control or neighbor runs before the
no-control core passes. The private ticket and final OOS remain sealed.
