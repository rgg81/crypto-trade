# Team 10 final pivot-02 template instructions

All `*.template.json` files for `t10-fpu-core-v1` are prospective. Zero hashes, byte-size strings,
reviewer placeholders, and `REPLACE_*` timestamps are sentinels and must never reach a lifecycle
command.

Preserve both existing family ledger lines, all four experiment ledger lines, and every completed
`t10-rtre-core-v1*` and `t10-dac-core-v1*` score artifact. For pivot-02, materialize exact hashes and
sizes in the new funding-pressure source and score manifests, obtain an independent semantic review,
bind its SHA into the score manifest, then register the parent-linked family and exact no-control
trial through `scripts/top40_v2_tournament_runtime_preload_v7.py`.

The lifecycle reads only root `strategy.py` and `risk_policy.json`. No side-path file selects a
variant. There are no active ablation, control, or neighbor templates. The sole family ranges are
singletons, and historical pivot-01 neighbor/control files must never be materialized for pivot-02.
