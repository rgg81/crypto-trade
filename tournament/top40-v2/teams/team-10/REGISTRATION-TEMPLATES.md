# Team 10 pivot-01 template instructions

All `*.template.json` files are prospective. Zero hashes, byte-size strings, reviewer placeholders,
and `REPLACE_*` timestamps are sentinels and must never reach a lifecycle command.

Preserve the existing initial ledger and completed `score-adapters/t10-rtre-core-v1*` artifacts.
For pivot-01, materialize exact hashes/sizes in `t10-dac-core-v1` source and score manifests, obtain an
independent semantic review, bind its SHA into the score manifest, then register the parent-linked
family and exact no-control trial through `scripts/top40_v2_tournament_runtime_preload_v7.py`.

The lifecycle reads only root `strategy.py` and `risk_policy.json`. No side-path file selects a
variant. Every ablation/neighbor therefore requires separately materialized executable bytes. All 12
neighbors must be registered before the first neighbor result; template paths are not evidence.
