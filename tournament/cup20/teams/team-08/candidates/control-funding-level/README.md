# control-funding-level

The nominee's machinery with the score replaced by the funding LEVEL and nothing else
changed. This is the dynamics-versus-level control the mandate asks for: if the book's edge
came from the level, this control would carry it.

