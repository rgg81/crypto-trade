# carry-guard-off — team-07 research arm

`tournament/cup20/teams/team-07/candidates/carry-crowd-guard` with **CROWDING_PENALTY = 0.00**.

The ablation's OFF arm: the nominee with the crowding guard disabled exactly and nothing else changed. Every other constant, comment and line position is byte-identical to `carry-crowd-guard/strategy.py`.

Not a nomination. See `RESEARCH-CERTIFICATE.md` for the journal sequence number and the result.
