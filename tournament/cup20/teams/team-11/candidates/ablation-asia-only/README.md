# ablation-asia-only

An **ablation** of the team-11 contender `desk-closed-share`, journaled with `--kind ablation`.
It is a control and is not eligible for nomination (charter amendment A6).

See the module docstring in `strategy.py` for exactly what was removed and why, and section 9 of
`tournament/cup20/teams/team-11/RESEARCH-CERTIFICATE.md` for the scored comparison against the
contender. Every other parameter is held at the contender's value so the difference between the
two runs is attributable to the one thing that changed.
