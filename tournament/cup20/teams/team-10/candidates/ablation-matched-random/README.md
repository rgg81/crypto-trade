# ablation-matched-random — team-10 ABLATION

Journaled with `--kind ablation` (charter amendment A6). It is a **control**, it forfeits the
right to be nominated, and it exists to tell team-10 whether its lane's mechanism is doing any
work. See the module docstring in `strategy.py` for what it changes and `RESEARCH-CERTIFICATE.md`
for the result.
