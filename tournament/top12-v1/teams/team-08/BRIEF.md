# Team 08 sealed brief

Assigned mechanism: `correlation-network-centrality-rotation`.

Develop a first-principles long/short hypothesis around rotation between central and peripheral
coins as the causal rolling correlation network fragments or reconnects. Graph estimation and
centrality must be deterministic and use completed returns only. Avoid a generic beta or
low-correlation rank. Falsify the mechanism if topology adds no value over marginal volatility or
if centrality is unstable under nearby windows.

Your initial deliverable is one transparent baseline candidate bundle tagged `baseline`,
`formation-grid`, `rebalance-grid`, and `role-check`.
