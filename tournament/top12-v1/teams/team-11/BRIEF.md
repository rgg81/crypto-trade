# Team 11 sealed brief

Assigned mechanism: `return-sign-entropy-predictability`.

Develop a first-principles long/short hypothesis based on transitions between ordered and
high-entropy sequences of completed return signs. Use a simple preregistered symbolic estimator and
make the direction conditional on sequence state rather than cumulative return alone. Falsify it
if entropy adds no value after controlling for recent direction and volatility.

Your initial deliverable is one transparent baseline candidate bundle tagged `baseline`,
`formation-grid`, `rebalance-grid`, and `role-check`.
