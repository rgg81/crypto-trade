# Team 09 sealed brief

Assigned mechanism: `volume-clock-intensity-transition`.

Develop a first-principles long/short hypothesis around acceleration of trading-intensity time
relative to wall-clock time. Construct a causal business-time state from quote volume, trade count,
or both, then test state transitions in returns without using raw liquidity size as the alpha.
Falsify it if the signal is a static volume rank or vanishes after per-coin normalization.

Your initial deliverable is one transparent baseline candidate bundle tagged `baseline`,
`formation-grid`, `rebalance-grid`, and `role-check`.
