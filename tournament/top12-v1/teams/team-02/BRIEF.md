# Team 02 sealed brief

Assigned mechanism: `range-compression-expansion-asymmetry`.

Develop a first-principles long/short hypothesis around the transition out of sustained own-coin
range compression. Use only completed ranges and prices. The alpha must depend on an asymmetric
state transition, not generic volatility scaling or a plain price breakout. Preregister the
directional condition and falsify the mechanism if expansion direction is symmetric, unstable, or
consumed by next-open costs.

Your initial deliverable is one transparent baseline candidate bundle tagged `baseline`,
`formation-grid`, `rebalance-grid`, and `role-check`.
