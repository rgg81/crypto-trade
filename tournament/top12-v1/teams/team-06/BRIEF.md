# Team 06 sealed brief

Assigned mechanism: `jump-aftershock-decay-state`.

Develop a first-principles event-time long/short hypothesis for the decay path after a completed
discontinuous price jump. Use a deterministic aftershock state machine with explicit event age and
invalidation. The alpha must be conditional on post-event decay, not a generic one-bar reversal.
Falsify it if the hazard profile is not stable across jump sizes, signs, and eras.

Your initial deliverable is one transparent baseline candidate bundle tagged `baseline`,
`formation-grid`, `rebalance-grid`, and `role-check`.
