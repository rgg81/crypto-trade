.....s.....ss........................................................... [  4%]
........................................................................ [  8%]
........................................................................ [ 12%]
......FF.................................................F.............. [ 16%]
...sss..............FF..................................ssssssssssssssss [ 20%]
........................................................................ [ 25%]
.......................sssssssss........................................ [ 29%]
.....................s.................................................. [ 33%]
........................................................................ [ 37%]
.................................................................F...... [ 41%]
......................................s................................. [ 46%]
........................................................................ [ 50%]
.......................................F..FF............................ [ 54%]
.......F...........FF................................................... [ 58%]
........................................................................ [ 62%]
........................................................................ [ 66%]
........................................................................ [ 71%]
........................................................................ [ 75%]
...........................F..F......................F.F................ [ 79%]
.................F...................................................FF. [ 83%]
................................F......F........F....................FF. [ 87%]
..................................................................FFF.F. [ 92%]
FF.................................F.................................... [ 96%]
.............................................................F..F        [100%]
=================================== FAILURES ===================================
___________ test_fracdiff_d05_close_present_in_all_4_symbol_parquets ___________

    def test_fracdiff_d05_close_present_in_all_4_symbol_parquets() -> None:
        """All 4 symbol parquets MUST contain fracdiff_d05_close column.

        iter-v3/051 brief Sub-fix 9: no feature regeneration required. QR claims the
        column is already present in all 4 parquets. This test verifies the claim.
        ALGOUSDT is included even though not in /051 V3_MODELS (reserved-for-future).
        """
        for sym in _V3_ALL_SYMBOLS:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
>               schema = pq.read_schema(path)
                         ^^^^^^^^^^^^^^^^^^^^

tests/features_v3/test_fracdiff_d05_universal.py:99:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:2421: in read_schema
    file_ctx = where = filesystem.open_input_file(where)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
pyarrow/_fs.pyx:815: in pyarrow._fs.FileSystem.open_input_file
    ???
pyarrow/error.pxi:155: in pyarrow.lib.pyarrow_internal_check_status
    return check_status(status)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

>   raise convert_status(status)
E   FileNotFoundError: [Errno 2] Failed to open local file 'data/features_v3/BCHUSDT_8h_features.parquet'. Detail: [errno 2] No such file or directory

pyarrow/error.pxi:92: FileNotFoundError

During handling of the above exception, another exception occurred:

    def test_fracdiff_d05_close_present_in_all_4_symbol_parquets() -> None:
        """All 4 symbol parquets MUST contain fracdiff_d05_close column.

        iter-v3/051 brief Sub-fix 9: no feature regeneration required. QR claims the
        column is already present in all 4 parquets. This test verifies the claim.
        ALGOUSDT is included even though not in /051 V3_MODELS (reserved-for-future).
        """
        for sym in _V3_ALL_SYMBOLS:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
                schema = pq.read_schema(path)
                cols = schema.names
            except Exception as exc:
>               pytest.fail(f"{sym} parquet not readable at {path}: {exc}")
E               Failed: BCHUSDT parquet not readable at data/features_v3/BCHUSDT_8h_features.parquet: [Errno 2] Failed to open local file 'data/features_v3/BCHUSDT_8h_features.parquet'. Detail: [errno 2] No such file or directory

tests/features_v3/test_fracdiff_d05_universal.py:102: Failed
________________ test_fracdiff_d05_close_stationary_per_symbol _________________

    def test_fracdiff_d05_close_stationary_per_symbol() -> None:
        """fracdiff_d05_close ADF p-value must be < 0.05 in IS subset for each /051 symbol.

        iter-v3/051 EDA evidence: ADF p≈0 for BCH/LDO/TRX. This smoke test reads the
        actual parquet IS subset and re-runs ADF to confirm the column is stationary.

        Uses statsmodels adfuller with autolag='AIC'.
        """
        import pyarrow.parquet as pq  # noqa: PLC0415
        from statsmodels.tsa.stattools import adfuller  # noqa: PLC0415

        for sym in _V3_MODELS_ITER_052:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
>               df = pq.read_table(
                    path,
                    columns=["open_time", "fracdiff_d05_close"],
                ).to_pandas()

tests/features_v3/test_fracdiff_d05_universal.py:128:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:1858: in read_table
    dataset = ParquetDataset(
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:1438: in __init__
    self._dataset = ds.dataset(path_or_paths, filesystem=filesystem,
.venv/lib/python3.13/site-packages/pyarrow/dataset.py:790: in dataset
    return _filesystem_dataset(source, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/pyarrow/dataset.py:472: in _filesystem_dataset
    fs, paths_or_selector = _ensure_single_source(source, filesystem)
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = 'data/features_v3/BCHUSDT_8h_features.parquet'
filesystem = <pyarrow._fs.LocalFileSystem object at 0x7fa2f8479230>

    def _ensure_single_source(path, filesystem=None):
        """
        Treat path as either a recursively traversable directory or a single file.

        Parameters
        ----------
        path : path-like
        filesystem : FileSystem or str, optional
            If an URI is passed, then its path component will act as a prefix for
            the file paths.

        Returns
        -------
        (FileSystem, list of str or fs.Selector)
            File system object and either a single item list pointing to a file or
            an fs.Selector object pointing to a directory.

        Raises
        ------
        TypeError
            If the passed filesystem has wrong type.
        FileNotFoundError
            If the referenced file or directory doesn't exist.
        """
        from pyarrow.fs import FileType, FileSelector, _resolve_filesystem_and_path

        # at this point we already checked that `path` is a path-like
        filesystem, path = _resolve_filesystem_and_path(path, filesystem)

        # ensure that the path is normalized before passing to dataset discovery
        path = filesystem.normalize_path(path)

        # retrieve the file descriptor
        file_info = filesystem.get_file_info(path)

        # depending on the path type either return with a recursive
        # directory selector or as a list containing a single file
        if file_info.type == FileType.Directory:
            paths_or_selector = FileSelector(path, recursive=True)
        elif file_info.type == FileType.File:
            paths_or_selector = [path]
        else:
>           raise FileNotFoundError(path)
E           FileNotFoundError: data/features_v3/BCHUSDT_8h_features.parquet

.venv/lib/python3.13/site-packages/pyarrow/dataset.py:437: FileNotFoundError

During handling of the above exception, another exception occurred:

    def test_fracdiff_d05_close_stationary_per_symbol() -> None:
        """fracdiff_d05_close ADF p-value must be < 0.05 in IS subset for each /051 symbol.

        iter-v3/051 EDA evidence: ADF p≈0 for BCH/LDO/TRX. This smoke test reads the
        actual parquet IS subset and re-runs ADF to confirm the column is stationary.

        Uses statsmodels adfuller with autolag='AIC'.
        """
        import pyarrow.parquet as pq  # noqa: PLC0415
        from statsmodels.tsa.stattools import adfuller  # noqa: PLC0415

        for sym in _V3_MODELS_ITER_052:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
                df = pq.read_table(
                    path,
                    columns=["open_time", "fracdiff_d05_close"],
                ).to_pandas()
            except Exception as exc:
>               pytest.fail(f"{sym} parquet read failed: {exc}")
E               Failed: BCHUSDT parquet read failed: data/features_v3/BCHUSDT_8h_features.parquet

tests/features_v3/test_fracdiff_d05_universal.py:133: Failed
________________ test_hurst_drift_50_200_stationary_per_symbol _________________

    def test_hurst_drift_50_200_stationary_per_symbol() -> None:
        """hurst_drift_50_200 ADF p-value must be < 0.05 in IS subset for each /053 symbol.

        iter-v3/053 EDA evidence (analysis/iteration_v3-053/axis2_adf_per_symbol.csv SHA `1fc6d55`):
        ADF p << 0.05 for BCH/LDO/TRX/ALGO. Structurally stationary by construction
        (bounded difference of two bounded R/S Hurst measurements).
        This smoke test computes hurst_drift_50_200 on-the-fly from parquet source primitives
        (hurst_100, hurst_diff_100_50, hurst_200) and runs ADF to confirm. NO parquet regen needed.

        Uses statsmodels adfuller with autolag='AIC'.
        """
        import pyarrow.parquet as pq  # noqa: PLC0415
        from statsmodels.tsa.stattools import adfuller  # noqa: PLC0415

        # This is a feature-MATH stationarity smoke test — it verifies the
        # hurst_drift_50_200 property, not the live universe composition. It runs on
        # the /059-canonical 3-symbol universe (iter-v3/084 reverted the /083 FILUSDT
        # expansion — /083 was NEGATIVE/NO-MERGE).
        _incumbents_with_parquets = ("BCHUSDT", "LDOUSDT", "TRXUSDT")
        for sym in _incumbents_with_parquets:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
>               df = pq.read_table(
                    path,
                    columns=["open_time", "hurst_100", "hurst_diff_100_50", "hurst_200"],
                ).to_pandas()

tests/features_v3/test_hurst_drift_50_200_universal.py:179:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:1858: in read_table
    dataset = ParquetDataset(
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:1438: in __init__
    self._dataset = ds.dataset(path_or_paths, filesystem=filesystem,
.venv/lib/python3.13/site-packages/pyarrow/dataset.py:790: in dataset
    return _filesystem_dataset(source, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/pyarrow/dataset.py:472: in _filesystem_dataset
    fs, paths_or_selector = _ensure_single_source(source, filesystem)
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = 'data/features_v3/BCHUSDT_8h_features.parquet'
filesystem = <pyarrow._fs.LocalFileSystem object at 0x7fa2f845b430>

    def _ensure_single_source(path, filesystem=None):
        """
        Treat path as either a recursively traversable directory or a single file.

        Parameters
        ----------
        path : path-like
        filesystem : FileSystem or str, optional
            If an URI is passed, then its path component will act as a prefix for
            the file paths.

        Returns
        -------
        (FileSystem, list of str or fs.Selector)
            File system object and either a single item list pointing to a file or
            an fs.Selector object pointing to a directory.

        Raises
        ------
        TypeError
            If the passed filesystem has wrong type.
        FileNotFoundError
            If the referenced file or directory doesn't exist.
        """
        from pyarrow.fs import FileType, FileSelector, _resolve_filesystem_and_path

        # at this point we already checked that `path` is a path-like
        filesystem, path = _resolve_filesystem_and_path(path, filesystem)

        # ensure that the path is normalized before passing to dataset discovery
        path = filesystem.normalize_path(path)

        # retrieve the file descriptor
        file_info = filesystem.get_file_info(path)

        # depending on the path type either return with a recursive
        # directory selector or as a list containing a single file
        if file_info.type == FileType.Directory:
            paths_or_selector = FileSelector(path, recursive=True)
        elif file_info.type == FileType.File:
            paths_or_selector = [path]
        else:
>           raise FileNotFoundError(path)
E           FileNotFoundError: data/features_v3/BCHUSDT_8h_features.parquet

.venv/lib/python3.13/site-packages/pyarrow/dataset.py:437: FileNotFoundError

During handling of the above exception, another exception occurred:

    def test_hurst_drift_50_200_stationary_per_symbol() -> None:
        """hurst_drift_50_200 ADF p-value must be < 0.05 in IS subset for each /053 symbol.

        iter-v3/053 EDA evidence (analysis/iteration_v3-053/axis2_adf_per_symbol.csv SHA `1fc6d55`):
        ADF p << 0.05 for BCH/LDO/TRX/ALGO. Structurally stationary by construction
        (bounded difference of two bounded R/S Hurst measurements).
        This smoke test computes hurst_drift_50_200 on-the-fly from parquet source primitives
        (hurst_100, hurst_diff_100_50, hurst_200) and runs ADF to confirm. NO parquet regen needed.

        Uses statsmodels adfuller with autolag='AIC'.
        """
        import pyarrow.parquet as pq  # noqa: PLC0415
        from statsmodels.tsa.stattools import adfuller  # noqa: PLC0415

        # This is a feature-MATH stationarity smoke test — it verifies the
        # hurst_drift_50_200 property, not the live universe composition. It runs on
        # the /059-canonical 3-symbol universe (iter-v3/084 reverted the /083 FILUSDT
        # expansion — /083 was NEGATIVE/NO-MERGE).
        _incumbents_with_parquets = ("BCHUSDT", "LDOUSDT", "TRXUSDT")
        for sym in _incumbents_with_parquets:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
                df = pq.read_table(
                    path,
                    columns=["open_time", "hurst_100", "hurst_diff_100_50", "hurst_200"],
                ).to_pandas()
            except Exception as exc:
>               pytest.fail(f"{sym} parquet read failed at {path}: {exc}")
E               Failed: BCHUSDT parquet read failed at data/features_v3/BCHUSDT_8h_features.parquet: data/features_v3/BCHUSDT_8h_features.parquet

tests/features_v3/test_hurst_drift_50_200_universal.py:184: Failed
_______ test_regime_momentum_signed_3d_present_in_all_4_symbol_parquets ________

    def test_regime_momentum_signed_3d_present_in_all_4_symbol_parquets() -> None:
        """All 4 symbol parquets MUST contain regime_momentum_signed_3d column.

        iter-v3/052 requires feature parquet regen (compute_regime_momentum_signed_3d was
        dead code before /052 dispatch activation; column was NOT in /051 parquets).
        ALGOUSDT included even though not in /052 V3_MODELS (reserved-for-future).
        Run: `uv run crypto-trade features --track v3 --interval 8h
             --symbols BCHUSDT,LDOUSDT,TRXUSDT,ALGOUSDT --format parquet --workers 4`
        """
        for sym in _V3_ALL_SYMBOLS:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
>               schema = pq.read_schema(path)
                         ^^^^^^^^^^^^^^^^^^^^

tests/features_v3/test_regime_momentum_signed_3d_universal.py:121:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:2421: in read_schema
    file_ctx = where = filesystem.open_input_file(where)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
pyarrow/_fs.pyx:815: in pyarrow._fs.FileSystem.open_input_file
    ???
pyarrow/error.pxi:155: in pyarrow.lib.pyarrow_internal_check_status
    return check_status(status)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

>   raise convert_status(status)
E   FileNotFoundError: [Errno 2] Failed to open local file 'data/features_v3/BCHUSDT_8h_features.parquet'. Detail: [errno 2] No such file or directory

pyarrow/error.pxi:92: FileNotFoundError

During handling of the above exception, another exception occurred:

    def test_regime_momentum_signed_3d_present_in_all_4_symbol_parquets() -> None:
        """All 4 symbol parquets MUST contain regime_momentum_signed_3d column.

        iter-v3/052 requires feature parquet regen (compute_regime_momentum_signed_3d was
        dead code before /052 dispatch activation; column was NOT in /051 parquets).
        ALGOUSDT included even though not in /052 V3_MODELS (reserved-for-future).
        Run: `uv run crypto-trade features --track v3 --interval 8h
             --symbols BCHUSDT,LDOUSDT,TRXUSDT,ALGOUSDT --format parquet --workers 4`
        """
        for sym in _V3_ALL_SYMBOLS:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
                schema = pq.read_schema(path)
                cols = schema.names
            except Exception as exc:
>               pytest.fail(f"{sym} parquet not readable at {path}: {exc}")
E               Failed: BCHUSDT parquet not readable at data/features_v3/BCHUSDT_8h_features.parquet: [Errno 2] Failed to open local file 'data/features_v3/BCHUSDT_8h_features.parquet'. Detail: [errno 2] No such file or directory

tests/features_v3/test_regime_momentum_signed_3d_universal.py:124: Failed
_____________ test_regime_momentum_signed_3d_stationary_per_symbol _____________

    def test_regime_momentum_signed_3d_stationary_per_symbol() -> None:
        """regime_momentum_signed_3d ADF p-value must be < 0.05 in IS subset for each /052 symbol.

        iter-v3/052 EDA evidence (analysis/iteration_v3-051/axis_c_regime_3d_adf.csv SHA `290f37b`):
        ADF p=0 for BCH/LDO/TRX/ALGO. Structurally stationary by construction
        (ret_3d is a log-return, stationary; sign(hurst-0.5) is bounded {-1,0,+1}).
        This smoke test reads the actual parquet IS subset and re-runs ADF to confirm.

        Uses statsmodels adfuller with autolag='AIC'.
        """
        from statsmodels.tsa.stattools import adfuller  # noqa: PLC0415

        for sym in _V3_MODELS_ITER_052:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
>               df = pq.read_table(
                    path,
                    columns=["open_time", "regime_momentum_signed_3d"],
                ).to_pandas()

tests/features_v3/test_regime_momentum_signed_3d_universal.py:153:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:1858: in read_table
    dataset = ParquetDataset(
.venv/lib/python3.13/site-packages/pyarrow/parquet/core.py:1438: in __init__
    self._dataset = ds.dataset(path_or_paths, filesystem=filesystem,
.venv/lib/python3.13/site-packages/pyarrow/dataset.py:790: in dataset
    return _filesystem_dataset(source, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/pyarrow/dataset.py:472: in _filesystem_dataset
    fs, paths_or_selector = _ensure_single_source(source, filesystem)
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = 'data/features_v3/BCHUSDT_8h_features.parquet'
filesystem = <pyarrow._fs.LocalFileSystem object at 0x7fa37bf3a6b0>

    def _ensure_single_source(path, filesystem=None):
        """
        Treat path as either a recursively traversable directory or a single file.

        Parameters
        ----------
        path : path-like
        filesystem : FileSystem or str, optional
            If an URI is passed, then its path component will act as a prefix for
            the file paths.

        Returns
        -------
        (FileSystem, list of str or fs.Selector)
            File system object and either a single item list pointing to a file or
            an fs.Selector object pointing to a directory.

        Raises
        ------
        TypeError
            If the passed filesystem has wrong type.
        FileNotFoundError
            If the referenced file or directory doesn't exist.
        """
        from pyarrow.fs import FileType, FileSelector, _resolve_filesystem_and_path

        # at this point we already checked that `path` is a path-like
        filesystem, path = _resolve_filesystem_and_path(path, filesystem)

        # ensure that the path is normalized before passing to dataset discovery
        path = filesystem.normalize_path(path)

        # retrieve the file descriptor
        file_info = filesystem.get_file_info(path)

        # depending on the path type either return with a recursive
        # directory selector or as a list containing a single file
        if file_info.type == FileType.Directory:
            paths_or_selector = FileSelector(path, recursive=True)
        elif file_info.type == FileType.File:
            paths_or_selector = [path]
        else:
>           raise FileNotFoundError(path)
E           FileNotFoundError: data/features_v3/BCHUSDT_8h_features.parquet

.venv/lib/python3.13/site-packages/pyarrow/dataset.py:437: FileNotFoundError

During handling of the above exception, another exception occurred:

    def test_regime_momentum_signed_3d_stationary_per_symbol() -> None:
        """regime_momentum_signed_3d ADF p-value must be < 0.05 in IS subset for each /052 symbol.

        iter-v3/052 EDA evidence (analysis/iteration_v3-051/axis_c_regime_3d_adf.csv SHA `290f37b`):
        ADF p=0 for BCH/LDO/TRX/ALGO. Structurally stationary by construction
        (ret_3d is a log-return, stationary; sign(hurst-0.5) is bounded {-1,0,+1}).
        This smoke test reads the actual parquet IS subset and re-runs ADF to confirm.

        Uses statsmodels adfuller with autolag='AIC'.
        """
        from statsmodels.tsa.stattools import adfuller  # noqa: PLC0415

        for sym in _V3_MODELS_ITER_052:
            path = f"{_PARQUET_DIR}/{sym}_8h_features.parquet"
            try:
                df = pq.read_table(
                    path,
                    columns=["open_time", "regime_momentum_signed_3d"],
                ).to_pandas()
            except Exception as exc:
>               pytest.fail(f"{sym} parquet read failed: {exc}")
E               Failed: BCHUSDT parquet read failed: data/features_v3/BCHUSDT_8h_features.parquet

tests/features_v3/test_regime_momentum_signed_3d_universal.py:158: Failed
______________________ test_iter090_runner_feature_count _______________________

    def test_iter090_runner_feature_count():
        """iter-v3/091: runner reverts to 13-feature base (downside features ABSENT)."""
        import run_cross_sectional_v3 as runner

>       assert len(runner.XS_BASE_FEATURES) == 14, (
            "14 base features (iter-v3/102 V3_FEATURE_COLUMNS_TOP_N=15 minus btc_ret_14d=14)"
        )
E       AssertionError: 14 base features (iter-v3/102 V3_FEATURE_COLUMNS_TOP_N=15 minus btc_ret_14d=14)
E       assert 13 == 14
E        +  where 13 = len(['max_dd_window_50', 'ema_spread_atr_20', 'ret_kurt_50', 'ret_skew_200', 'range_realized_vol_50', 'hurst_diff_100_50', ...])
E        +    where ['max_dd_window_50', 'ema_spread_atr_20', 'ret_kurt_50', 'ret_skew_200', 'range_realized_vol_50', 'hurst_diff_100_50', ...] = <module 'run_cross_sectional_v3' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/run_cross_sectional_v3.py'>.XS_BASE_FEATURES

tests/strategies/ml/test_cross_sectional.py:1334: AssertionError
__________________ test_v3_models_is_the_reanchored_universe ___________________

    def test_v3_models_is_the_reanchored_universe() -> None:
        """Section 9.2 test #1 (HARD build-fail).

        Asserts V3_MODELS symbols == {ATOMUSDT, RUNEUSDT, AVAXUSDT, HBARUSDT, ICPUSDT, ALGOUSDT}
        exactly — the iter-v3/128 WILD CYCLE-7 axis-7 wholesale universe replacement
        (BCH/LDO/TRX → ATOM/RUNE/AVAX/HBAR/ICP/ALGO, cardinality 3→6, sector-pure L1).

        History:
          iter-v3/110: CRV/AAVE/GRT/ADA (4-symbol expansion).
          iter-v3/111: Critic re-test; universe unchanged.
          iter-v3/112: BCH/LDO/TRX RESTORED (pooled arch).
          iter-v3/113-124: BCH/LDO/TRX retained.
          iter-v3/125: WILD axis — BCH/LDO/TRX → ATOM/RUNE/UNI (NEGATIVE-catastrophic).
          iter-v3/126: V3_MODELS REVERTED BCH/LDO/TRX (mandatory /125 baseline-restore).
          iter-v3/127: BCH/LDO/TRX retained; drawdown brake axis CLOSED (NEGATIVE).
          iter-v3/128: WILD axis — BCH/LDO/TRX → ATOM/RUNE/AVAX/HBAR/ICP/ALGO (cardinality 6).
        """
        runner = _load_runner()
        symbols = {sym for _label, sym in runner.V3_MODELS}

        expected = {"ATOMUSDT", "RUNEUSDT", "AVAXUSDT", "HBARUSDT", "ICPUSDT", "ALGOUSDT"}
>       assert symbols == expected, (
            f"V3_MODELS symbols {symbols} != expected {expected}. "
            "iter-v3/128: V3_MODELS = 6-symbol sector-pure L1 (ATOM/RUNE/AVAX/HBAR/ICP/ALGO). "
            "Edit V3_MODELS in run_baseline_v3.py."
        )
E       AssertionError: V3_MODELS symbols {'BCHUSDT', 'TRXUSDT', 'LDOUSDT'} != expected {'ALGOUSDT', 'ATOMUSDT', 'HBARUSDT', 'ICPUSDT', 'AVAXUSDT', 'RUNEUSDT'}. iter-v3/128: V3_MODELS = 6-symbol sector-pure L1 (ATOM/RUNE/AVAX/HBAR/ICP/ALGO). Edit V3_MODELS in run_baseline_v3.py.
E       assert {'BCHUSDT', '...T', 'TRXUSDT'} == {'ALGOUSDT', ...', 'RUNEUSDT'}
E
E         Extra items in the left set:
E         'BCHUSDT'
E         'TRXUSDT'
E         'LDOUSDT'
E         Extra items in the right set:
E         'AVAXUSDT'...
E
E         ...Full output truncated (6 lines hidden), use '-vv' to show

tests/strategies/ml/test_universe_reselection_v3.py:84: AssertionError
_________________ test_feature_columns_pinned_for_new_symbols __________________

    def test_feature_columns_pinned_for_new_symbols() -> None:
        """Section 9.2 test #4 (HARD build-fail).

        Asserts features_for_symbol returns the V3_FEATURE_COLUMNS_TOP_N feature list
        for all traded symbols, and that V3_FEATURES_PER_SYMBOL is empty (no per-symbol
        override). The feedback_explicit_feature_columns.md invariant — all 6 L1 symbols
        inherit the universal 14-feature stack, no per-symbol override.

        iter-v3/128: WILD axis — V3_MODELS = 6-symbol sector-pure L1 (ATOM/RUNE/AVAX/HBAR/ICP/ALGO).
        V3_FEATURE_COLUMNS_TOP_N stays at 14 (UNCHANGED from /121 — /126's d24 REVERTED at /127).
        """
        from crypto_trade.features_v3 import (
            V3_FEATURE_COLUMNS_TOP_N,
            V3_FEATURES_PER_SYMBOL,
            features_for_symbol,
        )

        assert len(V3_FEATURES_PER_SYMBOL) == 0, (
            f"V3_FEATURES_PER_SYMBOL has {len(V3_FEATURES_PER_SYMBOL)} entries — "
            "expected 0 (empty). iter-v3/128: no per-symbol feature overrides. "
            "All 6 L1 symbols inherit the universal 14-feature stack."
        )

        # iter-v3/128: 6-symbol universe ATOM/RUNE/AVAX/HBAR/ICP/ALGO
        for sym in ("ATOMUSDT", "RUNEUSDT", "AVAXUSDT", "HBARUSDT", "ICPUSDT", "ALGOUSDT"):
            feats = features_for_symbol(sym)
            n_expected = len(V3_FEATURE_COLUMNS_TOP_N)
            assert len(feats) == n_expected, (
                f"features_for_symbol('{sym}') returned {len(feats)} features — "
                f"expected {n_expected}. iter-v3/128: all symbols inherit "
                "V3_FEATURE_COLUMNS_TOP_N (14 features). Check V3_FEATURES_PER_SYMBOL is empty."
            )
            assert list(feats) == list(V3_FEATURE_COLUMNS_TOP_N), (
                f"features_for_symbol('{sym}') returned {list(feats)} — "
                f"expected V3_FEATURE_COLUMNS_TOP_N = {list(V3_FEATURE_COLUMNS_TOP_N)}. "
                "The feature list must be bit-identical to the universal feature stack."
            )

        # Explicit: BCH/LDO/TRX (/127 universe) must NOT appear in V3_MODELS at /128
        runner = _load_runner()
        traded_syms = [sym for _label, sym in runner.V3_MODELS]
>       assert "BCHUSDT" not in traded_syms, (
            "BCHUSDT still in V3_MODELS — must be removed at iter-v3/128 "
            "(BCH/LDO/TRX REPLACED by ATOM/RUNE/AVAX/HBAR/ICP/ALGO at /128 WILD axis)."
        )
E       AssertionError: BCHUSDT still in V3_MODELS — must be removed at iter-v3/128 (BCH/LDO/TRX REPLACED by ATOM/RUNE/AVAX/HBAR/ICP/ALGO at /128 WILD axis).
E       assert 'BCHUSDT' not in ['BCHUSDT', 'LDOUSDT', 'TRXUSDT']

tests/strategies/ml/test_universe_reselection_v3.py:292: AssertionError
_______________________ test_walk_forward_embargo_intact _______________________

    def test_walk_forward_embargo_intact() -> None:
        """Section 9.2 test #5 (HARD build-fail).

        Asserts the e149e9d walk-forward embargo (train_end_ms = test_start_ms - embargo_ms,
        compute_embargo_candles helper, cv_gap = embargo_candles * n_symbols) is present and
        load-bearing in the runner for the new 3-symbol universe (cv_gap resolves to 66).

        Checks:
        (a) walk_forward.compute_embargo_candles exists and returns 22 for 10080/480.
        (b) walk_forward.generate_monthly_splits accepts label_timeout_minutes + interval_minutes.
        (c) REQUIRED_GAP == 66 == (21+1)*3 (3-symbol universe; timeout 21 candles).
        (d) Source-level: generate_monthly_splits source contains 'train_end_ms = test_start_ms'
            (the embargo subtraction pattern).
        """
        from crypto_trade.strategies.ml import walk_forward
        from crypto_trade.strategies.ml.validation_v3 import REQUIRED_GAP

        # (a) compute_embargo_candles helper
        assert hasattr(walk_forward, "compute_embargo_candles"), (
            "walk_forward.compute_embargo_candles not found — e149e9d embargo fix missing. "
            "HARD-fail: the lookahead-bug fix commit e149e9d must be present."
        )
        embargo_candles = walk_forward.compute_embargo_candles(
            label_timeout_minutes=10080,
            interval_minutes=480,
        )
        assert embargo_candles == 22, (
            f"compute_embargo_candles(10080, 480) = {embargo_candles} — expected 22. "
            "embargo = 10080 // 480 + 1 = 21 + 1 = 22."
        )

        # (b) generate_monthly_splits accepts the required parameters
        sig = inspect.signature(walk_forward.generate_monthly_splits)
        params = set(sig.parameters)
        assert "label_timeout_minutes" in params, (
            "generate_monthly_splits missing 'label_timeout_minutes' parameter. "
            "e149e9d embargo fix requires this argument."
        )
        assert "interval_minutes" in params, (
            "generate_monthly_splits missing 'interval_minutes' parameter. "
            "e149e9d embargo fix requires this argument."
        )

        # (c) REQUIRED_GAP module constant stays 66 (3-symbol baseline); runner uses
        # a local override of 132 = (21+1)*6 for the new 6-symbol /128 universe.
        # validation_v3.REQUIRED_GAP is NOT changed — same pattern as 24h=72 at /117.
        runner = _load_runner()
        n_symbols = len(runner.V3_MODELS)
>       assert n_symbols == 6, (
            f"V3_MODELS has {n_symbols} symbols — expected 6 (ATOM/RUNE/AVAX/HBAR/ICP/ALGO). "
            "iter-v3/128: 6-symbol WILD CYCLE-7 axis-7 universe."
        )
E       AssertionError: V3_MODELS has 3 symbols — expected 6 (ATOM/RUNE/AVAX/HBAR/ICP/ALGO). iter-v3/128: 6-symbol WILD CYCLE-7 axis-7 universe.
E       assert 3 == 6

tests/strategies/ml/test_universe_reselection_v3.py:359: AssertionError
____________________ test_frozen_team09_authority_is_exact _____________________

    def test_frozen_team09_authority_is_exact() -> None:
>       authority = verify_frozen_authority()
                    ^^^^^^^^^^^^^^^^^^^^^^^^^

tests/team09/test_team09_pipeline.py:72:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = None

    def verify_frozen_authority(root: str | Path | None = None) -> FrozenAuthority:
        """Fail closed unless every locally load-bearing frozen identity is intact."""

        base = Path(root).resolve() if root is not None else repository_root()
        deployment = verify_deployment_authority(base)
        candidate = candidate_root(base)
        strategy = candidate / "strategy.py"
        risk_policy = candidate / "risk_policy.json"
        readme = candidate / "README.md"
        candidate_metadata = candidate / "candidate.json"
        dependency_lock = base / "uv.lock"
        archive = (
            base
            / "reports-top40-v4-r1"
            / "source-archives"
            / "sha256"
            / f"{SOURCE_ARCHIVE_SHA256}.json"
        )
        required = (
            strategy,
            risk_policy,
            readme,
            candidate_metadata,
            archive,
            dependency_lock,
        )
        missing = [str(path) for path in required if not path.is_file()]
        if missing:
            raise FileNotFoundError(f"Team 09 frozen authority is incomplete: {missing}")

        actual_strategy = sha256_file(strategy)
        actual_policy = sha256_file(risk_policy)
        actual_readme = sha256_file(readme)
        actual_candidate_metadata = sha256_file(candidate_metadata)
        actual_dependency_lock = sha256_file(dependency_lock)
        archive_present = archive.is_file()
        actual_archive = sha256_file(archive)
        evaluator_authority = {
            relative: sha256_file(base / relative) for relative in _EVALUATOR_AUTHORITY_PATHS
        }
        evaluator_sha256 = hashlib.sha256(
            json.dumps(
                evaluator_authority,
                allow_nan=False,
                ensure_ascii=True,
                separators=(",", ":"),
                sort_keys=True,
            ).encode("utf-8")
        ).hexdigest()
        mismatches: list[str] = []
        if actual_strategy != STRATEGY_SHA256:
            mismatches.append(f"strategy {actual_strategy} != {STRATEGY_SHA256}")
        if actual_policy != RISK_POLICY_SHA256:
            mismatches.append(f"risk policy {actual_policy} != {RISK_POLICY_SHA256}")
        if actual_readme != README_SHA256:
            mismatches.append(f"README {actual_readme} != {README_SHA256}")
        if actual_candidate_metadata != CANDIDATE_METADATA_SHA256:
            mismatches.append(
                f"candidate metadata {actual_candidate_metadata} != {CANDIDATE_METADATA_SHA256}"
            )
        if actual_dependency_lock != DEPENDENCY_LOCK_SHA256:
            mismatches.append(
                f"dependency lock {actual_dependency_lock} != {DEPENDENCY_LOCK_SHA256}"
            )
        if actual_archive != SOURCE_ARCHIVE_SHA256:
            mismatches.append(f"source archive {actual_archive} != {SOURCE_ARCHIVE_SHA256}")
        if evaluator_sha256 != EVALUATOR_AUTHORITY_SHA256:
            mismatches.append(
                f"evaluator authority {evaluator_sha256} != {EVALUATOR_AUTHORITY_SHA256}"
            )
        pure_crypto_module = evaluator_authority[
            "src/crypto_trade/tournament/pure_crypto_universe_v6.py"
        ]
        if pure_crypto_module != PURE_CRYPTO_MODULE_SHA256:
            mismatches.append(
                f"pure-crypto module {pure_crypto_module} != {PURE_CRYPTO_MODULE_SHA256}"
            )

        source_entries = [
            {"path": "README.md", "size": readme.stat().st_size, "sha256": actual_readme},
            {
                "path": "candidate.json",
                "size": candidate_metadata.stat().st_size,
                "sha256": actual_candidate_metadata,
            },
            {
                "path": "risk_policy.json",
                "size": risk_policy.stat().st_size,
                "sha256": actual_policy,
            },
            {"path": "strategy.py", "size": strategy.stat().st_size, "sha256": actual_strategy},
        ]
        source_payload = json.dumps(
            source_entries,
            separators=(",", ":"),
            sort_keys=True,
        ).encode("utf-8")
        actual_source_bundle = hashlib.sha256(source_payload).hexdigest()
        if actual_source_bundle != SOURCE_BUNDLE_SHA256:
            mismatches.append(
                f"source bundle {actual_source_bundle} != {SOURCE_BUNDLE_SHA256}"
            )
        archive_payload = json.loads(archive.read_text(encoding="utf-8"))
        if archive_payload.get("source_bundle_sha256") != SOURCE_BUNDLE_SHA256:
            mismatches.append("source archive carries the wrong source_bundle_sha256")
        if mismatches:
>           raise RuntimeError("Team 09 frozen-authority drift: " + "; ".join(mismatches))
E           RuntimeError: Team 09 frozen-authority drift: evaluator authority 7cb68129a2b4ad22eb779d996bee2762d341415c608217f30c1d814112ce35ae != ce1b13c81586e814e90a00f7f5020cc21d3bb7a3a5f9635702bce53c3873103d

src/crypto_trade/team09/authority.py:390: RuntimeError
___________________ test_snapshot_payload_hashes_are_checked ___________________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2e11ede10>

    def test_snapshot_payload_hashes_are_checked(monkeypatch: pytest.MonkeyPatch) -> None:
        real_sha256 = backtest.sha256_file

        def changed_parquet(path: str | Path) -> str:
            return "0" * 64 if Path(path).suffix == ".parquet" else real_sha256(path)

        monkeypatch.setattr(backtest, "sha256_file", changed_parquet)
        with pytest.raises(RuntimeError, match="snapshot hash drift"):
>           backtest.load_frozen_snapshot()

tests/team09/test_team09_pipeline.py:411:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/team09/backtest.py:117: in load_frozen_snapshot
    verify_frozen_authority(root)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = None

    def verify_frozen_authority(root: str | Path | None = None) -> FrozenAuthority:
        """Fail closed unless every locally load-bearing frozen identity is intact."""

        base = Path(root).resolve() if root is not None else repository_root()
        deployment = verify_deployment_authority(base)
        candidate = candidate_root(base)
        strategy = candidate / "strategy.py"
        risk_policy = candidate / "risk_policy.json"
        readme = candidate / "README.md"
        candidate_metadata = candidate / "candidate.json"
        dependency_lock = base / "uv.lock"
        archive = (
            base
            / "reports-top40-v4-r1"
            / "source-archives"
            / "sha256"
            / f"{SOURCE_ARCHIVE_SHA256}.json"
        )
        required = (
            strategy,
            risk_policy,
            readme,
            candidate_metadata,
            archive,
            dependency_lock,
        )
        missing = [str(path) for path in required if not path.is_file()]
        if missing:
            raise FileNotFoundError(f"Team 09 frozen authority is incomplete: {missing}")

        actual_strategy = sha256_file(strategy)
        actual_policy = sha256_file(risk_policy)
        actual_readme = sha256_file(readme)
        actual_candidate_metadata = sha256_file(candidate_metadata)
        actual_dependency_lock = sha256_file(dependency_lock)
        archive_present = archive.is_file()
        actual_archive = sha256_file(archive)
        evaluator_authority = {
            relative: sha256_file(base / relative) for relative in _EVALUATOR_AUTHORITY_PATHS
        }
        evaluator_sha256 = hashlib.sha256(
            json.dumps(
                evaluator_authority,
                allow_nan=False,
                ensure_ascii=True,
                separators=(",", ":"),
                sort_keys=True,
            ).encode("utf-8")
        ).hexdigest()
        mismatches: list[str] = []
        if actual_strategy != STRATEGY_SHA256:
            mismatches.append(f"strategy {actual_strategy} != {STRATEGY_SHA256}")
        if actual_policy != RISK_POLICY_SHA256:
            mismatches.append(f"risk policy {actual_policy} != {RISK_POLICY_SHA256}")
        if actual_readme != README_SHA256:
            mismatches.append(f"README {actual_readme} != {README_SHA256}")
        if actual_candidate_metadata != CANDIDATE_METADATA_SHA256:
            mismatches.append(
                f"candidate metadata {actual_candidate_metadata} != {CANDIDATE_METADATA_SHA256}"
            )
        if actual_dependency_lock != DEPENDENCY_LOCK_SHA256:
            mismatches.append(
                f"dependency lock {actual_dependency_lock} != {DEPENDENCY_LOCK_SHA256}"
            )
        if actual_archive != SOURCE_ARCHIVE_SHA256:
            mismatches.append(f"source archive {actual_archive} != {SOURCE_ARCHIVE_SHA256}")
        if evaluator_sha256 != EVALUATOR_AUTHORITY_SHA256:
            mismatches.append(
                f"evaluator authority {evaluator_sha256} != {EVALUATOR_AUTHORITY_SHA256}"
            )
        pure_crypto_module = evaluator_authority[
            "src/crypto_trade/tournament/pure_crypto_universe_v6.py"
        ]
        if pure_crypto_module != PURE_CRYPTO_MODULE_SHA256:
            mismatches.append(
                f"pure-crypto module {pure_crypto_module} != {PURE_CRYPTO_MODULE_SHA256}"
            )

        source_entries = [
            {"path": "README.md", "size": readme.stat().st_size, "sha256": actual_readme},
            {
                "path": "candidate.json",
                "size": candidate_metadata.stat().st_size,
                "sha256": actual_candidate_metadata,
            },
            {
                "path": "risk_policy.json",
                "size": risk_policy.stat().st_size,
                "sha256": actual_policy,
            },
            {"path": "strategy.py", "size": strategy.stat().st_size, "sha256": actual_strategy},
        ]
        source_payload = json.dumps(
            source_entries,
            separators=(",", ":"),
            sort_keys=True,
        ).encode("utf-8")
        actual_source_bundle = hashlib.sha256(source_payload).hexdigest()
        if actual_source_bundle != SOURCE_BUNDLE_SHA256:
            mismatches.append(
                f"source bundle {actual_source_bundle} != {SOURCE_BUNDLE_SHA256}"
            )
        archive_payload = json.loads(archive.read_text(encoding="utf-8"))
        if archive_payload.get("source_bundle_sha256") != SOURCE_BUNDLE_SHA256:
            mismatches.append("source archive carries the wrong source_bundle_sha256")
        if mismatches:
>           raise RuntimeError("Team 09 frozen-authority drift: " + "; ".join(mismatches))
E           RuntimeError: Team 09 frozen-authority drift: evaluator authority 7cb68129a2b4ad22eb779d996bee2762d341415c608217f30c1d814112ce35ae != ce1b13c81586e814e90a00f7f5020cc21d3bb7a3a5f9635702bce53c3873103d

src/crypto_trade/team09/authority.py:390: RuntimeError

During handling of the above exception, another exception occurred:

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2e11ede10>

    def test_snapshot_payload_hashes_are_checked(monkeypatch: pytest.MonkeyPatch) -> None:
        real_sha256 = backtest.sha256_file

        def changed_parquet(path: str | Path) -> str:
            return "0" * 64 if Path(path).suffix == ".parquet" else real_sha256(path)

        monkeypatch.setattr(backtest, "sha256_file", changed_parquet)
>       with pytest.raises(RuntimeError, match="snapshot hash drift"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'snapshot hash drift'
E         Actual message: 'Team 09 frozen-authority drift: evaluator authority 7cb68129a2b4ad22eb779d996bee2762d341415c608217f30c1d814112ce35ae != ce1b13c81586e814e90a00f7f5020cc21d3bb7a3a5f9635702bce53c3873103d'

tests/team09/test_team09_pipeline.py:410: AssertionError
_____________ test_released_all_period_statistics_match_tournament _____________

    def test_released_all_period_statistics_match_tournament() -> None:
        if not (release_team_root() / "daily_returns.csv").is_file():
            pytest.skip("local atomic tournament release is not present")
>       statistics = period_statistics(load_released_daily_returns())
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/team09/test_team09_pipeline.py:417:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/team09/report.py:37: in load_released_daily_returns
    verify_frozen_authority(root)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = None

    def verify_frozen_authority(root: str | Path | None = None) -> FrozenAuthority:
        """Fail closed unless every locally load-bearing frozen identity is intact."""

        base = Path(root).resolve() if root is not None else repository_root()
        deployment = verify_deployment_authority(base)
        candidate = candidate_root(base)
        strategy = candidate / "strategy.py"
        risk_policy = candidate / "risk_policy.json"
        readme = candidate / "README.md"
        candidate_metadata = candidate / "candidate.json"
        dependency_lock = base / "uv.lock"
        archive = (
            base
            / "reports-top40-v4-r1"
            / "source-archives"
            / "sha256"
            / f"{SOURCE_ARCHIVE_SHA256}.json"
        )
        required = (
            strategy,
            risk_policy,
            readme,
            candidate_metadata,
            archive,
            dependency_lock,
        )
        missing = [str(path) for path in required if not path.is_file()]
        if missing:
            raise FileNotFoundError(f"Team 09 frozen authority is incomplete: {missing}")

        actual_strategy = sha256_file(strategy)
        actual_policy = sha256_file(risk_policy)
        actual_readme = sha256_file(readme)
        actual_candidate_metadata = sha256_file(candidate_metadata)
        actual_dependency_lock = sha256_file(dependency_lock)
        archive_present = archive.is_file()
        actual_archive = sha256_file(archive)
        evaluator_authority = {
            relative: sha256_file(base / relative) for relative in _EVALUATOR_AUTHORITY_PATHS
        }
        evaluator_sha256 = hashlib.sha256(
            json.dumps(
                evaluator_authority,
                allow_nan=False,
                ensure_ascii=True,
                separators=(",", ":"),
                sort_keys=True,
            ).encode("utf-8")
        ).hexdigest()
        mismatches: list[str] = []
        if actual_strategy != STRATEGY_SHA256:
            mismatches.append(f"strategy {actual_strategy} != {STRATEGY_SHA256}")
        if actual_policy != RISK_POLICY_SHA256:
            mismatches.append(f"risk policy {actual_policy} != {RISK_POLICY_SHA256}")
        if actual_readme != README_SHA256:
            mismatches.append(f"README {actual_readme} != {README_SHA256}")
        if actual_candidate_metadata != CANDIDATE_METADATA_SHA256:
            mismatches.append(
                f"candidate metadata {actual_candidate_metadata} != {CANDIDATE_METADATA_SHA256}"
            )
        if actual_dependency_lock != DEPENDENCY_LOCK_SHA256:
            mismatches.append(
                f"dependency lock {actual_dependency_lock} != {DEPENDENCY_LOCK_SHA256}"
            )
        if actual_archive != SOURCE_ARCHIVE_SHA256:
            mismatches.append(f"source archive {actual_archive} != {SOURCE_ARCHIVE_SHA256}")
        if evaluator_sha256 != EVALUATOR_AUTHORITY_SHA256:
            mismatches.append(
                f"evaluator authority {evaluator_sha256} != {EVALUATOR_AUTHORITY_SHA256}"
            )
        pure_crypto_module = evaluator_authority[
            "src/crypto_trade/tournament/pure_crypto_universe_v6.py"
        ]
        if pure_crypto_module != PURE_CRYPTO_MODULE_SHA256:
            mismatches.append(
                f"pure-crypto module {pure_crypto_module} != {PURE_CRYPTO_MODULE_SHA256}"
            )

        source_entries = [
            {"path": "README.md", "size": readme.stat().st_size, "sha256": actual_readme},
            {
                "path": "candidate.json",
                "size": candidate_metadata.stat().st_size,
                "sha256": actual_candidate_metadata,
            },
            {
                "path": "risk_policy.json",
                "size": risk_policy.stat().st_size,
                "sha256": actual_policy,
            },
            {"path": "strategy.py", "size": strategy.stat().st_size, "sha256": actual_strategy},
        ]
        source_payload = json.dumps(
            source_entries,
            separators=(",", ":"),
            sort_keys=True,
        ).encode("utf-8")
        actual_source_bundle = hashlib.sha256(source_payload).hexdigest()
        if actual_source_bundle != SOURCE_BUNDLE_SHA256:
            mismatches.append(
                f"source bundle {actual_source_bundle} != {SOURCE_BUNDLE_SHA256}"
            )
        archive_payload = json.loads(archive.read_text(encoding="utf-8"))
        if archive_payload.get("source_bundle_sha256") != SOURCE_BUNDLE_SHA256:
            mismatches.append("source archive carries the wrong source_bundle_sha256")
        if mismatches:
>           raise RuntimeError("Team 09 frozen-authority drift: " + "; ".join(mismatches))
E           RuntimeError: Team 09 frozen-authority drift: evaluator authority 7cb68129a2b4ad22eb779d996bee2762d341415c608217f30c1d814112ce35ae != ce1b13c81586e814e90a00f7f5020cc21d3bb7a3a5f9635702bce53c3873103d

src/crypto_trade/team09/authority.py:390: RuntimeError
_ test_activation_sentinel_retires_parent_cli_but_preserves_historical_authority _

    def test_activation_sentinel_retires_parent_cli_but_preserves_historical_authority() -> None:
        assert integration.ACTIVATION_SENTINEL_PATH in phase0_v3._discover_repository_scope(ROOT)
        with pytest.raises(orchestrator_v3.OrchestratorError, match="Phase-0 verification failed"):
            orchestrator_v3._phase0_authority(ROOT)

>       authority = integration._historical_phase0_authority(ROOT)
                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_amendment_0001_integration.py:36:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')

    def _historical_phase0_authority(root: Path) -> phase0_v3.Phase0Authority:
        """Verify the parent record plus the exact sentinel and entrypoint-tombstone delta."""

        payload = orchestrator_v3._read_regular_bytes(root, phase0_v3.PHASE0_RECORD_PATH)
        output = orchestrator_v3._read_regular_bytes(
            root, orchestrator_v3.PHASE0_TEST_OUTPUT_PATH
        )
        if _sha256(payload) != PARENT_PHASE0_FILE_SHA256:
            raise Amendment0001IntegrationError("historical Phase-0 file hash changed")
        record = orchestrator_v3._strict_json_object(payload, "historical Phase-0 record")
        if orchestrator_v3._pretty_json_bytes(record) != payload:
            raise Amendment0001IntegrationError("historical Phase-0 record is not canonical")
        if record.get("record_sha256") != PARENT_PHASE0_RECORD_SHA256:
            raise Amendment0001IntegrationError("historical Phase-0 internal hash changed")
        raw_entries = record.get("scope_entries")
        if not isinstance(raw_entries, list) or not raw_entries:
            raise Amendment0001IntegrationError("historical Phase-0 scope is missing")
        if len(raw_entries) != 54 or not all(isinstance(entry, Mapping) for entry in raw_entries):
            raise Amendment0001IntegrationError("historical Phase-0 scope entries are malformed")
        recorded_paths = tuple(str(entry["path"]) for entry in raw_entries)
        historical_discovery = tuple(
            path for path in recorded_paths if _historically_discovered(path)
        )
        live_discovery = tuple(_FROZEN_DISCOVERY(root))
        if set(live_discovery) != set((*historical_discovery, ACTIVATION_SENTINEL_PATH)):
>           raise Amendment0001IntegrationError(
                "live Phase-0 discovery differs from the historical scope plus activation sentinel"
            )
E           crypto_trade.tournament.top40_amendment_0001_integration.Amendment0001IntegrationError: live Phase-0 discovery differs from the historical scope plus activation sentinel

src/crypto_trade/tournament/top40_amendment_0001_integration.py:325: Amendment0001IntegrationError
____ test_journal_boundary_accepts_a_normal_pending_post_activation_request ____

tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_journal_boundary_accepts_0')

    def test_journal_boundary_accepts_a_normal_pending_post_activation_request(
        tmp_path: Path,
    ) -> None:
        journal_path = tmp_path / orchestrator_v3.LAB_JOURNAL_PATH
        journal_path.parent.mkdir(parents=True)
        journal_path.write_bytes((ROOT / orchestrator_v3.LAB_JOURNAL_PATH).read_bytes())
        first = dict(journal_v3.replay_journal(journal_path).records[0])
        request_fields = {
            key: first[key]
            for key in (
                "team_id",
                "parent_candidate_id",
                "purpose",
                "source_bundle_sha256",
                "source_archive_path",
                "source_archive_sha256",
                "strategy_sha256",
                "dependency_lock_sha256",
                "config_sha256",
                "risk_policy_sha256",
                "data_authority_sha256",
                "evaluator_sha256",
                "seed",
                "material_parameters",
                "train_window",
                "cost_model",
            )
        }
>       state = journal_v3.append_request_accepted(
            journal_path,
            **request_fields,
            run_sequence=2,
            run_id="v3-team04-lab-000002-pending",
            candidate_id="team-04-utc-reference-001-v3-port",
            accepted_at_utc="2026-07-18T17:00:00Z",
            output_path="reports-top40-v3/labs/team-04/v3-team04-lab-000002-pending",
            cumulative_material_trial_count=2,
        )

tests/tournament/test_top40_amendment_0001_integration.py:100:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/journal_v3.py:662: in append_request_accepted
    return _append_validated_record(path, event)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/journal_v3.py:594: in _append_validated_record
    next_state = replay_journal_bytes(current_bytes + line)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

payload = b'{"accepted_at_utc":"2026-07-18T15:50:22Z","candidate_id":"team-04-utc-reference-001-v3-port","config_sha256":"fd24f2...,"team_id":"team-04","train_window":{"end_exclusive_utc":"2022-07-01T00:00:00Z","start_utc":"2020-02-03T00:00:00Z"}}\n'

    def replay_journal_bytes(payload: bytes) -> JournalState:
        """Validate and replay exact journal bytes, rejecting any ambiguity."""

        if not isinstance(payload, bytes):
            raise TypeError("payload must be bytes")
        if payload and not payload.endswith(b"\n"):
            raise JournalValidationError("journal ends with a truncated line")

        records: list[dict[str, Any]] = []
        requests: dict[str, dict[str, Any]] = {}
        terminal_requests: set[str] = set()
        run_ids: set[str] = set()
        team_sequences: dict[str, int] = {}
        trial_counts: dict[str, int] = {}
        previous_hash = GENESIS_SHA256
        previous_time: dt.datetime | None = None

        for event_sequence, line in enumerate(payload.splitlines(keepends=True), start=1):
            record = _decode_canonical_line(line, event_sequence)
            event_type = record.get("event_type")
            if event_type == "request_accepted":
                _validate_request_shape(record)
            elif event_type in _TERMINAL_TYPES:
                _validate_terminal_shape(record)
            else:
                raise JournalValidationError(f"journal line {event_sequence} has unknown event_type")

            if record["schema_version"] != SCHEMA_VERSION:
                raise JournalValidationError("journal schema_version is unsupported")
            if _integer(record["event_sequence"], "event_sequence", minimum=1) != event_sequence:
                raise JournalValidationError("journal event_sequence has a gap or duplicate")
            if record["previous_sha256"] != previous_hash:
                raise JournalValidationError("journal hash-chain predecessor mismatch")
            _hash(record["record_sha256"], "record_sha256")
            expected_hash = _record_sha256(record)
            if record["record_sha256"] != expected_hash:
                raise JournalValidationError("journal record hash mismatch")

            event_time = _event_time(record)
            if previous_time is not None and event_time < previous_time:
>               raise JournalValidationError("journal timestamps move backwards")
E               crypto_trade.tournament.journal_v3.JournalValidationError: journal timestamps move backwards

src/crypto_trade/tournament/journal_v3.py:454: JournalValidationError
__________ test_addendum_is_exactly_parented_to_frozen_amendment_0002 __________

    def test_addendum_is_exactly_parented_to_frozen_amendment_0002() -> None:
>       parent = validation.verify_activation(ROOT)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_amendment_0002_durability.py:83:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/top40_amendment_0002_validation.py:1314: in verify_activation
    parent = amendment_0001.verify_integration(root_path)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/top40_amendment_0001_integration.py:825: in verify_integration
    if dict(parent) != _parent_phase0(root_path):
                       ^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/top40_amendment_0001_integration.py:384: in _parent_phase0
    authority = _HISTORICAL_PHASE0_AUTHORITY(root)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')

    def _historical_phase0_authority(root: Path) -> phase0_v3.Phase0Authority:
        """Verify the parent record plus the exact sentinel and entrypoint-tombstone delta."""

        payload = orchestrator_v3._read_regular_bytes(root, phase0_v3.PHASE0_RECORD_PATH)
        output = orchestrator_v3._read_regular_bytes(
            root, orchestrator_v3.PHASE0_TEST_OUTPUT_PATH
        )
        if _sha256(payload) != PARENT_PHASE0_FILE_SHA256:
            raise Amendment0001IntegrationError("historical Phase-0 file hash changed")
        record = orchestrator_v3._strict_json_object(payload, "historical Phase-0 record")
        if orchestrator_v3._pretty_json_bytes(record) != payload:
            raise Amendment0001IntegrationError("historical Phase-0 record is not canonical")
        if record.get("record_sha256") != PARENT_PHASE0_RECORD_SHA256:
            raise Amendment0001IntegrationError("historical Phase-0 internal hash changed")
        raw_entries = record.get("scope_entries")
        if not isinstance(raw_entries, list) or not raw_entries:
            raise Amendment0001IntegrationError("historical Phase-0 scope is missing")
        if len(raw_entries) != 54 or not all(isinstance(entry, Mapping) for entry in raw_entries):
            raise Amendment0001IntegrationError("historical Phase-0 scope entries are malformed")
        recorded_paths = tuple(str(entry["path"]) for entry in raw_entries)
        historical_discovery = tuple(
            path for path in recorded_paths if _historically_discovered(path)
        )
        live_discovery = tuple(_FROZEN_DISCOVERY(root))
        if set(live_discovery) != set((*historical_discovery, ACTIVATION_SENTINEL_PATH)):
>           raise Amendment0001IntegrationError(
                "live Phase-0 discovery differs from the historical scope plus activation sentinel"
            )
E           crypto_trade.tournament.top40_amendment_0001_integration.Amendment0001IntegrationError: live Phase-0 discovery differs from the historical scope plus activation sentinel

src/crypto_trade/tournament/top40_amendment_0001_integration.py:325: Amendment0001IntegrationError
__________ test_live_cohort_reconstructs_exact_four_green_identities ___________

    def test_live_cohort_reconstructs_exact_four_green_identities() -> None:
>       cohort = validation.load_cohort(ROOT)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_amendment_0002_validation.py:155:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/top40_amendment_0002_validation.py:643: in load_cohort
    parent = amendment_0001.verify_integration(root_path)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/top40_amendment_0001_integration.py:825: in verify_integration
    if dict(parent) != _parent_phase0(root_path):
                       ^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/top40_amendment_0001_integration.py:384: in _parent_phase0
    authority = _HISTORICAL_PHASE0_AUTHORITY(root)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')

    def _historical_phase0_authority(root: Path) -> phase0_v3.Phase0Authority:
        """Verify the parent record plus the exact sentinel and entrypoint-tombstone delta."""

        payload = orchestrator_v3._read_regular_bytes(root, phase0_v3.PHASE0_RECORD_PATH)
        output = orchestrator_v3._read_regular_bytes(
            root, orchestrator_v3.PHASE0_TEST_OUTPUT_PATH
        )
        if _sha256(payload) != PARENT_PHASE0_FILE_SHA256:
            raise Amendment0001IntegrationError("historical Phase-0 file hash changed")
        record = orchestrator_v3._strict_json_object(payload, "historical Phase-0 record")
        if orchestrator_v3._pretty_json_bytes(record) != payload:
            raise Amendment0001IntegrationError("historical Phase-0 record is not canonical")
        if record.get("record_sha256") != PARENT_PHASE0_RECORD_SHA256:
            raise Amendment0001IntegrationError("historical Phase-0 internal hash changed")
        raw_entries = record.get("scope_entries")
        if not isinstance(raw_entries, list) or not raw_entries:
            raise Amendment0001IntegrationError("historical Phase-0 scope is missing")
        if len(raw_entries) != 54 or not all(isinstance(entry, Mapping) for entry in raw_entries):
            raise Amendment0001IntegrationError("historical Phase-0 scope entries are malformed")
        recorded_paths = tuple(str(entry["path"]) for entry in raw_entries)
        historical_discovery = tuple(
            path for path in recorded_paths if _historically_discovered(path)
        )
        live_discovery = tuple(_FROZEN_DISCOVERY(root))
        if set(live_discovery) != set((*historical_discovery, ACTIVATION_SENTINEL_PATH)):
>           raise Amendment0001IntegrationError(
                "live Phase-0 discovery differs from the historical scope plus activation sentinel"
            )
E           crypto_trade.tournament.top40_amendment_0001_integration.Amendment0001IntegrationError: live Phase-0 discovery differs from the historical scope plus activation sentinel

src/crypto_trade/tournament/top40_amendment_0001_integration.py:325: Amendment0001IntegrationError
______________ test_ledger_rejects_event_timestamps_in_the_future ______________

tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_ledger_rejects_event_time0')

    def test_ledger_rejects_event_timestamps_in_the_future(tmp_path):
        cli = _load_cli_module()
        ledger = tmp_path / "experiments.jsonl"
        rows = _events()
        rows[1]["timestamp_utc"] = "2026-07-14T00:00:00Z"
        _write_events(ledger, rows)
>       with pytest.raises(ValueError, match="cannot be in the future"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       Failed: DID NOT RAISE <class 'ValueError'>

tests/tournament/test_top40_cli.py:334: Failed
_____ test_real_research_status_preserves_all_tournament_and_report_bytes ______

capsys = <_pytest.capture.CaptureFixture object at 0x7fa2c86bc510>

    def test_real_research_status_preserves_all_tournament_and_report_bytes(
        capsys: pytest.CaptureFixture[str],
    ) -> None:
        root = Path.cwd().resolve()
        if not (root / "tournament/top40-v2/config.toml").is_file():
            pytest.skip("requires the Top40 V2 production worktree")
        before = _tree_hashes(root)

>       assert compat.run(["research-status", "team-03"], root=root) == 0
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_v2_amended_orchestrator_compat.py:149:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/amended_orchestrator_compat_v3.py:146: in run
    return _research_status_command(adapter, values, root=root_path)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/amended_orchestrator_compat_v3.py:96: in _research_status_command
    state, _state_bytes, _chain, _audit = authority["_read_amended_state_locked"](
src/crypto_trade/tournament/amendment_v2.py:2002: in _read_amended_state_locked
    authority = _phase0_authority(root, projection, config, clock=clock)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/amendment_v2.py:1267: in _phase0_authority
    return verify_phase0_authority(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
state_projection = {'config_path': 'tournament/top40-v2/config.toml', 'config_sha256': 'bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947', 'created_at_utc': '2026-07-15T15:28:34.550533+00:00', 'critic_confirmation_lock': None, ...}
config = LoadedV2Config(path=PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tournament/top40-v2/c...0.3, 'minimum_positive_quarter_fraction': 0.625, 'minimum_worst_regime_sharpe': -0.25, 'minimum_positive_regimes': 3}})
frozen_file_paths = ('TOURNAMENT-CHARTER-TOP40-V2.md', 'pyproject.toml', 'uv.lock', 'tournament/top40-v2/config.toml', 'tournament/top40-v2/README.md', 'tournament/top40-v2/PHASE0-POLICY.md', ...)

    def verify_phase0_authority(
        root: str | Path,
        state_projection: Mapping[str, Any],
        config: LoadedV2Config,
        frozen_file_paths: Sequence[str],
        *,
        clock: UtcClock = system_utc_now,
    ) -> Phase0Authority:
        root_path = Path(root).resolve()
        validate_run_state(state_projection, config)
        branch = git_text(root_path, "branch", "--show-current", label="current branch")
        if branch != TOP40_V2_LAYOUT.branch:
>           raise ValueError(f"current branch must be {TOP40_V2_LAYOUT.branch}")
E           ValueError: current branch must be quant-portfolio-blind-top40-v2

src/crypto_trade/tournament/amendment_integrity_v2.py:383: ValueError
____________ test_real_status_waits_for_the_result_transition_lock _____________

capsys = <_pytest.capture.CaptureFixture object at 0x7fa2e1188290>

    def test_real_status_waits_for_the_result_transition_lock(
        capsys: pytest.CaptureFixture[str],
    ) -> None:
        root = Path.cwd().resolve()
        if not (root / "tournament/top40-v2/config.toml").is_file():
            pytest.skip("requires the Top40 V2 production worktree")
        adapter = compat._load_frozen_adapter()
        state_lock = compat._locked_authority(adapter)["_state_lock"]

        with ThreadPoolExecutor(max_workers=1) as pool:
            with state_lock(root):
                future = pool.submit(compat.run, ["research-status", "team-01"], root=root)
                with pytest.raises(TimeoutError):
                    future.result(timeout=0.2)
>           assert future.result(timeout=20) == 0
                   ^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_v2_amended_orchestrator_compat.py:170:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
../../../.local/share/uv/python/cpython-3.13.12-linux-x86_64-gnu/lib/python3.13/concurrent/futures/_base.py:456: in result
    return self.__get_result()
           ^^^^^^^^^^^^^^^^^^^
../../../.local/share/uv/python/cpython-3.13.12-linux-x86_64-gnu/lib/python3.13/concurrent/futures/_base.py:401: in __get_result
    raise self._exception
../../../.local/share/uv/python/cpython-3.13.12-linux-x86_64-gnu/lib/python3.13/concurrent/futures/thread.py:59: in run
    result = self.fn(*self.args, **self.kwargs)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/amended_orchestrator_compat_v3.py:146: in run
    return _research_status_command(adapter, values, root=root_path)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/amended_orchestrator_compat_v3.py:96: in _research_status_command
    state, _state_bytes, _chain, _audit = authority["_read_amended_state_locked"](
src/crypto_trade/tournament/amendment_v2.py:2002: in _read_amended_state_locked
    authority = _phase0_authority(root, projection, config, clock=clock)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/amendment_v2.py:1267: in _phase0_authority
    return verify_phase0_authority(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
state_projection = {'config_path': 'tournament/top40-v2/config.toml', 'config_sha256': 'bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947', 'created_at_utc': '2026-07-15T15:28:34.550533+00:00', 'critic_confirmation_lock': None, ...}
config = LoadedV2Config(path=PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tournament/top40-v2/c...0.3, 'minimum_positive_quarter_fraction': 0.625, 'minimum_worst_regime_sharpe': -0.25, 'minimum_positive_regimes': 3}})
frozen_file_paths = ('TOURNAMENT-CHARTER-TOP40-V2.md', 'pyproject.toml', 'uv.lock', 'tournament/top40-v2/config.toml', 'tournament/top40-v2/README.md', 'tournament/top40-v2/PHASE0-POLICY.md', ...)

    def verify_phase0_authority(
        root: str | Path,
        state_projection: Mapping[str, Any],
        config: LoadedV2Config,
        frozen_file_paths: Sequence[str],
        *,
        clock: UtcClock = system_utc_now,
    ) -> Phase0Authority:
        root_path = Path(root).resolve()
        validate_run_state(state_projection, config)
        branch = git_text(root_path, "branch", "--show-current", label="current branch")
        if branch != TOP40_V2_LAYOUT.branch:
>           raise ValueError(f"current branch must be {TOP40_V2_LAYOUT.branch}")
E           ValueError: current branch must be quant-portfolio-blind-top40-v2

src/crypto_trade/tournament/amendment_integrity_v2.py:383: ValueError
__________ test_cli_and_schemas_expose_only_trusted_lifecycle_inputs ___________

capsys = <_pytest.capture.CaptureFixture object at 0x7fa2ff5dfac0>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_cli_and_schemas_expose_on0')

    def test_cli_and_schemas_expose_only_trusted_lifecycle_inputs(
        capsys: pytest.CaptureFixture[str],
        tmp_path: Path,
    ) -> None:
        script = REPOSITORY / "scripts/top40_v2_amendment_0001.py"
        spec = importlib.util.spec_from_file_location("top40_v2_amendment_cli_test", script)
        assert spec is not None and spec.loader is not None
        cli = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(cli)
        parser = cli.build_parser()
        assert parser.parse_args(["draft-amendment"]).command == "draft-amendment"
        assert parser.parse_args(["freeze-amendment"]).command == "freeze-amendment"
        assert parser.parse_args(
            ["run-diagnostic-backfill", "score-ic-team-01-rdf-ref-001"]
        ).command == "run-diagnostic-backfill"
        for forbidden in (
            ["draft-amendment", "--hold-reason", "caller supplied"],
            ["draft-amendment", "--started-at-utc", HOLD_START],
            ["status", "--json-out", "status.json"],
            ["resume-amendment", "--resumed-at-utc", HOLD_START],
            ["reserve-diagnostic-backfill", "request.json", "--reserved-at-utc", HOLD_START],
            ["record-diagnostic-backfill", "caller-result.json"],
        ):
            with pytest.raises(SystemExit):
                parser.parse_args(forbidden)
        cli._emit({"stdout_only": True})
        assert json.loads(capsys.readouterr().out) == {"stdout_only": True}
        invalid_request = tmp_path / "numeric-reservation.json"
        invalid_request.write_text(
            json.dumps(
                {
                    "schema_version": 1,
                    "diagnostic_id": 7,
                    "team_id": "team-01",
                    "candidate_id": 9,
                }
            ),
            encoding="utf-8",
        )
        with pytest.raises(ValueError, match="identifiers must be strings"):
            cli._reservation_input(str(invalid_request))

        template_root = REPOSITORY / amendment.AMENDMENT_ROOT / "templates"
        schemas = {
            path.name: json.loads(path.read_text(encoding="utf-8"))
            for path in sorted(template_root.glob("*.schema.json"))
        }
        assert set(schemas) == {
            "amendment-review.schema.json",
            "hold-notice.schema.json",
            "integration-manifest.schema.json",
            "diagnostic-backfill-reservation.schema.json",
            "diagnostic-backfill-result.schema.json",
        }
        assert all(schema["additionalProperties"] is False for schema in schemas.values())
        reservation = schemas["diagnostic-backfill-reservation.schema.json"]
        assert "target_path" not in reservation["properties"]
        result = schemas["diagnostic-backfill-result.schema.json"]
        assert "score" not in result["properties"]
        assert "diagnostic_outcomes" in result["properties"]
        assert "schema_version" not in result["properties"]
        integration = schemas["integration-manifest.schema.json"]
        assert integration["properties"]["score_calculation_implemented"]["const"] is True
        assert (
            integration["properties"]["diagnostic_engine"]["properties"]
            ["development_target_filename"]["const"]
            == "targets.parquet"
        )
        integration_files = set(
            integration["$defs"]["amendment_files"]["propertyNames"]["enum"]
        )
        diagnostic_files = set(
            integration["properties"]["diagnostic_engine"]["properties"]["files"]
            ["propertyNames"]["enum"]
        )
        orchestrator_files = set(
            integration["properties"]["amended_orchestrator"]["properties"]["files"]
            ["propertyNames"]["enum"]
        )
        assert integration_files == set(amendment.AMENDMENT_FILE_PATHS)
        assert diagnostic_files == set(amendment.DIAGNOSTIC_ENGINE_FILE_PATHS)
        assert orchestrator_files == set(amendment.AMENDED_ORCHESTRATOR_FILE_PATHS)
        assert integration["properties"]["required_backfills"]["items"] is False
        review_paths = set(
            review_path
            for review_path in schemas["amendment-review.schema.json"]["$defs"]["amendment_files"][
                "required"
            ]
        )
        assert review_paths == set(amendment.AMENDMENT_FILE_PATHS)
        assert amendment.CURRENT_INTEGRATION_BOUNDARY.edit_adapter_api == (
            "amendment_aware_legacy_state_edit"
        )
        assert amendment.CURRENT_INTEGRATION_BOUNDARY.read_adapter_api == (
            "read_amendment_aware_legacy_state"
        )
>       assert not (REPOSITORY / amendment.HOLD_NOTICE_PATH).exists()
E       AssertionError: assert not True
E        +  where True = exists()
E        +    where exists = (PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50') / 'tournament/top40-v2/amendments/0001/HOLD-NOTICE.json').exists
E        +      where 'tournament/top40-v2/amendments/0001/HOLD-NOTICE.json' = amendment.HOLD_NOTICE_PATH

tests/tournament/test_top40_v2_amendment_0001.py:1504: AssertionError
_______________ test_cli_parser_exposes_only_reviewed_operations _______________

    def test_cli_parser_exposes_only_reviewed_operations() -> None:
>       import scripts.top40_v2_amendment_0002 as cli
E       ModuleNotFoundError: No module named 'scripts.top40_v2_amendment_0002'

tests/tournament/test_top40_v2_amendment_0002.py:264: ModuleNotFoundError
__________ test_real_active_a6_parent_and_pure_crypto_audit_are_bound __________

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
binding = SnapshotFileBinding(name='contract_metadata', path='data/top40/snapshot-v1/contract_metadata.parquet', sha256='0995d50011e73de880358b994031fdf8bb58e75ab8901de2c5601e7abed05243', size=15984, rows=667, require_single_link=False)

    def _read_bound_file(root: Path, binding: SnapshotFileBinding) -> bytes:
        try:
>           relative, _path, payload, info = _READ_REPO_FILE(
                root,
                binding.path,
                f"pure-crypto {binding.name}",
                maximum_bytes=binding.size,
                # The frozen binary snapshot is deliberately shared by hard link across the
                # tournament worktrees.  Descriptor-relative O_NOFOLLOW reads, stable fstat
                # identity, exact size, and exact SHA-256 remain mandatory.  Repository-owned
                # amendment/manifest authorities are single-link files.
                require_single_link=binding.require_single_link,
            )

src/crypto_trade/tournament/pure_crypto_universe_v6.py:346:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/amendment_integrity_v2.py:221: in read_repo_file
    parent_fd, opened = _open_directory_chain(root, parts[:-1])
                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
parts = ('data', 'top40', 'snapshot-v1')

    def _open_directory_chain(root: Path, parts: Sequence[str]) -> tuple[int, list[int]]:
        flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0) | getattr(os, "O_CLOEXEC", 0)
        root_fd = os.open(root, flags)
        opened = [root_fd]
        current = root_fd
        try:
            for part in parts:
>               descriptor = os.open(
                    part,
                    flags | getattr(os, "O_NOFOLLOW", 0),
                    dir_fd=current,
                )
E               FileNotFoundError: [Errno 2] No such file or directory: 'top40'

src/crypto_trade/tournament/amendment_integrity_v2.py:195: FileNotFoundError

The above exception was the direct cause of the following exception:

    def test_real_active_a6_parent_and_pure_crypto_audit_are_bound() -> None:
        amendment.verify_parent_authorities(ROOT)
>       report = amendment._verified_pure_crypto_audit(ROOT)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_v2_amendment_0005.py:338:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/amendment_0005_v2.py:425: in _verified_pure_crypto_audit
    report = _A6_REPORT_BYTES(root)
             ^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/pure_crypto_universe_v6.py:901: in audit_report_bytes
    return _PRETTY_JSON_BYTES(audit_pure_crypto_universe(root))
                              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/pure_crypto_universe_v6.py:757: in audit_pure_crypto_universe
    binding.name: _read_bound_file(root_path, binding) for binding in SNAPSHOT_FILE_BINDINGS
                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
binding = SnapshotFileBinding(name='contract_metadata', path='data/top40/snapshot-v1/contract_metadata.parquet', sha256='0995d50011e73de880358b994031fdf8bb58e75ab8901de2c5601e7abed05243', size=15984, rows=667, require_single_link=False)

    def _read_bound_file(root: Path, binding: SnapshotFileBinding) -> bytes:
        try:
            relative, _path, payload, info = _READ_REPO_FILE(
                root,
                binding.path,
                f"pure-crypto {binding.name}",
                maximum_bytes=binding.size,
                # The frozen binary snapshot is deliberately shared by hard link across the
                # tournament worktrees.  Descriptor-relative O_NOFOLLOW reads, stable fstat
                # identity, exact size, and exact SHA-256 remain mandatory.  Repository-owned
                # amendment/manifest authorities are single-link files.
                require_single_link=binding.require_single_link,
            )
        except (OSError, ValueError) as exc:
>           raise PureCryptoUniverseError(f"cannot read bound {binding.name}: {exc}") from exc
E           crypto_trade.tournament.pure_crypto_universe_v6.PureCryptoUniverseError: cannot read bound contract_metadata: [Errno 2] No such file or directory: 'top40'

src/crypto_trade/tournament/pure_crypto_universe_v6.py:358: PureCryptoUniverseError
_________ test_real_frozen_snapshot_passes_with_exact_pure_crypto_sets _________

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
binding = SnapshotFileBinding(name='contract_metadata', path='data/top40/snapshot-v1/contract_metadata.parquet', sha256='0995d50011e73de880358b994031fdf8bb58e75ab8901de2c5601e7abed05243', size=15984, rows=667, require_single_link=False)

    def _read_bound_file(root: Path, binding: SnapshotFileBinding) -> bytes:
        try:
>           relative, _path, payload, info = _READ_REPO_FILE(
                root,
                binding.path,
                f"pure-crypto {binding.name}",
                maximum_bytes=binding.size,
                # The frozen binary snapshot is deliberately shared by hard link across the
                # tournament worktrees.  Descriptor-relative O_NOFOLLOW reads, stable fstat
                # identity, exact size, and exact SHA-256 remain mandatory.  Repository-owned
                # amendment/manifest authorities are single-link files.
                require_single_link=binding.require_single_link,
            )

src/crypto_trade/tournament/pure_crypto_universe_v6.py:346:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/amendment_integrity_v2.py:221: in read_repo_file
    parent_fd, opened = _open_directory_chain(root, parts[:-1])
                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
parts = ('data', 'top40', 'snapshot-v1')

    def _open_directory_chain(root: Path, parts: Sequence[str]) -> tuple[int, list[int]]:
        flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0) | getattr(os, "O_CLOEXEC", 0)
        root_fd = os.open(root, flags)
        opened = [root_fd]
        current = root_fd
        try:
            for part in parts:
>               descriptor = os.open(
                    part,
                    flags | getattr(os, "O_NOFOLLOW", 0),
                    dir_fd=current,
                )
E               FileNotFoundError: [Errno 2] No such file or directory: 'top40'

src/crypto_trade/tournament/amendment_integrity_v2.py:195: FileNotFoundError

The above exception was the direct cause of the following exception:

    def test_real_frozen_snapshot_passes_with_exact_pure_crypto_sets() -> None:
>       report = universe.audit_pure_crypto_universe(ROOT)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_v2_amendment_0006.py:43:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/pure_crypto_universe_v6.py:757: in audit_pure_crypto_universe
    binding.name: _read_bound_file(root_path, binding) for binding in SNAPSHOT_FILE_BINDINGS
                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
binding = SnapshotFileBinding(name='contract_metadata', path='data/top40/snapshot-v1/contract_metadata.parquet', sha256='0995d50011e73de880358b994031fdf8bb58e75ab8901de2c5601e7abed05243', size=15984, rows=667, require_single_link=False)

    def _read_bound_file(root: Path, binding: SnapshotFileBinding) -> bytes:
        try:
            relative, _path, payload, info = _READ_REPO_FILE(
                root,
                binding.path,
                f"pure-crypto {binding.name}",
                maximum_bytes=binding.size,
                # The frozen binary snapshot is deliberately shared by hard link across the
                # tournament worktrees.  Descriptor-relative O_NOFOLLOW reads, stable fstat
                # identity, exact size, and exact SHA-256 remain mandatory.  Repository-owned
                # amendment/manifest authorities are single-link files.
                require_single_link=binding.require_single_link,
            )
        except (OSError, ValueError) as exc:
>           raise PureCryptoUniverseError(f"cannot read bound {binding.name}: {exc}") from exc
E           crypto_trade.tournament.pure_crypto_universe_v6.PureCryptoUniverseError: cannot read bound contract_metadata: [Errno 2] No such file or directory: 'top40'

src/crypto_trade/tournament/pure_crypto_universe_v6.py:358: PureCryptoUniverseError
________________ test_canonical_report_bytes_are_deterministic _________________

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
binding = SnapshotFileBinding(name='contract_metadata', path='data/top40/snapshot-v1/contract_metadata.parquet', sha256='0995d50011e73de880358b994031fdf8bb58e75ab8901de2c5601e7abed05243', size=15984, rows=667, require_single_link=False)

    def _read_bound_file(root: Path, binding: SnapshotFileBinding) -> bytes:
        try:
>           relative, _path, payload, info = _READ_REPO_FILE(
                root,
                binding.path,
                f"pure-crypto {binding.name}",
                maximum_bytes=binding.size,
                # The frozen binary snapshot is deliberately shared by hard link across the
                # tournament worktrees.  Descriptor-relative O_NOFOLLOW reads, stable fstat
                # identity, exact size, and exact SHA-256 remain mandatory.  Repository-owned
                # amendment/manifest authorities are single-link files.
                require_single_link=binding.require_single_link,
            )

src/crypto_trade/tournament/pure_crypto_universe_v6.py:346:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/amendment_integrity_v2.py:221: in read_repo_file
    parent_fd, opened = _open_directory_chain(root, parts[:-1])
                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
parts = ('data', 'top40', 'snapshot-v1')

    def _open_directory_chain(root: Path, parts: Sequence[str]) -> tuple[int, list[int]]:
        flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0) | getattr(os, "O_CLOEXEC", 0)
        root_fd = os.open(root, flags)
        opened = [root_fd]
        current = root_fd
        try:
            for part in parts:
>               descriptor = os.open(
                    part,
                    flags | getattr(os, "O_NOFOLLOW", 0),
                    dir_fd=current,
                )
E               FileNotFoundError: [Errno 2] No such file or directory: 'top40'

src/crypto_trade/tournament/amendment_integrity_v2.py:195: FileNotFoundError

The above exception was the direct cause of the following exception:

    def test_canonical_report_bytes_are_deterministic() -> None:
>       first = universe.audit_report_bytes(ROOT)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_v2_amendment_0006.py:99:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src/crypto_trade/tournament/pure_crypto_universe_v6.py:901: in audit_report_bytes
    return _PRETTY_JSON_BYTES(audit_pure_crypto_universe(root))
                              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/crypto_trade/tournament/pure_crypto_universe_v6.py:757: in audit_pure_crypto_universe
    binding.name: _read_bound_file(root_path, binding) for binding in SNAPSHOT_FILE_BINDINGS
                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
binding = SnapshotFileBinding(name='contract_metadata', path='data/top40/snapshot-v1/contract_metadata.parquet', sha256='0995d50011e73de880358b994031fdf8bb58e75ab8901de2c5601e7abed05243', size=15984, rows=667, require_single_link=False)

    def _read_bound_file(root: Path, binding: SnapshotFileBinding) -> bytes:
        try:
            relative, _path, payload, info = _READ_REPO_FILE(
                root,
                binding.path,
                f"pure-crypto {binding.name}",
                maximum_bytes=binding.size,
                # The frozen binary snapshot is deliberately shared by hard link across the
                # tournament worktrees.  Descriptor-relative O_NOFOLLOW reads, stable fstat
                # identity, exact size, and exact SHA-256 remain mandatory.  Repository-owned
                # amendment/manifest authorities are single-link files.
                require_single_link=binding.require_single_link,
            )
        except (OSError, ValueError) as exc:
>           raise PureCryptoUniverseError(f"cannot read bound {binding.name}: {exc}") from exc
E           crypto_trade.tournament.pure_crypto_universe_v6.PureCryptoUniverseError: cannot read bound contract_metadata: [Errno 2] No such file or directory: 'top40'

src/crypto_trade/tournament/pure_crypto_universe_v6.py:358: PureCryptoUniverseError
______ test_run_window_development_closes_journal_and_archives_artifacts _______

cli = <module 'top40_v2_cli_for_test' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py'>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_run_window_development_cl0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2c83b06e0>

    def test_run_window_development_closes_journal_and_archives_artifacts(
        cli, tmp_path, monkeypatch
    ):
        _initialize(cli, tmp_path, monkeypatch)
        _open_research(tmp_path)
        _register_initial_family(cli, tmp_path)
        config = load_config(tmp_path / CONFIG)
>       _register_pending_candidate(
            cli, tmp_path, config, candidate_id="candidate-window"
        )

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:501:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:315: in _register_pending_candidate
    cli._register_trial(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py:645: in _register_trial
    plan = plan_registration_append(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:421: in plan_registration_append
    return _plan_event_append(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

journal_bytes = b'{"event_type":"genesis","payload":{"maximum_cpu_hours_per_team":12.0,"maximum_material_configurations_per_team":80,"...fedeb22f07802f6fedb1f279f0cd84726384","schema_version":2,"sequence":0,"timestamp_utc":"2026-08-17T10:24:58.532320Z"}\n'
event_bytes = b'{"candidate_id":"candidate-window","config_sha256":"bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947...-01","thesis":"A causal spread should survive conservative execution costs.","timestamp_utc":"2026-07-20T10:00:00Z"}\n'
team_ledger_bytes = b''

    def _plan_event_append(
        journal_bytes: bytes,
        event_bytes: bytes,
        team_ledger_bytes: bytes,
        *,
        event: dict[str, Any],
        policy: ResearchPolicy,
    ) -> PlannedJournalAppend:
        state = validate_journal_bytes(journal_bytes, policy)
        team_id = str(event["team_id"])
        validate_team_ledger_bytes(team_ledger_bytes, state, team_id)
        team = state.teams[team_id]
        timestamp = _validate_timestamp(event["timestamp_utc"], "planned event timestamp_utc")
        if timestamp < _validate_timestamp(state.last_timestamp_utc, "journal last timestamp_utc"):
>           raise ResearchAccountingError("planned event timestamp precedes journal head")
E           crypto_trade.tournament.research_v2.ResearchAccountingError: planned event timestamp precedes journal head

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:622: ResearchAccountingError
----------------------------- Captured stdout call -----------------------------
{
  "initialized": 10,
  "phase": "phase0_pending",
  "research_journal_genesis_sha256": "cb264ccfd8235cb428b113f2c939fedeb22f07802f6fedb1f279f0cd84726384",
  "research_journal_path": "tournament/top40-v2/organizer_research_journal.jsonl",
  "state_path": "tournament/top40-v2/run_state.json"
}
{
  "event": "registered",
  "family_id": "family-1",
  "team_id": "team-01"
}
_____ test_run_window_private_consumes_ticket_and_emits_only_gate_feedback _____

cli = <module 'top40_v2_cli_for_test' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py'>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_run_window_private_consum0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2c840af90>

    def test_run_window_private_consumes_ticket_and_emits_only_gate_feedback(
        cli, tmp_path, monkeypatch
    ):
        _initialize(cli, tmp_path, monkeypatch)
        _open_research(tmp_path)
        _register_initial_family(cli, tmp_path)
        config = load_config(tmp_path / CONFIG)
>       _register_completed_candidate(cli, tmp_path, config, candidate_id="candidate-1")

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:564:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:264: in _register_completed_candidate
    assert cli._register_trial(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py:645: in _register_trial
    plan = plan_registration_append(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:421: in plan_registration_append
    return _plan_event_append(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

journal_bytes = b'{"event_type":"genesis","payload":{"maximum_cpu_hours_per_team":12.0,"maximum_material_configurations_per_team":80,"...44c7aabf5beafa72696d40306b69fbd19d2e","schema_version":2,"sequence":0,"timestamp_utc":"2026-08-17T10:24:58.779762Z"}\n'
event_bytes = b'{"candidate_id":"candidate-1","config_sha256":"bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947","ev...thesis":"A causal cross-sectional spread should survive conservative costs.","timestamp_utc":"2026-07-17T10:00:00Z"}\n'
team_ledger_bytes = b''

    def _plan_event_append(
        journal_bytes: bytes,
        event_bytes: bytes,
        team_ledger_bytes: bytes,
        *,
        event: dict[str, Any],
        policy: ResearchPolicy,
    ) -> PlannedJournalAppend:
        state = validate_journal_bytes(journal_bytes, policy)
        team_id = str(event["team_id"])
        validate_team_ledger_bytes(team_ledger_bytes, state, team_id)
        team = state.teams[team_id]
        timestamp = _validate_timestamp(event["timestamp_utc"], "planned event timestamp_utc")
        if timestamp < _validate_timestamp(state.last_timestamp_utc, "journal last timestamp_utc"):
>           raise ResearchAccountingError("planned event timestamp precedes journal head")
E           crypto_trade.tournament.research_v2.ResearchAccountingError: planned event timestamp precedes journal head

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:622: ResearchAccountingError
----------------------------- Captured stdout call -----------------------------
{
  "initialized": 10,
  "phase": "phase0_pending",
  "research_journal_genesis_sha256": "6f8fcc7416c75208613240f10eb344c7aabf5beafa72696d40306b69fbd19d2e",
  "research_journal_path": "tournament/top40-v2/organizer_research_journal.jsonl",
  "state_path": "tournament/top40-v2/run_state.json"
}
{
  "event": "registered",
  "family_id": "family-1",
  "team_id": "team-01"
}
_ test_failed_development_is_research_not_submission_then_passing_candidate_freezes _

cli = <module 'top40_v2_cli_for_test' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py'>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_failed_development_is_res0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2c831dfd0>

    def test_failed_development_is_research_not_submission_then_passing_candidate_freezes(
        cli, tmp_path, monkeypatch
    ):
        _initialize(cli, tmp_path, monkeypatch)
        _open_research(tmp_path)
        _register_initial_family(cli, tmp_path)
        config = load_config(tmp_path / CONFIG)
>       _register_completed_candidate(cli, tmp_path, config, candidate_id="candidate-1")

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:615:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:264: in _register_completed_candidate
    assert cli._register_trial(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py:645: in _register_trial
    plan = plan_registration_append(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:421: in plan_registration_append
    return _plan_event_append(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

journal_bytes = b'{"event_type":"genesis","payload":{"maximum_cpu_hours_per_team":12.0,"maximum_material_configurations_per_team":80,"...ca301d6b6d16408021dc9f75cbfedbc33389","schema_version":2,"sequence":0,"timestamp_utc":"2026-08-17T10:24:59.029929Z"}\n'
event_bytes = b'{"candidate_id":"candidate-1","config_sha256":"bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947","ev...thesis":"A causal cross-sectional spread should survive conservative costs.","timestamp_utc":"2026-07-17T10:00:00Z"}\n'
team_ledger_bytes = b''

    def _plan_event_append(
        journal_bytes: bytes,
        event_bytes: bytes,
        team_ledger_bytes: bytes,
        *,
        event: dict[str, Any],
        policy: ResearchPolicy,
    ) -> PlannedJournalAppend:
        state = validate_journal_bytes(journal_bytes, policy)
        team_id = str(event["team_id"])
        validate_team_ledger_bytes(team_ledger_bytes, state, team_id)
        team = state.teams[team_id]
        timestamp = _validate_timestamp(event["timestamp_utc"], "planned event timestamp_utc")
        if timestamp < _validate_timestamp(state.last_timestamp_utc, "journal last timestamp_utc"):
>           raise ResearchAccountingError("planned event timestamp precedes journal head")
E           crypto_trade.tournament.research_v2.ResearchAccountingError: planned event timestamp precedes journal head

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:622: ResearchAccountingError
----------------------------- Captured stdout call -----------------------------
{
  "initialized": 10,
  "phase": "phase0_pending",
  "research_journal_genesis_sha256": "8a2984861053c31d6ddf91e89920ca301d6b6d16408021dc9f75cbfedbc33389",
  "research_journal_path": "tournament/top40-v2/organizer_research_journal.jsonl",
  "state_path": "tournament/top40-v2/run_state.json"
}
{
  "event": "registered",
  "family_id": "family-1",
  "team_id": "team-01"
}
_ test_private_ticket_is_single_shot_and_public_state_contains_no_observations _

cli = <module 'top40_v2_cli_for_test' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py'>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_private_ticket_is_single_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa341a2d550>

    def test_private_ticket_is_single_shot_and_public_state_contains_no_observations(
        cli, tmp_path, monkeypatch
    ):
        _initialize(cli, tmp_path, monkeypatch)
        _open_research(tmp_path)
        _register_initial_family(cli, tmp_path)
        config = load_config(tmp_path / CONFIG)
>       _register_completed_candidate(cli, tmp_path, config, candidate_id="candidate-1")

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:687:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:264: in _register_completed_candidate
    assert cli._register_trial(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py:645: in _register_trial
    plan = plan_registration_append(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:421: in plan_registration_append
    return _plan_event_append(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

journal_bytes = b'{"event_type":"genesis","payload":{"maximum_cpu_hours_per_team":12.0,"maximum_material_configurations_per_team":80,"...48eb19dd5d31870956443455dacfa87539f5","schema_version":2,"sequence":0,"timestamp_utc":"2026-08-17T10:24:59.451183Z"}\n'
event_bytes = b'{"candidate_id":"candidate-1","config_sha256":"bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947","ev...thesis":"A causal cross-sectional spread should survive conservative costs.","timestamp_utc":"2026-07-17T10:00:00Z"}\n'
team_ledger_bytes = b''

    def _plan_event_append(
        journal_bytes: bytes,
        event_bytes: bytes,
        team_ledger_bytes: bytes,
        *,
        event: dict[str, Any],
        policy: ResearchPolicy,
    ) -> PlannedJournalAppend:
        state = validate_journal_bytes(journal_bytes, policy)
        team_id = str(event["team_id"])
        validate_team_ledger_bytes(team_ledger_bytes, state, team_id)
        team = state.teams[team_id]
        timestamp = _validate_timestamp(event["timestamp_utc"], "planned event timestamp_utc")
        if timestamp < _validate_timestamp(state.last_timestamp_utc, "journal last timestamp_utc"):
>           raise ResearchAccountingError("planned event timestamp precedes journal head")
E           crypto_trade.tournament.research_v2.ResearchAccountingError: planned event timestamp precedes journal head

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:622: ResearchAccountingError
----------------------------- Captured stdout call -----------------------------
{
  "initialized": 10,
  "phase": "phase0_pending",
  "research_journal_genesis_sha256": "7c1fab086ab5090f4575310b7f3c48eb19dd5d31870956443455dacfa87539f5",
  "research_journal_path": "tournament/top40-v2/organizer_research_journal.jsonl",
  "state_path": "tournament/top40-v2/run_state.json"
}
{
  "event": "registered",
  "family_id": "family-1",
  "team_id": "team-01"
}
____ test_private_ticket_rejects_any_post_development_source_bundle_change _____

cli = <module 'top40_v2_cli_for_test' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py'>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_private_ticket_rejects_an0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2c8486900>

    def test_private_ticket_rejects_any_post_development_source_bundle_change(
        cli, tmp_path, monkeypatch
    ):
        _initialize(cli, tmp_path, monkeypatch)
        _open_research(tmp_path)
        _register_initial_family(cli, tmp_path)
        config = load_config(tmp_path / CONFIG)
>       _register_completed_candidate(cli, tmp_path, config, candidate_id="candidate-1")

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:755:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:264: in _register_completed_candidate
    assert cli._register_trial(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py:645: in _register_trial
    plan = plan_registration_append(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:421: in plan_registration_append
    return _plan_event_append(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

journal_bytes = b'{"event_type":"genesis","payload":{"maximum_cpu_hours_per_team":12.0,"maximum_material_configurations_per_team":80,"...f79b303b784b059665a939f8870b9321500c","schema_version":2,"sequence":0,"timestamp_utc":"2026-08-17T10:24:59.864904Z"}\n'
event_bytes = b'{"candidate_id":"candidate-1","config_sha256":"bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947","ev...thesis":"A causal cross-sectional spread should survive conservative costs.","timestamp_utc":"2026-07-17T10:00:00Z"}\n'
team_ledger_bytes = b''

    def _plan_event_append(
        journal_bytes: bytes,
        event_bytes: bytes,
        team_ledger_bytes: bytes,
        *,
        event: dict[str, Any],
        policy: ResearchPolicy,
    ) -> PlannedJournalAppend:
        state = validate_journal_bytes(journal_bytes, policy)
        team_id = str(event["team_id"])
        validate_team_ledger_bytes(team_ledger_bytes, state, team_id)
        team = state.teams[team_id]
        timestamp = _validate_timestamp(event["timestamp_utc"], "planned event timestamp_utc")
        if timestamp < _validate_timestamp(state.last_timestamp_utc, "journal last timestamp_utc"):
>           raise ResearchAccountingError("planned event timestamp precedes journal head")
E           crypto_trade.tournament.research_v2.ResearchAccountingError: planned event timestamp precedes journal head

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:622: ResearchAccountingError
----------------------------- Captured stdout call -----------------------------
{
  "initialized": 10,
  "phase": "phase0_pending",
  "research_journal_genesis_sha256": "030a85bcdb2deaa3f9762732dd0af79b303b784b059665a939f8870b9321500c",
  "research_journal_path": "tournament/top40-v2/organizer_research_journal.jsonl",
  "state_path": "tournament/top40-v2/run_state.json"
}
{
  "event": "registered",
  "family_id": "family-1",
  "team_id": "team-01"
}
__________ test_close_and_finalist_lock_advance_only_qualified_teams ___________

cli = <module 'top40_v2_cli_for_test' from '/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py'>
tmp_path = PosixPath('/tmp/pytest-of-roberto/pytest-518/test_close_and_finalist_lock_a0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2c846f850>

    def test_close_and_finalist_lock_advance_only_qualified_teams(cli, tmp_path, monkeypatch):
        _initialize(cli, tmp_path, monkeypatch)
        _open_research(tmp_path)
>       _qualify_team_one(cli, tmp_path, monkeypatch)

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:783:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:426: in _qualify_team_one
    _register_completed_candidate(cli, root, config, candidate_id="candidate-1")
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tests/tournament/test_top40_v2_cli.py:264: in _register_completed_candidate
    assert cli._register_trial(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/scripts/top40_v2_tournament.py:645: in _register_trial
    plan = plan_registration_append(
/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:421: in plan_registration_append
    return _plan_event_append(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

journal_bytes = b'{"event_type":"genesis","payload":{"maximum_cpu_hours_per_team":12.0,"maximum_material_configurations_per_team":80,"...719b6d7f29af13eda4561b5e5c0a1bb8f157","schema_version":2,"sequence":0,"timestamp_utc":"2026-08-17T10:25:00.107936Z"}\n'
event_bytes = b'{"candidate_id":"candidate-1","config_sha256":"bd0e4f70bcf335b2ac4e44ec049b89b8e8eaf4c6ac6502574d0accf12d9d9947","ev...thesis":"A causal cross-sectional spread should survive conservative costs.","timestamp_utc":"2026-07-17T10:00:00Z"}\n'
team_ledger_bytes = b''

    def _plan_event_append(
        journal_bytes: bytes,
        event_bytes: bytes,
        team_ledger_bytes: bytes,
        *,
        event: dict[str, Any],
        policy: ResearchPolicy,
    ) -> PlannedJournalAppend:
        state = validate_journal_bytes(journal_bytes, policy)
        team_id = str(event["team_id"])
        validate_team_ledger_bytes(team_ledger_bytes, state, team_id)
        team = state.teams[team_id]
        timestamp = _validate_timestamp(event["timestamp_utc"], "planned event timestamp_utc")
        if timestamp < _validate_timestamp(state.last_timestamp_utc, "journal last timestamp_utc"):
>           raise ResearchAccountingError("planned event timestamp precedes journal head")
E           crypto_trade.tournament.research_v2.ResearchAccountingError: planned event timestamp precedes journal head

/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/tournament/research_v2.py:622: ResearchAccountingError
----------------------------- Captured stdout call -----------------------------
{
  "initialized": 10,
  "phase": "phase0_pending",
  "research_journal_genesis_sha256": "8563c43de9c27b4b020b03774d26719b6d7f29af13eda4561b5e5c0a1bb8f157",
  "research_journal_path": "tournament/top40-v2/organizer_research_journal.jsonl",
  "state_path": "tournament/top40-v2/run_state.json"
}
{
  "event": "registered",
  "family_id": "family-1",
  "team_id": "team-01"
}
_________ test_production_phase0_snapshot_path_accepts_schema3_facade __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fa2c84558d0>

    def test_production_phase0_snapshot_path_accepts_schema3_facade(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        root = _production_root()
        manifest_path = root / "tournament/top40/data_manifest.json"
        manifest_text = manifest_path.read_text(encoding="utf-8")
        manifest = json.loads(manifest_text)

        def callback(_argv: Any, _root: Any) -> int:
            accepted = compat.runner_v2._v2_phase0_allows_fast_snapshot_verification(
                root,
                manifest_path,
                manifest,
                manifest_text,
            )
            return int(accepted)

        _install_fake_adapter(monkeypatch, callback)
>       assert _run_development(root) == 1
               ^^^^^^^^^^^^^^^^^^^^^^

tests/tournament/test_top40_v2_runner_schema3_compat.py:164:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
tests/tournament/test_top40_v2_runner_schema3_compat.py:40: in _run_development
    return compat.run_development_window_with_schema3_compatibility(
src/crypto_trade/tournament/runner_schema3_compat_v4.py:245: in run_development_window_with_schema3_compatibility
    raise failure.with_traceback(failure.__traceback__)
src/crypto_trade/tournament/runner_schema3_compat_v4.py:212: in run_development_window_with_schema3_compatibility
    result = runner_call()
             ^^^^^^^^^^^^^
src/crypto_trade/tournament/runner_schema3_compat_v4.py:144: in runner_call
    return int(adapter.run(values, root=root_path))
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/tournament/test_top40_v2_runner_schema3_compat.py:33: in run
    return callback(argv, root)
           ^^^^^^^^^^^^^^^^^^^^
tests/tournament/test_top40_v2_runner_schema3_compat.py:155: in callback
    accepted = compat.runner_v2._v2_phase0_allows_fast_snapshot_verification(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

root = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50')
manifest_path = PosixPath('/home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/tournament/top40/data_manifest.json')
manifest = {'files': [{'name': 'archive_provenance', 'path': 'data/top40/snapshot-v1/archive_provenance.jsonl', 'rows': 44977, 's...t or lacks that hour; no mark is interpolated.', ...], 'parser_version': 'top40-snapshot-v1', 'schema_version': 1, ...}
manifest_text = '{\n  "files": [\n    {\n      "name": "archive_provenance",\n      "path": "data/top40/snapshot-v1/archive_provenance...0",\n    "hard_end_exclusive": "2026-07-01T00:00:00+00:00",\n    "warmup_start": "2020-01-01T00:00:00+00:00"\n  }\n}\n'

    def _v2_phase0_allows_fast_snapshot_verification(
        root: Path,
        manifest_path: Path,
        manifest: Mapping[str, Any],
        manifest_text: str,
    ) -> bool:
        """Verify a generic V2 frozen-file map before using the snapshot fast path."""
        freeze_path = root / TOP40_V2_LAYOUT.phase0_freeze_path
        if not freeze_path.exists() and not freeze_path.is_symlink():
            return False
        if not freeze_path.is_file() or freeze_path.is_symlink():
            raise ValueError("V2 phase0_freeze.json is missing or unsafe")
        state_path = root / TOP40_V2_LAYOUT.state_path
        if not state_path.is_file() or state_path.is_symlink():
            raise ValueError("V2 run_state.json is missing or unsafe")
        try:
            freeze = json.loads(freeze_path.read_text(encoding="utf-8"))
            state = json.loads(state_path.read_text(encoding="utf-8"))
        except (OSError, UnicodeError, json.JSONDecodeError) as exc:
            raise ValueError(f"invalid V2 Phase-0 binding: {exc}") from exc
        if not isinstance(freeze, Mapping) or not isinstance(state, Mapping):
            raise ValueError("invalid V2 Phase-0 binding schema")
        required = {
            "schema_version",
            "frozen_at_utc",
            "branch",
            "common_freeze_commit",
            "config_sha256",
            "shared_snapshot_manifest_path",
            "shared_snapshot_manifest_sha256",
            "frozen_files",
        }
        if set(freeze) != required or freeze.get("schema_version") != 2:
            raise ValueError("V2 phase0_freeze.json has an invalid schema")
        if freeze.get("branch") != TOP40_V2_LAYOUT.branch:
            raise ValueError("V2 Phase-0 branch binding is invalid")
        branch = subprocess.run(
            ["git", "branch", "--show-current"],
            cwd=root,
            check=False,
            capture_output=True,
            text=True,
        )
        if branch.returncode or branch.stdout.strip() != TOP40_V2_LAYOUT.branch:
>           raise ValueError(f"current branch must be {TOP40_V2_LAYOUT.branch}")
E           ValueError: current branch must be quant-portfolio-blind-top40-v2

src/crypto_trade/tournament/runner_v2.py:1525: ValueError
____ test_all_ten_executable_bundles_have_unique_v3_identity_and_valid_risk ____

    def test_all_ten_executable_bundles_have_unique_v3_identity_and_valid_risk() -> None:
        candidate_ids: set[str] = set()
        for team_id in TEAM_IDS:
            team_dir = TEAM_ROOT / team_id
            assert team_dir.is_dir() and not team_dir.is_symlink()
            config = _config(team_id)
            assert config["artifact"] == "frozen_reference_config"
>           assert config["candidate_status"] == "transparent_baseline_not_yet_evaluated"
E           AssertionError: assert 'transparent_...yet_evaluated' == 'transparent_...yet_evaluated'
E
E             - transparent_baseline_not_yet_evaluated
E             ?             ^^ --  -
E             + transparent_efficiency_revision_not_yet_evaluated
E             ?             ^^^^^^^^^^^^^^^  +

tests/tournament/test_v3_team_bundles.py:29: AssertionError
_______ test_all_strategy_factories_implement_keyword_only_seed_protocol _______

    def test_all_strategy_factories_implement_keyword_only_seed_protocol() -> None:
        for team_id in TOP40_V4_LAYOUT.team_ids:
            for path in sorted((TEAM_ROOT / team_id / "candidates").glob("*/strategy.py")):
                # Activation must never import untrusted team code in the organizer process. The
                # actual factory and protocol are exercised only inside runner_v4's sealed worker.
                tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
                factories = [
                    node
                    for node in tree.body
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))
                    and node.name == "build_strategy"
                ]
                assert len(factories) == 1
                factory = factories[0]
                assert isinstance(factory, ast.FunctionDef)
                assert not factory.args.posonlyargs
                assert not factory.args.args
                assert factory.args.vararg is None
                assert not factory.args.kwonlyargs
                assert factory.args.kwarg is None

                methods = [
                    node
                    for node in ast.walk(tree)
                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))
                    and node.name == "target_weights"
                ]
>               assert len(methods) == 1
E               assert 2 == 1
E                +  where 2 = len([<ast.FunctionDef object at 0x7fa2c83567d0>, <ast.FunctionDef object at 0x7fa2c814f210>])

tests/tournament/test_v4_team_bundles.py:71: AssertionError
=============================== warnings summary ===============================
tests/features_v1/test_open_interest_v1.py::TestGroupRegistryRegistration::test_open_interest_v1_in_group_registry
  /home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/.venv/lib/python3.13/site-packages/pandas_ta/__init__.py:37: Pandas4Warning: The 'mode.copy_on_write' option is deprecated. Copy-on-Write can no longer be disabled (it is always enabled with pandas >= 3.0), and setting the option has no impact. This option will be removed in pandas 4.0.
    from pandas_ta.core import AnalysisIndicators

tests/live/test_portfolio_report.py::test_combined_report_aggregates_trades
tests/live/test_portfolio_report.py::test_combined_report_html_smoke
  /home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/src/crypto_trade/live/portfolio_report.py:48: UserWarning: Converting to PeriodArray/Index representation will drop timezone information.
    months = pd.to_datetime(t["close_time"], unit="ms", utc=True).dt.to_period("M")

tests/strategies/ml/test_cross_sectional.py: 200 warnings
  /home/roberto/crypto-trade/.worktrees/quant-portfolio-blind-top50/.venv/lib/python3.13/site-packages/sklearn/utils/validation.py:2691: UserWarning: X does not have valid feature names, but LGBMRanker was fitted with feature names
    warnings.warn(

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/features_v3/test_fracdiff_d05_universal.py::test_fracdiff_d05_close_present_in_all_4_symbol_parquets
FAILED tests/features_v3/test_fracdiff_d05_universal.py::test_fracdiff_d05_close_stationary_per_symbol
FAILED tests/features_v3/test_hurst_drift_50_200_universal.py::test_hurst_drift_50_200_stationary_per_symbol
FAILED tests/features_v3/test_regime_momentum_signed_3d_universal.py::test_regime_momentum_signed_3d_present_in_all_4_symbol_parquets
FAILED tests/features_v3/test_regime_momentum_signed_3d_universal.py::test_regime_momentum_signed_3d_stationary_per_symbol
FAILED tests/strategies/ml/test_cross_sectional.py::test_iter090_runner_feature_count
FAILED tests/strategies/ml/test_universe_reselection_v3.py::test_v3_models_is_the_reanchored_universe
FAILED tests/strategies/ml/test_universe_reselection_v3.py::test_feature_columns_pinned_for_new_symbols
FAILED tests/strategies/ml/test_universe_reselection_v3.py::test_walk_forward_embargo_intact
FAILED tests/team09/test_team09_pipeline.py::test_frozen_team09_authority_is_exact
FAILED tests/team09/test_team09_pipeline.py::test_snapshot_payload_hashes_are_checked
FAILED tests/team09/test_team09_pipeline.py::test_released_all_period_statistics_match_tournament
FAILED tests/tournament/test_top40_amendment_0001_integration.py::test_activation_sentinel_retires_parent_cli_but_preserves_historical_authority
FAILED tests/tournament/test_top40_amendment_0001_integration.py::test_journal_boundary_accepts_a_normal_pending_post_activation_request
FAILED tests/tournament/test_top40_amendment_0002_durability.py::test_addendum_is_exactly_parented_to_frozen_amendment_0002
FAILED tests/tournament/test_top40_amendment_0002_validation.py::test_live_cohort_reconstructs_exact_four_green_identities
FAILED tests/tournament/test_top40_cli.py::test_ledger_rejects_event_timestamps_in_the_future
FAILED tests/tournament/test_top40_v2_amended_orchestrator_compat.py::test_real_research_status_preserves_all_tournament_and_report_bytes
FAILED tests/tournament/test_top40_v2_amended_orchestrator_compat.py::test_real_status_waits_for_the_result_transition_lock
FAILED tests/tournament/test_top40_v2_amendment_0001.py::test_cli_and_schemas_expose_only_trusted_lifecycle_inputs
FAILED tests/tournament/test_top40_v2_amendment_0002.py::test_cli_parser_exposes_only_reviewed_operations
FAILED tests/tournament/test_top40_v2_amendment_0005.py::test_real_active_a6_parent_and_pure_crypto_audit_are_bound
FAILED tests/tournament/test_top40_v2_amendment_0006.py::test_real_frozen_snapshot_passes_with_exact_pure_crypto_sets
FAILED tests/tournament/test_top40_v2_amendment_0006.py::test_canonical_report_bytes_are_deterministic
FAILED tests/tournament/test_top40_v2_cli.py::test_run_window_development_closes_journal_and_archives_artifacts
FAILED tests/tournament/test_top40_v2_cli.py::test_run_window_private_consumes_ticket_and_emits_only_gate_feedback
FAILED tests/tournament/test_top40_v2_cli.py::test_failed_development_is_research_not_submission_then_passing_candidate_freezes
FAILED tests/tournament/test_top40_v2_cli.py::test_private_ticket_is_single_shot_and_public_state_contains_no_observations
FAILED tests/tournament/test_top40_v2_cli.py::test_private_ticket_rejects_any_post_development_source_bundle_change
FAILED tests/tournament/test_top40_v2_cli.py::test_close_and_finalist_lock_advance_only_qualified_teams
FAILED tests/tournament/test_top40_v2_runner_schema3_compat.py::test_production_phase0_snapshot_path_accepts_schema3_facade
FAILED tests/tournament/test_v3_team_bundles.py::test_all_ten_executable_bundles_have_unique_v3_identity_and_valid_risk
FAILED tests/tournament/test_v4_team_bundles.py::test_all_strategy_factories_implement_keyword_only_seed_protocol
33 failed, 1655 passed, 33 skipped, 3 deselected, 203 warnings in 92.10s (0:01:32)
