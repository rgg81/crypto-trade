# Research Brief — Iteration 101

**Type**: EXPLOITATION
**Hypothesis**: The baseline configuration (iter 093 params) still produces comparable results with 193-feature parquets (vs the original 185-feature parquets that were lost during iter 100's regeneration).

## Section 0: Data Split (Verbatim)

```
OOS_CUTOFF_DATE = 2025-03-24    (FIXED. NEVER CHANGES.)
```

## Section 1: Problem Statement

During iter 100, the baseline parquets (185 features) were permanently overwritten by regenerated parquets (193 features). The 8 extra features were always in the code but weren't in the original parquets because they were generated by an older code version.

This iteration validates that the baseline config still works with the new 193-feature parquets. If OOS Sharpe is within 15% of the baseline (+1.01), the extra features are harmless. If it degrades significantly, we have a feature-stability problem.

## Section 2: Proposed Change

**Zero code changes.** Run the exact baseline configuration (iter 093 params) on the current 193-feature parquets. This is a pure validation run.

Parameters (identical to baseline iter 093):
- 5-seed ensemble: [42, 123, 456, 789, 1001]
- Training months: 24
- TP=8%, SL=4%, timeout=7d
- Dynamic ATR barriers: TP=2.9×NATR, SL=1.45×NATR
- Cooldown: 2 candles
- 50 Optuna trials, 5 CV folds
- Pooled model, no sample weighting modifications

## Section 3: Expected Outcome

If 193 features ≈ 185 features: OOS Sharpe +0.85 to +1.15 → baseline confirmed
If degraded: OOS Sharpe < +0.50 → feature-stability problem, need to identify which 8 features cause issues
