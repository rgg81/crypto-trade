# CRITIC PRE-FLIGHT RULING — EXPLORATION-G (MN3 track, slow-label reformulation)

> Persisted VERBATIM by the orchestrator from the Critic's read-only pre-flight, 2026-07-11.
> Verdict PASS-WITH-CONDITIONS; MUST-1/MUST-2 + SHOULD-3..7 + FLAG-8 applied via
> EXPLORATION-G-AMENDMENT-001 before the retrain + scored run.

**Verdict: PASS-WITH-CONDITIONS** (2 MUST-record-before-run + 5 SHOULD + 1 governance FLAG). None re-opens the construction, the signal, the 14-HARD gate anchoring, the throttle Layer-2 legitimacy, the revision-round authorization, or the token governance. The two MUSTs are a leak-safety assert on the one duplicated-purge landmine and a binding no-config-selection lock; both are recordable in a dated `EXPLORATION-G-AMENDMENT-001` at zero compute cost, exactly the S4 precedent.

**MODEL DISCLOSURE (charter deviation).** This ruling was authored on **Opus 4.8, not Fable** — the Fable mandate is user-suspended this phase (Fable-5 rate limit; user direction "continue on Opus"). The brief under review is likewise Opus-authored (disclosed in its MODEL-NOTE), as were DIAG-G and EXPLORATION-S4. Adversarial burden and standard are **unchanged**; nothing has been revealed; the holdout is sealed; `MN3-G` is unspent (verified against `REVEAL-LEDGER.md`).

**Object reviewed:** `briefs-portfolio-mn3/EXPLORATION-G.md` (in full). **Governance cross-read:** `DIAG-G.md`, `EXPLORATION-S4-engineering.md` + `REVIEW-S4-preflight.md` (honest-engine lesson, C1/C2 MECE standard, C4 placebo bug), `PLAN.md` §3.1 + §6.1 + AMENDMENT-002 §C/§D + AMENDMENT-005, `ORCHESTRATOR_BRIEF_MN3.md`, retrain surface `mn3_g_oof.py` + `mn3_features.py` + `mn3_diag_j.forward_total_residual`.

The construction is disciplined and the pre-registration is very nearly airtight. Decisively, most slow-label realized numbers **do not exist yet at pre-flight** (slow IC, slow turnover, slow net-2×, slow maxDD, slow CRASH edge), so "fitted to the realized value" is structurally impossible for the gates that determine SUCCESS. Every threshold that *could* have been read off DIAG-G's scorecard is a charter/v2/S4-verbatim value or a zero/sign floor. The QR **found the one foundation-code landmine themselves** (the duplicated inline purge at `mn3_g_oof.py:143`) and named both change-sites in §9.1 step 3; MUST-1 only adds the verifying assert.

## Adversarial checklist — ruling on each

### 1. Horizon-mismatch diagnosis + H=21 anchor — PASS (with SHOULD-7)
The diagnosis is a well-founded TESTABLE hypothesis, correctly treated as such. Turnover arithmetic corroborates it: DIAG-G's ~3.0 Σ|dw|/rebal at gross-1.0 EXCEEDS the 2.0 full-flip maximum for a non-side-crossing quintile book (verified: a complete disjoint quintile flip at gross 1.0 is Σ|dw|=2.0), so names routinely cross top→bottom week-to-week — the signature of a near-independent weekly re-draw, which a 24h forecast at t vs t+21 produces. The reformulation's mechanism (a 21c forward return integrates the week → smoother feature→prediction map, down-weighting fast-decaying-but-3c-predictive features) is genuine and falsifiable: G-turnover ≤104×/yr and §5.1's turnover(slow)<0.5×turnover(fast) gate it. The "a 21c label could itself be noisy and fail to slow the book" failure mode is not smuggled past — it routes to MARGINAL with the explicit diagnosis "the slow label did NOT slow the book, revisit the label" (§6). H=21 is PRINCIPLE-anchored (=the pre-existing weekly hold, Grinold–Kahn horizon-match), no scanned DOF; §0.5 declines the cadence-change and the secondary Lever-A band. The winsor √-rescale (0.20→0.53=0.20·√(21/3)) preserves the clip-fraction — the uniquely principled choice (no-scaling over-clips the fat tails where the crash edge lives; linear ×7 scales the mean not the vol). **→ SHOULD-7:** report the realized clip-fraction at ±0.53 on the 21c label vs ±0.20 on the 3c label. Pinning c1 cuts in the SAFE direction (a possibly-suboptimal config makes SUCCESS harder, never easier) and is the clean single-variable choice; the 7-config cross-check is acceptable only under MUST-2.

### 2. Gate anchoring (14 HARD) — PASS (with SHOULD-3)
Checked every HARD threshold against DIAG-G's scorecard (IC +0.0369; per-year +0.037/+0.043/+0.023; CRASH +0.00491 t+3.21; turnover 156.6×; gross +18.2%; net1× +6.5%; net2× −5.3%; 9/21 phases). **No threshold is set just below a DIAG-G edge number.**
- NEUT (G1a/G1b/G2-CRASH/G2-MANIA/G3/G4): charter/S4 verbatim — G1a genuinely FAILED for S4 at 93.3%, so binding not decorative; +3.21 is a return-t not a beta.
- **G-2xcost** (the killer): zero floor (>0 AND ≥0.5×(1×) AND ann>0) — must cross −5.3%→positive, a sign-boundary gap not a nudge; S4-verbatim form.
- **G-turnover ≤104×/yr**: PURE GEOMETRY (verified 104 = 52 weekly rebals × 2.0 full-flip ceiling at gross-1.0), independent of any slow number; brief expects ~45× (2.3× below) → a failure-detector not a pass-guarantor. For a continuous book Σ|dw|=2.0 needs even more re-ranking, so 104 is generous (correct direction).
- G-sharpe-floor +0.35, G-maxdd −25%: v2/S4 verbatim; DIAG-G reported NO Sharpe and NO maxDD → no realized value to fit to.
- G-signal-IC (>0, ≥2/3 yrs): sign floor, relaxed from 0.02 — justified (21c target intrinsically noisier; IC here is a SURVIVAL gate, not the SUCCESS-determinant which is ECON+crash).
- G-durable/G-sample: sign-stability / structural (≥20 names/rebal = MIN_MEMBERS exactly, not the S4-C3 "just under realized breadth" problem).

**G-crash-preserve — the load-bearing all-weather gate — PASS with one residual soft spot.** Clause (i) CRASH mean net>0 AND (ii) fresh CRASH quintile-spread t>0. The sign anchor (NOT +3.21) is CORRECT not lax: a 21c-forward label has heavy label-overlap deflating the achievable t even for an identical edge, so demanding a high crash-t as HARD would risk a FALSE FAIL on a genuine edge. The gate forces the crash edge to survive (a chop-harvester flat/negative in crashes fails clause i). **→ SHOULD-3:** if G-crash-preserve passes but SOFT G-crash-strong (t>1.5) fails, flag the SUCCESS as "crash-edge-preserved-but-weakened" so a barely-crash-positive book cannot be dressed as fully crash-robust at Stage-2. Count verified: 6 NEUT + 1 SIGNAL + 1 ALL-WEATHER + 6 ECON = 14 HARD.

### 3. Crisis-throttle divergence from S4's C1 — PASS (LEGITIMATE, with SHOULD-4)
The divergence is JUSTIFIED. S4's C1 capped THROTTLE-HURTS at MARGINAL because S4 was a marginal-edge book that was crash-neutral-to-slightly-negative (worst bucket CRASH, G4 t=−0.82) and its ONLY tail defense WAS the throttle. G is structurally different and HARD-gated: G's tail defense is measured crash-POSITIVITY gated four independent ways (G-crash-preserve net>0 AND t>0, G4, G2-CRASH, G-maxdd), any failure forcing FAIL. So a throttle-off SUCCESS book has PROVEN (not assumed) it earns in crashes; the downside throttle is genuinely redundant and its HURTS result is confirmatory. §C is satisfied (it requires pre-registering a throttle + thresholds + falsification arm, NOT shipping it ON or tier-capping). The G-LCDD-z throttle is the verbatim-SCUD structural mirror (constants inherited; leg=top-0.33 predicted cohort + tail=P25 downside structurally forced) with its own leak battery + IS-behavior falsifier. **→ SHOULD-4:** §5.4's "un-throttled book independently gates the alpha floors" is imprecise (under HELPS the gates score the THROTTLED book) — restate the correct reason: a gross-reduction throttle (scalar∈[φ=0.50,1.0]) is structurally INCAPABLE of manufacturing positive alpha; §5.2/§5.3 gate the signal regardless of throttle state.

### 4. Decision-map MECE — PASS
§2.2 throttle partition {HURTS if Δmaxdd<0 OR s<0.90; else HELPS if Δmaxdd≥+2pp AND s≥0.90; else NEUTRAL} verified exhaustive (not-HURTS ⟹ Δmaxdd≥0 AND s≥0.90, HELPS/NEUTRAL partition exactly on +2pp — no gap/overlap); the S4-C2-corrected form inherited verbatim. §6 tier map reduced formally: FAIL=¬(N∧S∧C∧K); within not-FAIL, SUCCESS⟺E, MARGINAL⟺¬E — exhaustive + mutually exclusive. The throttle disposition is UPSTREAM (selects the single as-shipped book), never in the tier logic — the fix for the S4 overlap.

### 5. Placebo (C4 lesson) — PASS (with SHOULD-5)
The C4 bug is correctly avoided. S4's two-sided null on NET-of-cost Sharpe was structurally failed by any costed random book (centers at −cost). §5.2 nulls on GROSS Sharpe (two-sided CI∋0) + a real−placebo gross-Sharpe differential (>0). A rank-shuffled, demeaned, beta-projected book with NO cost has E[gross]≈0 → correctly centered. Stronger than S4's. **→ SHOULD-5:** specify the placebo GROSS return object is defined identically to the real book's gross (residual price+funding, pre-cost) — no funding-in-one-not-the-other asymmetry.

### 6. Ledger + revision-round legitimacy — PASS (with FLAG-8 + MUST-2)
§3.1 body bans "post-diagnostic label changes" as mining; the revision IS a label change (3c→21c). Resolution (ruled valid): (i) the ban targets OPEN-ENDED iteration (try 3c, 6c, 12c… until something passes); (ii) AMENDMENT-005 is a dated, appended, user-directed override recorded BEFORE the retrain — the sanctioned mechanism — invoking the explicitly-provisioned one-revision-round (8→16); (iii) a SINGLE principle-anchored horizon-match, not a fished iteration; (iv) it is the LAST round (G closes on next failure), cap-16 hard, mining surface bounded. **→ FLAG-8:** record this reconciliation. Not too large to be a revision (SAME features/config/seeds/window/walk-forward/universe/projection; only label horizon + its two horizon-linked constants change) → shares MN3-G forever, no new token (§6.1). Ledger +2→10 honest (+1 label horizon [purge/winsor are deterministic functions of H]; +1 G-LCDD-z throttle [inherited]). c1-pinned N=1 honest (harness computes all 8 — compute already spent — but SELECTION is a-priori-pinned). **→ MUST-2** locks this. Token unspent.

### 7. Un-suppressed comparison control §5.1 — PASS (with SHOULD-6)
§5.1 proves "cut cost not signal" via NET=GROSS−COST: holding form/projection/cost/cadence constant and varying only the signal (slow-c1 vs FROZEN fast-c1 parquet, no re-fit), requires turnover(slow)<0.5×turnover(fast) AND gross(slow)≤1.25×gross(fast). Falsifier can genuinely fire, HARD-gating. 0.5× is a sound relative anchor (fast-continuous turnover unmeasured pre-run). 1.25× is the least-principled constant (25% round number) but defensible. **→ SHOULD-6:** report explicit Δnet=Δgross−Δcost so the cost-driven fraction is auditable.

## CONDITIONS (record in a dated `EXPLORATION-G-AMENDMENT-001` before the retrain + scored run)

**MUST-1 — purge-leak safety (the one foundation landmine).** The slow retrain must change the purge in BOTH sites §9.1 step 3 names: the module constant `MN3_G_PURGE_CANDLES` 3→21 AND the hardcoded inline at `mn3_g_oof.py:143` (`t_idx_arr <= b_idx - 3 - 1` → `b_idx - 21 - 1`). The runner ignores `oof_month_slices`' `train_mask` and uses this duplicated inline, so a single-site change silently leaves the effective purge at 3 → training 21c-forward labels overlap the OOF window by up to 18 candles → look-ahead inflating the slow IC + every downstream metric. Before persisting the slow parquet, add an ABORT-ON-FAIL assert re-running DIAG-G §3's "Purge PASS" check at 21: for every walk-forward month, the last permitted training candle's forward-21 label window must end at index < b_idx (the OOF month's first candle). Equivalently, refactor the runner to consume `oof_month_slices`' `train_mask` (single source of truth).

**MUST-2 — c1-pin no-selection lock.** Record that the §6 tier is computed on the c1-slow as-shipped book ONLY. The other 7 slow-label configs are reported solely as the HP-insensitivity cross-check; no tier, gate, or design choice may key off them. A c1-slow HARD-gate FAIL coincident with any other slow-config passing is disclosed as a finding for a POSSIBLE FUTURE iteration (spending additional ledger under cap-16 / last-round), NEVER a within-iteration config re-selection.

**SHOULD-3 — crash-strong disclosure.** If G-crash-preserve passes but SOFT G-crash-strong (t>1.5) fails, flag the SUCCESS as "crash-edge-preserved-but-weakened."

**SHOULD-4 — §5.4 mechanism restatement.** Replace "un-throttled book independently gates the alpha floors" with: a gross-reduction throttle (scalar∈[φ,1]) cannot manufacture positive alpha; a HELPS disposition is legitimate risk-shaping; §5.2/§5.3 gate the signal regardless of throttle state.

**SHOULD-5 — placebo gross-object.** Specify the §5.2 GROSS placebo return stream is defined identically to the real book's gross (residual price+funding, pre-cost).

**SHOULD-6 — §5.1 Δnet decomposition.** Report Δnet=Δgross−Δcost so the cost-driven fraction of the net win is auditable.

**SHOULD-7 — winsor clip-fraction verification.** Report the realized clip-fraction at ±0.53 on the 21c label vs ±0.20 on the 3c label.

**FLAG-8 — §3.1-body reconciliation (governance record).** Record that AMENDMENT-005's label change overrides §3.1's "no post-diagnostic label changes" body clause via the sanctioned dated-amendment + one-revision-round mechanism; single principle-anchored horizon-match, not open-ended mining; family G's LAST round; G closes on next failure.

## Bottom line
Horizon-mismatch diagnosis is a properly-gated testable hypothesis; H=21 is the pre-existing hold not a fitted knob; the winsor √-rescale is a single principled formula. Every one of the 14 HARD thresholds is charter/v2/S4-verbatim or a zero/sign floor — the SUCCESS-determining ones (Sharpe, maxDD, net-2×, turnover, crash-preserve) had no realized DIAG-G value to fit to or are provably geometric. The throttle divergence from S4's C1 is legitimate (G HARD-gates crash-positivity, so shipping the redundant throttle OFF at SUCCESS is honest + §C-compliant). Both decision partitions MECE. Placebo correctly nulls on GROSS. Ledger/c1-pin/token/revision-round honest + bounded. The one real hazard — the duplicated inline purge — the QR already flagged; MUST-1 turns their flag into a guarantee. **Apply MUST-1 + MUST-2 and fold in SHOULD-3..7 + FLAG-8. The retrain (~12 min) and scored engine run may then proceed.**

*— Quant Critic, MN3 track, 2026-07-11 (Opus 4.8, Fable-suspended phase). Pre-flight; read-only; nothing run; holdout sealed; MN3-G unspent.*
