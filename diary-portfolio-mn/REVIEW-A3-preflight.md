# REVIEW-A3-preflight — Critic Pre-Flight of the EXPLORATION-A3 Brief (MN track, terminal attempt)

**Reviewer:** Quant Critic (Fable, read-only). **Date:** 2026-07-10. **Persisted by orchestrator.**
**Contract:** briefs-portfolio-mn/EXPLORATION-A3.md (da980cc2). No runs; quarantines honored;
holdout sealed.

## VERDICT: **PASS-WITH-CONDITIONS** — C1–C4, all additive disclosure/pins; none re-opens the
frozen construction/gates/predictions/decision map.

## Key rulings
1. **Leak surface CLOSED.** Whole SCUD-z chain past-only, consumed at [k−1] (engine
   gross_scalar_series path verified); forward-fill past-only under any reading; corrupt-future
   control mandated and well-specified. C2: pin the m<5 forward-fill target = the final SCALAR,
   backward-looking (reproducibility; no scored-window effect).
2. **Calibration: τ/φ parameter-free coverage anchors (standard-normal quantiles on an
   already-standardized z; RISK-006 honored). h=9 → DIAG-A Z_MEAN_WINDOW=9; W=90 → mn_regimes
   MN_REGIME_WINDOW=90 (both pre-established, non-episode anchors). q_short=0.33 weakest-anchored.**
   C1: per-parameter provenance disclosure for {h, W, q_short, φ, m_min} + affirmation that
   episode SHAPES were never inspected for parameter choice (dates only, for the forensic).
3. **Worth-it thresholds genuinely anti-favorable:** +1.30 = 0.77× A2 is a demanding tax
   CEILING (over-firing throttles FAIL it); both perverse corners (under-fire → maxDD FALSIFIER;
   over-fire → Sharpe FALSIFIER) closed; neutrality-degradation caught by the carried gates.
   C3: pin the MARGINAL disposition pre-run — maxDD ∈ (−25%,−22%] with Sharpe ≥ 1.30 → SUCCESS
   tier, reveal CANDIDATE presented WITH an explicit <3pp-fragility warning + QR "weak basis for
   spending the one-forever holdout" note; not auto-reveal, not shelve.
4. **Episode forensic scoreable but under-pinned.** C4: (a) pin the per-episode window (calendar
   month of each worst-10 trough or fixed ±k candles around it) so "fired during" can't be
   flexed; (b) annotate per-episode throttle TIMING (lead/coincident/lag vs peak→trough) — a
   trough-only firing scores "aligned" while doing nothing for maxDD; lead-timing is the
   mechanism-diagnostic.
5. **Terminality AIRTIGHT.** Unconditional shelve-on-FAIL (no "interesting failure" clause);
   every prior hatch explicitly nailed shut (maxDD re-spec permanently rejected; no throttle
   scan; no A4; broken-invariant robustness numbers non-citable). SUCCESS → USER-only reveal
   candidate. Both branches terminate cleanly.
6. **Standard: all pass.** Ledger 9 DOF honest; gate deltas ZERO (G-maxdd −25% correctly
   UNCHANGED on unseen numbers — Ruling 2 implemented); **isolation structurally verified:
   throttled executed weights = s·(A2 weights) exactly** (g-proportionality through projection,
   rescale, and fraction-invariant cap; member set and feasibility g-independent) — the §7
   neutrality-preservation test executes this proof; STATELESS confirmed (market-data-only
   indicator → no cost-dependent decision flips; GT 2× stays authoritative per catalog rule);
   QE spec complete.

## Conditions
C1 per-parameter provenance · C2 forward-fill = final scalar · C3 MARGINAL disposition pin ·
C4 episode windows + lead/lag timing annotation.
