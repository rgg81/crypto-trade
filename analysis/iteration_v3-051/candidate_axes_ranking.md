# iter-v3/051 candidate axes — ranking

Generated by `analysis/iteration_v3-051/multi_axis_eda.py` + 2 follow-up deep-dive scripts
(`axis_c_fracdiff_deep_dive.py`, `axis_c_regime_3d_compute.py`).

EDA basis: `synthesis.md` (READ FIRST) + 9 CSV outputs.

## Context

iter-v3/051 starting state inherits from iter-v3/050 head WITH SYSTEM-LEVEL REVERT applied
per `feedback_v3_per_symbol_lifts_oos_breaks_is.md` (UPDATED 2026-05-10 with second-cycle
confirmation; system-level constraint):

**Pre-REVERT state (iter-v3/050 head, what we DROP)**:
- V3_FEATURE_COLUMNS_TOP_N = 14 (regime_momentum_signed_5d included; vol_normalized_ret_5d
  ABSENT)
- V3_ATR_MULTIPLIERS_PER_SYMBOL = {ALGOUSDT: (2.0, 1.5), LDOUSDT: (2.0, 1.5)}
- block_long_for = ("BCHUSDT",)
- V3_MODELS = (BCH, LDO, TRX, ALGO) — 4 symbols
- V3_FEATURES_PER_SYMBOL = {} (empty)
- DEFAULT_ATR_MULTIPLIERS = (2.0, 1.0)
- adx_threshold_per_symbol = {} (empty per /049 closeout)

**Post-REVERT state (iter-v3/051 cycle 4 starting baseline; matches iter-v3/028 architecture)**:
- V3_FEATURE_COLUMNS_TOP_N = 14 (UNCHANGED for now; will be 15 after axis ADD)
- V3_ATR_MULTIPLIERS_PER_SYMBOL = {} (REVERT — system-level mandate)
- block_long_for = () (REVERT — system-level mandate)
- V3_MODELS = (BCH, LDO, TRX) — 3 symbols (REVERT — drop ALGO; system-level mandate)
- V3_FEATURES_PER_SYMBOL = {} (UNCHANGED empty)
- DEFAULT_ATR_MULTIPLIERS = (2.0, 1.0) (UNCHANGED)
- adx_threshold_per_symbol = {} (UNCHANGED empty)
- REQUIRED_GAP = 66 = (21+1)*3 (REVERT from 88 due to 3-sym universe)

iter-v3/051 axis = SINGLE NEW variation on top of post-REVERT state. Cycle 4 #1 of 10
EXPLORATIONs.

The 4 candidate axes (per Critic FINAL `b6339c5` of iter-v3/050 recommendation #2 + #3
+ #5) ranked by EDA quantitative leverage AND BOTH-must-improve preservation:

---

## Candidate 1 — fracdiff_d05_close at universal scope (RECOMMENDED)

**Axis classification**: A.1 (NEW universal engineered feature; Category 2 composed feature
per `feedback_v3_engineered_feature_pivot.md`; LdP AFML Ch. 5 fixed-width FFD at d=0.5)

**Mechanism**: ADD `fracdiff_d05_close` to `V3_FEATURE_COLUMNS_TOP_N` (14 → 15), placing
it as the 15th element after `regime_momentum_signed_5d`. The feature is the d=0.5
fractional differentiation of close price (López de Prado AFML Ch. 5: preserves long-memory
property while achieving stationarity).

The compute function `compute_fracdiff_d05_close` already exists in
`src/crypto_trade/features_v3/fracdiff_v3.py`; the feature is present in all 4 symbol
parquets at `data/features_v3/{SYMBOL}_8h_features.parquet`. No feature regeneration
required — only feature-column-list update + 5 adversarial tests + backtest.

**Quantitative basis (full per-symbol EDA)**:

ADF stationarity (IS subset, full sample):
- BCH: p=0.000000 (5528 obs), STATIONARY
- LDO: p=0.000000 (2542 obs), STATIONARY
- TRX: p=0.000008 (5470 obs), STATIONARY
- ALGO: p=0.000086 (5026 obs), STATIONARY (NOT in iter-v3/051 V3_MODELS but available
  for future re-inclusion)

**ALL 4 SYMBOLS STATIONARY at p<0.05** — the d=0.5 FFD achieves stationarity as
theoretically predicted.

IC matrix vs existing 14 features (max per symbol):
- BCH: max |IC| = 0.6597 with vwap_dev_20 (source primitive — carve-out)
- LDO: max |IC| = 0.7381 with vwap_dev_20 (source primitive — carve-out)
- TRX: max |IC| = 0.5238 with vwap_dev_20 (source primitive — carve-out)
- ALGO: max |IC| = 0.5881 with vwap_dev_20 (source primitive — carve-out)

After carve-out (excluding vwap_dev_20 source primitive): max |IC| = 0.6721 with
regime_momentum_signed_5d at LDO. ALL post-carve-out IC values are **BELOW the strict
0.70 gate.** Per `feedback_v3_engineered_feature_pivot.md` Category 2 carve-out PASSES.

Univariate Spearman ρ vs forward 1-bar return:
- BCH: ρ=-0.038, p=0.005, SIGNIFICANT
- LDO: ρ=-0.051, p=0.010, SIGNIFICANT
- TRX: ρ=-0.050, p=0.0002, SIGNIFICANT
- ALGO: ρ=-0.038, p=0.006, SIGNIFICANT

**ALL 4 SYMBOLS SIGNIFICANT** at p<0.05 with negative correlation (mean-reversion signal).
Effect size small but consistent.

NOTE per `feedback_v3_axis_selection_quant_discipline.md`: univariate Spearman is the
WEAKEST evidence type (warning from iter-v3/070 failure mode); used here as a sanity
check, NOT as primary justification.

**Implementation complexity**: LOW
- Add 1 element to `V3_FEATURE_COLUMNS_TOP_N` tuple (~3 LOC w/ comment block).
- Update 1 assertion in `_verify_feature_columns` (n != 14 → n != 15).
- Add 1 assertion: `fracdiff_d05_close` MUST BE PRESENT.
- 5 adversarial tests at `tests/features_v3/test_fracdiff_d05_universal.py`.
- Estimated setup time: 30-45 min; backtest 25-35 min; total ≤1.5h.

**Risks**:
- iter-v3/035 used fracdiff_d05_close per-symbol BCH-only and saw +37.98 OOS swing for
  BCH but TRX -20.11 / ALGO -8.24 / LDO -6.81 regression at single-seed. The
  PER-SYMBOL universal-scope test showed regression on non-BCH symbols.
- BUT context has changed since /035: iter-v3/028 added regime_momentum_signed_5d (which
  may compete for the same signal niche as fracdiff_d05_close). The universal-scope test
  at iter-v3/051 has a DIFFERENT 14-feature stack than iter-v3/035 had.
- Single-seed Optuna lottery risk per `feedback_v3_engineered_features_dont_stack.md`:
  any new engineered feature added at single-seed n_trials=35 carries the iter-v3/026/027
  pattern risk (NEGATIVE-SUSPICIOUS-OOS at IS-OOS daily ratio outside [0.5, 2.0]).
- Rank-14 INERT risk per `feedback_v3_inert_features_at_higher_budget.md`: if Optuna
  doesn't learn the feature (rank > 13/15 in all 3 symbols), it becomes dead weight at
  multi-seed CONFIRMATION.

**Pre-registered classification**:
- PATH A (PROMISING-clean): IS Sharpe Δ ≥ +0.10 vs iter-v3/028 baseline reference +0.5101
  AND OOS Sharpe Δ ≥ -0.20 vs iter-v3/028 baseline reference +0.5053 AND
  fracdiff_d05_close importance rank ≤ 10/15 in at least 1 of 3 symbols AND
  IS-OOS daily Sharpe ratio ∈ [0.5, 2.0].
- PATH B (PROMISING-INERT): fracdiff_d05_close importance rank > 13/15 in ALL 3 symbols
  AND |IS Sharpe Δ| ≤ 0.10.
- PATH C-clean (NEGATIVE-clean): IS Sharpe Δ < -0.10 OR OOS Sharpe Δ < -0.30.
- PATH C-suspicious (NEGATIVE-SUSPICIOUS-OOS): IS-OOS daily Sharpe ratio outside [0.5, 2.0].

**Compoundability**: STACKABLE with regime_momentum_signed_5d (different mechanism layer:
fracdiff is linear close-derived stationary; regime_momentum is sign-flip Hurst-regime
composed). IC at LDO = 0.6721 (moderate; not redundant). Compatible with cycle 4 #2 if
PROMISING.

**Predicted behavioral effect** (per `feedback_v3_axis_saturation_predictor.md`):
- Bundle IS trade count: 130-200 (reference: iter-v3/028 IS = 156).
- Bundle OOS trade count: 70-110 (reference: iter-v3/028 OOS = 95).
- fracdiff_d05_close importance rank: predicted top-10 in at least 1 of 3 symbols
  (per iter-v3/035 BCH 50%+ split-importance).
- Bundle IS Sharpe Δ vs iter-v3/028 baseline reference +0.5101: **predicted [+0.05, +0.30]**
  (lift; small to moderate).
- Bundle OOS Sharpe Δ vs iter-v3/028 baseline reference +0.5053: **predicted [-0.20, +0.30]**
  (uncertain; Optuna response uncertainty at single-seed).
- IS-OOS daily Sharpe ratio: predicted ∈ [0.7, 1.5] (clean lift, not suspicious).

**Falsifier (saturation predictor)**:
- If observed IS Sharpe Δ < -0.10 vs baseline reference: NEGATIVE-clean (axis CLOSED at
  /051; pivot to regime_momentum_signed_3d at /052).
- If fracdiff_d05_close importance rank > 13/15 in ALL 3 symbols: PROMISING-INERT.
- If IS trade count change > 30% relative vs iter-v3/028 reference: cascade effect;
  investigate.

---

## Candidate 2 — regime_momentum_signed_3d at universal scope (RANKED #2; queue for /052)

**Axis classification**: A.2 (NEW universal engineered feature; Category 2 composed feature
per `feedback_v3_engineered_feature_pivot.md`; orthogonal time-scale variant of iter-v3/028
edge ingredient regime_momentum_signed_5d)

**Mechanism**: ADD `regime_momentum_signed_3d = ret_3d × sign(hurst_100 - 0.5)` to
`V3_FEATURE_COLUMNS_TOP_N` (14 → 15). The compute function
`compute_regime_momentum_signed_3d` already exists as DEAD CODE in
`src/crypto_trade/features_v3/engineered_v3.py:330` (orchestrator's pre-iter-v3/044 pick;
REVERTED before backtest with QR EDA-derived rationale that 3d does NOT discriminate ALGO
LONG WR at 22.2%/13.3%).

**Quantitative basis (computed on-the-fly from `close` + `hurst_100` primitives)**:

ADF stationarity:
- BCH: p=0 (5628 obs), STATIONARY
- LDO: p=0 (2642 obs), STATIONARY
- TRX: p=0 (5570 obs), STATIONARY
- ALGO: p=0 (5126 obs), STATIONARY

ALL 4 SYMBOLS STATIONARY at p<0.05.

IC matrix vs existing 14 features (max per symbol):
- BCH: max |IC| = 0.6192 with vwap_dev_20 (NOT a source primitive for regime_momentum)
- LDO: max |IC| = 0.6139 with vwap_dev_20
- TRX: max |IC| = 0.5181 with vwap_dev_20
- ALGO: max |IC| = 0.5970 with vwap_dev_20

**ALL post-strict-gate PASS**: max |IC| = 0.6192 < 0.70 strict gate. NO carve-out needed.
This is CLEANER on the IC criterion than fracdiff_d05_close (which needed carve-out).

IC with regime_momentum_signed_5d (sister feature; potential redundancy):
- BCH: 0.4683 (moderate; not redundant)
- LDO: ~0.45 (estimated; same range)

Univariate Spearman ρ vs forward 1-bar return:
- BCH: ρ=-0.058, p=1e-5, SIGNIFICANT
- LDO: ρ=-0.044, p=0.024, SIGNIFICANT
- TRX: ρ=-0.068, p=4e-12, SIGNIFICANT
- ALGO: ρ=-0.058, p=3e-5, SIGNIFICANT

**Stronger univariate signal than fracdiff_d05_close** (mean ρ = -0.057 vs -0.044). All
4 symbols significant at p<0.05.

**Implementation complexity**: MEDIUM
- Activate dead code in engineered_v3.py (uncomment dispatch line; ~3 LOC).
- Add 1 element to `V3_FEATURE_COLUMNS_TOP_N` (~3 LOC w/ comment block).
- Update assertions in `_verify_feature_columns` (n != 14 → n != 15; add presence assertion).
- Regenerate feature parquets (uv run crypto-trade features --track v3 --interval 8h
  --symbols BCH,LDO,TRX,ALGO; ~5-10 min depending on symbols).
- 5 adversarial tests at `tests/features_v3/test_regime_momentum_signed_3d.py`.
- Estimated setup time: 60-80 min (vs 30-45 min for fracdiff which has no regen);
  backtest 25-35 min; total ≤2h (within EXPLORATION cap).

**Risks**:
- iter-v3/044 QR EDA (cycle3_is_diagnosis.py SHA `eff841e`) FALSIFIED 3d at IS-axis
  bottleneck symbol: "regime_momentum_signed_3d does NOT discriminate ALGO LONG WR".
  However ALGO is REVERTED at iter-v3/051 (3-sym universe), so this falsification may
  not apply at the cycle 4 starting baseline.
- Stacking with regime_momentum_signed_5d risks `feedback_v3_engineered_features_dont_stack.md`
  pattern (IS Sharpe collapse + OOS spike) at single-seed n_trials=35.
- Implementation cost is HIGHER than fracdiff_d05_close (feature regen required).

**Why RANKED #2 below fracdiff_d05_close**:
- iter-v3/035 fracdiff_d05_close BCH-only PROMISING precedent (single-seed +37.98 OOS
  swing) suggests the feature CAN learn signal in the v3 architecture.
- iter-v3/044 regime_momentum_signed_3d FALSIFIED at the bottleneck (ALGO LONG WR) by
  the SAME methodology (`feedback_v3_axis_selection_quant_discipline.md`).
- Implementation cost is LOWER for fracdiff (no feature regen).
- Cycle 4 #1 must be the most-promising axis given EXPLORATION budget; secondary
  candidates queue for #2.

**Verdict**: ELIGIBLE; queued for iter-v3/052 EXPLORATION as cycle 4 #2 axis IF /051
fracdiff_d05_close is PROMISING-clean OR NEGATIVE-clean (NOT NEGATIVE-SUSPICIOUS-OOS or
PROMISING-INERT — those would change the cycle 4 trajectory).

---

## Candidate 3 — LDO removal alone (drop LDO from V3_MODELS) (REJECTED on EDA IS axis FAIL)

**Axis classification**: B.1 (universe contraction; structurally distinct from feature
addition)

**Mechanism**: V3_MODELS = (BCHUSDT, TRXUSDT, ALGOUSDT) — 3 symbols (drop LDO; keep ALGO).
This is OPPOSITE direction from the system-level REVERT (which drops ALGO and keeps LDO).

**Quantitative basis (counterfactuals from /050 trade roster)**:

LDO attribution across 5 reference iterations:
- iter-v3/028 (3-sym baseline): IS LDO PnL = +5.02; OOS LDO PnL = -22.05
- iter-v3/045 (single-seed; per-sym ATR ON; primitive 10 OFF): IS LDO PnL = +43.90;
  OOS LDO PnL = +9.34 (SEED-SPECIFIC FAVORABLE DRAW per /050 retrospective)
- iter-v3/047 (multi-seed; per-sym ATR + primitive 10): IS LDO PnL = +0.85; OOS LDO
  PnL = -19.13
- iter-v3/049 (carry-forward + ADX retune): IS LDO PnL = +0.85; OOS LDO PnL = -19.13
  (BIT-IDENTICAL to /047; frozen-baseline pattern)
- iter-v3/050 (CONFIRMATION; same head as /049): IS LDO PnL = +0.85; OOS LDO PnL = -19.13
  (BIT-IDENTICAL; 3rd consecutive)

Counterfactual: drop LDO trades from /050 daily PnL aggregation:
- IS Sharpe: +0.4872 → +0.4947 (Δ -0.015) — EFFECTIVELY FLAT
- OOS Sharpe: +1.166 → +2.015 (Δ +0.85) — STRONG OOS LIFT
- vs iter-v3/028 anchor +0.5101 IS / +0.5053 OOS:
  - IS Δ = -0.015 (slightly NEGATIVE)
  - OOS Δ = +1.510 (strongly POSITIVE)

**Per `feedback_v3_strict_both_is_oos_baseline.md` BOTH-must-improve rule: FAILS on IS
axis at EDA stage.**

**EDA STATUS**: REJECTED on IS axis FAIL.

**Verdict**: REJECTED at EDA stage. The OOS lift is huge but IS axis is essentially flat
(LDO contributes near-zero IS PnL). Multi-seed CONFIRMATION at the 3-sym BCH+TRX+ALGO
universe would face the BOTH-must-improve gate FAIL.

QUEUED for iter-v3/052+ EXPLORATION as a structurally distinct axis (universe contraction
in DIFFERENT direction than the system-level REVERT). May resurface if cycle 4 needs an
OOS-axis lift after IS-axis improvements via universal features.

---

## Candidate 4 — per-symbol customization REVERT (b2/b3/b4 sub-axes)

**Axis classification**: C.1 (carry-forward state change; PARTIALLY-INCORPORATED into
iter-v3/051 setup as system-level mandate)

**Mechanism**: REVERT some subset of {V3_ATR_MULTIPLIERS_PER_SYMBOL clear, block_long_for
clear, V3_MODELS drop ALGO}. The system-level rule mandates the FULL REVERT (b4) as the
cycle 4 starting baseline, NOT as an axis under test.

**EDA STATUS**:
- (b2) per-sym ATR REVERT alone: NOT PRECISELY SIMULABLE; no EDA evidence
- (b3) primitive 10 REVERT alone: ELIMINATED on multi-seed compression precedent (/045
  single-seed lottery)
- (b4) full REVERT: IDENTITY = iter-v3/028 baseline = no axis variation; INCORPORATED
  into iter-v3/051 setup as the new starting baseline

**Verdict**:
- (b2): QUEUED for iter-v3/053+ EXPLORATION as standalone axis if needed
- (b3): QUEUED for iter-v3/054+ EXPLORATION if cycle 4 #1 produces NEGATIVE
- (b4): INCORPORATED into iter-v3/051 setup (system-level mandate)

The full REVERT (b4) is part of the cycle 4 starting baseline; testing it as a standalone
EXPLORATION would consume a slot to re-confirm the iter-v3/028 baseline (already known
at multi-seed +0.5101 IS / +0.5053 OOS).

---

## Recommended axis: Candidate 1 (fracdiff_d05_close at universal scope)

**Why**:

1. **Cycle 4 HIGH-priority axis #1** per Critic FINAL `b6339c5` recommendation #5 of
   iter-v3/050 closeout: "NEW universal engineered features ... fracdiff_d05_close
   for ALL 4 symbols (universal scope; iter-v3/035 used BCH-only — universal scope
   unstested at multi-seed)."

2. **Aligned with cycle 4 starting hypothesis** (iter-v3/050 diary §Cadence): "lift IS
   Sharpe to ≥ +0.5101 (BASELINE_V3.md update gate floor) while preserving OOS Sharpe
   ≥ +0.5053 via UNIVERSAL axes (per-symbol customizations rejected at bundle level)."

3. **NEW universal feature family** per `feedback_v3_structural_over_knob_exploration.md`
   priority hierarchy (NEW feature families > NEW model arch > NEW labeling > knob tunes).

4. **EDA-validated quantitative basis**:
   - ADF stationary at p<0.05 across all 4 symbols
   - IC carve-out PASS (max |IC| = 0.7381 with vwap_dev_20 source primitive; post-carve-out
     max |IC| = 0.6721 < 0.70 strict gate)
   - Univariate Spearman significant at p<0.05 across all 4 symbols (mean-reversion signal)
   - Already in feature parquets (no feature regeneration required)

5. **Implementation feasible within 2h cap**: 30-45 min setup + 25-35 min backtest = ≤1.5h.

6. **Single-axis discipline**: ONE new feature added to V3_FEATURE_COLUMNS_TOP_N. The
   per-symbol REVERT carry-forward is system-level mandate (NOT an axis under test).

7. **Categorically distinct from prior closed cycle 3 axes**:
   - Cycle 3 NEW universal engineered feature axis CLOSED at iter-v3/048 (5 attempts)
     — but cycle 4 RE-OPENS the axis per /050 diary §Cycle 4 Priorities axis #1.
   - fracdiff_d05_close at universal scope is the FIRST cycle 4 attempt at this axis.
   - Categorically distinct from iter-v3/048 vol_normalized_ret_5d (also d05-related but
     different structural family — vol-normalized momentum vs FFD).

8. **Per-symbol customization REVERT is SYSTEM-LEVEL MANDATED**, not a competing axis.
   The REVERT is part of the cycle 4 baseline reset; testing it as a standalone
   EXPLORATION wastes a slot.

9. **iter-v3/035 BCH-only PROMISING precedent**: fracdiff_d05_close already showed +37.98
   OOS swing for BCH at single-seed (per /050 retrospective). The universal-scope
   extension tests whether the feature has signal for non-BCH symbols (LDO/TRX in /051's
   3-sym universe).

10. **Single-seed lottery risk acknowledged**: per
    `feedback_v3_engineered_features_dont_stack.md`, any new engineered feature at
    single-seed n_trials=35 carries the iter-v3/026/027 NEGATIVE-SUSPICIOUS-OOS pattern
    risk. Pre-registered Falsifier (PATH C-suspicious): IS-OOS daily Sharpe ratio outside
    [0.5, 2.0] band.

**Pre-registered behavioral predictions** (locked in brief Section 4):
- fracdiff_d05_close importance rank ≤ 10/15 in at least 1 of 3 symbols (per /035 BCH-only
  precedent of 50%+ split importance)
- Bundle IS trade count: 130-200 (reference: iter-v3/028 IS = 156)
- Bundle OOS trade count: 70-110 (reference: iter-v3/028 OOS = 95)
- Bundle IS Sharpe Δ vs iter-v3/028 baseline reference +0.5101: predicted [+0.05, +0.30]
- Bundle OOS Sharpe Δ vs iter-v3/028 baseline reference +0.5053: predicted [-0.20, +0.30]
- IS-OOS daily Sharpe ratio: predicted ∈ [0.7, 1.5]

**Implementation plan**:
1. REVERT V3_MODELS = (BCH, LDO, TRX) — drop ALGO (system-level mandate)
2. REVERT V3_ATR_MULTIPLIERS_PER_SYMBOL = {} — clear (system-level mandate)
3. REVERT block_long_for = () — clear (system-level mandate)
4. UPDATE REQUIRED_GAP = 66 = (21+1)*3 (universe contraction)
5. ADD `fracdiff_d05_close` to V3_FEATURE_COLUMNS_TOP_N (14 → 15) — single axis under test
6. UPDATE `_verify_feature_columns` assertions
7. ADD 5 adversarial tests at `tests/features_v3/test_fracdiff_d05_universal.py`
8. UPDATE ITERATION_LABEL = "v3-051"

**Pre-registered MERGE/NO-MERGE thresholds** (locked in brief Section 8):
See brief Section 8 — non-renegotiable post-hoc per cycle 4 discipline.

## Status

EDA + ranking COMMITTED. Three EDA scripts + 9 CSV outputs + 2 markdown synthesis files
written under `analysis/iteration_v3-051/`. Next: write Section-2-grounded research brief
at iter-v3/051 (setup + backtest commit follows).
