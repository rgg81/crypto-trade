# iter-v3/051 EDA Synthesis — multi-axis ranking

Generated by:
- `analysis/iteration_v3-051/multi_axis_eda.py` (4-axis trade-roster counterfactuals)
- `analysis/iteration_v3-051/axis_c_fracdiff_deep_dive.py` (fracdiff_d05_close IC + ADF + univariate)
- `analysis/iteration_v3-051/axis_c_regime_3d_compute.py` (regime_momentum_signed_3d on-the-fly IC + ADF + univariate)

**Anchor**: iter-v3/028 BASELINE_V3.md (+0.5101 IS / +0.5053 OOS multi-seed mean). iter-v3/050
SECOND CONFIRMATION = NO MERGE (multi-seed mean +0.32 IS / +0.74 OOS; IS regression -0.19
vs /028 anchor; FAILS BOTH-must-improve gate). Per-symbol customization stacking AT
CONFIRMATION confirmed system-level anti-pattern across 2 cycles
(`feedback_v3_per_symbol_lifts_oos_breaks_is.md` UPDATED 2026-05-10).

**Cycle 4 cadence**: #1 of 10 EXPLORATIONs. Per `feedback_v3_strict_10_to_1_cadence.md`
iter-v3/051-060 are 10 SEPARATE single-seed EXPLORATIONs; iter-v3/061 is the cycle 4
CONFIRMATION (do NOT collapse the 10th EXPLORATION into CONFIRMATION).

**Pre-iter-v3/051 setup commit (mandated by system-level rule):** REVERT per-symbol
customizations to iter-v3/028 architecture (clear V3_ATR_MULTIPLIERS_PER_SYMBOL, clear
block_long_for, restore V3_MODELS = 3-sym BCH+LDO+TRX). This is NOT the axis under test;
it is the new cycle 4 starting baseline mandated by the system-level confirmation of
`feedback_v3_per_symbol_lifts_oos_breaks_is.md`.

## Headline finding

The strongest cycle 4 #1 axis is **(c1) ADD `fracdiff_d05_close` to V3_FEATURE_COLUMNS_TOP_N
at universal scope (14 → 15)** alongside the per-symbol customization REVERT to /028
architecture. This is a NEW universal engineered feature addition at `--seeds 1 --n-trials
35 --clean-oof` against the cycle 4 starting baseline (iter-v3/028 architecture exactly).

EDA evidence:
- **ADF stationarity PASS across all 4 symbols** (p=0 for BCH/LDO/TRX, p=0.000086 for ALGO)
- **IC carve-out PASS** (max |IC| = 0.7381 with `vwap_dev_20` at LDO; vwap_dev_20 IS a
  source primitive for fracdiff_d05_close — both are close-derived; per
  `feedback_v3_engineered_feature_pivot.md` Category 2 composed feature gets IC carve-out
  vs source primitives)
- **Univariate Spearman significant (negative mean reversion signal) at all 4 symbols**
  (BCH ρ=-0.038 p=0.005; LDO ρ=-0.051 p=0.010; TRX ρ=-0.050 p=0.0002; ALGO ρ=-0.038 p=0.006)
- **Universal scope UNTESTED at multi-seed**: iter-v3/035 used fracdiff_d05_close per-symbol
  BCH-only (REJECTED at iter-v3/039 CONFIRMATION-NO-MERGE per the per-symbol-customization
  anti-pattern). Universal scope test is the cycle 4 HIGH-priority axis #1 per Critic FINAL
  `b6339c5` recommendation.
- **Already in feature parquets** (no feature regeneration needed)

The competing axis (c3) regime_momentum_signed_3d at universal scope has comparable EDA
evidence (max |IC| = 0.6192 PASSES strict gate; univariate ρ=-0.058 to -0.044) and IS
implemented as dead code in `compute_regime_momentum_signed_3d` (engineered_v3.py line 330).
However it requires feature regeneration (~5-10 min) to enable; AND the iter-v3/044
orchestrator-ad-hoc-pick was REVERTED before backtest with QR EDA-derived rationale that
3d does NOT discriminate per-symbol bottleneck. RANKED #2 (queued for iter-v3/052).

Axes (b1/b3/b4) per-symbol REVERT alone are SYSTEM-LEVEL MANDATED (carry-forward state
change, NOT axes under test). Axis (d) 3-sym universe restoration WITHOUT ALGO is
empirically equivalent to b4 (full REVERT to /028 baseline) — would converge to
+0.51/+0.51 multi-seed at CONFIRMATION; testing as standalone EXPLORATION is informational
only.

## Why other candidates were ELIMINATED or DEFERRED

### (a) ≡ (b1) LDO removal alone (drop LDO from V3_MODELS)
**ELIMINATED at EDA stage on IS axis FAIL.**

- /050 trade roster counterfactual: dropping LDO trades from daily PnL series gives
  IS Sharpe -0.015 (+0.4872 → +0.4947) and OOS Sharpe +1.510 (+1.166 → +2.015).
- /028 baseline counterfactual: dropping LDO from /028 trades gives IS Sharpe -0.006
  (+0.5101 → +0.5040) and OOS Sharpe +1.040 (+0.5053 → +1.5449).
- LDO IS Sharpe contribution is essentially ZERO across all reference iterations:
  +0.85 wpnl IS contribution at /050 (vs +43.90 at /045 single-seed lottery) is
  near-zero IS impact.
- LDO OOS contribution is the MAJOR drag: -19.13 wpnl across 3 consecutive single-seed=42
  iterations (/047, /049, /050).
- BOTH-must-improve at EDA stage: LDO removal lifts OOS but does NOT lift IS. **FAILS
  on IS axis** per `feedback_v3_strict_both_is_oos_baseline.md` BOTH-must-improve rule.

QUEUED for iter-v3/052 EXPLORATION (universe contraction axis; structurally distinct
from feature axis).

### (b2) per-symbol ATR REVERT alone (clear V3_ATR_MULTIPLIERS_PER_SYMBOL)
**NOT PRECISELY SIMULABLE at EDA stage.**

- per-symbol ATR (2.0, 1.5) for ALGO+LDO at /050 modifies SL/TP price levels per trade;
  trade rosters from iter-v3/045 (which has the per-sym ATR ON) differ from a
  counterfactual /045 with default ATR by Optuna re-tune.
- The closest empirical proxy is iter-v3/028 baseline (default ATR + 3-sym universe);
  but that's not a "REVERT alone" since it's also a 3-sym universe.
- Cannot precisely simulate the per-sym ATR REVERT in isolation without a backtest run.

QUEUED for iter-v3/053+ EXPLORATION (could be tested as standalone EXPLORATION).

### (b3) primitive 10 BCH LONG block REVERT alone (clear block_long_for)
**ELIMINATED on multi-seed compression precedent.**

- iter-v3/045 single-seed (proxy for primitive 10 OFF): IS +0.7460 / OOS +3.5259.
- iter-v3/045 was the iter-v3/050 PROMISING anchor that COMPRESSED -57.3% IS / -79.0%
  OOS at multi-seed CONFIRMATION-spec.
- Standalone primitive 10 REVERT at multi-seed is expected to converge toward iter-v3/044
  multi-seed equivalent (which doesn't exist as a baseline — would need multi-seed run).
- The iter-v3/045 single-seed peak +3.53 OOS was a "lottery winner" per /050 diary
  retrospective; testing primitive 10 REVERT at single-seed risks repeating the lottery
  artifact.

QUEUED for iter-v3/054+ EXPLORATION if cycle 4 #1 produces NEGATIVE.

### (b4) full per-symbol REVERT to iter-v3/028 architecture
**ELIMINATED because IDENTITY = no axis variation.**

- Full REVERT = iter-v3/028 baseline state. Multi-seed reference is +0.5101 IS / +0.5053 OOS.
- Running iter-v3/028 architecture as a standalone EXPLORATION would consume a slot to
  re-confirm what we already know. NOT an axis under test.
- HOWEVER: the cycle 4 #1 EXPLORATION SETUP must implement the REVERT as the new
  cycle 4 starting baseline (mandated by the system-level rule from
  `feedback_v3_per_symbol_lifts_oos_breaks_is.md`). The single axis under test (e.g.,
  fracdiff_d05_close universal) is added ON TOP of this REVERTed baseline.

PARTIALLY-INCORPORATED into cycle 4 #1 setup (REVERT step is part of carry-forward state).

### (c2) hurst_drift_50_200 (composed engineered feature)
**DEFERRED on implementation cost.**

- New feature would require: (1) compute function in engineered_v3.py (~30 LOC), (2) add
  to V3_FEATURE_COLUMNS_TOP_N (14 → 15), (3) regenerate feature parquets (~10 min),
  (4) 5+ adversarial tests, (5) ADF + IC + univariate validation runs.
- Total implementation: ~1.5h setup + 25-40 min backtest = ~2h+ wall-clock; tight against
  EXPLORATION 2h cap.
- No EDA evidence of signal yet — feature has not been computed. Univariate test would
  need to run on fresh feature compute.

QUEUED for iter-v3/055+ EXPLORATION when budget allows.

### (c3) regime_momentum_signed_3d at universal scope
**ELIGIBLE but RANKED #2 — defer to iter-v3/052.**

EDA evidence (computed on-the-fly from `close` + `hurst_100` primitives in parquets):
- ADF stationarity p=0 across all 4 symbols (strongly stationary).
- IC: max |IC| = 0.6192 with vwap_dev_20 at BCH — PASSES STRICT |IC|<0.70 gate
  (no carve-out needed; cleaner than fracdiff_d05_close on IC criterion).
- Univariate Spearman: BCH ρ=-0.058 p=1e-5; LDO ρ=-0.044 p=0.024; TRX ρ=-0.068 p=4e-12;
  ALGO ρ=-0.058 p=3e-5 — all 4 symbols significant negative (mean reversion).
- Implementation: `compute_regime_momentum_signed_3d` already exists as dead code in
  engineered_v3.py:330 (orchestrator pre-iter-v3/044 setup, REVERTED before backtest).
  Reactivation cost: enable dispatch in engineered_v3.py + add to V3_FEATURE_COLUMNS_TOP_N
  + regenerate parquets (~10 min).

Why RANKED #2 instead of #1 vs fracdiff_d05_close:
- iter-v3/044 QR EDA (cycle3_is_diagnosis.py SHA `eff841e`) FALSIFIED 3d at the IS-axis
  bottleneck symbol: "regime_momentum_signed_3d does NOT discriminate ALGO LONG WR
  (22.2% > 0, 13.3% <=0)". The 3d horizon's ALGO signal is structurally weak.
- regime_momentum_signed_5d already in V3_FEATURE_COLUMNS_TOP_N as the iter-v3/028
  edge ingredient. Adding 3d alongside 5d risks the
  `feedback_v3_engineered_features_dont_stack.md` pattern — IC between 3d and 5d at BCH
  is 0.47 (moderate; not redundant but correlated).
- fracdiff_d05_close is structurally distinct (linear FFD transform of close; orthogonal
  to regime_momentum's sign-flip composition).

QUEUED for iter-v3/052 EXPLORATION; if /051 fracdiff_d05_close is PROMISING, /052 tests
3d as alternative; if /051 NEGATIVE, /052 tests 3d as fallback for cycle 4 #2.

### (d) 3-symbol universe restoration WITHOUT ALGO
**ELIMINATED — empirically equivalent to b4 (full REVERT).**

- iter-v3/050 trade roster counterfactual: dropping ALGO trades from daily PnL gives
  IS Sharpe +0.319 (+0.4872 → +0.8062) and OOS Sharpe -0.638 (+1.166 → +0.5284).
- The "+0.32 IS lift" is from removing ALGO's IS daily-PnL volatility contribution; not
  from a structural improvement in the model.
- Multi-seed Optuna re-tune at the 3-sym BCH+LDO+TRX universe with default ATR + no
  primitive 10 = exactly the iter-v3/028 baseline architecture = multi-seed
  +0.5101 IS / +0.5053 OOS.
- Therefore the +0.32 IS lift compresses to ZERO at multi-seed (it converges to the
  /028 baseline metrics).
- Single-seed test at iter-v3/051 would consume a slot to re-confirm baseline.

PARTIALLY-INCORPORATED into cycle 4 #1 setup (REVERT step includes universe restoration
to 3-sym BCH+LDO+TRX).

## Candidate (c1) detail — fracdiff_d05_close at universal scope

### Mechanism

`fracdiff_d05_close` = López de Prado AFML Chapter 5 Fixed-Width Window Fractional
Differentiation at d=0.5 applied to close price series. Theoretically: preserves long-memory
property of price series while achieving stationarity (avoids the standard differencing
trade-off between memory loss and unit-root removal).

The feature is already implemented (`compute_fracdiff_d05_close` in fracdiff_v3.py) and
present in all 4 symbol parquets. Universal-scope addition requires NO new feature code
and NO feature regeneration — only adding to `V3_FEATURE_COLUMNS_TOP_N` (14 → 15) and
updating `_verify_feature_columns` assertions.

### Why this candidate (cycle 4 #1)?

1. **Cycle 4 HIGH-priority axis #1** per Critic FINAL `b6339c5` recommendation #5
   (iter-v3/050 diary §Cycle 4 Priorities axis #1).
2. **NEW universal feature family** per `feedback_v3_structural_over_knob_exploration.md`
   priority hierarchy (NEW feature families > NEW model arch > NEW labeling > knob tunes).
3. **Universal scope UNTESTED at multi-seed**: iter-v3/035 used per-symbol BCH-only;
   iter-v3/039 CONFIRMATION-NO-MERGE rejected the per-symbol bundle but did NOT falsify
   the universal feature axis itself.
4. **Stationary, signal-bearing, IC carve-out PASS**: 4 of 4 symbols ADF p<0.05; 4 of
   4 univariate Spearman significant; IC violations confined to source primitives.
5. **Aligned with cycle 4 starting hypothesis**: lift IS Sharpe via UNIVERSAL axes
   (per-symbol customizations rejected at bundle level).
6. **Implementation feasible within 2h cap**: zero feature regen; only feature-column-list
   update + 5 adversarial tests + backtest = ~30-45 min total setup + 25-40 min backtest.
7. **First post-system-level-confirmation test**: validates whether the system-level rule
   (per-symbol REVERT) PRESERVES the iter-v3/028 baseline metrics AND whether a new
   universal feature can lift IS Sharpe further.

### EDA evidence (deep dive)

#### Per-symbol max |IC| with existing 14 features

```
symbol     max     mean
ALGOUSDT  0.5881  0.2524
BCHUSDT   0.6597  0.2640
LDOUSDT   0.7381  0.3138   <-- max overall
TRXUSDT   0.5238  0.2384
```

#### Top 5 |IC| pairs (carve-out evaluation)

```
symbol     candidate           existing_feature           abs_ic   is_source_primitive
LDOUSDT    fracdiff_d05_close  vwap_dev_20                0.7381   YES
LDOUSDT    fracdiff_d05_close  regime_momentum_signed_5d  0.6721   NO
BCHUSDT    fracdiff_d05_close  vwap_dev_20                0.6597   YES
BCHUSDT    fracdiff_d05_close  regime_momentum_signed_5d  0.6402   NO
LDOUSDT    fracdiff_d05_close  ema_spread_atr_20          0.5985   YES
```

The only IC violations (>0.70) are with `vwap_dev_20` at LDO. Both are close-derived
primitives (vwap_dev_20 = (close - vwap)/vwap; fracdiff_d05_close = FFD(close, d=0.5)).
Per `feedback_v3_engineered_feature_pivot.md` Category 2 carve-out: composed features
get IC carve-out vs source primitives. **CARVE-OUT PASSES.**

The next-highest IC after carve-out: `regime_momentum_signed_5d` at LDO (0.6721) and BCH
(0.6402) — moderate but BELOW strict 0.70 gate. NO additional carve-out needed.

#### ADF stationarity (IS subset, full-sample)

```
symbol     candidate           n_is_obs  adf_p_value  stationary_at_p005
BCHUSDT    fracdiff_d05_close  5528      0.000000     True
LDOUSDT    fracdiff_d05_close  2542      0.000000     True
TRXUSDT    fracdiff_d05_close  5470      0.000008     True
ALGOUSDT   fracdiff_d05_close  5026      0.000086     True
```

**ALL 4 symbols stationary at p<0.05.** The d=0.5 FFD is empirically achieving
stationarity as theoretically predicted by López de Prado.

#### Univariate Spearman vs forward 1-bar return (mean-reversion signal test)

```
symbol     candidate           n_obs  spearman_rho  spearman_p  significant_at_p005
BCHUSDT    fracdiff_d05_close  5527   -0.03753      0.00526     True
LDOUSDT    fracdiff_d05_close  2541   -0.05119     0.00985     True
TRXUSDT    fracdiff_d05_close  5469   -0.05016     0.00021     True
ALGOUSDT   fracdiff_d05_close  5025   -0.03849     0.00636     True
```

**ALL 4 symbols significant at p<0.05.** Negative ρ indicates mean-reversion: when
fracdiff_d05_close is HIGH (close has risen above its long-memory equilibrium), the
next 1-bar return tends to be NEGATIVE. The signal is small but consistent across 4
symbols.

NOTE per `feedback_v3_axis_selection_quant_discipline.md`: univariate Spearman is the
WEAKEST evidence type (warning from iter-v3/070 failure mode); used here as a sanity
check, NOT as primary justification. The primary justification is (1) stationarity,
(2) IC carve-out PASS, (3) presence in parquets, (4) cycle 4 axis #1 priority.

### Bundle assembly for iter-v3/051

**Carry-forward state from iter-v3/050 (with system-level REVERT applied)**:
- V3_FEATURE_COLUMNS_TOP_N = 14 (UNCHANGED — to be 15 after axis ADD)
- V3_ATR_MULTIPLIERS_PER_SYMBOL = {} (REVERT — system-level mandate; was
  {ALGOUSDT: (2.0, 1.5), LDOUSDT: (2.0, 1.5)} at /050)
- V3_FEATURES_PER_SYMBOL = {} (UNCHANGED empty)
- block_long_for = () (REVERT — system-level mandate; was ("BCHUSDT",) at /050)
- block_short_for = () (UNCHANGED)
- V3_MODELS = (BCH, LDO, TRX) — 3 symbols (REVERT — system-level mandate; was
  (BCH, LDO, TRX, ALGO) at /050)
- REQUIRED_GAP = 66 = (21+1)*3 (REVERT from 88 — universe contraction)
- regime_momentum_signed_5d PRESERVED in V3_FEATURE_COLUMNS_TOP_N (UNCHANGED — iter-v3/028
  baseline ingredient)
- adx_threshold = 20.0 (UNCHANGED global; per-symbol field empty)
- All other risk gates UNCHANGED

**Single axis under test**:
- ADD `fracdiff_d05_close` to V3_FEATURE_COLUMNS_TOP_N (14 → 15)
- ADD assertion in `_verify_feature_columns`: `fracdiff_d05_close` MUST BE PRESENT
- ADD comment block in `features_v3/__init__.py` documenting the iter-v3/051 axis

### Compoundability with future cycle 4 EXPLORATIONs

If iter-v3/051 PROMISING-clean: fracdiff_d05_close at universal scope carries forward as
cycle 4 PROMISING bundle ingredient. iter-v3/052+ test additional NEW universal features
(regime_momentum_signed_3d as candidate #2; LDO removal as alternative axis if needed).

If iter-v3/051 NEGATIVE-clean: fracdiff_d05_close at universal scope CLOSED for cycle 4
(joins the dead-paths catalog). iter-v3/052 pivots to regime_momentum_signed_3d as
cycle 4 NEW universal feature axis #2.

### Predicted behavioral effect

**Bundle starting state predictions (post-system-level REVERT, axis OFF, 3-sym universe,
default ATR, no primitive 10) — should match iter-v3/028 baseline at single-seed
n_trials=35**:
- Bundle IS Sharpe baseline reference: +0.5101 (multi-seed mean from /028); single-seed
  EXPLORATION reference would be in band [+0.40, +0.65] (approx ±0.10 single-seed
  variability vs multi-seed mean per cycle 3 base rate).
- Bundle OOS Sharpe baseline reference: +0.5053 (multi-seed mean from /028); single-seed
  in band [+0.30, +0.80] (similar variability).

**Bundle prediction with axis ON (fracdiff_d05_close ADDED universal scope, single seed=42,
n_trials=35)**:
- Bundle IS Sharpe: predicted band [+0.45, +0.85] (mean +0.65); lift +0.05 to +0.30
  vs baseline reference. Rationale: stationary feature with significant univariate
  signal across 4 symbols; tree models can use it for split decisions in regions where
  existing 14 features have flat importance distributions (TRX especially).
- Bundle OOS Sharpe: predicted band [+0.30, +0.85] (mean +0.55); regression up to -0.20
  OR lift up to +0.30. Rationale: OOS uncertainty is structural at single-seed n_trials=35;
  the EDA-evidenced effect is signal-positive but Optuna response uncertain.
- IS-OOS daily Sharpe ratio: predicted ∈ [0.5, 2.0] (clean lift, not suspicious — per
  `feedback_v3_engineered_features_dont_stack.md` falsifier band).
- Bundle IS trade count: predicted 130-200 (reference: iter-v3/028 IS = 156 with default
  ATR + 3-sym universe; single-seed deviation expected ±20%).
- Bundle OOS trade count: predicted 70-110 (reference: iter-v3/028 OOS = 95).
- fracdiff_d05_close importance rank: predicted top-5 in at least 1 of 3 symbols (per
  iter-v3/035 BCH-only result where it carried 50%+ of split importance for BCH).

**Falsifier (axis-saturation predictor per `feedback_v3_axis_saturation_predictor.md`)**:
- If observed IS Sharpe Δ < -0.10 vs baseline reference: NEGATIVE-clean (axis CLOSED at
  /051; pivot to regime_momentum_signed_3d at /052).
- If observed OOS Sharpe Δ < -0.30 vs baseline reference: NEGATIVE-clean (axis CLOSED).
- If fracdiff_d05_close importance rank > 12/15 in ALL 3 symbols: PROMISING-INERT (the
  feature was not learned by the model; INERT-OVERFIT pattern per
  `feedback_v3_inert_features_at_higher_budget.md`).
- If IS-OOS daily Sharpe ratio outside [0.5, 2.0]: NEGATIVE-SUSPICIOUS-OOS (the
  iter-v3/026/027 anti-pattern).
- If IS trade count change > 30% relative: cascade effect; investigate.

### Saturation context

- Cycle 3 NEW universal engineered feature axis CLOSED at iter-v3/048 (5 attempts: /035
  BCH-only fracdiff PROMISING but bundle FAIL, /041 universal pruning, /043
  efficiency_ratio_50, /044 regime_momentum_signed_3d ad-hoc REVERTED before backtest,
  /048 vol_normalized_ret_5d INERT).
- Cycle 4 RE-OPENS the NEW universal engineered feature axis per /050 diary §Cycle 4
  Priorities HIGH-priority axis #1 ("NEW universal engineered features ... fracdiff_d05_close
  for ALL 4 symbols (universal scope; iter-v3/035 used BCH-only — universal scope unstested
  at multi-seed)").
- iter-v3/051 is the FIRST cycle 4 EXPLORATION; saturation predictor will start fresh
  at attempt #1.

## Final ranking

| Rank | Axis | EDA verdict | Setup cost | Selected? |
|------|------|-------------|---:|-----------|
| 1 | (c1) fracdiff_d05_close UNIVERSAL | EDA-validated (stationary; IC carve-out PASS; univariate sig.) | 30-45 min (no feature regen) | YES |
| 2 | (c3) regime_momentum_signed_3d UNIVERSAL | EDA-validated (cleaner IC pass; same univariate signal) | 60-80 min (feature regen + dead-code activation) | Queued for /052 |
| 3 | (a) ≡ (b1) LDO removal alone | EDA FAIL on IS axis (-0.015 to -0.006 IS regression) | 30-45 min (universe contraction) | Queued for /052+ |
| 4 | (b2) per-symbol ATR REVERT alone | NOT SIMULABLE; no EDA evidence | 30-45 min | Queued for /053+ |
| 5 | (b3) primitive 10 REVERT alone | ELIMINATED (single-seed lottery precedent) | 30-45 min | Queued for /054+ |
| 6 | (b4) full REVERT (NO axis variation) | IDENTITY — incorporated into /051 setup | n/a | INCORPORATED |
| 7 | (d) 3-sym universe restoration WITHOUT ALGO | EQUIVALENT to (b4); incorporated | n/a | INCORPORATED |
| 8 | (c2) hurst_drift_50_200 | DEFERRED (new feature code; ~1.5h+ implementation) | 90+ min | Queued for /055+ |

## Implementation plan (axis c1 — fracdiff_d05_close UNIVERSAL)

### Setup commit changes

1. **REVERT per-symbol customizations** (system-level mandate; NOT axis under test):
   - `run_baseline_v3.py:V3_MODELS` = (BCHUSDT, LDOUSDT, TRXUSDT) — 3 symbols (drop ALGO)
   - `run_baseline_v3.py:V3_ATR_MULTIPLIERS_PER_SYMBOL` = {} (clear; default fallback for all)
   - `run_baseline_v3.py:_build_v3_model` RiskV2Config: `block_long_for=()` (clear; was BCH)
   - `run_baseline_v3.py:REQUIRED_GAP` = 66 = (21+1)*3 (revert from 88 due to 3-sym)
2. **ADD fracdiff_d05_close** (single axis under test):
   - `src/crypto_trade/features_v3/__init__.py:V3_FEATURE_COLUMNS_TOP_N` = 14 → 15 (add
     `fracdiff_d05_close` as element 15; place after regime_momentum_signed_5d)
   - Update `_verify_feature_columns` in run_baseline_v3.py:
     - `n != 15` (was `n != 14`)
     - ADD assertion: `fracdiff_d05_close` MUST BE PRESENT in V3_FEATURE_COLUMNS_TOP_N
     - REMOVE assertions referencing iter-v3/049 specific state (ATR per-sym + primitive 10
       + adx_threshold_per_symbol)
   - Update docstring on V3_FEATURE_COLUMNS_TOP_N to "Top-15 feature subset (as of
     iter-v3/051): fracdiff_d05_close ADDED at universal scope per cycle 4 #1 EXPLORATION"
3. **UPDATE ITERATION_LABEL** = "v3-051"
4. **5 adversarial tests** at `tests/features_v3/test_fracdiff_d05_universal.py`:
   - test_fracdiff_d05_close_in_universal_feature_list
   - test_fracdiff_d05_close_present_in_all_4_symbol_parquets (BCH/LDO/TRX/ALGO; ALGO
     reserved-for-future even though not in /051 V3_MODELS)
   - test_fracdiff_d05_close_stationary_per_symbol (ADF p<0.05)
   - test_fracdiff_d05_close_no_lookahead (uses past-only data; FFD shift convention)
   - test_v3_models_is_3_symbol_at_iter_v3_051 (BCH/LDO/TRX; ALGO REVERTED)
5. **NO feature regeneration required** (fracdiff_d05_close already in parquets)

### Run command (LOCKED in brief Section 0.5)

```bash
uv run python run_baseline_v3.py --seeds 1 --n-trials 35 --clean-oof
```

(EXPLORATION single-seed; ENSEMBLE_SIZE auto=5; n_trials=35 EXPLORATION default per
`feedback_v3_exploration_n_trials_35.md`; --clean-oof guardrail per SHA `6a216b5`.)

### Estimated wall-clock

- Setup commit (REVERT + ADD + tests): 30-45 min
- Phase 5.5 gate: 5-10 min
- Backtest (3-sym + 15 features at n_trials=35 single-seed): 25-35 min
- Phase 6/7 reports: 10-15 min
- TOTAL: ≤1.5h (well within 2h EXPLORATION cap)

## See Also

- `multi_axis_eda.py` — main EDA covering 4 candidate axes (a/b/c/d)
- `axis_c_fracdiff_deep_dive.py` — fracdiff_d05_close per-symbol IC + ADF + univariate
- `axis_c_regime_3d_compute.py` — regime_momentum_signed_3d on-the-fly EDA (queued for /052)
- `axis_a_ldo_attribution.csv` — LDO attribution across 5 reference iterations
- `axis_b_subaxis_ranking.csv` — 4-axis ranking (b0-b4) of /050 trade-roster counterfactuals
- `axis_c_eng_feature_screening.csv` — initial 5-candidate engineered feature screen
- `axis_c_fracdiff_per_sym_ic.csv` — full per-symbol IC matrix for fracdiff_d05_close
- `axis_c_fracdiff_adf.csv` — ADF stationarity for fracdiff_d05_close
- `axis_c_fracdiff_univariate.csv` — Spearman ρ vs forward 1-bar return for fracdiff_d05_close
- `axis_c_regime_3d_ic.csv` — full per-symbol IC matrix for regime_momentum_signed_3d
- `axis_c_regime_3d_adf.csv` — ADF stationarity for regime_momentum_signed_3d
- `axis_c_regime_3d_univariate.csv` — Spearman ρ for regime_momentum_signed_3d
- `axis_d_3sym_restoration.csv` — 3-sym restoration counterfactual analysis
- `regime_momentum_importance.csv` — regime_momentum_signed_5d importance across /028 + /050
- `candidate_axes_ranking.md` — final ranking + rationale (sister doc to this synthesis.md)
