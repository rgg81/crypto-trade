# iter-v3/093 — Cycle-4 EXPLORATION #1 (the cycle-4 opener) — the DERIVATIVES-MICROSTRUCTURE REGIME-CONDITIONED BOOK / FILED BLOCKED — central result CONFOUNDED by an un-frozen base book / Critic Phase-7.5 OVERALL=BLOCK

**Date**: 2026-05-18
**Type**: EXPLORATION (cycle-4 slot #1 of 10 — the cycle-4 opener) — a genuine RE-ARCHITECTURE: a per-symbol vol-regime classifier (13 derivatives-microstructure features — funding 7 + basis 4 + cross-asset-BTC 2) gating position SIZE on the `v0.v3-059` base directional book via a probability-weighted `{calm 1.0, normal 0.6, stressed 0.0}` multiplier. Universe BCH/LDO/TRX. EXPLORATION-mode single-seed=42, `ENSEMBLE_SIZE=5`, `--n-trials 35`, wall-clock 1.62h (exit 0).
**Verdict**: **Critic Phase-7.5 OVERALL = BLOCK** — `briefs-v3/iteration_v3-093/review.md`, committed `710a51c`. The Critic was NOT re-run; the BLOCK verdict is FINAL.
**Classification**: **BLOCKED** — the iteration is methodologically confounded at the artifact level and cannot be FILED as a clean EXPLORATION-NEGATIVE. The Section-8 taxonomy (8.1 SUSPICIOUS … 8.5 PROMISING) presumes the `v0.v3-059` base book was held bit-frozen and the regime overlay is the sole varied object — and it was not. A confounded iteration has no valid Section-8 class.
**Decision**: **NO-MERGE.** /093's central result — OOS monthly Sharpe **+1.7783** (Δ +1.20 vs the /059 OOS anchor +0.5791) against an IS regression to **+0.7916** (Δ −0.30 vs the /059 IS anchor +1.0894) — is **CONFOUNDED** and is **NOT a usable data point about the regime overlay**. It cannot be cited as evidence for OR against the derivatives-microstructure-regime architecture in any future brief, catalog row, or CONFIRMATION-bundling QR.
**BASELINE_V3.md**: **UNCHANGED** — canonical /059 metrics (IS monthly Sharpe +1.0894 / OOS monthly Sharpe +0.5791), tag `v0.v3-059`. An EXPLORATION never updates the baseline regardless of classification; a BLOCKED iteration never updates it.
**Branch**: `iteration-v3/093`

---

## 1. What was attempted — the cycle-4 derivatives-microstructure re-architecture

iter-v3/093 is the cycle-4 opener and v3's first architecture to predict a **non-price target from a non-price information layer**. Cycle 3 (`diary-v3/iteration_v3-091.md` / `iteration_v3-092.md`) closed with the honest verdict that both architectures v3 had explored — per-symbol absolute-barrier LightGBM (/082-087) and cross-sectional `LGBMRanker` momentum-rank (/088-092) — are tapped out as routes to a merge-grade book; the structural fact uniting every v3 failure is that **every prior v3 architecture predicted from price-derived features only.** The /091 closeout (§9.3/9.4) and the committed `briefs-v3/cycle4_prep_memo.md` recommended Candidate A — the derivatives-microstructure state-conditioning architecture — and resolved the de-risking question (the data is available).

The /093 design (brief `5bb5a76`, EDA `359bd74`), formalized from the prep memo, is a **3-layer derivatives-microstructure regime-conditioned book**:

- **Layer 1** — a 13-feature derivatives-microstructure panel (`derivatives_state_v3.py`): funding (7), perp-spot basis (4), cross-asset BTC (2). The original 18-feature spec dropped its 5-feature OI leg at the Section-11 Phase-6 amendment (`c463d5f` / `e792336`) — integration test #5's coverage gate fired on `oi_log_delta_1` (BCH IS coverage 0.632 < the pre-registered 0.90), an unavoidable Binance `metrics`-archive history limitation; the disciplined call was to drop the un-EDA-validated leg rather than override a pre-registered gate.
- **Layer 2** — a per-symbol LightGBM multi-class classifier (`objective=multiclass`, `num_class=3`) whose label is the forward 21-bar realized-vol regime, terciled {calm, normal, stressed} on **train-window-only** cut-points.
- **Layer 3** — the regime classifier gates the `v0.v3-059` base directional book's position SIZE by a smooth probability-weighted multiplier `1.0·P(calm) + 0.6·P(normal) + 0.0·P(stressed)`. The base book was specified — brief Section 3.3 — as the canonical `v0.v3-059` per-symbol price-barrier LightGBM book, **bit-UNCHANGED**, with the regime overlay as the single varied object.

The EDA was genuine and the IS evidence was real: the funding rate and its sign-persistence carry a forward-vol-regime IC of +0.18 / +0.17 at the 21-bar horizon (vs only ~0.08 against forward returns — the "predict the regime, not the price" architectural settle), and the derivatives panel is orthogonal to price (max |IC| ≤ 0.32).

---

## 2. Why /093 is FILED BLOCKED — the central result is CONFOUNDED

**Critic FINAL `710a51c` (`briefs-v3/iteration_v3-093/review.md`): OVERALL=BLOCK.** The BLOCK is recorded honestly here. Checks 1-6, 9-12 PASS; Checks 7 and 8 FAIL and are BLOCKING.

### 2.1 — The BLOCK: the "FROZEN /059 base book" is not frozen

The brief's entire single-axis claim rests on the `v0.v3-059` base directional book being held bit-fixed while the regime overlay is the sole varied object (brief Section 3.3, Section 6 defense #3). **The implementation contradicts this.** The Critic proved it three ways:

1. **Wrong inner-ensemble seeds.** `_run_059_base_book` (`run_derivatives_regime_v3.py` line 726) hardcodes `ensemble_seeds_059 = [42, 123, 456, 789, 1001]` — the legacy literal seeds. The `v0.v3-059` canonical baseline runs with the *derived* seeds `_derive_ensemble_seeds(42, 5) = [191664963, 1662057957, 1405681631, 942484272, 929893137]` (`run_baseline_v3.py:101`). Different inner seeds → different `TPESampler` trajectories → different `best_params` → different trained ensembles → a different trade roster.
2. **The base book is re-run, not loaded.** `_run_059_base_book` calls `run_backtest` on the /093 worktree's re-fetched `data/`. A walk-forward LightGBM+Optuna is stochastic per training run.
3. **Empirical proof — the rosters diverge.** /059 `in_sample/trades.csv` first trade `BCHUSDT open_time=1643587199999`; /093's first trade `BCHUSDT open_time=1644479999999`. Three /059 OOS BCH trades (`1744329599999`, `1746719999999`, `1748015999999`) are ABSENT from /093's OOS roster. The /093 base-book roster is **neither bit-identical to nor a subset of** the /059 anchor.

**Why this BLOCKs.** The registered comparison is "regime overlay applied to the /059 book vs the /059 book." If the base book is itself a different roster than /059, the comparison measures *base-book re-seeding noise + regime overlay*, not the regime overlay in isolation. **The iteration cannot answer its own question** — the control was not held fixed. The OOS +1.20 lift and the IS −0.30 regression are dominated by the differently-seeded base book drawing a different (single-draw OOS-favorable) roster — a single-seed base-book lottery (the `feedback_v3_single_seed_frozen_baseline.md` family) — NOT the regime overlay.

### 2.2 — The arithmetic that makes the confound certain, not merely possible

The regime gate fired "stressed" on **only 1.5% of bars** (mean `stressed_fire_rate` 0.0153 across 125 month-rows; in OOS it zeroed 4 of 47 TRX trades and 0 of BCH+LDO). It is **arithmetically impossible** for a gate touching 1.5% of bars to move OOS monthly Sharpe from /059's +0.5791 to /093's +1.7783. A near-no-op overlay cannot triple a Sharpe — therefore the +1.20 OOS lift is, by elimination, the re-seeded base book. The OOS-soars / IS-regresses signature (OOS/IS 2.25) is the textbook overfit/confound flag — but Check 1 PASSED, so it is **not a feature/label leak**; it is the base-book re-seeding defect.

### 2.3 — What is genuine in /093 (recorded for the record)

The Critic confirmed several things are sound and they are recorded so the BLOCK is not over-read:

- **Check 1 PASS — no look-ahead leak.** All 13 derivatives features are strictly past-only (`.shift(1)` conventions in `derivatives_state_v3.py`); the vol-regime label's tercile cut-points are train-window-only; the 22-candle embargo fully covers the 21-bar forward-vol horizon; the regime size multiplier reads only the past regime prediction.
- **Check 3 — the /092 anti-recurrence guard HELD.** DSR (−5.7536), PSR (0.9742), PBO (honest structural `null` sentinel + `frac_positive_paths` 0.7778) are **genuine `validation_v3` computations**, not /092-style hardcoded literals. `_verify_dsr_psr_call_sites` (runner lines 201-222) is a real load-bearing source-level grep preflight. The /090→/092 fabricated-gate defect class did **not** recur a third time — this is a genuine process win and it is recorded as such.

The BLOCK is a specific, contained reproducibility + hypothesis-implementation defect (Checks 7+8). It is not a leak and not a fabrication.

---

## 3. The Critic's 3 Recommendations — recorded as process lessons

The Critic closed with three process-level Recommendations. Per the v3 rule a methodology root-cause BLOCK becomes a NEW iteration with a new brief, new code, and a new backtest — the /093 artifact set cannot be FILED as a clean EXPLORATION-NEGATIVE because its central comparison is confounded. All three are recorded:

**Recommendation 1 — Genuinely freeze the /059 base book: LOAD the roster, do not re-run the architecture.** The "frozen oracle" premise requires bit-identity with the /059 anchor roster. Re-running a stochastic walk-forward LightGBM+Optuna — at any seed list, on any `data/` extent — cannot deliver that. The correct implementation reads the /059 base-book trade roster directly from `reports-v3/iteration_v3-059/{in_sample,out_of_sample}/trades.csv` and applies the regime-size multiplier to those exact `weight_factor` values. A `test_base_book_roster_matches_059` integration test asserting roster bit-identity catches this at Phase 6. **Recorded as a hard build requirement for any iteration that claims a frozen-base-book single-axis comparison.**

**Recommendation 2 — Every falsifier-driving input must be a correctly-unit'd, reproducible runner artifact with an integration test asserting both its existence AND its unit/granularity.** /090 (gross-Sharpe with an impossible anchor), /092 (DSR/PSR hardcoded literals), /093 (MaxDD unit mismatch — `_max_drawdown` emitted raw cumulative-PnL units 1845.24 vs /059's percentage-of-book 34.53, so F2 was un-evaluable; and no dedicated OOS regime-IC artifact for F3) are **three consecutive iterations with a falsifier-driving-input artifact gap.** The runner's `_max_drawdown` must emit MaxDD in the same unit as the anchor; the runner must emit the dedicated OOS regime-IC artifact F3 requires; integration tests must assert (a) `comparison.csv max_drawdown` is a percentage in a sane range and (b) the OOS regime-IC artifact exists and is finite. **Recorded as a standing methodology-axis discipline (`feedback_v3_methodology_axis_integration_test.md` extended to falsifier-driving artifacts).**

**Recommendation 3 — Drop or repair the `n_effective_trials` surrogate; never narrate a degenerate early-return as a computed result.** The runner feeds `n_effective_trials` a `(1, N)`-shaped array; the helper's `m.shape[0] < 2` guard returns 1 trivially (not a PCA). The engineering report Anomaly Note 4 narrated `n_eff = 1` as "PCA … identified rank 1" — a computation that did not occur. `n_eff` feeds no gate — an honest omission is strictly better than a fabricated PCA narrative. Either build a genuine `(n_trials × n_periods)` matrix and run the real PCA, or drop the field and state plainly it is not computed. **Recorded as a standing rule (same family as the /092 Critic Rec #3 — never narrate a constant as a computation).**

---

## 4. The META-LESSON — gate-flagged "verify in Phase 6" items MUST become hard build-tests

This is the load-bearing process finding of the /093 closeout, and the QR owns it explicitly.

**The Phase 5.5 gate SAW this defect coming.** The gate (`7be1167`, OVERALL=PASS) — Item 1, Item 3, and the "Additional Observations" bullet 3 — explicitly flagged it. The exact wording of the gate's third Additional Observation:

> *"The /059 base book as a 'frozen oracle': the brief's EDA and gate design implicitly treat the /059 trade roster as known (ORACLE EDA). This is valid because the gate is stateless. **The QE should verify in Phase 6** that `run_derivatives_regime_v3.py` runs the /059 base book unmodified (not a retrained version) so it IS the same roster as the canonical anchor — not a re-optimized one."*

And the gate's closing QE checklist, item 4: *"Verify the /059 base book is loaded as frozen (not retrained) in the new runner."*

**The Phase 5.5 gate identified the exact failure mode that BLOCKed the iteration — and then encoded it as a soft "should verify" instruction rather than a hard build-gate.** A soft instruction in a gate document is not executable. It does not fail a build. It relied on the QE reading the gate prose, internalizing it, and acting — and the un-frozen base book slipped untested all the way to Phase 7.5, wasting the entire `fetch-oi` build, the `derivatives_state_v3.py` module, the runner, and a 1.62h backtest.

**This is structurally the same defect family as Critic Rec #2's falsifier-artifact pattern** — and, one cycle earlier, the same family as the /092 process lesson (Section 6 of `diary-v3/iteration_v3-092.md`): *a process Recommendation addressed "to QR" cannot catch a QE implementation gap; only a test in the build can.* At /092 the un-tested gap was the DSR/PBO/PSR machinery; the structural fix was a `dsr.json` integration test. At /093 the un-tested gap is base-book bit-identity; a soft Phase-5.5 "should verify" was supposed to cover it and could not. **The /093 BLOCK is the third consecutive session BLOCK (/090 → /092 → /093), and the recurring root cause is the same: a known-foreseeable defect was flagged in a cheap phase as a soft instruction instead of a hard test, and slipped to the expensive phase.**

**THE FAIL-FAST FIX (recorded as a standing process rule):** **Any "verify in Phase 6" item that a Phase 5.5 gate flags MUST be converted into a hard integration test that fails the build — not a soft instruction.** Concretely: the Phase 5.5 gate's output format must change so that every "QE should verify X in Phase 6" line is restated as a named, mandatory integration test (`test_X`) in the brief's Section 9 test suite, and the Phase-6 build that omits it fails the suite *before* the backtest runs. A gate that can foresee a defect must be empowered to *block* it, not merely to *mention* it. This converts the Phase 5.5 gate from an advisory document into an executable contract — which is the only durable fix, exactly as the /092 closeout concluded for the `dsr.json` test. (Recorded for a new memory rule: `feedback_v3_gate_flags_become_hard_tests.md`.)

---

## 5. The cycle-4 FAIL-FAST reckoning — the key deliverable

The user's fail-fast directive (`feedback_fail_fast.md`, 2026-05-18) is explicit: do not spend multi-hour backtests on iterations whose failure is foreseeable; catch failure in the cheap phases. It must be applied honestly to the cycle-4 direction — while holding the bold-research mandate (`feedback_v3_bold_research_mandate.md`) with equal force: "exhausted" is forbidden, defeatism is unacceptable, and the recommendation must be a genuine high-EV swing, not a timid retreat. The two principles are compatible: fail-fast kills the *known-low-ceiling* so compute flows to the *genuine bold test*.

### 5.1 — The honest signals on the derivatives-microstructure-regime-OVERLAY direction

Five facts, weighed without varnish:

1. **/093's headline is confounded** — the regime overlay is, strictly, untested. /093 produced no usable evidence for or against it.
2. **BUT the regime classifier fires "stressed" on only 1.5% of bars** — and this is a property of the **CLASSIFIER**, not of the base-book confound. F6 fired (the brief pre-registered a [8%, 45%] gate-fire band and an [20%, 40%] estimate; 1.5% is far below both). A genuinely-frozen re-run — Critic Rec #1, load the /059 roster instead of re-running it — would still feed the *same 13-feature panel* into the *same multi-class label* and produce a classifier with a **~1.5%-firing stressed gate.** The base-book confound does not touch the gate's fire rate.
3. **A regime-avoidance overlay firing 1.5% of the time is a near-no-op.** It can zero a handful of trades and modestly tweak drawdown — nothing more. It structurally cannot move a Sharpe by a material amount. The /093 EDA's expected-OOS-impact section even conceded this: the modal expectation was "a modest OOS Sharpe improvement driven by drawdown avoidance" — and that was the *optimistic* read, predicated on a ~33% tercile-balanced fire rate that did not materialize.
4. **/093's own EDA found the derivatives-microstructure layer predicts vol-REGIMES (+0.18 IC) but NOT returns (~0.08 IC).** This is a genuine, EDA-validated finding and it is worth carrying forward as *knowledge*: derivatives microstructure **cannot be a directional alpha source** in this construction — only a regime/risk layer.
5. **A regime/risk OVERLAY on /059 is structurally a modest risk-adjusted tweak.** It does not predict returns; it only re-sizes the existing /059 directional book. It cannot create new alpha, and it cannot lift /059 to a step-change. The mission bar is top-quant-firm-grade results (`feedback_v3_bold_research_mandate.md` SHARPENED) — and a 1.5%-firing avoidance gate on the existing book is, by its own arithmetic, not that.

### 5.2 — The three options, weighed

**(a) Re-do /093 properly with a genuinely-frozen /059 base book (Critic Rec #1).** This is the *methodologically correct* re-run — it would un-confound the comparison. **But fail-fast questions it directly.** The thing it would cleanly measure is a **1.5%-firing near-no-op overlay** — and the 1.5% is classifier-determined and base-book-independent (§5.1 point 2). The honest pre-registered outcome of a correct re-run is NEGATIVE-no-effect at very high probability: a near-no-op gate produces a near-no-op result. Spending a build + a 1.6h+ backtest to confirm "the overlay is a near-no-op" — when the 1.5% fire rate already *tells us* it is a near-no-op from a cheap committed artifact (`per_month_regime_stats.csv`, already in hand) — is the textbook foreseeable-modest grind that `feedback_fail_fast.md` exists to prevent. **REJECTED on fail-fast grounds.**

**(b) Re-do with a materially recalibrated regime construction** — e.g. a 2-state {calm, stressed} label with a deliberately low stressed cut-point so the gate fires on a meaningful fraction; or replacing terciles with an absolute-vol threshold; or a hysteresis gate. This is *less* foreseeably-dead than (a) — a gate that actually fires can actually matter. **But it is still an OVERLAY on /059** — §5.1 point 5 is unchanged: a recalibrated avoidance gate is still a risk/exposure re-sizer, not an alpha source. The ceiling is still "modest risk-adjusted improvement on the existing book," and v3 has run risk-gate-recalibration axes to exhaustion (the cycle-1/2 knob axes; `feedback_v3_structural_over_knob_exploration.md`). Recalibrating a gate is a knob axis wearing a re-architecture costume. **REJECTED — low ceiling, and structurally a knob axis.**

**(c) Pivot the cycle-4 direction to a genuinely bolder, higher-EV axis.** This is the fail-fast-AND-bold-mandate-compatible choice — *provided* the pivot is to a concretely-researched axis with a genuine high-EV case, not a vague gesture. §5.3 makes that case.

### 5.3 — THE RECOMMENDATION: pivot to a DERIVATIVES ORDER-FLOW DIRECTIONAL architecture (a non-price ALPHA model, not a regime overlay)

**Recommended cycle-4 direction (for iter-v3/094, the next EXPLORATION slot): a per-symbol DIRECTIONAL model whose feature panel is the DERIVATIVES ORDER-FLOW state — signed taker-volume imbalance, open-interest delta, and the long/short positioning ratios — predicting the forward price-barrier label directly. The derivatives microstructure becomes the ALPHA signal, not a risk overlay.**

This is **not** "re-do /093." It is a different architecture, and it directly answers the honest weakness in the whole /093 line. The reasoning:

**The pivot keeps cycle 4's genuine insight and discards its dead end.** Cycle 4's correct premise — recommended by the /091 closeout and the prep memo — is that v3 has *never used a non-price information layer*, and that crypto perps carry a mechanically-causal one. /093 confirmed the data is real and orthogonal (max |IC| ≤ 0.32 vs price). The dead end was the *role* assigned to it: a regime-avoidance OVERLAY, which /093's own EDA showed cannot be directional (regime IC +0.18, return IC only ~0.08) and which the 1.5% fire rate showed is a near-no-op. The pivot keeps the non-price information layer and changes the role from **risk overlay → directional alpha** — and uses a *different slice* of the derivatives state (order flow) that the literature specifically identifies as directionally predictive.

**The research grounding is concrete and strong — and it is a different finding from /093's EDA.** /093's EDA tested *funding + basis* against forward *returns* and found a weak ~0.08 IC. It did **not** test **order flow** — signed taker-volume imbalance. The directly relevant paper is **Anastasopoulos, Gradojevic, Liu, Maynard & Tsiakas, "Order Flow and Cryptocurrency Returns"** (*Journal of Financial Markets*, 2024-26; SSRN 5020002): non-linear ML models conditioning on **daily order flow** produce long-short portfolios with an alpha of **0.79% per day (t ≈ 5.85)** and an **annualized Sharpe ≈ 3.63-3.68** — order flow *dominates* economic fundamentals for out-of-sample prediction, and the result "cannot be explained by limits to arbitrage." Crucially, the paper's order flow is a **daily-aggregated signed-volume measure** — it does NOT require tick/L2 order-book data. (Contrast: the tick-level microstructure literature — e.g. arXiv 2602.00776, 3-second-horizon L1 order-flow imbalance — *does* require L2 data and is correctly out of scope for v3's 8h cadence; the Anastasopoulos et al. daily-order-flow result is the in-scope one.)

**The data is already fetched — fail-fast's cheap-precondition test is already passed.** Order flow at the 8h bar is computable from data v3 *already has on disk*:
- The per-kline **taker-buy-base-volume** column is already in every `data/<SYM>/8h.csv` Binance kline (`takerBuyVol`) — signed taker imbalance per 8h bar = `(2·takerBuyVol − volume) / volume` is a direct OHLCV-derived field, no new fetch.
- The `metrics` archive **already fetched for /093** (`fetch-oi`, cached in `data/open_interest/`) carries `count_long_short_ratio`, `count_toptrader_long_short_ratio`, `sum_taker_long_short_vol_ratio` — the positioning + aggression ratios — at 5-min granularity, resampled to 8h. /093's `fetch-oi` build is **not wasted**: it is the precise data feed iter-v3/094 needs.
- So the iter-v3/094 setup is *smaller* than /093's — no new fetcher. This is the fail-fast point: the expensive precondition (does the data exist, is it fetched) is **already cleared by /093's own build**.

**Why this is genuinely higher-EV than (a) or (b), held against the bold mandate.** (a)/(b) re-test a risk overlay that cannot create alpha — ceiling = "modest risk-adjusted tweak on /059." The order-flow directional architecture is a **new alpha source**: a per-symbol directional model whose features are a documented-predictive non-price signal class. It attacks the structural root cause (price-myopia) the way cycle 4 was *meant* to — but in the directional-alpha role the literature actually supports, not the regime-overlay role /093's EDA falsified. It is a genuine re-architecture (new feature class, directional-alpha role), it has a concrete peer-reviewed Sharpe-3.6 precedent, the data is on disk, and the honest outcome distribution has real PROMISING mass — it is exactly the "genuinely-uncertain, EDA-validated bold test" `feedback_fail_fast.md` says resources SHOULD go to.

**The honest caveats — recorded so the iter-v3/094 brief inherits them, not so they kill the axis.** (i) The Anastasopoulos et al. result is *cross-sectional* long-short on a broad universe and *daily* frequency; v3 is per-symbol on 3 symbols at 8h — the IS→OOS and the construction transfer is the load-bearing uncertainty and the iter-v3/094 EDA must measure 8h-bar order-flow→forward-barrier IC on the BCH/LDO/TRX walk-forward window before the brief (per `feedback_v3_axis_selection_quant_discipline.md`). (ii) iter-v3/082's funding-rate features ranked 15-18/18 INERT — but order flow is a *different* derivatives slice (realized trade imbalance, not the funding cost-of-carry) and, more importantly, /082 bolted features onto a price-barrier model as side-inputs; iter-v3/094 makes the order-flow panel the *primary* feature set of a per-symbol directional model, the construction /082's own §11 Rec 1 names as exempt. (iii) The cheap GO/NO-GO is the iter-v3/094 EDA itself — if the 8h-bar order-flow→forward-barrier IC on the walk-forward window is at the ~0.08-or-below price-myopic ceiling, the axis is KILLED at Phase 5/5.5 with a committed EDA and no backtest (fail-fast applied as designed). The recommendation here is the *researched direction*; the iter-v3/094 EDA is its own Phase 1-5 GO/NO-GO.

**Recorded fallbacks** (carried from the prep memo, unchanged): Candidate B — crypto cointegration statistical arbitrage (zero new data, but altcoin cointegration is regime-fragile and turnover-sensitive); Candidate C — regime-switching TSMOM (lowest ambition; risks collapsing into a gate on a momentum book). Both remain available if the iter-v3/094 order-flow EDA comes back at the price-myopic ceiling.

---

## 6. Decision

**NO-MERGE. Critic Phase-7.5 OVERALL = BLOCK** (`briefs-v3/iteration_v3-093/review.md`, FINAL `710a51c`; the Critic was NOT re-run — the BLOCK is FINAL). iter-v3/093 is **FILED BLOCKED** — it has no valid Section-8 classification because the Section-8 taxonomy presumes a bit-frozen base book and the base book was not frozen.

**The central result is CONFOUNDED and is NOT a usable data point.** /093's OOS monthly Sharpe +1.7783 (Δ +1.20 vs the /059 anchor) against an IS regression to +0.7916 (Δ −0.30) is dominated by an un-frozen, re-seeded `v0.v3-059` base book drawing a different trade roster — a single-seed base-book lottery — NOT by the registered regime overlay. The Critic proved the base book is not bit-identical to the /059 anchor (`_run_059_base_book` hardcodes the legacy inner seeds `[42,123,456,789,1001]` instead of /059's derived seeds and re-runs a stochastic walk-forward rather than loading the /059 roster). With the regime gate firing "stressed" on only 1.5% of bars, it is arithmetically impossible for the overlay to triple the OOS Sharpe — the iteration cannot answer its own question. **/093's +1.78 OOS cannot be cited for or against the derivatives-microstructure-regime architecture in any future brief, catalog row, or CONFIRMATION-bundling QR.** It is NOT a leak (Check 1 PASS) and NOT a fabrication — the /092 anti-recurrence guard held and DSR/PBO/PSR are genuine (Check 3); it is a contained reproducibility + hypothesis-implementation defect (Checks 7+8).

**BASELINE_V3.md is UNCHANGED** — canonical /059 metrics (IS monthly Sharpe +1.0894 / OOS monthly Sharpe +0.5791), tag `v0.v3-059`. An EXPLORATION never updates the baseline; a BLOCKED iteration never updates it. `OOS_CUTOFF_DATE = 2025-03-24` and `training_months = 24` are untouched — the QR saw OOS for the first time in Phase 7.

**The Critic's 3 Recommendations are recorded as process lessons (Section 3):** (1) genuinely freeze the /059 base book — LOAD the roster, do not re-run the architecture, with a `test_base_book_roster_matches_059` bit-identity integration test; (2) every falsifier-driving input must be a correctly-unit'd reproducible runner artifact with an integration test asserting existence AND unit/granularity (the /090→/092→/093 falsifier-artifact pattern — MaxDD unit mismatch broke F2, no regime-IC artifact broke F3); (3) drop or repair the `n_effective_trials` surrogate and never narrate a degenerate early-return as a computed PCA result.

**THE META-LESSON (Section 4):** the Phase 5.5 gate **explicitly flagged** the un-frozen-base-book defect — Item 1, Item 3, and Additional Observation 3 ("the QE should verify in Phase 6 that the runner runs the /059 base book unmodified") — but as a **soft "should verify" instruction, not a hard build-gate.** A soft instruction does not fail a build; the un-frozen base book slipped untested to Phase 7.5 and wasted the `fetch-oi` build + the runner + a 1.62h backtest. This is the third consecutive session BLOCK (/090 → /092 → /093) with the same recurring root cause — a known-foreseeable defect flagged in a cheap phase as a soft instruction instead of a hard test — and it is the same defect family as Critic Rec #2's falsifier-artifact pattern and the /092 `dsr.json`-test process lesson. **THE FAIL-FAST FIX: any "verify in Phase 6" item a Phase 5.5 gate flags MUST be converted into a named, mandatory integration test that fails the build — not a soft instruction. The Phase 5.5 gate must become an executable contract, not an advisory document.** (Recorded for a new memory rule `feedback_v3_gate_flags_become_hard_tests.md`.)

**THE KEY FORWARD DECISION — the cycle-4 fail-fast reckoning (Section 5): PIVOT. Do NOT re-do /093.** Holding `feedback_fail_fast.md` AND `feedback_v3_bold_research_mandate.md` together: (a) re-doing /093 with a genuinely-frozen base book is methodologically correct but **re-tests a 1.5%-firing near-no-op overlay** — the 1.5% fire rate is a classifier property, base-book-independent, so a correct re-run's outcome (NEGATIVE-no-effect) is foreseeable from a cheap artifact already in hand; that is the textbook foreseeable-modest grind fail-fast forbids — REJECTED. (b) Recalibrating the regime construction is still an OVERLAY on /059 — a risk/exposure re-sizer, structurally a knob axis, low ceiling — REJECTED. (c) **PIVOT to a genuinely bolder, higher-EV axis — and the researched recommendation is a per-symbol DIRECTIONAL model whose feature panel is the DERIVATIVES ORDER-FLOW state (signed taker-volume imbalance + OI delta + the long/short positioning ratios), predicting the forward price-barrier label directly — derivatives microstructure as the ALPHA signal, not a regime overlay.** The grounding is concrete: Anastasopoulos, Gradojevic, Liu, Maynard & Tsiakas, "Order Flow and Cryptocurrency Returns" (*Journal of Financial Markets*, 2024-26; SSRN 5020002) — non-linear ML conditioning on **daily** (not tick) order flow yields a long-short alpha of 0.79%/day (t ≈ 5.85), annualized Sharpe ≈ 3.6, order flow dominating fundamentals for OOS prediction. The data is **already on disk** — per-kline taker-buy-base-volume in every `data/<SYM>/8h.csv`, plus the long/short ratios in the `metrics` archive **/093's own `fetch-oi` build already cached** — so iter-v3/094's setup is smaller than /093's and fail-fast's expensive-precondition (does the data exist) is already cleared. This pivot keeps cycle 4's genuine insight (v3 has never used a non-price information layer; /093 proved the layer is real and price-orthogonal) and discards its dead end (the regime-avoidance OVERLAY role, which /093's own EDA falsified as directional and the 1.5% fire rate showed is a near-no-op). The order-flow directional architecture is a genuine re-architecture in the directional-alpha role the literature actually supports — a real high-EV swing, fully compatible with the bold mandate. The iter-v3/094 EDA (committed before the brief per `feedback_v3_axis_selection_quant_discipline.md`) is its own Phase 1-5 GO/NO-GO: if the 8h-bar order-flow→forward-barrier IC on the BCH/LDO/TRX walk-forward window sits at the ~0.08 price-myopic ceiling, the axis is KILLED at Phase 5/5.5 with a committed EDA and no backtest — fail-fast applied as designed. Candidate B (crypto cointegration stat-arb) and Candidate C (regime-switching TSMOM) remain recorded fallbacks.

**iter-v3/093 is the first EXPLORATION of cycle 4 (slot #1 of 10).** The cycle-4 cadence advances; iter-v3/094 is slot #2. A closeout marker tag `v0.v3-093` is issued (annotated; NOT a baseline update — the `v0.v3-082`…`v0.v3-092` pattern; BASELINE_V3.md UNCHANGED at `v0.v3-059`).

---

**Commit chain:**
- Brief SHA: `5bb5a76` — `briefs-v3/iteration_v3-093/research_brief.md` (the cycle-4 derivatives-microstructure regime-conditioned book; Section 11 Phase-6 amendment `c463d5f` — OI-leg drop 18→13).
- Phase 5.5 gate SHA: `7be1167` (PASS — but flagged the un-frozen-base-book defect as a SOFT "should verify" instruction; see Section 4 meta-lesson).
- Setup SHA: `76af744` — `fetch-oi` CLI + `derivatives_state_v3.py` panel + `run_derivatives_regime_v3.py` runner + 6 integration tests; smoke-fix `e545d98`; OI-drop fix `e792336`.
- EDA SHA: `359bd74` — `analysis/iteration_v3-093/derivatives_microstructure_eda.py` + T1-T5 CSVs.
- Engineering report SHA: `0b5ecca` — `briefs-v3/iteration_v3-093/engineering_report.md` (IS +0.7916 / OOS +1.7783; F5+F6 fire; F2 INDETERMINATE — MaxDD unit mismatch; F3 cannot-confirm — no regime-IC artifact).
- Critic FINAL SHA: `710a51c` — `briefs-v3/iteration_v3-093/review.md` — **OVERALL=BLOCK** (Checks 1-6/9-12 PASS; Checks 7+8 FAIL BLOCKING — the un-frozen /059 base book; 3 Recommendations).
- Diary + catalog + tag SHA: this closeout — `docs(iter-v3/093): closeout diary + catalog — FILED BLOCKED / Critic OVERALL=BLOCK + cycle-4 fail-fast pivot`.
- **Reports**: `reports-v3/iteration_v3-093/` (confounded — not a usable data point).
- **Tag**: `v0.v3-093` (closeout marker; NOT a baseline update — BASELINE_V3.md UNCHANGED at `v0.v3-059`).
