# iter-v3/094 — Cycle-4 EXPLORATION #2 — derivatives ORDER-FLOW directional ALPHA / FILED NULL-AT-EDA — Phase-1 GO/NO-GO returned NO-GO; axis killed cheaply at the EDA, no brief, no backtest

**Date**: 2026-05-18
**Type**: EXPLORATION (cycle-4 slot #2 of 10) — Phase-1 hard FAIL-FAST GO/NO-GO checkpoint
**Verdict**: **NO-GO** — Phase-1 IS-only EDA. The derivatives order-flow-DIRECTIONAL axis is killed at the EDA. No 10-section research brief was written; no backtest was run.
**Classification**: **NULL-AT-EDA** — the axis is foreseeably-modest from a committed IS-only EDA artifact already in hand. Killing it cheaply at the EDA is the fail-fast WIN (`feedback_fail_fast.md`), not an iteration failure.
**Decision**: **NO-MERGE.** BASELINE_V3.md UNCHANGED — canonical /059 (IS monthly Sharpe +1.0894 / OOS monthly Sharpe +0.5791, tag `v0.v3-059`).
**Branch**: `iteration-v3/094`

---

iter-v3/094 was the cycle-4 PIVOT recommended by the /093 closeout (`diary-v3/iteration_v3-093.md` Section 5): use derivatives microstructure as a **directional ALPHA signal** — a per-symbol directional LightGBM whose feature panel is the **derivatives order-flow state** (signed taker-volume imbalance, OI delta, long/short positioning ratios), predicting the /059-style triple-barrier label, universe BCH/LDO/TRX. Research grounding: Anastasopoulos, Gradojevic, Liu, Maynard & Tsiakas, "Order Flow and Cryptocurrency Returns" (Journal of Financial Markets; SSRN 5020002) — non-linear ML on **daily** crypto order flow yields long-short alpha ~0.79%/day, annualized Sharpe ~3.6.

Phase 1 was an explicit hard FAIL-FAST GO/NO-GO checkpoint. A committed IS-only EDA (`analysis/iteration_v3-094/orderflow_return_ic_eda.py`, walk-forward-faithful per `feedback_v3_eda_walkforward_faithful` — measured on the runner's actual IS-eval window 2023-03-24..OOS_CUTOFF, not the full panel) constructed a 22-feature order-flow panel and measured its RETURN-IC. The decisive question: does derivatives order flow carry genuine directional return-predictability **materially stronger** than /093's ~0.08 price-myopic ceiling?

**It does not.** The architecture-faithful headline — barrier-label-IC (T3), per-symbol-best, cross-symbol mean — is **0.0981**, sitting at the ~0.10 price-myopic band, only marginally above /093's 0.08 and decisively below the 0.12 GO threshold. The NO-GO is unambiguous, not borderline, for three structural reasons surfaced by an honest adjudication of the T2 mixed-horizon headline (0.1406, which superficially looked above 0.12): **(1) horizon mismatch** — the strongest signal lives at the 21-bar (7-day) horizon, which the 9-candle-timeout triple-barrier label structurally cannot exploit; at the architecturally-correct 9-bar horizon the return-IC mean is 0.1110 and barrier-label-IC is 0.0981 — the ceiling, not a breakout; **(2) sign inconsistency** — BCH's strongest feature `of_oi_logdelta_9` is +0.145 on BCH but −0.097 on TRX; the sign flips across the 3-symbol universe with no economic reason — per-symbol curve-fit noise, non-transferable; **(3) the literature's headline signal fails** — signed taker-volume imbalance (`of_taker_imb` family, the paper's daily-order-flow analogue) is the *weakest* group, sub-0.04 barrier-label-IC with flipping signs on every symbol — the Anastasopoulos et al. cross-sectional daily Sharpe-3.6 result does NOT transfer to v3's per-symbol 8h BCH/LDO/TRX construction. Full evidence: `analysis/iteration_v3-094/NO_GO_NOTE.md` (the EDA closeout note) and the T1-T5 CSVs.

This is exactly the foreseeable-modest outcome `feedback_fail_fast.md` says resources should NOT go to — and it was killed for the cost of **one committed IS-only EDA script: no `fetch`, no runner, no backtest, no Critic.** The /093 closeout's own caveat (iii) ("if the 8h-bar order-flow IC sits at the ~0.08 price-myopic ceiling, the axis is KILLED at Phase 5/5.5 with a committed EDA and no backtest") fired precisely as designed. `OOS_CUTOFF_DATE = 2025-03-24` and `training_months = 24` untouched; the EDA is strictly IS-only (every measurement restricted to `open_time < OOS_CUTOFF_MS`); OOS was never touched.

The cycle-4 derivatives-microstructure line is now closed in **both** roles tested: /093 (risk-regime OVERLAY — BLOCKED + low-ceiling) and /094 (directional ALPHA feature — NO-GO at EDA). The honest structural lesson, consistent with /082 (funding INERT) and /086 (7-feed structural verdict): **crypto-native non-OHLCV feeds have not delivered transferable directional alpha on v3's per-symbol 8h architecture**, in either role. The next EXPLORATION (iter-v3/095) must NOT be an 8th crypto-native feed variant — recommended axes: the recorded cycle-4 fallback Candidate B (crypto cointegration statistical arbitrage — must clear its own cointegration-stability + turnover Phase-1 EDA GO/NO-GO), OR a genuine NEW model-architecture axis (a non-tree learner — shallow MLP / attention-pooled sequence model — on the existing validated 14-feature price-derived stack, attacking the "depth-3-5 tree cannot compose interactions" structural limitation directly). Per `feedback_v3_axis_selection_quant_discipline.md` the iter-v3/095 QR must select the axis with a committed EDA-driven quantitative basis.

iter-v3/094 is cycle-4 EXPLORATION slot #2 of 10; the cadence advances. A closeout marker tag `v0.v3-094` is issued (annotated; NOT a baseline update — the `v0.v3-082`…`v0.v3-093` pattern; BASELINE_V3.md UNCHANGED at `v0.v3-059`).

**Commit chain:**
- EDA SHA: `analysis(iter-v3/094): order-flow return-IC GO/NO-GO EDA + T1-T5 + NO_GO_NOTE` (this closeout's EDA commit).
- Diary SHA: this closeout — `docs(iter-v3/094): closeout diary — FILED NULL-AT-EDA / Phase-1 GO/NO-GO = NO-GO`.
- **No brief** (`briefs-v3/iteration_v3-094/` not created — NO-GO stopped the iteration at Phase 1).
- **No reports** (no backtest run).
- **Tag**: `v0.v3-094` (closeout marker; NOT a baseline update — BASELINE_V3.md UNCHANGED at `v0.v3-059`).
