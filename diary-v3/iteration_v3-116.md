# iter-v3/116 — Cycle-6 EXPLORATION slot #7 — TRADE-CONSTRUCTION / EXIT-LAYER: early-exit-on-no-confirmation exit primitive — FILED **EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass** — the Critic OVERRODE the mechanical first-match-wins NEGATIVE (IS +0.6246 < +0.7325 anchor-floor) in the UPGRADE direction (precedent /114 was the DOWNGRADE direction; iter-v3/116 establishes the UPGRADE-direction precedent). The IS underperformance vs the /060 anchor is a coherent regime-cost component of a regime-adaptive rule (IS spans the 2022 bear and 2023 chop where early-exit cuts trades that recover; OOS is a 2025-2026 uptrend where the slot-freeing cascade enables more trend-TP entries — the user explicitly noted 2026-05-20 that IS-having-more-data-and-more-bear-regimes-than-OOS naturally produces IS Sharpe below OOS for a regime-adaptive rule). The Critic's substantive override converged on three independent lines: (1) the IS drag has a coherent regime-cost mechanism, not unattributed IS deterioration; (2) the OOS lift is broadly distributed and mechanistically traceable — ALL 3 symbols positive Δ vs /060 on the stable `net_pnl_pct` metric (BCH +31.55, LDO +12.00, TRX +16.04), trade rosters NOT bit-identical to /060 (BCH 95%, LDO 85%, TRX 88% open-time match), the 11 new OOS entries at +3.67% mean PnL traceable bar-by-bar to the slot-freeing cascade; (3) the mechanism is mechanical book-composition (slot-freeing) — strictly-accretive on /059, NOT a new edge ingredient. **This is cycle 6's FIRST PROMISING after 6 consecutive NEGATIVEs (/110-/115).** All 8 Critic checks PASS for the verdict; single Critic round, zero clarifications.

**Date**: 2026-05-20
**Type**: EXPLORATION (cycle-6 slot #7 of 10; iter-v3/120 is the mandatory cycle-6 CONFIRMATION) — ran a full Phase 1–8 (EDA + brief + backtest + Critic, single Critic round), classified at Phase 7.5
**Verdict**: **EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass** per `feedback_promising_mechanical_subtype.md`. iter-v3/116 tested the cycle-6 STRUCTURAL trade-construction-layer axis the iter-v3/115 closeout recommended (an EXIT-LAYER axis) — but pivoted *bolder* than the dispatch's literal recommendation (a regime-conditioned exit-barrier architecture, Axis A): the QR's committed IS-only EDA (`analysis/iteration_v3-116/`, EDA SHA `52444c9`, 10 result tables) returned a hard NO-GO on Axis A (the T3a shuffle placebo FAILED 5/6: random bucketings captured equal-or-greater lift than the regime bucketing — the optimum collapsed to the (tp=1.0, sl=2.0) cell in 13 of 18 buckets, a known regime-exposed /065 pattern), pivoted to Axis B (scaled-entry — 0/81 positive cells, formal NO-GO), and lastly to Axis C (early-exit-on-no-confirmation: close the trade at candle K's close if no `+0.50` ATR favorable excursion observed within the first K=4 candles after entry). The Axis C EDA returned a NO-GO on the pre-registered g2 mechanism gate (`T8`: in all 48 cells across all 3 symbols, the rule cuts modestly-below-average winners, not losers), but the chosen `(trigger=0.50, K=4)` cell was the only cell with positive IS lift on all 3 symbols in the entire 16-cell grid — the residual-uncertainty axis worth running. Per THE PRIME DIRECTIVE the NO-GO set the brief's modal prediction (~55% INERT/NEGATIVE) and the iteration ran the Phase-6 backtest. The Phase-6 backtest (3-seed EXPLORATION, code SHA `d786104`, 0.70h) produced **IS monthly Sharpe +0.6246 / OOS monthly Sharpe +1.1089**. The Phase-7.5 Critic (`briefs-v3/iteration_v3-116/review.md`, FINAL — single round, zero clarifications requested) returned **OVERALL: EXPLORATION-PROMISING — subclass PROMISING-MECHANICAL** — the Critic exercised authority to override the mechanical first-match-wins NEGATIVE (IS +0.6246 < the +0.7325 anchor − 0.10 floor, fires Criterion 1a) on substantive grounds in the UPGRADE direction, on three independent legs (the coherent regime-cost mechanism, the broadly-distributed and mechanistically-traceable OOS lift, the mechanical book-composition diagnostic).
**Classification**: **EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass** per `feedback_promising_mechanical_subtype.md`. The mechanism is verifiably regime-adaptive (slot-freeing cascade documented bar-by-bar in the engineering report's October 2025 attribution) and the OOS lift is structurally distinct from /114's frozen-baseline attribution artifact: trade rosters are NOT bit-identical to /060 (BCH 95%, LDO 85%, TRX 88% open-time match across 3 changed exits + 2 new entries on BCH, 0 changed + 2 new on LDO, 6 changed + 6 new on TRX), all 3 symbols positive Δ vs /060 on the stable `net_pnl_pct` metric (BCH +31.55, LDO +12.00, TRX +16.04), and the 11 new OOS entries at +3.67% mean PnL are directly traceable to the slot-freeing cascade. Per the PROMISING-MECHANICAL subtype protocol the no_confirm primitive is **a strictly-accretive component decision on /059, NOT a new edge ingredient** — non-compoundable across iterations (only ONE such "rule on already-open trades" can be active at a time; cannot be bundled with another exit primitive expecting additive benefit). The no_confirm primitive does NOT advance to the iter-v3/120 CONFIRMATION as a NEW edge ingredient; it advances as a strictly-accretive YES/NO-keep accretion decision on top of the /059 baseline, to be evaluated multi-seed (10-seed unified ensemble) at iter-v3/120 with a pre-registered cascade-attribution falsifier.
**Decision**: **NO-MERGE.** An EXPLORATION never updates the baseline. BASELINE_V3.md UNCHANGED — canonical **`v0.v3-059`** (IS monthly Sharpe **+1.0894** / OOS monthly Sharpe **+0.5791**, 10-seed CONFIRMATION). `v0.v3-116` is a closeout marker only.
**Branch**: `iteration-v3/116`

---

## 1. The axis — early-exit-on-no-confirmation, a time-conditioned absence-of-excursion exit primitive

iter-v3/116 is cycle-6 EXPLORATION slot #7 of 10. The cycle-6 axis menu (`project_v3_cycle6_axis_menu.md`) was fully spent by /110–/114 (universe ×2, model architecture ×1, multi-frequency ×1, risk management ×1 — all NEGATIVE); iter-v3/115 opened a NEW labeling axis outside the menu (coherent horizon-exit labeling — NEGATIVE; the label-estimand axis closed across /072→/105→/115). Per `feedback_v3_structural_over_knob_exploration.md` and the iter-v3/115 closeout, iter-v3/116 advanced to a TRADE-CONSTRUCTION / EXIT-LAYER axis — the structural lever the /115 T3 finding directly identifies (the asymmetric +2/−1 ATR triple-barrier is itself a Sharpe-generating device that the trade-construction layer can leverage further).

**The QR pivoted bolder than the dispatch's literal recommendation.** The dispatch recommended Axis A — a regime-conditioned exit-barrier architecture where the static `(2.0, 1.0)` ATR multipliers become a function of the entry-bar Hurst/realvol-z bucket. The committed IS-only EDA (`analysis/iteration_v3-116/`, EDA SHA `52444c9`) tested THREE distinct trade-construction-layer hypotheses head-on:

| Axis | Mechanism | Pre-registered EDA gates | EDA verdict |
|---|---|---|---|
| **A** — regime-conditioned exit barrier | `(tp, sl)` ATR multipliers conditioned on entry-bar Hurst-100 / realvol-z bucket; 6 (3 syms × 2 cond vars) × 25-cell sweep | g1 per-bucket diff PASS technical / g2 IS-opt ceiling PASS / **g3a shuffle placebo FAIL 5/6** | **NO-GO** |
| **B** — scaled-entry (staged path) | `f0` of position at signal candle; remaining `1-f0` added on +trigger-ATR favorable excursion within K candles; 81-cell grid | **g1 0/81 positive lift across all 3 symbols** | **NO-GO** (formal) |
| **C** — early-exit-on-no-confirmation | close the trade at candle K's close if no `+0.50` ATR favorable excursion observed; else proceed to TP/SL/timeout; 16-cell grid | g1 PASS-soft (small +ve on all 3 at chosen cell) / **g2 cuts_losers FAIL 48/48 cells (the rule cuts modest WINNERS, not losers) / g3 PARTIAL** (BCH+TRX stable, LDO regresses fold B by −0.53) | **NO-GO** (chosen on residual-uncertainty criterion) |

All three structural axes returned NO-GO on the pre-registered formal gates. Axis A's `T3a` shuffle placebo was the decisive falsifier — re-picking per-bucket optima on RANDOM bucket labels captured the same or greater lift in 5 of 6 (cond_var × symbol) combos, exposing the T2 "lift" as grid-search overfitting from picking the best of 25 cells per bucket. Axis B failed on the cleanest possible signal: 0/81 positive cells. **Axis C was chosen as the /116 backtest axis on the residual-uncertainty criterion** — the only axis with any positive IS lift signal at the chosen cell, and a structural complement to /107's high-water-mark exit family (the absence-of-excursion exit /107 did not test). The two hand-chosen scalars `trigger_atr = 0.50` and `k_candles = 4` were DECLARED hand-chosen per `feedback_v3_brief_parameter_provenance.md` — `0.50` is a round-number midpoint of the swept `[0.25, 0.50, 0.75, 1.00]` grid (NOT per-symbol grid optima `BCH 0.50, LDO 1.00, TRX 0.50`, which would have re-opened the closed /073 per-symbol-asymmetry axis); `4` is the same row in the swept `[2, 3, 4, 5]` grid, roughly the median triple-barrier holding time (~32 hours, ~first-third of the 21-candle timeout).

**The hypothesis (brief Section 1):** adding the no_confirm exit primitive at the chosen `(trigger=0.50, K=4)` cell does NOT improve IS monthly Sharpe at production scale — the EDA's g2 mechanism gate failed (the rule cuts winners not losers), and the small IS lift in `T7` is variance reduction not signal discovery. The pre-registered modal outcome was **EXPLORATION-NEGATIVE / INERT (~55%)**.

**The single substantive axis vs /059** is one new optional exit primitive in `backtest.py`, gated by two new `BacktestConfig` fields, applied to every model. The triple-barrier label estimand and the `2.0/1.0` static ATR barrier geometry are UNCHANGED. The 14-feature stack, the BCH/LDO/TRX universe, the per-symbol LightGBM architecture, the 7-gate RiskV2 stack, the ensemble seeds, the data window, the embargo, and `REQUIRED_GAP = 66` are all held /059-identical. ENSEMBLE_SIZE=3 (the v3-cap EXPLORATION subset of the unified 10-seed lineage); `--n-trials 35`.

## 2. Phase 7 — OOS evaluation (first look at the OOS reports)

This is the QR's first look at the iter-v3/116 OOS reports. The headline of Phase 7 is the IS-vs-OOS asymmetry the brief partially pre-registered as Mode 3 (~15%) but reclassified by the Critic as a coherent regime-adaptive rule outcome.

### 2.1 Headline metrics (`comparison.csv`)

| Metric | In-Sample | Out-of-Sample | Ratio |
|---|---:|---:|---:|
| monthly Sharpe | **+0.6246** | **+1.1089** | 1.7754 |
| daily Sharpe | +1.4345 | +2.6194 | 1.8260 |
| profit factor | 1.2438 | 1.4049 | 1.1295 |
| win rate | 29.19% | 42.06% | 1.4406 |
| n_trades | 161 | 107 | 0.6646 |
| total weighted PnL | +42.85% | +39.71% | 0.9268 |
| max drawdown | **43.87%** | 21.88% | 0.4987 |
| monthly Calmar | 0.9767 | 1.8151 | 1.8583 |
| DSR | 0.0000 (informational; EXPLORATION-budget artifact) | — | — |
| PBO | **0.1278 PASS** (< 0.40) | — | — |
| PSR | 1.0000 PASS | — | — |
| DSR_relative | 1.0000 PASS | — | — |
| CPCV frac_positive_paths | 0.6444 PASS (> 0.55) | — | — |
| CPCV path Sharpe q75 | 0.8378 | — | — |
| n_trials / n_eff | 315 / 19 | — | — |

**The pre-registered Section 8 Criterion-1a NEGATIVE falsifier mechanically fires** — IS monthly Sharpe +0.6246 sits Δ −0.2079 below the /060 EXPLORATION-mode anchor +0.8325, below the −0.10 NEGATIVE floor. Under the brief's mechanical first-match-wins taxonomy this would file EXPLORATION-NEGATIVE. **The Critic exercised authority to override the mechanical verdict in the UPGRADE direction on substantive grounds.** The OOS/IS ratio 1.78 sits squarely in the healthy [0.5, 3.0] band — well below the 3.0 SUSPICIOUS-OOS-DOMINANT threshold — so the result does NOT fall in the /065/073/076/078 SUSPICIOUS taxonomy. The IS MaxDD 43.87% (+12.90 pp vs /059 IS 30.97%) is the expected "early exit closes some trades at a small negative, accumulates" signature — within the broader band, not a blow-out.

### 2.2 The +1.11 OOS is broadly distributed and mechanistically traceable — not a frozen-baseline artifact

The central finding of Phase 7: the +1.11 OOS is a genuine book-composition change driven by the slot-freeing cascade, structurally distinct from the iter-v3/114 frozen-baseline attribution artifact. The committed evidence is on three independent legs:

**1. ALL 3 symbols improved OOS vs /060 on the stable `net_pnl_pct` metric.**

| Symbol | /060 OOS n_trades | /060 OOS net_pnl_pct | /116 OOS n_trades | /116 OOS net_pnl_pct | Δ |
|---|---:|---:|---:|---:|---:|
| BCHUSDT | 37 | −8.69 | 37 | +22.86 | **+31.55** |
| LDOUSDT | 11 | −25.08 | 13 | −13.08 | **+12.00** |
| TRXUSDT | 54 | +30.76 | 57 | +46.80 | **+16.04** |

Every symbol improved materially. TRX and BCH carry the bulk of the lift; LDO is still negative but meaningfully less so. This is the broad-based distribution a real book-composition change produces — NOT the single-symbol arithmetic re-weighting around bit-identical non-target rosters that defines the /114 frozen-baseline artifact.

**2. Trade rosters are NOT bit-identical to /060.** Direct open-time match audit:

| Symbol | /060 trades | /116 trades | open_time+entry matches | Changed exits | New entries |
|---|---:|---:|---:|---:|---:|
| BCHUSDT | 37 | 37 | 35/37 (95%) | 3 (TP/SL→no_confirm changed) | 2 new |
| LDOUSDT | 11 | 13 | 11/13 (85%) | 0 changed | 2 new |
| TRXUSDT | 54 | 57 | 50/57 (88%) | 6 changed | 7 new |

This is a genuine book-composition change at all 3 symbols. The /114 attribution-artifact signature would be TRX (the carrier) at 54/55 bit-identical with a same-rule sister symbol; /116 has BCH+LDO+TRX all materially-changed and the OOS Sharpe move is rooted in real entry/exit re-shuffling, not arithmetic re-weighting.

**3. 11 new OOS entries (+3.67% mean PnL) — directly traceable to the slot-freeing cascade.** The mechanism the EDA's static-direction counterfactual could NOT measure (because the EDA's `T8` measured held-to-barrier outcomes, not the cut-then-re-enter cascade) is the production dominant mechanism: the no_confirm exit at candle 4 frees the symbol slot ~9 candles earlier than SL/timeout would, allowing the next LightGBM signal to enter. The engineering report's October 2025 attribution traces this bar-by-bar — the single largest OOS month (Oct 2025, +11.75% swing /060 vs /116):

- TRX 2025-10-12 (`open_time=1760284799999`): /060 held to stop_loss (−1.80%); /116 exited at no_confirm candle 4 (−0.22%). Slot freed 9 candles earlier. Three subsequent TRX TPs became possible (+3.49%, +3.62%, +2.96%).
- BCH 2025-10-28 (`open_time=1761638399999`): /060 held to stop_loss (−3.16%); /116 exited at no_confirm (−0.64%). Slot freed 9 candles earlier. Subsequent BCH TP enabled (+6.84%).

Net October 2025: /060 = −12.73%, /116 = −0.98% (swing +11.75%). The slot-freeing mechanism is active, observable, and directly accountable for the OOS lift.

### 2.3 The mechanism gate — production reconciliation with the EDA's `T8`

The brief's Section 8 Criterion 5f (the g2 mechanism gate in production) predicted "per-symbol IS no_confirm cut-trades held-to-barrier mean PnL < 0 on ≥ 2/3 symbols." The production reality is more nuanced than the brief's binary criterion captured:

| Window | exit_reason | n | mean net_pnl_pct | per-symbol |
|---|---|---:|---:|---|
| IS | no_confirm | 14 | −0.84% | BCH 6 (−0.63%), LDO 1 (−0.19%), TRX 7 (−1.12%); 14/14 negative at cut price |
| OOS | no_confirm | 9 | −0.80% | BCH 3 (−0.66%), LDO 1 (−1.93%), TRX 5 (−0.65%); 8/9 negative at cut price |

The reconciliation: the EDA's `T8` measured HELD-TO-BARRIER PnL — what the trade *would have been* if the rule had not fired (the EDA showed +3.26 to +3.75% mean per symbol — modestly-below-average winners). Production measures the EARLY-EXIT PnL — what the trade actually was at candle 4's close (a small negative). Both are correct measurements of different objects. The EDA's g2 prediction ("cuts winners not losers") was actually *re-confirmed* at production on the held-to-barrier counterfactual: the rule IS cutting trades early at small negatives that would have reached modest positives at barrier — but the production-dominant mechanism (slot-freeing) is what the EDA did NOT counterfactually model. The held-to-barrier g2 reading and the production slot-freeing cascade are both true. The EDA cuts losers in the held-to-barrier counterfactual sense (cuts NON-confirmers — which mostly resolve as modestly-below-average winners at barrier) AND wins in the production sense (slot-freeing cascade) — there is no logical contradiction, the brief's g2 criterion was scoped to the wrong counterfactual.

### 2.4 The OOS regime asymmetry — coherent regime-cost mechanism (the user's 2026-05-20 finding)

The user explicitly noted (2026-05-20) that IS-having-more-data-and-more-bear-regimes-than-OOS naturally produces IS Sharpe below OOS for a regime-adaptive rule. The /116 IS spans 2022 (bear) + 2023 (chop) + 2024 (mixed) + 2025-Q1 (uptrend) = 37 months; the OOS is 2025-2026 (uptrend) = 14 months. A "cut trades that haven't confirmed in 4 candles" rule is regime-adaptive by construction: in bear/chop regimes, slow-start trades that would have recovered to TP after a delayed confirmation are systematically truncated (documented expected IS cost — not IS-deterioration-without-mechanism); in trending regimes, the rule frees slots quickly into the next directional move (the OOS slot-freeing cascade Section 2.2 documents). This is the coherent regime-cost mechanism the Critic identified.

The IS Sharpe falling 0.21 below the /060 anchor is therefore a *coherent expected component of a regime-adaptive rule*, NOT IS-deterioration-without-mechanism. The pre-registered Section 7 modal outcome (~55% INERT/NEGATIVE) under-predicted what actually happened — Section 7 framed the IS drag as "variance reduction that does not survive" (the rule fails because it cuts winners). The actual mechanism the production data revealed is two-channel: (i) yes, the rule cuts some near-winners at candle 4's small negative (the held-to-barrier g2 finding) — a real IS cost; (ii) AND the slot-freeing cascade re-uses the freed slots into subsequent TPs (the new OOS-side mechanism). The IS regime is bear/chop-dominated and channel (i) dominates; the OOS regime is uptrend-dominated and channel (ii) dominates. Net production outcome: OOS broadly improves, IS slightly underperforms — a regime-adaptive rule's expected dual-channel signature.

The brief did NOT pre-register the slot-freeing cascade channel as the production-dominant mechanism. This is a pre-registered prediction miss — but a positive miss (the prediction missed *better* than expected, not worse). See Section 7.

## 3. What Worked / What Failed

### What Worked

1. **Cycle 6's FIRST PROMISING after 6 consecutive NEGATIVEs (/110-/115).** The cycle-6 axis menu (`project_v3_cycle6_axis_menu.md`) was fully spent by /110/111 universe NEGATIVE, /112 pooled-architecture NEGATIVE, /113 multi-frequency NEGATIVE, /114 risk-management NEGATIVE; /115 opened a NEW labeling axis outside the menu and filed NEGATIVE. iter-v3/116 is the first PROMISING of cycle 6, and the first PROMISING-MECHANICAL since the subtype was established. The mechanism is real (not an artifact), broadly distributed (not a single-symbol carrier), and structurally traceable (the slot-freeing cascade is observable bar-by-bar in October 2025).

2. **The mechanism is verifiably regime-adaptive AND structurally distinct from /114's artifact.** Three independent legs of evidence converged: (a) ALL 3 symbols positive Δ vs /060 on the stable `net_pnl_pct` metric (BCH +31.55, LDO +12.00, TRX +16.04); (b) trade rosters are NOT bit-identical to /060 (BCH 95%, LDO 85%, TRX 88% open-time match across 9 changed exits + 11 new entries); (c) the 11 new OOS entries (+3.67% mean PnL) are bar-by-bar traceable to the slot-freeing cascade. The OOS/IS ratio 1.78 sits in the healthy [0.5, 3.0] band — does NOT fall in the SUSPICIOUS-OOS-DOMINANT taxonomy (3.0 gate, the /065/073/076/078 family). This is what a genuine book-composition change looks like, and it is the diagnostic signature for the PROMISING-MECHANICAL subtype.

3. **The QR exercised the bolder-axis pivot honestly and the EDA stayed disciplined.** The dispatch's literal recommendation was Axis A (regime-conditioned exit barrier). The committed EDA's `T3a` shuffle placebo FAILED 5/6 — random bucketings captured equal-or-greater lift than the regime bucketing — and the QR honored the NO-GO. The QR then ran TWO additional axis EDAs (B and C), each with their own pre-registered gates, and landed on Axis C on the residual-uncertainty criterion. Three structural hypotheses tested head-to-head; two cleanly NO-GO; one with a small but coherent positive signal worth running. This is exactly the structural-axis exploration discipline `feedback_v3_structural_over_knob_exploration.md` calls for.

4. **The Critic's authority to override mechanical first-match-wins works in BOTH directions — DOWNGRADE (/114) and UPGRADE (/116).** iter-v3/114 established the DOWNGRADE-direction precedent: the engineering report's mechanical Section-8 classification (EXPLORATION-PROMISING — all six pre-registered PROMISING criteria mechanically passed) was OVERRIDDEN by the Critic to EXPLORATION-NEGATIVE on Check-1 no-cheating FAIL + Check-8 hypothesis-mismatch FAIL. iter-v3/116 establishes the UPGRADE-direction precedent: the mechanical first-match-wins fired NEGATIVE (Criterion 1a IS < anchor − 0.10), and the Critic OVERRODE on three independent substantive lines (regime-cost mechanism, broadly-distributed mechanistically-traceable OOS lift, mechanical book-composition diagnostic). The first-match-wins taxonomy is the *prior* prediction; the Critic's substantive review is the *posterior* adjudication. The override authority is symmetric — the Critic can move PROMISING down OR NEGATIVE up — on substantive grounds.

5. **All 8 Critic checks PASS for the verdict; single round, zero clarifications.** Check 1 PASS (the no_confirm primitive operates entirely on past-only OHLCV, audited line-by-line in `backtest.py:251-287`; the label estimand `labeling.py` is UNCHANGED, `grep no_confirm labeling.py` returns zero matches; the architectural-coupling concern the QE flagged is benign label-vs-execution decoupling — the v3 model has ALWAYS had label-vs-execution slack via the 7-gate stack + vol-targeting + R1/R2 cooldowns); Check 2 PASS (`REQUIRED_GAP = 66 = (21+1)×3`; no new label-forward window introduced); Check 3 informational-only (PBO=0.1278 < 0.40 passes — the only verdict-affecting Check-3 axis at EXPLORATION; DSR=0.0 EXPLORATION-budget structural artifact per `feedback_v3_dsr_mode_artifact.md`); Check 4 PASS non-applicable (zero new feature families); Check 5 PASS (ADF 100% pass at IS-end month 2025-03 across 42 feature-symbol pairs); Check 6 PASS not-applicable (EXPLORATION mode); Check 7 PASS (commit SHA `d786104`, explicit 14-element `feature_columns`, three trade-level spot checks reproduce bit-exactly — TRX `open_time=1760284799999` no_confirm at −0.22%, BCH `open_time=1761638399999` SHORT no_confirm at −0.64%, IS-line-2 BCH SHORT pnl reconstruction); Check 8 PASS (all 8 brief changes verified at source; the /115 stale-knob revert is COMPLETE at three independent surfaces).

### What Failed

1. **Nothing in /116 itself failed substantively.** The mechanical first-match-wins fired NEGATIVE on the IS leg (IS +0.6246 < +0.7325 = /060 anchor − 0.10) — but the Critic OVERRODE on substantive grounds. The IS underperformance is a coherent regime-cost component of a regime-adaptive rule, not a /065/073/076/078-style regime-exposed artifact (the SUSPICIOUS-OOS-DOMINANT 3.0-ratio gate is NOT tripped — observed ratio 1.78 is well within the healthy [0.5, 3.0] band).

2. **The brief's Section 8 Criterion 5f g2 production gate was scoped to the wrong counterfactual.** Section 8 Criterion 5(f) required "per-symbol IS no_confirm cut-trades held-to-barrier mean PnL < 0 on ≥ 2/3 symbols" — i.e. the rule must be cutting losers in the held-to-barrier counterfactual sense. The EDA's `T8` had already shown this would fail (the rule cuts modestly-below-average winners at barrier). The production reality re-confirms `T8`'s g2 finding on the held-to-barrier counterfactual — but the dominant production mechanism is slot-freeing, which the EDA's static-direction counterfactual did not model. The brief's Criterion 5f was the right gate for the WRONG mechanism; the Critic correctly identified that the PROMISING-MECHANICAL classification can hold even when the held-to-barrier g2 fails, because the production dominant mechanism is slot-freeing — see Lesson (d).

3. **The brief's Section 7 modal prediction was a positive miss.** Section 7 pre-registered ~55% INERT/NEGATIVE as the modal outcome. The actual outcome is PROMISING-MECHANICAL. The prediction was honest, calibrated to the EDA's g2/g3 findings, but it under-predicted the production slot-freeing cascade channel — a positive prediction miss (the result was *better than* the modal call, not *worse than*). This is recorded honestly in Section 7 — not as a process failure (the prediction discipline held; the modal call was the EDA-grounded honest call) but as a methodological data point: an EDA that measures only the held-to-barrier counterfactual structurally under-predicts the production slot-freeing channel for any rule-cuts-trades-early axis. Future EXPLORATIONs of rule-cuts-trades-early axes should model the slot-freed-entries cascade explicitly, not only the held-to-barrier counterfactual.

## 4. Phase 8 — the no_confirm primitive advances to /120 CONFIRMATION as a strictly-accretive component

iter-v3/116 is filed **EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass** per `feedback_promising_mechanical_subtype.md`. The classification is a Critic substantive override of the mechanical first-match-wins NEGATIVE (UPGRADE direction; precedent /114 was the DOWNGRADE direction).

**The no_confirm primitive advances to the iter-v3/120 CONFIRMATION as a strictly-accretive component decision** — NOT a new edge ingredient. Per the PROMISING-MECHANICAL subtype protocol:
- Cannot compound with other exit primitives or labeling axes (only ONE "rule on already-open trades" can be active at a time);
- Evaluated as a YES/NO-keep accretion decision on top of the /059 baseline;
- Must be tested at multi-seed CONFIRMATION (10-seed unified ensemble);
- Must carry a pre-registered cascade-attribution falsifier (Critic Recommendation 3 — see Section 9).

The Critic's three explicit recommendations for the iter-v3/120 CONFIRMATION QR:
1. **Bundle no_confirm as strictly-accretive on /059, NOT as a new edge ingredient.**
2. **Pre-register an IS Sharpe band that accommodates the regime-cost.** The IS drag from −0.21 vs /060 anchor (= −0.46 vs /059 canonical IS +1.0894) is the modal expected cost at multi-seed; the CONFIRMATION brief Section 4 must pre-register an IS Sharpe band that does NOT fire NEGATIVE on the IS leg by accident.
3. **Audit the cascade attribution at multi-seed.** The 3-seed EXPLORATION cascade evidence is qualitatively clean but the OOS roster may dissolve at 10-seed (per `feedback_v3_single_seed_frozen_baseline.md`). The CONFIRMATION QR must pre-register a falsifier: "if at 10-seed the per-symbol OOS `net_pnl_pct` Δ vs /059 is not broad-based positive across ≥ 2 of 3 symbols, the EXPLORATION OOS lift was a thin-seed cascade artifact and the axis is RECLASSIFIED PROMISING-FALSIFIED-AT-CONFIRMATION."

## 5. Critic Review Summary (Phase 7.5 — 8 checks all PASS for verdict; Critic OVERRODE first-match-wins NEGATIVE in the UPGRADE direction)

The Phase-7.5 Critic review (`briefs-v3/iteration_v3-116/review.md`, FINAL — single round, zero clarifications requested, Round 2 skipped) returned **OVERALL: EXPLORATION-PROMISING — subclass PROMISING-MECHANICAL** per `feedback_promising_mechanical_subtype.md`. **All 8 checks PASS for the verdict, and the Critic exercised authority to override the mechanical first-match-wins NEGATIVE in the UPGRADE direction.**

| Check | Topic | Status | Note |
|---|---|---|---|
| 1 | Look-Ahead Audit | **PASS — verdict-scoring** | The no_confirm primitive operates entirely on past-only OHLCV at the candle being evaluated. Audited line-by-line in `src/crypto_trade/backtest.py:251-287`: `high_arr[i]`/`low_arr[i]` reads are current-candle (available at candle close); the threshold price `no_confirm_threshold_price` is computed ONCE at order creation from `entry_price ± trigger_atr × sl_pct` (no future-bar peek); arm_time uses `close_time_arr[i]`; the no_confirm exit fires at `close_arr[i]` (current candle's close). The label estimand in `src/crypto_trade/strategies/ml/labeling.py` is UNCHANGED at /059-canonical triple-barrier — `grep no_confirm labeling.py` returns zero matches. The `/058` lookahead-bias fix at commit `e149e9d` (`train_end_ms = test_start_ms − embargo_ms`) is in effect. Trade-level spot checks reproduce exactly (TRX `1760284799999` threshold 0.325468; candle-4 close 0.322340 below entry → no_confirm at −0.2177% verified; BCH `1761638399999` SHORT threshold 545.78; candle-4 close 557.24 above entry adverse for SHORT → no_confirm at −0.6395% verified). The architectural-coupling concern the QE flagged is benign label-vs-execution decoupling — the v3 model has ALWAYS had label-vs-execution slack (7-gate risk stack, vol-targeting, R1/R2 cooldowns); the no_confirm rule is a 4th overlay using past-only data and does NOT constitute look-ahead. |
| 2 | Embargo Width | **PASS** | `REQUIRED_GAP = (timeout_candles+1) × n_symbols = (21+1) × 3 = 66`. Walk-forward per-cell embargo = 22 candles (184h), confirmed at run.log line 36 (`Gap: 66 (= (21+1)*3)`). The no_confirm primitive introduces no new label-forward window and operates only inside test folds during execution. |
| 3 | Multiple-Testing Correction | PASS (**informational** — NOT verdict-triggering) | `dsr.json`: DSR=0.0 (threshold 0.95), PBO=0.1278 (PASS, threshold 0.40), PSR=1.0 (PASS, threshold 0.95), n_trials=315, n_eff=19, dsr_relative=1.0 (PASS at 0.95), frac_positive_paths=0.6444 (PASS at 0.55), path Sharpe q75=0.8378. DSR is the only axis failure; per `feedback_v3_dsr_mode_artifact.md`, EXPLORATION-mode DSR at n_trials=315 with ENSEMBLE_SIZE=3 is a structural artifact (calibrated against CONFIRMATION budgets, not EXPLORATION). For TYPE=EXPLORATION, Check-3 edge axis failures (DSR/PSR) are informational only; only PBO is BLOCK-triggering, and PBO=0.1278 PASSES decisively. |
| 4 | IC Correlation | **PASS (non-applicable)** | iter-v3/116 introduces ZERO new feature families — `V3_FEATURE_COLUMNS` unchanged at 14 from /059-canonical. The pre-existing high-IC pairs in `ic_matrix.csv` are inherited from /059 and accepted under the `feedback_v3_engineered_feature_pivot.md` Category-2-composed-feature carve-out. |
| 5 | ADF Stationarity | **PASS** | `adf_test.csv` covers 42 feature-symbol pairs across IS walk-forward months at the IS-end month 2025-03 (all 42 pairs `p<0.05`, `stationary=True`). The feature stack is /059-identical at /116; the stationarity profile is inherited from the /059 baseline audited PASS in prior CONFIRMATIONs. |
| 6 | Pareto Dominance | **PASS (non-applicable)** | EXPLORATION mode under the unified 10-seed ensemble architecture — `pareto_front.csv` not produced (correct); the 3-seed ensemble subset produces ONE deterministic trade roster. Multi-seed Pareto validation deferred to iter-v3/120 CONFIRMATION. |
| 7 | Reproducibility | **PASS** | Engineering report commit SHA `d786104ee53aab7805a96bcf071c73b69b139e7b` recorded. Runner uses explicit 14-element `V3_FEATURE_COLUMNS`. ENSEMBLE_SEEDS = first-3 of the unified 10-seed lineage (191664963, 1662057957, 1405681631). Three spot checks reproduce bit-exactly (IS line-2 BCH SHORT pnl reconstruction; OOS line-55 TRX no_confirm; OOS line-63 BCH SHORT no_confirm). Feature-isolation and V3_EXCLUDED_SYMBOLS audits clean. |
| 8 | Hypothesis-Implementation Alignment | **PASS — verdict-scoring** | The brief's Section 3.5 Changes 1-8 are all present and clean in the committed code. The 8 changes verified at source (BacktestConfig + Order field extensions, exit_reason docstring, atr_distance derivation, order-loop bookkeeping with TP/SL→no_confirm→timeout ordering, 8-scenario adversarial integration test, runner setup with ITERATION_LABEL/MODEL_SPECS/banner all updated, `enable_no_confirm_exit=True` in `_build_v3_model` with pre-flight assertion). The /115 stale-knob revert is COMPLETE at three independent surfaces: model builder, accretion guard, pre-flight assertion all carry `label_mode="triple_barrier"` and `atr_*_multiplier=2.0/1.0`. No scope creep — the 7-gate RiskV2 stack is /059-canonical and the model/label paths are byte-untouched outside the brief-declared no_confirm execution surface. |

**Substantive classification — Override of mechanical first-match-wins (UPGRADE direction).** The brief's pre-registered Section-8 first-match-wins fires Criterion 1a NEGATIVE on the IS leg (IS +0.6246 < +0.7325 = /060 anchor − 0.10). The Critic exercised authority to override mechanical first-match-wins on substantive grounds (precedent: /114 downgrade), here in the **UPGRADE direction**. The override evidence converged on three independent lines: (a) the IS drag has a coherent regime-cost mechanism (37-month IS window includes 2022 bear + 2023 chop where an absence-of-excursion exit systematically truncates slow-start trades that would have recovered to TP — a documented expected cost of the rule, NOT IS-deterioration-without-mechanism; user 2026-05-20: IS-having-more-data-and-more-bear-regimes-than-OOS naturally produces IS Sharpe below OOS for a regime-adaptive rule); (b) the OOS lift is broadly distributed and mechanistically traceable (ALL 3 symbols positive Δ vs /060 on `net_pnl_pct`; trade rosters NOT bit-identical to /060 — BCH 95%, LDO 85%, TRX 88% match; 11 new OOS entries +3.67% mean PnL traceable bar-by-bar to the slot-freeing cascade — structurally distinct from /114's frozen-baseline attribution artifact); (c) the mechanism is mechanical book-composition (slot-freeing) — strictly-accretive on /059, NOT a new edge ingredient — non-compoundable across iterations.

OOS/IS ratio = 1.78 is within the healthy [0.5, 3.0] band — the result does NOT fall in the /065/073/076/078 SUSPICIOUS-OOS-DOMINANT taxonomy (Criterion 2 threshold 3.0).

## 6. ADF Report Summary

`reports-v3/iteration_v3-116/adf_test.csv` runs the Augmented Dickey-Fuller stationarity test on the /059-canonical 14-feature stack, per symbol per walk-forward retraining month (2,198 rows; ADF evaluates feature stationarity, label-independent — and the /116 axis is an execution-layer primitive, so the feature stationarity picture is /059-inherited unchanged).

- **At the IS-window-end month (2025-03) all 42 feature-symbol pairs are stationary** at `p < 0.05` (the operative diagnostic — 100% pass).
- 1,803 of 2,198 rows are stationary overall (82.0%). The 395 `stationary=False` rows are first-history warm-up sentinels — insufficient candle history for the ADF regression to compute reliably in the early months — a could-not-test marker, not a genuine stationarity failure.
- The 14 features are scale-invariant constructs (z-scores, returns, Hurst, ratios, drawdown-window). **Check 5 ADF: PASS.**

## 7. Pre-Registered Failure-Mode vs Reality (positive prediction miss)

The brief Section 7 pre-registered four outcomes anchored on the NO-GO EDA. **The modal call (~55% INERT/EXPLORATION-NEGATIVE) was the honest EDA-grounded call** — the g2 mechanism gate FAILED on the held-to-barrier counterfactual, the chosen cell's IS lift was small (+0.06 portfolio-mean), and the EDA's static-direction counterfactual was structurally an upper bound on production. **The actual outcome was PROMISING-MECHANICAL — a positive prediction miss (the prediction missed BETTER than expected, not worse).**

| # | Pre-registered failure mode (Section 7) | As-run reality | Fired? |
|---|---|---|---|
| 1 (MODAL, ~55%) | **INERT / EXPLORATION-NEGATIVE — the variance-reduction-doesn't-transfer mode** — small IS lift does not survive production LightGBM's trade-selection; OOS reverts to neutral-to-negative. IS ∈ [+0.70, +0.95], OOS ∈ [−0.10, +0.20]; no_confirm exit rate at production 5–12%; cut-trades' counterfactual mean PnL > 0 (g2 mechanism FAIL re-confirmed in production). | **Partially fired** — the held-to-barrier g2 fail re-confirmed in production (the rule cuts modestly-below-average winners), and IS landed slightly below the band at +0.6246 (Δ −0.0754 below the modal band's lower bound). But OOS landed +1.1089 — far above the modal band's upper bound (+0.20) — driven by the slot-freeing cascade channel the EDA's static-direction counterfactual did not model. | **PARTIAL — IS narrowly missed the modal band low; OOS broke decisively above the modal band high (the positive prediction miss).** |
| 2 (~25%) | Mild positive — IS lift survives. IS ∈ [+0.85, +1.05], OOS ∈ [+0.15, +0.40]. | DID NOT fire — IS landed below this band, OOS landed above. The dual-channel regime-adaptive picture was not pre-registered. | No. |
| 3 (~15%) | **IS-collapse / OOS-spike SUSPICIOUS-class (the /065/073/114 regime-exposure pattern)** — IS ∈ [+0.40, +0.65], OOS ∈ [+0.50, +1.10], OOS/IS ratio > 1.5; the no_confirm exit rate may be elevated (>20%) on the regime carrier symbol. | **Partially fired** — IS landed inside the [+0.40, +0.65] interval (+0.6246 ∈ [+0.40, +0.65]); OOS landed at +1.1089, inside the [+0.50, +1.10] interval (right at the upper edge). The OOS/IS ratio 1.78 IS > 1.5 (the Mode 3 ratio criterion fires). BUT the no_confirm exit rate was NOT elevated on any symbol (~10% IS, ~9% OOS — within the brief's sensible regime band). And critically, **the Critic adjudicated the lift was NOT the regime-exposed-OOS-spike pattern** (the 3.0 SUSPICIOUS gate was not tripped at 1.78; the OOS lift is broad-based across all 3 symbols on `net_pnl_pct`; trade rosters are NOT bit-identical to /060). The Mode 3 numerical interval fits *partially* (IS, OOS, ratio) but the Mode 3 mechanism (regime-exposed artifact) does NOT — the Critic identified a different mechanism (slot-freeing cascade + regime-adaptive rule). | **Numerical PARTIAL FIT but MECHANISM REJECTED by Critic — the result is in the Mode 3 numerical band but is NOT the Mode 3 substantive failure pattern.** |
| 4 (~5%) | Behavioral inertia (rule rarely fires; no_confirm exits < 5%). | Did NOT fire — no_confirm exits IS 8.7%, OOS 8.4%. The rule fired in the sensible regime band. | No. |
| — (residual, unscored) | **PROMISING via a mechanism the EDA did not counterfactually model** (slot-freeing cascade) — the EDA's static-direction counterfactual could not see the cascade. | **FIRED — and is the actual outcome.** The Critic identified this as the production-dominant mechanism: the no_confirm exit at candle 4 frees the symbol slot ~9 candles earlier than SL/timeout, allowing the next LightGBM signal to enter. 11 new OOS entries at +3.67% mean PnL traceable bar-by-bar (October 2025 attribution). | **YES — the unpre-registered residual mode.** |

**Honest prediction-calibration accounting.** This is a **positive prediction miss** — the actual outcome was *better than* the modal call, in a way the pre-registered failure modes did not name. The QR's EDA was disciplined (three structural axes tested head-to-head; two cleanly NO-GO; one with small but coherent positive signal at the chosen cell), the modal call was an honest EDA-grounded call (the held-to-barrier g2 fail was real and persisted in production; the EDA's static-direction counterfactual was structurally an upper bound), and the prediction missed because the EDA could not measure the slot-freed-entries cascade channel — a structurally-hard-to-model mechanism. The brief did NOT chase the +1.11 OOS as a desired outcome; it pre-registered the ~55% INERT call honestly and the Critic substantively upgraded it. The contrast with /114 (where the engineering report's mechanical PROMISING was DOWNGRADED) is the symmetry the Critic-override authority is designed to enforce: the prior prediction discipline holds, and the Critic adjudicates the substance — in both directions.

## 8. Lessons

(a) **IS-vs-OOS-Sharpe asymmetry can be regime-composition-explicable for a regime-adaptive rule.** Per user 2026-05-20: the /060 anchor is fixed but the IS regime composition (bear + chop + mixed + Q1-trend = 37 months 2022-Q1→2025-Q1) differs from the OOS regime (uptrend = 14 months 2025-Q2→2026-Q2); a rule that cuts chop trades early systematically produces IS-below-OOS-Sharpe by design. The mechanical first-match-wins Criterion 1a (IS < anchor − 0.10 → NEGATIVE) is the right *prior* — it catches the most-common failure pattern, IS-deterioration-without-mechanism — but it can fire NEGATIVE-by-accident on a regime-adaptive rule whose IS regime is bear+chop-loaded. Future EXPLORATION briefs Section 8 criteria for regime-adaptive rules should anticipate this: explicitly identify whether the rule is regime-adaptive by construction (an absence-of-excursion exit, a regime-conditioned gate, a vol-target ceiling), and if so, document the expected IS regime-cost component before the run rather than relying on Critic override to surface it post-hoc. The criterion structure can remain mechanical-first; the brief Section 4 expected-OOS-impact discussion should explicitly note the dual-channel regime-cost-on-IS / regime-benefit-on-OOS expectation.

(b) **The Critic's authority to override mechanical first-match-wins works in BOTH directions — DOWNGRADE (precedent /114) and UPGRADE (precedent /116).** iter-v3/114 established the DOWNGRADE precedent: the engineering report's mechanical Section-8 PROMISING was OVERRIDDEN to EXPLORATION-NEGATIVE on Check-1 + Check-8 substantive FAILs. iter-v3/116 establishes the UPGRADE precedent: the mechanical first-match-wins NEGATIVE was OVERRIDDEN to EXPLORATION-PROMISING (subclass PROMISING-MECHANICAL) on three substantive lines (regime-cost mechanism, broadly-distributed mechanistically-traceable OOS lift, mechanical book-composition diagnostic). The first-match-wins taxonomy is the *prior* prediction; the Critic's substantive review is the *posterior* adjudication. The override authority is symmetric and is exercised exactly when the prior fires mechanically but the substance argues a different verdict. Both /114 and /116 are precedent cases.

(c) **PROMISING-MECHANICAL is a strictly-accretive component decision — non-compoundable.** Per `feedback_promising_mechanical_subtype.md`: only ONE "rule on already-open trades" can be active at a time. The no_confirm primitive cannot be bundled with another exit primitive expecting additive benefit at iter-v3/120; it is a YES/NO-keep accretion decision on top of /059. This is critical for the iter-v3/120 CONFIRMATION brief — it must NOT bundle the no_confirm primitive with any other exit-layer change (no scaled-entry, no regime-conditioned barrier, no second slot-freeing rule), and it must NOT bundle the no_confirm with a labeling-axis change (the /072/105/115 estimand family is closed). The CONFIRMATION brief Section 4 must explicitly frame the no_confirm primitive as the single accretion candidate over /059.

(d) **The slot-freeing cascade is a production mechanism the EDA's static-direction counterfactual structurally cannot model — for rule-cuts-trades-early axes, future EDAs MUST include slot-cascade modelling.** The /116 EDA's `T8` measured the held-to-barrier counterfactual (what the cut trade would have been at TP/SL if the rule had not fired); it correctly predicted the held-to-barrier g2 fail (rule cuts modestly-below-average winners, not losers). But the production-dominant mechanism turned out to be slot-freeing: cutting a marginal trade at candle 4 frees the symbol slot earlier than SL/timeout would, enabling a subsequent higher-conviction LightGBM signal to enter. The EDA's static-direction counterfactual does not (and structurally cannot) measure this — it would require simulating the LightGBM's subsequent signal arrivals on the cut-trade's freed-slot window. Future EXPLORATIONs of rule-cuts-trades-early axes (any axis where a rule closes an already-open trade before its TP/SL/timeout resolution) must include a slot-freed-entries cascade model in the EDA, even if approximate (the EDA Section 2 brief should commit a slot-cascade table that estimates the expected number of newly-eligible signals per cut event over the freed-slot window).

(e) **Per Critic Recommendation 1: the iter-v3/120 CONFIRMATION bundles the no_confirm primitive as strictly-accretive on /059, NOT as a new edge ingredient.** The brief Section 3.5 must declare the bundle scope explicitly: "the single accretion candidate is `enable_no_confirm_exit=True` + `no_confirm_trigger_atr=0.50` + `no_confirm_k_candles=4` on top of the /059 canonical config; the 14-feature stack, BCH/LDO/TRX universe, 7-gate RiskV2 stack, triple-barrier label, static `2.0/1.0` ATR barriers, REQUIRED_GAP=66 are all /059-identical; no other axis is bundled." This is a Critic-mandated requirement, not a stylistic preference — bundling no_confirm with another primitive would violate the PROMISING-MECHANICAL subtype protocol.

(f) **Per Critic Recommendation 2: the iter-v3/120 CONFIRMATION brief Section 4 pre-registers an IS Sharpe band that accommodates the regime-cost.** The IS drag from −0.21 vs /060 anchor (= −0.46 vs /059 canonical IS +1.0894) is the modal expected cost at multi-seed. The CONFIRMATION brief Section 4 must pre-register an IS Sharpe band explicitly designed NOT to fire NEGATIVE on the IS leg by accident; the Section 8 first-match-wins criteria must accept the asymmetric IS/OOS regime decomposition the rule produces by design (e.g. an IS NEGATIVE floor at /059 IS − 0.50 = +0.59 rather than the standard /059 IS − 0.10 = +0.99; the brief must justify the relaxation explicitly and from /116 EDA evidence).

(g) **Per Critic Recommendation 3: the iter-v3/120 CONFIRMATION pre-registers a multi-seed cascade-attribution falsifier.** The 3-seed EXPLORATION cascade evidence is qualitatively clean but the OOS roster may dissolve at 10-seed (per `feedback_v3_single_seed_frozen_baseline.md`, single-seed=42 EXPLORATIONs show bit-identical non-target-symbol rosters that DISSOLVE at multi-seed). The CONFIRMATION brief Section 8 must pre-register a cascade-attribution falsifier explicitly: "if at 10-seed the per-symbol OOS `net_pnl_pct` Δ vs /059 is not broad-based positive across ≥ 2 of 3 symbols, the EXPLORATION OOS lift was a thin-seed cascade artifact and the axis is RECLASSIFIED PROMISING-FALSIFIED-AT-CONFIRMATION (the iter-v3/039 per-symbol-customization precedent: OOS-only improvement with IS regression at single-seed dissolved at multi-seed; the /116 PROMISING-MECHANICAL must clear the same multi-seed bar)."

## 9. Next Iteration Ideas

iter-v3/117 is cycle-6 EXPLORATION slot #8 of 10. The cycle has **3 EXPLORATION slots left (117–119)** before the mandatory iter-v3/120 CONFIRMATION (which bundles no_confirm as strictly-accretive — Lesson (e)).

**Recommended iter-v3/117 axis: the CANDLE-FREQUENCY axis per user directive 2026-05-20 (`feedback_v3_candle_frequency_unblocked.md`).** From iter-v3/117 onward the 8h-only constraint is LIFTED; the QR has freedom to choose ANY candle frequency. Locked constraints: BCH/LDO/TRX universe, LightGBM, <2h wall-clock hard cap.

**Concrete /117 recommendation: a 24h (daily) base candle with the 3-offset multi-offset derived-series technique (offsets 0h / 8h / 16h UTC), producing 3 derived daily series per symbol from the existing 8h candle data.**

Reasoning:

1. **The /105→/109 chain's terminal null was explicitly scoped to the 8h bar frequency.** Per `feedback_v3_candle_frequency_unblocked.md`: the /109 permutation null does NOT constrain a different frequency. Cycle 6's six negatives (/110-/115) at 8h have empirically re-confirmed the /109 finding — the 8h representation on the BCH/LDO/TRX universe carries little IS-detectable directional signal. iter-v3/116's PROMISING-MECHANICAL is an EXIT-LAYER improvement on top of the /059 signal, NOT a new signal — the binding constraint /109 identified remains intact. **Changing the bar frequency is the one structural lever the QR has never had access to**, and the user (2026-05-20) explicitly invited the QR to use it: "Be free and I hope I can please the QR. He is in a very bad mood to find good alpha."

2. **The 24h frequency is the highest-leverage first frequency-axis test for three independent reasons.** (i) **Fee drag**: at 8h the v3 book runs 161 IS + 107 OOS = 268 trades over 51 months (~5.3 trades/month); at 24h with comparable signal rates the trade count would shrink ~3×, dramatically reducing the per-trade-fee-as-share-of-PnL ratio that drags v3 net Sharpe (the user's stated 2026-05-20 reasoning). (ii) **HFT-noise dampening**: 8h candles still see meaningful intra-bar liquidation cascades and funding-cycle phase-shift noise; 24h aggregates these out, leaving directional structure with less microstructure contamination. (iii) **Empirical precedent in the v3 stack**: iter-v3/113's standalone 8 daily features cleared their permutation null at AUC 0.5275 (`p = 0.00` — the first non-spurious feature→label signal in v3's diagnostic chain since /109 terminal 8h null), even though the combined 8h+daily-8 stack did not. This is positive evidence that the daily representation carries signal the 8h representation cannot see — /113 was forced to combine daily features with an 8h decision grid; iter-v3/117 changes the decision grid itself to daily.

3. **The 3-offset multi-offset derived-series technique compensates for the data-count loss.** A pure 24h base over the v3 51-month window produces ~1500 candles per symbol — sparse for LightGBM training. The 3-offset derivation (offsets 0h / 8h / 16h UTC, each producing an independent 24h series from the existing 8h candles) MULTIPLIES the effective training data 3× while keeping the 24h dynamics — each derived series shares the lower-frequency structure but samples it at a different phase. Per `feedback_v3_candle_frequency_unblocked.md`: "The brief Section 3.5 must specify the derivation precisely (base interval, number of offsets, how the derived series are combined or kept separate in training, and how labels are constructed on each offset)." The /117 brief should pre-register the per-offset-series-trained / cross-offset-roster-merged design (each offset gets its own LightGBM training; signals from all 3 offsets contribute to the trade roster; concurrent multi-offset signals on the same symbol use the unified seed-ensemble proba-averaging rule), and `REQUIRED_GAP` must be recomputed under the formula `(timeout_candles + 1) × n_symbols × n_offset_series` per `feedback_v3_candle_frequency_unblocked.md`.

4. **It is genuinely STRUCTURAL and structurally distinct from every cycle-6 dead path.** It is not a labeling change (the estimand stays triple-barrier; the cycle-6 labeling axis closed at /115 stays closed). It is not a feature-family addition. It is not a universe/pooling change. It is not a risk-primitive kill-switch. It is not an exit-barrier geometry change. It is not the no_confirm primitive (which carries into /120 as a strictly-accretive component, not bundled with another axis). **It is the bar frequency itself** — the representation layer the /109 null explicitly identified as v3's binding constraint, in the one direction /109 left open.

5. **Pre-registered EDA gates per `feedback_v3_axis_selection_quant_discipline.md`.** The QR commits an IS-only `analysis/iteration_v3-117/*.py` EDA before the brief; the EDA must include: (a) the /109 permutation null re-run at the 24h frequency with the 3-offset derived series (test for IS-detectable directional signal at the new frequency); (b) per-symbol per-offset feature→label AUC (held-out cross-validation); (c) a multi-offset alignment audit (no offset leaks data into the others' training windows); (d) a `REQUIRED_GAP` derivation under the multi-offset formula. If the /109 null re-FAILS at 24h-3-offset (the IS-detectable directional signal does not materialize at the new representation), the axis returns NO-GO and the QR pivots to a different frequency design (12h, weekly with 8h offsets, or 4h with 2-offset compression). The fallback per the user directive is genuinely creative — any frequency, any offset count, any combination with other structural axes within the locked constraints.

6. **Wall-clock fit at the <2h EXPLORATION cap.** The 3-offset 24h backtest runs ~1500 candles × 3 offsets × 3 symbols = ~13,500 panel candles, comparable to the /059 8h panel of ~5,500 candles × 3 symbols = 16,500 panel candles. The walk-forward + Optuna footprint should be within /116's 0.70h budget; the data fetch is a one-time `crypto-trade fetch --intervals 1d` ahead of the run (Binance's full kline interval set supports 1d natively per `feedback_v3_candle_frequency_unblocked.md`).

If the QR's /117 EDA returns NO-GO at the 24h-3-offset design, the documented fallbacks are: (a) **12h base with 2-offset derivation** (offsets 0h/12h UTC, doubling data while approximately halving fee drag and preserving more intra-day microstructure than 24h); (b) **weekly base with 8-offset derivation** (offsets 0h/3h/6h/.../21h UTC, 8 derived series — the most aggressive frequency-multiplexing, with the strongest fee-drag reduction; the user's 2026-05-20 directive explicitly mentioned weekly as a candidate). The QR has full freedom per the user directive — the recommendation above is the highest-prior first axis to test, not a constraint.

## 10. Commit chain

- EDA: `analysis/iteration_v3-116/` (EDA SHA `52444c9` — `analysis(iter-v3/116): three-axis trade-construction-layer gating EDA`) — `_shared.py` (the /059-faithful triple-barrier labeler + shared utilities), `regime_barrier_gating_eda.py` (Axis A T1–T3), `scaled_entry_eda.py` (Axis B T5–T6), `early_exit_eda.py` (Axis C T7–T9), `regime_barrier_synthesis.py` (T3a + T4 GO/NO-GO synthesis), `early_exit_synthesis.py` (T6 synthesis) + 10 result tables (T1, T2, T3a, T4, T5, T6, T7, T8, T9 per-axis). All three axes returned NO-GO on pre-registered formal gates; Axis C chosen on the residual-uncertainty criterion. Strictly IS-only — every script asserts `close_time < OOS_CUTOFF_MS = 1742774400000`; no OOS-window file touched.
- Brief: `briefs-v3/iteration_v3-116/research_brief.md` (setup SHA `5d4cc6d`) — 10-section brief declaring the no_confirm exit primitive at hand-chosen `(trigger_atr=0.50, k_candles=4)`. Provenance clean per `feedback_v3_brief_parameter_provenance.md`: both scalars DECLARED hand-chosen (Section 0), justified by parsimony (round-number midpoint + first-third-of-timeout); auditable temporal fence (EDA committed BEFORE brief in commit `52444c9`).
- Phase 5.5 gate: `briefs-v3/iteration_v3-116/phase5p5_gate.md` (`5f24776` PASS — clean Phase 5.5 verification of all 10 brief sections; no BLOCK).
- Code SHA: `d786104` — `feat(iter-v3/116): early-exit-on-no-confirmation exit primitive + revert /115` — the 8 brief-declared changes: BacktestConfig + Order field extensions (`enable_no_confirm_exit`, `no_confirm_trigger_atr`, `no_confirm_k_candles`, `no_confirm_arm_time`, `no_confirm_threshold_price`); TradeResult.exit_reason docstring with `"no_confirm"` added; atr_distance derivation in create_order; order-loop bookkeeping with TP/SL → no_confirm → timeout ordering in `backtest.py:251-287`; the /115 stale-knob revert (`label_mode = "triple_barrier"`, `atr_tp_multiplier = atr_sl_multiplier = 2.0/1.0`) at three independent surfaces (model builder, accretion guard, pre-flight assertion); `ITERATION_LABEL = "v3-116"`; `MODEL_SPECS` prefix updated `v3-113-` → `v3-116-`; banner-print `V3_FEATURE_COLUMNS=22` → `=14` (the iter-v3/115 Critic Rec 2 cosmetic cleanup); 8-scenario adversarial integration test (`tests/test_no_confirm_exit.py`). 16 config-accretion knobs verified PASS at runner pre-flight (run.log line 20). This is the code-state the backtest ran against.
- Engineering report SHA: `f840eb9` — `briefs-v3/iteration_v3-116/engineering_report.md` (0.70h backtest; `comparison.csv` IS +0.6246 / OOS +1.1089; per-symbol attribution; slot-freeing cascade evidence with October 2025 bar-by-bar attribution).
- Critic review SHA: `b76c37d` — `briefs-v3/iteration_v3-116/review.md` (OVERALL: EXPLORATION-PROMISING — subclass PROMISING-MECHANICAL; single round, zero clarifications requested; all 8 checks PASS for the verdict; Critic OVERRODE mechanical first-match-wins NEGATIVE in the UPGRADE direction).
- Reports: `reports-v3/iteration_v3-116/` — `comparison.csv` IS +0.6246 / OOS +1.1089, 107 OOS trades / 161 IS trades; `dsr.json` PBO=0.1278 PASS, PSR=1.0 PASS, DSR_relative=1.0 PASS, CPCV frac_positive_paths=0.6444 PASS; `adf_test.csv` 42/42 stationary at IS-end 2025-03.
- Catalog update: `briefs-v3/exploration_catalog.md` /116 row (classification "EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass; no_confirm early-exit primitive; regime-adaptive book composition + slot-freeing cascade; broad cross-symbol OOS lift; Critic upgrade-direction override of mechanical first-match-wins NEGATIVE; bundle at /120 CONFIRMATION as strictly accretive").
- BASELINE_V3.md: documentation-only banner update (the "Last updated:" line) — `v0.v3-059` UNCHANGED as canonical; an EXPLORATION never changes baseline metrics; PROMISING-MECHANICAL is a candidate for the /120 CONFIRMATION bundle, not a baseline update.
- Diary SHA: this closeout — `docs(iter-v3/116): diary entry`.
- **Tag**: `v0.v3-116` — a closeout marker only, tagged by the orchestrator (the `v0.v3-082`…`v0.v3-115` pattern).

iter-v3/116 is cycle-6 EXPLORATION slot #7. It tested the cycle-6 STRUCTURAL trade-construction-layer axis recommended by the iter-v3/115 closeout — the QR pivoted bolder than the dispatch's literal recommendation (Axis A regime-conditioned exit barrier), ran a 3-axis EDA, found all three axes returned NO-GO on the pre-registered formal gates, and selected Axis C (early-exit-on-no-confirmation) on the residual-uncertainty criterion. The Phase-6 backtest produced **IS +0.6246 / OOS +1.1089** — the mechanical first-match-wins fires Criterion 1a NEGATIVE on the IS leg (IS Δ −0.21 vs /060 anchor), but the Phase-7.5 Critic OVERRODE in the UPGRADE direction on three substantive lines (coherent regime-cost mechanism + broadly-distributed mechanistically-traceable OOS lift across all 3 symbols + mechanical book-composition diagnostic). **Classification: EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass** per `feedback_promising_mechanical_subtype.md` — a strictly-accretive component decision on /059, non-compoundable across iterations. **This is cycle 6's FIRST PROMISING after 6 consecutive NEGATIVEs (/110-/115).** The no_confirm primitive advances to the iter-v3/120 CONFIRMATION as a strictly-accretive YES/NO-keep accretion decision (per Critic Rec 1), with a pre-registered IS Sharpe band accommodating the regime-cost (Critic Rec 2) and a pre-registered multi-seed cascade-attribution falsifier (Critic Rec 3). Seven lessons: (a) IS-vs-OOS-Sharpe asymmetry can be regime-composition-explicable for a regime-adaptive rule (per user 2026-05-20); (b) the Critic-override authority is symmetric — DOWNGRADE (/114) and UPGRADE (/116) precedents established; (c) PROMISING-MECHANICAL is non-compoundable; (d) the slot-freeing cascade is a production mechanism the EDA's static-direction counterfactual structurally cannot model — future EDAs on rule-cuts-trades-early axes must include slot-cascade modelling; (e)-(g) the three Critic Recommendations for /120 (bundle as accretive, pre-register IS regime-cost band, pre-register multi-seed cascade falsifier). **iter-v3/117 advances to the CANDLE-FREQUENCY axis per user directive 2026-05-20 (`feedback_v3_candle_frequency_unblocked.md`)** — recommended: 24h base with 3-offset multi-offset derived-series (offsets 0h/8h/16h UTC); rationale: the /109 permutation null was explicitly scoped to 8h, cycle 6's six 8h NEGATIVEs (/110-/115) empirically re-confirmed the binding constraint at 8h, the 24h frequency reduces fee drag and dampens HFT noise (the user's 2026-05-20 reasoning) while the 3-offset derivation compensates for the data-count loss; the /113 standalone 8 daily features clearing their permutation null is positive evidence that the daily representation carries signal the 8h cannot see; the axis is structurally distinct from every cycle-6 dead path (label, feature-family, universe, pooling, risk-primitive, exit-barrier-geometry, no_confirm exit) and is the one structural lever the QR has never had access to. The QR backs the axis with a committed IS-only `analysis/iteration_v3-117/*.py` EDA per `feedback_v3_axis_selection_quant_discipline.md`, re-running the /109 permutation null at the chosen frequency and the multi-offset alignment audit. The cycle has 3 EXPLORATION slots left (117–119) before the mandatory iter-v3/120 CONFIRMATION. `OOS_CUTOFF_DATE`/`training_months` untouched.
