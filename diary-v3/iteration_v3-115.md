# iter-v3/115 — Cycle-6 EXPLORATION slot #6 — LABELING ARCHITECTURE: a coherent horizon-exit labeling architecture (`fixed_horizon` directional label PAIRED with a non-binding-barrier fixed-21-candle time exit) — FILED EXPLORATION-NEGATIVE — the IS leg collapsed to +0.3172 (Δ −0.52 vs the /060 anchor); the +0.94 OOS headline is the pre-registered IS-collapse/OOS-spike regime artifact (Section 7 Mode 2), not a discovered edge. Critic single round, zero clarifications, all 8 checks PASS for the verdict. Check 1 PASS — the IS-collapse/OOS-spike is GENUINE regime exposure, audited leak-free at three surfaces. Check 8 PASS — provenance clean; the iter-v3/114 false-provenance failure mode did NOT recur (the new `feedback_v3_brief_parameter_provenance.md` discipline held on its first application). The coherent horizon-exit labeling axis is CLOSED — the 2nd falsification of a label-estimand change in the /072→/105→/115 lineage.

**Date**: 2026-05-19
**Type**: EXPLORATION (cycle-6 slot #6 of 10; iter-v3/120 is the mandatory cycle-6 CONFIRMATION) — ran a full Phase 1–8 (EDA + brief + backtest + Critic, single Critic round), classified at Phase 7.5
**Verdict**: **EXPLORATION-NEGATIVE.** iter-v3/115 tested the cycle-6 STRUCTURAL labeling axis the iter-v3/114 closeout recommended — a NEW labeling architecture. It swapped the v3-canonical triple-barrier labeling architecture (a +2/−1 ATR price-barrier label, executed through TP/SL/timeout barriers) for a **coherent horizon-exit architecture**: a fixed-horizon directional label (`label_mode="fixed_horizon"` — `sign` of the realized 21-candle forward net return) PAIRED with a horizon-coherent execution (every trade held to the fixed 21-candle timeout, the TP/SL barriers made non-binding via `atr_tp_multiplier=atr_sl_multiplier=100.0`). The label and the execution are the two inseparable halves of ONE axis — the labeling architecture (iter-v3/072's Critic Recommendation 1 established they are not separable; a fixed-horizon label with mismatched barrier execution is the /072 *incoherent* dead path). The Phase-6 backtest (3-seed EXPLORATION, code SHA `2b99a1b`, 0.69h) produced **IS monthly Sharpe +0.3172 / OOS monthly Sharpe +0.9370** (`reports-v3/iteration_v3-115/comparison.csv`). The Phase-7.5 Critic (`briefs-v3/iteration_v3-115/review.md`, FINAL — single round, zero clarifications requested) returned **OVERALL: EXPLORATION-NEGATIVE** — all 8 checks PASS for the verdict; the classification follows mechanically from the pre-registered Section 8 first-match-wins taxonomy (Criterion 1 fires on the IS leg: IS +0.3172 < the +0.7325 NEGATIVE floor).
**Classification**: **EXPLORATION-NEGATIVE.** The IS leg collapsed to +0.3172, a Δ of −0.52 vs the /060 EXPLORATION-mode anchor (+0.8325) — the pre-registered Section 8 Criterion-1 NEGATIVE falsifier (IS Δ < −0.10). The +0.94 OOS headline is NOT a discovered edge: it is the pre-registered Section 7 Mode 2 IS-collapse/OOS-spike regime artifact (the /105 signature) — the QR's NO-GO EDA correctly predicted the NEGATIVE before the backtest ran. The coherent horizon-exit labeling architecture does NOT advance to the iter-v3/120 CONFIRMATION bundle. **The coherent horizon-exit labeling axis is CLOSED** — the 2nd falsification of a label-estimand change in the /072→/105→/115 lineage. `label_mode="fixed_horizon"` must NOT be re-bundled at iter-v3/120.
**Decision**: **NO-MERGE.** BASELINE_V3.md UNCHANGED — canonical **`v0.v3-059`** (IS monthly Sharpe **+1.0894** / OOS monthly Sharpe **+0.5791**, 10-seed CONFIRMATION). An EXPLORATION never updates the baseline regardless of outcome; a NEGATIVE EXPLORATION does not advance. `v0.v3-115` is a closeout marker only.
**Branch**: `iteration-v3/115`

---

## 1. The axis — a coherent horizon-exit labeling architecture

iter-v3/115 is cycle-6 EXPLORATION slot #6 of 10. The cycle-6 axis menu (`project_v3_cycle6_axis_menu.md`) was fully spent by /114 — universe/symbol-selection ×2 (/110 label-confound, /111 clean-NEGATIVE), pooled-vs-per-symbol model architecture ×1 (/112 NEGATIVE), multi-frequency feature engineering ×1 (/113 NEGATIVE), risk management ×1 (/114 NEGATIVE with a Check-1 FAIL). The /114 closeout advanced to a STRUCTURAL axis *outside* the spent four-item menu, per `feedback_v3_structural_over_knob_exploration.md` (NEW labeling ranks above NEW risk primitive and above gate-threshold knobs), and recommended a NEW labeling architecture — the QR to adjudicate between triple-barrier + a meta-labeling secondary filter, OR a directional fixed-horizon return label.

**The QR adjudicated and chose the directional fixed-horizon label — but explicitly NOT the naive design.** Meta-labeling was rejected on existing committed evidence: iter-v3/108 ran the /017-corrected meta-labeling EDA and conclusively proved it dead (held-out winner-vs-loser AUC 0.561, sub-period-unstable, 0 of 5 model capacities clear the bar, permutation p=0.18). Re-proposing meta-labeling would re-tread a NULL-AT-EDA. The QR also identified that the *naive* fixed-horizon-label-only design is itself a dead path — iter-v3/072 tested fixed-horizon-label-only (execution left as TP/SL barriers) → EXPLORATION-NEGATIVE, and the /072 Critic Recommendation 1 ruled the coherent 2-component design the single correct test: *"A coherent fixed-horizon design needs BOTH a fixed-horizon label AND fixed-horizon execution exit — a 2-axis change requiring its own brief."* iter-v3/115 IS that brief.

**The hypothesis (brief Section 1):** replacing the triple-barrier labeling architecture with a coherent horizon-exit architecture — a fixed-horizon directional label executed through a horizon-coherent fixed-21-candle time exit — will change the OOS monthly Sharpe; the committed IS-only EDA predicts the change is **non-positive**.

**The single substantive axis vs /059 is the labeling architecture** — `label_mode` `triple_barrier` → `fixed_horizon`, and the inseparable execution change `atr_tp_multiplier`/`atr_sl_multiplier` `2.0`/`1.0` → `100.0`/`100.0` (barriers non-binding). The 14-feature `V3_FEATURE_COLUMNS`, the BCH/LDO/TRX universe, the per-symbol LightGBM architecture, the 7-gate RiskV2 stack, the ensemble seeds, the data window, and `REQUIRED_GAP = 66` are all held /059-identical. The horizon N = 21 candles is FIXED at the v3 triple-barrier timeout — chosen for embargo-invariance (the CV embargo and `REQUIRED_GAP` stay byte-identical to /059), declared hand-chosen, NOT tuned. The QR backed the axis with a committed IS-only EDA (`analysis/iteration_v3-115/`, EDA SHA `d871b22`, 6 result tables T1–T6) per `feedback_v3_axis_selection_quant_discipline.md`.

**The EDA returned a hard NO-GO** (brief Section 2.7, `horizon_exit_synthesis.py` T6, pre-registered GO rule `GO iff g1 AND g2 AND g3`):
- **g1 LABEL VALIDITY — PASS** (3/3 balanced, 3/3 differ materially from triple-barrier — the two labels agree on only 70–75% of candles).
- **g2 EXECUTION COHERENCE — PASS** (3/3 sanity; the /072 label-vs-execution mismatch is a real 25–30% on all 3 symbols — the coherent design drives it to 0 by construction).
- **g3 TRADE-BOOK GAIN — FAIL** (0/3 — the DECISIVE gate). T3 held the triple-barrier label's direction FIXED and compared what that identical direction sequence earns under triple-barrier execution vs horizon-exit execution. Horizon-exit execution monthly Sharpe was WORSE on all 3 symbols — Δ −1.11 (BCH), −0.80 (LDO), −0.37 (TRX). The asymmetric +2/−1 ATR price barrier — a tight −1 ATR stop that cuts losers and a +2 ATR target that books winners — is itself a Sharpe-generating device that a symmetric 21-candle time exit discards.
- **VERDICT = NO-GO.**

Per THE PRIME DIRECTIVE the EDA designs the experiment but does not terminate the iteration — the NO-GO set the brief's modal prediction (a non-positive OOS effect) and the Section-7 pre-registered modal failure mode, and the iteration ran the Phase-6 backtest. Running it was genuinely informative: T3 is an ORACLE counterfactual on perfect-direction calls; the production backtest tests whether the *real* LightGBM, trained on the more-feature-predictable horizon-exit label (T4: ~1.5× IC ratio; T5: clears the permutation null), produces a different trade selection that partially offsets the geometry penalty. The honest modal prediction was that it would not — and it did not.

## 2. Phase 7 — OOS evaluation (first look at the OOS reports)

This is the QR's first look at the iter-v3/115 OOS reports. The headline of Phase 7 is the IS-collapse/OOS-spike divergence the brief explicitly pre-registered.

### 2.1 Headline metrics (`comparison.csv`)

| Metric | In-Sample | Out-of-Sample | Ratio |
|---|---:|---:|---:|
| monthly Sharpe | **+0.3172** | **+0.9370** | 2.9543 |
| daily Sharpe | +1.1421 | +2.7507 | 2.4084 |
| profit factor | 1.2185 | 1.6022 | 1.3149 |
| win rate | 46.67% | 52.83% | 1.1321 |
| n_trades | 105 | 53 | 0.5048 |
| total PnL (weighted) | +33.62% | +41.42% | 1.2319 |
| max drawdown | **48.12%** | 33.51% | 0.6965 |
| monthly Calmar | 0.6987 | 1.2359 | 1.7688 |

**The IS leg collapsed.** IS monthly Sharpe +0.3172 is a Δ of **−0.5153** vs the /060 EXPLORATION-mode anchor (+0.8325) — far below the −0.10 NEGATIVE floor; the pre-registered Section 8 Criterion-1 falsifier fires on the IS leg. The OOS monthly Sharpe +0.9370 is a Δ of +0.7967 above the /060 anchor (+0.1403). The OOS/IS monthly Sharpe ratio is **2.9543** — just under the 3.0 SUSPICIOUS-gate threshold (`feedback_v3_oos_is_ratio_gate.md`); the gate does not formally fire, but the result sits at the structural edge of the SUSPICIOUS zone, and under the first-match-wins taxonomy the NEGATIVE criterion fires first on the IS leg before the SUSPICIOUS gate can be evaluated. **The classification is NEGATIVE; the IS-collapse/OOS-spike regime-artifact reading is the substantive Phase-7 finding behind it.**

IS MaxDD blew out to 48.12% — +17.15 percentage points vs /059's IS 30.97%. The brief Section 5 and Section 7 pre-registered "MaxDD up (longer holds)" as the NEGATIVE-outcome signature: the symmetric 21-candle hold keeps losers open for the full horizon, inflating IS MaxDD. Confirmed.

### 2.2 The +0.94 OOS is a regime artifact, not an edge — the IS leg collapsed

The central finding of Phase 7: **the +0.94 OOS is the pre-registered Section 7 Mode 2 IS-collapse/OOS-spike regime artifact (the /105 signature), not a discovered horizon-exit edge.** The committed evidence forces this on three legs:

1. **The IS leg collapsed (+0.3172, Δ −0.52).** A genuine edge produced by a labeling-architecture change shows up in IS — the IS window is 2.5× longer than the OOS window and spans bear+chop+recovery. A change whose only positive signal is in the 14-month OOS uptrend, while the 39-month IS book scrapes +0.32, has not discovered a transferable property of the data. The T3 ORACLE counterfactual predicted exactly this — the horizon-exit execution geometry costs −0.4 to −1.1 monthly Sharpe vs triple-barrier on identical direction calls — and the production backtest confirmed it: IS collapsed to +0.3172.

2. **The OOS book is genuinely MIXED — the +0.94 is carried by 2 regime-favorable months.** `out_of_sample/monthly_pnl.csv` shows 5 of 14 OOS months negative (2025-07, 2025-09, 2025-10, 2025-11, 2026-02, 2026-03, 2026-04 are losing or near-zero; the +0.94 Sharpe is carried by 2025-05 +20.72% and 2025-08 +24.74%). This is the Critic Check-1 evidence: a look-ahead leak inflates OOS PnL *uniformly*; instead the OOS Sharpe comes from 2 regime-favorable months against real losing months. A 21-candle symmetric hold is a longer-duration directional bet that the 2025-03→2026-05 OOS uptrend rewards selectively — regime exposure, not signal.

3. **The mechanism (Section 7 Mode 2):** IS Optuna fits on the more-feature-predictable horizon-exit label (T4: IC ratio ~1.5×) in an IS bear/chop regime, producing a *lower*-Sharpe trade fit because the feature→label predictability advantage does NOT translate to trade-book Sharpe (the exact /105 lesson — a more-learnable label does not imply a better trade book; the trade-construction layer, not the label, is the bottleneck). The OOS uptrend then rewards the longer 21-candle holds. The pattern is regime exposure, not a discovered edge — corroborated by DSR = 0.0 (no statistical significance at the EXPLORATION budget) and the IS-collapse fact itself.

The diary does NOT claim the +0.80 OOS Δ as a horizon-exit-coherence edge.

### 2.3 Per-symbol OOS attribution — ONE named metric (`concentration_pct`)

Per the Critic's Recommendation 3, this diary attributes the result on the **correctly-named `concentration_pct` from `comparison.csv`** — and explicitly records that the engineering report's "Per-Symbol OOS Attribution" table mislabeled its column: it listed `pct_of_total_pnl` values (BCH 57.68%, LDO 45.71%, TRX −3.39%) under a header reading "Pct of Total OOS PnL" while the brief Section 5 rule names `concentration_pct` as the metric (the OOS book total +41.42 is not near zero). `comparison.csv`'s `per_symbol` block carries the genuinely different `concentration_pct` values. The Critic flagged the conflation as not verdict-affecting (the NEGATIVE is locked on the IS leg) but to be corrected in the diary. This diary uses `concentration_pct` and explicitly does not use `pct_of_total_pnl`.

| Symbol | OOS trades | OOS WR | OOS weighted_pnl | OOS `concentration_pct` | Role |
|---|---:|---:|---:|---:|---|
| **LDOUSDT** | 11 | 54.5% | +29.54 | **+71.32%** | the OOS book's largest contributor — OOS-positive |
| BCHUSDT | 18 | 66.7% | +15.20 | **+36.71%** | OOS-positive |
| **TRXUSDT** | 24 | 41.7% | −3.32 | **−8.02%** | flat-to-slight-drag |

Per-symbol reading: **BCH and LDO are OOS-positive; TRX is flat (a slight net drag).** LDO carries 71.32% of the OOS weighted PnL — a concentration that would, at a CONFIRMATION, fail the ≤30% top-symbol gate; but this is an EXPLORATION (the bundle-level concentration check is deferred to iter-v3/120), and more to the point the IS leg already locked NEGATIVE. The per-symbol IS picture is the decisive one: IS BCH net_pnl_pct **−53.10%** / 54.2% WR (BCH IS collapses hardest under the symmetric time exit — the T3 prediction: a tight stop + wide target creates Sharpe the symmetric hold discards); IS TRX net_pnl_pct **−20.89%** / 52.2% WR (drag); IS LDO net_pnl_pct +8.77% / 54.5% WR (the only IS-positive symbol, on a thin 11-trade IS book). The IS book is net-negative on 2 of 3 symbols — the IS collapse is broad-based, not a single-symbol artifact.

**Comparison vs iter-v3/072** (the prior fixed-horizon-label-ONLY NEGATIVE): /072's LDO OOS catastrophe (−32.49 weighted PnL / 21.4% WR) did NOT recur — /115 LDO OOS is +31.04% net PnL / 54.5% WR. The coherent execution design removed the /072 label-execution mismatch mechanism exactly as the brief T2 diagnostic predicted (the /072 mismatch goes to 0 by construction). The /115 NEGATIVE is therefore a *distinct, new* failure — the time-exit geometry itself underperforms the asymmetric price barrier (the T3 finding) — not a recurrence of /072's mismatch.

### 2.4 OOS regime (`out_of_sample/per_regime.csv`)

The OOS trades are tagged under the standard single-regime v3 OOS reporting behaviour and carry no additional signal.

## 3. What Worked / What Failed

### What Worked

1. **The coherent horizon-exit architecture genuinely landed — both halves of the axis, verified independently.** Critic Check 8 PASS: `label_mode="fixed_horizon"` flows to `label_trades`; the execution change is independently confirmed — `out_of_sample/trades.csv` has 52/53 `timeout` exits + 1 `end_of_data` (the trailing candle truncated by the data boundary, not a barrier exit), `in_sample/trades.csv` has 105/105 `timeout`, ZERO barrier exits on either side. The ×100 ATR multipliers produced barriers of ~370%+ per trade — far beyond any 21-candle move. The behavioral-inertia falsifier (Section 8 Criterion 3) did not fire: the label and execution change clearly altered the trade geometry (100% of exits moved from a TP/SL/timeout mix to timeout-only). The axis genuinely ran and genuinely underperformed — exactly the honest NEGATIVE the EDA pre-registered.

2. **Provenance is clean — the iter-v3/114 false-provenance failure mode did NOT recur. This is a process win, and it is recorded as one.** iter-v3/115 is the first iteration after iter-v3/114's Check-1 FAIL, where a hardcoded threshold `0.30` was laundered through a false "T3 IS-only sweep" provenance and the new rule `feedback_v3_brief_parameter_provenance.md` was established. The /115 brief cites exactly ONE design parameter — the horizon N = 21 — and DECLARES it hand-chosen (Section 0), justified by embargo-invariance (N = 21 = the v3 triple-barrier timeout, so the CV embargo and `REQUIRED_GAP` stay byte-identical to /059), explicitly NOT chosen for IS performance. There is no IS-tuned scalar, no sweep, no threshold laundered as an optimization output, no false-provenance claim — the Critic confirmed at source (Check 8): *"nothing is tuned … Provenance is clean."* The new provenance discipline held on its first application. The iter-v3/110 stale-knob confound was also averted: Changes 5+6 reverted the /114 LDO kill-switch state (`regime_gate_symbols=()`, `enable_ldo_realvol_gate=False`) and a pre-flight guard asserts the reverted /059-canonical state — the /114 realvol gate does NOT run through the /115 backtest, no co-varying confound.

3. **The QR's NO-GO EDA correctly predicted the NEGATIVE — a well-calibrated prediction (see Section 7).** The EDA's decisive gate g3 (T3) FAILED 0/3 before the backtest ran, and the brief's Section 7 modal failure mode (the geometry-does-not-transfer mode, ~60%) is exactly what materialized. The EDA's T3 ORACLE counterfactual measured the geometry penalty at −0.4 to −1.1 monthly Sharpe; the production IS collapsed to +0.3172.

4. **The Critic review was clean and decisive — single round, zero clarifications.** All 8 checks resolved on unambiguous source evidence; the look-ahead audit traced the 21-candle forward-label window's embargo at three surfaces (label construction, train/test embargo, the mixed OOS book = regime artifact not leak). The verdict followed mechanically from the pre-registered Section 8 taxonomy.

### What Failed

1. **The coherent horizon-exit geometry does not transfer — the IS leg collapsed (the decisive failure).** IS monthly Sharpe +0.3172, Δ −0.5153 vs the /060 anchor — the pre-registered Section 8 Criterion-1 NEGATIVE falsifier. The mechanism is the EDA's T3 finding, confirmed in production: on identical directional calls, a symmetric 21-candle time exit produces a monthly Sharpe −0.4 to −1.1 *below* the asymmetric +2/−1 ATR price-barrier exit. The asymmetric barrier — a tight −1 ATR stop that cuts losers fast and a wide +2 ATR target that lets winners run — is itself a Sharpe-generating device. A symmetric time exit holds losers to the full 21-candle horizon (the IS MaxDD blow-out to 48.12% is the direct cost) and caps winners at the 21-candle mark, discarding the barrier's asymmetry. The label-estimand change did not fix the trade-construction bottleneck — it made it worse.

2. **The +0.94 OOS headline is a regime artifact, not a real upside.** Superficially the OOS resembles a strong PROMISING; Phase 7 (Section 2.2) establishes it is the pre-registered Section 7 Mode 2 IS-collapse/OOS-spike divergence — 5 of 14 OOS months negative, the +0.94 carried by 2 regime-favorable uptrend months (2025-05, 2025-08). A 21-candle directional hold is a longer-duration bet the OOS uptrend rewards selectively. DSR = 0.0 confirms no statistical significance at the EXPLORATION budget. The OOS number is regime exposure, not a discovered edge.

3. **The label-estimand axis is now twice-falsified.** /072 (fixed-horizon-label-only, incoherent execution) → NEGATIVE; /115 (fixed-horizon-label + coherent execution) → NEGATIVE. The estimand-change family has produced no transferable edge across two structurally distinct designs (see Lesson 1).

## 4. Phase 8 — the coherent horizon-exit labeling axis closes

The coherent horizon-exit labeling architecture is now tested and closed. Per the pre-registered Section 8 first-match-wins taxonomy, iter-v3/115 is catalogued **EXPLORATION-NEGATIVE** — Criterion 1 fires on the IS leg (IS +0.3172 < the +0.7325 floor). Criterion 2 (SUSPICIOUS-OOS-DOMINANT) is not evaluated — Criterion 1 fired first; the OOS/IS ratio 2.9543 is just under the 3.0 gate anyway, and the IS-collapse/OOS-spike regime-artifact reading is the contextual diagnosis behind the NEGATIVE, not a separate formal classification.

**The coherent horizon-exit labeling axis is CLOSED.** It does not re-open absent genuinely new, properly-fenced IS-only counterfactual evidence that the trade-construction-layer geometry penalty (the T3 finding) can be overcome. `label_mode="fixed_horizon"` must NOT be carried as a bundle ingredient at the iter-v3/120 CONFIRMATION (Critic Recommendation 1).

## 5. Critic Review Summary (Phase 7.5 — 8 checks)

The Phase-7.5 Critic review (`briefs-v3/iteration_v3-115/review.md`, FINAL — single round, zero clarifications requested, Round 2 skipped) scored the EXPLORATION on Checks 1, 2, 4, 5, 6, 8 (verdict-scoring) with Check 3 informational at the 3-seed/35-trial EXPLORATION budget per `feedback_v3_dsr_mode_artifact.md`. **All 8 checks PASS for the verdict.**

| Check | Topic | Status | Note |
|---|---|---|---|
| 1 | Look-Ahead Audit | **PASS — verdict-scoring** | The make-or-break check. The `fixed_horizon` label is forward-looking by construction (correct and intentional); the audit reduces to whether the 21-candle forward label window is embargo-purged at the train/test boundary. It is, verified at three surfaces: (1) label construction (`labeling.py:349-423`) — the forward window is exactly the `label_timeout_minutes` horizon; (2) train/test embargo (`walk_forward.py:113`) — `train_end_ms = test_start_ms − embargo_ms`, embargo = 22 candles, confirmed at the IS/OOS boundary (test 2025-04 → train ends exactly at the 2025-03-24 OOS cutoff); (3) **the OOS +0.94 is GENUINE regime exposure, not a leak** — `out_of_sample/monthly_pnl.csv` shows a genuinely mixed OOS book (5 of 14 months negative); a leak inflates OOS PnL uniformly, instead the +0.94 comes from 2 regime-favorable months against real losing months. The IS-collapse/OOS-spike is the pre-registered Section 7 Mode 2 regime artifact, NOT a leaked-future artifact. **PASS.** |
| 2 | Embargo Width | **PASS** | Label horizon = 21 candles (`10080/480`); required gap `(21+1)×3 = 66`; actual `REQUIRED_GAP = 66`. Intra-Optuna CV gap verified at every fold (`gap=184h / 22 rows`). Byte-identical to /059, correctly sized for the 21-candle `fixed_horizon` horizon (= the v3 triple-barrier timeout — the embargo-invariance rationale the brief gives for hand-fixing N=21). `_verify_timeout_consistency` PASS. |
| 3 | Multiple-Testing Correction | PASS (**informational** — NOT verdict-triggering) | Per TYPE=EXPLORATION, DSR/PSR are informational; only PBO is meaningful at the 3-seed/35-trial budget (`feedback_v3_dsr_mode_artifact.md`). **PBO = 0.1426 < 0.4 — passes.** `dsr.json` carries `dsr_relative=0.9593`, `dsr_relative_b4=1.0` — EXPLORATION-mode structural artifacts, informational only. `n_trials=315`, `n_eff=16`. (Minor: the engineering report Key Metrics block reports `dsr 0.000` without surfacing `dsr_relative` — a reporting incompleteness, not a defect.) |
| 4 | IC Correlation | **PASS (non-applicable)** | Zero feature families added; `V3_FEATURE_COLUMNS` unchanged at the 14-feature /059-canonical stack. No new-vs-existing pair to test. The pre-existing /059-canonical pair above 0.70 (`regime_momentum_signed_5d` vs `vwap_dev_20`) is inherited, covered by the `feedback_v3_engineered_feature_pivot.md` carve-out, not introduced by /115. |
| 5 | ADF Stationarity | **PASS** | `adf_test.csv` at the IS-window-end month 2025-03: all 42 feature-symbol pairs `p < 0.05`, `stationary=True`. The early-month `stationary=False` rows are insufficient-sample warm-up artifacts. Features unchanged from /059. |
| 6 | Pareto Dominance | **PASS (non-applicable)** | EXPLORATION mode produces a single deterministic roster; `pareto_front.csv` not produced (correct). The Pareto check is deferred to the iter-v3/120 CONFIRMATION. |
| 7 | Reproducibility | **PASS** | Commit SHA `2b99a1bd6e2bd655ac6812806abe5bd075746083` stamped; the runner uses an explicit 14-element `feature_columns` list, never `None`; the 3-seed ensemble seeds are literal in `ensemble_summary.json`. Trade spot-checks re-computed clean (OOS BCH SHORT net 4.0489% / weighted 1.4171; OOS BCH LONG weighted 5.7732; OOS TRX SHORT weighted −1.9284 — correct loss sign). PnL arithmetic, direction sign, fee deduction, weight_factor all correct. |
| 8 | Hypothesis-Implementation Alignment | **PASS — verdict-scoring** | Every brief change maps to code; no scope creep; the implementation tests the registered hypothesis. Change 1 (label) — `label_mode="fixed_horizon"` flows to `label_trades`, verified. Change 2 (execution) — `_atr_tp, _atr_sl = 100.0, 100.0`; barriers-non-binding independently confirmed (`out_of_sample/trades.csv` 52/53 `timeout` + 1 `end_of_data`, ZERO barrier exits; `in_sample/trades.csv` ZERO barrier exits). Change 3 (label_mode accretion guards) — both guard sites updated. Change 4 (`ITERATION_LABEL`) — `"v3-115"`. Change 5 (/114 LDO kill-switch revert) — `regime_gate_symbols=()`, `enable_ldo_realvol_gate=False`, `enable_regime_gate=False`; the /114 realvol gate does NOT run through the /115 backtest, no co-varying confound (the iter-v3/110 stale-knob failure mode averted; /072's LDO catastrophe did NOT recur — /115 LDO OOS +31.04%). Change 6 (pre-flight guard) — asserts the reverted /059-canonical state; the runner passed pre-flight. Integration test `test_fixed_horizon_label_mode.py` is a genuine adversarial end-to-end test (injects a +50% spike AND a −45% crash inside the 21-candle window, asserts `timeout` resolution). **Provenance discipline: the brief cites exactly one design parameter — horizon N=21 — and DECLARES it hand-chosen, justified by embargo-invariance; no IS-tuned scalar, no sweep, no false-provenance claim. The iter-v3/114 failure mode is structurally absent — nothing is tuned. PASS.** |

**All 8 checks PASS for the verdict, and two PASS results are structural process wins.** Check 1 PASS — the IS-collapse/OOS-spike is GENUINE regime exposure, audited leak-free at three surfaces; a look-ahead leak would have inflated the OOS book uniformly, and instead the OOS is genuinely mixed (5 of 14 months negative). **Check 8 PASS — provenance is clean: the iter-v3/114 false-provenance failure mode did NOT recur.** iter-v3/115 was the first iteration after /114's Check-1 FAIL established `feedback_v3_brief_parameter_provenance.md`; the new discipline held on its first application — the brief cites one design parameter, declares it hand-chosen, and laundered nothing as an optimization output. The Critic confirmed at source: *"nothing is tuned … Provenance is clean."* The OVERALL: EXPLORATION-NEGATIVE follows mechanically from the pre-registered Section 8 first-match-wins taxonomy — Criterion 1 fires on the IS leg (IS +0.3172 < +0.7325). The Critic noted two cosmetic stale-string defects (NOT verdict-affecting, NOT brief violations) — see Section 8 / Critic Recommendation 2.

## 6. ADF Report Summary

`reports-v3/iteration_v3-115/adf_test.csv` runs the Augmented Dickey-Fuller stationarity test on the /059-canonical 14-feature stack, per symbol per walk-forward retraining month (2,198 rows; ADF evaluates feature stationarity, label-independent — and the /115 axis is labeling, so the feature stationarity picture is /059-inherited unchanged).

- **At the IS-window-end month (2025-03) all 42 feature-symbol pairs are stationary** at `p < 0.05` — every one of the 14 features on each of BCH, LDO, TRX. The IS-end month is the operative diagnostic; it is clean.
- 1,803 of 2,198 rows are stationary overall. The 395 `stationary=False` rows are first-history warm-up sentinels — insufficient candle history for the ADF regression to compute reliably in the early months — a could-not-test marker, not a genuine stationarity failure.
- The 14 features are scale-invariant constructs (z-scores, returns, Hurst, ratios, drawdown-window). **Check 5 ADF: PASS.**

## 7. Pre-Registered Failure-Mode vs Reality

The brief Section 7 pre-registered four outcomes anchored on the NO-GO EDA. The modal call (~60%) was the geometry-does-not-transfer failure mode; the second mode (~25%) was the IS-collapse/OOS-spike SUSPICIOUS divergence. **The QR's NO-GO EDA and the Section-7 prediction were well-calibrated — both the modal mechanism and the second mode materialized, and they are the same NEGATIVE.**

| # | Pre-registered failure mode (Section 7) | As-run reality | Fired? |
|---|---|---|---|
| 1 (modal, ~60%) | **The horizon-exit geometry does not transfer** — on identical direction calls the fixed-horizon time exit produces a monthly Sharpe −0.4 to −1.1 below the triple-barrier price-barrier exit; IS in [−0.40, +0.85] (modal ~+0.30), OOS in [−0.55, +0.55] (modal ~+0.05), profit factor down, MaxDD up, trade roster materially changed. Section 8 NEGATIVE criterion fires. | **FIRED — and the point estimate was nearly exact.** IS landed +0.3172 vs the modal +0.30 prediction (inside the [−0.40, +0.85] interval, ~1pp off the point estimate). MaxDD blew out to 48.12% (pre-registered "MaxDD up"). The trade roster materially changed (100% timeout exits). The OOS landed +0.9370 — *above* the modal +0.05 and above the [−0.55, +0.55] interval's upper bound; that upside is explained by Mode 2 (the OOS-spike). | **YES — the modal mechanism, and the IS point estimate, were correctly predicted.** |
| 2 (second, ~25%) | **IS-collapse/OOS-spike SUSPICIOUS divergence — the /105 signature** — if /115 lands IS low and OOS high with OOS/IS ratio > 3.0, the result is a regime artifact not a discovered edge. | **FIRED in substance.** IS collapsed (+0.3172) and OOS spiked (+0.9370) — the /105 IS-collapse/OOS-spike signature. The OOS/IS ratio 2.9543 is just under the formal 3.0 gate, so the SUSPICIOUS classification did not formally trigger — and under the first-match-wins taxonomy Criterion 1 (NEGATIVE) fires first on the IS leg anyway. The regime-artifact mechanism the brief pre-registered for Mode 2 (a longer-duration directional bet the OOS uptrend rewards selectively) is exactly the Phase-7 Section-2.2 diagnosis. | **YES — the IS-collapse/OOS-spike pattern materialized; subsumed by NEGATIVE under first-match-wins.** |
| 3 (~10%) | **Behavioral inertia / mis-landed change** — IS roster Δ < 8% or exit-mix not ≥99% `timeout`. | Did NOT fire — exit mix is 100% `timeout` (IS) / 98.1%+`end_of_data` (OOS); the label+execution change landed cleanly (Critic Check 8 PASS). | No. |
| — (residual, ~5%) | **Coherence genuinely helps** — the production LightGBM selects a materially better trade set than the ORACLE T3 proxy, overcoming the geometry penalty. | Did NOT occur. The IS collapsed; the production model did not overcome the T3 geometry penalty. The brief assessed this as the least-likely outcome and that assessment was vindicated. | No. |

**Honest prediction-calibration accounting.** This is a well-calibrated prediction and is recorded as one. The QR's NO-GO EDA (g3/T3 FAIL, 0/3) correctly predicted the NEGATIVE before the backtest ran. The Section-7 modal failure mode (the geometry-does-not-transfer mode) fired, and the modal IS point estimate (+0.30) landed ~1pp from the realized IS (+0.3172). The second mode (IS-collapse/OOS-spike) also fired — the +0.94 OOS, which superficially looks like a PROMISING upside, was correctly pre-registered as a regime artifact, and Phase 7 confirmed it (5 of 14 OOS months negative; DSR 0.0). The brief did not chase the +0.94 OOS headline — it pre-registered it as the failure signature it turned out to be. The prediction discipline held, and the provenance discipline held (Section 5 / Lesson 3) — the contrast with iter-v3/114 (where the prediction discipline held but the provenance discipline did not) is the process improvement iter-v3/115 records.

## 8. Lessons

1. **The coherent horizon-exit labeling axis is CLOSED — the 2nd falsification of a label-estimand change in the /072→/105→/115 lineage. This goes to the dead-paths catalog.** Three label-estimand changes have now been tested: iter-v3/072 (fixed-horizon-label-only, incoherent TP/SL execution) → NEGATIVE; iter-v3/105 (trend-scanning label, incoherent triple-barrier execution) → FALSIFIED (a label the features predict +31–52% better still collapsed the multi-seed IS fit); iter-v3/115 (fixed-horizon label + coherent fixed-21-candle execution) → NEGATIVE. iter-v3/115 was the strongest possible form of the estimand-change hypothesis — it built the *coherent* design iter-v3/072's own Critic named as the un-spent axis, removing the /072 label-execution mismatch entirely — and it still produced an IS collapse. **The root cause is structural and now established:** the asymmetric +2/−1 ATR triple-barrier is itself a Sharpe-generating device — a tight stop that cuts losers and a wide target that books winners — and ANY symmetric time-exit estimand discards that asymmetry. A change of the training label's estimand does not address the v3 binding constraint, which the /105→/109 chain localized DOWNSTREAM of the label in the trade-construction / exit / risk layer. **Do NOT re-bundle `label_mode="fixed_horizon"` at iter-v3/120** (Critic Recommendation 1) — the CONFIRMATION must carry the /059-canonical `triple_barrier` label. Do not re-propose a label-estimand-change axis in cycle 6 absent genuinely new evidence that the trade-construction-layer geometry penalty can be overcome.

2. **The runner banner-print and `MODEL_SPECS` stale strings need cleanup at the next setup commit (Critic Recommendation 2).** `run_baseline_v3.py:2889` still prints a banner reading `label_mode=triple_barrier; V3_FEATURE_COLUMNS=22` (echoed in `run.log` line 34) while the actual config verified by the Critic is `fixed_horizon` / 14-feature (`run.log` lines 3/20/21/24–26) — the banner makes `run.log` self-contradictory (line 34 says `triple_barrier`, line 21 says `fixed_horizon`). And `MODEL_SPECS` at lines 197–199 still carry the `v3-113-` model-name prefix (cosmetic — the `ITERATION_LABEL` driving report paths is correctly `v3-115`). Both are display-string defects, NOT verdict-affecting and NOT brief violations, but the next iteration's setup commit must correct them so the runner's own log is internally consistent. This is a process-hygiene item, recorded so the iter-v3/116 setup picks it up.

3. **The brief-parameter-provenance discipline held on its first application — record this as a process win.** iter-v3/115 was the first iteration after iter-v3/114's Check-1 FAIL established `feedback_v3_brief_parameter_provenance.md` (every tuned scalar must name the committed table that produced it; a hand-chosen parameter must be DECLARED hand-chosen). The /115 brief cited exactly one design parameter — horizon N=21 — declared it hand-chosen, justified it by embargo-invariance (not IS performance), and laundered nothing as a sweep output. The Critic's Check 8 confirmed it at source: *"There is no IS-tuned scalar, no sweep, no threshold laundered as an optimization output, no false-provenance claim … Provenance is clean."* The discipline established in response to a failure worked on its very next test. Future labeling/risk-axis briefs that DO select a scalar parameter must continue to apply it — declare hand-chosen parameters, and IS-calibrate tuned ones with an auditable temporal fence (IS-only selection committed before any OOS-window script runs).

4. **A more-feature-predictable label does not imply a better trade book — the /105 lesson, re-confirmed.** The brief's T4/T5 showed the horizon-exit label is genuinely more learnable (IC ratio ~1.5×, clears the permutation null p=0.00) — and the IS book *still* collapsed. Feature→label IC is a property of the labeling problem; trade-book Sharpe is a property of the trade-construction problem. The decisive EDA gate must be the trade-book counterfactual (T3), never the IC test (T4) — and the /115 EDA correctly made T3 the gate and T4 explicitly non-gating. The discipline of not over-weighting IC carries forward.

## 9. Next Iteration Ideas

iter-v3/116 is cycle-6 EXPLORATION slot #7 of 10. The cycle has **4 EXPLORATION slots left (116–119)** before the mandatory iter-v3/120 CONFIRMATION. The cycle-6 axis menu is exhausted in BOTH the original four-item menu (universe ×2, model architecture ×1, multi-frequency feature engineering ×1, risk management ×1 — all NEGATIVE) **AND the labeling axis** that iter-v3/115 opened outside that menu (the coherent horizon-exit architecture — NEGATIVE; the label-estimand family twice-falsified across /072/105/115). Meta-labeling is dead at EDA (/108). Per the user's out-of-the-box mandate and `feedback_v3_structural_over_knob_exploration.md`, iter-v3/116 must be a genuinely creative STRUCTURAL axis, not a knob and not a re-tread.

**Recommended iter-v3/116 axis: a TRADE-CONSTRUCTION / EXIT-LAYER axis — an asymmetric, regime-conditioned exit-barrier architecture: keep the /059-canonical `triple_barrier` directional label, but make the TP/SL ATR multipliers a function of the entry-bar regime state (Hurst / ADX / realized-vol z-score) instead of the static `2.0`/`1.0`.**

Reasoning:

1. **It attacks the binding constraint the entire /105→/109→/115 chain has localized — and which iter-v3/115 has now positively identified.** The /105→/109 convergent chain localized the v3 binding constraint DOWNSTREAM of the label, in the trade-construction / exit / risk layer. iter-v3/115 went further — its T3 finding *names the mechanism*: the asymmetric +2/−1 ATR barrier is itself a Sharpe-generating device, and changing the label's estimand discards it. The logical next move is not another label change (estimand family closed) and not another off-the-shelf feature (/113 closed multi-frequency; /085/086 closed funding/basis) — it is to *push directly on the exit-barrier geometry the T3 finding just proved is doing the work*. If the 2:1 asymmetry generates Sharpe, the un-asked question is whether a *regime-conditioned* asymmetry generates more — e.g. a wider target / tighter stop in high-Hurst (trending) regimes where winners run, a tighter, more symmetric barrier in low-Hurst (mean-reverting/chop) regimes where targets do not fill. This is the structural axis the T3 finding *points at*.

2. **It is genuinely un-spent and structurally distinct from every cycle-6 dead path.** It is not a label change (the label stays `triple_barrier`, the estimand untouched — so it sidesteps the /072/105/115 estimand-family closure entirely). It is not a feature-family addition (no new `features_v3` columns). It is not a universe or pooling change. It is not a risk-primitive *kill-switch* (RiskV3 primitive 9 is closed) — it is an exit-barrier *geometry* change, a different lever: the kill-switch decides *whether* to trade, the exit barrier decides *how the trade resolves*. iter-v3/107 tested an exit-layer re-architecture but filed NULL-AT-EDA on 5 static exit designs — a *regime-conditioned* (state-dependent) barrier was not among them, and the /107 NULL was specifically about static re-architectures; the conditioning variable is the new degree of freedom.

3. **It is the structurally cleanest carrier of the T3 finding into a testable hypothesis.** The /115 T3 counterfactual already produced, on IS-only data, the per-symbol monthly-Sharpe sensitivity of the triple-barrier book to the exit geometry — that machinery is directly reusable as the iter-v3/116 EDA backbone: re-run the T3-style counterfactual sweeping the TP/SL multipliers *conditioned on the entry-bar Hurst/ADX/realvol-z bucket*, and test whether a regime-conditioned barrier schedule beats the static 2:1 on IS. Per `feedback_v3_axis_selection_quant_discipline.md` the QR must back the axis with a committed IS-only `analysis/iteration_v3-116/*.py` EDA before the brief; per `feedback_v3_brief_parameter_provenance.md` (and Lesson 3) every regime-bucket threshold and every conditional multiplier the EDA selects must be DECLARED hand-chosen if hand-chosen, or IS-calibrated with an auditable temporal fence (IS-only selection committed before any OOS-window script runs) — and the iter-v3/116 setup commit must also clear the Critic-Rec-2 banner-print / `MODEL_SPECS` stale strings (Lesson 2). If the regime-conditioned-barrier EDA returns a hard NO-GO, the documented fallback is a position-sizing-layer axis — a regime-conditioned vol-target ceiling — which is the one trade-construction-layer lever (sizing, as opposed to exit geometry) that neither /107 nor /115 nor the /114 kill-switch has exercised.

## 10. Commit chain

- EDA: `analysis/iteration_v3-115/` (EDA SHA `d871b22` — `analysis(iter-v3/115): coherent horizon-exit labeling gating EDA`) — `_shared.py` (the /059-faithful triple-barrier + horizon-exit labelers, walk-forward folding), `horizon_exit_gating_eda.py` (T1–T5), `horizon_exit_synthesis.py` (T6 GO/NO-GO) + 6 result tables. The EDA returned a hard NO-GO (g3/T3 FAIL 0/3 — horizon-exit execution Sharpe worse on all 3 symbols). Strictly IS-only — every script asserts `close_time < OOS_CUTOFF_MS`; no OOS-window file touched.
- Brief: `briefs-v3/iteration_v3-115/research_brief.md` (setup SHA `409102a` initial; `9fd11fa` Section 3.5 /114 kill-switch-revert update for the Phase 5.5 BLOCK fix) — 10-section brief. **Provenance clean (Critic Check 8 PASS):** the brief cites exactly one design parameter — horizon N=21 — declared hand-chosen, justified by embargo-invariance; nothing tuned, nothing laundered as a sweep output.
- Phase 5.5 gate: `briefs-v3/iteration_v3-115/phase5p5_gate.md` (`2ee9119` BLOCK — missing the /114 kill-switch revert; `fae6ff1` PASS after the brief Section 3.5 Changes 5+6 were added).
- Code SHA: `2b99a1b` — `feat(iter-v3/115): coherent horizon-exit labeling (fixed_horizon label + non-binding barriers) + revert /114 kill-switch` — `label_mode="fixed_horizon"`, `atr_tp_multiplier=atr_sl_multiplier=100.0`, the 2 `label_mode` accretion-guard sites, the /114 LDO kill-switch revert (`regime_gate_symbols=()`, `enable_ldo_realvol_gate=False`) + the pre-flight guard asserting the reverted /059-canonical state, `ITERATION_LABEL` "v3-115", the `test_fixed_horizon_label_mode.py` adversarial integration test. This is the code-state the backtest ran against. (Two cosmetic stale-string defects noted by the Critic — the `run_baseline_v3.py:2889` banner-print and the `v3-113-` `MODEL_SPECS` prefix — are NOT verdict-affecting and are flagged for the iter-v3/116 setup commit, Lesson 2.)
- Engineering report SHA: `f2b0099` — `briefs-v3/iteration_v3-115/engineering_report.md` (0.69h backtest; `comparison.csv` IS +0.3172 / OOS +0.9370). **Recorded report defect (Critic Rec 3):** the "Per-Symbol OOS Attribution" table lists `pct_of_total_pnl` values under a header labeling them as concentration; this diary attributes the result on the correctly-named `concentration_pct` from `comparison.csv` per Critic Rec 3.
- Critic review SHA: `08f4002` — `briefs-v3/iteration_v3-115/review.md` (OVERALL: EXPLORATION-NEGATIVE; single round, zero clarifications requested; all 8 checks PASS for the verdict — Check 1 PASS look-ahead clean, Check 8 PASS provenance clean).
- Reports: `reports-v3/iteration_v3-115/` — `comparison.csv` IS +0.3172 / OOS +0.9370, 53 OOS trades / 105 IS trades.
- Catalog update: `briefs-v3/exploration_catalog.md` /115 row (classification "EXPLORATION-NEGATIVE — coherent horizon-exit labeling; IS-collapse/OOS-spike regime artifact; label-estimand axis closed (/072→/105→/115)").
- BASELINE_V3.md: documentation-only banner update (the "Last updated:" line) — `v0.v3-059` UNCHANGED as canonical; an EXPLORATION never changes baseline metrics.
- Diary SHA: this closeout — `docs(iter-v3/115): diary entry`.
- **Tag**: `v0.v3-115` — a closeout marker only, tagged by the orchestrator (the `v0.v3-082`…`v0.v3-114` pattern).

iter-v3/115 is cycle-6 EXPLORATION slot #6. It tested the cycle-6 STRUCTURAL labeling axis — a coherent horizon-exit labeling architecture (`fixed_horizon` directional label PAIRED with a non-binding-barrier fixed-21-candle time exit) — and is classified **EXPLORATION-NEGATIVE**. The IS leg collapsed to **+0.3172** (Δ −0.52 vs the /060 anchor +0.8325 — the pre-registered Section 8 Criterion-1 NEGATIVE falsifier); the EDA's T3 ORACLE counterfactual had predicted exactly this geometry penalty (horizon-exit execution monthly Sharpe worse on all 3 symbols, Δ −0.37 to −1.11) and the production backtest confirmed it. The **+0.9370 OOS** headline is NOT a discovered edge — it is the pre-registered Section 7 Mode 2 IS-collapse/OOS-spike regime artifact: the OOS book is genuinely mixed (5 of 14 months negative; `out_of_sample/monthly_pnl.csv`), the +0.94 carried by 2 regime-favorable uptrend months (2025-05 +20.72%, 2025-08 +24.74%), with DSR 0.0 confirming no statistical significance at the EXPLORATION budget. Per-symbol OOS on the correctly-named `concentration_pct` (Critic Rec 3 — the engineering report's table mislabeled `pct_of_total_pnl`): LDOUSDT +71.32% / 54.5% WR and BCHUSDT +36.71% / 66.7% WR both OOS-positive, TRXUSDT −8.02% / 41.7% WR flat-to-slight-drag — but the IS leg already locked NEGATIVE (IS BCH −53.10%, IS TRX −20.89%, the IS collapse broad-based across 2 of 3 symbols). The Phase-7.5 Critic returned **OVERALL: EXPLORATION-NEGATIVE** (single round, zero clarifications) with **all 8 checks PASS for the verdict** — Check 1 PASS (the IS-collapse/OOS-spike is genuine regime exposure, audited leak-free at three surfaces — label construction, the 22-candle train/test embargo, the genuinely-mixed OOS book), Check 8 PASS (**provenance clean — the iter-v3/114 false-provenance failure mode did NOT recur; the new `feedback_v3_brief_parameter_provenance.md` discipline held on its first application** — the brief cites one design parameter, declares it hand-chosen, laundered nothing as a sweep output). The QR's NO-GO EDA and Section-7 prediction were well-calibrated — the modal failure mode fired and the modal IS point estimate (+0.30) landed ~1pp from the realized IS (+0.3172). **The coherent horizon-exit labeling axis is CLOSED** — the 2nd falsification of a label-estimand change in the /072→/105→/115 lineage; the asymmetric +2/−1 ATR triple-barrier is itself a Sharpe-generating device that a symmetric time exit discards, and the v3 binding constraint sits in the trade-construction layer, not the label. **`label_mode="fixed_horizon"` must NOT be re-bundled at iter-v3/120** (Critic Rec 1). Lessons: (1) the coherent horizon-exit labeling axis CLOSED — label-estimand family twice-falsified, the asymmetric barrier identified as the Sharpe device; (2) the `run_baseline_v3.py:2889` banner-print and `v3-113-` `MODEL_SPECS` stale strings need cleanup at the iter-v3/116 setup commit (Critic Rec 2 — cosmetic, makes `run.log` self-contradictory); (3) the brief-parameter-provenance discipline held on its first application — a process win recorded honestly; (4) a more-feature-predictable label does not imply a better trade book (the /105 lesson re-confirmed — the decisive EDA gate must be the trade-book counterfactual, not the IC test). iter-v3/116 advances to a TRADE-CONSTRUCTION / EXIT-LAYER axis — recommended: an asymmetric, regime-conditioned exit-barrier architecture (keep the `triple_barrier` label, make the TP/SL ATR multipliers a function of the entry-bar Hurst/ADX/realvol-z regime state) — the structural lever the /115 T3 finding directly points at; the QR to back it with a committed IS-only EDA, with a regime-conditioned vol-target ceiling as the documented NO-GO fallback. The cycle has 4 EXPLORATION slots left (116–119) before the mandatory iter-v3/120 CONFIRMATION.

**NO CHEATING.** `OOS_CUTOFF_DATE = 2025-03-24` and `training_months = 24` untouched. The walk-forward embargo fix (`e149e9d`, 22 candles, `train_end_ms = test_start_ms − embargo_ms`) is inherited unchanged (Critic Check 2 PASS — required cross-cell purge gap 66 = (21+1)×3, runner independently recomputed and matched; the embargo is correctly sized for the 21-candle `fixed_horizon` horizon). This EXPLORATION does NOT update BASELINE_V3.md regardless of outcome — `v0.v3-115` is a closeout marker only; canonical baseline remains `v0.v3-059` (IS monthly Sharpe +1.0894 / OOS monthly Sharpe +0.5791). The IS window did NOT change — the as-run result is reported honestly; the IS collapse to +0.3172 is reported, not trimmed. **Critic Check 1 (look-ahead) PASS — the IS-collapse/OOS-spike is genuine regime exposure, not a leak: a look-ahead leak inflates the OOS book uniformly, and instead the OOS is genuinely mixed (5 of 14 months negative).** Per-symbol attribution uses one named metric — the correctly-named `concentration_pct` from `comparison.csv` per Critic Recommendation 3; the engineering report's mislabeled `pct_of_total_pnl` column is explicitly not used.
