# v3 Baseline (iter-v3/121 — CYCLE-6 CLOSURE EVENT: first BASELINE_V3.md update since 2026-05-13 — /116 no_confirm RULE-layer primitive merged as first cycle-6 strictly-accretive ingredient)

> **CONFIRMATION-MERGE — BASELINE_V3.md UPDATES /059 → /121** per cycle-7 BOOTSTRAP / cycle-6 closure event (Critic FINAL `6d1017d`, 2026-05-20). /116 no_confirm RULE-layer exit primitive (`enable_no_confirm_exit=True, no_confirm_trigger_atr=0.50, no_confirm_k_candles=4`) merged as first cycle-6 strictly-accretive ingredient — first BASELINE_V3.md update since 2026-05-13. IS monthly Sharpe **+1.3108** (Δ +0.2214 vs /059) AND OOS monthly Sharpe **+0.9682** (Δ +0.3891 vs /059) — BOTH-must-improve gate PASSES. 4/5 binding falsifiers PASS with material margin (F1 BOTH-must-improve both legs, F2 IS regime-cost floor +0.79 by margin +0.52, F3 hard methodology gates PBO 0.1278 / PSR 1.0 / frac_positive_paths 0.6444, F5 per-symbol cascade 3/3); F4 trade-rate floor 98 < 130 INFORMATIONAL per pre-committed Section 4 carry-forward. Single Critic round, zero clarifications, all 8 Critic checks PASS. Component B (/119 C6 `ret5d_signed_tbi`) PERMANENTLY DROPPED per /120 F3 binding + /121 attribution finding (Component B marginal multi-seed effect = IS −0.5815 / OOS +0.7264). The /120 all-time v3 OOS record (+1.6946) stands as a historical multi-mechanism interaction artifact, NOT a replicable single-primitive property. Tag `v0.v3-059` RETIRED as canonical; **`v0.v3-121` is the new canonical baseline**. Cycle-6 final scoreboard: 1 ingredient merged (/116 no_confirm RULE-layer) + 1 ingredient dropped (/119 C6 FEATURE-layer) + 0 new direct-edge signals across 10 EXPLORATIONs.

**Sibling to:** `BASELINE.md` (v1) and `BASELINE_V2.md` (v2). All three coexist.

**Last updated:** 2026-05-20 — **CYCLE 6 CLOSURE EVENT + FIRST BASELINE UPDATE SINCE 2026-05-13: iter-v3/121-METHODOLOGY CONFIRMATION-MERGE (Critic FINAL `6d1017d`) — BASELINE_V3.md UPDATES /059 → /121 with /116 no_confirm RULE-layer primitive merged as the first cycle-6 strictly-accretive ingredient.** IS monthly Sharpe **+1.3108** (Δ +0.2214 vs /059) AND OOS monthly Sharpe **+0.9682** (Δ +0.3891 vs /059) — BOTH-must-improve gate PASSES with material margin. 4/5 binding falsifiers PASS (F1+F2+F3+F5); F4 trade-rate 98<130 INFORMATIONAL. Tag `v0.v3-121` supersedes `v0.v3-059` as canonical. Cycle-6 final scoreboard: 1 ingredient merged (/116 no_confirm) + 1 ingredient dropped (/119 C6 per /120 F3 binding + /121 attribution: Component B marginal effect IS −0.58 / OOS +0.73, non-compoundable) + 0 new direct-edge signals across 10 EXPLORATIONs. The /120 all-time v3 OOS record (+1.6946) stands as a historical multi-mechanism interaction artifact, NOT replicable. **[Previous CYCLE 6 closure note retained below for reference]** — **CYCLE 6 CLOSES: iter-v3/120 CONFIRMATION-NO-MERGE per pre-committed F3+F4 binding falsifiers — /059 remains canonical pending iter-v3/121-METHODOLOGY outcome; /120 attempted TWO-COMPONENT bundle (NO-MERGE per F3+F4); BASELINE_V3.md UNCHANGED at `v0.v3-059`.** iter-v3/120 ran the FIRST multi-mechanism CONFIRMATION bundle in v3 history (/116 `no_confirm` RULE-layer PROMISING-MECHANICAL + /119 `ret5d_signed_tbi` FEATURE-layer PROMISING-FEATURE-MECHANICAL) at 10-seed unified ensemble (`--n-trials 35`, no `--exploration`, no `--seeds`; 3.14h wall-clock, within 6h CONFIRMATION cap). Headline: **IS monthly Sharpe +0.7293 / OOS monthly Sharpe +1.6946** vs /059 IS +1.0894 / OOS +0.5791 → **Δ IS −0.3601 / Δ OOS +1.1155 — all-time v3 OOS record, not merged**. Five pre-committed Section-4 BINDING falsifiers ran: F1 stacking-linearity PASS (OOS +1.6946 ≥ +1.0089 threshold by surplus +0.6857); F2 TRX-concentration PASS (TRX 18.79% of positive-symbol total, well under 50% flag); F3 sister-redistribution stability **FIRES** (conjunctive AND: `regime_momentum_signed_5d` portfolio importance 506.67 → 171.9 = **−66.1%** AND `ret5d_signed_tbi` portfolio share 128.6/4731.2 = **2.72% < 5%** — both legs of allocation-accounting gate hold cleanly with remarkable multi-seed numerical stability vs /119 single-seed); F4 IS regime-cost floor **FAILS** by 0.0607 (IS +0.7293 < +0.79 floor = /059 IS − 0.30; BCH IS net_pnl_pct collapsed −69.31 pct mechanism-confirmed — the no_confirm channel cut BCH trades that would have recovered to TP, exactly the regime-cost mechanism F4 was designed to catch); F5 per-symbol cascade PASS 3/3 (BCH Δ +24.87, LDO Δ +9.07, TRX Δ +7.99 on weighted_pnl). BOTH-must-improve gate INDEPENDENTLY confirms NO baseline update (IS Δ −0.3601 from /059). The QR's Round-2 Q3 Jaccard set-comparison on committed OOS trade rosters (bundle vs /116-alone Jaccard = 0.48, bundle vs /119-alone Jaccard = 0.55, 30 trades unique to bundle) confirms GENUINE multi-mechanism interaction effect (NOT cannibal-only) — but F3 is an ALLOCATION-ACCOUNTING gate, fires regardless of interaction-vs-cannibal mechanism diagnosis. Three Critic rounds + 1 QR-response; all 8 Critic checks PASS for the verdict (Check 1 Look-Ahead: no /120-specific code introduced, bundle = union of /116-audited + /119-audited components; Check 3 PBO=0.0957 PASS, PSR=1.0 PASS, frac_positive_paths=0.6444 PASS, DSR informational artifact per `feedback_v3_dsr_mode_artifact.md`; Check 4 PASS-CARVEOUT Category-2 algebraic-sister IC; Check 5 ADF PASS C6 all 3 symbols at IS-end; Check 6 Pareto PASS via frac_positive_paths Gate 10 unified architecture; Check 7 reproducibility PASS setup SHA `294ac0e`; Check 8 hypothesis-implementation alignment PASS). Cycle 6 closes formally: **10/10 EXPLORATIONs done (/110-/119), 1 CONFIRMATION done (/120), 2 mechanical primitives + 0 ingredients merged**. Per brief Section 8.4 first-match-wins decision tree: F3 mandates DROP Component B (revert to /116-only); F4 mandates NO-MERGE on IS-leg regardless of OOS; Component A standalone evaluation is the ONLY remaining unevaluated data point in the cycle-6 multi-seed grid. iter-v3/121-METHODOLOGY AUTHORIZED as **cycle-7 BOOTSTRAP** (analogous to iter-v3/018 BOOTSTRAP precedent; NOT counted toward cycle-7 10/10 EXPLORATION cadence). /121-METHODOLOGY spec: `--n-trials 35 --clean-oof` no `--exploration` no `--seeds` (default 10-seed unified ensemble); single change vs /059 is `enable_no_confirm_exit=False → True` (trigger_atr=0.50, k_candles=4 inherited from /116 grid optimum); `V3_FEATURE_COLUMNS_TOP_N` REVERTS to /059's 14-feature stack (C6 DROPPED per F3 pre-commitment, regardless of /121 outcome); 6h HARD CAP; estimated 3-4h. If /121-METHODOLOGY PASSES all hard methodology gates (PBO < 0.40, PSR > 0.95, frac_positive_paths ≥ 0.55) AND BOTH-must-improve on /059 AND F4 IS regime-cost floor ≥ +0.79 → BASELINE_V3.md updates with Component A only (PARTIAL-MERGE RULE-layer baseline; 1 strictly-accretive primitive merged at cycle 6 closure); else cycle-6 closes with 0 ingredients merged and /059 remains canonical. iter-v3/122 = cycle-7 EXPLORATION axis-1 from /119 diary §8.2 candidate menu (cross-asset/external feeds, longer-cadence labels, NEW model architecture, NEW symbol universe), QR-selected per `feedback_v3_axis_selection_quant_discipline.md`, regardless of /121-METHODOLOGY outcome. Cycle-7 standard 10/10 + 1 CONFIRMATION cadence resumes from /122. The /105 → /109 cycle-5 binding-constraint finding (binding constraint is DOWNSTREAM of label, in trade-construction / exit / risk layer) is EMPIRICALLY RECONFIRMED by cycle 6: 6 cycle-6 axes that re-walked or refined upstream/horizontal axes (universe, pooled, multi-freq, risk primitives, labeling estimand, candle frequency) all NEGATIVE; the 2 cycle-6 axes that targeted the downstream trade-construction/exit layer (/116 exit-layer + /119 feature-layer at the loss-surface) produced the 2 PROMISING outcomes; but the CONFIRMATION at /120 nevertheless failed the BOTH-must-improve gate. NEW memory rule: **PROMISING-FEATURE-MECHANICAL F3 validated at /120 multi-seed** — F3 (sister-redistribution conjunctive AND gate: sister-family redistribution > 50% AND new feature < 5% split-budget) FIRES cleanly and catches the FEATURE-MECHANICAL signature EVEN WHEN the bundle achieves all-time OOS record AND the interaction effect is genuine (Jaccard 0.48 with /116-alone; 30 trades unique to bundle); pre-committed F3 is the BINDING gate for future PROMISING-FEATURE-MECHANICAL candidates (catches the redistribution-catalyst-without-direct-edge mechanism regardless of OOS outcome). `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-120.md`. PRIOR: iter-v3/116 cycle-6 EXPLORATION slot #7 closeout (**EXPLORATION-PROMISING — PROMISING-MECHANICAL subclass per `feedback_promising_mechanical_subtype.md`; no_confirm early-exit primitive; regime-adaptive book composition + slot-freeing cascade; broad cross-symbol OOS lift; Critic UPGRADE-direction override of mechanical first-match-wins NEGATIVE; bundle at /120 CONFIRMATION as strictly accretive** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-116` a closeout marker only; edit is documentation-only — this banner line: iter-v3/116 tested the cycle-6 STRUCTURAL trade-construction-layer axis the iter-v3/115 closeout recommended (an EXIT-LAYER axis per `feedback_v3_structural_over_knob_exploration.md`) — but pivoted BOLDER than the dispatch's literal recommendation (a regime-conditioned exit-barrier architecture, Axis A): the QR's committed IS-only EDA (`analysis/iteration_v3-116/`, EDA SHA `52444c9`, 10 result tables) tested THREE distinct trade-construction-layer hypotheses head-to-head — Axis A returned NO-GO at the decisive `T3a` shuffle placebo (FAILED 5/6; the optimum collapsed to the (tp=1.0, sl=2.0) cell in 13 of 18 buckets, a known regime-exposed /065 pattern), Axis B (scaled-entry) returned formal NO-GO (0/81 positive cells), Axis C (early-exit-on-no-confirmation, a time-conditioned absence-of-excursion exit primitive) was chosen on the residual-uncertainty criterion (the only axis with positive IS lift signal at the chosen cell). The chosen cell (`trigger_atr=0.50`, `k_candles=4`) DECLARED hand-chosen per `feedback_v3_brief_parameter_provenance.md`; auditable temporal fence (EDA committed BEFORE brief in commit `52444c9`); provenance discipline held for the second consecutive iteration after /115. The Axis C EDA also returned a NO-GO on the pre-registered g2 mechanism gate (`T8`: in all 48 cells across all 3 symbols the rule cuts modestly-below-average WINNERS at barrier, not losers). Per THE PRIME DIRECTIVE the NO-GO set the brief's modal prediction (~55% INERT/NEGATIVE) and the iteration ran the backtest. 3-seed EXPLORATION mode (`--exploration`, `ENSEMBLE_SIZE=3`, seeds 191664963/1662057957/1405681631 all outer=42 lineage, `--n-trials 35`); code SHA `d786104`; 0.70h backtest. ONE substantive axis changed (one new optional exit primitive in `backtest.py` controlled by two hand-chosen scalars); the 14 `V3_FEATURE_COLUMNS`, the BCH/LDO/TRX universe, the per-symbol architecture, the 7-gate RiskV2 stack, the data window, the embargo, `REQUIRED_GAP=66` all /059-identical; the /115 stale-knob revert (`label_mode="triple_barrier"`, `atr_*_multiplier=2.0/1.0`) verified at three independent surfaces. The iter-v3/115 closeout's Critic-Rec-2 cosmetic cleanup is COMPLETE (banner-print `V3_FEATURE_COLUMNS=22` → `=14`, `MODEL_SPECS` prefix `v3-113-` → `v3-116-`). The Phase-6 backtest produced **IS monthly Sharpe +0.6246 / OOS monthly Sharpe +1.1089, 107 OOS trades / 161 IS trades** (per-symbol OOS on the stable `net_pnl_pct` metric — ALL 3 symbols positive Δ vs /060, the broad-based distribution that defines the PROMISING-MECHANICAL subtype: BCHUSDT +22.86%/40.5% WR on 37 trades Δ +31.55, TRXUSDT +46.80%/52.6% WR on 57 trades Δ +16.04 the OOS carrier, LDOUSDT −13.08%/23.1% WR on 13 trades Δ +12.00 meaningfully less drag; trade rosters NOT bit-identical to /060 — BCH 35/37 (95%) + 3 changed exits + 2 new, LDO 11/13 (85%) + 0 changed + 2 new, TRX 50/57 (88%) + 6 changed + 6 new — structurally distinct from /114's frozen-baseline attribution artifact; 11 new OOS entries +3.67% mean PnL traceable bar-by-bar to the slot-freeing cascade — October 2025 attribution +11.75% swing /060 vs /116). The Phase-7.5 Critic returned **OVERALL: EXPLORATION-PROMISING — subclass PROMISING-MECHANICAL** per `feedback_promising_mechanical_subtype.md` (single round, zero clarifications requested, Round 2 skipped). The brief's pre-registered Section-8 first-match-wins fired Criterion 1a NEGATIVE on the IS leg (IS +0.6246 < +0.7325 = /060 anchor − 0.10); the **Critic exercised authority to OVERRIDE the mechanical first-match-wins NEGATIVE in the UPGRADE direction** — precedent /114 was the DOWNGRADE direction; iter-v3/116 establishes the UPGRADE-direction precedent (the Critic-override authority is SYMMETRIC). The override evidence converged on three independent lines: **(a) the IS drag has a coherent regime-cost mechanism, NOT IS-deterioration-without-mechanism** (the 37-month IS window includes 2022 bear + 2023 chop where an absence-of-excursion exit systematically truncates slow-start trades that would have recovered to TP after delayed confirmation — documented expected cost — and the 14-month OOS is a 2025-2026 uptrend where the slot-freeing cascade dominates; per user 2026-05-20: IS-having-more-data-and-more-bear-regimes-than-OOS naturally produces IS Sharpe below OOS for a regime-adaptive rule); **(b) the OOS lift is broadly distributed and mechanistically traceable** (ALL 3 symbols positive Δ vs /060 on `net_pnl_pct`; trade rosters NOT bit-identical to /060; the 11 new OOS entries traceable bar-by-bar to the slot-freeing cascade — structurally distinct from /114's frozen-baseline attribution artifact); **(c) the mechanism is mechanical book-composition (slot-freeing), NOT a new edge ingredient** (strictly-accretive on /059, non-compoundable across iterations). **All 8 Critic checks PASS for the verdict.** Check 1 (Look-Ahead Audit) PASS verdict-scoring (the no_confirm primitive operates entirely on past-only OHLCV; audited line-by-line in `backtest.py:251-287`; the label estimand `labeling.py` UNCHANGED — `grep no_confirm labeling.py` returns zero matches; the `e149e9d` walk-forward lookahead-bias fix is in effect; three trade-level spot checks reproduce bit-exactly). Check 8 (Hypothesis-Implementation Alignment) PASS verdict-scoring (the brief's 8 changes are all present and clean in code; the /115 stale-knob revert is COMPLETE at three independent surfaces — model builder, accretion guard, pre-flight assertion all carry `label_mode="triple_barrier"` and `atr_*_multiplier=2.0/1.0`; no scope creep). Check 2 embargo PASS (`REQUIRED_GAP=66`=(21+1)×3, runner independently recomputed and matched), Check 3 multiple-testing PASS informational-only (PBO=0.1278 PASS, PSR=1.0 PASS, DSR_relative=1.0 PASS, frac_positive_paths=0.6444 PASS, DSR=0.0 EXPLORATION-budget structural artifact per `feedback_v3_dsr_mode_artifact.md`), Check 4 IC PASS non-applicable (zero new feature families), Check 5 ADF PASS (all 42 feature-symbol pairs stationary at p<0.05 at IS-end month 2025-03), Check 6 Pareto PASS non-applicable (EXPLORATION mode), Check 7 reproducibility PASS (code SHA `d786104`, explicit 14-element `feature_columns`, three trade-level spot checks reproduce bit-exactly). OOS/IS ratio 1.78 sits squarely in the healthy [0.5, 3.0] band — well below the 3.0 SUSPICIOUS-OOS-DOMINANT gate; the result does NOT fall in the /065/073/076/078 taxonomy. Pre-registered Section 7 modal outcome was INERT/EXPLORATION-NEGATIVE (~55%) — actual is PROMISING-MECHANICAL — a **positive prediction miss** (the prediction missed BETTER than expected, not worse): the EDA's static-direction counterfactual structurally could not model the slot-freed-entries cascade channel that turned out to be production-dominant. The held-to-barrier g2 prediction was re-confirmed in production at the held-to-barrier counterfactual (the rule does cut modestly-below-average winners at barrier; the production no_confirm exit PnLs are −0.84% IS / −0.80% OOS at candle-4 close vs the EDA's +3.26 to +3.75% held-to-barrier counterfactual); the production-dominant mechanism (slot-freeing) is a separate channel — both are true, no contradiction. **This is cycle 6's FIRST PROMISING after 6 consecutive NEGATIVEs (/110-/115).** Per the PROMISING-MECHANICAL subtype protocol the no_confirm primitive is a **strictly-accretive component decision on /059** — it advances to the iter-v3/120 CONFIRMATION as a YES/NO-keep accretion decision on top of /059 (NOT a new edge ingredient; cannot compound with other exit primitives or labeling axes — only ONE "rule on already-open trades" can be active). Critic Recommendations: (1) bundle no_confirm as strictly-accretive on /059, NOT a new edge ingredient; (2) pre-register an IS Sharpe band that accommodates the regime-cost (CONFIRMATION brief Section 4 must pre-register an IS band that does NOT fire NEGATIVE on the IS leg by accident — e.g. IS NEGATIVE floor at /059 IS − 0.50 = +0.59 rather than the standard /059 IS − 0.10 = +0.99); (3) audit the cascade attribution at multi-seed — pre-register the falsifier: "if at 10-seed the per-symbol OOS `net_pnl_pct` Δ vs /059 is not broad-based positive across ≥ 2 of 3 symbols, the EXPLORATION OOS lift was a thin-seed cascade artifact and the axis is RECLASSIFIED PROMISING-FALSIFIED-AT-CONFIRMATION." Seven lessons: (a) IS-vs-OOS-Sharpe asymmetry can be regime-composition-explicable for a regime-adaptive rule (per user 2026-05-20); (b) the Critic-override authority is SYMMETRIC — DOWNGRADE (/114) and UPGRADE (/116) precedents established; (c) PROMISING-MECHANICAL is a strictly-accretive component decision — non-compoundable across iterations; (d) the slot-freeing cascade is a production mechanism the EDA's static-direction counterfactual structurally cannot model — future EDAs on rule-cuts-trades-early axes MUST include slot-cascade modelling; (e)-(g) the three Critic Recommendations for /120. **iter-v3/117 advances to the CANDLE-FREQUENCY axis per user directive 2026-05-20 (`feedback_v3_candle_frequency_unblocked.md`)** — the 8h-only constraint is LIFTED from /117 onward; the QR may pick any frequency. Recommended: 24h base with 3-offset multi-offset derived-series (offsets 0h/8h/16h UTC). Reasoning: the /109 permutation null was explicitly scoped to 8h; cycle 6's six 8h NEGATIVEs (/110-/115) empirically re-confirmed the binding constraint at 8h; the 24h frequency reduces fee drag and dampens HFT noise (the user's 2026-05-20 reasoning) while the 3-offset derivation MULTIPLIES effective training data 3×; the /113 standalone 8 daily features clearing their permutation null at AUC 0.5275 (p=0.00) is positive evidence the daily representation carries signal the 8h cannot see; the axis is structurally distinct from every cycle-6 dead path AND is the one structural lever the QR has never had access to. Documented NO-GO fallbacks: 12h-2-offset or weekly-8-offset (the user's 2026-05-20 directive explicitly named weekly as a candidate). Locked constraints: BCH/LDO/TRX universe, LightGBM, <2h wall-clock. The cycle has 3 EXPLORATION slots left (117–119) before the mandatory iter-v3/120 CONFIRMATION. `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-116.md`. PRIOR: iter-v3/115 cycle-6 EXPLORATION slot #6 closeout (**EXPLORATION-NEGATIVE — coherent horizon-exit labeling; IS-collapse/OOS-spike regime artifact; the label-estimand axis CLOSED (/072→/105→/115)** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-115` a closeout marker only; edit is documentation-only — this banner line: iter-v3/115 tested a STRUCTURAL labeling-architecture axis opened OUTSIDE the now-spent four-item cycle-6 menu (per `feedback_v3_structural_over_knob_exploration.md` — NEW labeling outranks NEW risk primitive and gate knobs): a coherent horizon-exit labeling architecture. The v3-canonical triple-barrier labeling architecture (+2/−1 ATR price-barrier label executed through TP/SL/timeout barriers) was replaced by a coherent horizon-exit architecture — `label_mode` `triple_barrier` → `fixed_horizon` (the directional label becomes `sign` of the realized 21-candle forward net return) PAIRED with the inseparable horizon-coherent execution change `atr_tp_multiplier`/`atr_sl_multiplier` `2.0`/`1.0` → `100.0`/`100.0` (TP/SL barriers made non-binding so every trade exits at the fixed 21-candle timeout). Horizon N=21 candles FIXED at the v3 triple-barrier timeout — declared hand-chosen for embargo-invariance, NOT tuned. The QR backed the axis with a committed IS-only EDA (`analysis/iteration_v3-115/`, EDA SHA `d871b22`, 6 tables) that returned a hard NO-GO — the pre-registered GO rule `GO iff g1 AND g2 AND g3`: g1 label-validity PASS, g2 execution-coherence PASS, g3 trade-book-gain FAIL 0/3 (the decisive T3 ORACLE counterfactual held the triple-barrier label direction FIXED and compared the identical direction sequence under triple-barrier vs horizon-exit execution: horizon-exit execution monthly Sharpe WORSE on all 3 symbols, Δ −1.11 BCH / −0.80 LDO / −0.37 TRX). Per THE PRIME DIRECTIVE the NO-GO set the brief's modal prediction; the iteration ran the backtest. The Phase-6 backtest (3-seed EXPLORATION, code SHA `2b99a1b`, 0.69h) produced **IS monthly Sharpe +0.3172 / OOS monthly Sharpe +0.9370**, **53 OOS trades / 105 IS trades** (per-symbol OOS `concentration_pct` — the correctly-named bounded metric per Critic Rec 3; the engineering report's "Per-Symbol OOS Attribution" table mislabeled `pct_of_total_pnl` values under a concentration header and that mislabeled column is NOT used: LDOUSDT +71.32%/54.5% WR the OOS book's largest contributor OOS-positive, BCHUSDT +36.71%/66.7% WR OOS-positive, TRXUSDT −8.02%/41.7% WR flat-to-slight-drag). The Phase-7.5 Critic (single round, zero clarifications requested) returned **OVERALL: EXPLORATION-NEGATIVE** — the verdict follows mechanically from the pre-registered Section 8 first-match-wins taxonomy: Criterion 1 fires on the IS leg (IS +0.3172 < the +0.7325 NEGATIVE floor; Δ −0.5153 vs the /060 anchor +0.8325). **All 8 checks PASS for the verdict.** **Check 1 (Look-Ahead Audit) PASS — the make-or-break check** — the `fixed_horizon` label is forward-looking by construction (correct and intentional); the audit reduces to whether the 21-candle forward label window is embargo-purged at the train/test boundary, verified at three surfaces (label construction `labeling.py:349-423`; train/test embargo `walk_forward.py:113` `train_end_ms = test_start_ms − embargo_ms`, embargo = 22 candles confirmed at the IS/OOS boundary; **the OOS +0.94 is GENUINE regime exposure not a leak** — a leak inflates OOS PnL uniformly, instead `out_of_sample/monthly_pnl.csv` shows a genuinely MIXED OOS book, 5 of 14 months negative, the +0.94 carried by 2 regime-favorable uptrend months 2025-05/2025-08). The IS-collapse/OOS-spike is the pre-registered Section 7 Mode 2 regime artifact (the /105 signature), NOT a leaked-future artifact; DSR 0.0 confirms no statistical significance at the EXPLORATION budget. **Check 8 (Hypothesis-Implementation Alignment) PASS — and it records a PROCESS WIN**: every brief change maps to code, no scope creep; both halves of the axis genuinely ran (`out_of_sample/trades.csv` 52/53 `timeout` + 1 `end_of_data`, `in_sample/trades.csv` 105/105 `timeout`, ZERO barrier exits either side); the /114 LDO kill-switch reverted clean (`regime_gate_symbols=()`, `enable_ldo_realvol_gate=False`, pre-flight-guarded — no co-varying confound, the iter-v3/110 stale-knob failure mode averted); **provenance is clean — the iter-v3/114 false-provenance failure mode did NOT recur** — iter-v3/115 was the first iteration after /114's Check-1 FAIL established `feedback_v3_brief_parameter_provenance.md`, and the /115 brief cites exactly ONE design parameter (horizon N=21), DECLARES it hand-chosen, justifies it by embargo-invariance, launders nothing as a sweep output (the Critic confirmed at source: "There is no IS-tuned scalar, no sweep, no threshold laundered as an optimization output, no false-provenance claim … Provenance is clean") — the new discipline held on its very first test. The structural finding: **the coherent horizon-exit labeling axis does not transfer** — the EDA's T3 ORACLE counterfactual measured the geometry penalty before the backtest (horizon-exit execution monthly Sharpe Δ −0.37 to −1.11 on identical direction calls) and the production IS collapsed to +0.3172; IS MaxDD blew out to 48.12% (+17.15pp vs /059 IS 30.97% — the pre-registered "MaxDD up, longer holds" signature); the IS book is net-negative on 2 of 3 symbols (IS BCH net_pnl_pct −53.10%, IS TRX −20.89%) — the IS collapse is broad-based. The mechanism, now positively identified: the asymmetric +2/−1 ATR triple-barrier — a tight stop that cuts losers, a wide target that books winners — is itself a Sharpe-generating device, and ANY symmetric time-exit estimand discards that asymmetry. Pre-registered Section 7 modes: Mode 1 (the geometry-does-not-transfer modal mode, ~60%) FIRED — and the modal IS point estimate +0.30 landed ~1pp from the realized +0.3172; Mode 2 (IS-collapse/OOS-spike SUSPICIOUS divergence, the /105 signature, ~25%) FIRED in substance (the OOS/IS ratio 2.9543 is just under the formal 3.0 gate, and Criterion 1 fires first under first-match-wins anyway); Mode 3 (behavioral inertia) did NOT fire; the ~5% residual-upside outcome did NOT occur — the QR's NO-GO EDA and Section-7 prediction were well-calibrated. Other Critic checks: Check 2 embargo PASS (`REQUIRED_GAP=66`=(21+1)×3, runner independently recomputed and matched; the 22-candle embargo correctly sized for the 21-candle `fixed_horizon` horizon; `e149e9d` walk-forward inherited unchanged), Check 3 multiple-testing PASS informational-only (PBO=0.1426 < 0.4 passes; `dsr_relative=0.9593` an EXPLORATION-mode structural artifact per `feedback_v3_dsr_mode_artifact.md`), Check 4 IC PASS non-applicable (zero feature families added; `V3_FEATURE_COLUMNS` unchanged at 14), Check 5 ADF PASS (all 42 feature-symbol pairs stationary at p<0.05 at the IS-window-end month 2025-03; features unchanged from /059), Check 6 Pareto PASS (EXPLORATION mode under unified architecture — `pareto_front.csv` not produced; deferred to /120), Check 7 reproducibility PASS (code SHA `2b99a1b`; explicit 14-element `feature_columns`; OOS trades PnL-spot-checked clean). Two cosmetic stale-string defects noted (NOT verdict-affecting, NOT brief violations — Critic Rec 2): `run_baseline_v3.py:2889` banner-print still reads `label_mode=triple_barrier; V3_FEATURE_COLUMNS=22` (makes `run.log` self-contradictory); `MODEL_SPECS` lines 197–199 still carry the `v3-113-` prefix — both flagged for cleanup at the iter-v3/116 setup commit. **The coherent horizon-exit labeling axis is CLOSED** — the 2nd falsification of a label-estimand change in the /072→/105→/115 lineage (/072 fixed-horizon-label-only incoherent-execution NEGATIVE, /105 trend-scanning-label incoherent-execution FALSIFIED, /115 fixed-horizon-label + COHERENT execution — the strongest form of the estimand-change hypothesis — NEGATIVE); the root cause is structural and now established (the asymmetric +2/−1 ATR triple-barrier is itself a Sharpe-generating device, and a label-estimand change does not address the v3 binding constraint, which the /105→/109 chain localized DOWNSTREAM of the label in the trade-construction / exit / risk layer). **`label_mode="fixed_horizon"` must NOT be re-bundled at the iter-v3/120 CONFIRMATION** (Critic Rec 1) — the CONFIRMATION carries the /059-canonical `triple_barrier` label. Three lessons for the dead-paths catalog: (a) the coherent horizon-exit labeling axis CLOSED — the label-estimand family twice-falsified, the asymmetric barrier identified as the Sharpe device; (b) the `run_baseline_v3.py:2889` banner-print and `v3-113-` `MODEL_SPECS` stale strings need cleanup at the iter-v3/116 setup commit; (c) the brief-parameter-provenance discipline held on its first application — a process win recorded honestly; and a fourth re-confirmation — a more-feature-predictable label does not imply a better trade book (the /105 lesson; the /115 EDA T4 showed the horizon-exit label ~1.5× more learnable yet the IS book still collapsed — the decisive EDA gate must be the trade-book counterfactual T3, never the IC test T4). iter-v3/116 advances to a TRADE-CONSTRUCTION / EXIT-LAYER axis per `feedback_v3_structural_over_knob_exploration.md` and the user's out-of-the-box mandate — recommended: an asymmetric, regime-conditioned exit-barrier architecture (keep the /059-canonical `triple_barrier` label, make the TP/SL ATR multipliers a function of the entry-bar Hurst/ADX/realvol-z regime state instead of the static 2.0/1.0) — the structural lever the /115 T3 finding directly points at; the QR to back it with a committed IS-only EDA, with a regime-conditioned vol-target ceiling as the documented NO-GO fallback. The cycle-6 axis menu is now fully spent (universe ×2 at /110/111, model architecture ×1 at /112, multi-frequency ×1 at /113, risk management ×1 at /114 — all NEGATIVE) AND the labeling axis opened outside it (/115 NEGATIVE); the cycle has 4 EXPLORATION slots left (116–119) before the mandatory iter-v3/120 CONFIRMATION. `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-115.md`. PRIOR: iter-v3/114 cycle-6 EXPLORATION slot #5 closeout (**EXPLORATION-NEGATIVE — with an explicit Check-1 (no-cheating) FAIL; LDO regime-conditional kill-switch; RiskV3 primitive 9 CLOSED** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-114` a closeout marker only; edit is documentation-only — this banner line: iter-v3/114 tested cycle-6 axis-menu item 4 — risk management: a per-symbol LDO regime-conditional kill-switch. RiskV3 primitive 9 was re-targeted from TRX to LDO with a NEW kill_LOW-polarity `ldo_realvol_zscore` trigger — when `abs(LDO-realized-vol-zscore) < 0.30` (a low-volatility-chop regime), LDO position-taking is suppressed (`enable_ldo_realvol_gate=True`, `regime_gate_symbols=("LDOUSDT",)`, `ldo_realvol_zscore_floor=0.30`, `ldo_realvol_lookback_bars=90`). The QR backed the axis with a committed IS-only EDA (`analysis/iteration_v3-114/`, EDA SHA `d8a9725`, 19 tables) that returned a SHARPENED-GO (the trigger separated LDO losers cleanly on the 9-trade IS roster, `std_gap` −0.74, but the per-bar OOS median-split polarity sign-flipped — the EDA's own `S2_honest_classification.csv` caveated the predicted OOS effect to `[−0.05, +0.25]`). The Phase-6 backtest (3-seed EXPLORATION, code SHA `451f897`, 0.71h) produced **IS monthly Sharpe +0.9747 / OOS monthly Sharpe +0.7991**, **97 OOS trades** (per-symbol OOS `concentration_pct` — one named, bounded, stable metric per the QR's Round-2 commitment, NOT the near-zero-denominator `pct_of_total_pnl`: TRXUSDT +95.61%/49.1% WR the entire OOS book, BCHUSDT +7.34%/32.4% WR near-flat, LDOUSDT −2.95%/20.0% WR the kill-switch's target with a net-negative residual). The Phase-7.5 Critic (two rounds; the QR's Round-2 response `qr_response.md` is a full concession) returned **OVERALL: EXPLORATION-NEGATIVE — Check 1 FAIL + Check 8 FAIL, both verdict-driving**. **Check 1 (Look-Ahead Audit) FAIL** — the `ldo_realvol_zscore` trigger *series* is past-only and clean (`.shift(1)` before the rolling std; strict `searchsorted(side="left")-1` past-only as-of contract, verified at source), but the leak surface is the threshold `0.30`: it is a hardcoded constant (`trigger_selection_synthesis.py:323` literal `chosen_thr = 0.30`), NOT an optimization output; the brief's "T3 IS-only sweep" provenance (Sections 0, 2.4, the `risk_v2.py:269` code comment) is factually FALSE — T3 sweeps the opposite `kill_HIGH` polarity at thresholds 1.12–1.75 and never produces 0.30; 0.30 enters the S3 sweep table ONLY because `main()` force-injects it via `extra_thr=0.30`; the OOS-roster trigger values were computed in the same atomic commit `d8a9725` with no committed ordering fencing the threshold from the OOS-window scripts; and 0.30 was selected over strictly-IS-better wider thresholds in `[0.3331, 0.5240]`. There is no auditable IS-only temporal fence; per `feedback_no_cheating.md` a hand-tuned scalar threshold whose IS-only provenance cannot be demonstrated is a look-ahead FAIL — the QR conceded. **Check 8 (Hypothesis-Implementation Alignment) FAIL** — the brief registered a "surgical ~13% fire-rate" low-vol-chop gate (and used surgicality as the explicit reason `ldo_realvol_zscore` was admitted over `ldo_vs_btc_30d`, S4-disqualified for a 46–79% fire-rate); the production gate fired 842 times on LDO against `signals_seen=595` — ~59% of the LDO candidate-candle space, inside the 46–79% regime S4 used to disqualify the alternative; the "13%" was never computed in any of the 19 committed EDA tables — it appears only as hardcoded comment strings; the hypothesis as registered ("a surgical chop-gate that fixes LDO") is not what ran ("broadly turn LDO off"), and the gate did not fix LDO (the 5 surviving LDO OOS trades are net-negative). The +0.80 OOS headline is a CONCEDED thin-roster aggregate artifact — TRX OOS (the +95.61% carrier) bit-identical to /060 (54/55 trades; the 55th a pure data-extent artifact), the surviving LDO OOS book net-negative (−7.39%), so the aggregate Sharpe move is arithmetic re-weighting around an unchanged book, not a discovered edge; it is +0.35 above the brief's pre-registered 80% interval `[−0.10, +0.45]` and 3× the EDA's own band. The engineering report's mechanical Section-8 classification (EXPLORATION-PROMISING — all six pre-registered PROMISING criteria mechanically passed) was OVERRIDDEN by the Critic: a pre-registered-criteria PASS computed on top of a Check-1 no-cheating FAIL and a Check-8 mechanism mismatch cannot stand as PROMISING. Pre-registered Section 7 modal outcome was "INERT-to-mildly-negative" — the honest call about the gate's true effect (the +0.80 was an artifact, not a real upside). Other Critic checks: Check 2 embargo PASS (`REQUIRED_GAP=66`=(21+1)×3, runner independently recomputed and matched; `e149e9d` walk-forward inherited unchanged), Check 3 multiple-testing FAIL informational-only (DSR=0.0/PSR=1.0 EXPLORATION-budget regime artifacts per `feedback_v3_dsr_mode_artifact.md`; PBO=0.1278 passes), Check 4 IC PASS non-applicable (zero feature families added; `V3_FEATURE_COLUMNS` reverts 22→14 — mandatory /113-closeout housekeeping), Check 5 ADF PASS (all 42 feature-symbol pairs stationary at p≈0 at the IS-window-end month 2025-03), Check 6 Pareto PASS (EXPLORATION mode under unified architecture — `pareto_front.csv` not produced; deferred to /120), Check 7 reproducibility PASS (code SHA `451f897`; explicit 14-element `feature_columns`; 5 OOS LDO trades PnL-spot-checked clean — reproducibility and correctness are orthogonal). **RiskV3 primitive 9 (the regime kill-switch) is CLOSED** — NEGATIVE/INERT across three data points (iter-v3/022 TRX, iter-v3/074 TRX, iter-v3/114 LDO) spanning two symbols and two trigger polarities; not re-opened absent genuinely new, properly-fenced IS-only counterfactual evidence. Three lessons for the dead-paths catalog: (a) **brief-parameter-provenance discipline (Critic Rec 2)** — every tuned scalar parameter cited in a brief must name the exact committed table that produced it, and that source must actually produce the value when run; a hand-chosen threshold must be DECLARED hand-chosen (not laundered as a sweep output) and IS-calibrated with an auditable temporal fence (the IS-only selection rule + selecting table committed in a commit that demonstrably runs BEFORE any script touches an OOS-window file; future risk-axis EDAs must SPLIT — IS-only selection committed first, the FENCED OOS coverage annex in a separate later commit); (b) RiskV3 primitive 9 is CLOSED; (c) a per-symbol axis must be attributed on the TARGET symbol's own roster, never the portfolio aggregate Sharpe — the single-lineage frozen-baseline pattern (`feedback_v3_single_seed_frozen_baseline.md`) makes the aggregate OOS Sharpe delta an artifact of the unchanged non-target symbols' arithmetic. iter-v3/115 advances to a STRUCTURAL axis per `feedback_v3_structural_over_knob_exploration.md` — the kill-switch / risk-primitive-knob family abandoned — recommended: a NEW labeling architecture (triple-barrier + meta-labeling secondary filter per López de Prado AFML Ch. 3, or a directional fixed-horizon return label), the QR to adjudicate with a committed IS-only EDA, applying the Lesson-(a) provenance discipline to every scalar parameter the labeling EDA selects. The cycle-6 axis menu is now fully spent (universe ×2 at /110/111, model architecture ×1 at /112, multi-frequency ×1 at /113, risk management ×1 at /114 — all NEGATIVE); the cycle has 5 EXPLORATION slots left (115–119) before the mandatory iter-v3/120 CONFIRMATION. `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-114.md`. PRIOR: iter-v3/113 cycle-6 EXPLORATION slot #4 closeout (**EXPLORATION-NEGATIVE — multi-frequency daily features behaviorally inert + IS-collapsing; axis CLOSED** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-113` a closeout marker only; edit is documentation-only — this banner line: iter-v3/113 tested cycle-6 axis-menu item 3 — multi-frequency feature engineering: 8 coarser-frequency 1-day-bar features (`d_ret_5d`, `d_ret_10d`, `d_trend_slope_10`, `d_realvol_10`, `d_realvol_ratio`, `d_atr_pctrank_60`, `d_efficiency_10`, `d_close_pos_20`), each aggregated look-ahead-free from the existing 8h candles and causally as-of-joined onto the 8h decision grid by a backward `merge_asof` keyed `day_close ≤ open_time`, added to the /059-canonical 14-feature 8h stack (`V3_FEATURE_COLUMNS` 14 → 22). No new data fetched. The QR backed the axis with a committed IS-only EDA (`analysis/iteration_v3-113/`, EDA SHA `ebd84e2`, 12 tables) that returned a SHARPENED-GO — T5 (the 8 daily features ALONE) cleared its no-signal permutation q95 decisively (held-out AUC 0.5275, p=0.00 — the first non-spurious feature→label signal in v3's diagnostic chain since the /109 terminal 8h null), but T4 (the COMBINED 8h+daily-8 stack) did NOT clear its own permutation q95 (pooled AUC 0.5015, p=0.39). The Phase-6 backtest (3-seed EXPLORATION, code SHA `0bd50fd`, 0.80h) produced **IS monthly Sharpe +0.1621 / OOS monthly Sharpe +0.0015**, **79 OOS trades** (per-symbol OOS `net_pnl_pct` — one named metric per the /111 Critic Rec 2 process rule: BCHUSDT +24.97%/48.1% WR and TRXUSDT +7.73%/38.9% WR carriers, LDOUSDT −33.09%/25.0% WR the dominant OOS drag). The Phase-7.5 Critic returned **OVERALL: EXPLORATION-NEGATIVE** (single round, Round 2 skipped — no clarification can change the OVERALL): the verdict follows mechanically from the pre-registered Section 8 criterion C1 — IS monthly Sharpe Δ = **−0.6704** vs the /060 anchor (+0.8325) fires the pre-registered NEGATIVE falsifier (IS Δ < −0.10); the behavioral-inertia falsifier fires alongside it (IS trade roster moved only **1.89%** vs the /060 roster, 156 vs 159, below the 8% threshold). **Check 1 (Look-Ahead Audit) PASS — a structural headline** — the multi-frequency aggregation, a genuinely new look-ahead surface, is causally clean, verified line-by-line at source in `src/crypto_trade/features_v3/multifreq_v3.py` (daily-bar `day_close` = the last 8h candle's `close_time` = `23:59:59.999` of the UTC day; the backward `merge_asof` matches every 8h decision candle in day D to daily bar D−1; all 8 daily features past-only on the daily grid; adversarial `test_past_only_property` + `test_day_close_alignment` genuinely test the properties); **Check 8 (Hypothesis-Implementation Alignment) PASS** — the 22-feature stack genuinely ran, no scope creep, per-symbol architecture correctly restored from /112's pooled build; **Check 4 (IC Correlation) FAIL on the strict reading** (4 of 8 daily features exceed |IC| < 0.70 with the 14-feature stack — per Critic Rec 1 the `feedback_v3_engineered_feature_pivot.md` carve-out is scoped to algebraically-composed Category 2 features and does NOT extend to coarser-frequency aggregations) — but it does NOT change the OVERALL (the iteration fails C1 at Δ −0.6704 regardless). The NEGATIVE is a genuine, attributable property of the axis. The structural finding: **the multi-frequency daily-feature axis is IS-collapsing and behaviorally inert** — the daily family carries a real but small *standalone* signal (T5, AUC 0.5275) but the *combined* 22-feature stack does not clear its own permutation null (T4, p=0.39), so the small daily edge washes when concatenated into a model dominated by the larger, near-noise 14-feature 8h stack, and the enlarged feature space lets Optuna overfit IS noise. Pre-registered Section 7 Mode 1 (the standalone signal washes in the combined stack) AND Mode 3 (behavioral inertia, IS roster Δ < 8%) BOTH fired — the modal pre-registered classification was INERT and the actual was NEGATIVE; the mechanism was correctly predicted, the classification under-predicted (per Critic Rec 3, when the EDA's combined-stack horse race fails its permutation null, the modal classification is NEGATIVE and the predicted-effect interval — IS and OOS — must be centered at or below the anchor). The multi-frequency daily-feature axis is **CLOSED for cycle 6** — it joins /085's funding family and /086's basis family as a CLOSED feature-family axis; the 8 daily features are dropped (`V3_FEATURE_COLUMNS` reverts to the 14-feature stack for iter-v3/114; the `multifreq_v3` module retained as dormant infrastructure). iter-v3/114 advances to the last untested cycle-6 menu item — item #4, risk management (`project_v3_cycle6_axis_menu.md`; universe spent ×2 at /110/111, model architecture ×1 at /112, multi-frequency ×1 at /113 — all NEGATIVE) — recommended: a per-symbol LDO regime-conditional exogenous-trigger kill-switch (the /113 OOS data names LDO the −33.09% dominant drag, the one symbol that is the difference between a flat and a positive book; a binary off/on kill-switch is a permitted orthogonal mechanism per `feedback_v3_concentration_is_signal.md`; the kill-switch is a STATEFUL primitive so the iter-v3/114 EDA must use a closed-loop simulator OR a deadlock-impossibility proof per `feedback_v3_oracle_eda_validity.md` — an exogenous trigger sidesteps the /054 stateful-deadlock failure mode). The cycle has 6 EXPLORATION slots left (114-119) before the iter-v3/120 CONFIRMATION. `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-113.md`. PRIOR: iter-v3/112 cycle-6 EXPLORATION slot #3 closeout (**EXPLORATION-NEGATIVE — pooled-full architecture worse than per-symbol** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-112` a closeout marker only; edit is documentation-only — this banner line: iter-v3/112 tested cycle-6 axis-menu item 2 — a POOLED cross-symbol model architecture: ONE LightGbmStrategy trained on the concatenated BCH+LDO+TRX panel, vs the per-symbol architecture v3 has used for all 111 prior iterations. The hypothesis: pooling rescues the sample-starved LDO (per-symbol model trains on only ~370–1,200 rows; pooling expands its sample 10.3×) at a small dilution cost to the sample-rich BCH/TRX. The QR backed the axis with a committed IS-only EDA (`analysis/iteration_v3-112/`, EDA SHA `2cead80`, 18 tables) that returned a sharpened-GO — an aggregate held-out-AUC NO-GO (T5: pooled AUC lift −0.0008) with a credible permutation-validated per-symbol LDO rescue (T11/T12). The Phase-6 backtest (3-seed EXPLORATION, code SHA `e38aebd`, 0.43h) produced **IS monthly Sharpe −1.1236 / OOS monthly Sharpe −0.7228**, **64 OOS trades** (per-symbol OOS `net_pnl_pct` — one named metric per the /111 Critic Rec 2 process rule: BCHUSDT −42.8003/20.7% WR the dominant loss driver, TRXUSDT +12.1696/45.8% WR a carrier, LDOUSDT +0.7623/36.4% WR a weak rescue). The Phase-7.5 Critic returned **OVERALL: EXPLORATION-NEGATIVE** (single round, zero clarifications): the verdict follows mechanically from the pre-registered Section 8 criteria — Section 4 Falsifier #1 (OOS monthly Sharpe < −0.10) and Falsifier #3 (aggregate IS monthly Sharpe < +0.40) both FIRE; Section 8 PASS criteria C1 (IS Δ ≥ +0.10) and C2 (OOS Δ ≥ +0.20) both FAIL. **Check 8 (Hypothesis-Implementation Alignment) PASS — the headline of this iteration** — the pooled architecture genuinely ran, verified at source (`run_baseline_v3.py:2549`/`:2562-2575`, `run.log` lines 62/20937 confirm a SINGLE pooled LightGbmStrategy on the 3-symbol panel, not three per-symbol models; no scope creep), so the EXPLORATION-NEGATIVE is a genuine, attributable property of the pooled architecture, not an implementation artifact. The structural finding: **the pooled-full architecture is markedly worse than per-symbol** — pooling 3 *heterogeneous* symbols (BCH/LDO/TRX have different feature→label structure; BCH is the only one with a real per-symbol edge, 95.76% of /059's IS PnL) into one model forces a blurred cross-symbol rule that fits none of them and inverted the sample-rich BCH from a strong OOS positive to a −42.80 OOS loss at a 20.7% win rate; the BASELINE_V3.md cycle-1 BCH-fragility flag was correctly cited in brief Section 4 and confirmed. The LDO sample-rescue the EDA pre-registered showed only weak transfer (+0.76 OOS `net_pnl_pct` on 11 trades — too small to offset the BCH collapse). Pre-registered Section 7 Mode 1 (pooling dilutes the sample-rich symbols → BCH collapse) FIRED — the QR's pre-registration was correct (a prediction-calibration win; honest magnitude under-prediction — the brief framed the dilution as "light", the realized collapse was severe). The pooled-full model architecture is **CLOSED for cycle 6** — a pooled cross-symbol model only helps when the pooled symbols share feature→label structure, and the v3 universe is the survivor set of v3's symbol search, three symbols kept for individually surviving, not for sharing structure. iter-v3/113 advances to the next cycle-6 structural axis (`project_v3_cycle6_axis_menu.md`, per `feedback_v3_structural_over_knob_exploration.md`) — recommended: menu item 3, multi-frequency feature engineering (the highest-leverage untouched axis — universe spent ×2 at /110/111, model architecture ×1 at /112; it attacks the representation the /105→/109 chain identified as v3's binding constraint, in the one direction the /109 terminal 8h-representation null explicitly leaves open). The Critic's Recommendation 2 (partial pooling — pool LDO only, keep BCH/TRX per-symbol) is recorded as a documented lower-priority option: a refinement of a now-closed axis whose entire upside is the LDO rescue /112 already measured at only +0.76 OOS `net_pnl_pct`. The cycle has 7 EXPLORATION slots left (113-119) before the iter-v3/120 CONFIRMATION. `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-112.md`. PRIOR: iter-v3/111 cycle-6 EXPLORATION slot #2 closeout (**EXPLORATION-NEGATIVE — clean** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-111` a closeout marker only; edit is documentation-only — this banner line: iter-v3/111 was the Critic-mandated CLEAN RE-TEST of iter-v3/110's SYMBOL-SELECTION axis. iter-v3/110 ran the universe swap `V3_MODELS` BCH/LDO/TRX → CRV/AAVE/GRT/ADA but its runner trained every model under a stale `label_mode="trend_scanning"` (a four-iteration-old carry-over from iter-v3/105), confounding the run — so the symbol-selection axis was filed UNRESOLVED. iter-v3/111 reverts the runner to the /059-canonical `triple_barrier` label and re-executes (keeping /110's universe, the /110 EDA `analysis/iteration_v3-110/` SHA `cfeaf34` REUSED, and the /110 brief intact; the runner correction is the core deliverable). The Phase-6 backtest (3-seed EXPLORATION, code SHA `7dd84ee`, 1.09h) produced IS monthly Sharpe **−0.3075** / OOS monthly Sharpe **+0.1073**, **80 OOS trades** (per-symbol OOS `net_pnl_pct` — one named metric per Critic Rec 2: CRV +5.51% and AAVE +3.54% OOS-positive carriers; ADA −14.56% a dragger; GRT −51.94% the dominant dragger). The Phase-7.5 Critic returned **OVERALL: EXPLORATION-NEGATIVE — clean** (single round, zero clarifications): **Check 8 (Hypothesis-Implementation Alignment) PASS — the headline of the iteration** — the check iter-v3/110 FAILED is now PASS, the runner correction verified at source (`run_baseline_v3.py:1907`/`:1139`/`:1044`/`:1050`, corroborated by `run.log` lines 19-20: `13 knobs verified — ALL /111-canonical (... label_mode=triple_barrier) PASS`). The /110 confound is genuinely absent; the symbol-selection axis was, for the first time, cleanly tested. The verdict follows mechanically from the pre-registered Section 8 criteria — IS monthly Sharpe −0.3075 far below the +0.7325 NEGATIVE floor (Δ −1.14 vs the /060 anchor +0.8325); aggregate OOS trades 80 below the 130 bundle-level trade-rate floor; OOS/IS monthly Sharpe ratio sign-inverted. Per Critic Rec 1 this is **EXPLORATION-NEGATIVE — clean: the verdict that LEGITIMATELY CLOSES the symbol-selection-by-feature-AUC-screen axis for cycle 6** (the /110 review required the axis recorded UNRESOLVED *pending a corrected re-run*; iter-v3/111 IS that corrected re-run). The honest finding: the EDA's thin broad-population AUC margin (only CRV cleared the strict 3-criterion GO bar; AAVE/GRT/ADA marginal) did not survive the production Optuna + 7-gate stack — the +0.25 IS gated 2:1-barrier book proxy (`T9`) INVERTED to −0.31. Pre-registered Section 7 modes: Mode 3 (AAVE underperforms) DID NOT FIRE (AAVE OOS-positive); Mode 4 (ADA underperforms) FIRED (dead-path re-confirmed clean); GRT — the actual dominant dragger — was NOT pre-registered as primary (an honest prediction miss). iter-v3/112 advances to the next cycle-6 structural axis (`project_v3_cycle6_axis_menu.md`, per Critic Rec 3 + `feedback_v3_structural_over_knob_exploration.md`) — recommended: menu item 2, pooled vs per-symbol model architecture (the axis whose mechanism — per-symbol sample starvation — most directly answers why the cycle's universe work kept failing); a single-symbol drop-GRT patch is explicitly NOT proposed (axis-saturation territory). `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-111.md`. PRIOR: iter-v3/110 cycle-6 EXPLORATION slot #1 closeout (**EXPLORATION-NEGATIVE — label confound** — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-110` a closeout marker only; edit is documentation-only — iter-v3/110 opened the post-/109 structural axis menu (`project_v3_cycle6_axis_menu.md`) with a SYMBOL-SELECTION axis — a QR-led, EDA-backed wholesale universe replacement, `V3_MODELS` swapped BCH/LDO/TRX → CRV/AAVE/GRT/ADA, selected by a per-symbol walk-forward-faithful feature→label predictive-signal screen. iter-v3/110 ran a full Phase 1-8 — the FIRST runner execution since iter-v3/105, breaking the /103→/109 six-iteration NULL-AT-EDA chain. The Phase-6 backtest produced IS monthly Sharpe +0.2942 / OOS monthly Sharpe −0.4455 (per-symbol OOS: CRV the sole carrier +5.68%/36.8% WR; AAVE −13.03% the largest dragger, GRT −12.68%, ADA −9.80% — 3 of 4 OOS-negative). The Phase-7.5 Critic returned **OVERALL: EXPLORATION-NEGATIVE** — Check 8 (Hypothesis-Implementation Alignment) FAIL the verdict driver: the runner trained EVERY /110 model under `label_mode="trend_scanning"` (stale carry-over from iter-v3/105), NOT the brief-declared `triple_barrier`. iter-v3/105 closed `trend_scanning` EXPLORATION-NEGATIVE and mandated a two-line `label_mode` revert at the /106 setup; iter-v3/106-109 were ALL NULL-AT-EDA and never ran a runner setup, so the un-reverted knob lay dormant for FOUR iterations and /110 inherited it. The run therefore varied TWO variables — the intended universe swap AND the unintended labeling-geometry carry-over — so the symbol-selection axis is NOT cleanly tested; the as-run +0.2942 IS is statistically indistinguishable from /105's trend-scanning IS-collapse +0.2201. Per Critic Rec 1 this is recorded as a **label-confound NEGATIVE — the universe-selection axis is UNRESOLVED, NOT closed** (the CRV/AAVE/GRT/ADA screen is QR-led, EDA-backed, computed under `triple_barrier` — the axis design survives; only the as-run measurement is void; recording it as "universe-no-edge" would retire a structural cycle-6-menu axis on a false attribution). Despite the NEGATIVE verdict, iter-v3/110 was a PROCESS WIN: it ran a real experiment (vs the /103→/109 NULL-AT-EDA chain) and the Critic's implementation audit surfaced a four-iteration-old latent bug the NULL-AT-EDA pattern had buried. iter-v3/111 = the clean re-test of the CRV/AAVE/GRT/ADA universe under a `triple_barrier`-corrected runner (Critic Rec 4); the /111 setup must FIRST land Critic Rec 2 (revert `label_mode` → `triple_barrier`, fix the pre-flight assertion target, rewrite the stale /105 comment blocks) and Rec 3 (add `label_mode`/`trend_scan_grid` to the `_canonical_v059` accretion guard) as PRECONDITIONS. `OOS_CUTOFF_DATE`/`training_months` untouched. Detail: `diary-v3/iteration_v3-110.md`. PRIOR: iter-v3/105 cycle-5 EXPLORATION slot #5 closeout (EXPLORATION-NEGATIVE — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical, `v0.v3-105` a closeout marker only; edit is documentation-only: a NEW Dead Ideas entry records the **trend-scanning label** (`label_mode="trend_scanning"`, López de Prado MLAM §5.4) as NEGATIVE — IS-collapse (IS monthly Sharpe +0.2201, F2 fires, < +0.60 floor; F5 IS/OOS daily-Sharpe ratio 5.06 also fires); the OOS +1.1897 is the /102 IS-collapse / OOS-spike overfitting signature, NOT a leak (Critic Check 1 traced `_trend_scan_label` line-by-line, embargo over-covers the forward window by 1 candle); the KEY META-FINDING is that /105 FALSIFIES the /104 hypothesis that "the label geometry / the estimand is the binding constraint" — a label the 14 features predict +31-52% better, robustness-checked, nonetheless collapsed the multi-seed Optuna IS fit, localizing the binding constraint DOWNSTREAM of the label in the trade-construction / exit / risk layer; `label_mode` reverts to `triple_barrier`, the `_trend_scan_label` branch retained as zero-cost dead-code infrastructure). PRIOR: iter-v3/089 cycle-3 EXPLORATION #8 closeout (CONSTRUCTION-PARTIAL — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical; edit is documentation-only: the "Active Research Direction — cross-sectional" section below updated with /089's progress — the corrected cross-sectional build contained turnover (the HARD turnover gate PASSED, IS turnover/bar 0.1153 ≤ 0.138), transferred OOS (rank-IC +0.0279 > 0), and turned the book GROSS-POSITIVE on both windows (IS gross monthly Sharpe +0.0925, OOS gross monthly Sharpe +0.1717), lifting the OOS book +0.44 vs /088 to OOS monthly Sharpe −0.0985; the book fails NET only on fees (OOS fees/gross 1.58×, down from /088's 8.8×); classified CONSTRUCTION-PARTIAL — the corrected construction works and transfers but the thin gross signal keeps the net book sub-zero; /088 → /089 is v3's most sustained positive trajectory; /090 is the gross-signal-strengthening next build). PRIOR: iter-v3/088 cycle-3 EXPLORATION #7 closeout (the cross-sectional RE-ARCHITECTURE — ARCHITECTURE-PARTIAL; the Active Research Direction section created). PRIOR: iter-v3/087 cycle-3 EXPLORATION #6 closeout (NEGATIVE — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical; the /084 EXPLORATION-MODE-REFERENCE IS +0.8325 / OOS +0.3322 unchanged; edit is documentation-only: a NEW Dead Ideas entry records the WHOLESALE universe expansion 3→6 — `V3_MODELS` grew to BCH/LDO/TRX/GALA/MANA/SAND — as NEGATIVE (OOS monthly Sharpe collapsed to −0.5027, Δ −0.8349 vs the /084 EXPLORATION-MODE-REFERENCE; all 3 new symbols lost OOS; IS lifted +0.0883 — the breadth math transferred in-sample but the 3 new symbols' per-symbol models overfit IS and inverted OOS), and CLOSES Direction 2 (symbol-universe EXPANSION) for the remainder of cycle 3 at a **4-failure track record** spanning every expansion topology — /021 (HBAR+AVAX double), /069 (ADA single), /083 (FIL single), /087 (GALA+MANA+SAND wholesale); the breadth `√N` benefit transferred to production OOS in none of the four. PRIOR: iter-v3/086 cycle-3 EXPLORATION #5 closeout (INERT — non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical; the /084 EXPLORATION-MODE-REFERENCE IS +0.8325 / OOS +0.3322 unchanged; edit is documentation-only: a NEW Dead Ideas entry records the 7-FEED STRUCTURAL VERDICT — the perp-spot basis feed (/086) is the 7th non-OHLCV crypto-native feature family v3 has tried (funding /019/023/024/082/085, microstructure /015, basis /086) and ALL 7 ARE INERT-by-importance; the v3 per-symbol depth-3-5 LightGBM does not allocate ranked split capacity to crypto-native features regardless of the feed — /087+ must not be an 8th crypto-native feature family; the `fetch-spot` subcommand + `basis_v3.py` + `data/spot/` cache are RETAINED as reusable infrastructure). PRIOR: iter-v3/085 cycle-3 EXPLORATION #4 closeout (SUSPICIOUS — trade-selection sub-channel, non-advancing; NO-MERGE; canonical /059 anchor METRICS UNCHANGED, tag `v0.v3-059` canonical; the /084 EXPLORATION-MODE-REFERENCE IS +0.8325 / OOS +0.3322 unchanged; edit is documentation-only: the Dead Ideas funding-rate entry updated 4→5 data points — the v3 funding axis is now CLOSED across BOTH the direct-feature construction (/019/023/024/082) AND the composed-sign-switch construction (/085); /085's `funding_regime_momentum_5d` was INERT-by-importance rank 13/14/15-of-15 AND its OOS roster swap fired the /076 trade-selection sub-channel falsifier (added-minus-removed OOS mean-duration gap +1.578 > +1.0)). PRIOR: iter-v3/084 cycle-3 slot #3 closeout (REFERENCE / METHODOLOGY — REFERENCE-REANCHOR, non-advancing; canonical /059 anchor METRICS UNCHANGED; the "EXPLORATION-Mode Anchor Staleness" subsection below updated with /084's cycle-3 EXPLORATION-MODE-REFERENCE — IS +0.8325 / OOS +0.3322, 3-seed, current data — and the EXPLORATION-vs-CONFIRMATION architecture-gap finding; the `PER_CELL_GAP` 43→22 methodology fix is recorded as a permanent strictly-accretive correction). PRIOR: iter-v3/083 cycle-3 EXPLORATION #2 closeout (NEGATIVE, non-advancing; universe expansion 3→4 with FILUSDT added to Dead Ideas — IS monthly Sharpe collapsed −0.9156, FIL's own −32% IS edge + a ~24× IS-edge-screen magnitude under-prediction; FILUSDT joins HBAR/AVAX/ADA as CLOSED universe-expansion candidates). [Earlier closeouts /082 and before — see prior diary entries `diary-v3/iteration_v3-NNN.md` and `briefs-v3/exploration_catalog.md`.]

## Unified 10-Seed Ensemble Architecture (Phase B-3 at commit `ab2d9ac`)

Per memory rule `feedback_v3_unified_10seed_baseline.md` (user directive 2026-05-13 architectural change): the canonical v3 baseline anchors on a **single unified inference path** producing **ONE deterministic trade roster** suitable for live deployment.

The architecture change:
- **ENSEMBLE_SIZE**: 5 → 10
- **ENSEMBLE_SEEDS**: hardcoded 10-tuple with lineage preservation
  - Seeds 0-4 (outer=42 lineage): 191664963, 1662057957, 1405681631, 942484272, 929893137
  - Seeds 5-9 (outer=123 lineage): 33158374, 1465339467, 1273345680, 115579757, 1952249162
- **Outer-seed loop ELIMINATED** in `run_baseline_v3.py`: single `_run_single_seed` call replaces 2-outer × 5-inner loop
- **Single LightGbmStrategy instance per (symbol, walk-forward month) cell**: one Optuna optimization, one trained-model bundle, one inference path with proba averaging across all 10 models
- **Single trade roster**: ONE unified roster from 10-model averaged probability vs /058's two rosters merged via arithmetic-mean Sharpe
- **`--seeds` deprecated**: logs warning if passed, value ignored
- **`_verify_feature_columns` ENSEMBLE_SIZE=10 assertion** added at runtime
- **Report file changes**: `seed_summary.json` → `ensemble_summary.json` (10 rows with lineage annotation); `pareto_front.csv` no longer produced
- **Total Optuna trials UNCHANGED**: 35 × 3 syms × 10 seeds = 1050

**Why this matters for live deployment**: The prior 2-outer × 5-inner architecture required running two independent live strategy instances and merging their trade rosters — operationally infeasible. The unified architecture produces one deterministic inference path from one LightGbmStrategy instance per cell. This is the architecture that will be deployed in the live engine. Multi-seed-mean Sharpe is operationally inaccessible (cannot average two independent live rosters).

Regression coverage: `tests/strategies/ml/test_ensemble_unified.py::TestEnsembleSeedsLineage` regression-tests that `ENSEMBLE_SEEDS[0:5] == _derive_ensemble_seeds(42, 5)` and `ENSEMBLE_SEEDS[5:10] == _derive_ensemble_seeds(123, 5)` to catch future drift.

## Walk-forward Lookahead Fix at Commit `e149e9d` (carried over from /058 RE-ANCHOR #1)

Per memory rule `feedback_v3_walkforward_lookahead_bug.md` (user decision 2026-05-12 path a): ALL v3 iterations PRE-`e149e9d` are INVALIDATED. The walk-forward fix:

- NEW helper `walk_forward.compute_embargo_candles(timeout_minutes, interval_minutes)` returns `timeout_minutes // interval_minutes + 1 = 22` for 10080/480
- `walk_forward.generate_monthly_splits()` requires `label_timeout_minutes` + `interval_minutes` params
- `train_end_ms = test_start_ms - embargo_ms` (purges 22 training candles per (model, month) per symbol)
- `lgbm._train_for_month()` reuses the same helper for `cv_gap = embargo_candles * n_symbols = 66` — single source of truth
- Regression coverage: `tests/test_lookahead_embargo.py` 11/11 PASS

iter-v3/059 inherits the walk-forward fix from /058 unchanged. The Phase B-3 refactor at `ab2d9ac` did not touch `generate_monthly_splits()` — only the outer-seed loop in `run_baseline_v3.py` was eliminated.

## Critic Enhancement Protocol (Second Use at /059)

iter-v3/058 was the FIRST iteration audited under the enhanced Critic protocol at commit `414368a`. iter-v3/059 is the SECOND. Critic FINAL `0fc18c2` confirms:

- **Boot Step 9 Foundation Audit: PASS** — `compute_embargo_candles` helper + `train_end_ms = test_start_ms - embargo_ms` + `cv_gap = embargo_candles * n_symbols` single source of truth verified at Phase B-3 + post-walk-forward-fix state
- **Boot Step 10 Regression Test Confirmation: PASS** — including new `test_ensemble_unified.py::TestEnsembleSeedsLineage`
- **Boot Step 11 Anti-Pattern Static Scan: PASS** — all 13 §11 Anti-Pattern Catalog entries CLEAN; zero unexplained matches
- **Architecture-specific concerns** (FIRST AUDIT under unified 10-seed) — all resolved CLEAN (lineage preservation; `_confidence_threshold` consistency; proba averaging; trade roster construction matches brief Section 4.1 prediction; Optuna efficiency n_eff=19 architecture-independent)

## Headline Metrics (unified 10-seed ensemble — single trade roster; /116 no_confirm RULE-layer primitive merged at /121)

- IS monthly Sharpe: **+1.3108** (was +1.0894 at /059; lift **+0.2214**)
- OOS monthly Sharpe: **+0.9682** (was +0.5791 at /059; lift **+0.3891**)
- IS daily Sharpe: 3.1180 (was 2.7092 at /059)
- OOS daily Sharpe: 2.3979 (was 1.4359 at /059)
- OOS/IS monthly Sharpe ratio: **0.7386** — PASS ≥ 0.5 by margin +0.24 (was 0.5316 at /059; rebalanced toward healthier OOS/IS ratio without sacrificing the IS-Sharpe ≥ 1.0 floor first cleared at /059)
- IS Trades: 173 (was 171 at /059; Δ +2)
- OOS Trades: 98 (was 94 at /059; Δ +4); 7.0 trades/month over 14 OOS months; trade-rate floor of ≥10/month NOT met (informational carry-forward per pre-committed brief Section 4)
- IS MaxDD: **26.38%** (was 30.97% at /059; compressed −4.59pp — no_confirm RULE-layer tightens drawdown envelope by cutting adverse-direction trades before full SL realization)
- OOS MaxDD: **25.70%** (was 34.53% at /059; compressed −8.83pp)
- OOS Calmar: **1.4843** (was 0.6585 at /059; lift +0.83)
- DSR (legacy): 0.0 (structural at v3 trade volume; same root cause as all prior v3 CONFIRMATIONs)
- **DSR_relative: 0.9999** (was 0.1134 at /059) — **INFORMATIONAL under unified architecture** per `feedback_v3_dsr_mode_artifact.md`; 0.95 threshold needs cycle-1 recalibration. /121 dsr_relative is at the saturation cap by the legacy formula but informational only — Path B4 carries dsr_relative_b4=1.0 (PASS on the corrected formula).
- **frac_positive_paths (CPCV): 0.6444** — **IDENTICAL to /059** (CPCV architecture-independent; CPCV path-generation depends on cell-level seed ensemble + walk-forward geometry, which are unchanged at /121)
- **CPCV path Sharpe Q75: 0.8378** — **IDENTICAL to /059** (CPCV architecture-independent)
- PBO mean: **0.1278** — **IDENTICAL to /059** (cell-level invariant under the unified architecture; same CPCV embargo, same path count, same per-cell PCA)
- PSR: 1.0 (n_trials=1050 saturation)
- n_eff: 19 (per-cell PCA-95 median; architecture-independent)
- n_trials (Optuna total): 1050 (= 35 × 3 sym × 10 unified seeds; same as /059)
- min_trl_months: 5.77 (was 5.70 at /059; architecture-consistent)

## Per-symbol Attribution (single trade roster from comparison.csv)

OOS attribution:

| Symbol | OOS weighted_pnl | OOS n_trades | OOS WR | OOS concentration_pct |
|--------|----------------:|-------------:|-------:|----------------------:|
| BCH | **+35.83** | 35 | 48.6% | 93.92% (driver; lifted +11.08 vs /059's +24.75 — Δ broad-based) |
| TRX | **+5.21** | 51 | 43.1% | 13.65% (lifted +1.04 vs /059; WR climbed +1.4pp) |
| LDO | **-2.89** | 12 | 25.0% | -7.57% (improved +3.29 vs /059's -6.18; still OOS-negative but materially closer to neutral; the no_confirm RULE-layer cuts LDO's worst losing trades early) |

**3/3 symbols positive OOS Δ vs /059 — F5 cascade gate PASS broad-based.** BCH +11.08, LDO +3.29, TRX +1.04. The OOS uplift is genuinely distributed, NOT a single-symbol carrier effect.

IS attribution:

| Symbol | IS trades | IS WR | IS Net PnL% | % of Total IS PnL |
|---|---:|---:|---:|---:|
| BCH | 85 | **50.6%** | +119.15% | **87.62%** |
| TRX | 79 | 34.2% | +7.31% | 5.37% |
| LDO | 9 | 33.3% | +9.53% | 7.01% |

**3/3 symbols improve IS net_pnl_pct vs /059** — BCH +9.92pp (109.23 → 119.15), LDO +8.64pp (the chronic IS-drag symbol with +0.89 at /059 climbs to +9.53), TRX +3.36pp (3.95 → 7.31). BCH IS concentration 87.62% (down from 95.76% at /059; LDO lift narrows the concentration gap but BCH remains the dominant IS-share symbol; concentration is a structural property of the 3-symbol universe, not a /121-specific issue).

## Code Configuration

- **V3_FEATURE_COLUMNS (14)** — UNCHANGED from /028/059 spec: `max_dd_window_50, ema_spread_atr_20, ret_kurt_50, ret_skew_200, range_realized_vol_50, hurst_diff_100_50, ret_kurt_200, hurst_100, btc_ret_14d, ret_skew_50, vwap_dev_20, ret_autocorr_lag1_50, sym_vs_btc_ret_7d, regime_momentum_signed_5d` (V3_FEATURE_COLUMNS_TOP_N at `src/crypto_trade/features_v3/__init__.py`)
- **V3_MODELS**: BCHUSDT, LDOUSDT, TRXUSDT (drop-MKR per iter-v3/013)
- **V3_EXCLUDED_SYMBOLS**: BTCUSDT, ETHUSDT, LINKUSDT, LTCUSDT, DOTUSDT, BNBUSDT, SOLUSDT, XRPUSDT, DOGEUSDT, NEARUSDT, MKRUSDT
- **ATR labeling multipliers**: (atr_tp=2.0, atr_sl=1.0) — DEFAULT for all symbols (V3_ATR_MULTIPLIERS_PER_SYMBOL={} empty per iter-v3/039)
- **RiskV2Config**: `zscore_threshold=2.0` (iter-v3/011), `adx_threshold=20.0`, `adx_threshold_per_symbol={}`, `BTC_TREND_CONFIG.threshold_pct=15.0`, `block_long_for=()`, `block_short_for=()`, `enable_per_symbol_drawdown_brake=False`
- **7-primitive risk gate stack**: BTC trend kill, vol scaling, ADX, Hurst regime, feature z-score OOD, low-vol filter, hit-rate (DISABLED). Regime gate disabled (per iter-v3/022). Per-symbol cap disabled (per iter-v3/020). Per-symbol drawdown brake disabled (per iter-v3/054).
- **Triple-barrier labeling**: 21-candle (10080-min / 8h) timeout
- **/116 no_confirm RULE-layer exit primitive (MERGED at /121 — first cycle-6 ingredient)**: `enable_no_confirm_exit=True`, `no_confirm_trigger_atr=0.50`, `no_confirm_k_candles=4`. Mechanism: after a position is opened, if the price has moved adversely by ≥ 0.50 ATR within the first K=4 candles without confirmation (i.e., no return to entry), exit early (before full SL realization). Code surface: `src/crypto_trade/backtest.py:251-301` (`_no_confirm_confirmed` flag) + arm_time computed at order creation (`backtest.py:660-680`). Look-ahead clean per Critic /121 Check 1. The rule fires at ~6-9% of trades with ~10% WR and ~−1% avg net_pnl_pct — designed to cut losing positions early; the OOS lift comes from the slot-freeing cascade (early exits free position slots earlier so the model can rotate into next signal). Validated standalone at 10-seed CONFIRMATION at /121 (IS Δ +0.22, OOS Δ +0.39 vs /059). Component B (/119 C6 `ret5d_signed_tbi`) DROPPED at /121 per /120 F3 binding falsifier + /121 attribution finding (Component B's marginal multi-seed effect: IS −0.58 / OOS +0.73 — degrades IS, drives /120 bundle super-additive OOS via FEATURE-RULE interaction; non-replicable as single-primitive).
- **ENSEMBLE_SIZE = 10** (unified 10-seed ensemble — Phase B-3 architecture)
- **ENSEMBLE_SEEDS** (10-tuple, lineage-preserving):
  - Seeds 0-4 (outer=42 lineage): `(191664963, 1662057957, 1405681631, 942484272, 929893137)`
  - Seeds 5-9 (outer=123 lineage): `(33158374, 1465339467, 1273345680, 115579757, 1952249162)`
- **CPCV**: n_paths=45, embargo=27, REQUIRED_GAP=66 = (timeout_candles=21+1) × n_symbols=3
- **Walk-forward (POST-FIX at `e149e9d`)**: `walk_forward.generate_monthly_splits` applies embargo of `compute_embargo_candles(10080, 480) = 22` candles so `train_end_ms = test_start_ms - embargo_ms`; single source of truth shared with `lgbm._train_for_month()` via the same helper
- **Optuna**: `--n-trials 35` per cell × 10 seeds × 3 symbols = 1050 total trials; `n_jobs=1` (Phase A n_jobs=2 ATTEMPTED at `0a3c30e`, REVERTED at `31665f6` due to 5× GIL slowdown); `colsample_bytree` Optuna-tuned (NOT hardcoded 1.0)
- **OOF parquet guardrail**: `--clean-oof` flag active (per `feedback_v3_oof_parquet_guardrail.md`). The OOF parquet append (`trial_oof_returns.parquet`) is **atomic** since iter-v3/082 commit `a7abba4` — the in-place `to_parquet` write was replaced with a temp-file + `os.replace` POSIX-rename pattern, so a concurrent reader sees either the fully-written old or fully-written new file (no partial-write window). Critic-verified metric-neutral; a permanent runner robustness improvement for every v3 iteration.
- **Outer seeds**: deprecated under unified architecture; `--seeds` flag logs warning if passed
- **DSR reporting (Path B4 — retained infrastructure from iter-v3/070 CONFIRMATION)**: `dsr.json`
  reports BOTH the legacy `dsr_relative` field AND the new `dsr_relative_b4` field. Path B4
  (the annualized-both-sides reformulation specified at iter-v3/062 brief Section 3) corrects a
  granularity-mismatch bug in the legacy computation — trade-level cumulative PnL had entered
  `psr()` as if it were an annualized Sharpe. Path B4 puts both inputs at the same granularity:
  observed Sharpe at √252 (annualized daily), benchmark CPCV-Q75 at √756. `dsr.json` additionally
  carries the informational tracking fields `daily_sharpe_oos_b4_at_sqrt252`,
  `cpcv_q75_annualized_b4`, and `n_daily_obs_oos`. Path B4 was the one durable methodology gain
  of cycle 1 — bundled and validated at iter-v3/070 CONFIRMATION (Component B; ACCEPTED). It is a
  strictly-accretive methodology improvement per `feedback_v3_promising_mechanical_subtype.md`
  (non-compoundable across iterations). **The /059 anchor METRICS are UNCHANGED** — Path B4 is a
  reporting-layer-only change with zero behavioral effect on the trade roster. The /059 legacy
  `dsr_relative` of 0.1134 (recorded in Headline Metrics above) was a measurement artifact; under
  Path B4 the same /059 OOS performance would report `dsr_relative_b4 ≈ 1.0`.
- **Sacred constants**: OOS_CUTOFF_DATE=2025-03-24, training_months=24 (IMMUTABLE)

## Reproducibility Stamp (iter-v3/121 — current canonical)

- Setup commit SHA: `704ee6c` (REVERT V3_FEATURE_COLUMNS_TOP_N 15→14; pre-flight C6-PRESENT → C6-ABSENT inversion; ITERATION_LABEL=v3-121)
- Phase 5.5 gate SHA: `c5a86e5` (PASS — all 11 sections verified)
- Brief SHA: `ad8af39`
- Engineering report SHA: `4b1fea2`
- Critic FINAL SHA: `6d1017d` (CONFIRMATION-MERGE; single round; zero clarifications; all 8 checks PASS)
- BASELINE_V3.md update SHA: (this commit)
- Diary SHA: (this commit cycle)
- Tag: `v0.v3-121` (cycle-6 closure event; first BASELINE_V3.md update since 2026-05-13; supersedes `v0.v3-059` as canonical)
- Wall-clock: 3.21h (within 6h CONFIRMATION cap)
- Hardware: WSL2 Linux 6.6.114.1-microsoft-standard-WSL2
- Library stack pinned: lightgbm 4.6.0, optuna 4.8.0, numpy 2.2.6, pandas 3.0.0, scikit-learn 1.8.0, scipy 1.17.0, statsmodels 0.14.6, pyarrow 23.0.1
- Run command: `uv run python run_baseline_v3.py --n-trials 35 --clean-oof` (no `--seeds` flag; deprecated in Phase B-3 architecture; no `--exploration` flag — CONFIRMATION mode)

### Inherited Infrastructure (carry-forward from /059 — UNCHANGED at /121)

- Phase A revert SHA: `31665f6` (n_jobs=2 → n_jobs=1; GIL contention)
- Phase B-3 commit SHA: `ab2d9ac` (unified 10-seed ensemble refactor)
- Walk-forward fix SHA: `e149e9d` (carried over from /058 RE-ANCHOR #1; cherry-pick from main `5566a69`)
- Critic enhancement SHA: `414368a` (inherited from /058 — Foundation Audit + §11 Anti-Pattern Catalog)
- /116 no_confirm primitive source EDA: SHA `52444c9` (cycle-6 EXPLORATION slot #7)

## Baseline History

| Tag | Date | IS Sharpe | OOS Sharpe | Status |
|---|---|---:|---:|---|
| `v0.v3-018` | 2026-05-07 | +0.3788 | +0.3869 | BOOTSTRAP (RETIRED — biased walk-forward) |
| `v0.v3-028` | 2026-05-08 | +0.5101 | +0.5053 | first CONFIRMATION-MERGE (RETIRED — biased walk-forward) |
| `v0.v3-058` | 2026-05-12 | +0.7481 | +0.8700 | RE-ANCHOR #1 multi-seed mean (RETIRED — superseded by unified architecture) |
| `v0.v3-059` | 2026-05-13 | +1.0894 | +0.5791 | RE-ANCHOR #2 unified 10-seed (PRIOR CANONICAL — replaced 2026-05-20) |
| **`v0.v3-121`** | **2026-05-20** | **+1.3108** | **+0.9682** | **CURRENT CANONICAL — cycle-6 closure; /116 no_confirm MERGED** |

### iter-v3/120 — record OOS NOT MERGED

iter-v3/120 (CONFIRMATION TWO-COMPONENT bundle: /116 no_confirm + /119 C6 `ret5d_signed_tbi`) achieved the all-time v3 OOS monthly Sharpe record (+1.6946) but was NOT merged. Pre-committed F3 (sister-redistribution conjunctive AND gate) FIRED cleanly + F4 (IS regime-cost floor +0.79) FAILED by 0.0607; BOTH-must-improve gate independently confirmed NO baseline update (IS Δ −0.3601 vs /059). The interaction effect was GENUINE (Jaccard 0.48 between bundle and /116-alone OOS rosters; 30 trades unique to bundle out of 96) but unmergeable due to IS regime-cost. /121 attribution finding (this iteration) decomposes the bundle as: Component A standalone (IS +1.3108 / OOS +0.9682; strictly-accretive) + Component B marginal effect (IS −0.5815 / OOS +0.7264; degrades IS, drives interaction lift). Component A merges at /121; Component B DROPPED PERMANENTLY. The /120 all-time OOS record stands as a historical landmark — a documented multi-mechanism interaction artifact, not a replicable single-primitive property.

## Sacred Constants

```
OOS_CUTOFF_DATE             = 2025-03-24       # IMMUTABLE
training_months             = 24                # IMMUTABLE
ENSEMBLE_SIZE               = 10                # unified 10-seed (Phase B-3)
ENSEMBLE_SEEDS              = (191664963, 1662057957, 1405681631, 942484272, 929893137,
                                33158374, 1465339467, 1273345680, 115579757, 1952249162)
enable_no_confirm_exit      = True              # /116 RULE-layer primitive MERGED at /121
no_confirm_trigger_atr      = 0.50              # adverse-move threshold in ATR units
no_confirm_k_candles        = 4                 # confirmation window (candles)
```

Plus v3-specific hard thresholds (per `ITERATION_PLAN_8H_V3.md`):

```
DSR_threshold          = 0.95     # Legacy Deflated Sharpe Ratio — structural FAIL at v3 trade volume
DSR_relative_threshold = 0.95     # NEEDS CYCLE 1 RECALIBRATION under unified architecture
                                  # (calibrated for 2-outer × 5-inner min_trl_months ~11.5;
                                  # unified architecture min_trl_months ~5.7 may make 0.95
                                  # structurally unreachable — see Critic FINAL `0fc18c2` Recommendation #1)
PBO_threshold          = 0.40     # Probability of Backtest Overfitting
PSR_threshold          = 0.95     # Probabilistic Sharpe Ratio
IC_threshold           = 0.70     # |IC_pearson| between feature families
ADF_threshold          = 0.05     # ADF p-value (rejects unit root)
```

NEW Gate 10-CPCV (replaces retired Gate 10 Pareto under unified architecture):

```
Gate 10-CPCV: cpcv_frac_positive_paths >= 0.55
```

iter-v3/121: cpcv_frac_positive_paths = 0.6444 — PASS (IDENTICAL to /059 — CPCV invariant under unified architecture; cell-level seed ensemble + walk-forward geometry unchanged).

Inherited project-level merge gates:

- IS monthly Sharpe > 1.0
- OOS monthly Sharpe > 1.0
- OOS / IS Sharpe ratio ≥ 0.5
- ≥10 trades/month OOS, ≥130 OOS total trades
- Top symbol concentration ≤ 30% of OOS PnL (or explicit exception)
- Single-roster validation under unified architecture (replaces multi-seed concentration validation)

## Failed MERGE Gates (Outstanding Constraints — carry forward to cycle 7)

| # | Gate | Threshold | iter-v3/121 Observed | /059 Observed | Required Lift |
|---|---|---:|---:|---:|---:|
| 1 | IS monthly Sharpe ≥ +1.0 | ≥ 1.0 | **+1.3108** | +1.0894 | **PASS by +0.31 margin** |
| 2 | OOS monthly Sharpe ≥ +1.0 | ≥ 1.0 | +0.9682 | +0.5791 | **+0.03 (very close — could clear in any cycle-7 PROMISING)** |
| 4 | Legacy DSR > 0.95 | > 0.95 | 0.0 | 0.0 | structural; needs gate reformulation |
| 4b | DSR_relative > 0.95 | > 0.95 | **0.9999** | 0.1134 | INFORMATIONAL (architecture-dependent; Path B4 carries dsr_relative_b4=1.0 PASS) |
| 7 | Top-symbol concentration ≤ 30% | ≤ 30% | BCH 93.92% OOS (87.62% IS) | 108.86% / 95.76% | structural property of 3-symbol BCH-dominant universe |
| 8a | OOS trades ≥ 130 aggregate | ≥ 130 | 98 | 94 | **+32 trades** (or +18 OOS months of data accumulation, or universe expansion) |
| 8b | OOS trades per-month ≥ 10 | ≥ 10/month | 7.0/month (14 OOS months) | 6.7/month | **+3.0/month** |

**Gate 1 (IS Sharpe ≥ +1.0) PASS by margin +0.31** — second consecutive PASS in v3 history (after /059's first PASS at +1.0894). **Gate 2 (OOS Sharpe ≥ +1.0) within +0.03 of clearing** — any cycle-7 PROMISING could push it over the line. Per `feedback_v3_strict_both_is_oos_baseline.md`, future v3 CONFIRMATIONs must produce a multi-seed result where BOTH IS Sharpe AND OOS Sharpe improve over **/121** (the new canonical), NOT /059.

Per user directive 2026-05-08 STRICTLY-BETTER-than-prior-baseline policy: these aspirational MERGE-gate failures **inform future-iteration priorities but do NOT block the /121 baseline update**.

DSR (legacy) root cause: at n_trials=1050 / n_eff=19, López de Prado E[max_SR] formula returns required SR ≈ 2.61; observed annualized ≈ 3.31 → DSR=0 via the SBT formula. NOT a code bug; structural at v3's trade volume + Optuna budget.

DSR_relative under unified architecture: per `feedback_v3_dsr_mode_artifact.md` the legacy `dsr_relative` 0.95 threshold was calibrated for the 2-outer × 5-inner architecture (min_trl_months ~11.5); under unified architecture (min_trl_months ~5.77) the legacy formula produces 0.9999 at /121 which is at the saturation cap — INFORMATIONAL only. Path B4 (the annualized-both-sides reformulation) is the structurally correct measure: `dsr_relative_b4=1.0` at /121, PASS on the corrected gate.

OOS trade-rate floor remains the binding v3-wide constraint. The 3-symbol BCH/LDO/TRX universe at current regime generates ~7 OOS trades/month — reaching the 130-aggregate floor requires ~18-19 OOS months of coverage or universe expansion (universe expansion CLOSED at 4-failure track record per Dead Ideas).

## Methodological Successes (PASSED Gates)

- **Gate 1: IS monthly Sharpe ≥ +1.0 — PASS at +1.3108** (second consecutive PASS in v3 history after /059's first PASS at +1.0894; margin +0.31)
- **Gate 3: OOS/IS Sharpe ratio ≥ 0.5 — PASS at 0.7386** (margin +0.24 above floor; substantial improvement from /059's borderline 0.5316)
- **Gate 5: PBO mean < 0.4 — PASS at 0.1278** (IDENTICAL to /059 — cell-level invariant)
- **Gate 6: PSR > 0.95 — PASS at 1.0** at n_trials=1050 saturation
- **Gate 10-CPCV: frac_positive_paths ≥ 0.55 — PASS at 0.6444** (IDENTICAL to /059 — CPCV architecture-independent). Replaces Pareto Gate 10 (RETIRED under unified architecture).
- **F1 BOTH-must-improve — PASS** at the BASELINE_V3.md update gate: IS Δ +0.2214 above /059 AND OOS Δ +0.3891 above /059, both legs clear
- **F2 IS regime-cost floor (+0.79) — PASS** by margin +0.5208 (IS +1.3108)
- **F5 per-symbol cascade ≥ 2/3 — PASS at 3/3** (BCH +11.08, LDO +3.29, TRX +1.04 OOS weighted_pnl Δ vs /059)
- **All 8 Critic checks** (look-ahead, embargo, multiple-testing, IC, ADF, Pareto-replacement, reproducibility, hypothesis-implementation alignment) — PASS at single review round with ZERO clarifications requested
- **Enhanced Critic protocol Boot Steps 9-11 + Check 13** — PASS-CARRY-FORWARD (Foundation Audit + Regression Test Confirmation + Anti-Pattern Static Scan; all 13 §11 catalog entries CLEAN)

## What Changed vs /059 RE-ANCHOR #2 (PRIOR CANONICAL)

**SAME bundle composition for everything except the no_confirm RULE-layer primitive** (V3_FEATURE_COLUMNS=14 unchanged from /028/059; V3_MODELS unchanged; ATR multipliers unchanged; risk gate stack unchanged; ensemble seeds unchanged; walk-forward + CPCV geometry unchanged).

**SINGLE SUBSTANTIVE CHANGE**: /116 no_confirm RULE-layer exit primitive ENABLED (`enable_no_confirm_exit=True`, `no_confirm_trigger_atr=0.50`, `no_confirm_k_candles=4`). The early-exit-on-no-confirmation mechanism cuts adverse-direction trades after 4 candles if they have moved ≥0.50 ATR adversely without confirmation. Lives in `backtest.py:251-301`; look-ahead clean per Critic /121 Check 1.

- IS Sharpe lift: **+0.2214** (+1.0894 → +1.3108)
- OOS Sharpe lift: **+0.3891** (+0.5791 → +0.9682)
- OOS/IS Sharpe ratio: 0.5316 → **0.7386** (rebalanced toward healthier OOS/IS ratio without sacrificing IS-Sharpe ≥ 1.0 floor)
- OOS MaxDD compression: 34.53% → **25.70%** (−8.83pp — the no_confirm RULE-layer tightens drawdown envelope by cutting adverse-direction trades before full SL realization)
- IS MaxDD compression: 30.97% → **26.38%** (−4.59pp)
- OOS Calmar lift: 0.6585 → **1.4843** (+0.83)
- DSR_relative: 0.1134 → **0.9999** (legacy formula at saturation cap under unified architecture; INFORMATIONAL — Path B4 dsr_relative_b4=1.0 is the structurally correct measure, PASS at both /059 and /121)
- frac_positive_paths: **IDENTICAL** at 0.6444 (CPCV architecture-independent)
- PBO: **IDENTICAL** at 0.1278 (cell-level invariant under unified architecture)
- CPCV path Sharpe Q75: **IDENTICAL** at 0.8378
- IS trades: 171 → 173 (+2)
- OOS trades: 94 → 98 (+4)

**Per-symbol OOS Δ vs /059 (3/3 positive — F5 broad-based PASS)**:
- BCH OOS weighted_pnl: +24.75 → **+35.83** (Δ +11.08; OOS WR +7.4pp)
- LDO OOS weighted_pnl: −6.18 → **−2.89** (Δ +3.29; still OOS-negative but materially closer to neutral)
- TRX OOS weighted_pnl: +4.16 → **+5.21** (Δ +1.04)

**Per-symbol IS Δ vs /059 (3/3 positive)**:
- BCH IS net_pnl_pct: +109.23 → **+119.15** (Δ +9.92pp)
- LDO IS net_pnl_pct: +0.89 → **+9.53** (Δ +8.64pp; the chronic IS-drag symbol at /059 substantially improved)
- TRX IS net_pnl_pct: +3.95 → **+7.31** (Δ +3.36pp)
- TRX OOS weighted_pnl: +23.03 (/058 seed 42) → +4.16 (collapsed -18.87 — primary OOS drag)
- LDO OOS weighted_pnl: -15.29 (/058 seed 42) → -6.18 (improved +9.11)
- BCH OOS weighted_pnl: +27.25 (/058 seed 42) → +24.75 (-2.50; stable)

The structural mechanism: at /058 seed 42 produced IS/OOS = 1.60× (IS-dominant); seed 123 produced OOS/IS = 3.91× (strongly OOS-dominant); the arithmetic mean yielded apparent balance (0.86). The unified 10-seed ensemble inherits seed-42 lineage IS-dominance at prediction-averaging stage while suppressing seed-123 lineage OOS-dominance into the averaged signal. **Cross-seed cancellation was unmasked at /059** — the unified architecture exposes the true IS-dominant character of the BCH/LDO/TRX bundle directly. Detail in `diary-v3/iteration_v3-059.md` Section 5.

## Anchor Comparison Cheat-Sheet (for cycle 7 EXPLORATIONs iter-v3/122+)

iter-v3/122+ EXPLORATIONs anchor against THIS baseline (iter-v3/121 unified 10-seed with /116 no_confirm primitive merged), NOT the retired /059 anchor or earlier retired anchors:

| Reference | Architecture | IS Sharpe | OOS Sharpe | Status |
|---|---|---:|---:|---|
| **iter-v3/121 NEW BASELINE (unified 10-seed + no_confirm)** | unified | **+1.3108** | **+0.9682** | CURRENT CANONICAL |
| iter-v3/120 TWO-COMPONENT bundle (not merged) | unified | +0.7293 | +1.6946 | all-time OOS record but unmergeable (F3+F4) |
| iter-v3/059 RE-ANCHOR #2 (PRIOR CANONICAL — retired 2026-05-20) | unified | +1.0894 | +0.5791 | retired |
| iter-v3/058 RE-ANCHOR #1 (multi-seed mean — RETIRED) | 2-outer × 5-inner | +0.7481 | +0.8700 | retired |
| iter-v3/028 BIASED anchor (RETIRED) | 2-outer × 5-inner buggy WF | +0.5101 | +0.5053 | retired |
| iter-v3/018 BOOTSTRAP (further retired) | 2-outer × 5-inner buggy WF | +0.3788 | +0.3869 | retired |

EXPLORATION-mode PROMISING bands (single-axis variation on top of iter-v3/121 baseline, EXPLORATION-mode 3-seed run):

The /121 metrics are CONFIRMATION-mode 10-seed unified-ensemble numbers. EXPLORATION-mode runs (`--exploration` flag, `EXPLORATION_ENSEMBLE_SIZE=3`) produce structurally different numbers — per `feedback_v3_dsr_mode_artifact.md` EXPLORATION-mode DSR/PSR are regime-specific artifacts NOT comparable to CONFIRMATION-mode. The first cycle-7 EXPLORATION (/122) should establish the EXPLORATION-MODE-REFERENCE for /121 by running the /121-config at 3-seed mode on current data (analogous to /077, /084 cycle-N reference re-runs).

- **PROMISING (single-seed)**: IS Δ ≥ +0.10 vs iter-v3/121 baseline; OOS Δ ≥ +0.10 vs iter-v3/121 baseline
- **NEGATIVE (single-seed)**: IS Δ < −0.10 OR OOS Δ < −0.10
- **NEGATIVE-SUSPICIOUS-OOS qualifier**: IS-OOS daily ratio outside [0.5, 2.0] (per iter-v3/026/027 pattern)

Note: at CONFIRMATION, BOTH-must-improve discipline applies (per `feedback_v3_strict_both_is_oos_baseline.md`) against the /121 multi-seed baseline. At EXPLORATION level, single-axis improvement on either axis is informational.

## Forbidden Symbols (V3_EXCLUDED_SYMBOLS)

v3 cannot trade any symbol already traded by v1 or v2 (plus dropped/v3-failed symbols). The runner enforces this at startup:

```python
V3_EXCLUDED_SYMBOLS = (
    # v1 traded
    "BTCUSDT", "ETHUSDT", "LINKUSDT", "LTCUSDT", "DOTUSDT",
    # historical reservation
    "BNBUSDT",
    # v2 traded
    "SOLUSDT", "XRPUSDT", "DOGEUSDT", "NEARUSDT",
    # v3 dropped per iter-v3/013 universe-axis EXPLORATION
    "MKRUSDT",
)
assert set(cfg.symbols).isdisjoint(V3_EXCLUDED_SYMBOLS), \
    f"v3 cannot trade v1/v2 symbols: {set(cfg.symbols) & set(V3_EXCLUDED_SYMBOLS)}"
```

## Active Research Direction — the cross-sectional relative-value architecture (LIVE; established iter-v3/088)

> This is a LIVE research direction, NOT a Dead Idea. iter-v3/088 produced v3's FIRST genuine OOS signal transfer and the cross-sectional architecture is the active v3 research line; it is recorded here, not in Dead Ideas, because /089-091 build directly on it.

**iter-v3/088 — the cross-sectional relative-value RE-ARCHITECTURE — ARCHITECTURE-PARTIAL — produced v3's first OOS-positive new signal (cycle-3 EXPLORATION #7).** iter-v3/088 was the most ambitious v3 iteration since iter-v3/001: it REPLACED the v3 model architecture — the N per-symbol absolute-barrier `LightGbmStrategy` classifiers → ONE pooled `LGBMRanker(objective="lambdarank")` (LambdaMART) trained on the pooled cross-section of a 22-symbol universe (`XS_UNIVERSE`), predicting the cross-sectional forward-return rank (H=3-bar / 1-day, tercile-graded {0,1,2} per timestamp — a RELATIVE label replacing the `(2.0,1.0)`-ATR triple-barrier), traded as a dollar-neutral tercile long-short book (inverse-vol weighting + portfolio vol-targeting, every-8h-bar rebalance, 0.1% turnover-based fee). New module `cross_sectional.py` (1073 lines) + new runner `run_cross_sectional_v3.py` (the legacy per-symbol `run_baseline_v3.py` untouched); 13 features (the 14-feature /059 anchor stack minus `btc_ret_14d` for zero cross-sectional dispersion, cross-sectionally rank-normalized); `XS_REQUIRED_GAP = 88 = (3+1)×22`; the legacy 7-gate risk stack intentionally absent (3 structural controls — dollar-neutral construction, inverse-vol + vol-target, tercile diversification). **The result: the dollar-neutral tercile long-short BOOK loses money (IS monthly Sharpe −0.6403, OOS monthly Sharpe −0.5418) — BUT the pooled cross-sectional MODEL's OOS rank-IC is +0.0430 ± 0.3317 over n = 1255 OOS timestamps (t ≈ +4.59) — statistically significant and POSITIVE: the cross-sectional signal genuinely TRANSFERRED out-of-sample.** Classified **ARCHITECTURE-PARTIAL** (brief Section 8.4 — the re-architecture-adapted taxonomy; Critic FINAL `1acfb9c` OVERALL=MERGE methodology-only; the QR's Phase-8 call): F1 (OOS rank-IC ≤ 0 → ARCHITECTURE-FALSIFIED) does NOT fire — OOS rank-IC +0.043 > 0, the signal transferred; non-F1 falsifiers F3 (OOS Sharpe < 0) and F4 (frac_positive_paths 0.000 < 0.55) fire — the long-short CONSTRUCTION loses money. **The architecture is NOT falsified — the position CONSTRUCTION is.** Two diagnosed, fixable failures (QE + Critic concurring): (a) a SIGN INVERSION in the brief — a `LGBMRanker` trained on a FORWARD-return-grade label learns high score = high future return (IS Spearman(score, grade) +0.0328, p = 1.2e-12), so the brief's "long the bottom tercile / low-score" construction longs the predicted future LOSERS; (b) TURNOVER DRAG is the DOMINANT failure — IS fees 0.6866 are 8.8× IS gross PnL 0.078; the flipped-IS diagnostic shows even the corrected sign yields only IS *gross* Sharpe ≈ +0.067 and IS *net* Sharpe still −0.4308. **The significance: across the prior ~27 v3 EXPLORATIONs (3 cycles) the recurring failure was per-symbol models scoring strongly-positive IS and inverting OOS; iter-v3/088 is the FIRST v3 EXPLORATION to produce a genuine, statistically-significant, OOS-positive signal transfer.** The cross-sectional architecture is the active v3 research line; **iter-v3/089 is the corrected next build (NOT a fresh re-architecture)** — fix the sign (LONG the top tercile, SHORT the bottom), attack turnover structurally with a pre-registered turnover ceiling as a hard gate, fix the `_compute_xs_cpcv` path-Sharpe proxy (it collapsed to 45 rows of `sharpe = 0.0`), and consider strengthening the gross signal (the corrected-sign IS gross Sharpe ≈ +0.067 is thin — high-conviction quintile/decile concentration, a longer horizon, a cross-sectionally-normalized feature expansion). The `cross_sectional.py` module, `run_cross_sectional_v3.py` runner, `XS_UNIVERSE` constant, and pooled-ranking infrastructure are RETAINED and built upon. BASELINE_V3.md UNCHANGED — /059 stays canonical; an EXPLORATION cannot update the baseline. Detail: `diary-v3/iteration_v3-088.md`.

**iter-v3/089 — the CORRECTED cross-sectional iteration — CONSTRUCTION-PARTIAL — the book is now GROSS-POSITIVE; /088 → /089 is v3's most sustained positive trajectory (cycle-3 EXPLORATION #8).** iter-v3/089 was the corrected next build on /088's RETAINED `cross_sectional.py` infrastructure — NOT a fresh re-architecture. Five changes vs /088 (all IS-EDA-selected, EDA `c172a12`): (1) the SIGN FIX (Critic /088 Rec #2) — `build_positions` now LONGs the TOP quantile (highest predicted scores = predicted future winners) and SHORTs the BOTTOM, the inverse of /088, specified from first principles (`lambdarank` on a forward-return-grade label learns high score = high future return); (2) the CPCV-PROXY FIX (Critic /088 Rec #3) — `_compute_xs_cpcv` rewritten to compute the ACTUAL realised long-short NET return per CPCV path (`cpcv_paths.csv` now non-degenerate, 45 distinct Sharpes); (3) the cost-aware construction — `XS_QUANTILE_FRAC=0.20` quintile legs + `XS_HOLD_BARS=3` Jegadeesh-Titman overlapping holds + `XS_NO_TRADE_BAND=0.020` Constantinides/Davis-Norman no-trade band; (4) the pre-registered HARD turnover ceiling `XS_TURNOVER_CEILING=0.138` (a breach is NO-MERGE regardless of Sharpe); (5) the 13-feature stack / 22-symbol `XS_UNIVERSE` / H=3 label / `XS_REQUIRED_GAP=88` embargo UNCHANGED. **The result: the corrected cross-sectional book is GROSS-POSITIVE on both windows — IS gross monthly Sharpe +0.0925, OOS gross monthly Sharpe +0.1717 (the Critic FINAL `74054c2` verified this genuine, not a look-ahead artifact) — and fails NET only on fees; the NET book is IS monthly Sharpe −0.1960 / OOS monthly Sharpe −0.0985, a +0.4443 IS and +0.4433 OOS lift vs the /088 cross-sectional book.** Fees/gross collapsed from /088's 8.8× to IS 3.35× / OOS 1.58× — a 2.6× reduction attributable to the quintile + overlapping-hold + no-trade-band construction; the OOS gross PnL (+0.0567) needs only ~37% further fee reduction OR gross-signal lift to cross breakeven net. IS mean gross turnover/bar 0.1153 ≤ the 0.138 ceiling — the HARD turnover gate PASSES. OOS rank-IC +0.0279 > 0 — the signal transfers. The SHORT leg carries the entire gross spread (OOS short-leg gross +0.1200; long-leg gross −0.0633 — a real, pre-registerable signal-anatomy finding). Classified **CONSTRUCTION-PARTIAL** (brief Section 8.4; Critic FINAL `74054c2` OVERALL=MERGE methodology-only; the QR's Phase-8 call): 8.1 SUSPICIOUS does not fire; 8.2 CONSTRUCTION-FALSIFIED does not fire (F2 turnover-gate PASSES, F3 does not fire — OOS −0.0985 > /088's −0.5418); 8.3 CONSTRUCTION-VALIDATED-PROMISING does not fire (it requires OOS monthly Sharpe > 0; observed −0.0985 ≤ 0); 8.4 CONSTRUCTION-PARTIAL is the disjunctive-precedence match — the corrected construction works, contains turnover, transfers OOS, and materially improves the OOS book, but the thin gross signal keeps it sub-zero. **The cross-sectional trajectory — /088 OOS −0.5418 ARCHITECTURE-PARTIAL → /089 OOS −0.0985 CONSTRUCTION-PARTIAL, gross-positive — is v3's most sustained positive trajectory: across two consecutive iterations the architecture responds to fixes (the +0.44 OOS lift matched the IS-internal-walk-forward EDA prediction exactly), the signal transfers OOS, and the book has turned gross-positive; the gap to a net-positive book is now a concrete, bounded cost/signal problem.** NO-MERGE; an EXPLORATION carries no merge ingredient. **iter-v3/090 is the gross-signal-strengthening next build (NOT a fresh re-architecture, NOT a third round of turnover reduction — turnover reduction has steeply diminishing returns per Critic Rec #2; the OOS gross Sharpe +0.17 is the asset to grow): the scoped axis is the G4 cross-sectional-momentum feature expansion (multivariate-contribution-tested per the iter-v3/070 dead-path discipline), with the short-leg-asymmetry hypothesis (Critic Rec #3) a candidate /090/091 axis the QR weighs on IS-only EDA.** The `cross_sectional.py` module, `run_cross_sectional_v3.py` runner, `XS_UNIVERSE` constant, the pooled-ranking infrastructure, AND the /089 cost-aware construction (quintile + 3-bar overlapping holds + no-trade band + the 0.138 turnover ceiling) are all RETAINED and built upon. BASELINE_V3.md UNCHANGED — /059 stays canonical; an EXPLORATION cannot update the baseline. Detail: `diary-v3/iteration_v3-089.md`.

## Dead Ideas (populated as v3 iterations fail — pre-fix verdicts marked for re-evaluation)

> **Re-evaluation note**: Per Critic FINAL `cdd94a3` recommendation #1 from /058, pre-fix NEGATIVE/INERT verdicts in cycle 4 may not transfer to post-fix landscape. Per `feedback_v3_engineered_features_dont_stack.md`, single-seed EXPLORATION SAME-FAMILY stacking continues to be FORBIDDEN at any IC. Cycle 1 EXPLORATIONs should commission fresh EDA before blanket exclusions.

- **iter-v3/013 universe drop-MKR (PROMISING-MECHANICAL)** — falsified at iter-v3/018 multi-seed CONFIRMATION; drop-MKR architectural decision RETAINED.
- **iter-v3/014 ADX-25** (NEGATIVE-clean): largest negative OOS Δ at -1.83 pre-fix; ADX axis closed.
- **iter-v3/015 tbr_zscore_30 microstructure feature** (NEGATIVE-no-effect pre-fix): rank 14/14; **eligible for re-evaluation in cycle 1 per post-fix axis-rethink rule**.
- **iter-v3/016 XGBoost head-to-head** (NEGATIVE-clean pre-fix): worst OOS Δ at -2.53. NOT closed for all configs.
- **iter-v3/017 meta-labeling** (NEGATIVE-over-filter, PATH C pre-fix): M2 filters 42.7% per-candle but kept trades show no quality lift.
- **iter-v3/019/023/024/082/085 funding-rate feature axis — CLOSED across 5 EXPLORATION data points, across BOTH feature-construction families; re-evaluation eligibility DISCHARGED.** /019 (`funding_rate_zscore_30`, n_trials=10, PROMISING-INERT rank 14/14), /023 (same single z-score, n_trials=35, OOS collapsed), /024 (BTC `funding_rate_zscore_30` cross-asset variant, n_trials=35, OOS collapsed) — three single-z-score attempts, all pre-walk-forward-fix. **iter-v3/082** (cycle-3 EXPLORATION #1): a literature-grounded 4-member funding FEATURE FAMILY (`funding_sign_persist_9`, `funding_momentum_3`, `funding_accel_3`, `funding_price_divergence_6`; `V3_FEATURE_COLUMNS` 14→18) encoding crowding/momentum/shock/divergence — the genuinely-distinct family construction the single-z-score attempts never made. /082 reproduced the INERT verdict: the 4 funding features ranked **15/16/17/18 of 18** by importance (combined 9.90%, below the 5.56% uniform-parity baseline); IS Δ flat (-0.0118), OOS Δ +1.2081 — but the OOS lift is the documented INERT-feature Optuna-perturbation / 3-seed-lottery artifact (`feedback_v3_inert_features_at_higher_budget.md`), NOT funding signal — classified **SUSPICIOUS-OOS-DOMINANT** (Critic FINAL `d5670aa`). **iter-v3/085** (cycle-3 EXPLORATION #4): the 5th data point and the one that closes the axis across BOTH construction families. /085 fed funding NOT as a direct feature but as a `sign()` switch *inside* a Category-2 composed feature — `funding_regime_momentum_5d = regime_momentum_signed_5d × sign(funding_z_30)` (`V3_FEATURE_COLUMNS_TOP_N` 14→15) — the explicitly-different construction (funding conditions *what raw momentum means*, never a splittable column). The Phase 5.5 gate's funding-differentiation adjudication was legitimate — the construction IS structurally distinct from the closed Category-1 funding-as-direct-feature axis — but the RESULT reproduced the INERT-by-importance pattern (rank **13/14/15-of-15**, share ~3%, portfolio rank 15/15) AND the /085-vs-/084 OOS roster swap loaded the /076 trade-selection sub-channel (added-minus-removed mean-duration gap +1.578 > the +1.0 LOCKED falsifier) → classified **SUSPICIOUS** (Critic FINAL `50f32be`; trade-selection sub-channel). The +0.1418 IS lift was an Optuna-search-perturbation artifact of an unused 15th column, NOT funding signal. **All 5 funding-axis data points are INERT-by-importance** — across two structurally orthogonal constructions (direct-feature: /019/023/024/082; composed-sign-switch: /085) the v3 LightGBM declines to allocate ranked importance to funding-derived information on the BCH/LDO/TRX universe at v3's per-symbol scale + EXPLORATION budget. **The funding axis is CLOSED in ALL feature constructions on the funding rate.** Future "funding" axes require a fundamentally different VEHICLE — (a) a NEW crypto-native data feed entirely (open interest / perp-spot basis / liquidation cascades; no current fetcher — a real Phase-6 build), or (b) a non-tree model that can compose the interaction differently — NOT another feature construction (direct OR composed) on the same funding rate. The literal-name bans on `funding_rate_zscore_30` / `btc_funding_rate_zscore_30` stay intact; `funding_regime_momentum_5d` joins the runner ABSENT-assertion ban at the iter-v3/086 setup; per `feedback_v3_inert_features_at_higher_budget.md` an INERT feature must NOT be carried forward and must NOT be retested at a higher Optuna budget. Detail: `diary-v3/iteration_v3-082.md`, `diary-v3/iteration_v3-085.md`.
- **iter-v3/086 perp-spot BASIS feature family — INERT — and the 7-FEED STRUCTURAL VERDICT (cycle-3 EXPLORATION #5).** iter-v3/086 acquired a genuinely NEW crypto-native data feed — the perp-spot basis — via a real Phase-6 build: the `crypto-trade fetch-spot` CLI subcommand (`data.binance.vision` monthly spot-kline archives + `/api/v3/klines` REST fallback, with mandatory 2025-01 microsecond→millisecond timestamp normalisation), a track-isolated `basis_v3.py` feature module (the `cross_btc_v3.py` external-CSV-merge pattern), and the `data/spot/` cache (BCH 7037 / LDO 4358 / TRX 8642 rows, 100% IS spot-merge coverage). A 3-feature basis family (`basis_zscore_30`, `basis_momentum_3`, `basis_extreme_flag` — all on `basis.shift(1)`, a one-full-candle lag stricter than the funding convention) was appended to `V3_FEATURE_COLUMNS_TOP_N` (14→17). **The result reproduced the INERT-by-importance pattern**: the 3 basis features ranked **15/16/17 of 17** by portfolio-pooled last-IS-month importance (combined share 0.04265, a 2.15× gap below the weakest anchor `regime_momentum_signed_5d` at 0.04034) — the F1 INERT falsifier satisfied. Neither LOCKED SUSPICIOUS sub-channel fired — the Phase-8 OOS roster-diff (`analysis/iteration_v3-086/roster_diff_oos.py`) measured F2 added-minus-removed mean-duration gap +0.9630 ≤ +1.0 and F3 full-roster shift +0.3489 ≤ +1.0 — and the ratio gate (1.149) and OOS-DOMINANT sub-mode (IS Δ +0.063 ≥ 0) did not fire either; classified **INERT** (Critic FINAL `a3297e1` OVERALL=MERGE methodology-only; the QR's Phase-8 Section 8 call). The +0.6962 OOS lift (to +1.0284) was the documented `feedback_v3_inert_features_at_higher_budget.md` Optuna-search-perturbation artifact of 3 INERT columns (the exact /082 signature), NOT basis signal. **THE 7-FEED STRUCTURAL VERDICT: the perp-spot basis feed is the 7th non-OHLCV crypto-native feature family v3 has tried, and ALL 7 are INERT-by-importance** — funding rate (/019/023/024 direct z-scores, /082 the 4-channel direct family, /085 the Category-2 composed sign-switch; 5 data points across two construction families), microstructure (/015 `tbr_zscore_30`; 1), perp-spot basis (/086; 1). Across seven independent crypto-native data sources and multiple construction families, the v3 per-symbol depth-3-5 LightGBM trained on ~2700-5700 IS rows (LDO only 2741) declines to allocate ranked split capacity to crypto-native sentiment information. This is a **structural verdict on the v3 architecture**, not a per-feed result: **iter-v3/087+ MUST NOT be an 8th crypto-native feature family on the same architecture.** The remaining structural levers are Direction 2 (universe expansion / breadth — WHOLESALE, not a single-weak-symbol add) and Direction 3 (a multi-symbol-pooled model — the naive form was /085-EDA-falsified; a non-naive variant is untested). The 3 basis feature columns (`basis_zscore_30` / `basis_momentum_3` / `basis_extreme_flag`) are dropped at the iter-v3/087 setup and join the runner ABSENT-assertion ban (the `funding_regime_momentum_5d` pattern); per `feedback_v3_inert_features_at_higher_budget.md` they must NOT be carried to /092 and must NOT be retested at a higher Optuna budget. **The `fetch-spot` subcommand, `basis_v3.py`, and `data/spot/` cache are RETAINED as reusable infrastructure** — only the 3 feature columns are dropped. Detail: `diary-v3/iteration_v3-086.md`.
- **iter-v3/020 per-symbol PnL share cap (0.40)** (NEGATIVE-clean PATH C): concentration is lottery-REWARD source NOT lottery-RISK source. Per-symbol PnL share caps CLOSED-MECHANISM.
- **iter-v3/021 universe expansion +HBAR +AVAX** (NEGATIVE-clean): both drag; EDA correlation captured price diversity not signal diversity. CLOSED-symbols-cycle.
- **iter-v3/083 universe expansion 3→4 +FILUSDT** (NEGATIVE — cycle-3 EXPLORATION #2). `V3_MODELS` grew BCH/LDO/TRX → BCH/LDO/TRX/FILUSDT (denominator expansion, distinct from the CLOSED swap-by-replacement family). FILUSDT was selected on a genuine IS-edge + portfolio-aggregate screen (rank #1 of 4 deep-history liquid large-cap survivors — the methodology fix /021 and /069 lacked) — but the production result was an aggregate **IS monthly Sharpe collapse to +0.1738 (Δ −0.9156 vs /059)**, IS MaxDD blowout 30.97%→73.18%, CPCV `frac_positive_paths` 0.4667 < 0.55. Decomposition of the −101.56pp IS-PnL collapse: **FIL's own negative edge −32.45pp** (32%; FIL standalone IS net_pnl% −32.45, 36.7% WR — a genuine net detractor, FIL does NOT transfer) + **incumbent drift −69.11pp** (68%). The incumbent drift is **data-extent drift, not FIL-perturbation** — proven cleanly by /082 (the same 3 incumbents on the same fresh data, FIL absent) already showing the incumbent aggregate ~70pp below /059 (/082 incumbent Δ vs /059 = −71.74pp; /082-vs-/083 incumbent Δ = +2.63pp, essentially identical). The engineering report's "FIL reshaped the incumbents' Optuna landscape" claim is mechanistically false (the Critic traced the runner — `_build_v3_model` gives every per-symbol model byte-identical inputs regardless of `len(V3_MODELS)`; each symbol's Optuna study is fully independent). The IS-edge screen predicted −0.0382 aggregate IS Sharpe Δ; realized −0.9156 — a **~24× magnitude under-prediction**: the screen is a defensible relative-ranking tool but NOT an absolute-magnitude predictor — future universe-expansion screens must treat the aggregate-Δ as direction-only. **FILUSDT is a CLOSED universe-expansion candidate** (joining HBAR/AVAX at /021 and ADA at /078). Universe expansion as an axis remains structurally valid (the Fundamental Law breadth lever) but exposed a confounding ~70pp /059 anchor-staleness drift — the /083 closeout recommends iter-v3/084 be a fresh /059-config anchor re-run on current data. Detail: `diary-v3/iteration_v3-083.md`.
- **iter-v3/087 WHOLESALE universe expansion 3→6 +GALA+MANA+SAND — NEGATIVE — and DIRECTION 2 (universe expansion) is now CLOSED for the rest of cycle 3 at a 4-FAILURE TRACK RECORD (cycle-3 EXPLORATION #6).** iter-v3/087 ran the genuinely WHOLESALE breadth expansion the /083/086 closeouts mandated (not /083's single-weak-symbol add): `V3_MODELS` grew BCH/LDO/TRX → BCH/LDO/TRX/**GALAUSDT/MANAUSDT/SANDUSDT** in one step — denominator expansion, the Grinold-Kahn `IR = IC·√breadth` lever, each added symbol fully UNIVERSAL (one independent per-symbol LightGBM, the identical 14-feature stack, the identical `(2.0,1.0)`-ATR triple-barrier, the identical 7-gate risk stack; no per-symbol features/ATR/gates). The symbols were screened on the rigorous Bailey-López de Prado *Sharpe-ratio indifference-curve* framework — the EDA's T3 measured a +0.2371 screen aggregate-IS-Sharpe lift at the N=6 peak, leave-one-out all positive, on the strategy-PnL correlation ρ̄=+0.087 (NOT the price-return correlation /021/069 wrongly used) — the strongest pre-backtest case any cycle-3 EXPLORATION has had. **The production result was NEGATIVE: IS monthly Sharpe lifted +0.0883 (to +0.9208 vs the /084 EXPLORATION-MODE-REFERENCE +0.8325) but OOS monthly Sharpe COLLAPSED to −0.5027 (Δ −0.8349 vs /084's +0.3322), OOS MaxDD blew out to 76.42%, worst CPCV path −3.47 Sharpe.** All 3 new symbols lost in OOS: GALA +67.2% IS → −19.0% OOS, MANA +101.6% IS → −27.2% OOS (the largest single-symbol IS→OOS reversal in v3 history at this iteration), SAND −18.1% IS → −28.1% OOS (IS-negative too — the EDA's restricted-window screen mis-signed SAND, which screened +0.13 standalone Sharpe on an un-gated 1485-trade proxy roster but the production 7-gate+Optuna run selected only 41 IS trades netting −18.10%; the /083-lesson recurring — a per-symbol screen does not survive the gate stack). Classified **NEGATIVE** via the Section 8.2 general `OOS Δ < −0.20` clause (Critic FINAL `24a9dcc` OVERALL=MERGE methodology-only; result-read NEGATIVE; the QR's Phase-8 Section 8 call). SUSPICIOUS does NOT fire — every SUSPICIOUS sub-channel is an OOS-up-on-flat-IS divergence and /087 produced the OPPOSITE (`IS up, OOS crashes`) signature; the Phase-8 roster-diff (`analysis/iteration_v3-087/roster_diff_oos.py`) confirmed the /087 OOS roster is a near-strict SUPERSET of /084's (all 104 /084 trades survive, REMOVES=0, the 76 added are exactly the GALA+MANA+SAND OOS trades; incumbent BCH/LDO bit-identical, TRX off by 1 trade — the `feedback_v3_single_seed_frozen_baseline.md` pattern), so the F4 added-vs-removed sub-channel is a degenerate empty-set artifact and the economically meaningful sub-channel (d) — the added-symbol-minus-incumbent OOS mean-duration gap −0.2546 — does NOT fire (no holding-time regime-loading). The breadth math transferred in-sample (IS lifted +0.09) but the 3 new symbols' per-symbol models overfit IS and inverted OOS. **DIRECTION 2 — symbol-universe EXPANSION — is now CLOSED for the remainder of cycle 3 at a 4-FAILURE TRACK RECORD spanning every expansion topology: /021 (HBAR+AVAX, double add, NEGATIVE), /069 (ADA, single add), /083 (FILUSDT, single add, NEGATIVE — IS collapse), /087 (GALA+MANA+SAND, wholesale add, NEGATIVE — OOS collapse).** The breadth `√N` benefit transferred to production OOS in none of the four — universe expansion is exhausted as a v3 axis. GALAUSDT/MANAUSDT/SANDUSDT join HBAR/AVAX/ADA/FILUSDT as CLOSED universe-expansion candidates. The /088 setup MUST revert `V3_MODELS` 6→3 (BCH/LDO/TRX) and recompute `REQUIRED_GAP` 132→66 — the standard baseline-restore of a NEGATIVE/NO-MERGE axis; `PER_CELL_GAP` stays 22 (universe-count-invariant). NOTE the engineering-report config-diff doc error (Critic Rec #4): the report records `/084 PER_CELL_GAP = 11` and `/087 = 22 (= 132/6)` — both wrong; `PER_CELL_GAP = (timeout_candles+1) = 22` for a single-symbol cell with NO `n_symbols` factor — universe-count-invariant at 22 in both /084 and /087; the code (`run_baseline_v3.py:1600`) is correct, only the report prose is wrong. Future universe/per-symbol briefs must pre-register a SYMMETRIC `OOS Δ < −0.20 regardless of IS sign → NEGATIVE` named falsifier (Critic Rec #2 — /087's F1 was IS-collapse-specific and did not fire on the IS-up/OOS-down signature). Detail: `diary-v3/iteration_v3-087.md`.
- **Regime-conditional kill switch (primitive 9) — CLOSED, tested-twice-no-signal**: NEGATIVE-pre-fix at iter-v3/022 (TRX/2022-Q4 regime gate, NEGATIVE-clean PARTIALLY-EFFECTIVE — TRX/2022-10 cell SUCCESS at single-seed but TRX/2023-01 NOT cleared) and INERT-post-fix at iter-v3/074 (TRX-scoped regime gate, BTC drawdown_30d > 20% OR |vol_z_30d| > 1.5; the gate fired only 3 IS + 5 OOS TRX suppressions — too few to move headline metrics; IS Δ +0.013, OOS Δ +0.069, both inside noise bands). The iter-v3/074 post-fix re-evaluation cleanly discharged the `feedback_v3_walkforward_lookahead_bug.md` re-eval eligibility for this axis. The regime-conditional kill switch is CLOSED across two data points and must not be re-proposed (Critic FINAL `2371324` Recommendation #2). NOTE iter-v3/074 was a clean holding-time-ORTHOGONAL axis — it confirmed (per `feedback_v3_is_oos_regime_divergence.md`) that holding-time-orthogonal axes do not load the v3 IS/OOS regime factor.
- **iter-v3/026 vol_adj_autocorr stacked on regime_momentum** (NEGATIVE-SUSPICIOUS-OOS pre-fix): 27× IS/OOS daily ratio. Engineered features DON'T STACK at single-seed n_trials=35.
- **iter-v3/027 cross_asset_divergence_norm swap for vol_adj_autocorr** (NEGATIVE-SUSPICIOUS-OOS pre-fix): 3-iter monotonic IS degradation pattern REPLICATED.
- **iter-v3/029-/057 cycle-4 EXPLORATION verdicts** — all produced under BUGGY walk-forward; eligible for re-evaluation as part of post-fix axis-rethink rule.
- **WorldQuant Alpha#32 (`alpha032`) formulaic-alpha feature — NEGATIVE — IS-collapse (cycle-5 EXPLORATION slot #2, iter-v3/102).** A v3-portable per-symbol port of Kakushadze (2015, arXiv 1601.00991) WorldQuant Alpha#32 — `scale_ts((sum(close,7)/7 − close), 100) + 20·scale_ts(corr(vwap, delay(close,5), 230), 100)`, a fast 7-bar SMA-gap mean-reversion term plus a slow 230-bar vwap/lagged-close lead-lag correlation term (`vwap = quote_volume/volume`, `scale_ts` a causal trailing-100-bar mean-abs normalization) — added as the 15th `V3_FEATURE_COLUMNS` feature via a new track-isolated `formulaic_v3.py` module. `alpha032` was the **basket horse-race winner**: the /102 EDA screened 18 v3-portable time-series-pure WorldQuant-101 alphas and `alpha032` ranked **1/18** on the multivariate held-out-tail directional-accuracy lift (EDA T6 `dShACC` +0.01373) and 1/18 on the composite selection score (T7 1.897). **But its held-out-tail proxy 95% CI straddled zero** — T6 CI [−0.0085, +0.0399]; the brief honestly recorded that no alpha in the basket cleared a CI lower bound > 0. At the 3-seed multi-seed Optuna backtest the feature **collapsed the in-sample fit**: IS monthly Sharpe fell to **+0.3993** (Δ −0.43 vs the /060 EXPLORATION-mode anchor +0.8325; the pre-registered Falsifier F2 fires, < +0.60 floor), IS profit_factor only 1.1558, BCH IS net_pnl_pct down to +70.05 at a 45.1% win rate — even though the tree genuinely **LEARNED** `alpha032` (BCH last-IS-month importance rank **8/15**, mid-table — NOT the rank-15/15 INERT pattern; F4 does not fire). The OOS headline spiked to +1.5458 — **but the Phase-7.5 Critic Check 1 traced `compute_alpha032` line-by-line and confirmed it is strictly past-only** (`min_periods == window` on every rolling op, no centered/full-series operation, no `bfill`; `test_hard_causality` max_abs_diff = 0.0; EDA T3 adversarial audit 0/54 cells fail) — so the OOS spike is **NOT a look-ahead leak**; it is the iter-v3/026/027/030/034/036/037 IS-collapse / OOS-spike overfitting signature (an OOS spike sitting on a collapsed IS fit). Classified **EXPLORATION-NEGATIVE** (brief Section 8 LOCKED disjunctive taxonomy step 2 — F2 fires; first match wins; Critic OVERALL=MERGE methodology-only — the result-read is the QR's Phase-8 call). **The generalizable lesson: a held-out-tail single-classifier accuracy proxy does NOT predict the multi-seed Optuna IS fit.** A basket horse-race that selects on held-out single-classifier accuracy (the /098 methodology) ranked `alpha032` 1/18 and predicted a BCH IS *gain* (T8 d_acc +0.0591); the production multi-seed Optuna backtest, with a 15th feature widening the search space, *collapsed* BCH IS — the proxy is one classifier's held-out accuracy, the production number is a multi-seed Optuna ensemble's walk-forward Sharpe, and a positive point estimate on a CI-straddling-zero proxy is not a GO signal. A LEARNED feature that collapses IS is worse than an INERT one — the `feedback_v3_inert_features_at_higher_budget.md` over-allocation-of-noise mechanism, sharpened. `alpha032` must be **REVERTED** (NEGATIVE, not carried forward): `V3_FEATURE_COLUMNS_TOP_N` reverts to the 14-feature /059 stack, `_verify_feature_columns` count 15→14, the `add_formulaic_v3_features` GROUP_REGISTRY entry removed from the active build; `"alpha032"` joins the runner ABSENT-assertion ban (the `funding_regime_momentum_5d`/basis-feature pattern) and must NOT be retested at a higher Optuna budget per `feedback_v3_inert_features_at_higher_budget.md`. The `formulaic_v3.py` module + its unit tests are RETAINED as reusable infrastructure (the /086 `fetch-spot`/`basis_v3.py` precedent) — only the feature column is dropped. The WorldQuant-101 formulaic-alpha axis closes at one data point; feature-addition is now an attacked-and-repeatedly-closed v3 frame (the 7-FEED INERT verdict, /098 NO-GO, /102 NEGATIVE-by-IS-collapse) — the binding constraint remains the thin per-symbol 8h triple-barrier signal. Detail: `diary-v3/iteration_v3-102.md`.
- **Trend-scanning label (`label_mode="trend_scanning"`, López de Prado MLAM §5.4) — NEGATIVE — IS-collapse (cycle-5 EXPLORATION slot #5, iter-v3/105).** The v3 single fixed 21-candle triple-barrier *training label* was replaced with a **trend-scanning label**: per bar, fit an OLS linear trend over the horizon grid `{5,8,13,21}`, select the horizon with the largest |t-statistic|, label the bar by the sign of that slope — a per-bar **data-selected** prediction horizon. ONE clean variable: the training `label_mode` `triple_barrier` → `trend_scanning`; everything else bit-identical to /059 (the 14 `V3_FEATURE_COLUMNS`, the BCH/LDO/TRX universe, the triple-barrier TP/SL EXITS at ATR 2.0/1.0 + 21-candle timeout, the 7-gate RiskV2 stack, `REQUIRED_GAP=66`, embargo 22 — the grid was capped at the incumbent 21-candle horizon so the embargo over-covers the trend-scanning forward window with zero new leakage surface). The first v3 axis to attack the **estimand** (what the model predicts). The IS-only gating EDA (3 committed scripts `analysis/iteration_v3-105/`, EDA SHA `857e176`) returned a Phase-1 **GO**: the 14-feature stack predicts the trend-scanning label **+52% better** (gating grid) / **+31% better** under the longest-wins-confound-removed capped grid `{5,8,13,21}` (mean |IC| 0.03977 vs 0.03034, Wilcoxon signed-rank p=0.013 over 42 cells, robustness-checked against the mechanical longest-wins confound), and the label is *more* temporally stable than the incumbent. **At the 3-seed multi-seed Optuna backtest the label COLLAPSED the in-sample fit**: IS monthly Sharpe **+0.2201** (Δ −0.61 below the /060 EXPLORATION-mode anchor +0.8325 — the pre-registered Falsifier F2 fires, < +0.60 floor; F5 IS/OOS daily-Sharpe ratio 5.06 also fires). The OOS headline spiked to +1.1897 — **but the Phase-7.5 Critic Check 1 traced `_trend_scan_label` line-by-line and conclusively ruled out look-ahead** (the forward window never exceeds `max(grid)=21` candles, the embargo `compute_embargo_candles(10080,480)=22` over-covers it by 1 candle, the QE `test_trend_scanning_hard_causality` is genuine and exercises h=21 at the boundary) — so the OOS spike is **NOT a leak**; it is the iter-v3/026/027/030/034/036/037/102 IS-collapse / OOS-spike overfitting signature (an OOS spike sitting on a collapsed IS fit). Classified **EXPLORATION-NEGATIVE** (brief Section 8 LOCKED disjunctive taxonomy step 2 — F2 fires; first match wins; Critic OVERALL=MERGE methodology-only — the result-read is the QR's Phase-8 call). **THE KEY META-FINDING: iter-v3/105 FALSIFIES the /104 hypothesis that "the label geometry / the estimand is the binding constraint."** The trend-scanning label was the literal test of that hypothesis; the EDA confirmed the re-framing is real and substantive (+31-52% feature→label IC, robustness-checked) — and if the label were the constraint, a label the features predict 31-52% better should have *lifted* the IS fit. It did the opposite: the IS fit collapsed −0.61. The binding constraint is **DOWNSTREAM of the training label** — in the **trade-construction / exit / risk layer**: the mechanism is a *mismatch* — the model was trained on a trend-scanning label (per-bar data-selected-horizon trend sign) but trades were executed through the unchanged triple-barrier execution geometry (fixed ±ATR barrier, fixed 21-candle timeout). **The generalizable lesson: a label with a measurably better feature→label IC (the EDA's +31-52% lift, robustness-checked) nonetheless COLLAPSED the multi-seed Optuna IS fit — a better feature→label IC does NOT predict trade-Sharpe when the training label and the triple-barrier execution geometry are mismatched.** Feature→label IC is a property of the labeling problem; it is not a sufficient statistic for the trade-construction problem when label geometry ≠ execution geometry. This generalizes the /102 lesson ("a held-out-tail accuracy proxy does not predict the multi-seed Optuna IS fit") to the label-geometry axis. `label_mode` must **REVERT to `triple_barrier`** at the iter-v3/106 setup (the standard NEGATIVE-axis baseline-restore — a two-line runner change). The `_trend_scan_label` function, the `trend_scanning` branch in `label_trades`, the `trend_scan_grid` parameter plumbed through `LightGbmStrategy` / `MetaLabelingStrategy`, and the unit/integration tests are **RETAINED as zero-revert-cost dead-code infrastructure** (the `fixed_horizon` / `formulaic_v3.py` / `basis_v3.py` / `fetch-spot` retained-infrastructure precedent) — only the `label_mode` value reverts; the trend-scanning label must NOT be re-proposed. With /105, every TRAINING-SIDE lever is comprehensively closed — features (the 7-FEED INERT verdict, /098, /102, /103), the external-data layer (/104), model architecture (/016/093/096/100), the training objective (/101), the label class (/099), the universe (/097), and now the label geometry (/105); the productive frontier is the trade-construction / exit / risk layer (the iter-v3/106 mandate). Detail: `diary-v3/iteration_v3-105.md`.
- **Kaufman path-efficiency feature family (`efficiency_ratio_50` / `range_efficiency_50`) — CLOSED across 2 data points; re-evaluation eligibility DISCHARGED.** `range_efficiency_50` is mathematically identical to `efficiency_ratio_50` (same Kaufman formula `|close[t]-close[t-50]| / sum(|close.diff()|, 50)`, same 50-bar window, same `1e-9` epsilon, clip, shift, fillna). The literal `efficiency_ratio_50` name remains runner-banned (`raise RuntimeError`) + a prohibited test entry; the column is never generated. Two data points: **iter-v3/043** added `efficiency_ratio_50` to a 4-symbol universe pre-walk-forward-fix → DISASTROUS (IS -0.8445 / OOS -0.8990); **iter-v3/076** re-added the bit-identical math as `range_efficiency_50` (15th `V3_FEATURE_COLUMNS` feature, 3-symbol BCH/LDO/TRX universe, post-walk-forward-fix) as a rule-sanctioned re-evaluation per `feedback_v3_walkforward_lookahead_bug.md` → SUSPICIOUS-OOS-DOMINANT (IS collapsed to +0.0431, MaxDD blew out to 61.69%, OOS/IS ratio 15.04 — the clearest SUSPICIOUS signature in cycle 2; BCH IS edge destroyed +79.45%→-8.27%). The /076 re-evaluation did NOT vindicate the feature — IS is still economically broken. The `feedback_v3_walkforward_lookahead_bug.md` re-evaluation eligibility for this axis is **DISCHARGED**. `efficiency_ratio_50` / `range_efficiency_50` must NOT be re-proposed (Critic FINAL `8203410` Recommendation #3). NOTE: /076's failure mode (regime-loading via trade SELECTION — the feature shifted which trades the model picks toward longer-held OOS-uptrend trades, with NO barrier-extension mechanism) is a material extension of the v3 IS/OOS regime-divergence finding — see `briefs-v3/exploration_catalog.md` cycle-level-finding section and `feedback_v3_is_oos_regime_divergence.md`.

## Measurement Discipline

(Inherited from `BASELINE_V2.md`.)

### EXPLORATION-Mode Anchor Staleness (established iter-v3/077 closeout)

> This note concerns ONLY the EXPLORATION-MODE-REFERENCE intra-cycle delta anchor. The **/059 CONFIRMATION baseline above (the canonical anchor, tag `v0.v3-059`, IS +1.0894 / OOS +0.5791) is UNCHANGED and unaffected** — it is a unified-architecture CONFIRMATION number, not an EXPLORATION-mode reference.

Cycle-2 EXPLORATIONs /071-/076 quoted the iter-v3/060 EXPLORATION-MODE-REFERENCE as a frozen IS +0.8325 / OOS +0.1403. iter-v3/077 — the first iteration since /060 to run the exact /060 14-feature config with no axis — established that the frozen value no longer reproduces on current code + current data. The **current-code /060-config baseline is IS +0.8236 / OOS +0.2078**, with the exact additive decomposition: IS code-drift **-0.0089** (the iter-v3/061 TRX `vol_scale_floor=0.5`, a permanent deterministic offset) + OOS data-extent **+0.0675** (the 2026-05 OOS month post-dating /060's data fetch, growing monotonically with calendar time). Cycle-2 EXPLORATIONs **/078+ re-anchor against the current-code /060-config baseline (IS +0.8236 / OOS +0.2078)**, not the stale frozen value. This is a methodology correction (a stale reference value replaced by the freshest reproducible no-axis run of the canonical config), not a measurement-window change — the OOS-cutoff and start dates are untouched. The /071-/076 classifications remain robust (the drift is well inside the ±0.10 IS / ±0.20 OOS noise bands and cannot move a SUSPICIOUS ratio across the 3.0 gate). Detail: `diary-v3/iteration_v3-077.md` Section 4 and the anchor-staleness cycle-note in `briefs-v3/exploration_catalog.md`.

**Cycle-3 EXPLORATION-MODE-REFERENCE (established iter-v3/084 closeout — REFERENCE-REANCHOR).** iter-v3/084 — the cycle-3 reference re-run, the direct analogue of cycle 1's /077 — ran the canonical 3-symbol BCH/LDO/TRX / 14-feature / `(2.0,1.0)`-ATR / 7-gate /059 configuration at EXPLORATION-mode 3-seed on freshly-fetched current data, with no axis. It landed at **IS +0.8325 / OOS +0.3322**. The brief Section 4.3 LOCKED re-anchor decision rule fired the re-anchor branch (both deltas vs /059's recorded numbers ≈ −0.25, outside the ±0.10 band). **The cycle-3 EXPLORATION-MODE-REFERENCE is therefore IS +0.8325 / OOS +0.3322 (3-seed EXPLORATION-mode, current data)** — cycle-3 EXPLORATIONs /085-091 anchor their intra-cycle Δ classification against it.

> **The EXPLORATION-vs-CONFIRMATION architecture-gap finding (iter-v3/084 Critic FINAL `16b4cc1` Recommendation #1).** The ~0.26 IS gap between /084's IS +0.8325 and /059's recorded IS +1.0894 is **NOT "the /059 config went stale."** /084's +0.8325 is a **3-seed EXPLORATION-mode** number (`EXPLORATION_ENSEMBLE_SIZE=3`); /059's +1.0894 is a **10-seed CONFIRMATION-mode** number (the unified `ENSEMBLE_SIZE=10`) — two structurally different ensemble architectures (different proba-averaged trade rosters, different Sharpe). Cycle 2's reference re-run /077 landed at IS **+0.8236** (3-seed) — near-identical to /084's +0.8325 — while /059's 10-seed CONFIRMATION is +1.0894. The /077≈/084 near-identity proves essentially the entire ~0.26 IS gap is the **EXPLORATION-vs-CONFIRMATION architecture component** (3-seed-vs-10-seed proba-averaging), with only a small residual genuine data-extent component. The honest finding: the prior cycle-3 EXPLORATIONs /082-/083 were anchoring their 3-seed EXPLORATION-mode deltas against a non-comparable 10-seed CONFIRMATION number — /084 corrects that **reference-architecture mismatch**. The /082 (SUSPICIOUS-OOS-DOMINANT) and /083 (NEGATIVE) classifications STAND — re-anchoring sharpens the framing but does not overturn closed verdicts.

> **The TWO-ANCHOR STRUCTURE (iter-v3/084 Critic Recommendation #2 — MANDATORY for every /085-091 brief).** Every cycle-3 EXPLORATION brief Section 4 must state TWO anchors: **(1) the EXPLORATION-MODE-REFERENCE** — /084: IS +0.8325 / OOS +0.3322 (3-seed) — for intra-cycle Δ classification (an EXPLORATION runs 3-seed, so it must be compared to a 3-seed reference); **(2) the /059 CONFIRMATION baseline** — IS +1.0894 / OOS +0.5791 (10-seed, tag `v0.v3-059`) — RESERVED for the iter-v3/092 CONFIRMATION (which runs 10-seed CONFIRMATION-mode and must be compared to a 10-seed baseline). Mixing the two is the /082-/083 error.

This is a methodology correction (a non-comparable reference value replaced by the freshest architecturally-matched no-axis run of the canonical config), not a measurement-window change — the OOS-cutoff and start dates are untouched. The /082 / /083 classifications remain robust. The /059 CONFIRMATION baseline (the canonical anchor, tag `v0.v3-059`, IS +1.0894 / OOS +0.5791) is UNCHANGED and unaffected — it is a unified-architecture CONFIRMATION number, not an EXPLORATION-mode reference. Detail: `diary-v3/iteration_v3-084.md` Sections 3-5 and the iter-v3/084 catalog row in `briefs-v3/exploration_catalog.md`.

### Data Extent Rule

Every kline CSV in `data/<SYMBOL>/8h.csv` must have `close_time` within 16 hours of the measurement time. Stale data silently corrupts feature computations and label horizons. The Engineer's Phase 6 pre-flight check verifies this before running any backtest.

### Forming-Candle Filter

`fetcher.py` filter: `if k.close_time < now_ms` drops forming candles. iter-v2/059 lost 50 days of OOS data due to a forming-candle bug; the v3 fix is non-negotiable.

### Re-Fetch Protocol

If freshness check fails, the Engineer re-fetches the affected symbols via:

```bash
uv run crypto-trade fetch --interval 8h --symbols <comma-separated>
```

The re-fetch is documented in the engineering report, and the backtest is re-run from the fresh data.

### Reproducibility

Every iteration's engineering report stamps:
- Git commit SHA (must exist in `git rev-parse <SHA>`)
- Hardware (CPU model, RAM)
- Wall-clock time
- Library versions (per brief Section 9)

The Critic's Check 7 verifies reproducibility properties — explicit `feature_columns`, ensemble seeds literal, trade-row PnL spot-check.

## Status

**CONFIRMATION-MERGE — CYCLE-6 CLOSURE EVENT** per Critic FINAL `6d1017d` 2026-05-20. iter-v3/121-METHODOLOGY (CYCLE-7 BOOTSTRAP / METHODOLOGY) is the new canonical baseline — the first BASELINE_V3.md update since 2026-05-13 (/059 RE-ANCHOR #2 stood unchanged for 7 days across 62 iterations from /060 through /120). **/116 no_confirm RULE-layer primitive is merged as the first cycle-6 strictly-accretive ingredient.** Tag `v0.v3-121` supersedes `v0.v3-059` as canonical; /059 retained in baseline-history as PRIOR CANONICAL (2026-05-13 to 2026-05-20).

### Cycle Counting (cycle 6 CLOSED at iter-v3/121-METHODOLOGY; cycle 7 starts at /122)

Per user directives + Critic FINAL `6d1017d` 2026-05-20:

- **iter-v3/059 = RE-ANCHOR #2** (unified 10-seed; canonical 2026-05-13 to 2026-05-20; PRIOR CANONICAL)
- **iter-v3/060-069 = CYCLE 1 EXPLORATIONs** (COMPLETE — no BASELINE update)
- **iter-v3/070 = CYCLE 1 CONFIRMATION** (NO-MERGE — SUSPICIOUS-OOS-DOMINANT)
- **iter-v3/071-080 = CYCLE 2 EXPLORATIONs** (COMPLETE — no BASELINE update)
- **iter-v3/081 = CYCLE 2 CONFIRMATION** (RE-VALIDATION — no edge to bundle; /059 confirmed; no tag)
- **iter-v3/082-091 = CYCLE 3 EXPLORATIONs** (cross-sectional re-architecture line, /088 first OOS signal transfer)
- **iter-v3/092 = CYCLE 3 CONFIRMATION** (no BASELINE update)
- **iter-v3/093-099 = CYCLE 4 EXPLORATIONs** (RETIRED — model-class axes saturated)
- **iter-v3/100-104 = CYCLE 5 EXPLORATIONs** (the /105→/109 cycle-5 binding-constraint chain)
- **iter-v3/105-109 = CYCLE 5 closure** (the structural finding: binding constraint is DOWNSTREAM of label)
- **iter-v3/110-119 = CYCLE 6 EXPLORATIONs** (COMPLETE — 2 PROMISING-MECHANICAL, 8 NEGATIVE)
- **iter-v3/120 = CYCLE 6 CONFIRMATION** (NO-MERGE — TWO-COMPONENT bundle; all-time OOS record +1.6946 NOT merged per F3+F4)
- **iter-v3/121-METHODOLOGY = CYCLE-7 BOOTSTRAP / cycle-6 closure event** (MERGE — /116 no_confirm standalone; BASELINE UPDATES /059 → /121)
- **NEXT iteration = iter-v3/122 = CYCLE 7 EXPLORATION #1 of 10**
- **Cycle 7 CONFIRMATION = iter-v3/132** (or later per cadence discipline)
- **EXPLORATION cap 2h** (unchanged)
- **CONFIRMATION cap 6h** (iter-v3/121 ran 3.21h within cap)

Per `feedback_v3_strict_10_to_1_cadence.md`: STRICT 10:1 EXPLORATION:CONFIRMATION cadence. iter-v3/121-METHODOLOGY BOOTSTRAP is NOT counted toward the cycle-7 10/10 cadence (analogous to iter-v3/018 BOOTSTRAP precedent).

### Cycle 6 Outcome (iter-v3/110-121 — CLOSED)

**Cycle 6 produced 1 BASELINE_V3.md update at /121.** Net outcome: **1 ingredient merged (/116 no_confirm RULE-layer exit primitive) + 1 ingredient dropped (/119 C6 FEATURE-layer composed feature) + 0 new direct-edge signals**.

EXPLORATION distribution (10/10):
- **2 PROMISING (both mechanical)**: /116 PROMISING-MECHANICAL (RULE form — MERGED at /121); /119 PROMISING-FEATURE-MECHANICAL (FEATURE form — DROPPED per /120 F3 + /121 attribution)
- **8 NEGATIVE/UNRESOLVED**: /110 (UNRESOLVED label-confound) → /111 (clean NEGATIVE), /112 (pooled), /113 (multi-freq), /114 (risk mgmt — Check-1+Check-8 FAIL), /115 (coherent horizon-exit labeling), /117 (24h-multi-offset catastrophic), /118 (Category-2 vol-regime composed-feature catastrophic)

CONFIRMATION outcomes:
- **iter-v3/120 CONFIRMATION-NO-MERGE** — TWO-COMPONENT bundle achieved all-time v3 OOS record (+1.6946) but F3 (sister-redistribution conjunctive AND gate) FIRED + F4 (IS regime-cost floor +0.79) FAILED by 0.0607; Component B DROPPED per F3
- **iter-v3/121-METHODOLOGY CONFIRMATION-MERGE** — Component A standalone at 10-seed produced IS +1.3108 / OOS +0.9682 (Δ vs /059: IS +0.2214, OOS +0.3891); 4/5 binding falsifiers PASS, F4 trade-rate INFORMATIONAL

The cycle-6 axis-menu hypothesis (user directive 2026-05-19, `project_v3_cycle6_axis_menu.md`) is now fully adjudicated:
- **FALSIFIED on the new-direct-edge axis**: 0 of 10 EXPLORATIONs produced direct-edge PROMISING
- **PARTIALLY VALIDATED on the strictly-accretive mechanical-primitive axis**: 2 mechanical PROMISING outcomes (1 merged, 1 dropped)

The /105→/109 cycle-5 structural finding (binding constraint is DOWNSTREAM of training label, in the trade-construction / exit / risk layer) is reconfirmed by cycle-6's empirical outcome: the only PROMISING outcomes were at the downstream layer; the 8 upstream/horizontal axes all NEGATIVE.

Detail in `diary-v3/iteration_v3-121.md` (this cycle's closure diary) + `diary-v3/iteration_v3-120.md` (TWO-COMPONENT CONFIRMATION) + `diary-v3/iteration_v3-119.md` (cycle-6 EXPLORATION slot #10).

### Cycle 7 Axis Priorities (post-iter-v3/121 BASELINE update)

Anchor: **/121 (IS +1.3108 / OOS +0.9682), NOT /059**. All cycle-7 EXPLORATIONs anchor against the new baseline.

Candidate axis menu (from /119 diary §8.2 + `project_v3_cycle7_setup.md`):

1. **HIGH — Cross-asset/external feeds** — STRUCTURALLY DIFFERENT primitives than the 7 prior INERT-by-importance crypto-native feeds (funding /019/023/024/082/085, microstructure /015, basis /086). Candidates: Glassnode on-chain BTC metrics (Exchange Whale Ratio, MVRV-Z, NUPL/SOPR), perp-spot basis from a different venue, liquidations data, DeFi TVL/lending utilization.

2. **HIGH — Non-LightGBM model classes** not yet tested at /109 depth. /109 terminal 8h null was scoped to LightGBM depth-3-5; alternative inductive biases (LSTM, Transformer, deeper neural networks, CatBoost, XGBoost-v2 with regime distinct from /016) unexplored.

3. **MEDIUM — Longer-cadence labels** — 1-week or 1-month horizon labels with appropriate execution geometry. /072→/105→/115 closed at 21-candle horizon.

4. **MEDIUM — NEW symbol universe NOT-already-tested variants** per `feedback_v3_concentration_is_signal.md`. Extreme-out-of-distribution candidates selected via STRUCTURALLY DIFFERENT screening than prior universe-expansion failures.

**FORBIDDEN cycle-7 axes**:

- Re-walking cycle-6 NEGATIVE axes without genuinely-new structural delta
- Knob-tuning saturated axes (ADX, z-score, BTC-band)
- Per-symbol customizations (closed at /039 NEGATIVE)
- Component B-style FEATURE-layer cannibalizer features (per F3 binding-gate validation at /120 + /121 attribution finding — cycle-7 engineered-feature axes must be /025-class direct-edge candidates, not /119-class FEATURE-MECHANICAL cannibalizers)

iter-v3/122 = cycle-7 EXPLORATION axis-1; QR-led per `feedback_v3_axis_selection_quant_discipline.md`.

## Relationship to v1 and v2

`BASELINE_V3` is independent of `BASELINE.md` (v1) and `BASELINE_V2.md` (v2). The three tracks evolve independently:

- v1 tagged at `v0.NNN` after MERGE
- v2 tagged at `v0.v2-NNN` after MERGE
- v3 tagged at `v0.v3-NNN` after MERGE (`v0.v3-018` BOOTSTRAP [RETIRED], `v0.v3-028` first CONFIRMATION-MERGE post-bootstrap [BIASED, RETIRED], `v0.v3-058` first RE-ANCHOR post-walk-forward-fix [RETIRED], `v0.v3-059` RE-ANCHOR #2 unified 10-seed [PRIOR CANONICAL, retired 2026-05-20], **`v0.v3-121` cycle-6 closure event with /116 no_confirm merged as first cycle-6 strictly-accretive ingredient — CURRENT CANONICAL**)

The combined-portfolio runner (future work) weights all three tracks. v3 is expected to contribute to combined-portfolio diversification because its symbol universe excludes v1+v2 traded symbols by hard rule. The unified 10-seed architecture makes v3 directly deployable in live: one model per coin per account.

## See Also

- `ITERATION_PLAN_8H_V3.md` — workflow doc for v3
- `.claude/commands/quant-iteration-v3.md` — the v3 skill
- `.claude/agents/quant-engineer.md` — Engineer subagent
- `.claude/agents/quant-critic.md` — Critic subagent (enhanced at `414368a`)
- `BASELINE_V2.md` — sibling baseline (v2)
- `BASELINE.md` — sibling baseline (v1)
- `briefs-v3/exploration_catalog.md` — running EXPLORATION ledger (cadence anchor)
- `briefs-v3/iteration_v3-121/research_brief.md`, `engineering_report.md`, `review.md` — current canonical artifacts (CONFIRMATION-MERGE; first cycle-6 merge)
- `diary-v3/iteration_v3-121.md` — current canonical diary entry (cycle-6 closure)
- `briefs-v3/iteration_v3-120/` — TWO-COMPONENT bundle (CONFIRMATION-NO-MERGE; all-time OOS record +1.6946 not merged)
- `briefs-v3/iteration_v3-119/` — PROMISING-FEATURE-MECHANICAL precedent (Component B; DROPPED at /121)
- `briefs-v3/iteration_v3-116/` — PROMISING-MECHANICAL precedent (Component A; MERGED at /121)
- `briefs-v3/iteration_v3-059/` — PRIOR CANONICAL artifacts (RE-ANCHOR #2; retired 2026-05-20)
- `briefs-v3/iteration_v3-058/` — prior RE-ANCHOR #1 artifacts (RETIRED)
- `briefs-v3/iteration_v3-028/` — prior CONFIRMATION-MERGE artifacts (BIASED, RETIRED)
- `briefs-v3/iteration_v3-018/` — prior BOOTSTRAP artifacts (BIASED, RETIRED)
